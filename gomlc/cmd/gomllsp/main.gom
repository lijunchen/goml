package main;

use module::lsp;
use std::io;
use std::process;

fn main() -> unit {
    match lsp::run_server() {
        Ok(_) => (),
        Err(message) => {
            io::eprintln(message);
            process::exit(1)
        },
    }
}
