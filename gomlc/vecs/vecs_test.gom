package vecs;

use std::testing;

#[test]
fn checks_membership_with_element_equality() -> unit {
    testing::assert(contains(Vec::["goml", "gomlc"], "gomlc"));
    testing::assert(!contains(Vec::["goml"], "rust"))
}
