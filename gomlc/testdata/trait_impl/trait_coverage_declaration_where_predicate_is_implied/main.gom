trait Ready {
    fn ready(self: Self) -> ();
}

trait Service[T] where T: Ready {
    fn serve(self: Self, arg1: T) -> ();
}

fn require_ready[T: Ready](value: T) -> () { () }

fn consume[T, S: Service[T]](value: T) -> () {
    require_ready(value)
}

fn main() -> () { () }
