trait Mark {
    fn mark(self: Self) -> ();
}

trait Source {
    type Item: Mark;
    fn get(self: Self) -> Self::Item;
}

fn require_mark[T: Mark](value: T) -> () { () }

fn consume[S: Source](source: S) -> () {
    require_mark(Source::get(source))
}

fn main() -> () { () }
