trait Source {
    type Item;
    fn get(self: Self) -> Self::Item;
}

struct Value {}

impl Source for Value {
    type Item = i32;
    type Item = i32;
    fn get(self: Value) -> i32 { 1 }
}

fn main() -> () { () }
