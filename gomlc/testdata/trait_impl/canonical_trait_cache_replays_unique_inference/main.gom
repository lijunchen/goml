trait Mark {
    fn mark(self: Self) -> ();
}

impl Mark for Vec[i32] {
    fn mark(self: Vec[i32]) -> () { () }
}

fn require_mark[T: Mark](value: T) -> () {
    ()
}

fn main() -> () {
    let first = vec_new();
    require_mark(first);
    let second = vec_new();
    require_mark(second)
}
