enum Maybe[T] {
    Just(T),
    Nothing,
}

trait Display {
    fn show(self: Self) -> string;
    fn debug(self: Self) -> string;
}

impl Display for Maybe[i32] {
    fn show(self: Maybe[i32]) -> string { "value" }
}

fn main() -> unit { () }
