trait Mark {
    fn mark(self: Self) -> unit;
}

impl Mark for Vec[i32] {
    fn mark(self: Vec[i32]) -> unit { () }
}

fn require_mark[T: Mark](value: T) -> unit {
    ()
}

fn main() -> unit {
    let values = vec_new();
    require_mark(values)
}
