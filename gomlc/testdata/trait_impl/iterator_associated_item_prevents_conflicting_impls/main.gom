struct Values {}

impl Iterator for Values {
    type Item = i32;
    fn next(self: Values) -> Option[i32] { Option::None }
}

impl Iterator for Values {
    type Item = string;
    fn next(self: Values) -> Option[string] { Option::None }
}

fn main() -> () { () }
