trait Mark {
    fn mark(self: Self) -> ();
}

impl Mark for i32 {
    fn mark(self: i32) -> () { () }
}

impl Mark for string {
    fn mark(self: string) -> () { () }
}

fn require_mark[T: Mark](value: T) -> () {
    ()
}

fn main() -> () {
    let consume = |value| require_mark(value);
    ()
}
