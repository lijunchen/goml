trait Render {
    fn render(self: Self) -> string;
}

struct Item {}

impl Render for Item {}

fn require_render[T: Render](value: T) -> () {
    ()
}

fn use_item(value: Item) -> () {
    require_render(value)
}
