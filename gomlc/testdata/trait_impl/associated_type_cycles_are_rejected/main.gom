trait Pair {
    type First;
    type Second;
    fn touch(self: Self) -> ();
}

struct Value {}

impl Pair for Value {
    type First = Self::Second;
    type Second = Self::First;
    fn touch(self: Value) -> () { () }
}
