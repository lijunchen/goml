struct Values {}

impl Iterator for Values {
    type Item = int32;
    fn next(self: Values) -> Option[int32] { Option::None }
}

fn consume_int(value: int32) -> unit { () }

fn main() -> unit {
    for value in (Values {}) {
        consume_int(value);
    };
}
