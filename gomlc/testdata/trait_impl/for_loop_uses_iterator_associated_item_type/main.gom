struct Values {}

impl Iterator for Values {
    type Item = i32;
    fn next(self: Values) -> Option[i32] { Option::None }
}

fn consume_int(value: i32) -> () { () }

fn main() -> () {
    for value in (Values {}) {
        consume_int(value);
    };
}
