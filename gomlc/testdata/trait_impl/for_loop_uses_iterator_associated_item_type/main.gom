struct Values {}

impl Iterator for Values {
    type Item = i32;
    fn next(self: Values) -> Option[i32] { Option::None }
}

fn consume_int(value: i32) -> unit { () }

fn main() -> unit {
    for value in (Values {}) {
        consume_int(value);
    };
}
