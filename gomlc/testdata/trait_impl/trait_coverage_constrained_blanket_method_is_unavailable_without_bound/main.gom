trait Mark {
    fn mark(self: Self) -> ();
}

trait Extra {
    fn extra(self: Self) -> i32;
}

impl[T: Mark] Extra for T {
    fn extra(self: T) -> i32 { 1 }
}

fn main() -> () {
    let _ = 1.extra();
}
