fn sum[S: IntoIterator](source: S) -> i32
where
    S::Item = i32,
{
    let total = Ref::new(0);
    for value in source {
        total.set(total.get() + value);
    };
    total.get()
}

fn main() -> () { () }
