trait Selected {
    fn selected(self: Self) -> ();
}

struct Wrap[T] { value: T }

impl[T] Selected for Wrap[T] where T = i32 {
    fn selected(self: Wrap[T]) -> () { () }
}

fn require[T: Selected](value: T) -> () { () }

fn main() -> () {
    require(Wrap { value: "no" })
}
