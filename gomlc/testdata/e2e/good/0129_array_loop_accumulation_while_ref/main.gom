fn main() {
    let arr: [i32; 3] = [1, 2, 3];
    let sum = Ref::new(0);
    let i = Ref::new(0);
    while i.get() < 3 {
        sum.set(sum.get() + arr[i.get()]);
        i.set(i.get() + 1);
    };
    println(sum.get().to_string());
}
