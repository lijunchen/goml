struct UserError {
    value: i32,
}

fn fail() -> Result[i32, UserError] {
    Result::Err(UserError { value: 7 })
}

fn main() -> () {
    match fail() {
        Result::Ok(value) => println(value),
        Result::Err(err) => println(err.value),
    }
}
