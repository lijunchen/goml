trait Named {
    fn name(self: Self) -> string;
}

struct Cat {
    n: string,
}

impl Named for Cat {
    fn name(self: Cat) -> string {
        self.n
    }
}

fn main() {
    let a: dyn Named = Cat { n: "Whiskers" } as dyn Named;
    println(Named::name(a));
}
