trait Compute {
    fn compute(self: Self) -> i32;
}

struct Adder { a: i32, b: i32 }

impl Compute for Adder {
    fn compute(self: Adder) -> i32 { self.a + self.b }
}

fn main() {
    let a = Adder { a: 10, b: 32 };
    println(a.compute().to_string());
}
