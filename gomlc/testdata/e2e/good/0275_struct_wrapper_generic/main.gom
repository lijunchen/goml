struct Wrapper[T] {
    inner: T,
}

fn wrap[T](x: T) -> Wrapper[T] {
    Wrapper { inner: x }
}

fn unwrap[T](w: Wrapper[T]) -> T {
    w.inner
}

fn main() {
    let w: Wrapper[u8] = wrap(42);
    let v: u8 = unwrap(w);
    println(v.to_string());
}
