fn swap_refs(a: Ref[i32], b: Ref[i32]) {
    let tmp = a.get();
    a.set(b.get());
    b.set(tmp);
}

fn main() {
    let x: Ref[i32] = Ref::new(10);
    let y: Ref[i32] = Ref::new(20);
    println(x.get().to_string() + " " + y.get().to_string());
    swap_refs(x, y);
    println(x.get().to_string() + " " + y.get().to_string());
}
