fn make(a: i32) -> (i32) -> i32 {
    |x: i32| x + a
}

fn main() -> () {
    let m: HashMap[i32, (i32) -> i32] = HashMap::new();
    m.set(0, make(2));
    match m.get(0) {
        Option::Some(f) => println(f(40).to_string()),
        Option::None => println("none"),
    }
}
