struct Config {
    transform: (i32) -> i32,
}

fn run(c: Config, x: i32) -> i32 {
    c.transform(x)
}

fn main() {
    let result = run(Config { transform: |x| x * x }, 7);
    println(result.to_string());
}
