fn main() {
    let x: i32 = 0;
    let y = match x {
        0 => match true {
            true => "zero-true",
            false => "zero-false",
        },
        _ => "other",
    };
    println(y);
}
