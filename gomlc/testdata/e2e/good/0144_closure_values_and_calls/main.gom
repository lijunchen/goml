fn main() {
    let f = |x: i32, y: i32| x + y;
    let g = |x: i32, y: i32, z: i32| x * y + z;
    
    println(f(3, 4).to_string());
    println(g(2, 3, 4).to_string());
    
    let h: (i32, i32) -> i32 = f;
    println(h(10, 20).to_string());
}
