struct Builder {
    value: i32,
}

impl Builder {
    fn new() -> Builder {
        Builder { value: 0 }
    }
    
    fn add(self: Builder, v: i32) -> Builder {
        Builder { value: self.value + v }
    }
    
    fn build(self: Builder) -> i32 {
        self.value
    }
}

fn main() {
    let result = Builder::new().add(10).add(20).add(12).build();
    println(result.to_string());
}
