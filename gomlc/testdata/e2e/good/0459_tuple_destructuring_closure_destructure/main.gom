fn main() {
    let f = |x: i32| {
        let g = |y: i32| x + y;
        (g(1), g(10))
    };
    let (a, b) = f(100);
    println(a.to_string() + " " + b.to_string());
    
    let make_adder = |base: i32| |x: i32| base + x;
    let add5 = make_adder(5);
    let add10 = make_adder(10);
    println(add5(3).to_string() + " " + add10(3).to_string());
}
