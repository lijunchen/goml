fn flip[A, B, C](f: (A, B) -> C) -> (B, A) -> C {
    |b: B, a: A| f(a, b)
}

fn sub(a: i32, b: i32) -> i32 {
    a - b
}

fn main() {
    let rsub = flip(sub);
    println(sub(10, 3).to_string());
    println(rsub(10, 3).to_string());
}
