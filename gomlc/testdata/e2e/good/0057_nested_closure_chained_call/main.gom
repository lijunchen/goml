fn main() {
    let f = |x: i32| {
        let g = |y: i32| {
            let h = |z: i32| x + y + z;
            h
        };
        g
    };
    let result = f(1)(2)(3);
    println(result.to_string());
}
