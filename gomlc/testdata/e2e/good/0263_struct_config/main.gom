struct Config {
    width: u16,
    height: u16,
}

fn main() {
    let width: u16 = 800;
    let height: u16 = 600;
    let c = Config { width, height };
    println(c.width.to_string());
    println(c.height.to_string());
}
