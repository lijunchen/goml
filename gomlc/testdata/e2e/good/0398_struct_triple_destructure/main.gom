struct Triple { a: i32, b: i32, c: i32 }

fn main() {
    let Triple { a, b, c } = Triple { a: 1, b: 2, c: 3 };
    println(a.to_string() + " " + b.to_string() + " " + c.to_string());
    
    let (x, (y, z)) = (10, (20, 30));
    println(x.to_string() + " " + y.to_string() + " " + z.to_string());
}
