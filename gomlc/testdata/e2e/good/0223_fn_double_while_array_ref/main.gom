fn double(x: i32) -> i32 { x * 2 }
fn triple(x: i32) -> i32 { x * 3 }
fn negate(x: i32) -> i32 { 0 - x }

fn main() {
    let fns: [(i32) -> i32; 3] = [double, triple, negate];
    let i = Ref::new(0);
    while i.get() < 3 {
        let f = fns[i.get()];
        println(f(10).to_string());
        i.set(i.get() + 1);
    };
}
