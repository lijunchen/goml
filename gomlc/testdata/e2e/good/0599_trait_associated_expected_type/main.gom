trait Default {
    fn default() -> Self;
}

struct MyVal {
    number: int32,
}

impl Default for MyVal {
    fn default() -> MyVal {
        MyVal { number: 0 }
    }
}

fn main() -> unit {
    let value: MyVal = Default::default();
    println(value.number)
}
