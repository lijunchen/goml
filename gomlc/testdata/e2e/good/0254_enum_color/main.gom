#[derive(ToString)]
enum Color {
    Red,
    Green,
    Blue,
    Custom(i32, i32, i32),
}

fn main() {
    let c = Color::Red;
    println(c.to_string());
    let c2 = Color::Custom(255, 128, 0);
    println(c2.to_string());
}
