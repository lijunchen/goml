fn apply(f: (i32) -> i32, x: i32) -> i32 {
    f(x)
}

fn main() {
    let op = 1;
    let f: (i32) -> i32 = match op {
        1 => |x| x + 1,
        2 => |x| x * 2,
        _ => |x| x,
    };
    println(apply(f, 10).to_string());
}
