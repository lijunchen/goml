enum Result[T, E] {
    Ok(T),
    Err(E),
}

fn main() {
    let r: Result[u8, string] = Result::Ok(42);
    match r {
        Result::Ok(v) => println(v.to_string()),
        Result::Err(e) => println(e),
    };
}
