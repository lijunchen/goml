fn make_counter() -> (() -> i32) {
    let count = Ref::new(0);
    || {
        count.set(count.get() + 1);
        count.get()
    }
}

fn main() {
    let counter = make_counter();
    println(counter().to_string());
    println(counter().to_string());
    println(counter().to_string());
}
