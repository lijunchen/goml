fn main() {
    let adders: [(i32) -> i32; 3] = [
        |x| x + 1,
        |x| x + 10,
        |x| x + 100,
    ];
    
    let i = Ref::new(0);
    while i.get() < 3 {
        let f = adders[i.get()];
        println(f(0).to_string());
        i.set(i.get() + 1);
    };
}
