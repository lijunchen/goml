fn take_uint16(x: u16) -> u16 { x }

fn main() {
    let a = take_uint16(1000);
    println(a.to_string());
    
    let b: i16 = -100;
    println(b.to_string());
    
    let c: u32 = 1 + 2 + 3;
    println(c.to_string());
}
