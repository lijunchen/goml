trait Speak {
    fn speak(self: Self) -> string;
}

struct Dog {}

impl Speak for Dog {
    fn speak(self: Dog) -> string {
        "woof"
    }
}

fn make_dog() -> dyn Speak {
    let d = Dog {};
    d as dyn Speak
}

fn main() -> () {
    let a = make_dog();
    println(Speak::speak(a))
}
