struct Wrapper[T] {
    inner: T,
}

impl Wrapper[i32] {
    fn double(self: Self) -> i32 {
        self.inner * 2
    }
}

fn make_wrapper[T](val: T) -> Wrapper[T] {
    Wrapper { inner: val }
}

fn main() {
    let w: Wrapper[i32] = make_wrapper(21);
    println(w.double().to_string());
}
