trait Monoid {
    fn empty(self: Self) -> Self;
    fn combine(self: Self, arg1: Self) -> Self;
}

impl Monoid for i32 {
    fn empty(self: i32) -> i32 { 0 }
    fn combine(self: i32, other: i32) -> i32 { self + other }
}

impl Monoid for string {
    fn empty(self: string) -> string { "" }
    fn combine(self: string, other: string) -> string { self + other }
}

fn fold[T: Monoid](items: (T, T, T)) -> T {
    let (a, b, c) = items;
    Monoid::combine(Monoid::combine(a, b), c)
}

fn main() {
    let nums = (1, 2, 3);
    println(fold(nums).to_string());
    
    let strs = ("hello", " ", "world");
    println(fold(strs));
}
