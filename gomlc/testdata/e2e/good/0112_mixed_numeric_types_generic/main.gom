fn identity[T](x: T) -> T {
    x
}

fn main() {
    let a: u8 = identity(42);
    println(a.to_string());
    
    let b: i64 = identity(100);
    println(b.to_string());
    
    let c: f32 = identity(3.14);
    println(c.to_string());
}
