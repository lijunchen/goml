struct Empty {}

fn take_empty(e: Empty) -> string {
    "got empty"
}

fn return_empty() -> Empty {
    Empty {}
}

fn main() {
    let e = return_empty();
    println(take_empty(e));
    
    let u: unit = ();
    let _ = u;
    println("unit works");
    
    let t: (unit, int32, unit) = ((), 42, ());
    let (_, n, _) = t;
    println(n.to_string());
}
