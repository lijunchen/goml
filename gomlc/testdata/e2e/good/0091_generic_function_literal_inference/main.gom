fn identity[T](x: T) -> T {
    x
}

fn main() {
    let a: u8 = identity(42);
    println(a.to_string());
    
    let b: f32 = identity(3.14);
    println(b.to_string());
    
    let c: i64 = identity(100);
    println(c.to_string());
}
