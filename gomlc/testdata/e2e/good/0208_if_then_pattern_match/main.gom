fn main() {
    let x: i32 = if true { 1 } else { 2 };
    let y = match x {
        1 => "one",
        2 => "two",
        _ => "other",
    };
    println(y);
    
    let z: u8 = if true { 10 } else { 20 };
    println(z.to_string());
}
