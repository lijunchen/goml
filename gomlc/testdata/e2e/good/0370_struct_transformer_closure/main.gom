struct Transformer {
    f: (i32) -> i32,
    g: (i32) -> string,
}

fn apply(t: Transformer, x: i32) -> string {
    t.g(t.f(x))
}

fn main() {
    let t = Transformer {
        f: |x| x * 2 + 1,
        g: |x| "result: " + x.to_string(),
    };
    println(apply(t, 20));
}
