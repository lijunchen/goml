fn fib(n: int32) -> int32 {
    if n <= 1 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn main() {
    let i = Ref::new(0);
    while i.get() < 10 {
        println(fib(i.get()).to_string());
        println(" ");
        i.set(i.get() + 1);
    };
    println("");
}
