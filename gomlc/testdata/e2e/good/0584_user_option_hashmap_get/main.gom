enum Option[T] {
    Nada,
    Just(T),
}

fn main() -> () {
    let map = HashMap::new();
    map.set(1, 2);
    match map.get(1) {
        Option::Just(value) => println(value),
        Option::Nada => println(0),
    }
}
