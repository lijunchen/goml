#[derive(ToString)]
struct Point {
    x: i32,
    y: i32,
}

#[derive(ToString)]
enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let p = Point { x: 1, y: 2 };
    println(p.to_string());
    
    let c = Color::Red;
    println(c.to_string());
}
