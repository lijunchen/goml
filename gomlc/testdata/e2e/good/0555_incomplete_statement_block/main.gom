fn early_return(x: int32) -> string {
    if x > 0 {
        return "positive"
    } else {
        return "non-positive"
    }
}

fn main() {
    println(early_return(5));
    println(early_return(-3));
}
