fn make_fn() -> () -> i32 {
    || 42
}

fn make_adder(n: i32) -> (i32) -> i32 {
    |x| x + n
}

fn main() {
    let a: i32 = (make_fn())();
    println(a);
    
    let b = (make_adder(5))(10);
    println(b);
}
