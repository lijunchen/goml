fn side_effect_loop(n: i32) -> unit {
    let i = Ref::new(0);
    while i.get() < n {
        println(i.get().to_string());
        i.set(i.get() + 1);
    }
}

fn main() {
    side_effect_loop(3);
    println("done");
}
