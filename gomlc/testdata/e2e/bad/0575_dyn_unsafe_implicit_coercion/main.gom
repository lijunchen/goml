trait Cloneish {
    fn clone(self: Self) -> Self;
}

struct A {
    x: i32,
}

impl Cloneish for A {
    fn clone(self: A) -> A {
        A { x: self.x }
    }
}

fn main() -> () {
    let _a: dyn Cloneish = A { x: 1 } as dyn Cloneish;
    ()
}
