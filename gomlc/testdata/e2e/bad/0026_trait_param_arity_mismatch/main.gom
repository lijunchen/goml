trait Transformer {
  fn transform(self: Self, arg1: i32, arg2: i32) -> i32;
}

struct Scaler {
  factor: i32,
}

impl Transformer for Scaler {
  fn transform(self: Scaler, n: i32) -> i32 {
    n * self.factor
  }
}

fn main() -> () {
  ()
}
