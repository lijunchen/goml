enum Status {
  Active,
  Inactive,
  Pending,
}

fn main() -> () {
  let code: i32 = 1;
  match code {
    Status::Active => println("active"),
    Status::Inactive => println("inactive"),
    _ => println("unknown"),
  }
}
