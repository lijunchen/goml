#[derive(ToString)]
enum Option[T] {
    Some(T),
    None,
}

fn main() {
    let a: Option[int32] = Option::Some(42);
    let b: Option[int32] = Option::None;
    println(a.to_string());
    println(b.to_string());
}
