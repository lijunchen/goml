#[derive(ToString)]
enum Option[T] {
    Some(T),
    None,
}

struct Hidden {}

fn main() {
    let a: Option[Hidden] = Option::Some(Hidden {});
    let b: Option[Hidden] = Option::None;
    println(a.to_string());
    println(b.to_string());
}
