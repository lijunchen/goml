struct Config {
  timeout: i32,
  retries: i32,
}

fn main() -> () {
  let m = HashMap::new();
  m.set(Config { timeout: 30, retries: 3 }, "production");
  let result = m.get(Config { timeout: 30, retries: 3 });
  match result {
    Option::Some(v) => println(v),
    Option::None => println("not found"),
  }
}
