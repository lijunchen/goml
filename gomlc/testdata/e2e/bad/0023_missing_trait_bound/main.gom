trait Printable {
  fn print_it(self: Self) -> ();
}

fn show_all[T: Printable](items: Vec[T]) -> () {
  ()
}

struct Raw {
  data: i32,
}

fn main() -> () {
  let items = Vec::new();
  items.push(Raw { data: 42 });
  show_all(items)
}
