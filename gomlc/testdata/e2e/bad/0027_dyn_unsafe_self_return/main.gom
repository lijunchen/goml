trait Builder {
  fn build(self: Self) -> Self;
}

struct Widget {
  id: i32,
}

impl Builder for Widget {
  fn build(self: Widget) -> Widget {
    Widget { id: self.id + 1 }
  }
}

fn create(b: dyn Builder) -> dyn Builder {
  Builder::build(b)
}

fn main() -> () {
  ()
}
