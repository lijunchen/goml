struct Stack[T] {
    items: Vec[T],
    size: Ref[i32],
}

impl Stack[i32] {
    fn new() -> Stack[i32] {
        Stack { items: Vec::new(), size: Ref::new(0) }
    }
}

fn main() {
    let s = Stack::new();
    println(s.size.get().to_string());
}
