struct Box[T] {
    value: T,
}

impl Box[T] {
    fn unwrap(self: Box[T]) -> T {
        self.value
    }
}

fn main() {
    let b: Box[u8] = Box { value: 42 };
    let v = Box::unwrap(b);
    println(v.to_string());
}
