fn apply(f: (i32) -> i32, x: i32) -> i32 {
    f(x)
}

fn main() {
    let op = 1;
    let f = match op {
        1 => |x: i32| -> i32 { x + 1 },
        2 => |x: i32| -> i32 { x * 2 },
        _ => |x: i32| -> i32 { x },
    };
    println(apply(f, 10).to_string());
}
