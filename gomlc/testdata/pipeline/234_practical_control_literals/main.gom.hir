package main
file main.gom
  fn unwrap_or_negative(local/1/0: Option[int]) -> int {
        let Some(local/1/1) = value/0 else {
            return -1;
        };
        result/1
    }

    fn count_to(local/1/2: int) -> int {
        let local/1/3 = Ref::new(0);
        loop {
            let local/1/4 = counter/3.get();
            if current/4 >= limit/2 {
                break current/4;
            } else ();
            counter/3.set(current/4 + 1);
        }
    }

    fn loop_option(local/1/5: Option[int]) -> int {
        loop {
            let Some(local/1/6) = value/5 else {
                break -2;
            };
            break item/6;
        }
    }

    fn nested_loop_value() -> int {
        loop {
            loop {
                break;
            };
            break 7;
        }
    }

    fn never_returns() -> int {
        loop {}
    }

    fn matches_raw_path(local/1/7: string) -> bool {
        match value/7 {
            "C:\\tmp" => true,
            _ => false,
        }
    }

    fn main() {
        string_println("C:\\tmp\\\"quoted\\\"");
        string_println(f"{"}"}");
        println(def/1/0(Option::Some(11)).to_string());
        println(def/1/0(Option::None).to_string());
        println(def/1/1(4).to_string());
        println(def/1/2(Option::Some(9)).to_string());
        println(def/1/2(Option::None).to_string());
        println(def/1/3().to_string());
        println(def/1/5("C:\\tmp").to_string());
    }