fn unwrap_or_negative(value/0: Option__isize) -> isize {
  match value/0 {
    Tag_1 => {
      let x797 = Option__isize::Some._0(value/0) in
      x797
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/2: isize) -> isize {
  join inline_cont942(counter/3: Ref[isize]) -> isize {
    join exit824(jp825: isize) -> isize {
      jp825
    } in
    joinrec {
      join loop_expr826() -> isize {
        join inline_cont940(current/4: isize) -> isize {
          let t829 = (current/4 >= limit/2) in
          join k827() -> isize {
            let t828 = (current/4 + 1) in
            let inline939 = @intrinsic(ref.set)(counter/3, t828) in
            jump loop_expr826()
          } in
          if t829 {
            jump exit824(current/4)
          } else {
            jump k827()
          }
        } in
        let inline941 = @intrinsic(ref.get)(counter/3) in
        jump inline_cont940(inline941)
      } in
    } in
    jump loop_expr826()
  } in
  let inline943 = 0 in
  let inline944 = @intrinsic(ref.new)(inline943) in
  jump inline_cont942(inline944)
}

fn loop_option(value/5: Option__isize) -> isize {
  join exit832(jp833: isize) -> isize {
    jp833
  } in
  joinrec {
    join loop_expr834() -> isize {
      match value/5 {
        Tag_1 => {
          let x803 = Option__isize::Some._0(value/5) in
          jump exit832(x803)
        },
        _ => {
          jump exit832(-2)
        },
      }
    } in
  } in
  jump loop_expr834()
}

fn nested_loop_value() -> isize {
  join exit838(jp839: isize) -> isize {
    jp839
  } in
  joinrec {
    join loop_expr840() -> isize {
      join exit841() -> isize {
        jump exit838(7)
      } in
      joinrec {
        join loop_expr842() -> isize {
          jump exit841()
        } in
      } in
      jump loop_expr842()
    } in
  } in
  jump loop_expr840()
}

fn never_returns() -> isize {
  joinrec {
    join loop_expr847() -> isize {
      jump loop_expr847()
    } in
  } in
  jump loop_expr847()
}

fn matches_raw_path(value/7: string) -> bool {
  match value/7 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild809 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t853 = ("" + "}") in
  join inline_cont986(_wild810: unit) -> unit {
    let t854 = Option__isize::Some(11) in
    let t855 = unwrap_or_negative(t854) in
    join inline_cont984(t856: string) -> unit {
      join inline_cont981(_wild811: unit) -> unit {
        join inline_cont977(t857: isize) -> unit {
          join inline_cont975(t858: string) -> unit {
            join inline_cont972(_wild812: unit) -> unit {
              let t859 = count_to(4) in
              join inline_cont970(t860: string) -> unit {
                join inline_cont967(_wild813: unit) -> unit {
                  let t861 = Option__isize::Some(9) in
                  let t862 = loop_option(t861) in
                  join inline_cont965(t863: string) -> unit {
                    join inline_cont962(_wild814: unit) -> unit {
                      let t864 = loop_option(Tag_0) in
                      join inline_cont960(t865: string) -> unit {
                        join inline_cont957(_wild815: unit) -> unit {
                          let t866 = nested_loop_value() in
                          join inline_cont955(t867: string) -> unit {
                            join inline_cont952(_wild816: unit) -> unit {
                              join inline_cont950(t868: bool) -> unit {
                                join inline_cont948(t869: string) -> unit {
                                  let inline946 = trait_impl#ToString#string#to_string(t869) in
                                  let inline947 = @runtime(core.string_println)(inline946) in
                                  ()
                                } in
                                let inline949 = @runtime(core.bool_to_string)(t868) in
                                jump inline_cont948(inline949)
                              } in
                              let inline951 = "C:\\tmp" in
                              match inline951 {
                                "C:\\tmp" => {
                                  jump inline_cont950(true)
                                },
                                _ => {
                                  jump inline_cont950(false)
                                },
                              }
                            } in
                            let inline953 = trait_impl#ToString#string#to_string(t867) in
                            let inline954 = @runtime(core.string_println)(inline953) in
                            jump inline_cont952(())
                          } in
                          let inline956 = __goml_builtin_int_to_string(t866) in
                          jump inline_cont955(inline956)
                        } in
                        let inline958 = trait_impl#ToString#string#to_string(t865) in
                        let inline959 = @runtime(core.string_println)(inline958) in
                        jump inline_cont957(())
                      } in
                      let inline961 = __goml_builtin_int_to_string(t864) in
                      jump inline_cont960(inline961)
                    } in
                    let inline963 = trait_impl#ToString#string#to_string(t863) in
                    let inline964 = @runtime(core.string_println)(inline963) in
                    jump inline_cont962(())
                  } in
                  let inline966 = __goml_builtin_int_to_string(t862) in
                  jump inline_cont965(inline966)
                } in
                let inline968 = trait_impl#ToString#string#to_string(t860) in
                let inline969 = @runtime(core.string_println)(inline968) in
                jump inline_cont967(())
              } in
              let inline971 = __goml_builtin_int_to_string(t859) in
              jump inline_cont970(inline971)
            } in
            let inline973 = trait_impl#ToString#string#to_string(t858) in
            let inline974 = @runtime(core.string_println)(inline973) in
            jump inline_cont972(())
          } in
          let inline976 = __goml_builtin_int_to_string(t857) in
          jump inline_cont975(inline976)
        } in
        jump inline_cont977(-1)
      } in
      let inline982 = trait_impl#ToString#string#to_string(t856) in
      let inline983 = @runtime(core.string_println)(inline982) in
      jump inline_cont981(())
    } in
    let inline985 = __goml_builtin_int_to_string(t855) in
    jump inline_cont984(inline985)
  } in
  let inline987 = trait_impl#ToString#string#to_string(t853) in
  let inline988 = @runtime(core.string_println)(inline987) in
  jump inline_cont986(())
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t872 = @intrinsic(ref.new)(value/684) in
  t872
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t875 = @intrinsic(ref.get)(self/685) in
  t875
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t877 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont989(t879: string) -> unit {
    let t880 = @runtime(core.string_println)(t879) in
    ()
  } in
  jump inline_cont989(value/1)
}

fn inherent#isize#isize#to_string(self/285: isize) -> string {
  let inline991 = (self/285 as i64) in
  let inline992 = signed_decimal_string(inline991) in
  inline992
}

fn trait_impl#ToString#bool#to_string(self/401: bool) -> string {
  let t886 = @runtime(core.bool_to_string)(self/401) in
  t886
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t891 = (value/222 as i64) in
  let inline994 = (t891 < 0) in
  if inline994 {
    let inline995 = (t891 as u64) in
    let inline996 = (0 - inline995) in
    let inline997 = decimal_string(inline996) in
    let inline998 = ("-" + inline997) in
    inline998
  } else {
    let inline999 = (t891 as u64) in
    let inline1000 = decimal_string(inline999) in
    inline1000
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t897 = (value/214 < 0) in
  if t897 {
    let t898 = (value/214 as u64) in
    let t899 = (0 - t898) in
    let t900 = decimal_string(t899) in
    let t901 = ("-" + t900) in
    t901
  } else {
    let t902 = (value/214 as u64) in
    let t903 = decimal_string(t902) in
    t903
  }
}

fn decimal_string(value/208: u64) -> string {
  let t926 = (value/208 == 0) in
  join k906() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit907() -> string {
      join inline_cont1009(t908: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t908) in
        let offset/212 = 0 in
        join exit909() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop910() -> string {
            join inline_cont1007(t911: isize) -> string {
              let t912 = (offset/212 < t911) in
              if t912 {
                join inline_cont1005(t913: isize) -> string {
                  let t914 = (t913 - offset/212) in
                  let t915 = (t914 - 1) in
                  let t916 = @intrinsic(vec.get)(reversed/209, t915) in
                  join inline_cont1003(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t917 = (compound_old358 + compound_value359) in
                    let t918 = offset/212 = t917 in
                    jump loop910()
                  } in
                  let inline1004 = @intrinsic(vec.push)(bytes/211, t916) in
                  jump inline_cont1003(())
                } in
                let inline1006 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont1005(inline1006)
              } else {
                jump exit909()
              }
            } in
            let inline1008 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont1007(inline1008)
          } in
        } in
        jump loop910()
      } in
      let inline1010 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont1009(inline1010)
    } in
    joinrec {
      join loop919() -> string {
        let t920 = (remaining/210 > 0) in
        if t920 {
          let t921 = (remaining/210 % 10) in
          let t922 = (t921 as u8) in
          let t923 = (t922 + 48) in
          join inline_cont1001(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t924 = (compound_old353 / compound_value354) in
            let t925 = remaining/210 = t924 in
            jump loop919()
          } in
          let inline1002 = @intrinsic(vec.push)(reversed/209, t923) in
          jump inline_cont1001(())
        } else {
          jump exit907()
        }
      } in
    } in
    jump loop919()
  } in
  if t926 {
    "0"
  } else {
    jump k906()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t928 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t931 = @intrinsic(vec.len)(self/526) in
  t931
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t934 = (self/438 == other/439) in
  t934
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t937 = @runtime(core.int_hash)(self/464) in
  t937
}