fn unwrap_or_negative(value/0: Option__isize) -> isize {
  match value/0 {
    Tag_1 => {
      let x0 = Option__isize::Some._0(value/0) in
      x0
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/0: isize) -> isize {
  join inline_cont0(counter/0: Ref[isize]) -> isize {
    join exit0(jp0: isize) -> isize {
      jp0
    } in
    joinrec {
      join loop_expr0() -> isize {
        join inline_cont1(current/0: isize) -> isize {
          let t0 = (current/0 >= limit/0) in
          join k0() -> isize {
            let t1 = (current/0 + 1) in
            let inline0 = @intrinsic(ref.set)(counter/0, t1) in
            jump loop_expr0()
          } in
          if t0 {
            jump exit0(current/0)
          } else {
            jump k0()
          }
        } in
        let inline1 = @intrinsic(ref.get)(counter/0) in
        jump inline_cont1(inline1)
      } in
    } in
    jump loop_expr0()
  } in
  let inline2 = 0 in
  let inline3 = @intrinsic(ref.new)(inline2) in
  jump inline_cont0(inline3)
}

fn loop_option(value/0: Option__isize) -> isize {
  join exit0(jp0: isize) -> isize {
    jp0
  } in
  joinrec {
    join loop_expr0() -> isize {
      match value/0 {
        Tag_1 => {
          let x0 = Option__isize::Some._0(value/0) in
          jump exit0(x0)
        },
        _ => {
          jump exit0(-2)
        },
      }
    } in
  } in
  jump loop_expr0()
}

fn nested_loop_value() -> isize {
  join exit0(jp0: isize) -> isize {
    jp0
  } in
  joinrec {
    join loop_expr0() -> isize {
      join exit1() -> isize {
        jump exit0(7)
      } in
      joinrec {
        join loop_expr1() -> isize {
          jump exit1()
        } in
      } in
      jump loop_expr1()
    } in
  } in
  jump loop_expr0()
}

fn never_returns() -> isize {
  joinrec {
    join loop_expr0() -> isize {
      jump loop_expr0()
    } in
  } in
  jump loop_expr0()
}

fn matches_raw_path(value/0: string) -> bool {
  match value/0 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild0 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t0 = ("" + "}") in
  join inline_cont0(_wild1: unit) -> unit {
    let t1 = Option__isize::Some(11) in
    let t2 = unwrap_or_negative(t1) in
    join inline_cont1(t3: string) -> unit {
      join inline_cont2(_wild2: unit) -> unit {
        join inline_cont3(t4: isize) -> unit {
          join inline_cont4(t5: string) -> unit {
            join inline_cont5(_wild3: unit) -> unit {
              let t6 = count_to(4) in
              join inline_cont6(t7: string) -> unit {
                join inline_cont7(_wild4: unit) -> unit {
                  let t8 = Option__isize::Some(9) in
                  let t9 = loop_option(t8) in
                  join inline_cont8(t10: string) -> unit {
                    join inline_cont9(_wild5: unit) -> unit {
                      let t11 = loop_option(Tag_0) in
                      join inline_cont10(t12: string) -> unit {
                        join inline_cont11(_wild6: unit) -> unit {
                          let t13 = nested_loop_value() in
                          join inline_cont12(t14: string) -> unit {
                            join inline_cont13(_wild7: unit) -> unit {
                              join inline_cont14(t15: bool) -> unit {
                                join inline_cont15(t16: string) -> unit {
                                  let inline0 = trait_impl#ToString#string#to_string(t16) in
                                  let inline1 = @runtime(core.string_println)(inline0) in
                                  ()
                                } in
                                let inline2 = @runtime(core.bool_to_string)(t15) in
                                jump inline_cont15(inline2)
                              } in
                              let inline3 = "C:\\tmp" in
                              match inline3 {
                                "C:\\tmp" => {
                                  jump inline_cont14(true)
                                },
                                _ => {
                                  jump inline_cont14(false)
                                },
                              }
                            } in
                            let inline4 = trait_impl#ToString#string#to_string(t14) in
                            let inline5 = @runtime(core.string_println)(inline4) in
                            jump inline_cont13(())
                          } in
                          let inline6 = __goml_builtin_int_to_string(t13) in
                          jump inline_cont12(inline6)
                        } in
                        let inline7 = trait_impl#ToString#string#to_string(t12) in
                        let inline8 = @runtime(core.string_println)(inline7) in
                        jump inline_cont11(())
                      } in
                      let inline9 = __goml_builtin_int_to_string(t11) in
                      jump inline_cont10(inline9)
                    } in
                    let inline10 = trait_impl#ToString#string#to_string(t10) in
                    let inline11 = @runtime(core.string_println)(inline10) in
                    jump inline_cont9(())
                  } in
                  let inline12 = __goml_builtin_int_to_string(t9) in
                  jump inline_cont8(inline12)
                } in
                let inline13 = trait_impl#ToString#string#to_string(t7) in
                let inline14 = @runtime(core.string_println)(inline13) in
                jump inline_cont7(())
              } in
              let inline15 = __goml_builtin_int_to_string(t6) in
              jump inline_cont6(inline15)
            } in
            let inline16 = trait_impl#ToString#string#to_string(t5) in
            let inline17 = @runtime(core.string_println)(inline16) in
            jump inline_cont5(())
          } in
          let inline18 = __goml_builtin_int_to_string(t4) in
          jump inline_cont4(inline18)
        } in
        jump inline_cont3(-1)
      } in
      let inline19 = trait_impl#ToString#string#to_string(t3) in
      let inline20 = @runtime(core.string_println)(inline19) in
      jump inline_cont2(())
    } in
    let inline21 = __goml_builtin_int_to_string(t2) in
    jump inline_cont1(inline21)
  } in
  let inline22 = trait_impl#ToString#string#to_string(t0) in
  let inline23 = @runtime(core.string_println)(inline22) in
  jump inline_cont0(())
}

fn inherent#Ref#Ref[T]#new__T_isize(value/0: isize) -> Ref[isize] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_isize(self/0: Ref[isize]) -> isize {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_isize(self/0: Ref[isize], value/0: isize) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn inherent#isize#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}