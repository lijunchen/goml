fn unwrap_or_negative(value/0: Option__int) -> int {
  match value/0 {
    Tag_1 => {
      let x183 = Option__int::Some._0(value/0) in
      x183
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/2: int) -> int {
  join inline_cont285(counter/3: Ref[int]) -> int {
    join exit210(jp211: int) -> int {
      jp211
    } in
    joinrec {
      join loop_expr212() -> int {
        join inline_cont283(current/4: int) -> int {
          let t215 = (current/4 >= limit/2) in
          join k213() -> int {
            let t214 = (current/4 + 1) in
            let inline282 = @intrinsic(ref.set)(counter/3, t214) in
            jump loop_expr212()
          } in
          if t215 {
            jump exit210(current/4)
          } else {
            jump k213()
          }
        } in
        let inline284 = @intrinsic(ref.get)(counter/3) in
        jump inline_cont283(inline284)
      } in
    } in
    jump loop_expr212()
  } in
  let inline286 = 0 in
  let inline287 = @intrinsic(ref.new)(inline286) in
  jump inline_cont285(inline287)
}

fn loop_option(value/5: Option__int) -> int {
  join exit218(jp219: int) -> int {
    jp219
  } in
  joinrec {
    join loop_expr220() -> int {
      match value/5 {
        Tag_1 => {
          let x189 = Option__int::Some._0(value/5) in
          jump exit218(x189)
        },
        _ => {
          jump exit218(-2)
        },
      }
    } in
  } in
  jump loop_expr220()
}

fn nested_loop_value() -> int {
  join exit224(jp225: int) -> int {
    jp225
  } in
  joinrec {
    join loop_expr226() -> int {
      join exit227() -> int {
        jump exit224(7)
      } in
      joinrec {
        join loop_expr228() -> int {
          jump exit227()
        } in
      } in
      jump loop_expr228()
    } in
  } in
  jump loop_expr226()
}

fn never_returns() -> int {
  joinrec {
    join loop_expr233() -> int {
      jump loop_expr233()
    } in
  } in
  jump loop_expr233()
}

fn matches_raw_path(value/7: string) -> bool {
  match value/7 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild195 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t239 = ("" + "}") in
  join inline_cont329(_wild196: unit) -> unit {
    let t240 = Option__int::Some(11) in
    let t241 = unwrap_or_negative(t240) in
    join inline_cont327(t242: string) -> unit {
      join inline_cont324(_wild197: unit) -> unit {
        join inline_cont320(t243: int) -> unit {
          join inline_cont318(t244: string) -> unit {
            join inline_cont315(_wild198: unit) -> unit {
              let t245 = count_to(4) in
              join inline_cont313(t246: string) -> unit {
                join inline_cont310(_wild199: unit) -> unit {
                  let t247 = Option__int::Some(9) in
                  let t248 = loop_option(t247) in
                  join inline_cont308(t249: string) -> unit {
                    join inline_cont305(_wild200: unit) -> unit {
                      let t250 = loop_option(Tag_0) in
                      join inline_cont303(t251: string) -> unit {
                        join inline_cont300(_wild201: unit) -> unit {
                          let t252 = nested_loop_value() in
                          join inline_cont298(t253: string) -> unit {
                            join inline_cont295(_wild202: unit) -> unit {
                              join inline_cont293(t254: bool) -> unit {
                                join inline_cont291(t255: string) -> unit {
                                  let inline289 = trait_impl#ToString#string#to_string(t255) in
                                  let inline290 = @runtime(core.string_println)(inline289) in
                                  ()
                                } in
                                let inline292 = @runtime(core.bool_to_string)(t254) in
                                jump inline_cont291(inline292)
                              } in
                              let inline294 = "C:\\tmp" in
                              match inline294 {
                                "C:\\tmp" => {
                                  jump inline_cont293(true)
                                },
                                _ => {
                                  jump inline_cont293(false)
                                },
                              }
                            } in
                            let inline296 = trait_impl#ToString#string#to_string(t253) in
                            let inline297 = @runtime(core.string_println)(inline296) in
                            jump inline_cont295(())
                          } in
                          let inline299 = @runtime(core.int_to_string)(t252) in
                          jump inline_cont298(inline299)
                        } in
                        let inline301 = trait_impl#ToString#string#to_string(t251) in
                        let inline302 = @runtime(core.string_println)(inline301) in
                        jump inline_cont300(())
                      } in
                      let inline304 = @runtime(core.int_to_string)(t250) in
                      jump inline_cont303(inline304)
                    } in
                    let inline306 = trait_impl#ToString#string#to_string(t249) in
                    let inline307 = @runtime(core.string_println)(inline306) in
                    jump inline_cont305(())
                  } in
                  let inline309 = @runtime(core.int_to_string)(t248) in
                  jump inline_cont308(inline309)
                } in
                let inline311 = trait_impl#ToString#string#to_string(t246) in
                let inline312 = @runtime(core.string_println)(inline311) in
                jump inline_cont310(())
              } in
              let inline314 = @runtime(core.int_to_string)(t245) in
              jump inline_cont313(inline314)
            } in
            let inline316 = trait_impl#ToString#string#to_string(t244) in
            let inline317 = @runtime(core.string_println)(inline316) in
            jump inline_cont315(())
          } in
          let inline319 = @runtime(core.int_to_string)(t243) in
          jump inline_cont318(inline319)
        } in
        jump inline_cont320(-1)
      } in
      let inline325 = trait_impl#ToString#string#to_string(t242) in
      let inline326 = @runtime(core.string_println)(inline325) in
      jump inline_cont324(())
    } in
    let inline328 = @runtime(core.int_to_string)(t241) in
    jump inline_cont327(inline328)
  } in
  let inline330 = trait_impl#ToString#string#to_string(t239) in
  let inline331 = @runtime(core.string_println)(inline330) in
  jump inline_cont329(())
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t258 = @intrinsic(ref.new)(value/270) in
  t258
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t261 = @intrinsic(ref.get)(self/271) in
  t261
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t263 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont332(t265: string) -> unit {
    let t266 = @runtime(core.string_println)(t265) in
    ()
  } in
  jump inline_cont332(value/1)
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t269 = @runtime(core.int_to_string)(self/32) in
  t269
}

fn trait_impl#ToString#bool#to_string(self/64: bool) -> string {
  let t272 = @runtime(core.bool_to_string)(self/64) in
  t272
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t277 = (self/101 == other/102) in
  t277
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t280 = @runtime(core.int_hash)(self/127) in
  t280
}