fn unwrap_or_negative(value/0: Option__isize) -> isize {
  match value/0 {
    Tag_1 => {
      let x412 = Option__isize::Some._0(value/0) in
      x412
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/2: isize) -> isize {
  join inline_cont514(counter/3: Ref[isize]) -> isize {
    join exit439(jp440: isize) -> isize {
      jp440
    } in
    joinrec {
      join loop_expr441() -> isize {
        join inline_cont512(current/4: isize) -> isize {
          let t444 = (current/4 >= limit/2) in
          join k442() -> isize {
            let t443 = (current/4 + 1) in
            let inline511 = @intrinsic(ref.set)(counter/3, t443) in
            jump loop_expr441()
          } in
          if t444 {
            jump exit439(current/4)
          } else {
            jump k442()
          }
        } in
        let inline513 = @intrinsic(ref.get)(counter/3) in
        jump inline_cont512(inline513)
      } in
    } in
    jump loop_expr441()
  } in
  let inline515 = 0 in
  let inline516 = @intrinsic(ref.new)(inline515) in
  jump inline_cont514(inline516)
}

fn loop_option(value/5: Option__isize) -> isize {
  join exit447(jp448: isize) -> isize {
    jp448
  } in
  joinrec {
    join loop_expr449() -> isize {
      match value/5 {
        Tag_1 => {
          let x418 = Option__isize::Some._0(value/5) in
          jump exit447(x418)
        },
        _ => {
          jump exit447(-2)
        },
      }
    } in
  } in
  jump loop_expr449()
}

fn nested_loop_value() -> isize {
  join exit453(jp454: isize) -> isize {
    jp454
  } in
  joinrec {
    join loop_expr455() -> isize {
      join exit456() -> isize {
        jump exit453(7)
      } in
      joinrec {
        join loop_expr457() -> isize {
          jump exit456()
        } in
      } in
      jump loop_expr457()
    } in
  } in
  jump loop_expr455()
}

fn never_returns() -> isize {
  joinrec {
    join loop_expr462() -> isize {
      jump loop_expr462()
    } in
  } in
  jump loop_expr462()
}

fn matches_raw_path(value/7: string) -> bool {
  match value/7 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild424 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t468 = ("" + "}") in
  join inline_cont558(_wild425: unit) -> unit {
    let t469 = Option__isize::Some(11) in
    let t470 = unwrap_or_negative(t469) in
    join inline_cont556(t471: string) -> unit {
      join inline_cont553(_wild426: unit) -> unit {
        join inline_cont549(t472: isize) -> unit {
          join inline_cont547(t473: string) -> unit {
            join inline_cont544(_wild427: unit) -> unit {
              let t474 = count_to(4) in
              join inline_cont542(t475: string) -> unit {
                join inline_cont539(_wild428: unit) -> unit {
                  let t476 = Option__isize::Some(9) in
                  let t477 = loop_option(t476) in
                  join inline_cont537(t478: string) -> unit {
                    join inline_cont534(_wild429: unit) -> unit {
                      let t479 = loop_option(Tag_0) in
                      join inline_cont532(t480: string) -> unit {
                        join inline_cont529(_wild430: unit) -> unit {
                          let t481 = nested_loop_value() in
                          join inline_cont527(t482: string) -> unit {
                            join inline_cont524(_wild431: unit) -> unit {
                              join inline_cont522(t483: bool) -> unit {
                                join inline_cont520(t484: string) -> unit {
                                  let inline518 = trait_impl#ToString#string#to_string(t484) in
                                  let inline519 = @runtime(core.string_println)(inline518) in
                                  ()
                                } in
                                let inline521 = @runtime(core.bool_to_string)(t483) in
                                jump inline_cont520(inline521)
                              } in
                              let inline523 = "C:\\tmp" in
                              match inline523 {
                                "C:\\tmp" => {
                                  jump inline_cont522(true)
                                },
                                _ => {
                                  jump inline_cont522(false)
                                },
                              }
                            } in
                            let inline525 = trait_impl#ToString#string#to_string(t482) in
                            let inline526 = @runtime(core.string_println)(inline525) in
                            jump inline_cont524(())
                          } in
                          let inline528 = @runtime(core.int_to_string)(t481) in
                          jump inline_cont527(inline528)
                        } in
                        let inline530 = trait_impl#ToString#string#to_string(t480) in
                        let inline531 = @runtime(core.string_println)(inline530) in
                        jump inline_cont529(())
                      } in
                      let inline533 = @runtime(core.int_to_string)(t479) in
                      jump inline_cont532(inline533)
                    } in
                    let inline535 = trait_impl#ToString#string#to_string(t478) in
                    let inline536 = @runtime(core.string_println)(inline535) in
                    jump inline_cont534(())
                  } in
                  let inline538 = @runtime(core.int_to_string)(t477) in
                  jump inline_cont537(inline538)
                } in
                let inline540 = trait_impl#ToString#string#to_string(t475) in
                let inline541 = @runtime(core.string_println)(inline540) in
                jump inline_cont539(())
              } in
              let inline543 = @runtime(core.int_to_string)(t474) in
              jump inline_cont542(inline543)
            } in
            let inline545 = trait_impl#ToString#string#to_string(t473) in
            let inline546 = @runtime(core.string_println)(inline545) in
            jump inline_cont544(())
          } in
          let inline548 = @runtime(core.int_to_string)(t472) in
          jump inline_cont547(inline548)
        } in
        jump inline_cont549(-1)
      } in
      let inline554 = trait_impl#ToString#string#to_string(t471) in
      let inline555 = @runtime(core.string_println)(inline554) in
      jump inline_cont553(())
    } in
    let inline557 = @runtime(core.int_to_string)(t470) in
    jump inline_cont556(inline557)
  } in
  let inline559 = trait_impl#ToString#string#to_string(t468) in
  let inline560 = @runtime(core.string_println)(inline559) in
  jump inline_cont558(())
}

fn inherent#Ref#Ref[T]#new__T_isize(value/431: isize) -> Ref[isize] {
  let t487 = @intrinsic(ref.new)(value/431) in
  t487
}

fn inherent#Ref#Ref[T]#get__T_isize(self/432: Ref[isize]) -> isize {
  let t490 = @intrinsic(ref.get)(self/432) in
  t490
}

fn inherent#Ref#Ref[T]#set__T_isize(self/433: Ref[isize], value/434: isize) -> unit {
  let t492 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont561(t494: string) -> unit {
    let t495 = @runtime(core.string_println)(t494) in
    ()
  } in
  jump inline_cont561(value/1)
}

fn inherent#isize#isize#to_string(self/32: isize) -> string {
  let t498 = @runtime(core.int_to_string)(self/32) in
  t498
}

fn trait_impl#ToString#bool#to_string(self/148: bool) -> string {
  let t501 = @runtime(core.bool_to_string)(self/148) in
  t501
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#isize#eq(self/185: isize, other/186: isize) -> bool {
  let t506 = (self/185 == other/186) in
  t506
}

fn trait_impl#Hash#isize#hash(self/211: isize) -> u64 {
  let t509 = @runtime(core.int_hash)(self/211) in
  t509
}