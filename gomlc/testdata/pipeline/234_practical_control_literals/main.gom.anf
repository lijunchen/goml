fn unwrap_or_negative(value/0: Option__int) -> int {
  let mtmp155 = value/0 in
  match value/0 {
    Tag_1 => {
      let x156 = Option__int::Some._0(value/0) in
      let result/1 = x156 in
      x156
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/2: int) -> int {
  join inline_cont258(counter/3: Ref[int]) -> int {
    join exit183(jp184: int) -> int {
      jp184
    } in
    joinrec {
      join loop_expr185() -> int {
        join inline_cont256(current/4: int) -> int {
          let t188 = (current/4 >= limit/2) in
          join k186() -> int {
            let _wild159 = () in
            let t187 = (current/4 + 1) in
            let inline255 = @intrinsic(ref.set)(counter/3, t187) in
            jump loop_expr185()
          } in
          if t188 {
            jump exit183(current/4)
          } else {
            jump k186()
          }
        } in
        let inline257 = @intrinsic(ref.get)(counter/3) in
        jump inline_cont256(inline257)
      } in
    } in
    jump loop_expr185()
  } in
  let inline259 = 0 in
  let inline260 = @intrinsic(ref.new)(inline259) in
  jump inline_cont258(inline260)
}

fn loop_option(value/5: Option__int) -> int {
  join exit191(jp192: int) -> int {
    jp192
  } in
  joinrec {
    join loop_expr193() -> int {
      let mtmp161 = value/5 in
      match value/5 {
        Tag_1 => {
          let x162 = Option__int::Some._0(value/5) in
          let item/6 = x162 in
          jump exit191(x162)
        },
        _ => {
          jump exit191(-2)
        },
      }
    } in
  } in
  jump loop_expr193()
}

fn nested_loop_value() -> int {
  join exit197(jp198: int) -> int {
    jp198
  } in
  joinrec {
    join loop_expr199() -> int {
      join exit200() -> int {
        let _wild166 = () in
        jump exit197(7)
      } in
      joinrec {
        join loop_expr201() -> int {
          jump exit200()
        } in
      } in
      jump loop_expr201()
    } in
  } in
  jump loop_expr199()
}

fn never_returns() -> int {
  joinrec {
    join loop_expr206() -> int {
      jump loop_expr206()
    } in
  } in
  jump loop_expr206()
}

fn matches_raw_path(value/7: string) -> bool {
  match value/7 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild168 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t212 = ("" + "}") in
  join inline_cont302(_wild169: unit) -> unit {
    let t213 = Option__int::Some(11) in
    let t214 = unwrap_or_negative(t213) in
    join inline_cont300(t215: string) -> unit {
      join inline_cont297(_wild170: unit) -> unit {
        join inline_cont293(t216: int) -> unit {
          join inline_cont291(t217: string) -> unit {
            join inline_cont288(_wild171: unit) -> unit {
              let t218 = count_to(4) in
              join inline_cont286(t219: string) -> unit {
                join inline_cont283(_wild172: unit) -> unit {
                  let t220 = Option__int::Some(9) in
                  let t221 = loop_option(t220) in
                  join inline_cont281(t222: string) -> unit {
                    join inline_cont278(_wild173: unit) -> unit {
                      let t223 = loop_option(Tag_0) in
                      join inline_cont276(t224: string) -> unit {
                        join inline_cont273(_wild174: unit) -> unit {
                          let t225 = nested_loop_value() in
                          join inline_cont271(t226: string) -> unit {
                            join inline_cont268(_wild175: unit) -> unit {
                              join inline_cont266(t227: bool) -> unit {
                                join inline_cont264(t228: string) -> unit {
                                  let inline262 = trait_impl#ToString#string#to_string(t228) in
                                  let inline263 = @runtime(core.string_println)(inline262) in
                                  ()
                                } in
                                let inline265 = @runtime(core.bool_to_string)(t227) in
                                jump inline_cont264(inline265)
                              } in
                              let inline267 = "C:\\tmp" in
                              match inline267 {
                                "C:\\tmp" => {
                                  jump inline_cont266(true)
                                },
                                _ => {
                                  jump inline_cont266(false)
                                },
                              }
                            } in
                            let inline269 = trait_impl#ToString#string#to_string(t226) in
                            let inline270 = @runtime(core.string_println)(inline269) in
                            jump inline_cont268(())
                          } in
                          let inline272 = @runtime(core.int_to_string)(t225) in
                          jump inline_cont271(inline272)
                        } in
                        let inline274 = trait_impl#ToString#string#to_string(t224) in
                        let inline275 = @runtime(core.string_println)(inline274) in
                        jump inline_cont273(())
                      } in
                      let inline277 = @runtime(core.int_to_string)(t223) in
                      jump inline_cont276(inline277)
                    } in
                    let inline279 = trait_impl#ToString#string#to_string(t222) in
                    let inline280 = @runtime(core.string_println)(inline279) in
                    jump inline_cont278(())
                  } in
                  let inline282 = @runtime(core.int_to_string)(t221) in
                  jump inline_cont281(inline282)
                } in
                let inline284 = trait_impl#ToString#string#to_string(t219) in
                let inline285 = @runtime(core.string_println)(inline284) in
                jump inline_cont283(())
              } in
              let inline287 = @runtime(core.int_to_string)(t218) in
              jump inline_cont286(inline287)
            } in
            let inline289 = trait_impl#ToString#string#to_string(t217) in
            let inline290 = @runtime(core.string_println)(inline289) in
            jump inline_cont288(())
          } in
          let inline292 = @runtime(core.int_to_string)(t216) in
          jump inline_cont291(inline292)
        } in
        let inline294 = Tag_0 in
        jump inline_cont293(-1)
      } in
      let inline298 = trait_impl#ToString#string#to_string(t215) in
      let inline299 = @runtime(core.string_println)(inline298) in
      jump inline_cont297(())
    } in
    let inline301 = @runtime(core.int_to_string)(t214) in
    jump inline_cont300(inline301)
  } in
  let inline303 = trait_impl#ToString#string#to_string(t212) in
  let inline304 = @runtime(core.string_println)(inline303) in
  jump inline_cont302(())
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  let t231 = @intrinsic(ref.new)(value/207) in
  t231
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  let t234 = @intrinsic(ref.get)(self/208) in
  t234
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  let t236 = @intrinsic(ref.set)(self/209, value/210) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont305(t238: string) -> unit {
    let t239 = @runtime(core.string_println)(t238) in
    ()
  } in
  jump inline_cont305(value/1)
}

fn inherent#int#int#to_string(self/5: int) -> string {
  let t242 = @runtime(core.int_to_string)(self/5) in
  t242
}

fn trait_impl#ToString#bool#to_string(self/37: bool) -> string {
  let t245 = @runtime(core.bool_to_string)(self/37) in
  t245
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  self/38
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  let t250 = @intrinsic(eq.primitive)(self/59, other/60) in
  t250
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  let t253 = @runtime(core.int_hash)(self/85) in
  t253
}