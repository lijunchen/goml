fn unwrap_or_negative(value/0: Option__int) -> int {
  match value/0 {
    Tag_1 => {
      let x173 = Option__int::Some._0(value/0) in
      x173
    },
    _ => {
      -1
    },
  }
}

fn count_to(limit/2: int) -> int {
  join inline_cont275(counter/3: Ref[int]) -> int {
    join exit200(jp201: int) -> int {
      jp201
    } in
    joinrec {
      join loop_expr202() -> int {
        join inline_cont273(current/4: int) -> int {
          let t205 = (current/4 >= limit/2) in
          join k203() -> int {
            let t204 = (current/4 + 1) in
            let inline272 = @intrinsic(ref.set)(counter/3, t204) in
            jump loop_expr202()
          } in
          if t205 {
            jump exit200(current/4)
          } else {
            jump k203()
          }
        } in
        let inline274 = @intrinsic(ref.get)(counter/3) in
        jump inline_cont273(inline274)
      } in
    } in
    jump loop_expr202()
  } in
  let inline276 = 0 in
  let inline277 = @intrinsic(ref.new)(inline276) in
  jump inline_cont275(inline277)
}

fn loop_option(value/5: Option__int) -> int {
  join exit208(jp209: int) -> int {
    jp209
  } in
  joinrec {
    join loop_expr210() -> int {
      match value/5 {
        Tag_1 => {
          let x179 = Option__int::Some._0(value/5) in
          jump exit208(x179)
        },
        _ => {
          jump exit208(-2)
        },
      }
    } in
  } in
  jump loop_expr210()
}

fn nested_loop_value() -> int {
  join exit214(jp215: int) -> int {
    jp215
  } in
  joinrec {
    join loop_expr216() -> int {
      join exit217() -> int {
        jump exit214(7)
      } in
      joinrec {
        join loop_expr218() -> int {
          jump exit217()
        } in
      } in
      jump loop_expr218()
    } in
  } in
  jump loop_expr216()
}

fn never_returns() -> int {
  joinrec {
    join loop_expr223() -> int {
      jump loop_expr223()
    } in
  } in
  jump loop_expr223()
}

fn matches_raw_path(value/7: string) -> bool {
  match value/7 {
    "C:\\tmp" => {
      true
    },
    _ => {
      false
    },
  }
}

fn main() -> unit {
  let _wild185 = println__T_string("C:\\tmp\\\"quoted\\\"") in
  let t229 = ("" + "}") in
  join inline_cont319(_wild186: unit) -> unit {
    let t230 = Option__int::Some(11) in
    let t231 = unwrap_or_negative(t230) in
    join inline_cont317(t232: string) -> unit {
      join inline_cont314(_wild187: unit) -> unit {
        join inline_cont310(t233: int) -> unit {
          join inline_cont308(t234: string) -> unit {
            join inline_cont305(_wild188: unit) -> unit {
              let t235 = count_to(4) in
              join inline_cont303(t236: string) -> unit {
                join inline_cont300(_wild189: unit) -> unit {
                  let t237 = Option__int::Some(9) in
                  let t238 = loop_option(t237) in
                  join inline_cont298(t239: string) -> unit {
                    join inline_cont295(_wild190: unit) -> unit {
                      let t240 = loop_option(Tag_0) in
                      join inline_cont293(t241: string) -> unit {
                        join inline_cont290(_wild191: unit) -> unit {
                          let t242 = nested_loop_value() in
                          join inline_cont288(t243: string) -> unit {
                            join inline_cont285(_wild192: unit) -> unit {
                              join inline_cont283(t244: bool) -> unit {
                                join inline_cont281(t245: string) -> unit {
                                  let inline279 = trait_impl#ToString#string#to_string(t245) in
                                  let inline280 = @runtime(core.string_println)(inline279) in
                                  ()
                                } in
                                let inline282 = @runtime(core.bool_to_string)(t244) in
                                jump inline_cont281(inline282)
                              } in
                              let inline284 = "C:\\tmp" in
                              match inline284 {
                                "C:\\tmp" => {
                                  jump inline_cont283(true)
                                },
                                _ => {
                                  jump inline_cont283(false)
                                },
                              }
                            } in
                            let inline286 = trait_impl#ToString#string#to_string(t243) in
                            let inline287 = @runtime(core.string_println)(inline286) in
                            jump inline_cont285(())
                          } in
                          let inline289 = @runtime(core.int_to_string)(t242) in
                          jump inline_cont288(inline289)
                        } in
                        let inline291 = trait_impl#ToString#string#to_string(t241) in
                        let inline292 = @runtime(core.string_println)(inline291) in
                        jump inline_cont290(())
                      } in
                      let inline294 = @runtime(core.int_to_string)(t240) in
                      jump inline_cont293(inline294)
                    } in
                    let inline296 = trait_impl#ToString#string#to_string(t239) in
                    let inline297 = @runtime(core.string_println)(inline296) in
                    jump inline_cont295(())
                  } in
                  let inline299 = @runtime(core.int_to_string)(t238) in
                  jump inline_cont298(inline299)
                } in
                let inline301 = trait_impl#ToString#string#to_string(t236) in
                let inline302 = @runtime(core.string_println)(inline301) in
                jump inline_cont300(())
              } in
              let inline304 = @runtime(core.int_to_string)(t235) in
              jump inline_cont303(inline304)
            } in
            let inline306 = trait_impl#ToString#string#to_string(t234) in
            let inline307 = @runtime(core.string_println)(inline306) in
            jump inline_cont305(())
          } in
          let inline309 = @runtime(core.int_to_string)(t233) in
          jump inline_cont308(inline309)
        } in
        jump inline_cont310(-1)
      } in
      let inline315 = trait_impl#ToString#string#to_string(t232) in
      let inline316 = @runtime(core.string_println)(inline315) in
      jump inline_cont314(())
    } in
    let inline318 = @runtime(core.int_to_string)(t231) in
    jump inline_cont317(inline318)
  } in
  let inline320 = trait_impl#ToString#string#to_string(t229) in
  let inline321 = @runtime(core.string_println)(inline320) in
  jump inline_cont319(())
}

fn inherent#Ref#Ref[T]#new__T_int(value/255: int) -> Ref[int] {
  let t248 = @intrinsic(ref.new)(value/255) in
  t248
}

fn inherent#Ref#Ref[T]#get__T_int(self/256: Ref[int]) -> int {
  let t251 = @intrinsic(ref.get)(self/256) in
  t251
}

fn inherent#Ref#Ref[T]#set__T_int(self/257: Ref[int], value/258: int) -> unit {
  let t253 = @intrinsic(ref.set)(self/257, value/258) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont322(t255: string) -> unit {
    let t256 = @runtime(core.string_println)(t255) in
    ()
  } in
  jump inline_cont322(value/1)
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t259 = @runtime(core.int_to_string)(self/32) in
  t259
}

fn trait_impl#ToString#bool#to_string(self/64: bool) -> string {
  let t262 = @runtime(core.bool_to_string)(self/64) in
  t262
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t267 = (self/101 == other/102) in
  t267
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t270 = @runtime(core.int_hash)(self/127) in
  t270
}