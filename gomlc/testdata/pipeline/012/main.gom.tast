fn fib(x/0: i32) -> i32 {
    match (x/0 : i32) < 2 {
        false => (fib : (i32) -> i32)((x/0 : i32) - 1) + (fib : (i32) -> i32)((x/0 : i32) - 2),
        true => 1,
    }
}

fn main() -> unit {
    let _ : unit = (print : (i32) -> unit)((fib : (i32) -> i32)(10));
}