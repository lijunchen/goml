package main
file main.gom
  trait Measure {
    fn measure(Self) -> isize;
  }

  impl Measure for Vec[i32] {
    fn measure(local/1/0: Vec[i32]) -> isize {
        self/0.len()
    }
  }

  fn read_measure[T: Measure](local/1/1: T) -> isize {
      Measure::measure(value/1)
  }

  fn main() -> unit {
      let local/1/2 = vec_new();
      println(def/1/2(values/2).to_string())
  }