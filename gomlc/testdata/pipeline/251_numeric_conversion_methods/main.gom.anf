const NEGATIVE_ONE: i16 = -1;

const WRAPPED: u8 = 255;

const LETTER: u32 = 65;

fn narrow_at_compile_time(value/0: i16) -> u8 {
  let t0 = (value/0 as u8) in
  t0
}

fn exercise_int(value/0: isize) -> () {
  ()
}

fn exercise_int8(value/0: i8) -> () {
  ()
}

fn exercise_int16(value/0: i16) -> () {
  ()
}

fn exercise_int32(value/0: i32) -> () {
  ()
}

fn exercise_int64(value/0: i64) -> () {
  ()
}

fn exercise_uint(value/0: usize) -> () {
  ()
}

fn exercise_uint8(value/0: u8) -> () {
  ()
}

fn exercise_uint16(value/0: u16) -> () {
  ()
}

fn exercise_uint32(value/0: u32) -> () {
  ()
}

fn exercise_uint64(value/0: u64) -> () {
  ()
}

fn main() -> () {
  join inline_cont0(_wild0: ()) -> () {
    join inline_cont1(_wild1: ()) -> () {
      join inline_cont2(_wild2: ()) -> () {
        join inline_cont3(_wild3: ()) -> () {
          join inline_cont4(_wild4: ()) -> () {
            join inline_cont5(_wild5: ()) -> () {
              join inline_cont6(_wild6: ()) -> () {
                join inline_cont7(_wild7: ()) -> () {
                  join inline_cont8(_wild8: ()) -> () {
                    join inline_cont9(_wild9: ()) -> () {
                      let value/0 = 511 in
                      let t0 = (value/0 as u8) in
                      join inline_cont10(_wild10: ()) -> () {
                        join inline_cont11(_wild11: ()) -> () {
                          join inline_cont12(_wild12: ()) -> () {
                            join inline_cont13(t1: u8) -> () {
                              join inline_cont14(_wild13: ()) -> () {
                                let octet/0 = 255 in
                                let t2 = (octet/0 as i16) in
                                let inline0 = trait_impl#ToString#i16#to_string(t2) in
                                let inline1 = @runtime(core.string_println)(inline0) in
                                ()
                              } in
                              let inline2 = trait_impl#ToString#u8#to_string(t1) in
                              let inline3 = @runtime(core.string_println)(inline2) in
                              jump inline_cont14(())
                            } in
                            let inline4 = -1 in
                            let inline5 = (inline4 as u8) in
                            jump inline_cont13(inline5)
                          } in
                          let inline6 = trait_impl#ToString#u32#to_string(LETTER) in
                          let inline7 = @runtime(core.string_println)(inline6) in
                          jump inline_cont12(())
                        } in
                        let inline8 = trait_impl#ToString#u8#to_string(WRAPPED) in
                        let inline9 = @runtime(core.string_println)(inline8) in
                        jump inline_cont11(())
                      } in
                      let inline10 = trait_impl#ToString#u8#to_string(t0) in
                      let inline11 = @runtime(core.string_println)(inline10) in
                      jump inline_cont10(())
                    } in
                    jump inline_cont9(())
                  } in
                  jump inline_cont8(())
                } in
                jump inline_cont7(())
              } in
              jump inline_cont6(())
            } in
            jump inline_cont5(())
          } in
          jump inline_cont4(())
        } in
        jump inline_cont3(())
      } in
      jump inline_cont2(())
    } in
    jump inline_cont1(())
  } in
  jump inline_cont0(())
}

fn println__T_u8(value/0: u8) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_uint8_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn println__T_u32(value/0: u32) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_uint32_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn println__T_i16(value/0: i16) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int16_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#u8#to_string(self/0: u8) -> string {
  let inline0 = (self/0 as u64) in
  let inline1 = decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#u32#to_string(self/0: u32) -> string {
  let inline0 = (self/0 as u64) in
  let inline1 = decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#i16#to_string(self/0: i16) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_uint8_to_string(value/0: u8) -> string {
  let t0 = (value/0 as u64) in
  let t1 = decimal_string(t0) in
  t1
}

fn __goml_builtin_uint32_to_string(value/0: u32) -> string {
  let t0 = (value/0 as u64) in
  let t1 = decimal_string(t0) in
  t1
}

fn __goml_builtin_int16_to_string(value/0: i16) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}