const NEGATIVE_ONE: i16 = -(1);

const WRAPPED: u8 = 255;

const LETTER: u32 = 65;

fn narrow_at_compile_time(value/0: i16) -> u8 {
  let t902 = (value/0 as u8) in
  t902
}

fn exercise_int(value/1: isize) -> unit {
  ()
}

fn exercise_int8(value/2: i8) -> unit {
  ()
}

fn exercise_int16(value/3: i16) -> unit {
  ()
}

fn exercise_int32(value/4: i32) -> unit {
  ()
}

fn exercise_int64(value/5: i64) -> unit {
  ()
}

fn exercise_uint(value/6: usize) -> unit {
  ()
}

fn exercise_uint8(value/7: u8) -> unit {
  ()
}

fn exercise_uint16(value/8: u16) -> unit {
  ()
}

fn exercise_uint32(value/9: u32) -> unit {
  ()
}

fn exercise_uint64(value/10: u64) -> unit {
  ()
}

fn main() -> unit {
  join inline_cont1023(_wild886: unit) -> unit {
    join inline_cont1021(_wild887: unit) -> unit {
      join inline_cont1019(_wild888: unit) -> unit {
        join inline_cont1017(_wild889: unit) -> unit {
          join inline_cont1015(_wild890: unit) -> unit {
            join inline_cont1013(_wild891: unit) -> unit {
              join inline_cont1011(_wild892: unit) -> unit {
                join inline_cont1009(_wild893: unit) -> unit {
                  join inline_cont1007(_wild894: unit) -> unit {
                    join inline_cont1005(_wild895: unit) -> unit {
                      let value/11 = 511 in
                      let t914 = (value/11 as u8) in
                      join inline_cont1002(_wild896: unit) -> unit {
                        join inline_cont999(_wild897: unit) -> unit {
                          join inline_cont996(_wild898: unit) -> unit {
                            join inline_cont993(t915: u8) -> unit {
                              join inline_cont990(_wild899: unit) -> unit {
                                let octet/12 = 255 in
                                let t916 = (octet/12 as i16) in
                                let inline988 = trait_impl#ToString#i16#to_string(t916) in
                                let inline989 = @runtime(core.string_println)(inline988) in
                                ()
                              } in
                              let inline991 = trait_impl#ToString#u8#to_string(t915) in
                              let inline992 = @runtime(core.string_println)(inline991) in
                              jump inline_cont990(())
                            } in
                            let inline994 = -1 in
                            let inline995 = (inline994 as u8) in
                            jump inline_cont993(inline995)
                          } in
                          let inline997 = trait_impl#ToString#u32#to_string(LETTER) in
                          let inline998 = @runtime(core.string_println)(inline997) in
                          jump inline_cont996(())
                        } in
                        let inline1000 = trait_impl#ToString#u8#to_string(WRAPPED) in
                        let inline1001 = @runtime(core.string_println)(inline1000) in
                        jump inline_cont999(())
                      } in
                      let inline1003 = trait_impl#ToString#u8#to_string(t914) in
                      let inline1004 = @runtime(core.string_println)(inline1003) in
                      jump inline_cont1002(())
                    } in
                    jump inline_cont1005(())
                  } in
                  jump inline_cont1007(())
                } in
                jump inline_cont1009(())
              } in
              jump inline_cont1011(())
            } in
            jump inline_cont1013(())
          } in
          jump inline_cont1015(())
        } in
        jump inline_cont1017(())
      } in
      jump inline_cont1019(())
    } in
    jump inline_cont1021(())
  } in
  jump inline_cont1023(())
}

fn println__T_u8(value/1: u8) -> unit {
  join inline_cont1025(t919: string) -> unit {
    let t920 = @runtime(core.string_println)(t919) in
    ()
  } in
  let inline1026 = __goml_builtin_uint8_to_string(value/1) in
  jump inline_cont1025(inline1026)
}

fn println__T_u32(value/1: u32) -> unit {
  join inline_cont1027(t922: string) -> unit {
    let t923 = @runtime(core.string_println)(t922) in
    ()
  } in
  let inline1028 = __goml_builtin_uint32_to_string(value/1) in
  jump inline_cont1027(inline1028)
}

fn println__T_i16(value/1: i16) -> unit {
  join inline_cont1029(t925: string) -> unit {
    let t926 = @runtime(core.string_println)(t925) in
    ()
  } in
  let inline1030 = __goml_builtin_int16_to_string(value/1) in
  jump inline_cont1029(inline1030)
}

fn trait_impl#ToString#u8#to_string(self/409: u8) -> string {
  let inline1032 = (self/409 as u64) in
  let inline1033 = decimal_string(inline1032) in
  inline1033
}

fn trait_impl#ToString#u32#to_string(self/411: u32) -> string {
  let inline1035 = (self/411 as u64) in
  let inline1036 = decimal_string(inline1035) in
  inline1036
}

fn trait_impl#ToString#i16#to_string(self/406: i16) -> string {
  let inline1038 = (self/406 as i64) in
  let inline1039 = signed_decimal_string(inline1038) in
  inline1039
}

fn __goml_builtin_uint8_to_string(value/228: u8) -> string {
  let t938 = (value/228 as u64) in
  let t939 = decimal_string(t938) in
  t939
}

fn __goml_builtin_uint32_to_string(value/230: u32) -> string {
  let t942 = (value/230 as u64) in
  let t943 = decimal_string(t942) in
  t943
}

fn __goml_builtin_int16_to_string(value/224: i16) -> string {
  let t946 = (value/224 as i64) in
  let inline1041 = (t946 < 0) in
  if inline1041 {
    let inline1042 = (t946 as u64) in
    let inline1043 = (0 - inline1042) in
    let inline1044 = decimal_string(inline1043) in
    let inline1045 = ("-" + inline1044) in
    inline1045
  } else {
    let inline1046 = (t946 as u64) in
    let inline1047 = decimal_string(inline1046) in
    inline1047
  }
}

fn decimal_string(value/208: u64) -> string {
  let t970 = (value/208 == 0) in
  join k950() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit951() -> string {
      join inline_cont1056(t952: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t952) in
        let offset/212 = 0 in
        join exit953() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop954() -> string {
            join inline_cont1054(t955: isize) -> string {
              let t956 = (offset/212 < t955) in
              if t956 {
                join inline_cont1052(t957: isize) -> string {
                  let t958 = (t957 - offset/212) in
                  let t959 = (t958 - 1) in
                  let t960 = @intrinsic(vec.get)(reversed/209, t959) in
                  join inline_cont1050(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t961 = (compound_old358 + compound_value359) in
                    let t962 = offset/212 = t961 in
                    jump loop954()
                  } in
                  let inline1051 = @intrinsic(vec.push)(bytes/211, t960) in
                  jump inline_cont1050(())
                } in
                let inline1053 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont1052(inline1053)
              } else {
                jump exit953()
              }
            } in
            let inline1055 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont1054(inline1055)
          } in
        } in
        jump loop954()
      } in
      let inline1057 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont1056(inline1057)
    } in
    joinrec {
      join loop963() -> string {
        let t964 = (remaining/210 > 0) in
        if t964 {
          let t965 = (remaining/210 % 10) in
          let t966 = (t965 as u8) in
          let t967 = (t966 + 48) in
          join inline_cont1048(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t968 = (compound_old353 / compound_value354) in
            let t969 = remaining/210 = t968 in
            jump loop963()
          } in
          let inline1049 = @intrinsic(vec.push)(reversed/209, t967) in
          jump inline_cont1048(())
        } else {
          jump exit951()
        }
      } in
    } in
    jump loop963()
  } in
  if t970 {
    "0"
  } else {
    jump k950()
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t975 = (value/214 < 0) in
  if t975 {
    let t976 = (value/214 as u64) in
    let t977 = (0 - t976) in
    let t978 = decimal_string(t977) in
    let t979 = ("-" + t978) in
    t979
  } else {
    let t980 = (value/214 as u64) in
    let t981 = decimal_string(t980) in
    t981
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t983 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t986 = @intrinsic(vec.len)(self/526) in
  t986
}