fn main() -> unit {
  join inline_cont270(sum/0: Ref[int]) -> unit {
    join inline_cont267(i/1: Ref[int]) -> unit {
      join exit196() -> unit {
        join inline_cont263(mtmp191: unit) -> unit {
          join inline_cont261(t197: int) -> unit {
            join inline_cont258(mtmp192: unit) -> unit {
              join inline_cont254(mtmp193: unit) -> unit {
                join inline_cont252(t198: int) -> unit {
                  let inline250 = trait_impl#ToString#int#to_string(t198) in
                  let inline251 = @runtime(core.string_println)(inline250) in
                  ()
                } in
                let inline253 = @intrinsic(ref.get)(i/1) in
                jump inline_cont252(inline253)
              } in
              let inline255 = "i at break: " in
              let inline256 = trait_impl#ToString#string#to_string(inline255) in
              let inline257 = @runtime(core.string_print)(inline256) in
              jump inline_cont254(())
            } in
            let inline259 = trait_impl#ToString#int#to_string(t197) in
            let inline260 = @runtime(core.string_println)(inline259) in
            jump inline_cont258(())
          } in
          let inline262 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont261(inline262)
        } in
        let inline264 = "sum: " in
        let inline265 = trait_impl#ToString#string#to_string(inline264) in
        let inline266 = @runtime(core.string_print)(inline265) in
        jump inline_cont263(())
      } in
      joinrec {
        join loop199() -> unit {
          join inline_cont247(t200: int) -> unit {
            let t201 = (t200 < 20) in
            if t201 {
              join inline_cont245(t202: int) -> unit {
                let t203 = (t202 + 1) in
                join inline_cont243(_wild187: unit) -> unit {
                  join inline_cont241(t208: int) -> unit {
                    let t209 = (t208 > 5) in
                    join k204() -> unit {
                      join inline_cont239(t205: int) -> unit {
                        join inline_cont237(t206: int) -> unit {
                          let t207 = (t205 + t206) in
                          let inline236 = @intrinsic(ref.set)(sum/0, t207) in
                          jump loop199()
                        } in
                        let inline238 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont237(inline238)
                      } in
                      let inline240 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont239(inline240)
                    } in
                    if t209 {
                      jump exit196()
                    } else {
                      jump k204()
                    }
                  } in
                  let inline242 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont241(inline242)
                } in
                let inline244 = @intrinsic(ref.set)(i/1, t203) in
                jump inline_cont243(())
              } in
              let inline246 = @intrinsic(ref.get)(i/1) in
              jump inline_cont245(inline246)
            } else {
              jump exit196()
            }
          } in
          let inline248 = @intrinsic(ref.get)(i/1) in
          jump inline_cont247(inline248)
        } in
      } in
      jump loop199()
    } in
    let inline268 = 0 in
    let inline269 = @intrinsic(ref.new)(inline268) in
    jump inline_cont267(inline269)
  } in
  let inline271 = 0 in
  let inline272 = @intrinsic(ref.new)(inline271) in
  jump inline_cont270(inline272)
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t212 = @intrinsic(ref.new)(value/273) in
  t212
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t215 = @intrinsic(ref.get)(self/274) in
  t215
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t217 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont273(t219: string) -> unit {
    let t220 = @runtime(core.string_print)(t219) in
    ()
  } in
  jump inline_cont273(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont274(t222: string) -> unit {
    let t223 = @runtime(core.string_println)(t222) in
    ()
  } in
  let inline275 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont274(inline275)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t228 = @runtime(core.int_to_string)(self/67) in
  t228
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t231 = (self/101 == other/102) in
  t231
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t234 = @runtime(core.int_hash)(self/127) in
  t234
}