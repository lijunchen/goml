fn main() -> unit {
  join inline_cont219(sum/0: Ref[int]) -> unit {
    join inline_cont216(i/1: Ref[int]) -> unit {
      join exit145() -> unit {
        let _wild139 = () in
        join inline_cont212(mtmp140: unit) -> unit {
          join inline_cont210(t146: int) -> unit {
            join inline_cont207(mtmp141: unit) -> unit {
              join inline_cont203(mtmp142: unit) -> unit {
                join inline_cont201(t147: int) -> unit {
                  let inline199 = trait_impl#ToString#int#to_string(t147) in
                  let inline200 = @runtime(core.string_println)(inline199) in
                  ()
                } in
                let inline202 = @intrinsic(ref.get)(i/1) in
                jump inline_cont201(inline202)
              } in
              let inline204 = "i at break: " in
              let inline205 = trait_impl#ToString#string#to_string(inline204) in
              let inline206 = @runtime(core.string_print)(inline205) in
              jump inline_cont203(())
            } in
            let inline208 = trait_impl#ToString#int#to_string(t146) in
            let inline209 = @runtime(core.string_println)(inline208) in
            jump inline_cont207(())
          } in
          let inline211 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont210(inline211)
        } in
        let inline213 = "sum: " in
        let inline214 = trait_impl#ToString#string#to_string(inline213) in
        let inline215 = @runtime(core.string_print)(inline214) in
        jump inline_cont212(())
      } in
      joinrec {
        join loop148() -> unit {
          join inline_cont196(t149: int) -> unit {
            let t150 = (t149 < 20) in
            if t150 {
              join inline_cont194(t151: int) -> unit {
                let t152 = (t151 + 1) in
                join inline_cont192(_wild136: unit) -> unit {
                  join inline_cont190(t157: int) -> unit {
                    let t158 = (t157 > 5) in
                    join k153() -> unit {
                      let _wild137 = () in
                      join inline_cont188(t154: int) -> unit {
                        join inline_cont186(t155: int) -> unit {
                          let t156 = (t154 + t155) in
                          let inline185 = @intrinsic(ref.set)(sum/0, t156) in
                          jump loop148()
                        } in
                        let inline187 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont186(inline187)
                      } in
                      let inline189 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont188(inline189)
                    } in
                    if t158 {
                      jump exit145()
                    } else {
                      jump k153()
                    }
                  } in
                  let inline191 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont190(inline191)
                } in
                let inline193 = @intrinsic(ref.set)(i/1, t152) in
                jump inline_cont192(())
              } in
              let inline195 = @intrinsic(ref.get)(i/1) in
              jump inline_cont194(inline195)
            } else {
              jump exit145()
            }
          } in
          let inline197 = @intrinsic(ref.get)(i/1) in
          jump inline_cont196(inline197)
        } in
      } in
      jump loop148()
    } in
    let inline217 = 0 in
    let inline218 = @intrinsic(ref.new)(inline217) in
    jump inline_cont216(inline218)
  } in
  let inline220 = 0 in
  let inline221 = @intrinsic(ref.new)(inline220) in
  jump inline_cont219(inline221)
}

fn inherent#Ref#Ref[T]#new__T_int(value/215: int) -> Ref[int] {
  let t161 = @intrinsic(ref.new)(value/215) in
  t161
}

fn inherent#Ref#Ref[T]#get__T_int(self/216: Ref[int]) -> int {
  let t164 = @intrinsic(ref.get)(self/216) in
  t164
}

fn inherent#Ref#Ref[T]#set__T_int(self/217: Ref[int], value/218: int) -> unit {
  let t166 = @intrinsic(ref.set)(self/217, value/218) in
  ()
}

fn print__T_string(value/30: string) -> unit {
  join inline_cont222(t168: string) -> unit {
    let t169 = @runtime(core.string_print)(t168) in
    ()
  } in
  jump inline_cont222(value/30)
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont223(t171: string) -> unit {
    let t172 = @runtime(core.string_println)(t171) in
    ()
  } in
  let inline224 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont223(inline224)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t177 = @runtime(core.int_to_string)(self/69) in
  t177
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t180 = @intrinsic(eq.primitive)(self/88, other/89) in
  t180
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t183 = @runtime(core.int_hash)(self/114) in
  t183
}