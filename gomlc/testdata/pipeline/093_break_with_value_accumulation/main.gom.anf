fn main() -> unit {
  join inline_cont260(sum/0: Ref[int]) -> unit {
    join inline_cont257(i/1: Ref[int]) -> unit {
      join exit186() -> unit {
        let _wild180 = () in
        join inline_cont253(mtmp181: unit) -> unit {
          join inline_cont251(t187: int) -> unit {
            join inline_cont248(mtmp182: unit) -> unit {
              join inline_cont244(mtmp183: unit) -> unit {
                join inline_cont242(t188: int) -> unit {
                  let inline240 = trait_impl#ToString#int#to_string(t188) in
                  let inline241 = @runtime(core.string_println)(inline240) in
                  ()
                } in
                let inline243 = @intrinsic(ref.get)(i/1) in
                jump inline_cont242(inline243)
              } in
              let inline245 = "i at break: " in
              let inline246 = trait_impl#ToString#string#to_string(inline245) in
              let inline247 = @runtime(core.string_print)(inline246) in
              jump inline_cont244(())
            } in
            let inline249 = trait_impl#ToString#int#to_string(t187) in
            let inline250 = @runtime(core.string_println)(inline249) in
            jump inline_cont248(())
          } in
          let inline252 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont251(inline252)
        } in
        let inline254 = "sum: " in
        let inline255 = trait_impl#ToString#string#to_string(inline254) in
        let inline256 = @runtime(core.string_print)(inline255) in
        jump inline_cont253(())
      } in
      joinrec {
        join loop189() -> unit {
          join inline_cont237(t190: int) -> unit {
            let t191 = (t190 < 20) in
            if t191 {
              join inline_cont235(t192: int) -> unit {
                let t193 = (t192 + 1) in
                join inline_cont233(_wild177: unit) -> unit {
                  join inline_cont231(t198: int) -> unit {
                    let t199 = (t198 > 5) in
                    join k194() -> unit {
                      let _wild178 = () in
                      join inline_cont229(t195: int) -> unit {
                        join inline_cont227(t196: int) -> unit {
                          let t197 = (t195 + t196) in
                          let inline226 = @intrinsic(ref.set)(sum/0, t197) in
                          jump loop189()
                        } in
                        let inline228 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont227(inline228)
                      } in
                      let inline230 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont229(inline230)
                    } in
                    if t199 {
                      jump exit186()
                    } else {
                      jump k194()
                    }
                  } in
                  let inline232 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont231(inline232)
                } in
                let inline234 = @intrinsic(ref.set)(i/1, t193) in
                jump inline_cont233(())
              } in
              let inline236 = @intrinsic(ref.get)(i/1) in
              jump inline_cont235(inline236)
            } else {
              jump exit186()
            }
          } in
          let inline238 = @intrinsic(ref.get)(i/1) in
          jump inline_cont237(inline238)
        } in
      } in
      jump loop189()
    } in
    let inline258 = 0 in
    let inline259 = @intrinsic(ref.new)(inline258) in
    jump inline_cont257(inline259)
  } in
  let inline261 = 0 in
  let inline262 = @intrinsic(ref.new)(inline261) in
  jump inline_cont260(inline262)
}

fn inherent#Ref#Ref[T]#new__T_int(value/236: int) -> Ref[int] {
  let t202 = @intrinsic(ref.new)(value/236) in
  t202
}

fn inherent#Ref#Ref[T]#get__T_int(self/237: Ref[int]) -> int {
  let t205 = @intrinsic(ref.get)(self/237) in
  t205
}

fn inherent#Ref#Ref[T]#set__T_int(self/238: Ref[int], value/239: int) -> unit {
  let t207 = @intrinsic(ref.set)(self/238, value/239) in
  ()
}

fn print__T_string(value/30: string) -> unit {
  join inline_cont263(t209: string) -> unit {
    let t210 = @runtime(core.string_print)(t209) in
    ()
  } in
  jump inline_cont263(value/30)
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont264(t212: string) -> unit {
    let t213 = @runtime(core.string_println)(t212) in
    ()
  } in
  let inline265 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont264(inline265)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t218 = @runtime(core.int_to_string)(self/69) in
  t218
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t221 = @intrinsic(eq.primitive)(self/88, other/89) in
  t221
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t224 = @runtime(core.int_hash)(self/114) in
  t224
}