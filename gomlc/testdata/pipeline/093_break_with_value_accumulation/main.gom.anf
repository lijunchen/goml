fn main() -> unit {
  join inline_cont491(sum/0: Ref[int]) -> unit {
    join inline_cont488(i/1: Ref[int]) -> unit {
      join exit417() -> unit {
        join inline_cont484(mtmp412: unit) -> unit {
          join inline_cont482(t418: int) -> unit {
            join inline_cont479(mtmp413: unit) -> unit {
              join inline_cont475(mtmp414: unit) -> unit {
                join inline_cont473(t419: int) -> unit {
                  let inline471 = trait_impl#ToString#int#to_string(t419) in
                  let inline472 = @runtime(core.string_println)(inline471) in
                  ()
                } in
                let inline474 = @intrinsic(ref.get)(i/1) in
                jump inline_cont473(inline474)
              } in
              let inline476 = "i at break: " in
              let inline477 = trait_impl#ToString#string#to_string(inline476) in
              let inline478 = @runtime(core.string_print)(inline477) in
              jump inline_cont475(())
            } in
            let inline480 = trait_impl#ToString#int#to_string(t418) in
            let inline481 = @runtime(core.string_println)(inline480) in
            jump inline_cont479(())
          } in
          let inline483 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont482(inline483)
        } in
        let inline485 = "sum: " in
        let inline486 = trait_impl#ToString#string#to_string(inline485) in
        let inline487 = @runtime(core.string_print)(inline486) in
        jump inline_cont484(())
      } in
      joinrec {
        join loop420() -> unit {
          join inline_cont468(t421: int) -> unit {
            let t422 = (t421 < 20) in
            if t422 {
              join inline_cont466(t423: int) -> unit {
                let t424 = (t423 + 1) in
                join inline_cont464(_wild408: unit) -> unit {
                  join inline_cont462(t429: int) -> unit {
                    let t430 = (t429 > 5) in
                    join k425() -> unit {
                      join inline_cont460(t426: int) -> unit {
                        join inline_cont458(t427: int) -> unit {
                          let t428 = (t426 + t427) in
                          let inline457 = @intrinsic(ref.set)(sum/0, t428) in
                          jump loop420()
                        } in
                        let inline459 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont458(inline459)
                      } in
                      let inline461 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont460(inline461)
                    } in
                    if t430 {
                      jump exit417()
                    } else {
                      jump k425()
                    }
                  } in
                  let inline463 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont462(inline463)
                } in
                let inline465 = @intrinsic(ref.set)(i/1, t424) in
                jump inline_cont464(())
              } in
              let inline467 = @intrinsic(ref.get)(i/1) in
              jump inline_cont466(inline467)
            } else {
              jump exit417()
            }
          } in
          let inline469 = @intrinsic(ref.get)(i/1) in
          jump inline_cont468(inline469)
        } in
      } in
      jump loop420()
    } in
    let inline489 = 0 in
    let inline490 = @intrinsic(ref.new)(inline489) in
    jump inline_cont488(inline490)
  } in
  let inline492 = 0 in
  let inline493 = @intrinsic(ref.new)(inline492) in
  jump inline_cont491(inline493)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t433 = @intrinsic(ref.new)(value/431) in
  t433
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t436 = @intrinsic(ref.get)(self/432) in
  t436
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t438 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont494(t440: string) -> unit {
    let t441 = @runtime(core.string_print)(t440) in
    ()
  } in
  jump inline_cont494(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont495(t443: string) -> unit {
    let t444 = @runtime(core.string_println)(t443) in
    ()
  } in
  let inline496 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont495(inline496)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t449 = @runtime(core.int_to_string)(self/151) in
  t449
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t452 = (self/185 == other/186) in
  t452
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t455 = @runtime(core.int_hash)(self/211) in
  t455
}