fn main() -> unit {
  join inline_cont922(sum/0: Ref[isize]) -> unit {
    join inline_cont919(i/1: Ref[isize]) -> unit {
      join exit805() -> unit {
        join inline_cont915(mtmp800: unit) -> unit {
          join inline_cont913(t806: isize) -> unit {
            join inline_cont910(mtmp801: unit) -> unit {
              join inline_cont906(mtmp802: unit) -> unit {
                join inline_cont904(t807: isize) -> unit {
                  let inline902 = trait_impl#ToString#isize#to_string(t807) in
                  let inline903 = @runtime(core.string_println)(inline902) in
                  ()
                } in
                let inline905 = @intrinsic(ref.get)(i/1) in
                jump inline_cont904(inline905)
              } in
              let inline907 = "i at break: " in
              let inline908 = trait_impl#ToString#string#to_string(inline907) in
              let inline909 = @runtime(core.string_print)(inline908) in
              jump inline_cont906(())
            } in
            let inline911 = trait_impl#ToString#isize#to_string(t806) in
            let inline912 = @runtime(core.string_println)(inline911) in
            jump inline_cont910(())
          } in
          let inline914 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont913(inline914)
        } in
        let inline916 = "sum: " in
        let inline917 = trait_impl#ToString#string#to_string(inline916) in
        let inline918 = @runtime(core.string_print)(inline917) in
        jump inline_cont915(())
      } in
      joinrec {
        join loop808() -> unit {
          join inline_cont899(t809: isize) -> unit {
            let t810 = (t809 < 20) in
            if t810 {
              join inline_cont897(t811: isize) -> unit {
                let t812 = (t811 + 1) in
                join inline_cont895(_wild796: unit) -> unit {
                  join inline_cont893(t817: isize) -> unit {
                    let t818 = (t817 > 5) in
                    join k813() -> unit {
                      join inline_cont891(t814: isize) -> unit {
                        join inline_cont889(t815: isize) -> unit {
                          let t816 = (t814 + t815) in
                          let inline888 = @intrinsic(ref.set)(sum/0, t816) in
                          jump loop808()
                        } in
                        let inline890 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont889(inline890)
                      } in
                      let inline892 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont891(inline892)
                    } in
                    if t818 {
                      jump exit805()
                    } else {
                      jump k813()
                    }
                  } in
                  let inline894 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont893(inline894)
                } in
                let inline896 = @intrinsic(ref.set)(i/1, t812) in
                jump inline_cont895(())
              } in
              let inline898 = @intrinsic(ref.get)(i/1) in
              jump inline_cont897(inline898)
            } else {
              jump exit805()
            }
          } in
          let inline900 = @intrinsic(ref.get)(i/1) in
          jump inline_cont899(inline900)
        } in
      } in
      jump loop808()
    } in
    let inline920 = 0 in
    let inline921 = @intrinsic(ref.new)(inline920) in
    jump inline_cont919(inline921)
  } in
  let inline923 = 0 in
  let inline924 = @intrinsic(ref.new)(inline923) in
  jump inline_cont922(inline924)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t821 = @intrinsic(ref.new)(value/684) in
  t821
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t824 = @intrinsic(ref.get)(self/685) in
  t824
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t826 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont925(t828: string) -> unit {
    let t829 = @runtime(core.string_print)(t828) in
    ()
  } in
  jump inline_cont925(value/0)
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont926(t831: string) -> unit {
    let t832 = @runtime(core.string_println)(t831) in
    ()
  } in
  let inline927 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont926(inline927)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline929 = (self/404 as i64) in
  let inline930 = signed_decimal_string(inline929) in
  inline930
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t840 = (value/222 as i64) in
  let inline932 = (t840 < 0) in
  if inline932 {
    let inline933 = (t840 as u64) in
    let inline934 = (0 - inline933) in
    let inline935 = decimal_string(inline934) in
    let inline936 = ("-" + inline935) in
    inline936
  } else {
    let inline937 = (t840 as u64) in
    let inline938 = decimal_string(inline937) in
    inline938
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t846 = (value/214 < 0) in
  if t846 {
    let t847 = (value/214 as u64) in
    let t848 = (0 - t847) in
    let t849 = decimal_string(t848) in
    let t850 = ("-" + t849) in
    t850
  } else {
    let t851 = (value/214 as u64) in
    let t852 = decimal_string(t851) in
    t852
  }
}

fn decimal_string(value/208: u64) -> string {
  let t875 = (value/208 == 0) in
  join k855() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit856() -> string {
      join inline_cont947(t857: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t857) in
        let offset/212 = 0 in
        join exit858() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop859() -> string {
            join inline_cont945(t860: isize) -> string {
              let t861 = (offset/212 < t860) in
              if t861 {
                join inline_cont943(t862: isize) -> string {
                  let t863 = (t862 - offset/212) in
                  let t864 = (t863 - 1) in
                  let t865 = @intrinsic(vec.get)(reversed/209, t864) in
                  join inline_cont941(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t866 = (compound_old358 + compound_value359) in
                    let t867 = offset/212 = t866 in
                    jump loop859()
                  } in
                  let inline942 = @intrinsic(vec.push)(bytes/211, t865) in
                  jump inline_cont941(())
                } in
                let inline944 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont943(inline944)
              } else {
                jump exit858()
              }
            } in
            let inline946 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont945(inline946)
          } in
        } in
        jump loop859()
      } in
      let inline948 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont947(inline948)
    } in
    joinrec {
      join loop868() -> string {
        let t869 = (remaining/210 > 0) in
        if t869 {
          let t870 = (remaining/210 % 10) in
          let t871 = (t870 as u8) in
          let t872 = (t871 + 48) in
          join inline_cont939(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t873 = (compound_old353 / compound_value354) in
            let t874 = remaining/210 = t873 in
            jump loop868()
          } in
          let inline940 = @intrinsic(vec.push)(reversed/209, t872) in
          jump inline_cont939(())
        } else {
          jump exit856()
        }
      } in
    } in
    jump loop868()
  } in
  if t875 {
    "0"
  } else {
    jump k855()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t877 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t880 = @intrinsic(vec.len)(self/526) in
  t880
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t883 = (self/438 == other/439) in
  t883
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t886 = @runtime(core.int_hash)(self/464) in
  t886
}