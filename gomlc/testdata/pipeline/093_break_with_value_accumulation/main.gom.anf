fn main() -> unit {
  join inline_cont265(sum/0: Ref[int]) -> unit {
    join inline_cont262(i/1: Ref[int]) -> unit {
      join exit191() -> unit {
        join inline_cont258(mtmp186: unit) -> unit {
          join inline_cont256(t192: int) -> unit {
            join inline_cont253(mtmp187: unit) -> unit {
              join inline_cont249(mtmp188: unit) -> unit {
                join inline_cont247(t193: int) -> unit {
                  let inline245 = trait_impl#ToString#int#to_string(t193) in
                  let inline246 = @runtime(core.string_println)(inline245) in
                  ()
                } in
                let inline248 = @intrinsic(ref.get)(i/1) in
                jump inline_cont247(inline248)
              } in
              let inline250 = "i at break: " in
              let inline251 = trait_impl#ToString#string#to_string(inline250) in
              let inline252 = @runtime(core.string_print)(inline251) in
              jump inline_cont249(())
            } in
            let inline254 = trait_impl#ToString#int#to_string(t192) in
            let inline255 = @runtime(core.string_println)(inline254) in
            jump inline_cont253(())
          } in
          let inline257 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont256(inline257)
        } in
        let inline259 = "sum: " in
        let inline260 = trait_impl#ToString#string#to_string(inline259) in
        let inline261 = @runtime(core.string_print)(inline260) in
        jump inline_cont258(())
      } in
      joinrec {
        join loop194() -> unit {
          join inline_cont242(t195: int) -> unit {
            let t196 = (t195 < 20) in
            if t196 {
              join inline_cont240(t197: int) -> unit {
                let t198 = (t197 + 1) in
                join inline_cont238(_wild182: unit) -> unit {
                  join inline_cont236(t203: int) -> unit {
                    let t204 = (t203 > 5) in
                    join k199() -> unit {
                      join inline_cont234(t200: int) -> unit {
                        join inline_cont232(t201: int) -> unit {
                          let t202 = (t200 + t201) in
                          let inline231 = @intrinsic(ref.set)(sum/0, t202) in
                          jump loop194()
                        } in
                        let inline233 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont232(inline233)
                      } in
                      let inline235 = @intrinsic(ref.get)(sum/0) in
                      jump inline_cont234(inline235)
                    } in
                    if t204 {
                      jump exit191()
                    } else {
                      jump k199()
                    }
                  } in
                  let inline237 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont236(inline237)
                } in
                let inline239 = @intrinsic(ref.set)(i/1, t198) in
                jump inline_cont238(())
              } in
              let inline241 = @intrinsic(ref.get)(i/1) in
              jump inline_cont240(inline241)
            } else {
              jump exit191()
            }
          } in
          let inline243 = @intrinsic(ref.get)(i/1) in
          jump inline_cont242(inline243)
        } in
      } in
      jump loop194()
    } in
    let inline263 = 0 in
    let inline264 = @intrinsic(ref.new)(inline263) in
    jump inline_cont262(inline264)
  } in
  let inline266 = 0 in
  let inline267 = @intrinsic(ref.new)(inline266) in
  jump inline_cont265(inline267)
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t207 = @intrinsic(ref.new)(value/270) in
  t207
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t210 = @intrinsic(ref.get)(self/271) in
  t210
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t212 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont268(t214: string) -> unit {
    let t215 = @runtime(core.string_print)(t214) in
    ()
  } in
  jump inline_cont268(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont269(t217: string) -> unit {
    let t218 = @runtime(core.string_println)(t217) in
    ()
  } in
  let inline270 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont269(inline270)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t223 = @runtime(core.int_to_string)(self/67) in
  t223
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t226 = (self/101 == other/102) in
  t226
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t229 = @runtime(core.int_hash)(self/127) in
  t229
}