package main
file main.gom
  fn parse_flag(local/1/0: bool) -> Result[i32, string] {
      if flag/0 {
          Result::Ok(7)
      } else {
          Result::Err("nope")
      }
  }

  fn add(local/1/1: i32, local/1/2: i32) -> i32 {
      a/1 + b/2
  }

  fn plus_one(local/1/3: bool) -> Result[i32, string] {
      let local/1/5 =
        || {
            let local/1/4 = def/1/0(flag/3)?;
            Result::Ok(def/1/1(value/4, 1))
        };
      run/5()
  }

  fn show(local/1/6: Result[i32, string]) -> string {
      match res/6 {
          Result::Ok(local/1/7) => "ok=" + value/7.to_string(),
          Result::Err(local/1/8) => "err=" + err/8,
      }
  }

  fn main() -> () {
      println(def/1/3(def/1/2(true)));
      println(def/1/3(def/1/2(false)));
  }