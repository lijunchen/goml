fn combine(left/0: usize, right/0: usize) -> usize {
  let t0 = (left/0 + right/0) in
  let t1 = (t0 * 2) in
  t1
}

fn classify(value/0: usize) -> string {
  match value/0 {
    0 => {
      "zero"
    },
    42 => {
      "answer"
    },
    _ => {
      "other"
    },
  }
}

fn main() -> unit {
  let left/0 = 19 in
  let right/0 = (2 as usize) in
  join inline_cont0(result/0: usize) -> unit {
    join inline_cont1(_wild0: unit) -> unit {
      join inline_cont2(t0: string) -> unit {
        join inline_cont3(_wild1: unit) -> unit {
          join inline_cont4(t1: string) -> unit {
            join inline_cont5(_wild2: unit) -> unit {
              let t2 = (result/0 > left/0) in
              join inline_cont6(_wild3: unit) -> unit {
                let t3 = (result/0 & 15) in
                let t4 = (t3 as u64) in
                join inline_cont7(_wild4: unit) -> unit {
                  join inline_cont8(values/0: HashMap[usize, string]) -> unit {
                    join inline_cont9(_wild5: unit) -> unit {
                      join inline_cont10(mtmp0: Option__string) -> unit {
                        match mtmp0 {
                          Tag_0 => {
                            let inline0 = "missing" in
                            let inline1 = trait_impl#ToString#string#to_string(inline0) in
                            let inline2 = @runtime(core.string_println)(inline1) in
                            ()
                          },
                          Tag_1 => {
                            let x0 = Option__string::Some._0(mtmp0) in
                            let inline3 = trait_impl#ToString#string#to_string(x0) in
                            let inline4 = @runtime(core.string_println)(inline3) in
                            ()
                          },
                        }
                      } in
                      let inline5 = 42 in
                      let inline6 = @intrinsic(hashmap.get)(values/0, inline5) in
                      jump inline_cont10(inline6)
                    } in
                    let inline7 = "stored" in
                    let inline8 = @intrinsic(hashmap.set)(values/0, result/0, inline7) in
                    jump inline_cont9(())
                  } in
                  let inline9 = @intrinsic(hashmap.new)() in
                  jump inline_cont8(inline9)
                } in
                let inline10 = trait_impl#ToString#u64#to_string(t4) in
                let inline11 = @runtime(core.string_println)(inline10) in
                jump inline_cont7(())
              } in
              let inline12 = trait_impl#ToString#bool#to_string(t2) in
              let inline13 = @runtime(core.string_println)(inline12) in
              jump inline_cont6(())
            } in
            let inline14 = trait_impl#ToString#string#to_string(t1) in
            let inline15 = @runtime(core.string_println)(inline14) in
            jump inline_cont5(())
          } in
          match result/0 {
            0 => {
              jump inline_cont4("zero")
            },
            42 => {
              jump inline_cont4("answer")
            },
            _ => {
              jump inline_cont4("other")
            },
          }
        } in
        let inline16 = trait_impl#ToString#string#to_string(t0) in
        let inline17 = @runtime(core.string_println)(inline16) in
        jump inline_cont3(())
      } in
      let inline18 = __goml_builtin_uint_to_string(result/0) in
      jump inline_cont2(inline18)
    } in
    let inline19 = trait_impl#ToString#usize#to_string(result/0) in
    let inline20 = @runtime(core.string_println)(inline19) in
    jump inline_cont1(())
  } in
  let inline21 = (left/0 + right/0) in
  let inline22 = (inline21 * 2) in
  jump inline_cont0(inline22)
}

fn println__T_usize(value/0: usize) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_uint_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#usize#to_string(self/0: usize) -> string {
  let inline0 = (self/0 as u64) in
  let inline1 = decimal_string(inline0) in
  inline1
}

fn println__T_bool(value/0: bool) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn println__T_u64(value/0: u64) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_uint64_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn inherent#HashMap#HashMap[K,V]#new__K_usize__V_string() -> HashMap[usize, string] {
  let t0 = @intrinsic(hashmap.new)() in
  t0
}

fn inherent#HashMap#HashMap[K,V]#set__K_usize__V_string(
  self/0: HashMap[usize, string],
  key/0: usize,
  value/0: string
) -> unit {
  let t0 = @intrinsic(hashmap.set)(self/0, key/0, value/0) in
  ()
}

fn inherent#HashMap#HashMap[K,V]#get__K_usize__V_string(
  self/0: HashMap[usize, string],
  key/0: usize
) -> Option__string {
  let t0 = @intrinsic(hashmap.get)(self/0, key/0) in
  t0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_uint_to_string(value/0: usize) -> string {
  let t0 = (value/0 as u64) in
  let t1 = decimal_string(t0) in
  t1
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn trait_impl#ToString#u64#to_string(self/0: u64) -> string {
  let inline0 = decimal_string(self/0) in
  inline0
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn __goml_builtin_uint64_to_string(value/0: u64) -> string {
  let t0 = decimal_string(value/0) in
  t0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#usize#eq(self/0: usize, other/0: usize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#usize#hash(self/0: usize) -> u64 {
  let t0 = @runtime(core.uint_hash)(self/0) in
  t0
}