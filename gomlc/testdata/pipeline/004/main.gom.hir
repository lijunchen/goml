package main
file main.gom
  enum Color {
        Red,
        Green,
        Blue
    }

    fn main() -> bool {
        let local/1/0 = (ctor(def/1/0::v2), ctor(def/1/0::v2));
        match a/0 {
            (Red, Green) => true,
            (Red, Red) => true,
            (Blue, Blue) => {
                  let _ = print(true);
                  false
              },
            _ => false,
        }
    }