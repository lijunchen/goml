package main
file main.gom
  trait Display {
    fn show(Self) -> string;
  }

  impl Display for i32 {
    fn show(local/1/0: i32) -> string {
        self/0.to_string()
    }
  }

  fn mk_renderer[T: Display](local/1/1: T) -> () -> string {
      let local/1/2: dyn Display = x/1 as dyn Display;
      || Display::show(d/2)
  }

  fn main() {
      let local/1/3: i32 = 42;
      let local/1/4 = def/1/2(value/3);
      println(f/4());
  }