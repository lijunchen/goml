fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_isize(0) in
  join exit427() -> unit {
    join inline_cont529(t428: isize) -> unit {
      join inline_cont526(_wild417: unit) -> unit {
        join inline_cont523(j/2: Ref[isize]) -> unit {
          join inline_cont520(total2/3: Ref[isize]) -> unit {
            join exit429() -> unit {
              join inline_cont518(t430: isize) -> unit {
                let inline516 = trait_impl#ToString#isize#to_string(t430) in
                let inline517 = @runtime(core.string_println)(inline516) in
                ()
              } in
              let inline519 = @intrinsic(ref.get)(total2/3) in
              jump inline_cont518(inline519)
            } in
            joinrec {
              join loop431() -> unit {
                join inline_cont513(mtmp418: isize) -> unit {
                  join k432(jp433: bool) -> unit {
                    if jp433 {
                      join inline_cont511(t434: isize) -> unit {
                        join inline_cont509(t435: isize) -> unit {
                          let t436 = (t434 + t435) in
                          join inline_cont507(_wild421: unit) -> unit {
                            join inline_cont505(t438: isize) -> unit {
                              let t439 = (t438 == 2) in
                              join k437() -> unit {
                                jump loop431()
                              } in
                              if t439 {
                                jump exit429()
                              } else {
                                jump k437()
                              }
                            } in
                            let inline506 = @intrinsic(ref.get)(j/2) in
                            jump inline_cont505(inline506)
                          } in
                          let inline508 = @intrinsic(ref.set)(total2/3, t436) in
                          jump inline_cont507(())
                        } in
                        let inline510 = @intrinsic(ref.get)(j/2) in
                        jump inline_cont509(inline510)
                      } in
                      let inline512 = @intrinsic(ref.get)(total2/3) in
                      jump inline_cont511(inline512)
                    } else {
                      jump exit429()
                    }
                  } in
                  match mtmp418 {
                    0 => {
                      let inline500 = 1 in
                      let inline501 = @intrinsic(ref.set)(j/2, inline500) in
                      jump k432(true)
                    },
                    1 => {
                      let inline503 = 2 in
                      let inline504 = @intrinsic(ref.set)(j/2, inline503) in
                      jump k432(true)
                    },
                    2 => {
                      jump k432(true)
                    },
                    _ => {
                      jump k432(false)
                    },
                  }
                } in
                let inline514 = @intrinsic(ref.get)(j/2) in
                jump inline_cont513(inline514)
              } in
            } in
            jump loop431()
          } in
          let inline521 = 0 in
          let inline522 = @intrinsic(ref.new)(inline521) in
          jump inline_cont520(inline522)
        } in
        let inline524 = 0 in
        let inline525 = @intrinsic(ref.new)(inline524) in
        jump inline_cont523(inline525)
      } in
      let inline527 = trait_impl#ToString#isize#to_string(t428) in
      let inline528 = @runtime(core.string_println)(inline527) in
      jump inline_cont526(())
    } in
    let inline530 = @intrinsic(ref.get)(total/1) in
    jump inline_cont529(inline530)
  } in
  joinrec {
    join loop440() -> unit {
      join inline_cont497(t451: isize) -> unit {
        let t452 = (t451 == 0) in
        join k441(jp442: bool) -> unit {
          if jp442 {
            join inline_cont495(t443: isize) -> unit {
              join inline_cont493(t444: isize) -> unit {
                let t445 = (t443 + t444) in
                join inline_cont491(_wild412: unit) -> unit {
                  join inline_cont489(t449: isize) -> unit {
                    let t450 = (t449 == 1) in
                    join k446() -> unit {
                      join inline_cont487(t447: isize) -> unit {
                        let t448 = (t447 + 1) in
                        let inline486 = @intrinsic(ref.set)(i/0, t448) in
                        jump loop440()
                      } in
                      let inline488 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont487(inline488)
                    } in
                    if t450 {
                      let inline483 = 2 in
                      let inline484 = @intrinsic(ref.set)(i/0, inline483) in
                      jump loop440()
                    } else {
                      jump k446()
                    }
                  } in
                  let inline490 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont489(inline490)
                } in
                let inline492 = @intrinsic(ref.set)(total/1, t445) in
                jump inline_cont491(())
              } in
              let inline494 = @intrinsic(ref.get)(i/0) in
              jump inline_cont493(inline494)
            } in
            let inline496 = @intrinsic(ref.get)(total/1) in
            jump inline_cont495(inline496)
          } else {
            jump exit427()
          }
        } in
        if t452 {
          let inline478 = 1 in
          let inline479 = @intrinsic(ref.set)(i/0, inline478) in
          jump k441(true)
        } else {
          join inline_cont480(t455: isize) -> unit {
            let t456 = (t455 < 4) in
            if t456 {
              jump k441(true)
            } else {
              jump k441(false)
            }
          } in
          let inline481 = @intrinsic(ref.get)(i/0) in
          jump inline_cont480(inline481)
        }
      } in
      let inline498 = @intrinsic(ref.get)(i/0) in
      jump inline_cont497(inline498)
    } in
  } in
  jump loop440()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/431: isize) -> Ref[isize] {
  let t459 = @intrinsic(ref.new)(value/431) in
  t459
}

fn inherent#Ref#Ref[T]#get__T_isize(self/432: Ref[isize]) -> isize {
  let t462 = @intrinsic(ref.get)(self/432) in
  t462
}

fn inherent#Ref#Ref[T]#set__T_isize(self/433: Ref[isize], value/434: isize) -> unit {
  let t464 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont531(t466: string) -> unit {
    let t467 = @runtime(core.string_println)(t466) in
    ()
  } in
  let inline532 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont531(inline532)
}

fn trait_impl#ToString#isize#to_string(self/151: isize) -> string {
  let t470 = @runtime(core.int_to_string)(self/151) in
  t470
}

fn trait_impl#PartialEq#isize#eq(self/185: isize, other/186: isize) -> bool {
  let t473 = (self/185 == other/186) in
  t473
}

fn trait_impl#Hash#isize#hash(self/211: isize) -> u64 {
  let t476 = @runtime(core.int_hash)(self/211) in
  t476
}