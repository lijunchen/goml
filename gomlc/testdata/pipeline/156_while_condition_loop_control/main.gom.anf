fn main() -> unit {
  join ret123() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit124() -> unit {
    let _wild113 = () in
    let t125 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild114 = println__T_int(t125) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total2/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit126() -> unit {
      let _wild121 = () in
      let t127 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
      let _wild122 = println__T_int(t127) in
      jump ret123()
    } in
    joinrec {
      join loop128() -> unit {
        let mtmp115 = inherent#Ref#Ref[T]#get__T_int(j/2) in
        join k129(jp130: bool) -> unit {
          if jp130 {
            let t131 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
            let t132 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t133 = (t131 + t132) in
            let _wild118 = inherent#Ref#Ref[T]#set__T_int(total2/3, t133) in
            let t135 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t136 = trait_impl#Eq#int#eq(t135, 2) in
            join k134() -> unit {
              let _wild120 = () in
              jump loop128()
            } in
            if t136 {
              jump exit126()
            } else {
              jump k134()
            }
          } else {
            jump exit126()
          }
        } in
        match mtmp115 {
          0 => {
            let _wild116 = inherent#Ref#Ref[T]#set__T_int(j/2, 1) in
            jump k129(true)
          },
          1 => {
            let _wild117 = inherent#Ref#Ref[T]#set__T_int(j/2, 2) in
            jump k129(true)
          },
          2 => {
            jump k129(true)
          },
          _ => {
            jump k129(false)
          },
        }
      } in
    } in
    jump loop128()
  } in
  joinrec {
    join loop137() -> unit {
      let t148 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t149 = trait_impl#Eq#int#eq(t148, 0) in
      join k138(jp139: bool) -> unit {
        if jp139 {
          let t140 = inherent#Ref#Ref[T]#get__T_int(total/1) in
          let t141 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t142 = (t140 + t141) in
          let _wild109 = inherent#Ref#Ref[T]#set__T_int(total/1, t142) in
          let t146 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t147 = trait_impl#Eq#int#eq(t146, 1) in
          join k143() -> unit {
            let _wild111 = () in
            let t144 = inherent#Ref#Ref[T]#get__T_int(i/0) in
            let t145 = (t144 + 1) in
            let _wild112 = inherent#Ref#Ref[T]#set__T_int(i/0, t145) in
            jump loop137()
          } in
          if t147 {
            let _wild110 = inherent#Ref#Ref[T]#set__T_int(i/0, 2) in
            jump loop137()
          } else {
            jump k143()
          }
        } else {
          jump exit124()
        }
      } in
      if t149 {
        let _wild108 = inherent#Ref#Ref[T]#set__T_int(i/0, 1) in
        jump k138(true)
      } else {
        let t152 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t153 = (t152 < 4) in
        join k150(jp151: bool) -> unit {
          jump k138(jp151)
        } in
        if t153 {
          jump k150(true)
        } else {
          jump k150(false)
        }
      }
    } in
  } in
  jump loop137()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret154(retv155: Ref[int]) -> Ref[int] {
    retv155
  } in
  let t156 = @intrinsic(ref.new)(value/207) in
  jump ret154(t156)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret157(retv158: int) -> int {
    retv158
  } in
  let t159 = @intrinsic(ref.get)(self/208) in
  jump ret157(t159)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret160(retv161: bool) -> bool {
    retv161
  } in
  let t162 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret160(t162)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret163() -> unit {
    ()
  } in
  let t164 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret163()
}

fn println__T_int(value/1: int) -> unit {
  join ret165() -> unit {
    ()
  } in
  let t166 = trait_impl#ToString#int#to_string(value/1) in
  let t167 = @runtime(core.string_println)(t166) in
  jump ret165()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret168(retv169: string) -> string {
    retv169
  } in
  let t170 = @runtime(core.int_to_string)(self/40) in
  jump ret168(t170)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret171(retv172: uint64) -> uint64 {
    retv172
  } in
  let t173 = @runtime(core.int_hash)(self/85) in
  jump ret171(t173)
}