fn main() -> unit {
  join ret170() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit171() -> unit {
    let _wild160 = () in
    let t172 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild161 = println__T_int(t172) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total2/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit173() -> unit {
      let _wild168 = () in
      let t174 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
      let _wild169 = println__T_int(t174) in
      jump ret170()
    } in
    joinrec {
      join loop175() -> unit {
        let mtmp162 = inherent#Ref#Ref[T]#get__T_int(j/2) in
        join k176(jp177: bool) -> unit {
          if jp177 {
            let t178 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
            let t179 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t180 = (t178 + t179) in
            let _wild165 = inherent#Ref#Ref[T]#set__T_int(total2/3, t180) in
            let t182 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t183 = trait_impl#Eq#int#eq(t182, 2) in
            join k181() -> unit {
              let _wild167 = () in
              jump loop175()
            } in
            if t183 {
              jump exit173()
            } else {
              jump k181()
            }
          } else {
            jump exit173()
          }
        } in
        match mtmp162 {
          0 => {
            let _wild163 = inherent#Ref#Ref[T]#set__T_int(j/2, 1) in
            jump k176(true)
          },
          1 => {
            let _wild164 = inherent#Ref#Ref[T]#set__T_int(j/2, 2) in
            jump k176(true)
          },
          2 => {
            jump k176(true)
          },
          _ => {
            jump k176(false)
          },
        }
      } in
    } in
    jump loop175()
  } in
  joinrec {
    join loop184() -> unit {
      let t195 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t196 = trait_impl#Eq#int#eq(t195, 0) in
      join k185(jp186: bool) -> unit {
        if jp186 {
          let t187 = inherent#Ref#Ref[T]#get__T_int(total/1) in
          let t188 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t189 = (t187 + t188) in
          let _wild156 = inherent#Ref#Ref[T]#set__T_int(total/1, t189) in
          let t193 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t194 = trait_impl#Eq#int#eq(t193, 1) in
          join k190() -> unit {
            let _wild158 = () in
            let t191 = inherent#Ref#Ref[T]#get__T_int(i/0) in
            let t192 = (t191 + 1) in
            let _wild159 = inherent#Ref#Ref[T]#set__T_int(i/0, t192) in
            jump loop184()
          } in
          if t194 {
            let _wild157 = inherent#Ref#Ref[T]#set__T_int(i/0, 2) in
            jump loop184()
          } else {
            jump k190()
          }
        } else {
          jump exit171()
        }
      } in
      if t196 {
        let _wild155 = inherent#Ref#Ref[T]#set__T_int(i/0, 1) in
        jump k185(true)
      } else {
        let t199 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t200 = (t199 < 4) in
        join k197(jp198: bool) -> unit {
          jump k185(jp198)
        } in
        if t200 {
          jump k197(true)
        } else {
          jump k197(false)
        }
      }
    } in
  } in
  jump loop184()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret201(retv202: Ref[int]) -> Ref[int] {
    retv202
  } in
  let t203 = @intrinsic(ref.new)(value/207) in
  jump ret201(t203)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret204(retv205: int) -> int {
    retv205
  } in
  let t206 = @intrinsic(ref.get)(self/208) in
  jump ret204(t206)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret207(retv208: bool) -> bool {
    retv208
  } in
  let t209 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret207(t209)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret210() -> unit {
    ()
  } in
  let t211 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret210()
}

fn println__T_int(value/1: int) -> unit {
  join ret212() -> unit {
    ()
  } in
  let t213 = trait_impl#ToString#int#to_string(value/1) in
  let t214 = @runtime(core.string_println)(t213) in
  jump ret212()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret215(retv216: string) -> string {
    retv216
  } in
  let t217 = @runtime(core.int_to_string)(self/40) in
  jump ret215(t217)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret218(retv219: uint64) -> uint64 {
    retv219
  } in
  let t220 = @runtime(core.int_hash)(self/85) in
  jump ret218(t220)
}