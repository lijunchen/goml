fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit424() -> unit {
    join inline_cont526(t425: int) -> unit {
      join inline_cont523(_wild414: unit) -> unit {
        join inline_cont520(j/2: Ref[int]) -> unit {
          join inline_cont517(total2/3: Ref[int]) -> unit {
            join exit426() -> unit {
              join inline_cont515(t427: int) -> unit {
                let inline513 = trait_impl#ToString#int#to_string(t427) in
                let inline514 = @runtime(core.string_println)(inline513) in
                ()
              } in
              let inline516 = @intrinsic(ref.get)(total2/3) in
              jump inline_cont515(inline516)
            } in
            joinrec {
              join loop428() -> unit {
                join inline_cont510(mtmp415: int) -> unit {
                  join k429(jp430: bool) -> unit {
                    if jp430 {
                      join inline_cont508(t431: int) -> unit {
                        join inline_cont506(t432: int) -> unit {
                          let t433 = (t431 + t432) in
                          join inline_cont504(_wild418: unit) -> unit {
                            join inline_cont502(t435: int) -> unit {
                              let t436 = (t435 == 2) in
                              join k434() -> unit {
                                jump loop428()
                              } in
                              if t436 {
                                jump exit426()
                              } else {
                                jump k434()
                              }
                            } in
                            let inline503 = @intrinsic(ref.get)(j/2) in
                            jump inline_cont502(inline503)
                          } in
                          let inline505 = @intrinsic(ref.set)(total2/3, t433) in
                          jump inline_cont504(())
                        } in
                        let inline507 = @intrinsic(ref.get)(j/2) in
                        jump inline_cont506(inline507)
                      } in
                      let inline509 = @intrinsic(ref.get)(total2/3) in
                      jump inline_cont508(inline509)
                    } else {
                      jump exit426()
                    }
                  } in
                  match mtmp415 {
                    0 => {
                      let inline497 = 1 in
                      let inline498 = @intrinsic(ref.set)(j/2, inline497) in
                      jump k429(true)
                    },
                    1 => {
                      let inline500 = 2 in
                      let inline501 = @intrinsic(ref.set)(j/2, inline500) in
                      jump k429(true)
                    },
                    2 => {
                      jump k429(true)
                    },
                    _ => {
                      jump k429(false)
                    },
                  }
                } in
                let inline511 = @intrinsic(ref.get)(j/2) in
                jump inline_cont510(inline511)
              } in
            } in
            jump loop428()
          } in
          let inline518 = 0 in
          let inline519 = @intrinsic(ref.new)(inline518) in
          jump inline_cont517(inline519)
        } in
        let inline521 = 0 in
        let inline522 = @intrinsic(ref.new)(inline521) in
        jump inline_cont520(inline522)
      } in
      let inline524 = trait_impl#ToString#int#to_string(t425) in
      let inline525 = @runtime(core.string_println)(inline524) in
      jump inline_cont523(())
    } in
    let inline527 = @intrinsic(ref.get)(total/1) in
    jump inline_cont526(inline527)
  } in
  joinrec {
    join loop437() -> unit {
      join inline_cont494(t448: int) -> unit {
        let t449 = (t448 == 0) in
        join k438(jp439: bool) -> unit {
          if jp439 {
            join inline_cont492(t440: int) -> unit {
              join inline_cont490(t441: int) -> unit {
                let t442 = (t440 + t441) in
                join inline_cont488(_wild409: unit) -> unit {
                  join inline_cont486(t446: int) -> unit {
                    let t447 = (t446 == 1) in
                    join k443() -> unit {
                      join inline_cont484(t444: int) -> unit {
                        let t445 = (t444 + 1) in
                        let inline483 = @intrinsic(ref.set)(i/0, t445) in
                        jump loop437()
                      } in
                      let inline485 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont484(inline485)
                    } in
                    if t447 {
                      let inline480 = 2 in
                      let inline481 = @intrinsic(ref.set)(i/0, inline480) in
                      jump loop437()
                    } else {
                      jump k443()
                    }
                  } in
                  let inline487 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont486(inline487)
                } in
                let inline489 = @intrinsic(ref.set)(total/1, t442) in
                jump inline_cont488(())
              } in
              let inline491 = @intrinsic(ref.get)(i/0) in
              jump inline_cont490(inline491)
            } in
            let inline493 = @intrinsic(ref.get)(total/1) in
            jump inline_cont492(inline493)
          } else {
            jump exit424()
          }
        } in
        if t449 {
          let inline475 = 1 in
          let inline476 = @intrinsic(ref.set)(i/0, inline475) in
          jump k438(true)
        } else {
          join inline_cont477(t452: int) -> unit {
            let t453 = (t452 < 4) in
            if t453 {
              jump k438(true)
            } else {
              jump k438(false)
            }
          } in
          let inline478 = @intrinsic(ref.get)(i/0) in
          jump inline_cont477(inline478)
        }
      } in
      let inline495 = @intrinsic(ref.get)(i/0) in
      jump inline_cont494(inline495)
    } in
  } in
  jump loop437()
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t456 = @intrinsic(ref.new)(value/431) in
  t456
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t459 = @intrinsic(ref.get)(self/432) in
  t459
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t461 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont528(t463: string) -> unit {
    let t464 = @runtime(core.string_println)(t463) in
    ()
  } in
  let inline529 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont528(inline529)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t467 = @runtime(core.int_to_string)(self/151) in
  t467
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t470 = (self/185 == other/186) in
  t470
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t473 = @runtime(core.int_hash)(self/211) in
  t473
}