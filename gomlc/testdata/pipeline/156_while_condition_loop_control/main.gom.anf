fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_isize(0) in
  join exit812() -> unit {
    join inline_cont957(t813: isize) -> unit {
      join inline_cont954(_wild802: unit) -> unit {
        join inline_cont951(j/2: Ref[isize]) -> unit {
          join inline_cont948(total2/3: Ref[isize]) -> unit {
            join exit814() -> unit {
              join inline_cont946(t815: isize) -> unit {
                let inline944 = trait_impl#ToString#isize#to_string(t815) in
                let inline945 = @runtime(core.string_println)(inline944) in
                ()
              } in
              let inline947 = @intrinsic(ref.get)(total2/3) in
              jump inline_cont946(inline947)
            } in
            joinrec {
              join loop816() -> unit {
                join inline_cont941(mtmp803: isize) -> unit {
                  join k817(jp818: bool) -> unit {
                    if jp818 {
                      join inline_cont939(t819: isize) -> unit {
                        join inline_cont937(t820: isize) -> unit {
                          let t821 = (t819 + t820) in
                          join inline_cont935(_wild806: unit) -> unit {
                            join inline_cont933(t823: isize) -> unit {
                              let t824 = (t823 == 2) in
                              join k822() -> unit {
                                jump loop816()
                              } in
                              if t824 {
                                jump exit814()
                              } else {
                                jump k822()
                              }
                            } in
                            let inline934 = @intrinsic(ref.get)(j/2) in
                            jump inline_cont933(inline934)
                          } in
                          let inline936 = @intrinsic(ref.set)(total2/3, t821) in
                          jump inline_cont935(())
                        } in
                        let inline938 = @intrinsic(ref.get)(j/2) in
                        jump inline_cont937(inline938)
                      } in
                      let inline940 = @intrinsic(ref.get)(total2/3) in
                      jump inline_cont939(inline940)
                    } else {
                      jump exit814()
                    }
                  } in
                  match mtmp803 {
                    0 => {
                      let inline928 = 1 in
                      let inline929 = @intrinsic(ref.set)(j/2, inline928) in
                      jump k817(true)
                    },
                    1 => {
                      let inline931 = 2 in
                      let inline932 = @intrinsic(ref.set)(j/2, inline931) in
                      jump k817(true)
                    },
                    2 => {
                      jump k817(true)
                    },
                    _ => {
                      jump k817(false)
                    },
                  }
                } in
                let inline942 = @intrinsic(ref.get)(j/2) in
                jump inline_cont941(inline942)
              } in
            } in
            jump loop816()
          } in
          let inline949 = 0 in
          let inline950 = @intrinsic(ref.new)(inline949) in
          jump inline_cont948(inline950)
        } in
        let inline952 = 0 in
        let inline953 = @intrinsic(ref.new)(inline952) in
        jump inline_cont951(inline953)
      } in
      let inline955 = trait_impl#ToString#isize#to_string(t813) in
      let inline956 = @runtime(core.string_println)(inline955) in
      jump inline_cont954(())
    } in
    let inline958 = @intrinsic(ref.get)(total/1) in
    jump inline_cont957(inline958)
  } in
  joinrec {
    join loop825() -> unit {
      join inline_cont925(t836: isize) -> unit {
        let t837 = (t836 == 0) in
        join k826(jp827: bool) -> unit {
          if jp827 {
            join inline_cont923(t828: isize) -> unit {
              join inline_cont921(t829: isize) -> unit {
                let t830 = (t828 + t829) in
                join inline_cont919(_wild797: unit) -> unit {
                  join inline_cont917(t834: isize) -> unit {
                    let t835 = (t834 == 1) in
                    join k831() -> unit {
                      join inline_cont915(t832: isize) -> unit {
                        let t833 = (t832 + 1) in
                        let inline914 = @intrinsic(ref.set)(i/0, t833) in
                        jump loop825()
                      } in
                      let inline916 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont915(inline916)
                    } in
                    if t835 {
                      let inline911 = 2 in
                      let inline912 = @intrinsic(ref.set)(i/0, inline911) in
                      jump loop825()
                    } else {
                      jump k831()
                    }
                  } in
                  let inline918 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont917(inline918)
                } in
                let inline920 = @intrinsic(ref.set)(total/1, t830) in
                jump inline_cont919(())
              } in
              let inline922 = @intrinsic(ref.get)(i/0) in
              jump inline_cont921(inline922)
            } in
            let inline924 = @intrinsic(ref.get)(total/1) in
            jump inline_cont923(inline924)
          } else {
            jump exit812()
          }
        } in
        if t837 {
          let inline906 = 1 in
          let inline907 = @intrinsic(ref.set)(i/0, inline906) in
          jump k826(true)
        } else {
          join inline_cont908(t840: isize) -> unit {
            let t841 = (t840 < 4) in
            if t841 {
              jump k826(true)
            } else {
              jump k826(false)
            }
          } in
          let inline909 = @intrinsic(ref.get)(i/0) in
          jump inline_cont908(inline909)
        }
      } in
      let inline926 = @intrinsic(ref.get)(i/0) in
      jump inline_cont925(inline926)
    } in
  } in
  jump loop825()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t844 = @intrinsic(ref.new)(value/684) in
  t844
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t847 = @intrinsic(ref.get)(self/685) in
  t847
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t849 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont959(t851: string) -> unit {
    let t852 = @runtime(core.string_println)(t851) in
    ()
  } in
  let inline960 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont959(inline960)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline962 = (self/404 as i64) in
  let inline963 = signed_decimal_string(inline962) in
  inline963
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t858 = (value/222 as i64) in
  let inline965 = (t858 < 0) in
  if inline965 {
    let inline966 = (t858 as u64) in
    let inline967 = (0 - inline966) in
    let inline968 = decimal_string(inline967) in
    let inline969 = ("-" + inline968) in
    inline969
  } else {
    let inline970 = (t858 as u64) in
    let inline971 = decimal_string(inline970) in
    inline971
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t864 = (value/214 < 0) in
  if t864 {
    let t865 = (value/214 as u64) in
    let t866 = (0 - t865) in
    let t867 = decimal_string(t866) in
    let t868 = ("-" + t867) in
    t868
  } else {
    let t869 = (value/214 as u64) in
    let t870 = decimal_string(t869) in
    t870
  }
}

fn decimal_string(value/208: u64) -> string {
  let t893 = (value/208 == 0) in
  join k873() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit874() -> string {
      join inline_cont980(t875: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t875) in
        let offset/212 = 0 in
        join exit876() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop877() -> string {
            join inline_cont978(t878: isize) -> string {
              let t879 = (offset/212 < t878) in
              if t879 {
                join inline_cont976(t880: isize) -> string {
                  let t881 = (t880 - offset/212) in
                  let t882 = (t881 - 1) in
                  let t883 = @intrinsic(vec.get)(reversed/209, t882) in
                  join inline_cont974(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t884 = (compound_old358 + compound_value359) in
                    let t885 = offset/212 = t884 in
                    jump loop877()
                  } in
                  let inline975 = @intrinsic(vec.push)(bytes/211, t883) in
                  jump inline_cont974(())
                } in
                let inline977 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont976(inline977)
              } else {
                jump exit876()
              }
            } in
            let inline979 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont978(inline979)
          } in
        } in
        jump loop877()
      } in
      let inline981 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont980(inline981)
    } in
    joinrec {
      join loop886() -> string {
        let t887 = (remaining/210 > 0) in
        if t887 {
          let t888 = (remaining/210 % 10) in
          let t889 = (t888 as u8) in
          let t890 = (t889 + 48) in
          join inline_cont972(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t891 = (compound_old353 / compound_value354) in
            let t892 = remaining/210 = t891 in
            jump loop886()
          } in
          let inline973 = @intrinsic(vec.push)(reversed/209, t890) in
          jump inline_cont972(())
        } else {
          jump exit874()
        }
      } in
    } in
    jump loop886()
  } in
  if t893 {
    "0"
  } else {
    jump k873()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t895 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t898 = @intrinsic(vec.len)(self/526) in
  t898
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t901 = (self/438 == other/439) in
  t901
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t904 = @runtime(core.int_hash)(self/464) in
  t904
}