fn main() -> unit {
  join ret167() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit168() -> unit {
    let _wild157 = () in
    let t169 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild158 = println__T_int(t169) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total2/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit170() -> unit {
      let _wild165 = () in
      let t171 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
      let _wild166 = println__T_int(t171) in
      jump ret167()
    } in
    joinrec {
      join loop172() -> unit {
        let mtmp159 = inherent#Ref#Ref[T]#get__T_int(j/2) in
        join k173(jp174: bool) -> unit {
          if jp174 {
            let t175 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
            let t176 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t177 = (t175 + t176) in
            let _wild162 = inherent#Ref#Ref[T]#set__T_int(total2/3, t177) in
            let t179 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t180 = trait_impl#Eq#int#eq(t179, 2) in
            join k178() -> unit {
              let _wild164 = () in
              jump loop172()
            } in
            if t180 {
              jump exit170()
            } else {
              jump k178()
            }
          } else {
            jump exit170()
          }
        } in
        match mtmp159 {
          0 => {
            let _wild160 = inherent#Ref#Ref[T]#set__T_int(j/2, 1) in
            jump k173(true)
          },
          1 => {
            let _wild161 = inherent#Ref#Ref[T]#set__T_int(j/2, 2) in
            jump k173(true)
          },
          2 => {
            jump k173(true)
          },
          _ => {
            jump k173(false)
          },
        }
      } in
    } in
    jump loop172()
  } in
  joinrec {
    join loop181() -> unit {
      let t192 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t193 = trait_impl#Eq#int#eq(t192, 0) in
      join k182(jp183: bool) -> unit {
        if jp183 {
          let t184 = inherent#Ref#Ref[T]#get__T_int(total/1) in
          let t185 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t186 = (t184 + t185) in
          let _wild153 = inherent#Ref#Ref[T]#set__T_int(total/1, t186) in
          let t190 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t191 = trait_impl#Eq#int#eq(t190, 1) in
          join k187() -> unit {
            let _wild155 = () in
            let t188 = inherent#Ref#Ref[T]#get__T_int(i/0) in
            let t189 = (t188 + 1) in
            let _wild156 = inherent#Ref#Ref[T]#set__T_int(i/0, t189) in
            jump loop181()
          } in
          if t191 {
            let _wild154 = inherent#Ref#Ref[T]#set__T_int(i/0, 2) in
            jump loop181()
          } else {
            jump k187()
          }
        } else {
          jump exit168()
        }
      } in
      if t193 {
        let _wild152 = inherent#Ref#Ref[T]#set__T_int(i/0, 1) in
        jump k182(true)
      } else {
        let t196 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t197 = (t196 < 4) in
        join k194(jp195: bool) -> unit {
          jump k182(jp195)
        } in
        if t197 {
          jump k194(true)
        } else {
          jump k194(false)
        }
      }
    } in
  } in
  jump loop181()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret198(retv199: Ref[int]) -> Ref[int] {
    retv199
  } in
  let t200 = @intrinsic(ref.new)(value/207) in
  jump ret198(t200)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret201(retv202: int) -> int {
    retv202
  } in
  let t203 = @intrinsic(ref.get)(self/208) in
  jump ret201(t203)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret204(retv205: bool) -> bool {
    retv205
  } in
  let t206 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret204(t206)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret207() -> unit {
    ()
  } in
  let t208 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret207()
}

fn println__T_int(value/1: int) -> unit {
  join ret209() -> unit {
    ()
  } in
  let t210 = trait_impl#ToString#int#to_string(value/1) in
  let t211 = @runtime(core.string_println)(t210) in
  jump ret209()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret212(retv213: string) -> string {
    retv213
  } in
  let t214 = @runtime(core.int_to_string)(self/40) in
  jump ret212(t214)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret215(retv216: uint64) -> uint64 {
    retv216
  } in
  let t217 = @runtime(core.int_hash)(self/85) in
  jump ret215(t217)
}