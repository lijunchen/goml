fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit198() -> unit {
    let t199 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild188 = println__T_int(t199) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont300(total2/3: Ref[int]) -> unit {
      join exit200() -> unit {
        join inline_cont298(t201: int) -> unit {
          let inline296 = trait_impl#ToString#int#to_string(t201) in
          let inline297 = @runtime(core.string_println)(inline296) in
          ()
        } in
        let inline299 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont298(inline299)
      } in
      joinrec {
        join loop202() -> unit {
          join inline_cont293(mtmp189: int) -> unit {
            join k203(jp204: bool) -> unit {
              if jp204 {
                join inline_cont291(t205: int) -> unit {
                  join inline_cont289(t206: int) -> unit {
                    let t207 = (t205 + t206) in
                    join inline_cont287(_wild192: unit) -> unit {
                      join inline_cont285(t209: int) -> unit {
                        join inline_cont282(t210: bool) -> unit {
                          join k208() -> unit {
                            jump loop202()
                          } in
                          if t210 {
                            jump exit200()
                          } else {
                            jump k208()
                          }
                        } in
                        let inline283 = 2 in
                        let inline284 = (t209 == inline283) in
                        jump inline_cont282(inline284)
                      } in
                      let inline286 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont285(inline286)
                    } in
                    let inline288 = @intrinsic(ref.set)(total2/3, t207) in
                    jump inline_cont287(())
                  } in
                  let inline290 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont289(inline290)
                } in
                let inline292 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont291(inline292)
              } else {
                jump exit200()
              }
            } in
            match mtmp189 {
              0 => {
                let inline277 = 1 in
                let inline278 = @intrinsic(ref.set)(j/2, inline277) in
                jump k203(true)
              },
              1 => {
                let inline280 = 2 in
                let inline281 = @intrinsic(ref.set)(j/2, inline280) in
                jump k203(true)
              },
              2 => {
                jump k203(true)
              },
              _ => {
                jump k203(false)
              },
            }
          } in
          let inline294 = @intrinsic(ref.get)(j/2) in
          jump inline_cont293(inline294)
        } in
      } in
      jump loop202()
    } in
    let inline301 = 0 in
    let inline302 = @intrinsic(ref.new)(inline301) in
    jump inline_cont300(inline302)
  } in
  joinrec {
    join loop211() -> unit {
      join inline_cont274(t222: int) -> unit {
        join inline_cont271(t223: bool) -> unit {
          join k212(jp213: bool) -> unit {
            if jp213 {
              join inline_cont269(t214: int) -> unit {
                join inline_cont267(t215: int) -> unit {
                  let t216 = (t214 + t215) in
                  join inline_cont265(_wild183: unit) -> unit {
                    join inline_cont263(t220: int) -> unit {
                      join inline_cont260(t221: bool) -> unit {
                        join k217() -> unit {
                          join inline_cont258(t218: int) -> unit {
                            let t219 = (t218 + 1) in
                            let inline257 = @intrinsic(ref.set)(i/0, t219) in
                            jump loop211()
                          } in
                          let inline259 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont258(inline259)
                        } in
                        if t221 {
                          let inline254 = 2 in
                          let inline255 = @intrinsic(ref.set)(i/0, inline254) in
                          jump loop211()
                        } else {
                          jump k217()
                        }
                      } in
                      let inline261 = 1 in
                      let inline262 = (t220 == inline261) in
                      jump inline_cont260(inline262)
                    } in
                    let inline264 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont263(inline264)
                  } in
                  let inline266 = @intrinsic(ref.set)(total/1, t216) in
                  jump inline_cont265(())
                } in
                let inline268 = @intrinsic(ref.get)(i/0) in
                jump inline_cont267(inline268)
              } in
              let inline270 = @intrinsic(ref.get)(total/1) in
              jump inline_cont269(inline270)
            } else {
              jump exit198()
            }
          } in
          if t223 {
            let inline249 = 1 in
            let inline250 = @intrinsic(ref.set)(i/0, inline249) in
            jump k212(true)
          } else {
            join inline_cont251(t226: int) -> unit {
              let t227 = (t226 < 4) in
              if t227 {
                jump k212(true)
              } else {
                jump k212(false)
              }
            } in
            let inline252 = @intrinsic(ref.get)(i/0) in
            jump inline_cont251(inline252)
          }
        } in
        let inline272 = 0 in
        let inline273 = (t222 == inline272) in
        jump inline_cont271(inline273)
      } in
      let inline275 = @intrinsic(ref.get)(i/0) in
      jump inline_cont274(inline275)
    } in
  } in
  jump loop211()
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t230 = @intrinsic(ref.new)(value/270) in
  t230
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t233 = @intrinsic(ref.get)(self/271) in
  t233
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t236 = (self/101 == other/102) in
  t236
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t238 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont303(t240: string) -> unit {
    let t241 = @runtime(core.string_println)(t240) in
    ()
  } in
  let inline304 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont303(inline304)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t244 = @runtime(core.int_to_string)(self/67) in
  t244
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t247 = @runtime(core.int_hash)(self/127) in
  t247
}