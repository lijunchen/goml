fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit152() -> unit {
    let _wild141 = () in
    let t153 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild142 = println__T_int(t153) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont254(total2/3: Ref[int]) -> unit {
      join exit154() -> unit {
        let _wild149 = () in
        join inline_cont252(t155: int) -> unit {
          let inline250 = trait_impl#ToString#int#to_string(t155) in
          let inline251 = @runtime(core.string_println)(inline250) in
          ()
        } in
        let inline253 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont252(inline253)
      } in
      joinrec {
        join loop156() -> unit {
          join inline_cont247(mtmp143: int) -> unit {
            join k157(jp158: bool) -> unit {
              if jp158 {
                join inline_cont245(t159: int) -> unit {
                  join inline_cont243(t160: int) -> unit {
                    let t161 = (t159 + t160) in
                    join inline_cont241(_wild146: unit) -> unit {
                      join inline_cont239(t163: int) -> unit {
                        join inline_cont236(t164: bool) -> unit {
                          join k162() -> unit {
                            let _wild148 = () in
                            jump loop156()
                          } in
                          if t164 {
                            jump exit154()
                          } else {
                            jump k162()
                          }
                        } in
                        let inline237 = 2 in
                        let inline238 = @intrinsic(eq.primitive)(t163, inline237) in
                        jump inline_cont236(inline238)
                      } in
                      let inline240 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont239(inline240)
                    } in
                    let inline242 = @intrinsic(ref.set)(total2/3, t161) in
                    jump inline_cont241(())
                  } in
                  let inline244 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont243(inline244)
                } in
                let inline246 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont245(inline246)
              } else {
                jump exit154()
              }
            } in
            match mtmp143 {
              0 => {
                let inline231 = 1 in
                let inline232 = @intrinsic(ref.set)(j/2, inline231) in
                jump k157(true)
              },
              1 => {
                let inline234 = 2 in
                let inline235 = @intrinsic(ref.set)(j/2, inline234) in
                jump k157(true)
              },
              2 => {
                jump k157(true)
              },
              _ => {
                jump k157(false)
              },
            }
          } in
          let inline248 = @intrinsic(ref.get)(j/2) in
          jump inline_cont247(inline248)
        } in
      } in
      jump loop156()
    } in
    let inline255 = 0 in
    let inline256 = @intrinsic(ref.new)(inline255) in
    jump inline_cont254(inline256)
  } in
  joinrec {
    join loop165() -> unit {
      join inline_cont228(t176: int) -> unit {
        join inline_cont225(t177: bool) -> unit {
          join k166(jp167: bool) -> unit {
            if jp167 {
              join inline_cont223(t168: int) -> unit {
                join inline_cont221(t169: int) -> unit {
                  let t170 = (t168 + t169) in
                  join inline_cont219(_wild137: unit) -> unit {
                    join inline_cont217(t174: int) -> unit {
                      join inline_cont214(t175: bool) -> unit {
                        join k171() -> unit {
                          let _wild139 = () in
                          join inline_cont212(t172: int) -> unit {
                            let t173 = (t172 + 1) in
                            let inline211 = @intrinsic(ref.set)(i/0, t173) in
                            jump loop165()
                          } in
                          let inline213 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont212(inline213)
                        } in
                        if t175 {
                          let inline208 = 2 in
                          let inline209 = @intrinsic(ref.set)(i/0, inline208) in
                          jump loop165()
                        } else {
                          jump k171()
                        }
                      } in
                      let inline215 = 1 in
                      let inline216 = @intrinsic(eq.primitive)(t174, inline215) in
                      jump inline_cont214(inline216)
                    } in
                    let inline218 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont217(inline218)
                  } in
                  let inline220 = @intrinsic(ref.set)(total/1, t170) in
                  jump inline_cont219(())
                } in
                let inline222 = @intrinsic(ref.get)(i/0) in
                jump inline_cont221(inline222)
              } in
              let inline224 = @intrinsic(ref.get)(total/1) in
              jump inline_cont223(inline224)
            } else {
              jump exit152()
            }
          } in
          if t177 {
            let inline203 = 1 in
            let inline204 = @intrinsic(ref.set)(i/0, inline203) in
            jump k166(true)
          } else {
            join inline_cont205(t180: int) -> unit {
              let t181 = (t180 < 4) in
              if t181 {
                jump k166(true)
              } else {
                jump k166(false)
              }
            } in
            let inline206 = @intrinsic(ref.get)(i/0) in
            jump inline_cont205(inline206)
          }
        } in
        let inline226 = 0 in
        let inline227 = @intrinsic(eq.primitive)(t176, inline226) in
        jump inline_cont225(inline227)
      } in
      let inline229 = @intrinsic(ref.get)(i/0) in
      jump inline_cont228(inline229)
    } in
  } in
  jump loop165()
}

fn inherent#Ref#Ref[T]#new__T_int(value/215: int) -> Ref[int] {
  let t184 = @intrinsic(ref.new)(value/215) in
  t184
}

fn inherent#Ref#Ref[T]#get__T_int(self/216: Ref[int]) -> int {
  let t187 = @intrinsic(ref.get)(self/216) in
  t187
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t190 = @intrinsic(eq.primitive)(self/88, other/89) in
  t190
}

fn inherent#Ref#Ref[T]#set__T_int(self/217: Ref[int], value/218: int) -> unit {
  let t192 = @intrinsic(ref.set)(self/217, value/218) in
  ()
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont257(t194: string) -> unit {
    let t195 = @runtime(core.string_println)(t194) in
    ()
  } in
  let inline258 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont257(inline258)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t198 = @runtime(core.int_to_string)(self/69) in
  t198
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t201 = @runtime(core.int_hash)(self/114) in
  t201
}