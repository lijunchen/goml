fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit188() -> unit {
    let _wild177 = () in
    let t189 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild178 = println__T_int(t189) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont290(total2/3: Ref[int]) -> unit {
      join exit190() -> unit {
        let _wild185 = () in
        join inline_cont288(t191: int) -> unit {
          let inline286 = trait_impl#ToString#int#to_string(t191) in
          let inline287 = @runtime(core.string_println)(inline286) in
          ()
        } in
        let inline289 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont288(inline289)
      } in
      joinrec {
        join loop192() -> unit {
          join inline_cont283(mtmp179: int) -> unit {
            join k193(jp194: bool) -> unit {
              if jp194 {
                join inline_cont281(t195: int) -> unit {
                  join inline_cont279(t196: int) -> unit {
                    let t197 = (t195 + t196) in
                    join inline_cont277(_wild182: unit) -> unit {
                      join inline_cont275(t199: int) -> unit {
                        join inline_cont272(t200: bool) -> unit {
                          join k198() -> unit {
                            let _wild184 = () in
                            jump loop192()
                          } in
                          if t200 {
                            jump exit190()
                          } else {
                            jump k198()
                          }
                        } in
                        let inline273 = 2 in
                        let inline274 = @intrinsic(eq.primitive)(t199, inline273) in
                        jump inline_cont272(inline274)
                      } in
                      let inline276 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont275(inline276)
                    } in
                    let inline278 = @intrinsic(ref.set)(total2/3, t197) in
                    jump inline_cont277(())
                  } in
                  let inline280 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont279(inline280)
                } in
                let inline282 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont281(inline282)
              } else {
                jump exit190()
              }
            } in
            match mtmp179 {
              0 => {
                let inline267 = 1 in
                let inline268 = @intrinsic(ref.set)(j/2, inline267) in
                jump k193(true)
              },
              1 => {
                let inline270 = 2 in
                let inline271 = @intrinsic(ref.set)(j/2, inline270) in
                jump k193(true)
              },
              2 => {
                jump k193(true)
              },
              _ => {
                jump k193(false)
              },
            }
          } in
          let inline284 = @intrinsic(ref.get)(j/2) in
          jump inline_cont283(inline284)
        } in
      } in
      jump loop192()
    } in
    let inline291 = 0 in
    let inline292 = @intrinsic(ref.new)(inline291) in
    jump inline_cont290(inline292)
  } in
  joinrec {
    join loop201() -> unit {
      join inline_cont264(t212: int) -> unit {
        join inline_cont261(t213: bool) -> unit {
          join k202(jp203: bool) -> unit {
            if jp203 {
              join inline_cont259(t204: int) -> unit {
                join inline_cont257(t205: int) -> unit {
                  let t206 = (t204 + t205) in
                  join inline_cont255(_wild173: unit) -> unit {
                    join inline_cont253(t210: int) -> unit {
                      join inline_cont250(t211: bool) -> unit {
                        join k207() -> unit {
                          let _wild175 = () in
                          join inline_cont248(t208: int) -> unit {
                            let t209 = (t208 + 1) in
                            let inline247 = @intrinsic(ref.set)(i/0, t209) in
                            jump loop201()
                          } in
                          let inline249 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont248(inline249)
                        } in
                        if t211 {
                          let inline244 = 2 in
                          let inline245 = @intrinsic(ref.set)(i/0, inline244) in
                          jump loop201()
                        } else {
                          jump k207()
                        }
                      } in
                      let inline251 = 1 in
                      let inline252 = @intrinsic(eq.primitive)(t210, inline251) in
                      jump inline_cont250(inline252)
                    } in
                    let inline254 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont253(inline254)
                  } in
                  let inline256 = @intrinsic(ref.set)(total/1, t206) in
                  jump inline_cont255(())
                } in
                let inline258 = @intrinsic(ref.get)(i/0) in
                jump inline_cont257(inline258)
              } in
              let inline260 = @intrinsic(ref.get)(total/1) in
              jump inline_cont259(inline260)
            } else {
              jump exit188()
            }
          } in
          if t213 {
            let inline239 = 1 in
            let inline240 = @intrinsic(ref.set)(i/0, inline239) in
            jump k202(true)
          } else {
            join inline_cont241(t216: int) -> unit {
              let t217 = (t216 < 4) in
              if t217 {
                jump k202(true)
              } else {
                jump k202(false)
              }
            } in
            let inline242 = @intrinsic(ref.get)(i/0) in
            jump inline_cont241(inline242)
          }
        } in
        let inline262 = 0 in
        let inline263 = @intrinsic(eq.primitive)(t212, inline262) in
        jump inline_cont261(inline263)
      } in
      let inline265 = @intrinsic(ref.get)(i/0) in
      jump inline_cont264(inline265)
    } in
  } in
  jump loop201()
}

fn inherent#Ref#Ref[T]#new__T_int(value/257: int) -> Ref[int] {
  let t220 = @intrinsic(ref.new)(value/257) in
  t220
}

fn inherent#Ref#Ref[T]#get__T_int(self/258: Ref[int]) -> int {
  let t223 = @intrinsic(ref.get)(self/258) in
  t223
}

fn trait_impl#PartialEq#int#eq(self/103: int, other/104: int) -> bool {
  let t226 = @intrinsic(eq.primitive)(self/103, other/104) in
  t226
}

fn inherent#Ref#Ref[T]#set__T_int(self/259: Ref[int], value/260: int) -> unit {
  let t228 = @intrinsic(ref.set)(self/259, value/260) in
  ()
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont293(t230: string) -> unit {
    let t231 = @runtime(core.string_println)(t230) in
    ()
  } in
  let inline294 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont293(inline294)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t234 = @runtime(core.int_to_string)(self/69) in
  t234
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t237 = @runtime(core.int_hash)(self/129) in
  t237
}