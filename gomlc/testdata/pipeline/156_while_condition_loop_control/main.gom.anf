fn main() -> () {
  let i/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  let total/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  join exit0() -> () {
    join inline_cont0(t0: isize) -> () {
      join inline_cont1(_wild0: ()) -> () {
        join inline_cont2(j/0: Ref[isize]) -> () {
          join inline_cont3(total2/0: Ref[isize]) -> () {
            join exit1() -> () {
              join inline_cont4(t1: isize) -> () {
                let inline0 = trait_impl#ToString#isize#to_string(t1) in
                let inline1 = @runtime(core.string_println)(inline0) in
                ()
              } in
              let inline2 = @intrinsic(ref.get)(total2/0) in
              jump inline_cont4(inline2)
            } in
            joinrec {
              join loop1() -> () {
                join inline_cont5(mtmp0: isize) -> () {
                  join k0(jp0: bool) -> () {
                    if jp0 {
                      join inline_cont6(t2: isize) -> () {
                        join inline_cont7(t3: isize) -> () {
                          let t4 = (t2 + t3) in
                          join inline_cont8(_wild1: ()) -> () {
                            join inline_cont9(t5: isize) -> () {
                              let t6 = (t5 == 2) in
                              join k1() -> () {
                                jump loop1()
                              } in
                              if t6 {
                                jump exit1()
                              } else {
                                jump k1()
                              }
                            } in
                            let inline3 = @intrinsic(ref.get)(j/0) in
                            jump inline_cont9(inline3)
                          } in
                          let inline4 = @intrinsic(ref.set)(total2/0, t4) in
                          jump inline_cont8(())
                        } in
                        let inline5 = @intrinsic(ref.get)(j/0) in
                        jump inline_cont7(inline5)
                      } in
                      let inline6 = @intrinsic(ref.get)(total2/0) in
                      jump inline_cont6(inline6)
                    } else {
                      jump exit1()
                    }
                  } in
                  match mtmp0 {
                    0 => {
                      let inline7 = 1 in
                      let inline8 = @intrinsic(ref.set)(j/0, inline7) in
                      jump k0(true)
                    },
                    1 => {
                      let inline9 = 2 in
                      let inline10 = @intrinsic(ref.set)(j/0, inline9) in
                      jump k0(true)
                    },
                    2 => {
                      jump k0(true)
                    },
                    _ => {
                      jump k0(false)
                    },
                  }
                } in
                let inline11 = @intrinsic(ref.get)(j/0) in
                jump inline_cont5(inline11)
              } in
            } in
            jump loop1()
          } in
          let inline12 = 0 in
          let inline13 = @intrinsic(ref.new)(inline12) in
          jump inline_cont3(inline13)
        } in
        let inline14 = 0 in
        let inline15 = @intrinsic(ref.new)(inline14) in
        jump inline_cont2(inline15)
      } in
      let inline16 = trait_impl#ToString#isize#to_string(t0) in
      let inline17 = @runtime(core.string_println)(inline16) in
      jump inline_cont1(())
    } in
    let inline18 = @intrinsic(ref.get)(total/0) in
    jump inline_cont0(inline18)
  } in
  joinrec {
    join loop0() -> () {
      join inline_cont10(t7: isize) -> () {
        let t8 = (t7 == 0) in
        join k2(jp1: bool) -> () {
          if jp1 {
            join inline_cont11(t9: isize) -> () {
              join inline_cont12(t10: isize) -> () {
                let t11 = (t9 + t10) in
                join inline_cont13(_wild2: ()) -> () {
                  join inline_cont14(t12: isize) -> () {
                    let t13 = (t12 == 1) in
                    join k3() -> () {
                      join inline_cont15(t14: isize) -> () {
                        let t15 = (t14 + 1) in
                        let inline19 = @intrinsic(ref.set)(i/0, t15) in
                        jump loop0()
                      } in
                      let inline20 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont15(inline20)
                    } in
                    if t13 {
                      let inline21 = 2 in
                      let inline22 = @intrinsic(ref.set)(i/0, inline21) in
                      jump loop0()
                    } else {
                      jump k3()
                    }
                  } in
                  let inline23 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont14(inline23)
                } in
                let inline24 = @intrinsic(ref.set)(total/0, t11) in
                jump inline_cont13(())
              } in
              let inline25 = @intrinsic(ref.get)(i/0) in
              jump inline_cont12(inline25)
            } in
            let inline26 = @intrinsic(ref.get)(total/0) in
            jump inline_cont11(inline26)
          } else {
            jump exit0()
          }
        } in
        if t8 {
          let inline27 = 1 in
          let inline28 = @intrinsic(ref.set)(i/0, inline27) in
          jump k2(true)
        } else {
          join inline_cont16(t16: isize) -> () {
            let t17 = (t16 < 4) in
            if t17 {
              jump k2(true)
            } else {
              jump k2(false)
            }
          } in
          let inline29 = @intrinsic(ref.get)(i/0) in
          jump inline_cont16(inline29)
        }
      } in
      let inline30 = @intrinsic(ref.get)(i/0) in
      jump inline_cont10(inline30)
    } in
  } in
  jump loop0()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/0: isize) -> Ref[isize] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_isize(self/0: Ref[isize]) -> isize {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_isize(self/0: Ref[isize], value/0: isize) -> () {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_isize(value/0: isize) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}