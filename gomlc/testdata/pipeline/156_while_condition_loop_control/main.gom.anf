fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit424() -> unit {
    let t425 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild414 = println__T_int(t425) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont526(total2/3: Ref[int]) -> unit {
      join exit426() -> unit {
        join inline_cont524(t427: int) -> unit {
          let inline522 = trait_impl#ToString#int#to_string(t427) in
          let inline523 = @runtime(core.string_println)(inline522) in
          ()
        } in
        let inline525 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont524(inline525)
      } in
      joinrec {
        join loop428() -> unit {
          join inline_cont519(mtmp415: int) -> unit {
            join k429(jp430: bool) -> unit {
              if jp430 {
                join inline_cont517(t431: int) -> unit {
                  join inline_cont515(t432: int) -> unit {
                    let t433 = (t431 + t432) in
                    join inline_cont513(_wild418: unit) -> unit {
                      join inline_cont511(t435: int) -> unit {
                        join inline_cont508(t436: bool) -> unit {
                          join k434() -> unit {
                            jump loop428()
                          } in
                          if t436 {
                            jump exit426()
                          } else {
                            jump k434()
                          }
                        } in
                        let inline509 = 2 in
                        let inline510 = (t435 == inline509) in
                        jump inline_cont508(inline510)
                      } in
                      let inline512 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont511(inline512)
                    } in
                    let inline514 = @intrinsic(ref.set)(total2/3, t433) in
                    jump inline_cont513(())
                  } in
                  let inline516 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont515(inline516)
                } in
                let inline518 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont517(inline518)
              } else {
                jump exit426()
              }
            } in
            match mtmp415 {
              0 => {
                let inline503 = 1 in
                let inline504 = @intrinsic(ref.set)(j/2, inline503) in
                jump k429(true)
              },
              1 => {
                let inline506 = 2 in
                let inline507 = @intrinsic(ref.set)(j/2, inline506) in
                jump k429(true)
              },
              2 => {
                jump k429(true)
              },
              _ => {
                jump k429(false)
              },
            }
          } in
          let inline520 = @intrinsic(ref.get)(j/2) in
          jump inline_cont519(inline520)
        } in
      } in
      jump loop428()
    } in
    let inline527 = 0 in
    let inline528 = @intrinsic(ref.new)(inline527) in
    jump inline_cont526(inline528)
  } in
  joinrec {
    join loop437() -> unit {
      join inline_cont500(t448: int) -> unit {
        join inline_cont497(t449: bool) -> unit {
          join k438(jp439: bool) -> unit {
            if jp439 {
              join inline_cont495(t440: int) -> unit {
                join inline_cont493(t441: int) -> unit {
                  let t442 = (t440 + t441) in
                  join inline_cont491(_wild409: unit) -> unit {
                    join inline_cont489(t446: int) -> unit {
                      join inline_cont486(t447: bool) -> unit {
                        join k443() -> unit {
                          join inline_cont484(t444: int) -> unit {
                            let t445 = (t444 + 1) in
                            let inline483 = @intrinsic(ref.set)(i/0, t445) in
                            jump loop437()
                          } in
                          let inline485 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont484(inline485)
                        } in
                        if t447 {
                          let inline480 = 2 in
                          let inline481 = @intrinsic(ref.set)(i/0, inline480) in
                          jump loop437()
                        } else {
                          jump k443()
                        }
                      } in
                      let inline487 = 1 in
                      let inline488 = (t446 == inline487) in
                      jump inline_cont486(inline488)
                    } in
                    let inline490 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont489(inline490)
                  } in
                  let inline492 = @intrinsic(ref.set)(total/1, t442) in
                  jump inline_cont491(())
                } in
                let inline494 = @intrinsic(ref.get)(i/0) in
                jump inline_cont493(inline494)
              } in
              let inline496 = @intrinsic(ref.get)(total/1) in
              jump inline_cont495(inline496)
            } else {
              jump exit424()
            }
          } in
          if t449 {
            let inline475 = 1 in
            let inline476 = @intrinsic(ref.set)(i/0, inline475) in
            jump k438(true)
          } else {
            join inline_cont477(t452: int) -> unit {
              let t453 = (t452 < 4) in
              if t453 {
                jump k438(true)
              } else {
                jump k438(false)
              }
            } in
            let inline478 = @intrinsic(ref.get)(i/0) in
            jump inline_cont477(inline478)
          }
        } in
        let inline498 = 0 in
        let inline499 = (t448 == inline498) in
        jump inline_cont497(inline499)
      } in
      let inline501 = @intrinsic(ref.get)(i/0) in
      jump inline_cont500(inline501)
    } in
  } in
  jump loop437()
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t456 = @intrinsic(ref.new)(value/431) in
  t456
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t459 = @intrinsic(ref.get)(self/432) in
  t459
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t462 = (self/185 == other/186) in
  t462
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t464 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont529(t466: string) -> unit {
    let t467 = @runtime(core.string_println)(t466) in
    ()
  } in
  let inline530 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont529(inline530)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t470 = @runtime(core.int_to_string)(self/151) in
  t470
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t473 = @runtime(core.int_hash)(self/211) in
  t473
}