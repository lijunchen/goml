fn main() -> unit {
  join ret83() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit84() -> unit {
    let _wild73 = () in
    let t85 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild74 = println__T_int(t85) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total2/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit86() -> unit {
      let _wild81 = () in
      let t87 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
      let _wild82 = println__T_int(t87) in
      jump ret83()
    } in
    joinrec {
      join loop88() -> unit {
        let mtmp75 = inherent#Ref#Ref[T]#get__T_int(j/2) in
        join k89(jp90: bool) -> unit {
          if jp90 {
            let t91 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
            let t92 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t93 = (t91 + t92) in
            let _wild78 = inherent#Ref#Ref[T]#set__T_int(total2/3, t93) in
            let t95 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t96 = trait_impl#Eq#int#eq(t95, 2) in
            join k94() -> unit {
              let _wild80 = () in
              jump loop88()
            } in
            if t96 {
              jump exit86()
            } else {
              jump k94()
            }
          } else {
            jump exit86()
          }
        } in
        match mtmp75 {
          0 => {
            let _wild76 = inherent#Ref#Ref[T]#set__T_int(j/2, 1) in
            jump k89(true)
          },
          1 => {
            let _wild77 = inherent#Ref#Ref[T]#set__T_int(j/2, 2) in
            jump k89(true)
          },
          2 => {
            jump k89(true)
          },
          _ => {
            jump k89(false)
          },
        }
      } in
    } in
    jump loop88()
  } in
  joinrec {
    join loop97() -> unit {
      let t108 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t109 = trait_impl#Eq#int#eq(t108, 0) in
      join k98(jp99: bool) -> unit {
        if jp99 {
          let t100 = inherent#Ref#Ref[T]#get__T_int(total/1) in
          let t101 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t102 = (t100 + t101) in
          let _wild69 = inherent#Ref#Ref[T]#set__T_int(total/1, t102) in
          let t106 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t107 = trait_impl#Eq#int#eq(t106, 1) in
          join k103() -> unit {
            let _wild71 = () in
            let t104 = inherent#Ref#Ref[T]#get__T_int(i/0) in
            let t105 = (t104 + 1) in
            let _wild72 = inherent#Ref#Ref[T]#set__T_int(i/0, t105) in
            jump loop97()
          } in
          if t107 {
            let _wild70 = inherent#Ref#Ref[T]#set__T_int(i/0, 2) in
            jump loop97()
          } else {
            jump k103()
          }
        } else {
          jump exit84()
        }
      } in
      if t109 {
        let _wild68 = inherent#Ref#Ref[T]#set__T_int(i/0, 1) in
        jump k98(true)
      } else {
        let t112 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t113 = (t112 < 4) in
        join k110(jp111: bool) -> unit {
          jump k98(jp111)
        } in
        if t113 {
          jump k110(true)
        } else {
          jump k110(false)
        }
      }
    } in
  } in
  jump loop97()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret114(retv115: Ref[int]) -> Ref[int] {
    retv115
  } in
  let t116 = @intrinsic(ref.new)(value/207) in
  jump ret114(t116)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret117(retv118: int) -> int {
    retv118
  } in
  let t119 = @intrinsic(ref.get)(self/208) in
  jump ret117(t119)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret120(retv121: bool) -> bool {
    retv121
  } in
  let t122 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret120(t122)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret123() -> unit {
    ()
  } in
  let t124 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret123()
}

fn println__T_int(value/1: int) -> unit {
  join ret125() -> unit {
    ()
  } in
  let t126 = trait_impl#ToString#int#to_string(value/1) in
  let t127 = @runtime(core.string_println)(t126) in
  jump ret125()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret128(retv129: string) -> string {
    retv129
  } in
  let t130 = @runtime(core.int_to_string)(self/40) in
  jump ret128(t130)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret131(retv132: uint64) -> uint64 {
    retv132
  } in
  let t133 = @runtime(core.int_hash)(self/85) in
  jump ret131(t133)
}