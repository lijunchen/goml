fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit203() -> unit {
    let t204 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild193 = println__T_int(t204) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont305(total2/3: Ref[int]) -> unit {
      join exit205() -> unit {
        join inline_cont303(t206: int) -> unit {
          let inline301 = trait_impl#ToString#int#to_string(t206) in
          let inline302 = @runtime(core.string_println)(inline301) in
          ()
        } in
        let inline304 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont303(inline304)
      } in
      joinrec {
        join loop207() -> unit {
          join inline_cont298(mtmp194: int) -> unit {
            join k208(jp209: bool) -> unit {
              if jp209 {
                join inline_cont296(t210: int) -> unit {
                  join inline_cont294(t211: int) -> unit {
                    let t212 = (t210 + t211) in
                    join inline_cont292(_wild197: unit) -> unit {
                      join inline_cont290(t214: int) -> unit {
                        join inline_cont287(t215: bool) -> unit {
                          join k213() -> unit {
                            jump loop207()
                          } in
                          if t215 {
                            jump exit205()
                          } else {
                            jump k213()
                          }
                        } in
                        let inline288 = 2 in
                        let inline289 = (t214 == inline288) in
                        jump inline_cont287(inline289)
                      } in
                      let inline291 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont290(inline291)
                    } in
                    let inline293 = @intrinsic(ref.set)(total2/3, t212) in
                    jump inline_cont292(())
                  } in
                  let inline295 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont294(inline295)
                } in
                let inline297 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont296(inline297)
              } else {
                jump exit205()
              }
            } in
            match mtmp194 {
              0 => {
                let inline282 = 1 in
                let inline283 = @intrinsic(ref.set)(j/2, inline282) in
                jump k208(true)
              },
              1 => {
                let inline285 = 2 in
                let inline286 = @intrinsic(ref.set)(j/2, inline285) in
                jump k208(true)
              },
              2 => {
                jump k208(true)
              },
              _ => {
                jump k208(false)
              },
            }
          } in
          let inline299 = @intrinsic(ref.get)(j/2) in
          jump inline_cont298(inline299)
        } in
      } in
      jump loop207()
    } in
    let inline306 = 0 in
    let inline307 = @intrinsic(ref.new)(inline306) in
    jump inline_cont305(inline307)
  } in
  joinrec {
    join loop216() -> unit {
      join inline_cont279(t227: int) -> unit {
        join inline_cont276(t228: bool) -> unit {
          join k217(jp218: bool) -> unit {
            if jp218 {
              join inline_cont274(t219: int) -> unit {
                join inline_cont272(t220: int) -> unit {
                  let t221 = (t219 + t220) in
                  join inline_cont270(_wild188: unit) -> unit {
                    join inline_cont268(t225: int) -> unit {
                      join inline_cont265(t226: bool) -> unit {
                        join k222() -> unit {
                          join inline_cont263(t223: int) -> unit {
                            let t224 = (t223 + 1) in
                            let inline262 = @intrinsic(ref.set)(i/0, t224) in
                            jump loop216()
                          } in
                          let inline264 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont263(inline264)
                        } in
                        if t226 {
                          let inline259 = 2 in
                          let inline260 = @intrinsic(ref.set)(i/0, inline259) in
                          jump loop216()
                        } else {
                          jump k222()
                        }
                      } in
                      let inline266 = 1 in
                      let inline267 = (t225 == inline266) in
                      jump inline_cont265(inline267)
                    } in
                    let inline269 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont268(inline269)
                  } in
                  let inline271 = @intrinsic(ref.set)(total/1, t221) in
                  jump inline_cont270(())
                } in
                let inline273 = @intrinsic(ref.get)(i/0) in
                jump inline_cont272(inline273)
              } in
              let inline275 = @intrinsic(ref.get)(total/1) in
              jump inline_cont274(inline275)
            } else {
              jump exit203()
            }
          } in
          if t228 {
            let inline254 = 1 in
            let inline255 = @intrinsic(ref.set)(i/0, inline254) in
            jump k217(true)
          } else {
            join inline_cont256(t231: int) -> unit {
              let t232 = (t231 < 4) in
              if t232 {
                jump k217(true)
              } else {
                jump k217(false)
              }
            } in
            let inline257 = @intrinsic(ref.get)(i/0) in
            jump inline_cont256(inline257)
          }
        } in
        let inline277 = 0 in
        let inline278 = (t227 == inline277) in
        jump inline_cont276(inline278)
      } in
      let inline280 = @intrinsic(ref.get)(i/0) in
      jump inline_cont279(inline280)
    } in
  } in
  jump loop216()
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t235 = @intrinsic(ref.new)(value/273) in
  t235
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t238 = @intrinsic(ref.get)(self/274) in
  t238
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t241 = (self/101 == other/102) in
  t241
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t243 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont308(t245: string) -> unit {
    let t246 = @runtime(core.string_println)(t245) in
    ()
  } in
  let inline309 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont308(inline309)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t249 = @runtime(core.int_to_string)(self/67) in
  t249
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t252 = @runtime(core.int_hash)(self/127) in
  t252
}