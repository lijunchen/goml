fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit193() -> unit {
    let _wild182 = () in
    let t194 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild183 = println__T_int(t194) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join inline_cont295(total2/3: Ref[int]) -> unit {
      join exit195() -> unit {
        let _wild190 = () in
        join inline_cont293(t196: int) -> unit {
          let inline291 = trait_impl#ToString#int#to_string(t196) in
          let inline292 = @runtime(core.string_println)(inline291) in
          ()
        } in
        let inline294 = @intrinsic(ref.get)(total2/3) in
        jump inline_cont293(inline294)
      } in
      joinrec {
        join loop197() -> unit {
          join inline_cont288(mtmp184: int) -> unit {
            join k198(jp199: bool) -> unit {
              if jp199 {
                join inline_cont286(t200: int) -> unit {
                  join inline_cont284(t201: int) -> unit {
                    let t202 = (t200 + t201) in
                    join inline_cont282(_wild187: unit) -> unit {
                      join inline_cont280(t204: int) -> unit {
                        join inline_cont277(t205: bool) -> unit {
                          join k203() -> unit {
                            let _wild189 = () in
                            jump loop197()
                          } in
                          if t205 {
                            jump exit195()
                          } else {
                            jump k203()
                          }
                        } in
                        let inline278 = 2 in
                        let inline279 = @intrinsic(eq.primitive)(t204, inline278) in
                        jump inline_cont277(inline279)
                      } in
                      let inline281 = @intrinsic(ref.get)(j/2) in
                      jump inline_cont280(inline281)
                    } in
                    let inline283 = @intrinsic(ref.set)(total2/3, t202) in
                    jump inline_cont282(())
                  } in
                  let inline285 = @intrinsic(ref.get)(j/2) in
                  jump inline_cont284(inline285)
                } in
                let inline287 = @intrinsic(ref.get)(total2/3) in
                jump inline_cont286(inline287)
              } else {
                jump exit195()
              }
            } in
            match mtmp184 {
              0 => {
                let inline272 = 1 in
                let inline273 = @intrinsic(ref.set)(j/2, inline272) in
                jump k198(true)
              },
              1 => {
                let inline275 = 2 in
                let inline276 = @intrinsic(ref.set)(j/2, inline275) in
                jump k198(true)
              },
              2 => {
                jump k198(true)
              },
              _ => {
                jump k198(false)
              },
            }
          } in
          let inline289 = @intrinsic(ref.get)(j/2) in
          jump inline_cont288(inline289)
        } in
      } in
      jump loop197()
    } in
    let inline296 = 0 in
    let inline297 = @intrinsic(ref.new)(inline296) in
    jump inline_cont295(inline297)
  } in
  joinrec {
    join loop206() -> unit {
      join inline_cont269(t217: int) -> unit {
        join inline_cont266(t218: bool) -> unit {
          join k207(jp208: bool) -> unit {
            if jp208 {
              join inline_cont264(t209: int) -> unit {
                join inline_cont262(t210: int) -> unit {
                  let t211 = (t209 + t210) in
                  join inline_cont260(_wild178: unit) -> unit {
                    join inline_cont258(t215: int) -> unit {
                      join inline_cont255(t216: bool) -> unit {
                        join k212() -> unit {
                          let _wild180 = () in
                          join inline_cont253(t213: int) -> unit {
                            let t214 = (t213 + 1) in
                            let inline252 = @intrinsic(ref.set)(i/0, t214) in
                            jump loop206()
                          } in
                          let inline254 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont253(inline254)
                        } in
                        if t216 {
                          let inline249 = 2 in
                          let inline250 = @intrinsic(ref.set)(i/0, inline249) in
                          jump loop206()
                        } else {
                          jump k212()
                        }
                      } in
                      let inline256 = 1 in
                      let inline257 = @intrinsic(eq.primitive)(t215, inline256) in
                      jump inline_cont255(inline257)
                    } in
                    let inline259 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont258(inline259)
                  } in
                  let inline261 = @intrinsic(ref.set)(total/1, t211) in
                  jump inline_cont260(())
                } in
                let inline263 = @intrinsic(ref.get)(i/0) in
                jump inline_cont262(inline263)
              } in
              let inline265 = @intrinsic(ref.get)(total/1) in
              jump inline_cont264(inline265)
            } else {
              jump exit193()
            }
          } in
          if t218 {
            let inline244 = 1 in
            let inline245 = @intrinsic(ref.set)(i/0, inline244) in
            jump k207(true)
          } else {
            join inline_cont246(t221: int) -> unit {
              let t222 = (t221 < 4) in
              if t222 {
                jump k207(true)
              } else {
                jump k207(false)
              }
            } in
            let inline247 = @intrinsic(ref.get)(i/0) in
            jump inline_cont246(inline247)
          }
        } in
        let inline267 = 0 in
        let inline268 = @intrinsic(eq.primitive)(t217, inline267) in
        jump inline_cont266(inline268)
      } in
      let inline270 = @intrinsic(ref.get)(i/0) in
      jump inline_cont269(inline270)
    } in
  } in
  jump loop206()
}

fn inherent#Ref#Ref[T]#new__T_int(value/236: int) -> Ref[int] {
  let t225 = @intrinsic(ref.new)(value/236) in
  t225
}

fn inherent#Ref#Ref[T]#get__T_int(self/237: Ref[int]) -> int {
  let t228 = @intrinsic(ref.get)(self/237) in
  t228
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t231 = @intrinsic(eq.primitive)(self/88, other/89) in
  t231
}

fn inherent#Ref#Ref[T]#set__T_int(self/238: Ref[int], value/239: int) -> unit {
  let t233 = @intrinsic(ref.set)(self/238, value/239) in
  ()
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont298(t235: string) -> unit {
    let t236 = @runtime(core.string_println)(t235) in
    ()
  } in
  let inline299 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont298(inline299)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t239 = @runtime(core.int_to_string)(self/69) in
  t239
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t242 = @runtime(core.int_hash)(self/114) in
  t242
}