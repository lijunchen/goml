fn countdown(start/0: i32) -> FnIterator__i32 {
  let current/0 = inherent#Ref#Ref[T]#new__T_i0(start/0) in
  inherent#FnIterator#FnIterator[T]#from_fn__T_i0(
    pack_closure[inherent#closure_env_countdown_0#closure_env_countdown_0#apply](
      closure_env_countdown_0 { current_0: current/0 }
    )
  )
}

fn counted_range(calls/0: Ref[i32]) -> FnIterator__isize {
  let _wild0 =
    inherent#Ref#Ref[T]#set__T_i0(calls/0, (inherent#Ref#Ref[T]#get__T_i0(calls/0) + 1)) in
  range(1, 5)
}

fn first_even(values/0: FnIterator__isize) -> isize {
  let _wild0 =
    let for_iter0 = trait_impl#IntoIterator#FnIterator__isize#into_iter(values/0) in
    while true {
        let for_next0 = trait_impl#Iterator#FnIterator__isize#next(for_iter0) in
        match for_next0 {
          Option__isize::None => {
            break
          },
          Option__isize::Some(x0) => {
            let x0 = Option__isize::Some._0(for_next0) in
            let value/0 = x0 in
            let _wild1 =
              if (((value/0 / 2) * 2) == value/0) {
                  return value/0
              } else {
                  ()
              } in
            ()
          },
        }
    } in
  -1
}

fn main() -> unit {
  let values/0 = inherent#Vec#Vec[T]#new__T_i0() in
  let _wild0 = inherent#Vec#Vec[T]#push__T_i0(values/0, 10) in
  let _wild1 = inherent#Vec#Vec[T]#push__T_i0(values/0, 20) in
  let _wild2 = inherent#Vec#Vec[T]#push__T_i0(values/0, 30) in
  let sum/0 = inherent#Ref#Ref[T]#new__T_i0(0) in
  let _wild3 =
    let for_source0 = values/0 in
    let for_limit0 = @intrinsic(vec.len)(for_source0) in
    let for_index0 = 0 in
    while (for_index0 < for_limit0) {
        let for_item0 = @intrinsic(vec.get)(for_source0, for_index0) in
        let _wild4 = for_index0 = (for_index0 + 1) in
        let value/0 = for_item0 in
        let _wild5 =
          if (value/0 == 20) {
              continue
          } else {
              ()
          } in
        let _wild6 =
          inherent#Ref#Ref[T]#set__T_i0(sum/0, (inherent#Ref#Ref[T]#get__T_i0(sum/0) + value/0)) in
        ()
    } in
  let _wild7 = println__T_i0(inherent#Ref#Ref[T]#get__T_i0(sum/0)) in
  let pairs/0 = inherent#Vec#Vec[T]#new__T_(i32,string)() in
  let _wild8 = inherent#Vec#Vec[T]#push__T_(i32,string)(pairs/0, (1, "a")) in
  let _wild9 = inherent#Vec#Vec[T]#push__T_(i32,string)(pairs/0, (2, "b")) in
  let _wild10 =
    let for_source1 = pairs/0 in
    let for_limit1 = @intrinsic(vec.len)(for_source1) in
    let for_index1 = 0 in
    while (for_index1 < for_limit1) {
        let for_item1 = @intrinsic(vec.get)(for_source1, for_index1) in
        let _wild11 = for_index1 = (for_index1 + 1) in
        let x0 = for_item1.0 in
        let x1 = for_item1.1 in
        let text/0 = x1 in
        let number/0 = x0 in
        let _wild12 = println__T_string((inherent#i32#i32#to_string(number/0) + text/0)) in
        ()
    } in
  let calls/0 = inherent#Ref#Ref[T]#new__T_i0(0) in
  let range_sum/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  let _wild13 =
    let for_iter0 = trait_impl#IntoIterator#FnIterator__isize#into_iter(counted_range(calls/0)) in
    while true {
        let for_next0 = trait_impl#Iterator#FnIterator__isize#next(for_iter0) in
        match for_next0 {
          Option__isize::None => {
            break
          },
          Option__isize::Some(x2) => {
            let x2 = Option__isize::Some._0(for_next0) in
            let value/1 = x2 in
            let _wild14 =
              inherent#Ref#Ref[T]#set__T_isize(
                range_sum/0,
                (inherent#Ref#Ref[T]#get__T_isize(range_sum/0) + value/1)
              ) in
            ()
          },
        }
    } in
  let _wild15 = println__T_i0(inherent#Ref#Ref[T]#get__T_i0(calls/0)) in
  let _wild16 = println__T_isize(inherent#Ref#Ref[T]#get__T_isize(range_sum/0)) in
  let slice_sum/0 = inherent#Ref#Ref[T]#new__T_i0(0) in
  let _wild17 =
    let for_source2 = inherent#Vec#Vec[T]#slice__T_i0(values/0, 1, 3) in
    let for_limit2 = @intrinsic(slice.len)(for_source2) in
    let for_index2 = 0 in
    while (for_index2 < for_limit2) {
        let for_item2 = @intrinsic(slice.get)(for_source2, for_index2) in
        let _wild18 = for_index2 = (for_index2 + 1) in
        let value/2 = for_item2 in
        let _wild19 =
          inherent#Ref#Ref[T]#set__T_i0(
            slice_sum/0,
            (inherent#Ref#Ref[T]#get__T_i0(slice_sum/0) + value/2)
          ) in
        ()
    } in
  let _wild20 = println__T_i0(inherent#Ref#Ref[T]#get__T_i0(slice_sum/0)) in
  let _wild21 =
    let for_iter1 = trait_impl#IntoIterator#FnIterator__i32#into_iter(countdown(4)) in
    while true {
        let for_next1 = trait_impl#Iterator#FnIterator__i32#next(for_iter1) in
        match for_next1 {
          Option__i32::None => {
            break
          },
          Option__i32::Some(x3) => {
            let x3 = Option__i32::Some._0(for_next1) in
            let value/3 = x3 in
            let _wild22 =
              if (value/3 == 2) {
                  break
              } else {
                  let _wild23 = println__T_i0(value/3) in
                  ()
              } in
            ()
          },
        }
    } in
  let empty/0 = range(0, 0) in
  let _wild24 =
    let for_iter2 = trait_impl#IntoIterator#FnIterator__isize#into_iter(empty/0) in
    while true {
        let for_next2 = trait_impl#Iterator#FnIterator__isize#next(for_iter2) in
        match for_next2 {
          Option__isize::None => {
            break
          },
          Option__isize::Some(x4) => {
            ()
          },
        }
    } in
  let _wild25 = println__T_isize(first_even(range(3, 8))) in
  let _wild26 = println__T_string("done") in
  ()
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  @intrinsic(ref.new)(value/0)
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  @intrinsic(ref.get)(self/0)
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> unit {
  @intrinsic(ref.set)(self/0, value/0)
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_i32(
  next_fn/0: () -> Option__i32
) -> FnIterator__i32 {
  FnIterator__i32 { next_fn: next_fn/0 }
}

fn range(start/0: isize, end/0: isize) -> FnIterator__isize {
  __goml_builtin_range(start/0, end/0)
}

fn trait_impl#IntoIterator#FnIterator__isize#into_iter(
  self/0: FnIterator__isize
) -> FnIterator__isize {
  self/0
}

fn trait_impl#Iterator#FnIterator__isize#next(self/0: FnIterator__isize) -> Option__isize {
  FnIterator__isize.next_fn(self/0)()
}

fn inherent#Vec#Vec[T]#new__T_i32() -> Vec[i32] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#push__T_i32(self/0: Vec[i32], elem/0: i32) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn println__T_i32(value/0: i32) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#i32#to_string(value/0))
}

fn inherent#Vec#Vec[T]#new__T_(i32,string)() -> Vec[(i32, string)] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#push__T_(i32,string)(
  self/0: Vec[(i32, string)],
  elem/0: (i32, string)
) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn println__T_string(value/0: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/0: isize) -> Ref[isize] {
  @intrinsic(ref.new)(value/0)
}

fn inherent#Ref#Ref[T]#get__T_isize(self/0: Ref[isize]) -> isize {
  @intrinsic(ref.get)(self/0)
}

fn inherent#Ref#Ref[T]#set__T_isize(self/0: Ref[isize], value/0: isize) -> unit {
  @intrinsic(ref.set)(self/0, value/0)
}

fn println__T_isize(value/0: isize) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#isize#to_string(value/0))
}

fn inherent#Vec#Vec[T]#slice__T_i32(self/0: Vec[i32], start/0: isize, end/0: isize) -> Slice[i32] {
  @intrinsic(slice.new)(self/0, start/0, end/0)
}

fn trait_impl#IntoIterator#FnIterator__i32#into_iter(self/0: FnIterator__i32) -> FnIterator__i32 {
  self/0
}

fn trait_impl#Iterator#FnIterator__i32#next(self/0: FnIterator__i32) -> Option__i32 {
  FnIterator__i32.next_fn(self/0)()
}

fn __goml_builtin_range(start/0: isize, end/0: isize) -> FnIterator__isize {
  let current/0 = @intrinsic(ref.new)(start/0) in
  inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
    pack_closure[inherent#closure_env_goml_builtin_range_1#closure_env_goml_builtin_range_1#apply](
      closure_env_goml_builtin_range_1 { current_0: current/0, end_1: end/0 }
    )
  )
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  __goml_builtin_int_to_string(self/0)
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
  next_fn/0: () -> Option__isize
) -> FnIterator__isize {
  FnIterator__isize { next_fn: next_fn/0 }
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  signed_decimal_string((value/0 as i64))
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  (self/0 == other/0)
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  @runtime(core.int32_hash)(self/0)
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  (self/0 == other/0)
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  @runtime(core.int_hash)(self/0)
}

fn inherent#closure_env_countdown_0#closure_env_countdown_0#apply(
  env0: closure_env_countdown_0
) -> Option__i32 {
  let current/0 = closure_env_countdown_0.current_0(env0) in
  let value/0 = inherent#Ref#Ref[T]#get__T_i0(current/0) in
  if (value/0 > 0) {
      let _wild0 = inherent#Ref#Ref[T]#set__T_i0(current/0, (value/0 - 1)) in
      Option__i32::Some(value/0)
  } else {
      Option__i32::None
  }
}

fn inherent#closure_env_goml_builtin_range_1#closure_env_goml_builtin_range_1#apply(
  env0: closure_env_goml_builtin_range_1
) -> Option__isize {
  let current/0 = closure_env_goml_builtin_range_1.current_0(env0) in
  let end/0 = closure_env_goml_builtin_range_1.end_1(env0) in
  let value/0 = @intrinsic(ref.get)(current/0) in
  if (value/0 < end/0) {
      let _wild0 = @intrinsic(ref.set)(current/0, (value/0 + 1)) in
      Option__isize::Some(value/0)
  } else {
      Option__isize::None
  }
}