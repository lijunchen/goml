fn make_some(value/0: i32) -> Maybe__i32 {
  let t0 = Maybe__i32::Some(value/0) in
  t0
}

fn build_point(x/0: i32, y/0: i32) -> Point {
  let t0 = Point { x: x/0, y: y/0 } in
  t0
}

fn magnitude(p/0: Point) -> i32 {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  let t0 = (x0 + x1) in
  t0
}

fn main() -> i32 {
  join commute_arm0(commute_field0: i32) -> i32 {
    join inline_cont0(pt/0: Point) -> i32 {
      let t0 = Point.x(pt/0) in
      join inline_cont1(t1: i32) -> i32 {
        let t2 = (t0 + t1) in
        t2
      } in
      let inline0 = Point.x(pt/0) in
      let inline1 = Point.y(pt/0) in
      let inline2 = (inline0 + inline1) in
      jump inline_cont1(inline2)
    } in
    let inline3 = 7 in
    let inline4 = Point { x: commute_field0, y: inline3 } in
    jump inline_cont0(inline4)
  } in
  let inline5 = 5 in
  jump commute_arm0(inline5)
}