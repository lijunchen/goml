fn trait_impl#Parent#Count#get(self/0: Count) -> i32 {
  let t799 = Count.value(self/0) in
  t799
}

fn trait_impl#Child#Count#child(self/1: Count) -> unit {
  ()
}

fn main() -> unit {
  let t802 = Count { value: 42 } in
  join inline_cont859(t803: i32) -> unit {
    let inline857 = trait_impl#ToString#i32#to_string(t803) in
    let inline858 = @runtime(core.string_println)(inline857) in
    ()
  } in
  let inline860 = trait_impl#Parent#Count#get(t802) in
  jump inline_cont859(inline860)
}

fn println__T_i32(value/1: i32) -> unit {
  join inline_cont861(t805: string) -> unit {
    let t806 = @runtime(core.string_println)(t805) in
    ()
  } in
  let inline862 = __goml_builtin_int32_to_string(value/1) in
  jump inline_cont861(inline862)
}

fn get_from_child__C_Count(value/2: Count) -> i32 {
  let inline864 = Count.value(value/2) in
  inline864
}

fn trait_impl#ToString#i32#to_string(self/407: i32) -> string {
  let inline866 = (self/407 as i64) in
  let inline867 = signed_decimal_string(inline866) in
  inline867
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t815 = (value/225 as i64) in
  let inline869 = (t815 < 0) in
  if inline869 {
    let inline870 = (t815 as u64) in
    let inline871 = (0 - inline870) in
    let inline872 = decimal_string(inline871) in
    let inline873 = ("-" + inline872) in
    inline873
  } else {
    let inline874 = (t815 as u64) in
    let inline875 = decimal_string(inline874) in
    inline875
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t821 = (value/214 < 0) in
  if t821 {
    let t822 = (value/214 as u64) in
    let t823 = (0 - t822) in
    let t824 = decimal_string(t823) in
    let t825 = ("-" + t824) in
    t825
  } else {
    let t826 = (value/214 as u64) in
    let t827 = decimal_string(t826) in
    t827
  }
}

fn decimal_string(value/208: u64) -> string {
  let t850 = (value/208 == 0) in
  join k830() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit831() -> string {
      join inline_cont884(t832: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t832) in
        let offset/212 = 0 in
        join exit833() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop834() -> string {
            join inline_cont882(t835: isize) -> string {
              let t836 = (offset/212 < t835) in
              if t836 {
                join inline_cont880(t837: isize) -> string {
                  let t838 = (t837 - offset/212) in
                  let t839 = (t838 - 1) in
                  let t840 = @intrinsic(vec.get)(reversed/209, t839) in
                  join inline_cont878(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t841 = (compound_old358 + compound_value359) in
                    let t842 = offset/212 = t841 in
                    jump loop834()
                  } in
                  let inline879 = @intrinsic(vec.push)(bytes/211, t840) in
                  jump inline_cont878(())
                } in
                let inline881 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont880(inline881)
              } else {
                jump exit833()
              }
            } in
            let inline883 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont882(inline883)
          } in
        } in
        jump loop834()
      } in
      let inline885 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont884(inline885)
    } in
    joinrec {
      join loop843() -> string {
        let t844 = (remaining/210 > 0) in
        if t844 {
          let t845 = (remaining/210 % 10) in
          let t846 = (t845 as u8) in
          let t847 = (t846 + 48) in
          join inline_cont876(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t848 = (compound_old353 / compound_value354) in
            let t849 = remaining/210 = t848 in
            jump loop843()
          } in
          let inline877 = @intrinsic(vec.push)(reversed/209, t847) in
          jump inline_cont876(())
        } else {
          jump exit831()
        }
      } in
    } in
    jump loop843()
  } in
  if t850 {
    "0"
  } else {
    jump k830()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t852 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t855 = @intrinsic(vec.len)(self/526) in
  t855
}