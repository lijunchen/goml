fn maybe_value(flag/0: bool) -> Option[i32] {
    if (flag/0 : bool) {
        {
            Option::Some(41)
        }
    } else {
        {
            Option::None
        }
    }
}

fn compute(flag/1: bool) -> Option[i32] {
    let value/2: i32 =
      match (maybe_value : (bool) -> Option[i32])((flag/1 : bool)) {
          Option::Some(try_value/11: i32) => (try_value/11 : i32),
          Option::None => return Option::None,
      };
    Option::Some((value/2 : i32) + 1)
}

fn show(value/3: Option[i32]) -> string {
    match (value/3 : Option[i32]) {
        Option::Some(value/4: i32) => (i32::to_string : (i32) -> string)((value/4 : i32)),
        Option::None => "none",
    }
}

fn main() -> () {
    (println : (string) -> ())(
      (show : (Option[i32]) -> string)((compute : (bool) -> Option[i32])(true))
    );
    (println : (string) -> ())(
      (show : (Option[i32]) -> string)((compute : (bool) -> Option[i32])(false))
    )
}