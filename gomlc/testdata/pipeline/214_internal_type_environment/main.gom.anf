fn inherent#Point#Point#new(x/0: i32, y/0: i32) -> Point {
  let t0 = Point { x: x/0, y: y/0 } in
  t0
}

fn inherent#Point#Point#copy(self/0: Point, other/0: Point) -> Point {
  let t0 = Point.x(self/0) in
  let t1 = Point.x(other/0) in
  let t2 = (t0 + t1) in
  let t3 = Point.y(self/0) in
  let t4 = Point.y(other/0) in
  let t5 = (t3 + t4) in
  let t6 = Point { x: t2, y: t5 } in
  t6
}

fn inherent#Point#Point#origin() -> Point {
  let t0 = Point { x: 0, y: 0 } in
  t0
}

fn shape_value(value/0: Shape__i32) -> i32 {
  match value/0 {
    Tag_0 => {
      let x0 = Shape__i32::Dot._0(value/0) in
      let t0 = Point.x(x0) in
      let t1 = Point.y(x0) in
      let t2 = (t0 + t1) in
      t2
    },
    Tag_1 => {
      let x1 = Shape__i32::Wrapped._0(value/0) in
      let t3 = Wrapper__i32.value(x1) in
      t3
    },
    Tag_2 => {
      0
    },
  }
}

fn list_value(value/0: List) -> i32 {
  match value/0 {
    Tag_0 => {
      let x0 = List::Cons._0(value/0) in
      let t0 = Node.value(x0) in
      let t1 = Node.next(x0) in
      let t2 = list_value(t1) in
      let t3 = (t0 + t2) in
      t3
    },
    Tag_1 => {
      0
    },
  }
}

fn main() -> unit {
  let offset/0 = 1 in
  let t0 = closure_env_add_0 { offset_0: offset/0 } in
  let add/0 = pack_closure[inherent#closure_env_add_0#closure_env_add_0#apply](t0) in
  let t1 = add/0(1) in
  join inline_cont0(point/0: Point) -> unit {
    join inline_cont1(t2: Point) -> unit {
      join inline_cont2(combined/0: Point) -> unit {
        join inline_cont3(t3: i32) -> unit {
          let t4 = Node { value: 5, next: Tag_1 } in
          let list/0 = List::Cons(t4) in
          let t5 = Point.x(combined/0) in
          let t6 = Point.y(combined/0) in
          let t7 = (t5 + t6) in
          join inline_cont4(t8: i32) -> unit {
            let t9 = (t7 + t8) in
            let t10 = list_value(list/0) in
            let t11 = (t9 + t10) in
            join inline_cont5(t12: string) -> unit {
              let inline0 = trait_impl#ToString#string#to_string(t12) in
              let inline1 = @runtime(core.string_println)(inline0) in
              ()
            } in
            let inline2 = __goml_builtin_int32_to_string(t11) in
            jump inline_cont5(inline2)
          } in
          jump inline_cont4(t3)
        } in
        let inline3 = 4 in
        let inline4 = closure_env_id_1 {} in
        let inline5 = pack_closure[inherent#closure_env_id_1#closure_env_id_1#apply](inline4) in
        let inline6 = inline5(inline3) in
        jump inline_cont3(inline6)
      } in
      let inline7 = Point.x(point/0) in
      let inline8 = Point.x(t2) in
      let inline9 = (inline7 + inline8) in
      let inline10 = Point.y(point/0) in
      let inline11 = Point.y(t2) in
      let inline12 = (inline10 + inline11) in
      let inline13 = Point { x: inline9, y: inline12 } in
      jump inline_cont2(inline13)
    } in
    let inline14 = Point { x: 0, y: 0 } in
    jump inline_cont1(inline14)
  } in
  let inline15 = 3 in
  let inline16 = Point { x: t1, y: inline15 } in
  jump inline_cont0(inline16)
}

fn wrap__T_i32(value/0: i32) -> i32 {
  let t0 = closure_env_id_1 {} in
  let id/0 = pack_closure[inherent#closure_env_id_1#closure_env_id_1#apply](t0) in
  let t1 = id/0(value/0) in
  t1
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn inherent#closure_env_add_0#closure_env_add_0#apply(
  env0: closure_env_add_0,
  value/0: i32
) -> i32 {
  let offset/0 = closure_env_add_0.offset_0(env0) in
  let t0 = (value/0 + offset/0) in
  t0
}

fn inherent#closure_env_id_1#closure_env_id_1#apply(env0: closure_env_id_1, item/0: i32) -> i32 {
  item/0
}