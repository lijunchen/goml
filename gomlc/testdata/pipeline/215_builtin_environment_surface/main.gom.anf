fn main() -> unit {
  let native/0 = 7 in
  let small/0 = 8 in
  let unsigned/0 = 9 in
  join inline_cont0(t0: string) -> unit {
    join inline_cont1(t1: string) -> unit {
      let t2 = (t0 + t1) in
      join inline_cont2(t3: string) -> unit {
        let t4 = (t2 + t3) in
        join inline_cont3(t5: string) -> unit {
          let text/0 = (t4 + t5) in
          let value/0 = @intrinsic(ref.new)(1) in
          let _wild0 = @intrinsic(ref.set)(value/0, 2) in
          let same/0 = @intrinsic(ref.ptr_eq)(value/0, value/0) in
          let mtmp0 = @intrinsic(ref.ptr_hash)(value/0) in
          let values/0 = @intrinsic(vec.new)() in
          let t6 = @intrinsic(ref.get)(value/0) in
          let _wild1 = @intrinsic(vec.push)(values/0, t6) in
          let _wild2 = @intrinsic(vec.push)(values/0, 3) in
          let _wild3 = @intrinsic(vec.set)(values/0, 1, 4) in
          let t7 = @intrinsic(vec.len)(values/0) in
          let values_slice/0 = @intrinsic(slice.new)(values/0, 0, t7) in
          let t8 = @intrinsic(slice.len)(values_slice/0) in
          let nested/0 = @intrinsic(slice.sub)(values_slice/0, 0, t8) in
          let channel/0 = @intrinsic(channel.new)(1) in
          let t9 = @intrinsic(slice.get)(nested/0, 1) in
          let _wild4 = @intrinsic(channel.send)(channel/0, t9) in
          let received/0 = @intrinsic(channel.recv)(channel/0) in
          let _wild5 = @intrinsic(channel.close)(channel/0) in
          join inline_cont4(t10: FnIterator__isize) -> unit {
            let t11 = closure_env_main_0 {} in
            let t12 = pack_closure[inherent#closure_env_main_0#closure_env_main_0#apply](t11) in
            let total/0 = std::iter::fold__A_isize__I_FnIterator[isize]__T_isize(t10, 0, t12) in
            join inline_cont5(t13: string) -> unit {
              let t14 = (text/0 + t13) in
              let t15 = received/0.0 in
              join inline_cont6(t16: string) -> unit {
                let t17 = (t14 + t16) in
                let t18 = received/0.1 in
                join inline_cont7(t19: string) -> unit {
                  let t20 = (t17 + t19) in
                  join inline_cont8(t21: string) -> unit {
                    let t22 = (t20 + t21) in
                    let inline0 = trait_impl#ToString#string#to_string(t22) in
                    let inline1 = @runtime(core.string_println)(inline0) in
                    ()
                  } in
                  let inline2 = __goml_builtin_int_to_string(total/0) in
                  jump inline_cont8(inline2)
                } in
                let inline3 = @runtime(core.bool_to_string)(t18) in
                jump inline_cont7(inline3)
              } in
              let inline4 = __goml_builtin_int32_to_string(t15) in
              jump inline_cont6(inline4)
            } in
            let inline5 = @runtime(core.bool_to_string)(same/0) in
            jump inline_cont5(inline5)
          } in
          let inline6 = 0 in
          let inline7 = 3 in
          let inline8 = __goml_builtin_range(inline6, inline7) in
          jump inline_cont4(inline8)
        } in
        let inline9 = "abcd" in
        let inline10 = 1 in
        let inline11 = 3 in
        let inline12 = string_is_char_boundary(inline9, inline10) in
        join inline_join0(inline13: bool) -> unit {
          if inline13 {
            let inline14 = @runtime(core.string_byte_slice)(inline9, inline10, inline11) in
            jump inline_cont3(inline14)
          } else {
            let inline15 = @runtime(core.string_byte_slice)(inline9, -1, -1) in
            jump inline_cont3(inline15)
          }
        } in
        if inline12 {
          let inline16 = string_is_char_boundary(inline9, inline11) in
          jump inline_join0(inline16)
        } else {
          jump inline_join0(false)
        }
      } in
      let inline17 = __goml_builtin_uint8_to_string(unsigned/0) in
      jump inline_cont2(inline17)
    } in
    let inline18 = __goml_builtin_int8_to_string(small/0) in
    jump inline_cont1(inline18)
  } in
  let inline19 = __goml_builtin_int_to_string(native/0) in
  jump inline_cont0(inline19)
}

fn int_to_string(value/0: isize) -> string {
  let inline0 = (value/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn int8_to_string(value/0: i8) -> string {
  let inline0 = (value/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn uint8_to_string(value/0: u8) -> string {
  let inline0 = (value/0 as u64) in
  let inline1 = decimal_string(inline0) in
  inline1
}

fn string_byte_slice(value/0: string, start/0: isize, end/0: isize) -> string {
  let t0 = string_is_char_boundary(value/0, start/0) in
  join k0(jp0: bool) -> string {
    if jp0 {
      let t1 = @runtime(core.string_byte_slice)(value/0, start/0, end/0) in
      t1
    } else {
      let t2 = @runtime(core.string_byte_slice)(value/0, -1, -1) in
      t2
    }
  } in
  if t0 {
    let t3 = string_is_char_boundary(value/0, end/0) in
    jump k0(t3)
  } else {
    jump k0(false)
  }
}

fn std::iter::fold__A_isize__I_FnIterator[isize]__T_isize(
  iterator/0: FnIterator__isize,
  initial/0: isize,
  combine/0: (isize, isize) -> isize
) -> isize {
  let accumulator/0 = initial/0 in
  join exit0() -> isize {
    accumulator/0
  } in
  joinrec {
    join loop_expr0() -> isize {
      join inline_cont0(mtmp0: Option__isize) -> isize {
        match mtmp0 {
          Tag_0 => {
            jump exit0()
          },
          Tag_1 => {
            let x0 = Option__isize::Some._0(mtmp0) in
            let t0 = combine/0(accumulator/0, x0) in
            let _wild0 = accumulator/0 = t0 in
            jump loop_expr0()
          },
        }
      } in
      let inline0 = FnIterator__isize.next_fn(iterator/0) in
      let inline1 = inline0() in
      jump inline_cont0(inline1)
    } in
  } in
  jump loop_expr0()
}

fn range(start/0: isize, end/0: isize) -> FnIterator__isize {
  let inline0 = @intrinsic(ref.new)(start/0) in
  let inline1 = closure_env_goml_builtin_range_1 { current_0: inline0, end_1: end/0 } in
  let inline2 =
    pack_closure[inherent#closure_env_goml_builtin_range_1#closure_env_goml_builtin_range_1#apply](
      inline1
    ) in
  let inline3 = inherent#FnIterator#FnIterator[T]#from_fn__T_isize(inline2) in
  inline3
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#isize#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn __goml_builtin_int8_to_string(value/0: i8) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn __goml_builtin_uint8_to_string(value/0: u8) -> string {
  let t0 = (value/0 as u64) in
  let t1 = decimal_string(t0) in
  t1
}

fn string_is_char_boundary(value/0: string, index/0: isize) -> bool {
  let t0 = (index/0 < 0) in
  join k0(jp0: bool) -> bool {
    if jp0 {
      false
    } else {
      join inline_cont0(t1: isize) -> bool {
        let t2 = (index/0 == t1) in
        if t2 {
          true
        } else {
          join inline_cont1(t3: u8) -> bool {
            let t4 = (t3 & 192) in
            let t5 = (t4 != 128) in
            t5
          } in
          let inline0 = @runtime(core.string_byte_get)(value/0, index/0) in
          jump inline_cont1(inline0)
        }
      } in
      let inline1 = @runtime(core.string_len)(value/0) in
      jump inline_cont0(inline1)
    }
  } in
  if t0 {
    jump k0(true)
  } else {
    join inline_cont2(t6: isize) -> bool {
      let t7 = (index/0 > t6) in
      jump k0(t7)
    } in
    let inline2 = @runtime(core.string_len)(value/0) in
    jump inline_cont2(inline2)
  }
}

fn trait_impl#Iterator#FnIterator__isize#next(self/0: FnIterator__isize) -> Option__isize {
  let t0 = FnIterator__isize.next_fn(self/0) in
  let t1 = t0() in
  t1
}

fn __goml_builtin_range(start/0: isize, end/0: isize) -> FnIterator__isize {
  let current/0 = @intrinsic(ref.new)(start/0) in
  let t0 = closure_env_goml_builtin_range_1 { current_0: current/0, end_1: end/0 } in
  let t1 =
    pack_closure[inherent#closure_env_goml_builtin_range_1#closure_env_goml_builtin_range_1#apply](
      t0
    ) in
  let inline0 = FnIterator__isize { next_fn: t1 } in
  inline0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#string#string#byte_len(self/0: string) -> isize {
  let t0 = @runtime(core.string_len)(self/0) in
  t0
}

fn inherent#string#string#byte_get(self/0: string, index/0: isize) -> u8 {
  let t0 = @runtime(core.string_byte_get)(self/0, index/0) in
  t0
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
  next_fn/0: () -> Option__isize
) -> FnIterator__isize {
  let t0 = FnIterator__isize { next_fn: next_fn/0 } in
  t0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}

fn inherent#closure_env_main_0#closure_env_main_0#apply(
  env0: closure_env_main_0,
  sum/0: isize,
  item/0: isize
) -> isize {
  let t0 = (sum/0 + item/0) in
  t0
}

fn inherent#closure_env_goml_builtin_range_1#closure_env_goml_builtin_range_1#apply(
  env0: closure_env_goml_builtin_range_1
) -> Option__isize {
  let current/0 = closure_env_goml_builtin_range_1.current_0(env0) in
  let end/0 = closure_env_goml_builtin_range_1.end_1(env0) in
  let value/0 = @intrinsic(ref.get)(current/0) in
  let t0 = (value/0 < end/0) in
  if t0 {
    let t1 = (value/0 + 1) in
    let _wild0 = @intrinsic(ref.set)(current/0, t1) in
    let t2 = Option__isize::Some(value/0) in
    t2
  } else {
    Tag_0
  }
}