fn absurd(value/0: Never) -> i32 {
  {
    @intrinsic(compiler.missing)("")
  }
}

fn absurd_loop(value/0: Loop) -> i32 {
  {
    @intrinsic(compiler.missing)("")
  }
}

fn maybe_never(value/0: MaybeNever) -> i32 {
  match value/0 {
    MaybeNever::Empty => {
      0
    },
  }
}

fn empty_never_vec(value/0: Vec[Never]) -> i32 {
  {
    let match0 = value/0 in
    if (@intrinsic(vec.len)(match0) == 0) {
        0
    } else {
        @intrinsic(compiler.missing)("")
    }
  }
}

fn sequence_coverage(value/0: Vec[bool]) -> i32 {
  {
    let match0 = value/0 in
    if (@intrinsic(vec.len)(match0) == 0) {
        0
    } else {
        if (@intrinsic(vec.len)(match0) == 1) {
            1
        } else {
            if (@intrinsic(vec.len)(match0) >= 2) {
                if (@intrinsic(vec.get)(match0, 0) == true) {
                    2
                } else {
                    if (@intrinsic(vec.len)(match0) >= 2) {
                        if (@intrinsic(vec.get)(match0, 0) == false) {
                            3
                        } else {
                            @intrinsic(compiler.missing)("")
                        }
                    } else {
                        @intrinsic(compiler.missing)("")
                    }
                }
            } else {
                if (@intrinsic(vec.len)(match0) >= 2) {
                    if (@intrinsic(vec.get)(match0, 0) == false) {
                        3
                    } else {
                        @intrinsic(compiler.missing)("")
                    }
                } else {
                    @intrinsic(compiler.missing)("")
                }
            }
        }
    }
  }
}

fn unwrap(value/0: Single) -> i32 {
  let mtmp0 = value/0 in
  match mtmp0 {
    Single::Only(x0) => {
      {
        let x0 = Single::Only._0(mtmp0) in
        {
          let inner/0 = x0 in
          {
            inner/0
          }
        }
      }
    },
  }
}

fn decide(flag/0: bool) -> i32 {
  match flag/0 {
    true => {
      return 1
    },
    false => {
      return 2
    },
  }
}

fn signed_zero(value/0: f64) -> string {
  match value/0 {
    -0 => {
      "zero"
    },
    _ => "other",
  }
}

fn main() -> () {
  let mtmp0 = (Single::Only(12)) in
  {
    let x0 = mtmp0.0 in
    {
      let single/0 = x0 in
      {
        let _wild0 = println__T_i0(unwrap(single/0)) in
        let _wild1 = println__T_i0(decide(true)) in
        let _wild2 = println__T_string(signed_zero(0)) in
      }
    }
  }
}

fn println__T_i32(value/0: i32) -> () {
  @runtime(core.string_println)(trait_impl#ToString#i32#to_string(value/0))
}

fn println__T_string(value/0: string) -> () {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      {
        ("-" + decimal_string((0 - (value/0 as u64))))
      }
  } else {
      {
        decimal_string((value/0 as u64))
      }
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        {
          return "0"
        }
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        {
          let _wild2 =
            inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
          let _wild3 =
            {
              let compound_old0 = remaining/0 in
              let compound_value0 = 10 in
              remaining/0 = (compound_old0 / compound_value0)
            } in
        }
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        {
          let _wild5 =
            inherent#Vec#Vec[T]#push__T_u0(
              bytes/0,
              @intrinsic(vec.get)(
                reversed/0,
                ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
              )
            ) in
          let _wild6 =
            {
              let compound_old1 = offset/0 in
              let compound_value1 = 1 in
              offset/0 = (compound_old1 + compound_value1)
            } in
        }
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  {
    let x0 = mtmp0.1 in
    {
      let result/0 = x0 in
      {
        result/0
      }
    }
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}