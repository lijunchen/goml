fn make_point() -> Point {
  let t0 = Point { x: 0, y: 0 } in
  t0
}

fn flip(point/0: Point) -> Point {
  let x0 = Point.x(point/0) in
  let x1 = Point.y(point/0) in
  let t0 = Point { x: x1, y: x0 } in
  t0
}

fn wrap_int(x/0: i32) -> Wrapper__i32 {
  let t0 = Wrapper__i32 { value: x/0 } in
  t0
}

fn x_add_1(p/0: Point) -> Point {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  let t0 = (x0 + 1) in
  let t1 = Point { x: t0, y: x1 } in
  t1
}

fn get_x(p/0: Point) -> i32 {
  let x0 = Point.x(p/0) in
  x0
}

fn get_y(p/0: Point) -> i32 {
  let x0 = Point.y(p/0) in
  x0
}

fn point32_to_string(p/0: Point) -> string {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  join inline_cont0(t0: string) -> string {
    let t1 = ("Point { x: " + t0) in
    let t2 = (t1 + ", y: ") in
    join inline_cont1(t3: string) -> string {
      let t4 = (t2 + t3) in
      let t5 = (t4 + "}") in
      t5
    } in
    let inline0 = __goml_builtin_int32_to_string(x1) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(x0) in
  jump inline_cont0(inline1)
}

fn point32_to_string2(p/0: Point) -> string {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  join inline_cont0(t0: string) -> string {
    let t1 = ("Point { x: " + t0) in
    let t2 = (t1 + ", y: ") in
    join inline_cont1(t3: string) -> string {
      let t4 = (t2 + t3) in
      let t5 = (t4 + "}") in
      t5
    } in
    let inline0 = __goml_builtin_int32_to_string(x1) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(x0) in
  jump inline_cont0(inline1)
}

fn point32_to_string3(p/0: Point) -> string {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  join inline_cont0(t0: string) -> string {
    let t1 = ("Point { x: " + t0) in
    let t2 = (t1 + ", y: ") in
    join inline_cont1(t3: string) -> string {
      let t4 = (t2 + t3) in
      let t5 = (t4 + "}") in
      t5
    } in
    let inline0 = __goml_builtin_int32_to_string(x1) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(x0) in
  jump inline_cont0(inline1)
}

fn point32_to_string4(p/0: Point) -> string {
  let x0 = Point.x(p/0) in
  let x1 = Point.y(p/0) in
  join inline_cont0(t0: string) -> string {
    let t1 = ("Point { x: " + t0) in
    let t2 = (t1 + ", y: ") in
    join inline_cont1(t3: string) -> string {
      let t4 = (t2 + t3) in
      let t5 = (t4 + "}") in
      t5
    } in
    let inline0 = __goml_builtin_int32_to_string(x1) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(x0) in
  jump inline_cont0(inline1)
}

fn main() -> unit {
  let start/0 = make_point() in
  let t0 = point32_to_string(start/0) in
  let mtmp0 = println__T_string(t0) in
  let t1 = Point { x: 1, y: 2 } in
  let swapped/0 = flip(t1) in
  let t2 = point32_to_string0(swapped/0) in
  let mtmp1 = println__T_string(t2) in
  join inline_cont0(boxed/0: Wrapper__i32) -> unit {
    let a/0 = x_add_0(start/0) in
    join inline_cont1(t3: string) -> unit {
      join inline_cont2(mtmp2: unit) -> unit {
        join inline_cont3(t4: Point) -> unit {
          join inline_cont4(a/1: Point) -> unit {
            join inline_cont5(t5: string) -> unit {
              let inline0 = trait_impl#ToString#string#to_string(t5) in
              let inline1 = @runtime(core.string_println)(inline0) in
              ()
            } in
            let inline2 = Point.x(a/1) in
            let inline3 = Point.y(a/1) in
            let inline4 = inherent#i32#i32#to_string(inline2) in
            let inline5 = ("Point { x: " + inline4) in
            let inline6 = (inline5 + ", y: ") in
            let inline7 = inherent#i32#i32#to_string(inline3) in
            let inline8 = (inline6 + inline7) in
            let inline9 = (inline8 + "}") in
            jump inline_cont5(inline9)
          } in
          let inline10 = Point.x(t4) in
          let inline11 = Point.y(t4) in
          let inline12 = Point { x: inline11, y: inline10 } in
          jump inline_cont4(inline12)
        } in
        let inline13 = Point.x(start/0) in
        let inline14 = Point.y(start/0) in
        let inline15 = (inline13 + 1) in
        let inline16 = Point { x: inline15, y: inline14 } in
        jump inline_cont3(inline16)
      } in
      let inline17 = trait_impl#ToString#string#to_string(t3) in
      let inline18 = @runtime(core.string_println)(inline17) in
      jump inline_cont2(())
    } in
    let inline19 = Point.x(a/0) in
    let inline20 = Point.y(a/0) in
    let inline21 = inherent#i32#i32#to_string(inline19) in
    let inline22 = ("Point { x: " + inline21) in
    let inline23 = (inline22 + ", y: ") in
    let inline24 = inherent#i32#i32#to_string(inline20) in
    let inline25 = (inline23 + inline24) in
    let inline26 = (inline25 + "}") in
    jump inline_cont1(inline26)
  } in
  let inline27 = 3 in
  let inline28 = Wrapper__i32 { value: inline27 } in
  jump inline_cont0(inline28)
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}