fn main() -> unit {
  let v/0 = inherent#Vec#Vec[T]#new__T_i32() in
  join inline_cont948(_wild796: unit) -> unit {
    join inline_cont945(_wild797: unit) -> unit {
      join inline_cont942(_wild798: unit) -> unit {
        join inline_cont939(_wild799: unit) -> unit {
          join inline_cont935(s/1: Slice[i32]) -> unit {
            join inline_cont933(t808: isize) -> unit {
              join inline_cont930(_wild800: unit) -> unit {
                join inline_cont927(t809: i32) -> unit {
                  join inline_cont924(_wild801: unit) -> unit {
                    join inline_cont921(t810: i32) -> unit {
                      join inline_cont918(_wild802: unit) -> unit {
                        join inline_cont915(t811: i32) -> unit {
                          join inline_cont912(_wild803: unit) -> unit {
                            join inline_cont908(t/2: Slice[i32]) -> unit {
                              join inline_cont906(t812: isize) -> unit {
                                join inline_cont903(_wild804: unit) -> unit {
                                  join inline_cont900(t813: i32) -> unit {
                                    join inline_cont897(_wild805: unit) -> unit {
                                      join inline_cont894(t814: i32) -> unit {
                                        let inline892 = trait_impl#ToString#i32#to_string(t814) in
                                        let inline893 = @runtime(core.string_println)(inline892) in
                                        ()
                                      } in
                                      let inline895 = 1 in
                                      let inline896 = @intrinsic(slice.get)(t/2, inline895) in
                                      jump inline_cont894(inline896)
                                    } in
                                    let inline898 = trait_impl#ToString#i32#to_string(t813) in
                                    let inline899 = @runtime(core.string_println)(inline898) in
                                    jump inline_cont897(())
                                  } in
                                  let inline901 = 0 in
                                  let inline902 = @intrinsic(slice.get)(t/2, inline901) in
                                  jump inline_cont900(inline902)
                                } in
                                let inline904 = trait_impl#ToString#isize#to_string(t812) in
                                let inline905 = @runtime(core.string_println)(inline904) in
                                jump inline_cont903(())
                              } in
                              let inline907 = @intrinsic(slice.len)(t/2) in
                              jump inline_cont906(inline907)
                            } in
                            let inline909 = 1 in
                            let inline910 = 3 in
                            let inline911 = @intrinsic(slice.sub)(s/1, inline909, inline910) in
                            jump inline_cont908(inline911)
                          } in
                          let inline913 = trait_impl#ToString#i32#to_string(t811) in
                          let inline914 = @runtime(core.string_println)(inline913) in
                          jump inline_cont912(())
                        } in
                        let inline916 = 2 in
                        let inline917 = @intrinsic(slice.get)(s/1, inline916) in
                        jump inline_cont915(inline917)
                      } in
                      let inline919 = trait_impl#ToString#i32#to_string(t810) in
                      let inline920 = @runtime(core.string_println)(inline919) in
                      jump inline_cont918(())
                    } in
                    let inline922 = 1 in
                    let inline923 = @intrinsic(slice.get)(s/1, inline922) in
                    jump inline_cont921(inline923)
                  } in
                  let inline925 = trait_impl#ToString#i32#to_string(t809) in
                  let inline926 = @runtime(core.string_println)(inline925) in
                  jump inline_cont924(())
                } in
                let inline928 = 0 in
                let inline929 = @intrinsic(slice.get)(s/1, inline928) in
                jump inline_cont927(inline929)
              } in
              let inline931 = trait_impl#ToString#isize#to_string(t808) in
              let inline932 = @runtime(core.string_println)(inline931) in
              jump inline_cont930(())
            } in
            let inline934 = @intrinsic(slice.len)(s/1) in
            jump inline_cont933(inline934)
          } in
          let inline936 = 1 in
          let inline937 = 4 in
          let inline938 = @intrinsic(slice.new)(v/0, inline936, inline937) in
          jump inline_cont935(inline938)
        } in
        let inline940 = 40 in
        let inline941 = @intrinsic(vec.push)(v/0, inline940) in
        jump inline_cont939(())
      } in
      let inline943 = 30 in
      let inline944 = @intrinsic(vec.push)(v/0, inline943) in
      jump inline_cont942(())
    } in
    let inline946 = 20 in
    let inline947 = @intrinsic(vec.push)(v/0, inline946) in
    jump inline_cont945(())
  } in
  let inline949 = 10 in
  let inline950 = @intrinsic(vec.push)(v/0, inline949) in
  jump inline_cont948(())
}

fn inherent#Vec#Vec[T]#new__T_i32() -> Vec[i32] {
  let t817 = @intrinsic(vec.new)() in
  t817
}

fn inherent#Vec#Vec[T]#push__T_i32(self/511: Vec[i32], elem/512: i32) -> unit {
  let t819 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#slice__T_i32(self/564: Vec[i32], start/565: isize, end/566: isize) -> Slice[
  i32
] {
  let t822 = @intrinsic(slice.new)(self/564, start/565, end/566) in
  t822
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont951(t824: string) -> unit {
    let t825 = @runtime(core.string_println)(t824) in
    ()
  } in
  let inline952 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont951(inline952)
}

fn inherent#Slice#Slice[T]#len__T_i32(self/660: Slice[i32]) -> isize {
  let t828 = @intrinsic(slice.len)(self/660) in
  t828
}

fn println__T_i32(value/1: i32) -> unit {
  join inline_cont953(t830: string) -> unit {
    let t831 = @runtime(core.string_println)(t830) in
    ()
  } in
  let inline954 = __goml_builtin_int32_to_string(value/1) in
  jump inline_cont953(inline954)
}

fn inherent#Slice#Slice[T]#get__T_i32(self/658: Slice[i32], index/659: isize) -> i32 {
  let t834 = @intrinsic(slice.get)(self/658, index/659) in
  t834
}

fn inherent#Slice#Slice[T]#sub__T_i32(
  self/661: Slice[i32],
  start/662: isize,
  end/663: isize
) -> Slice[i32] {
  let t837 = @intrinsic(slice.sub)(self/661, start/662, end/663) in
  t837
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline956 = (self/404 as i64) in
  let inline957 = signed_decimal_string(inline956) in
  inline957
}

fn trait_impl#ToString#i32#to_string(self/407: i32) -> string {
  let inline959 = (self/407 as i64) in
  let inline960 = signed_decimal_string(inline959) in
  inline960
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t846 = (value/222 as i64) in
  let inline962 = (t846 < 0) in
  if inline962 {
    let inline963 = (t846 as u64) in
    let inline964 = (0 - inline963) in
    let inline965 = decimal_string(inline964) in
    let inline966 = ("-" + inline965) in
    inline966
  } else {
    let inline967 = (t846 as u64) in
    let inline968 = decimal_string(inline967) in
    inline968
  }
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t850 = (value/225 as i64) in
  let inline970 = (t850 < 0) in
  if inline970 {
    let inline971 = (t850 as u64) in
    let inline972 = (0 - inline971) in
    let inline973 = decimal_string(inline972) in
    let inline974 = ("-" + inline973) in
    inline974
  } else {
    let inline975 = (t850 as u64) in
    let inline976 = decimal_string(inline975) in
    inline976
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t856 = (value/214 < 0) in
  if t856 {
    let t857 = (value/214 as u64) in
    let t858 = (0 - t857) in
    let t859 = decimal_string(t858) in
    let t860 = ("-" + t859) in
    t860
  } else {
    let t861 = (value/214 as u64) in
    let t862 = decimal_string(t861) in
    t862
  }
}

fn decimal_string(value/208: u64) -> string {
  let t885 = (value/208 == 0) in
  join k865() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit866() -> string {
      join inline_cont985(t867: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t867) in
        let offset/212 = 0 in
        join exit868() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop869() -> string {
            join inline_cont983(t870: isize) -> string {
              let t871 = (offset/212 < t870) in
              if t871 {
                join inline_cont981(t872: isize) -> string {
                  let t873 = (t872 - offset/212) in
                  let t874 = (t873 - 1) in
                  let t875 = @intrinsic(vec.get)(reversed/209, t874) in
                  join inline_cont979(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t876 = (compound_old358 + compound_value359) in
                    let t877 = offset/212 = t876 in
                    jump loop869()
                  } in
                  let inline980 = @intrinsic(vec.push)(bytes/211, t875) in
                  jump inline_cont979(())
                } in
                let inline982 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont981(inline982)
              } else {
                jump exit868()
              }
            } in
            let inline984 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont983(inline984)
          } in
        } in
        jump loop869()
      } in
      let inline986 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont985(inline986)
    } in
    joinrec {
      join loop878() -> string {
        let t879 = (remaining/210 > 0) in
        if t879 {
          let t880 = (remaining/210 % 10) in
          let t881 = (t880 as u8) in
          let t882 = (t881 + 48) in
          join inline_cont977(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t883 = (compound_old353 / compound_value354) in
            let t884 = remaining/210 = t883 in
            jump loop878()
          } in
          let inline978 = @intrinsic(vec.push)(reversed/209, t882) in
          jump inline_cont977(())
        } else {
          jump exit866()
        }
      } in
    } in
    jump loop878()
  } in
  if t885 {
    "0"
  } else {
    jump k865()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t887 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t890 = @intrinsic(vec.len)(self/526) in
  t890
}