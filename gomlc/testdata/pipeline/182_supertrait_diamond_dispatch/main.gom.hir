package main
file main.gom
  trait Diamond: Left + Right {
    fn diamond(Self) -> string;
  }

  trait Left: Base {
    fn left(Self) -> string;
  }

  trait Right: Base {
    fn right(Self) -> string;
  }

  trait Base {
    fn base(Self) -> string;
  }

  struct Value {}

  impl Base for Value {
    fn base(local/1/0: Value) -> string {
        "B"
    }
  }

  impl Left for Value {
    fn left(local/1/1: Value) -> string {
        "L"
    }
  }

  impl Right for Value {
    fn right(local/1/2: Value) -> string {
        "R"
    }
  }

  impl Diamond for Value {
    fn diamond(local/1/3: Value) -> string {
        "D"
    }
  }

  fn describe[T: Diamond](local/1/4: T) -> string {
      value/4.base() + value/4.left() + value/4.right() + value/4.diamond()
  }

  fn main() -> unit {
      println(def/1/9(Value {}));
  }