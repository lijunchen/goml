fn trait_impl#Base#Value#base(self/0: Value) -> string {
  "B"
}

fn trait_impl#Left#Value#left(self/1: Value) -> string {
  "L"
}

fn trait_impl#Right#Value#right(self/2: Value) -> string {
  "R"
}

fn trait_impl#Diamond#Value#diamond(self/3: Value) -> string {
  "D"
}

fn main() -> unit {
  let t182 = Value {} in
  join inline_cont201(t183: string) -> unit {
    let inline199 = trait_impl#ToString#string#to_string(t183) in
    let inline200 = @runtime(core.string_println)(inline199) in
    ()
  } in
  let inline202 = trait_impl#Base#Value#base(t182) in
  let inline203 = trait_impl#Left#Value#left(t182) in
  let inline204 = (inline202 + inline203) in
  let inline205 = trait_impl#Right#Value#right(t182) in
  let inline206 = (inline204 + inline205) in
  let inline207 = trait_impl#Diamond#Value#diamond(t182) in
  let inline208 = (inline206 + inline207) in
  jump inline_cont201(inline208)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont209(t185: string) -> unit {
    let t186 = @runtime(core.string_println)(t185) in
    ()
  } in
  jump inline_cont209(value/31)
}

fn describe__T_Value(value/4: Value) -> string {
  join inline_cont213(t189: string) -> string {
    join inline_cont212(t190: string) -> string {
      let t191 = (t189 + t190) in
      join inline_cont211(t192: string) -> string {
        let t193 = (t191 + t192) in
        join inline_cont210(t194: string) -> string {
          let t195 = (t193 + t194) in
          t195
        } in
        jump inline_cont210("D")
      } in
      jump inline_cont211("R")
    } in
    jump inline_cont212("L")
  } in
  jump inline_cont213("B")
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}