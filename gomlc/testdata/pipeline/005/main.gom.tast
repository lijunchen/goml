fn main() -> unit {
    let a/0: (Color, Color) = (Color::Blue, Color::Red);
    match (a/0 : (Color, Color)) {
        (Color::Red, Color::Green) => (print : (isize) -> unit)(0),
        (Color::Red, Color::Red) => (print : (isize) -> unit)(1),
        (Color::Blue, Color::Blue) => (print : (isize) -> unit)(2),
        _ : (Color, Color) => (print : (isize) -> unit)(3),
    }
}