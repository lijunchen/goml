package main
file main.gom
  enum Color {
      Red,
      Green,
      Blue
  }

  fn main() {
      let local/1/0 = (ctor(def/1/0::v2), ctor(def/1/0::v0));
      match a/0 {
          (Red, Green) => print(0),
          (Red, Red) => print(1),
          (Blue, Blue) => print(2),
          _ => print(3),
      }
  }