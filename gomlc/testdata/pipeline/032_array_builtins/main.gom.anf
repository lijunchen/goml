fn update_array(arr/0: [i32; 3]) -> [i32; 3] {
  let arr/1 = arr/0 in
  let place_root796 = arr/1 in
  let index797 = 1 in
  let place798 = @intrinsic(array.get)(place_root796, index797) in
  let value799 = 42 in
  let t804 = @intrinsic(array.set)(place_root796, index797, value799) in
  let t805 = arr/1 = t804 in
  arr/1
}

fn read_array(arr/2: [i32; 3]) -> i32 {
  let t808 = @intrinsic(array.get)(arr/2, 1) in
  t808
}

fn main() -> unit {
  let arr/3 = [1, 2, 3] in
  join inline_cont869(updated/4: [i32; 3]) -> unit {
    join inline_cont867(value/5: i32) -> unit {
      join inline_cont865(t810: string) -> unit {
        let inline863 = trait_impl#ToString#string#to_string(t810) in
        let inline864 = @runtime(core.string_println)(inline863) in
        ()
      } in
      let inline866 = __goml_builtin_int32_to_string(value/5) in
      jump inline_cont865(inline866)
    } in
    let inline868 = @intrinsic(array.get)(updated/4, 1) in
    jump inline_cont867(inline868)
  } in
  let inline870 = arr/3 in
  let inline871 = inline870 in
  let inline872 = 1 in
  let inline873 = @intrinsic(array.get)(inline871, inline872) in
  let inline874 = 42 in
  let inline875 = @intrinsic(array.set)(inline871, inline872, inline874) in
  let inline876 = inline870 = inline875 in
  jump inline_cont869(inline870)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont878(t812: string) -> unit {
    let t813 = @runtime(core.string_println)(t812) in
    ()
  } in
  jump inline_cont878(value/1)
}

fn inherent#i32#i32#to_string(self/286: i32) -> string {
  let inline880 = (self/286 as i64) in
  let inline881 = signed_decimal_string(inline880) in
  inline881
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t821 = (value/225 as i64) in
  let inline883 = (t821 < 0) in
  if inline883 {
    let inline884 = (t821 as u64) in
    let inline885 = (0 - inline884) in
    let inline886 = decimal_string(inline885) in
    let inline887 = ("-" + inline886) in
    inline887
  } else {
    let inline888 = (t821 as u64) in
    let inline889 = decimal_string(inline888) in
    inline889
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t827 = (value/214 < 0) in
  if t827 {
    let t828 = (value/214 as u64) in
    let t829 = (0 - t828) in
    let t830 = decimal_string(t829) in
    let t831 = ("-" + t830) in
    t831
  } else {
    let t832 = (value/214 as u64) in
    let t833 = decimal_string(t832) in
    t833
  }
}

fn decimal_string(value/208: u64) -> string {
  let t856 = (value/208 == 0) in
  join k836() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit837() -> string {
      join inline_cont898(t838: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t838) in
        let offset/212 = 0 in
        join exit839() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop840() -> string {
            join inline_cont896(t841: isize) -> string {
              let t842 = (offset/212 < t841) in
              if t842 {
                join inline_cont894(t843: isize) -> string {
                  let t844 = (t843 - offset/212) in
                  let t845 = (t844 - 1) in
                  let t846 = @intrinsic(vec.get)(reversed/209, t845) in
                  join inline_cont892(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t847 = (compound_old358 + compound_value359) in
                    let t848 = offset/212 = t847 in
                    jump loop840()
                  } in
                  let inline893 = @intrinsic(vec.push)(bytes/211, t846) in
                  jump inline_cont892(())
                } in
                let inline895 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont894(inline895)
              } else {
                jump exit839()
              }
            } in
            let inline897 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont896(inline897)
          } in
        } in
        jump loop840()
      } in
      let inline899 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont898(inline899)
    } in
    joinrec {
      join loop849() -> string {
        let t850 = (remaining/210 > 0) in
        if t850 {
          let t851 = (remaining/210 % 10) in
          let t852 = (t851 as u8) in
          let t853 = (t852 + 48) in
          join inline_cont890(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t854 = (compound_old353 / compound_value354) in
            let t855 = remaining/210 = t854 in
            jump loop849()
          } in
          let inline891 = @intrinsic(vec.push)(reversed/209, t853) in
          jump inline_cont890(())
        } else {
          jump exit837()
        }
      } in
    } in
    jump loop849()
  } in
  if t856 {
    "0"
  } else {
    jump k836()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t858 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t861 = @intrinsic(vec.len)(self/526) in
  t861
}