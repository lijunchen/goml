fn update_array(arr/0: [int32; 3]) -> [int32; 3] {
  let arr/1 = arr/0 in
  let place_root136 = arr/1 in
  let index137 = 1 in
  let place138 = @intrinsic(array.get)(place_root136, index137) in
  let value139 = 42 in
  let t144 = @intrinsic(array.set)(place_root136, index137, value139) in
  let t145 = arr/1 = t144 in
  let _wild140 = t145 in
  arr/1
}

fn read_array(arr/2: [int32; 3]) -> int32 {
  let t148 = @intrinsic(array.get)(arr/2, 1) in
  t148
}

fn main() -> unit {
  let arr/3 = [1, 2, 3] in
  join inline_cont166(updated/4: [int32; 3]) -> unit {
    join inline_cont164(value/5: int32) -> unit {
      join inline_cont162(t150: string) -> unit {
        let inline160 = trait_impl#ToString#string#to_string(t150) in
        let inline161 = @runtime(core.string_println)(inline160) in
        ()
      } in
      let inline163 = @runtime(core.int32_to_string)(value/5) in
      jump inline_cont162(inline163)
    } in
    let inline165 = @intrinsic(array.get)(updated/4, 1) in
    jump inline_cont164(inline165)
  } in
  let inline167 = arr/3 in
  let inline168 = inline167 in
  let inline169 = 1 in
  let inline170 = @intrinsic(array.get)(inline168, inline169) in
  let inline171 = 42 in
  let inline172 = @intrinsic(array.set)(inline168, inline169, inline171) in
  let inline173 = inline167 = inline172 in
  let inline174 = inline173 in
  jump inline_cont166(inline167)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont175(t152: string) -> unit {
    let t153 = @runtime(core.string_println)(t152) in
    ()
  } in
  jump inline_cont175(value/31)
}

fn inherent#int32#int32#to_string(self/35: int32) -> string {
  let t156 = @runtime(core.int32_to_string)(self/35) in
  t156
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}