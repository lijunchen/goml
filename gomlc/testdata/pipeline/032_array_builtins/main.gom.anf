fn update_array(arr/0: [i32; 3]) -> [i32; 3] {
  let arr/1 = arr/0 in
  let place_root411 = arr/1 in
  let index412 = 1 in
  let place413 = @intrinsic(array.get)(place_root411, index412) in
  let value414 = 42 in
  let t419 = @intrinsic(array.set)(place_root411, index412, value414) in
  let t420 = arr/1 = t419 in
  arr/1
}

fn read_array(arr/2: [i32; 3]) -> i32 {
  let t423 = @intrinsic(array.get)(arr/2, 1) in
  t423
}

fn main() -> unit {
  let arr/3 = [1, 2, 3] in
  join inline_cont441(updated/4: [i32; 3]) -> unit {
    join inline_cont439(value/5: i32) -> unit {
      join inline_cont437(t425: string) -> unit {
        let inline435 = trait_impl#ToString#string#to_string(t425) in
        let inline436 = @runtime(core.string_println)(inline435) in
        ()
      } in
      let inline438 = @runtime(core.int32_to_string)(value/5) in
      jump inline_cont437(inline438)
    } in
    let inline440 = @intrinsic(array.get)(updated/4, 1) in
    jump inline_cont439(inline440)
  } in
  let inline442 = arr/3 in
  let inline443 = inline442 in
  let inline444 = 1 in
  let inline445 = @intrinsic(array.get)(inline443, inline444) in
  let inline446 = 42 in
  let inline447 = @intrinsic(array.set)(inline443, inline444, inline446) in
  let inline448 = inline442 = inline447 in
  jump inline_cont441(inline442)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont450(t427: string) -> unit {
    let t428 = @runtime(core.string_println)(t427) in
    ()
  } in
  jump inline_cont450(value/1)
}

fn inherent#i32#i32#to_string(self/33: i32) -> string {
  let t431 = @runtime(core.int32_to_string)(self/33) in
  t431
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}