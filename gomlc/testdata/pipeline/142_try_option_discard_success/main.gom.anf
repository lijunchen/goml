fn cut_pair(ok/0: bool) -> Option__(string,string) {
  if ok/0 {
    let t421 = ("left", "right") in
    let t422 = Option__(string,string)::Some(t421) in
    t422
  } else {
    Tag_0
  }
}

fn check(ok/1: bool) -> Option__string {
  join inline_cont443(mtmp411: Option__(string,string)) -> Option__string {
    join k425(jp426: (string, string)) -> Option__string {
      let t427 = Option__string::Some("ok") in
      t427
    } in
    match mtmp411 {
      Tag_0 => {
        Tag_0
      },
      Tag_1 => {
        let x412 = Option__(string,string)::Some._0(mtmp411) in
        jump k425(x412)
      },
    }
  } in
  if ok/1 {
    let inline444 = ("left", "right") in
    let inline445 = Option__(string,string)::Some(inline444) in
    jump inline_cont443(inline445)
  } else {
    jump inline_cont443(Tag_0)
  }
}

fn show(opt/2: Option__string) -> string {
  match opt/2 {
    Tag_0 => {
      "none"
    },
    Tag_1 => {
      let x414 = Option__string::Some._0(opt/2) in
      let t432 = ("some " + x414) in
      t432
    },
  }
}

fn main() -> unit {
  join inline_cont469(t434: Option__string) -> unit {
    join inline_cont465(t435: string) -> unit {
      join inline_cont462(_wild415: unit) -> unit {
        join inline_cont453(t436: Option__string) -> unit {
          join inline_cont449(t437: string) -> unit {
            let inline447 = trait_impl#ToString#string#to_string(t437) in
            let inline448 = @runtime(core.string_println)(inline447) in
            ()
          } in
          match t436 {
            Tag_0 => {
              jump inline_cont449("none")
            },
            Tag_1 => {
              let inline450 = Option__string::Some._0(t436) in
              let inline452 = ("some " + inline450) in
              jump inline_cont449(inline452)
            },
          }
        } in
        let inline454 = false in
        let inline455 = cut_pair(inline454) in
        join inline_join456(inline457: (string, string)) -> unit {
          let inline459 = Option__string::Some("ok") in
          jump inline_cont453(inline459)
        } in
        match inline455 {
          Tag_0 => {
            jump inline_cont453(Tag_0)
          },
          Tag_1 => {
            let inline460 = Option__(string,string)::Some._0(inline455) in
            jump inline_join456(inline460)
          },
        }
      } in
      let inline463 = trait_impl#ToString#string#to_string(t435) in
      let inline464 = @runtime(core.string_println)(inline463) in
      jump inline_cont462(())
    } in
    match t434 {
      Tag_0 => {
        jump inline_cont465("none")
      },
      Tag_1 => {
        let inline466 = Option__string::Some._0(t434) in
        let inline468 = ("some " + inline466) in
        jump inline_cont465(inline468)
      },
    }
  } in
  let inline470 = true in
  let inline471 = cut_pair(inline470) in
  join inline_join472(inline473: (string, string)) -> unit {
    let inline475 = Option__string::Some("ok") in
    jump inline_cont469(inline475)
  } in
  match inline471 {
    Tag_0 => {
      jump inline_cont469(Tag_0)
    },
    Tag_1 => {
      let inline476 = Option__(string,string)::Some._0(inline471) in
      jump inline_join472(inline476)
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont478(t439: string) -> unit {
    let t440 = @runtime(core.string_println)(t439) in
    ()
  } in
  jump inline_cont478(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}