fn a_value() -> i8 {
  90
}

fn b_value() -> i8 {
  -20
}

fn c_value() -> i8 {
  3
}

fn show_int8(label/0: string, value/1: i8) -> unit {
  join inline_cont877(t814: string) -> unit {
    let t815 = (label/0 + t814) in
    let inline875 = trait_impl#ToString#string#to_string(t815) in
    let inline876 = @runtime(core.string_println)(inline875) in
    ()
  } in
  let inline878 = __goml_builtin_int8_to_string(value/1) in
  jump inline_cont877(inline878)
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join inline_cont882(t817: string) -> unit {
    let t818 = (label/2 + t817) in
    let inline880 = trait_impl#ToString#string#to_string(t818) in
    let inline881 = @runtime(core.string_println)(inline880) in
    ()
  } in
  let inline883 = @runtime(core.bool_to_string)(value/3) in
  jump inline_cont882(inline883)
}

fn main() -> unit {
  join inline_cont931(a/4: i8) -> unit {
    join inline_cont930(b/5: i8) -> unit {
      join inline_cont929(c/6: i8) -> unit {
        let sum/7 = (a/4 + b/5) in
        let diff/8 = (a/4 - c/6) in
        let prod/9 = (b/5 * c/6) in
        let quot/10 = (a/4 / c/6) in
        let neg/11 = (-b/5) in
        let less/12 = (b/5 < a/4) in
        join inline_cont924(mtmp798: unit) -> unit {
          join inline_cont919(mtmp799: unit) -> unit {
            join inline_cont914(mtmp800: unit) -> unit {
              join inline_cont909(mtmp801: unit) -> unit {
                join inline_cont904(mtmp802: unit) -> unit {
                  join inline_cont899(mtmp803: unit) -> unit {
                    join inline_cont894(mtmp804: unit) -> unit {
                      join inline_cont889(mtmp805: unit) -> unit {
                        let inline885 = "b<a=" in
                        let inline886 = trait_impl#ToString#bool#to_string(less/12) in
                        let inline887 = (inline885 + inline886) in
                        let inline888 = println__T_string(inline887) in
                        ()
                      } in
                      let inline890 = "neg=" in
                      let inline891 = trait_impl#ToString#i8#to_string(neg/11) in
                      let inline892 = (inline890 + inline891) in
                      let inline893 = println__T_string(inline892) in
                      jump inline_cont889(())
                    } in
                    let inline895 = "quot=" in
                    let inline896 = trait_impl#ToString#i8#to_string(quot/10) in
                    let inline897 = (inline895 + inline896) in
                    let inline898 = println__T_string(inline897) in
                    jump inline_cont894(())
                  } in
                  let inline900 = "prod=" in
                  let inline901 = trait_impl#ToString#i8#to_string(prod/9) in
                  let inline902 = (inline900 + inline901) in
                  let inline903 = println__T_string(inline902) in
                  jump inline_cont899(())
                } in
                let inline905 = "diff=" in
                let inline906 = trait_impl#ToString#i8#to_string(diff/8) in
                let inline907 = (inline905 + inline906) in
                let inline908 = println__T_string(inline907) in
                jump inline_cont904(())
              } in
              let inline910 = "sum=" in
              let inline911 = trait_impl#ToString#i8#to_string(sum/7) in
              let inline912 = (inline910 + inline911) in
              let inline913 = println__T_string(inline912) in
              jump inline_cont909(())
            } in
            let inline915 = "c=" in
            let inline916 = trait_impl#ToString#i8#to_string(c/6) in
            let inline917 = (inline915 + inline916) in
            let inline918 = println__T_string(inline917) in
            jump inline_cont914(())
          } in
          let inline920 = "b=" in
          let inline921 = trait_impl#ToString#i8#to_string(b/5) in
          let inline922 = (inline920 + inline921) in
          let inline923 = println__T_string(inline922) in
          jump inline_cont919(())
        } in
        let inline925 = "a=" in
        let inline926 = trait_impl#ToString#i8#to_string(a/4) in
        let inline927 = (inline925 + inline926) in
        let inline928 = println__T_string(inline927) in
        jump inline_cont924(())
      } in
      jump inline_cont929(3)
    } in
    jump inline_cont930(-20)
  } in
  jump inline_cont931(90)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont932(t821: string) -> unit {
    let t822 = @runtime(core.string_println)(t821) in
    ()
  } in
  jump inline_cont932(value/1)
}

fn trait_impl#ToString#i8#to_string(self/405: i8) -> string {
  let inline934 = (self/405 as i64) in
  let inline935 = signed_decimal_string(inline934) in
  inline935
}

fn trait_impl#ToString#bool#to_string(self/401: bool) -> string {
  let t828 = @runtime(core.bool_to_string)(self/401) in
  t828
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int8_to_string(value/223: i8) -> string {
  let t833 = (value/223 as i64) in
  let inline937 = (t833 < 0) in
  if inline937 {
    let inline938 = (t833 as u64) in
    let inline939 = (0 - inline938) in
    let inline940 = decimal_string(inline939) in
    let inline941 = ("-" + inline940) in
    inline941
  } else {
    let inline942 = (t833 as u64) in
    let inline943 = decimal_string(inline942) in
    inline943
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t839 = (value/214 < 0) in
  if t839 {
    let t840 = (value/214 as u64) in
    let t841 = (0 - t840) in
    let t842 = decimal_string(t841) in
    let t843 = ("-" + t842) in
    t843
  } else {
    let t844 = (value/214 as u64) in
    let t845 = decimal_string(t844) in
    t845
  }
}

fn decimal_string(value/208: u64) -> string {
  let t868 = (value/208 == 0) in
  join k848() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit849() -> string {
      join inline_cont952(t850: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t850) in
        let offset/212 = 0 in
        join exit851() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop852() -> string {
            join inline_cont950(t853: isize) -> string {
              let t854 = (offset/212 < t853) in
              if t854 {
                join inline_cont948(t855: isize) -> string {
                  let t856 = (t855 - offset/212) in
                  let t857 = (t856 - 1) in
                  let t858 = @intrinsic(vec.get)(reversed/209, t857) in
                  join inline_cont946(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t859 = (compound_old358 + compound_value359) in
                    let t860 = offset/212 = t859 in
                    jump loop852()
                  } in
                  let inline947 = @intrinsic(vec.push)(bytes/211, t858) in
                  jump inline_cont946(())
                } in
                let inline949 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont948(inline949)
              } else {
                jump exit851()
              }
            } in
            let inline951 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont950(inline951)
          } in
        } in
        jump loop852()
      } in
      let inline953 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont952(inline953)
    } in
    joinrec {
      join loop861() -> string {
        let t862 = (remaining/210 > 0) in
        if t862 {
          let t863 = (remaining/210 % 10) in
          let t864 = (t863 as u8) in
          let t865 = (t864 + 48) in
          join inline_cont944(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t866 = (compound_old353 / compound_value354) in
            let t867 = remaining/210 = t866 in
            jump loop861()
          } in
          let inline945 = @intrinsic(vec.push)(reversed/209, t865) in
          jump inline_cont944(())
        } else {
          jump exit849()
        }
      } in
    } in
    jump loop861()
  } in
  if t868 {
    "0"
  } else {
    jump k848()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t870 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t873 = @intrinsic(vec.len)(self/526) in
  t873
}