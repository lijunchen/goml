fn a_value() -> i8 {
  90
}

fn b_value() -> i8 {
  -20
}

fn c_value() -> i8 {
  3
}

fn show_int8(label/0: string, value/0: i8) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = (label/0 + t0) in
    let inline0 = trait_impl#ToString#string#to_string(t1) in
    let inline1 = @runtime(core.string_println)(inline0) in
    ()
  } in
  let inline2 = __goml_builtin_int8_to_string(value/0) in
  jump inline_cont0(inline2)
}

fn show_bool(label/0: string, value/0: bool) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = (label/0 + t0) in
    let inline0 = trait_impl#ToString#string#to_string(t1) in
    let inline1 = @runtime(core.string_println)(inline0) in
    ()
  } in
  let inline2 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline2)
}

fn main() -> () {
  join inline_cont0(a/0: i8) -> () {
    join inline_cont1(b/0: i8) -> () {
      join inline_cont2(c/0: i8) -> () {
        let sum/0 = (a/0 + b/0) in
        let diff/0 = (a/0 - c/0) in
        let prod/0 = (b/0 * c/0) in
        let quot/0 = (a/0 / c/0) in
        let neg/0 = (-b/0) in
        let less/0 = (b/0 < a/0) in
        join inline_cont3(mtmp0: ()) -> () {
          join inline_cont4(mtmp1: ()) -> () {
            join inline_cont5(mtmp2: ()) -> () {
              join inline_cont6(mtmp3: ()) -> () {
                join inline_cont7(mtmp4: ()) -> () {
                  join inline_cont8(mtmp5: ()) -> () {
                    join inline_cont9(mtmp6: ()) -> () {
                      join inline_cont10(mtmp7: ()) -> () {
                        let inline0 = "b<a=" in
                        let inline1 = trait_impl#ToString#bool#to_string(less/0) in
                        let inline2 = (inline0 + inline1) in
                        let inline3 = println__T_string(inline2) in
                        ()
                      } in
                      let inline4 = "neg=" in
                      let inline5 = trait_impl#ToString#i8#to_string(neg/0) in
                      let inline6 = (inline4 + inline5) in
                      let inline7 = println__T_string(inline6) in
                      jump inline_cont10(())
                    } in
                    let inline8 = "quot=" in
                    let inline9 = trait_impl#ToString#i8#to_string(quot/0) in
                    let inline10 = (inline8 + inline9) in
                    let inline11 = println__T_string(inline10) in
                    jump inline_cont9(())
                  } in
                  let inline12 = "prod=" in
                  let inline13 = trait_impl#ToString#i8#to_string(prod/0) in
                  let inline14 = (inline12 + inline13) in
                  let inline15 = println__T_string(inline14) in
                  jump inline_cont8(())
                } in
                let inline16 = "diff=" in
                let inline17 = trait_impl#ToString#i8#to_string(diff/0) in
                let inline18 = (inline16 + inline17) in
                let inline19 = println__T_string(inline18) in
                jump inline_cont7(())
              } in
              let inline20 = "sum=" in
              let inline21 = trait_impl#ToString#i8#to_string(sum/0) in
              let inline22 = (inline20 + inline21) in
              let inline23 = println__T_string(inline22) in
              jump inline_cont6(())
            } in
            let inline24 = "c=" in
            let inline25 = trait_impl#ToString#i8#to_string(c/0) in
            let inline26 = (inline24 + inline25) in
            let inline27 = println__T_string(inline26) in
            jump inline_cont5(())
          } in
          let inline28 = "b=" in
          let inline29 = trait_impl#ToString#i8#to_string(b/0) in
          let inline30 = (inline28 + inline29) in
          let inline31 = println__T_string(inline30) in
          jump inline_cont4(())
        } in
        let inline32 = "a=" in
        let inline33 = trait_impl#ToString#i8#to_string(a/0) in
        let inline34 = (inline32 + inline33) in
        let inline35 = println__T_string(inline34) in
        jump inline_cont3(())
      } in
      jump inline_cont2(3)
    } in
    jump inline_cont1(-20)
  } in
  jump inline_cont0(90)
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#i8#to_string(self/0: i8) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int8_to_string(value/0: i8) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}