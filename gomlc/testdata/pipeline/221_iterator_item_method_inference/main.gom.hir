package main
file main.gom
  struct Values {
    }

    impl Iterator for Values {
      type Item = int32;
      fn next(local/1/0: Values) -> Option[int32] {
          Option::None
      }
    }

    fn main() -> unit {
        for local/1/1 in Values {} {
            string_println(value/1.to_string());
        };
    }