impl Iterator for Values {
  fn next(self/0: Values) -> Option[i32] {
      Option::None
  }
}

fn main() -> unit {
    for value/1: i32 in
      Values {} {
        (println : (string) -> unit)((i32::to_string : (i32) -> string)((value/1 : i32)));
    }
}