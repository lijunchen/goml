package main;
struct Values {}

impl Iterator for Values {
  type Item = i32;
  fn next(self: Values) -> Option[i32] {
      Option::None
  }
}

fn main() -> () {
    for value in
      Values {} {
        println(value.to_string());
    }
}

