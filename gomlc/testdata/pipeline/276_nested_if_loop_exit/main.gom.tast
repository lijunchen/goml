fn then_break_outer_continue(flag/0: bool) -> isize {
    let rounds/1: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(0);
    loop {
        if (Ref[isize]::get : (Ref[isize]) -> isize)((rounds/1 : Ref[isize])) == 0 {
            {
                (Ref[isize]::set : (Ref[isize], isize) -> ())((rounds/1 : Ref[isize]), 1);
                loop {
                    if (flag/0 : bool) {
                        {
                            if (Ref[isize]::get : (Ref[isize]) -> isize)((rounds/1 : Ref[isize])) ==
                              1 {
                                {
                                    break
                                }
                            } else {
                                {
                                    continue
                                }
                            }
                        }
                    } else {
                        {
                            continue
                        }
                    }
                };
                continue
            }
        } else {
            {
                break 21
            }
        }
    }
}

fn then_break_outer_break(flag/2: bool) -> isize {
    let state/3: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(1);
    loop {
        loop {
            if (flag/2 : bool) {
                {
                    if (Ref[isize]::get : (Ref[isize]) -> isize)((state/3 : Ref[isize])) == 1 {
                        {
                            break
                        }
                    } else {
                        {
                            continue
                        }
                    }
                }
            } else {
                {
                    continue
                }
            }
        };
        break 22
    }
}

fn else_break_outer_continue(flag/4: bool) -> isize {
    let rounds/5: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(0);
    loop {
        if (Ref[isize]::get : (Ref[isize]) -> isize)((rounds/5 : Ref[isize])) == 0 {
            {
                (Ref[isize]::set : (Ref[isize], isize) -> ())((rounds/5 : Ref[isize]), 1);
                loop {
                    if (flag/4 : bool) {
                        {
                            continue
                        }
                    } else {
                        {
                            if (Ref[isize]::get : (Ref[isize]) -> isize)((rounds/5 : Ref[isize])) ==
                              1 {
                                {
                                    break
                                }
                            } else {
                                {
                                    continue
                                }
                            }
                        }
                    }
                };
                continue
            }
        } else {
            {
                break 23
            }
        }
    }
}

fn else_break_outer_break(flag/6: bool) -> isize {
    let state/7: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(1);
    loop {
        loop {
            if (flag/6 : bool) {
                {
                    continue
                }
            } else {
                {
                    if (Ref[isize]::get : (Ref[isize]) -> isize)((state/7 : Ref[isize])) == 1 {
                        {
                            break
                        }
                    } else {
                        {
                            continue
                        }
                    }
                }
            }
        };
        break 24
    }
}

fn main() -> () {
    (println : (isize) -> ())((then_break_outer_continue : (bool) -> isize)(true));
    (println : (isize) -> ())((then_break_outer_break : (bool) -> isize)(true));
    (println : (isize) -> ())((else_break_outer_continue : (bool) -> isize)(false));
    (println : (isize) -> ())((else_break_outer_break : (bool) -> isize)(false))
}