fn main() -> unit {
  join inline_cont226(direct/3: string) -> unit {
    join inline_cont223(_wild182: unit) -> unit {
      let t187 = Prefix { value: "generic:" } in
      join inline_cont221(generic/4: string) -> unit {
        join inline_cont218(_wild183: unit) -> unit {
          join inline_cont213(ufcs/5: string) -> unit {
            let inline211 = trait_impl#ToString#string#to_string(ufcs/5) in
            let inline212 = @runtime(core.string_println)(inline211) in
            ()
          } in
          let inline214 = 12 in
          let inline215 = "ufcs:" in
          let inline216 = trait_impl#ToString#int#to_string(inline214) in
          let inline217 = (inline215 + inline216) in
          jump inline_cont213(inline217)
        } in
        let inline219 = trait_impl#ToString#string#to_string(generic/4) in
        let inline220 = @runtime(core.string_println)(inline219) in
        jump inline_cont218(())
      } in
      let inline222 = trait_impl#RenderValue#Prefix#render__mono2(t187, 11) in
      jump inline_cont221(inline222)
    } in
    let inline224 = trait_impl#ToString#string#to_string(direct/3) in
    let inline225 = @runtime(core.string_println)(inline224) in
    jump inline_cont223(())
  } in
  let inline227 = "ok" in
  let inline228 = "direct:" in
  let inline229 = trait_impl#ToString#string#to_string(inline227) in
  let inline230 = (inline228 + inline229) in
  jump inline_cont226(inline230)
}

fn trait_impl#RenderValue#Prefix#render__mono1(self/0: Prefix, value/1: string) -> string {
  let t191 = Prefix.value(self/0) in
  join inline_cont231(t192: string) -> string {
    let t193 = (t191 + t192) in
    t193
  } in
  jump inline_cont231(value/1)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont232(t195: string) -> unit {
    let t196 = @runtime(core.string_println)(t195) in
    ()
  } in
  jump inline_cont232(value/1)
}

fn render_generic__T_Prefix(renderer/2: Prefix) -> string {
  let inline234 = 11 in
  let inline235 = Prefix.value(renderer/2) in
  let inline236 = trait_impl#ToString#int#to_string(inline234) in
  let inline237 = (inline235 + inline236) in
  inline237
}

fn trait_impl#RenderValue#Prefix#render__mono2(self/0: Prefix, value/1: int) -> string {
  let t202 = Prefix.value(self/0) in
  join inline_cont238(t203: string) -> string {
    let t204 = (t202 + t203) in
    t204
  } in
  let inline239 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont238(inline239)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t209 = @runtime(core.int_to_string)(self/67) in
  t209
}