fn main() -> unit {
  join inline_cont455(direct/3: string) -> unit {
    join inline_cont452(_wild411: unit) -> unit {
      let t416 = Prefix { value: "generic:" } in
      join inline_cont450(generic/4: string) -> unit {
        join inline_cont447(_wild412: unit) -> unit {
          join inline_cont442(ufcs/5: string) -> unit {
            let inline440 = trait_impl#ToString#string#to_string(ufcs/5) in
            let inline441 = @runtime(core.string_println)(inline440) in
            ()
          } in
          let inline443 = 12 in
          let inline444 = "ufcs:" in
          let inline445 = trait_impl#ToString#int#to_string(inline443) in
          let inline446 = (inline444 + inline445) in
          jump inline_cont442(inline446)
        } in
        let inline448 = trait_impl#ToString#string#to_string(generic/4) in
        let inline449 = @runtime(core.string_println)(inline448) in
        jump inline_cont447(())
      } in
      let inline451 = trait_impl#RenderValue#Prefix#render__mono2(t416, 11) in
      jump inline_cont450(inline451)
    } in
    let inline453 = trait_impl#ToString#string#to_string(direct/3) in
    let inline454 = @runtime(core.string_println)(inline453) in
    jump inline_cont452(())
  } in
  let inline456 = "ok" in
  let inline457 = "direct:" in
  let inline458 = trait_impl#ToString#string#to_string(inline456) in
  let inline459 = (inline457 + inline458) in
  jump inline_cont455(inline459)
}

fn trait_impl#RenderValue#Prefix#render__mono1(self/0: Prefix, value/1: string) -> string {
  let t420 = Prefix.value(self/0) in
  join inline_cont460(t421: string) -> string {
    let t422 = (t420 + t421) in
    t422
  } in
  jump inline_cont460(value/1)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont461(t424: string) -> unit {
    let t425 = @runtime(core.string_println)(t424) in
    ()
  } in
  jump inline_cont461(value/1)
}

fn render_generic__T_Prefix(renderer/2: Prefix) -> string {
  let inline463 = 11 in
  let inline464 = Prefix.value(renderer/2) in
  let inline465 = trait_impl#ToString#int#to_string(inline463) in
  let inline466 = (inline464 + inline465) in
  inline466
}

fn trait_impl#RenderValue#Prefix#render__mono2(self/0: Prefix, value/1: int) -> string {
  let t431 = Prefix.value(self/0) in
  join inline_cont467(t432: string) -> string {
    let t433 = (t431 + t432) in
    t433
  } in
  let inline468 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont467(inline468)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t438 = @runtime(core.int_to_string)(self/151) in
  t438
}