fn main() -> unit {
  let t176 = Prefix { value: "direct:" } in
  join inline_cont216(direct/3: string) -> unit {
    join inline_cont213(_wild172: unit) -> unit {
      let t177 = Prefix { value: "generic:" } in
      join inline_cont211(generic/4: string) -> unit {
        join inline_cont208(_wild173: unit) -> unit {
          let t178 = Prefix { value: "ufcs:" } in
          join inline_cont203(ufcs/5: string) -> unit {
            let inline201 = trait_impl#ToString#string#to_string(ufcs/5) in
            let inline202 = @runtime(core.string_println)(inline201) in
            ()
          } in
          let inline204 = 12 in
          let inline205 = "ufcs:" in
          let inline206 = trait_impl#ToString#int#to_string(inline204) in
          let inline207 = (inline205 + inline206) in
          jump inline_cont203(inline207)
        } in
        let inline209 = trait_impl#ToString#string#to_string(generic/4) in
        let inline210 = @runtime(core.string_println)(inline209) in
        jump inline_cont208(())
      } in
      let inline212 = trait_impl#RenderValue#Prefix#render__mono2(t177, 11) in
      jump inline_cont211(inline212)
    } in
    let inline214 = trait_impl#ToString#string#to_string(direct/3) in
    let inline215 = @runtime(core.string_println)(inline214) in
    jump inline_cont213(())
  } in
  let inline217 = "ok" in
  let inline218 = "direct:" in
  let inline219 = trait_impl#ToString#string#to_string(inline217) in
  let inline220 = (inline218 + inline219) in
  jump inline_cont216(inline220)
}

fn trait_impl#RenderValue#Prefix#render__mono1(self/0: Prefix, value/1: string) -> string {
  let t181 = Prefix.value(self/0) in
  join inline_cont221(t182: string) -> string {
    let t183 = (t181 + t182) in
    t183
  } in
  jump inline_cont221(value/1)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont222(t185: string) -> unit {
    let t186 = @runtime(core.string_println)(t185) in
    ()
  } in
  jump inline_cont222(value/31)
}

fn render_generic__T_Prefix(renderer/2: Prefix) -> string {
  let inline224 = 11 in
  let inline225 = Prefix.value(renderer/2) in
  let inline226 = trait_impl#ToString#int#to_string(inline224) in
  let inline227 = (inline225 + inline226) in
  inline227
}

fn trait_impl#RenderValue#Prefix#render__mono2(self/0: Prefix, value/1: int) -> string {
  let t192 = Prefix.value(self/0) in
  join inline_cont228(t193: string) -> string {
    let t194 = (t192 + t193) in
    t194
  } in
  let inline229 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont228(inline229)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t199 = @runtime(core.int_to_string)(self/69) in
  t199
}