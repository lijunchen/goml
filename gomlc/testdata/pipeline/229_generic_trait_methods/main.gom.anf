fn main() -> unit {
  let t181 = Prefix { value: "direct:" } in
  join inline_cont221(direct/3: string) -> unit {
    join inline_cont218(_wild177: unit) -> unit {
      let t182 = Prefix { value: "generic:" } in
      join inline_cont216(generic/4: string) -> unit {
        join inline_cont213(_wild178: unit) -> unit {
          let t183 = Prefix { value: "ufcs:" } in
          join inline_cont208(ufcs/5: string) -> unit {
            let inline206 = trait_impl#ToString#string#to_string(ufcs/5) in
            let inline207 = @runtime(core.string_println)(inline206) in
            ()
          } in
          let inline209 = 12 in
          let inline210 = "ufcs:" in
          let inline211 = trait_impl#ToString#int#to_string(inline209) in
          let inline212 = (inline210 + inline211) in
          jump inline_cont208(inline212)
        } in
        let inline214 = trait_impl#ToString#string#to_string(generic/4) in
        let inline215 = @runtime(core.string_println)(inline214) in
        jump inline_cont213(())
      } in
      let inline217 = trait_impl#RenderValue#Prefix#render__mono2(t182, 11) in
      jump inline_cont216(inline217)
    } in
    let inline219 = trait_impl#ToString#string#to_string(direct/3) in
    let inline220 = @runtime(core.string_println)(inline219) in
    jump inline_cont218(())
  } in
  let inline222 = "ok" in
  let inline223 = "direct:" in
  let inline224 = trait_impl#ToString#string#to_string(inline222) in
  let inline225 = (inline223 + inline224) in
  jump inline_cont221(inline225)
}

fn trait_impl#RenderValue#Prefix#render__mono1(self/0: Prefix, value/1: string) -> string {
  let t186 = Prefix.value(self/0) in
  join inline_cont226(t187: string) -> string {
    let t188 = (t186 + t187) in
    t188
  } in
  jump inline_cont226(value/1)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont227(t190: string) -> unit {
    let t191 = @runtime(core.string_println)(t190) in
    ()
  } in
  jump inline_cont227(value/31)
}

fn render_generic__T_Prefix(renderer/2: Prefix) -> string {
  let inline229 = 11 in
  let inline230 = Prefix.value(renderer/2) in
  let inline231 = trait_impl#ToString#int#to_string(inline229) in
  let inline232 = (inline230 + inline231) in
  inline232
}

fn trait_impl#RenderValue#Prefix#render__mono2(self/0: Prefix, value/1: int) -> string {
  let t197 = Prefix.value(self/0) in
  join inline_cont233(t198: string) -> string {
    let t199 = (t197 + t198) in
    t199
  } in
  let inline234 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont233(inline234)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t204 = @runtime(core.int_to_string)(self/69) in
  t204
}