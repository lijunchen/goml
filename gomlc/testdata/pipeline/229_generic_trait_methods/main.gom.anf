fn main() -> unit {
  join inline_cont452(direct/3: string) -> unit {
    join inline_cont449(_wild408: unit) -> unit {
      let t413 = Prefix { value: "generic:" } in
      join inline_cont447(generic/4: string) -> unit {
        join inline_cont444(_wild409: unit) -> unit {
          join inline_cont439(ufcs/5: string) -> unit {
            let inline437 = trait_impl#ToString#string#to_string(ufcs/5) in
            let inline438 = @runtime(core.string_println)(inline437) in
            ()
          } in
          let inline440 = 12 in
          let inline441 = "ufcs:" in
          let inline442 = trait_impl#ToString#int#to_string(inline440) in
          let inline443 = (inline441 + inline442) in
          jump inline_cont439(inline443)
        } in
        let inline445 = trait_impl#ToString#string#to_string(generic/4) in
        let inline446 = @runtime(core.string_println)(inline445) in
        jump inline_cont444(())
      } in
      let inline448 = trait_impl#RenderValue#Prefix#render__mono2(t413, 11) in
      jump inline_cont447(inline448)
    } in
    let inline450 = trait_impl#ToString#string#to_string(direct/3) in
    let inline451 = @runtime(core.string_println)(inline450) in
    jump inline_cont449(())
  } in
  let inline453 = "ok" in
  let inline454 = "direct:" in
  let inline455 = trait_impl#ToString#string#to_string(inline453) in
  let inline456 = (inline454 + inline455) in
  jump inline_cont452(inline456)
}

fn trait_impl#RenderValue#Prefix#render__mono1(self/0: Prefix, value/1: string) -> string {
  let t417 = Prefix.value(self/0) in
  join inline_cont457(t418: string) -> string {
    let t419 = (t417 + t418) in
    t419
  } in
  jump inline_cont457(value/1)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont458(t421: string) -> unit {
    let t422 = @runtime(core.string_println)(t421) in
    ()
  } in
  jump inline_cont458(value/1)
}

fn render_generic__T_Prefix(renderer/2: Prefix) -> string {
  let inline460 = 11 in
  let inline461 = Prefix.value(renderer/2) in
  let inline462 = trait_impl#ToString#int#to_string(inline460) in
  let inline463 = (inline461 + inline462) in
  inline463
}

fn trait_impl#RenderValue#Prefix#render__mono2(self/0: Prefix, value/1: int) -> string {
  let t428 = Prefix.value(self/0) in
  join inline_cont464(t429: string) -> string {
    let t430 = (t428 + t429) in
    t430
  } in
  let inline465 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont464(inline465)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t435 = @runtime(core.int_to_string)(self/151) in
  t435
}