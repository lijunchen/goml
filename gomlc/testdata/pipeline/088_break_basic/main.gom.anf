fn main() -> unit {
  join inline_cont243(i/0: Ref[int]) -> unit {
    join exit188() -> unit {
      let inline240 = "done" in
      let inline241 = trait_impl#ToString#string#to_string(inline240) in
      let inline242 = @runtime(core.string_println)(inline241) in
      ()
    } in
    joinrec {
      join loop189() -> unit {
        join inline_cont237(t190: int) -> unit {
          let t191 = (t190 < 10) in
          if t191 {
            join inline_cont235(t196: int) -> unit {
              join inline_cont232(t197: bool) -> unit {
                join k192() -> unit {
                  join inline_cont230(t193: int) -> unit {
                    join inline_cont227(mtmp183: unit) -> unit {
                      join inline_cont225(t194: int) -> unit {
                        let t195 = (t194 + 1) in
                        let inline224 = @intrinsic(ref.set)(i/0, t195) in
                        jump loop189()
                      } in
                      let inline226 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont225(inline226)
                    } in
                    let inline228 = trait_impl#ToString#int#to_string(t193) in
                    let inline229 = @runtime(core.string_println)(inline228) in
                    jump inline_cont227(())
                  } in
                  let inline231 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont230(inline231)
                } in
                if t197 {
                  jump exit188()
                } else {
                  jump k192()
                }
              } in
              let inline233 = 5 in
              let inline234 = (t196 == inline233) in
              jump inline_cont232(inline234)
            } in
            let inline236 = @intrinsic(ref.get)(i/0) in
            jump inline_cont235(inline236)
          } else {
            jump exit188()
          }
        } in
        let inline238 = @intrinsic(ref.get)(i/0) in
        jump inline_cont237(inline238)
      } in
    } in
    jump loop189()
  } in
  let inline244 = 0 in
  let inline245 = @intrinsic(ref.new)(inline244) in
  jump inline_cont243(inline245)
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t200 = @intrinsic(ref.new)(value/270) in
  t200
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t203 = @intrinsic(ref.get)(self/271) in
  t203
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t206 = (self/101 == other/102) in
  t206
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont246(t208: string) -> unit {
    let t209 = @runtime(core.string_println)(t208) in
    ()
  } in
  let inline247 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont246(inline247)
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t211 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont248(t213: string) -> unit {
    let t214 = @runtime(core.string_println)(t213) in
    ()
  } in
  jump inline_cont248(value/1)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t217 = @runtime(core.int_to_string)(self/67) in
  t217
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t222 = @runtime(core.int_hash)(self/127) in
  t222
}