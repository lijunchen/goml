fn main() -> unit {
  join inline_cont897(i/0: Ref[isize]) -> unit {
    join exit802() -> unit {
      let inline894 = "done" in
      let inline895 = trait_impl#ToString#string#to_string(inline894) in
      let inline896 = @runtime(core.string_println)(inline895) in
      ()
    } in
    joinrec {
      join loop803() -> unit {
        join inline_cont891(t804: isize) -> unit {
          let t805 = (t804 < 10) in
          if t805 {
            join inline_cont889(t810: isize) -> unit {
              let t811 = (t810 == 5) in
              join k806() -> unit {
                join inline_cont887(t807: isize) -> unit {
                  join inline_cont884(mtmp797: unit) -> unit {
                    join inline_cont882(t808: isize) -> unit {
                      let t809 = (t808 + 1) in
                      let inline881 = @intrinsic(ref.set)(i/0, t809) in
                      jump loop803()
                    } in
                    let inline883 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont882(inline883)
                  } in
                  let inline885 = trait_impl#ToString#isize#to_string(t807) in
                  let inline886 = @runtime(core.string_println)(inline885) in
                  jump inline_cont884(())
                } in
                let inline888 = @intrinsic(ref.get)(i/0) in
                jump inline_cont887(inline888)
              } in
              if t811 {
                jump exit802()
              } else {
                jump k806()
              }
            } in
            let inline890 = @intrinsic(ref.get)(i/0) in
            jump inline_cont889(inline890)
          } else {
            jump exit802()
          }
        } in
        let inline892 = @intrinsic(ref.get)(i/0) in
        jump inline_cont891(inline892)
      } in
    } in
    jump loop803()
  } in
  let inline898 = 0 in
  let inline899 = @intrinsic(ref.new)(inline898) in
  jump inline_cont897(inline899)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t814 = @intrinsic(ref.new)(value/684) in
  t814
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t817 = @intrinsic(ref.get)(self/685) in
  t817
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont900(t819: string) -> unit {
    let t820 = @runtime(core.string_println)(t819) in
    ()
  } in
  let inline901 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont900(inline901)
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t822 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont902(t824: string) -> unit {
    let t825 = @runtime(core.string_println)(t824) in
    ()
  } in
  jump inline_cont902(value/1)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline904 = (self/404 as i64) in
  let inline905 = signed_decimal_string(inline904) in
  inline905
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t833 = (value/222 as i64) in
  let inline907 = (t833 < 0) in
  if inline907 {
    let inline908 = (t833 as u64) in
    let inline909 = (0 - inline908) in
    let inline910 = decimal_string(inline909) in
    let inline911 = ("-" + inline910) in
    inline911
  } else {
    let inline912 = (t833 as u64) in
    let inline913 = decimal_string(inline912) in
    inline913
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t839 = (value/214 < 0) in
  if t839 {
    let t840 = (value/214 as u64) in
    let t841 = (0 - t840) in
    let t842 = decimal_string(t841) in
    let t843 = ("-" + t842) in
    t843
  } else {
    let t844 = (value/214 as u64) in
    let t845 = decimal_string(t844) in
    t845
  }
}

fn decimal_string(value/208: u64) -> string {
  let t868 = (value/208 == 0) in
  join k848() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit849() -> string {
      join inline_cont922(t850: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t850) in
        let offset/212 = 0 in
        join exit851() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop852() -> string {
            join inline_cont920(t853: isize) -> string {
              let t854 = (offset/212 < t853) in
              if t854 {
                join inline_cont918(t855: isize) -> string {
                  let t856 = (t855 - offset/212) in
                  let t857 = (t856 - 1) in
                  let t858 = @intrinsic(vec.get)(reversed/209, t857) in
                  join inline_cont916(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t859 = (compound_old358 + compound_value359) in
                    let t860 = offset/212 = t859 in
                    jump loop852()
                  } in
                  let inline917 = @intrinsic(vec.push)(bytes/211, t858) in
                  jump inline_cont916(())
                } in
                let inline919 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont918(inline919)
              } else {
                jump exit851()
              }
            } in
            let inline921 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont920(inline921)
          } in
        } in
        jump loop852()
      } in
      let inline923 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont922(inline923)
    } in
    joinrec {
      join loop861() -> string {
        let t862 = (remaining/210 > 0) in
        if t862 {
          let t863 = (remaining/210 % 10) in
          let t864 = (t863 as u8) in
          let t865 = (t864 + 48) in
          join inline_cont914(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t866 = (compound_old353 / compound_value354) in
            let t867 = remaining/210 = t866 in
            jump loop861()
          } in
          let inline915 = @intrinsic(vec.push)(reversed/209, t865) in
          jump inline_cont914(())
        } else {
          jump exit849()
        }
      } in
    } in
    jump loop861()
  } in
  if t868 {
    "0"
  } else {
    jump k848()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t870 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t873 = @intrinsic(vec.len)(self/526) in
  t873
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t876 = (self/438 == other/439) in
  t876
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t879 = @runtime(core.int_hash)(self/464) in
  t879
}