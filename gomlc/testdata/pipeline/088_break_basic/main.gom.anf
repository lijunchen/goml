fn main() -> unit {
  join inline_cont197(i/0: Ref[int]) -> unit {
    join exit142() -> unit {
      let _wild139 = () in
      let inline194 = "done" in
      let inline195 = trait_impl#ToString#string#to_string(inline194) in
      let inline196 = @runtime(core.string_println)(inline195) in
      ()
    } in
    joinrec {
      join loop143() -> unit {
        join inline_cont191(t144: int) -> unit {
          let t145 = (t144 < 10) in
          if t145 {
            join inline_cont189(t150: int) -> unit {
              join inline_cont186(t151: bool) -> unit {
                join k146() -> unit {
                  let _wild136 = () in
                  join inline_cont184(t147: int) -> unit {
                    join inline_cont181(mtmp137: unit) -> unit {
                      join inline_cont179(t148: int) -> unit {
                        let t149 = (t148 + 1) in
                        let inline178 = @intrinsic(ref.set)(i/0, t149) in
                        jump loop143()
                      } in
                      let inline180 = @intrinsic(ref.get)(i/0) in
                      jump inline_cont179(inline180)
                    } in
                    let inline182 = trait_impl#ToString#int#to_string(t147) in
                    let inline183 = @runtime(core.string_println)(inline182) in
                    jump inline_cont181(())
                  } in
                  let inline185 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont184(inline185)
                } in
                if t151 {
                  jump exit142()
                } else {
                  jump k146()
                }
              } in
              let inline187 = 5 in
              let inline188 = @intrinsic(eq.primitive)(t150, inline187) in
              jump inline_cont186(inline188)
            } in
            let inline190 = @intrinsic(ref.get)(i/0) in
            jump inline_cont189(inline190)
          } else {
            jump exit142()
          }
        } in
        let inline192 = @intrinsic(ref.get)(i/0) in
        jump inline_cont191(inline192)
      } in
    } in
    jump loop143()
  } in
  let inline198 = 0 in
  let inline199 = @intrinsic(ref.new)(inline198) in
  jump inline_cont197(inline199)
}

fn inherent#Ref#Ref[T]#new__T_int(value/215: int) -> Ref[int] {
  let t154 = @intrinsic(ref.new)(value/215) in
  t154
}

fn inherent#Ref#Ref[T]#get__T_int(self/216: Ref[int]) -> int {
  let t157 = @intrinsic(ref.get)(self/216) in
  t157
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t160 = @intrinsic(eq.primitive)(self/88, other/89) in
  t160
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont200(t162: string) -> unit {
    let t163 = @runtime(core.string_println)(t162) in
    ()
  } in
  let inline201 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont200(inline201)
}

fn inherent#Ref#Ref[T]#set__T_int(self/217: Ref[int], value/218: int) -> unit {
  let t165 = @intrinsic(ref.set)(self/217, value/218) in
  ()
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont202(t167: string) -> unit {
    let t168 = @runtime(core.string_println)(t167) in
    ()
  } in
  jump inline_cont202(value/31)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t171 = @runtime(core.int_to_string)(self/69) in
  t171
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t176 = @runtime(core.int_hash)(self/114) in
  t176
}