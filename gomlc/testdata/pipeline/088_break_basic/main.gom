fn main() {
    let i = Ref::new(0);
    while i.get() < 10 {
        if i.get() == 5 {
            break
        } else {};
        let _ = println(i.get());
        i.set(i.get() + 1);
    }
    let _ = println("done");
}
