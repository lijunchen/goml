fn main() -> unit {
    (println : (bool) -> unit)("" + "{{" == "{{");
    (println : (bool) -> unit)("" + "}}" == "}}");
    (println : (bool) -> unit)("" + "{{" == "{{");
    (println : (bool) -> unit)("" + "}}" == "}}")
}