fn pair(first: i32, second: string) -> (i32, string) {
    (first, second)
}

fn main() -> unit {
    let make: (i32, string) -> (i32, string) = pair;
    let value = make(1, "x");
    println(value.0.to_string() + value.1)
}
