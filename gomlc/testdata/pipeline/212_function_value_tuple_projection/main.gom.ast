package main;
fn pair(first: int32, second: string) -> (int32, string) {
    (first, second)
}

fn main() -> unit {
    let make: (int32, string) -> (int32, string) = pair;
    let value = make(1, "x");
    println(value.0.to_string() + value.1)
}

