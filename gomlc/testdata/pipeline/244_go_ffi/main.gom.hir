package main
file main.gom
  #[go_ffi("strings", "ToUpper")]
  extern fn to_upper(value: string) -> string

  #[go_ffi("strings", "Cut")]
  extern fn cut(value: string, separator: string) -> (string, string, bool)

  #[go_ffi("runtime", "Gosched")]
  extern fn gosched() -> ()

  fn main() -> () {
      println(def/1/0("goml"));
      let (local/1/0, local/1/1, local/1/2) = def/1/1("left:right", ":");
      println(before/0);
      println(after/1);
      println(found/2);
      def/1/2()
  }