fn choose(flag/0: bool) -> int32 {
  join k195(jp196: int32) -> int32 {
    let t197 = (jp196 + 1) in
    t197
  } in
  if flag/0 {
    10
  } else {
    jump k195(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont259(count/2: Ref[int]) -> unit {
    join exit199() -> unit {
      ()
    } in
    joinrec {
      join loop200() -> unit {
        join inline_cont257(t201: int) -> unit {
          let t202 = (t201 < 2) in
          if t202 {
            join inline_cont255(t203: int) -> unit {
              let t204 = (t203 + 1) in
              join inline_cont253(_wild187: unit) -> unit {
                join inline_cont251(t208: int) -> unit {
                  join inline_cont248(t209: bool) -> unit {
                    join k205(jp206: int) -> unit {
                      let inline246 = trait_impl#ToString#int#to_string(jp206) in
                      let inline247 = @runtime(core.string_println)(inline246) in
                      jump loop200()
                    } in
                    if t209 {
                      jump loop200()
                    } else {
                      jump k205(7)
                    }
                  } in
                  let inline249 = 1 in
                  let inline250 = (t208 == inline249) in
                  jump inline_cont248(inline250)
                } in
                let inline252 = @intrinsic(ref.get)(count/2) in
                jump inline_cont251(inline252)
              } in
              let inline254 = @intrinsic(ref.set)(count/2, t204) in
              jump inline_cont253(())
            } in
            let inline256 = @intrinsic(ref.get)(count/2) in
            jump inline_cont255(inline256)
          } else {
            jump exit199()
          }
        } in
        let inline258 = @intrinsic(ref.get)(count/2) in
        jump inline_cont257(inline258)
      } in
    } in
    jump loop200()
  } in
  let inline260 = 0 in
  let inline261 = @intrinsic(ref.new)(inline260) in
  jump inline_cont259(inline261)
}

fn break_branch(stop/4: bool) -> unit {
  join exit211() -> unit {
    ()
  } in
  joinrec {
    join loop212() -> unit {
      join k213(jp214: int) -> unit {
        let inline263 = trait_impl#ToString#int#to_string(jp214) in
        let inline264 = @runtime(core.string_println)(inline263) in
        jump exit211()
      } in
      if stop/4 {
        jump exit211()
      } else {
        jump k213(9)
      }
    } in
  } in
  jump loop212()
}

fn main() -> unit {
  join inline_cont277(t216: int32) -> unit {
    join inline_cont274(_wild189: unit) -> unit {
      join inline_cont268(t217: int32) -> unit {
        join inline_cont265(_wild190: unit) -> unit {
          let _wild191 = continue_branch() in
          let _wild192 = break_branch(false) in
          let t218 = break_branch(true) in
          ()
        } in
        let inline266 = trait_impl#ToString#int32#to_string(t217) in
        let inline267 = @runtime(core.string_println)(inline266) in
        jump inline_cont265(())
      } in
      let inline269 = true in
      join inline_join270(inline271: int32) -> unit {
        let inline273 = (inline271 + 1) in
        jump inline_cont268(inline273)
      } in
      if inline269 {
        jump inline_cont268(10)
      } else {
        jump inline_join270(20)
      }
    } in
    let inline275 = trait_impl#ToString#int32#to_string(t216) in
    let inline276 = @runtime(core.string_println)(inline275) in
    jump inline_cont274(())
  } in
  let inline278 = false in
  join inline_join279(inline280: int32) -> unit {
    let inline282 = (inline280 + 1) in
    jump inline_cont277(inline282)
  } in
  if inline278 {
    jump inline_cont277(10)
  } else {
    jump inline_join279(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t221 = @intrinsic(ref.new)(value/273) in
  t221
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t224 = @intrinsic(ref.get)(self/274) in
  t224
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t226 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t229 = (self/101 == other/102) in
  t229
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont283(t231: string) -> unit {
    let t232 = @runtime(core.string_println)(t231) in
    ()
  } in
  let inline284 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont283(inline284)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont285(t234: string) -> unit {
    let t235 = @runtime(core.string_println)(t234) in
    ()
  } in
  let inline286 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont285(inline286)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t238 = @runtime(core.int_to_string)(self/67) in
  t238
}

fn trait_impl#ToString#int32#to_string(self/70: int32) -> string {
  let t241 = @runtime(core.int32_to_string)(self/70) in
  t241
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t244 = @runtime(core.int_hash)(self/127) in
  t244
}