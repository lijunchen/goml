fn choose(flag/0: bool) -> int32 {
  join k419(jp420: int32) -> int32 {
    let t421 = (jp420 + 1) in
    t421
  } in
  if flag/0 {
    10
  } else {
    jump k419(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont480(count/2: Ref[int]) -> unit {
    join exit423() -> unit {
      ()
    } in
    joinrec {
      join loop424() -> unit {
        join inline_cont478(t425: int) -> unit {
          let t426 = (t425 < 2) in
          if t426 {
            join inline_cont476(t427: int) -> unit {
              let t428 = (t427 + 1) in
              join inline_cont474(_wild411: unit) -> unit {
                join inline_cont472(t432: int) -> unit {
                  let t433 = (t432 == 1) in
                  join k429(jp430: int) -> unit {
                    let inline470 = trait_impl#ToString#int#to_string(jp430) in
                    let inline471 = @runtime(core.string_println)(inline470) in
                    jump loop424()
                  } in
                  if t433 {
                    jump loop424()
                  } else {
                    jump k429(7)
                  }
                } in
                let inline473 = @intrinsic(ref.get)(count/2) in
                jump inline_cont472(inline473)
              } in
              let inline475 = @intrinsic(ref.set)(count/2, t428) in
              jump inline_cont474(())
            } in
            let inline477 = @intrinsic(ref.get)(count/2) in
            jump inline_cont476(inline477)
          } else {
            jump exit423()
          }
        } in
        let inline479 = @intrinsic(ref.get)(count/2) in
        jump inline_cont478(inline479)
      } in
    } in
    jump loop424()
  } in
  let inline481 = 0 in
  let inline482 = @intrinsic(ref.new)(inline481) in
  jump inline_cont480(inline482)
}

fn break_branch(stop/4: bool) -> unit {
  join exit435() -> unit {
    ()
  } in
  joinrec {
    join loop436() -> unit {
      join k437(jp438: int) -> unit {
        let inline484 = trait_impl#ToString#int#to_string(jp438) in
        let inline485 = @runtime(core.string_println)(inline484) in
        jump exit435()
      } in
      if stop/4 {
        jump exit435()
      } else {
        jump k437(9)
      }
    } in
  } in
  jump loop436()
}

fn main() -> unit {
  join inline_cont498(t440: int32) -> unit {
    join inline_cont495(_wild413: unit) -> unit {
      join inline_cont489(t441: int32) -> unit {
        join inline_cont486(_wild414: unit) -> unit {
          let _wild415 = continue_branch() in
          let _wild416 = break_branch(false) in
          let t442 = break_branch(true) in
          ()
        } in
        let inline487 = trait_impl#ToString#int32#to_string(t441) in
        let inline488 = @runtime(core.string_println)(inline487) in
        jump inline_cont486(())
      } in
      let inline490 = true in
      join inline_join491(inline492: int32) -> unit {
        let inline494 = (inline492 + 1) in
        jump inline_cont489(inline494)
      } in
      if inline490 {
        jump inline_cont489(10)
      } else {
        jump inline_join491(20)
      }
    } in
    let inline496 = trait_impl#ToString#int32#to_string(t440) in
    let inline497 = @runtime(core.string_println)(inline496) in
    jump inline_cont495(())
  } in
  let inline499 = false in
  join inline_join500(inline501: int32) -> unit {
    let inline503 = (inline501 + 1) in
    jump inline_cont498(inline503)
  } in
  if inline499 {
    jump inline_cont498(10)
  } else {
    jump inline_join500(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t445 = @intrinsic(ref.new)(value/431) in
  t445
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t448 = @intrinsic(ref.get)(self/432) in
  t448
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t450 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont504(t452: string) -> unit {
    let t453 = @runtime(core.string_println)(t452) in
    ()
  } in
  let inline505 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont504(inline505)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont506(t455: string) -> unit {
    let t456 = @runtime(core.string_println)(t455) in
    ()
  } in
  let inline507 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont506(inline507)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t459 = @runtime(core.int_to_string)(self/151) in
  t459
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t462 = @runtime(core.int32_to_string)(self/154) in
  t462
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t465 = (self/185 == other/186) in
  t465
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t468 = @runtime(core.int_hash)(self/211) in
  t468
}