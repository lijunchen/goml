fn choose(flag/0: bool) -> int32 {
  join k144(jp145: int32) -> int32 {
    let value/1 = jp145 in
    let t146 = (jp145 + 1) in
    t146
  } in
  if flag/0 {
    10
  } else {
    jump k144(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont208(count/2: Ref[int]) -> unit {
    join exit148() -> unit {
      ()
    } in
    joinrec {
      join loop149() -> unit {
        join inline_cont206(t150: int) -> unit {
          let t151 = (t150 < 2) in
          if t151 {
            join inline_cont204(t152: int) -> unit {
              let t153 = (t152 + 1) in
              join inline_cont202(_wild136: unit) -> unit {
                join inline_cont200(t157: int) -> unit {
                  join inline_cont197(t158: bool) -> unit {
                    join k154(jp155: int) -> unit {
                      let value/3 = jp155 in
                      let inline195 = trait_impl#ToString#int#to_string(jp155) in
                      let inline196 = @runtime(core.string_println)(inline195) in
                      jump loop149()
                    } in
                    if t158 {
                      jump loop149()
                    } else {
                      jump k154(7)
                    }
                  } in
                  let inline198 = 1 in
                  let inline199 = @intrinsic(eq.primitive)(t157, inline198) in
                  jump inline_cont197(inline199)
                } in
                let inline201 = @intrinsic(ref.get)(count/2) in
                jump inline_cont200(inline201)
              } in
              let inline203 = @intrinsic(ref.set)(count/2, t153) in
              jump inline_cont202(())
            } in
            let inline205 = @intrinsic(ref.get)(count/2) in
            jump inline_cont204(inline205)
          } else {
            jump exit148()
          }
        } in
        let inline207 = @intrinsic(ref.get)(count/2) in
        jump inline_cont206(inline207)
      } in
    } in
    jump loop149()
  } in
  let inline209 = 0 in
  let inline210 = @intrinsic(ref.new)(inline209) in
  jump inline_cont208(inline210)
}

fn break_branch(stop/4: bool) -> unit {
  join exit160() -> unit {
    ()
  } in
  joinrec {
    join loop161() -> unit {
      join k162(jp163: int) -> unit {
        let value/5 = jp163 in
        let inline212 = trait_impl#ToString#int#to_string(jp163) in
        let inline213 = @runtime(core.string_println)(inline212) in
        jump exit160()
      } in
      if stop/4 {
        jump exit160()
      } else {
        jump k162(9)
      }
    } in
  } in
  jump loop161()
}

fn main() -> unit {
  join inline_cont226(t165: int32) -> unit {
    join inline_cont223(_wild138: unit) -> unit {
      join inline_cont217(t166: int32) -> unit {
        join inline_cont214(_wild139: unit) -> unit {
          let _wild140 = continue_branch() in
          let _wild141 = break_branch(false) in
          let t167 = break_branch(true) in
          ()
        } in
        let inline215 = trait_impl#ToString#int32#to_string(t166) in
        let inline216 = @runtime(core.string_println)(inline215) in
        jump inline_cont214(())
      } in
      let inline218 = true in
      join inline_join219(inline220: int32) -> unit {
        let inline221 = inline220 in
        let inline222 = (inline220 + 1) in
        jump inline_cont217(inline222)
      } in
      if inline218 {
        jump inline_cont217(10)
      } else {
        jump inline_join219(20)
      }
    } in
    let inline224 = trait_impl#ToString#int32#to_string(t165) in
    let inline225 = @runtime(core.string_println)(inline224) in
    jump inline_cont223(())
  } in
  let inline227 = false in
  join inline_join228(inline229: int32) -> unit {
    let inline230 = inline229 in
    let inline231 = (inline229 + 1) in
    jump inline_cont226(inline231)
  } in
  if inline227 {
    jump inline_cont226(10)
  } else {
    jump inline_join228(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/232: int) -> Ref[int] {
  let t170 = @intrinsic(ref.new)(value/232) in
  t170
}

fn inherent#Ref#Ref[T]#get__T_int(self/233: Ref[int]) -> int {
  let t173 = @intrinsic(ref.get)(self/233) in
  t173
}

fn inherent#Ref#Ref[T]#set__T_int(self/234: Ref[int], value/235: int) -> unit {
  let t175 = @intrinsic(ref.set)(self/234, value/235) in
  ()
}

fn trait_impl#Eq#int#eq(self/103: int, other/104: int) -> bool {
  let t178 = @intrinsic(eq.primitive)(self/103, other/104) in
  t178
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont232(t180: string) -> unit {
    let t181 = @runtime(core.string_println)(t180) in
    ()
  } in
  let inline233 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont232(inline233)
}

fn println__T_int32(value/31: int32) -> unit {
  join inline_cont234(t183: string) -> unit {
    let t184 = @runtime(core.string_println)(t183) in
    ()
  } in
  let inline235 = @runtime(core.int32_to_string)(value/31) in
  jump inline_cont234(inline235)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t187 = @runtime(core.int_to_string)(self/69) in
  t187
}

fn trait_impl#ToString#int32#to_string(self/72: int32) -> string {
  let t190 = @runtime(core.int32_to_string)(self/72) in
  t190
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t193 = @runtime(core.int_hash)(self/129) in
  t193
}