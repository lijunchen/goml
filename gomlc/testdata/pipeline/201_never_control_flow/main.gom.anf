fn choose(flag/0: bool) -> int32 {
  join k185(jp186: int32) -> int32 {
    let value/1 = jp186 in
    let t187 = (jp186 + 1) in
    t187
  } in
  if flag/0 {
    10
  } else {
    jump k185(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont249(count/2: Ref[int]) -> unit {
    join exit189() -> unit {
      ()
    } in
    joinrec {
      join loop190() -> unit {
        join inline_cont247(t191: int) -> unit {
          let t192 = (t191 < 2) in
          if t192 {
            join inline_cont245(t193: int) -> unit {
              let t194 = (t193 + 1) in
              join inline_cont243(_wild177: unit) -> unit {
                join inline_cont241(t198: int) -> unit {
                  join inline_cont238(t199: bool) -> unit {
                    join k195(jp196: int) -> unit {
                      let value/3 = jp196 in
                      let inline236 = trait_impl#ToString#int#to_string(jp196) in
                      let inline237 = @runtime(core.string_println)(inline236) in
                      jump loop190()
                    } in
                    if t199 {
                      jump loop190()
                    } else {
                      jump k195(7)
                    }
                  } in
                  let inline239 = 1 in
                  let inline240 = @intrinsic(eq.primitive)(t198, inline239) in
                  jump inline_cont238(inline240)
                } in
                let inline242 = @intrinsic(ref.get)(count/2) in
                jump inline_cont241(inline242)
              } in
              let inline244 = @intrinsic(ref.set)(count/2, t194) in
              jump inline_cont243(())
            } in
            let inline246 = @intrinsic(ref.get)(count/2) in
            jump inline_cont245(inline246)
          } else {
            jump exit189()
          }
        } in
        let inline248 = @intrinsic(ref.get)(count/2) in
        jump inline_cont247(inline248)
      } in
    } in
    jump loop190()
  } in
  let inline250 = 0 in
  let inline251 = @intrinsic(ref.new)(inline250) in
  jump inline_cont249(inline251)
}

fn break_branch(stop/4: bool) -> unit {
  join exit201() -> unit {
    ()
  } in
  joinrec {
    join loop202() -> unit {
      join k203(jp204: int) -> unit {
        let value/5 = jp204 in
        let inline253 = trait_impl#ToString#int#to_string(jp204) in
        let inline254 = @runtime(core.string_println)(inline253) in
        jump exit201()
      } in
      if stop/4 {
        jump exit201()
      } else {
        jump k203(9)
      }
    } in
  } in
  jump loop202()
}

fn main() -> unit {
  join inline_cont267(t206: int32) -> unit {
    join inline_cont264(_wild179: unit) -> unit {
      join inline_cont258(t207: int32) -> unit {
        join inline_cont255(_wild180: unit) -> unit {
          let _wild181 = continue_branch() in
          let _wild182 = break_branch(false) in
          let t208 = break_branch(true) in
          ()
        } in
        let inline256 = trait_impl#ToString#int32#to_string(t207) in
        let inline257 = @runtime(core.string_println)(inline256) in
        jump inline_cont255(())
      } in
      let inline259 = true in
      join inline_join260(inline261: int32) -> unit {
        let inline262 = inline261 in
        let inline263 = (inline261 + 1) in
        jump inline_cont258(inline263)
      } in
      if inline259 {
        jump inline_cont258(10)
      } else {
        jump inline_join260(20)
      }
    } in
    let inline265 = trait_impl#ToString#int32#to_string(t206) in
    let inline266 = @runtime(core.string_println)(inline265) in
    jump inline_cont264(())
  } in
  let inline268 = false in
  join inline_join269(inline270: int32) -> unit {
    let inline271 = inline270 in
    let inline272 = (inline270 + 1) in
    jump inline_cont267(inline272)
  } in
  if inline268 {
    jump inline_cont267(10)
  } else {
    jump inline_join269(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/236: int) -> Ref[int] {
  let t211 = @intrinsic(ref.new)(value/236) in
  t211
}

fn inherent#Ref#Ref[T]#get__T_int(self/237: Ref[int]) -> int {
  let t214 = @intrinsic(ref.get)(self/237) in
  t214
}

fn inherent#Ref#Ref[T]#set__T_int(self/238: Ref[int], value/239: int) -> unit {
  let t216 = @intrinsic(ref.set)(self/238, value/239) in
  ()
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t219 = @intrinsic(eq.primitive)(self/88, other/89) in
  t219
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont273(t221: string) -> unit {
    let t222 = @runtime(core.string_println)(t221) in
    ()
  } in
  let inline274 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont273(inline274)
}

fn println__T_int32(value/31: int32) -> unit {
  join inline_cont275(t224: string) -> unit {
    let t225 = @runtime(core.string_println)(t224) in
    ()
  } in
  let inline276 = @runtime(core.int32_to_string)(value/31) in
  jump inline_cont275(inline276)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t228 = @runtime(core.int_to_string)(self/69) in
  t228
}

fn trait_impl#ToString#int32#to_string(self/72: int32) -> string {
  let t231 = @runtime(core.int32_to_string)(self/72) in
  t231
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t234 = @runtime(core.int_hash)(self/114) in
  t234
}