fn choose(flag/0: bool) -> int32 {
  join k190(jp191: int32) -> int32 {
    let t192 = (jp191 + 1) in
    t192
  } in
  if flag/0 {
    10
  } else {
    jump k190(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont254(count/2: Ref[int]) -> unit {
    join exit194() -> unit {
      ()
    } in
    joinrec {
      join loop195() -> unit {
        join inline_cont252(t196: int) -> unit {
          let t197 = (t196 < 2) in
          if t197 {
            join inline_cont250(t198: int) -> unit {
              let t199 = (t198 + 1) in
              join inline_cont248(_wild182: unit) -> unit {
                join inline_cont246(t203: int) -> unit {
                  join inline_cont243(t204: bool) -> unit {
                    join k200(jp201: int) -> unit {
                      let inline241 = trait_impl#ToString#int#to_string(jp201) in
                      let inline242 = @runtime(core.string_println)(inline241) in
                      jump loop195()
                    } in
                    if t204 {
                      jump loop195()
                    } else {
                      jump k200(7)
                    }
                  } in
                  let inline244 = 1 in
                  let inline245 = (t203 == inline244) in
                  jump inline_cont243(inline245)
                } in
                let inline247 = @intrinsic(ref.get)(count/2) in
                jump inline_cont246(inline247)
              } in
              let inline249 = @intrinsic(ref.set)(count/2, t199) in
              jump inline_cont248(())
            } in
            let inline251 = @intrinsic(ref.get)(count/2) in
            jump inline_cont250(inline251)
          } else {
            jump exit194()
          }
        } in
        let inline253 = @intrinsic(ref.get)(count/2) in
        jump inline_cont252(inline253)
      } in
    } in
    jump loop195()
  } in
  let inline255 = 0 in
  let inline256 = @intrinsic(ref.new)(inline255) in
  jump inline_cont254(inline256)
}

fn break_branch(stop/4: bool) -> unit {
  join exit206() -> unit {
    ()
  } in
  joinrec {
    join loop207() -> unit {
      join k208(jp209: int) -> unit {
        let inline258 = trait_impl#ToString#int#to_string(jp209) in
        let inline259 = @runtime(core.string_println)(inline258) in
        jump exit206()
      } in
      if stop/4 {
        jump exit206()
      } else {
        jump k208(9)
      }
    } in
  } in
  jump loop207()
}

fn main() -> unit {
  join inline_cont272(t211: int32) -> unit {
    join inline_cont269(_wild184: unit) -> unit {
      join inline_cont263(t212: int32) -> unit {
        join inline_cont260(_wild185: unit) -> unit {
          let _wild186 = continue_branch() in
          let _wild187 = break_branch(false) in
          let t213 = break_branch(true) in
          ()
        } in
        let inline261 = trait_impl#ToString#int32#to_string(t212) in
        let inline262 = @runtime(core.string_println)(inline261) in
        jump inline_cont260(())
      } in
      let inline264 = true in
      join inline_join265(inline266: int32) -> unit {
        let inline268 = (inline266 + 1) in
        jump inline_cont263(inline268)
      } in
      if inline264 {
        jump inline_cont263(10)
      } else {
        jump inline_join265(20)
      }
    } in
    let inline270 = trait_impl#ToString#int32#to_string(t211) in
    let inline271 = @runtime(core.string_println)(inline270) in
    jump inline_cont269(())
  } in
  let inline273 = false in
  join inline_join274(inline275: int32) -> unit {
    let inline277 = (inline275 + 1) in
    jump inline_cont272(inline277)
  } in
  if inline273 {
    jump inline_cont272(10)
  } else {
    jump inline_join274(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t216 = @intrinsic(ref.new)(value/270) in
  t216
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t219 = @intrinsic(ref.get)(self/271) in
  t219
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t221 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t224 = (self/101 == other/102) in
  t224
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont278(t226: string) -> unit {
    let t227 = @runtime(core.string_println)(t226) in
    ()
  } in
  let inline279 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont278(inline279)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont280(t229: string) -> unit {
    let t230 = @runtime(core.string_println)(t229) in
    ()
  } in
  let inline281 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont280(inline281)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t233 = @runtime(core.int_to_string)(self/67) in
  t233
}

fn trait_impl#ToString#int32#to_string(self/70: int32) -> string {
  let t236 = @runtime(core.int32_to_string)(self/70) in
  t236
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t239 = @runtime(core.int_hash)(self/127) in
  t239
}