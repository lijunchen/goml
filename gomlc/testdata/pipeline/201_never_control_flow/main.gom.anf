fn choose(flag/0: bool) -> int32 {
  join k163(jp164: int32) -> int32 {
    let value/1 = jp164 in
    let t165 = (jp164 + 1) in
    t165
  } in
  if flag/0 {
    10
  } else {
    jump k163(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont227(count/2: Ref[int]) -> unit {
    join exit167() -> unit {
      ()
    } in
    joinrec {
      join loop168() -> unit {
        join inline_cont225(t169: int) -> unit {
          let t170 = (t169 < 2) in
          if t170 {
            join inline_cont223(t171: int) -> unit {
              let t172 = (t171 + 1) in
              join inline_cont221(_wild155: unit) -> unit {
                join inline_cont219(t176: int) -> unit {
                  join inline_cont216(t177: bool) -> unit {
                    join k173(jp174: int) -> unit {
                      let value/3 = jp174 in
                      let inline214 = trait_impl#ToString#int#to_string(jp174) in
                      let inline215 = @runtime(core.string_println)(inline214) in
                      jump loop168()
                    } in
                    if t177 {
                      jump loop168()
                    } else {
                      jump k173(7)
                    }
                  } in
                  let inline217 = 1 in
                  let inline218 = @intrinsic(eq.primitive)(t176, inline217) in
                  jump inline_cont216(inline218)
                } in
                let inline220 = @intrinsic(ref.get)(count/2) in
                jump inline_cont219(inline220)
              } in
              let inline222 = @intrinsic(ref.set)(count/2, t172) in
              jump inline_cont221(())
            } in
            let inline224 = @intrinsic(ref.get)(count/2) in
            jump inline_cont223(inline224)
          } else {
            jump exit167()
          }
        } in
        let inline226 = @intrinsic(ref.get)(count/2) in
        jump inline_cont225(inline226)
      } in
    } in
    jump loop168()
  } in
  let inline228 = 0 in
  let inline229 = @intrinsic(ref.new)(inline228) in
  jump inline_cont227(inline229)
}

fn break_branch(stop/4: bool) -> unit {
  join exit179() -> unit {
    ()
  } in
  joinrec {
    join loop180() -> unit {
      join k181(jp182: int) -> unit {
        let value/5 = jp182 in
        let inline231 = trait_impl#ToString#int#to_string(jp182) in
        let inline232 = @runtime(core.string_println)(inline231) in
        jump exit179()
      } in
      if stop/4 {
        jump exit179()
      } else {
        jump k181(9)
      }
    } in
  } in
  jump loop180()
}

fn main() -> unit {
  join inline_cont245(t184: int32) -> unit {
    join inline_cont242(_wild157: unit) -> unit {
      join inline_cont236(t185: int32) -> unit {
        join inline_cont233(_wild158: unit) -> unit {
          let _wild159 = continue_branch() in
          let _wild160 = break_branch(false) in
          let t186 = break_branch(true) in
          ()
        } in
        let inline234 = trait_impl#ToString#int32#to_string(t185) in
        let inline235 = @runtime(core.string_println)(inline234) in
        jump inline_cont233(())
      } in
      let inline237 = true in
      join inline_join238(inline239: int32) -> unit {
        let inline240 = inline239 in
        let inline241 = (inline239 + 1) in
        jump inline_cont236(inline241)
      } in
      if inline237 {
        jump inline_cont236(10)
      } else {
        jump inline_join238(20)
      }
    } in
    let inline243 = trait_impl#ToString#int32#to_string(t184) in
    let inline244 = @runtime(core.string_println)(inline243) in
    jump inline_cont242(())
  } in
  let inline246 = false in
  join inline_join247(inline248: int32) -> unit {
    let inline249 = inline248 in
    let inline250 = (inline248 + 1) in
    jump inline_cont245(inline250)
  } in
  if inline246 {
    jump inline_cont245(10)
  } else {
    jump inline_join247(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  let t189 = @intrinsic(ref.new)(value/207) in
  t189
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  let t192 = @intrinsic(ref.get)(self/208) in
  t192
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  let t194 = @intrinsic(ref.set)(self/209, value/210) in
  ()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  let t197 = @intrinsic(eq.primitive)(self/59, other/60) in
  t197
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont251(t199: string) -> unit {
    let t200 = @runtime(core.string_println)(t199) in
    ()
  } in
  let inline252 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont251(inline252)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont253(t202: string) -> unit {
    let t203 = @runtime(core.string_println)(t202) in
    ()
  } in
  let inline254 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont253(inline254)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  let t206 = @runtime(core.int_to_string)(self/40) in
  t206
}

fn trait_impl#ToString#int32#to_string(self/43: int32) -> string {
  let t209 = @runtime(core.int32_to_string)(self/43) in
  t209
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  let t212 = @runtime(core.int_hash)(self/85) in
  t212
}