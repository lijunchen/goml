fn choose(flag/0: bool) -> int32 {
  join k416(jp417: int32) -> int32 {
    let t418 = (jp417 + 1) in
    t418
  } in
  if flag/0 {
    10
  } else {
    jump k416(20)
  }
}

fn continue_branch() -> unit {
  join inline_cont477(count/2: Ref[int]) -> unit {
    join exit420() -> unit {
      ()
    } in
    joinrec {
      join loop421() -> unit {
        join inline_cont475(t422: int) -> unit {
          let t423 = (t422 < 2) in
          if t423 {
            join inline_cont473(t424: int) -> unit {
              let t425 = (t424 + 1) in
              join inline_cont471(_wild408: unit) -> unit {
                join inline_cont469(t429: int) -> unit {
                  let t430 = (t429 == 1) in
                  join k426(jp427: int) -> unit {
                    let inline467 = trait_impl#ToString#int#to_string(jp427) in
                    let inline468 = @runtime(core.string_println)(inline467) in
                    jump loop421()
                  } in
                  if t430 {
                    jump loop421()
                  } else {
                    jump k426(7)
                  }
                } in
                let inline470 = @intrinsic(ref.get)(count/2) in
                jump inline_cont469(inline470)
              } in
              let inline472 = @intrinsic(ref.set)(count/2, t425) in
              jump inline_cont471(())
            } in
            let inline474 = @intrinsic(ref.get)(count/2) in
            jump inline_cont473(inline474)
          } else {
            jump exit420()
          }
        } in
        let inline476 = @intrinsic(ref.get)(count/2) in
        jump inline_cont475(inline476)
      } in
    } in
    jump loop421()
  } in
  let inline478 = 0 in
  let inline479 = @intrinsic(ref.new)(inline478) in
  jump inline_cont477(inline479)
}

fn break_branch(stop/4: bool) -> unit {
  join exit432() -> unit {
    ()
  } in
  joinrec {
    join loop433() -> unit {
      join k434(jp435: int) -> unit {
        let inline481 = trait_impl#ToString#int#to_string(jp435) in
        let inline482 = @runtime(core.string_println)(inline481) in
        jump exit432()
      } in
      if stop/4 {
        jump exit432()
      } else {
        jump k434(9)
      }
    } in
  } in
  jump loop433()
}

fn main() -> unit {
  join inline_cont495(t437: int32) -> unit {
    join inline_cont492(_wild410: unit) -> unit {
      join inline_cont486(t438: int32) -> unit {
        join inline_cont483(_wild411: unit) -> unit {
          let _wild412 = continue_branch() in
          let _wild413 = break_branch(false) in
          let t439 = break_branch(true) in
          ()
        } in
        let inline484 = trait_impl#ToString#int32#to_string(t438) in
        let inline485 = @runtime(core.string_println)(inline484) in
        jump inline_cont483(())
      } in
      let inline487 = true in
      join inline_join488(inline489: int32) -> unit {
        let inline491 = (inline489 + 1) in
        jump inline_cont486(inline491)
      } in
      if inline487 {
        jump inline_cont486(10)
      } else {
        jump inline_join488(20)
      }
    } in
    let inline493 = trait_impl#ToString#int32#to_string(t437) in
    let inline494 = @runtime(core.string_println)(inline493) in
    jump inline_cont492(())
  } in
  let inline496 = false in
  join inline_join497(inline498: int32) -> unit {
    let inline500 = (inline498 + 1) in
    jump inline_cont495(inline500)
  } in
  if inline496 {
    jump inline_cont495(10)
  } else {
    jump inline_join497(20)
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t442 = @intrinsic(ref.new)(value/431) in
  t442
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t445 = @intrinsic(ref.get)(self/432) in
  t445
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t447 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont501(t449: string) -> unit {
    let t450 = @runtime(core.string_println)(t449) in
    ()
  } in
  let inline502 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont501(inline502)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont503(t452: string) -> unit {
    let t453 = @runtime(core.string_println)(t452) in
    ()
  } in
  let inline504 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont503(inline504)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t456 = @runtime(core.int_to_string)(self/151) in
  t456
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t459 = @runtime(core.int32_to_string)(self/154) in
  t459
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t462 = (self/185 == other/186) in
  t462
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t465 = @runtime(core.int_hash)(self/211) in
  t465
}