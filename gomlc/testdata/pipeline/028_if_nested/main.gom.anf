fn classify(x/0: int32) -> string {
  join ret74(retv75: string) -> string {
    retv75
  } in
  let t78 = (x/0 < 0) in
  join k76(jp77: string) -> string {
    jump ret74(jp77)
  } in
  if t78 {
    jump k76("negative")
  } else {
    let t81 = (0 < x/0) in
    join k79(jp80: string) -> string {
      jump k76(jp80)
    } in
    if t81 {
      jump k79("positive")
    } else {
      jump k79("zero")
    }
  }
}

fn triangle_type(a/1: int32, b/2: int32, c/3: int32) -> string {
  join ret82(retv83: string) -> string {
    retv83
  } in
  let t86 = (a/1 < b/2) in
  join k84(jp85: string) -> string {
    jump ret82(jp85)
  } in
  if t86 {
    let t89 = (b/2 < c/3) in
    join k87(jp88: string) -> string {
      jump k84(jp88)
    } in
    if t89 {
      jump k87("ascending")
    } else {
      jump k87("peak")
    }
  } else {
    let t92 = (a/1 < c/3) in
    join k90(jp91: string) -> string {
      jump k84(jp91)
    } in
    if t92 {
      jump k90("valley")
    } else {
      jump k90("flat")
    }
  }
}

fn main() -> unit {
  join ret93() -> unit {
    ()
  } in
  let first/4 = classify(-42) in
  let second/5 = classify(0) in
  let third/6 = classify(17) in
  let shape1/7 = triangle_type(1, 2, 3) in
  let shape2/8 = triangle_type(3, 2, 1) in
  let shape3/9 = triangle_type(2, 3, 2) in
  let mtmp68 = println__T_string(first/4) in
  let mtmp69 = println__T_string(second/5) in
  let mtmp70 = println__T_string(third/6) in
  let mtmp71 = println__T_string(shape1/7) in
  let mtmp72 = println__T_string(shape2/8) in
  let mtmp73 = println__T_string(shape3/9) in
  jump ret93()
}

fn println__T_string(value/1: string) -> unit {
  join ret94() -> unit {
    ()
  } in
  let t95 = trait_impl#ToString#string#to_string(value/1) in
  let t96 = @runtime(core.string_println)(t95) in
  jump ret94()
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret97(retv98: string) -> string {
    retv98
  } in
  jump ret97(self/38)
}