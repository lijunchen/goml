fn classify(x/0: i32) -> string {
  let t0 = (x/0 < 0) in
  if t0 {
    "negative"
  } else {
    let t1 = (0 < x/0) in
    if t1 {
      "positive"
    } else {
      "zero"
    }
  }
}

fn triangle_type(a/0: i32, b/0: i32, c/0: i32) -> string {
  let t0 = (a/0 < b/0) in
  if t0 {
    let t1 = (b/0 < c/0) in
    if t1 {
      "ascending"
    } else {
      "peak"
    }
  } else {
    let t2 = (a/0 < c/0) in
    if t2 {
      "valley"
    } else {
      "flat"
    }
  }
}

fn main() -> () {
  let first/0 = classify(-42) in
  let second/0 = classify(0) in
  let third/0 = classify(17) in
  join inline_cont0(shape1/0: string) -> () {
    join inline_cont1(shape2/0: string) -> () {
      join inline_cont2(shape3/0: string) -> () {
        join inline_cont3(mtmp0: ()) -> () {
          join inline_cont4(mtmp1: ()) -> () {
            join inline_cont5(mtmp2: ()) -> () {
              join inline_cont6(mtmp3: ()) -> () {
                join inline_cont7(mtmp4: ()) -> () {
                  let inline0 = trait_impl#ToString#string#to_string(shape3/0) in
                  let inline1 = @runtime(core.string_println)(inline0) in
                  ()
                } in
                let inline2 = trait_impl#ToString#string#to_string(shape2/0) in
                let inline3 = @runtime(core.string_println)(inline2) in
                jump inline_cont7(())
              } in
              let inline4 = trait_impl#ToString#string#to_string(shape1/0) in
              let inline5 = @runtime(core.string_println)(inline4) in
              jump inline_cont6(())
            } in
            let inline6 = trait_impl#ToString#string#to_string(third/0) in
            let inline7 = @runtime(core.string_println)(inline6) in
            jump inline_cont5(())
          } in
          let inline8 = trait_impl#ToString#string#to_string(second/0) in
          let inline9 = @runtime(core.string_println)(inline8) in
          jump inline_cont4(())
        } in
        let inline10 = trait_impl#ToString#string#to_string(first/0) in
        let inline11 = @runtime(core.string_println)(inline10) in
        jump inline_cont3(())
      } in
      let inline12 = 2 in
      let inline13 = 3 in
      let inline14 = 2 in
      let inline15 = (inline12 < inline13) in
      if inline15 {
        let inline16 = (inline13 < inline14) in
        if inline16 {
          jump inline_cont2("ascending")
        } else {
          jump inline_cont2("peak")
        }
      } else {
        let inline17 = (inline12 < inline14) in
        if inline17 {
          jump inline_cont2("valley")
        } else {
          jump inline_cont2("flat")
        }
      }
    } in
    let inline18 = 3 in
    let inline19 = 2 in
    let inline20 = 1 in
    let inline21 = (inline18 < inline19) in
    if inline21 {
      let inline22 = (inline19 < inline20) in
      if inline22 {
        jump inline_cont1("ascending")
      } else {
        jump inline_cont1("peak")
      }
    } else {
      let inline23 = (inline18 < inline20) in
      if inline23 {
        jump inline_cont1("valley")
      } else {
        jump inline_cont1("flat")
      }
    }
  } in
  let inline24 = 1 in
  let inline25 = 2 in
  let inline26 = 3 in
  let inline27 = (inline24 < inline25) in
  if inline27 {
    let inline28 = (inline25 < inline26) in
    if inline28 {
      jump inline_cont0("ascending")
    } else {
      jump inline_cont0("peak")
    }
  } else {
    let inline29 = (inline24 < inline26) in
    if inline29 {
      jump inline_cont0("valley")
    } else {
      jump inline_cont0("flat")
    }
  }
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}