fn classify(x/0: int32) -> string {
  let t421 = (x/0 < 0) in
  if t421 {
    "negative"
  } else {
    let t424 = (0 < x/0) in
    if t424 {
      "positive"
    } else {
      "zero"
    }
  }
}

fn triangle_type(a/1: int32, b/2: int32, c/3: int32) -> string {
  let t429 = (a/1 < b/2) in
  if t429 {
    let t432 = (b/2 < c/3) in
    if t432 {
      "ascending"
    } else {
      "peak"
    }
  } else {
    let t435 = (a/1 < c/3) in
    if t435 {
      "valley"
    } else {
      "flat"
    }
  }
}

fn main() -> unit {
  let first/4 = classify(-42) in
  let second/5 = classify(0) in
  let third/6 = classify(17) in
  join inline_cont474(shape1/7: string) -> unit {
    join inline_cont467(shape2/8: string) -> unit {
      join inline_cont460(shape3/9: string) -> unit {
        join inline_cont457(mtmp411: unit) -> unit {
          join inline_cont454(mtmp412: unit) -> unit {
            join inline_cont451(mtmp413: unit) -> unit {
              join inline_cont448(mtmp414: unit) -> unit {
                join inline_cont445(mtmp415: unit) -> unit {
                  let inline443 = trait_impl#ToString#string#to_string(shape3/9) in
                  let inline444 = @runtime(core.string_println)(inline443) in
                  ()
                } in
                let inline446 = trait_impl#ToString#string#to_string(shape2/8) in
                let inline447 = @runtime(core.string_println)(inline446) in
                jump inline_cont445(())
              } in
              let inline449 = trait_impl#ToString#string#to_string(shape1/7) in
              let inline450 = @runtime(core.string_println)(inline449) in
              jump inline_cont448(())
            } in
            let inline452 = trait_impl#ToString#string#to_string(third/6) in
            let inline453 = @runtime(core.string_println)(inline452) in
            jump inline_cont451(())
          } in
          let inline455 = trait_impl#ToString#string#to_string(second/5) in
          let inline456 = @runtime(core.string_println)(inline455) in
          jump inline_cont454(())
        } in
        let inline458 = trait_impl#ToString#string#to_string(first/4) in
        let inline459 = @runtime(core.string_println)(inline458) in
        jump inline_cont457(())
      } in
      let inline461 = 2 in
      let inline462 = 3 in
      let inline463 = 2 in
      let inline464 = (inline461 < inline462) in
      if inline464 {
        let inline465 = (inline462 < inline463) in
        if inline465 {
          jump inline_cont460("ascending")
        } else {
          jump inline_cont460("peak")
        }
      } else {
        let inline466 = (inline461 < inline463) in
        if inline466 {
          jump inline_cont460("valley")
        } else {
          jump inline_cont460("flat")
        }
      }
    } in
    let inline468 = 3 in
    let inline469 = 2 in
    let inline470 = 1 in
    let inline471 = (inline468 < inline469) in
    if inline471 {
      let inline472 = (inline469 < inline470) in
      if inline472 {
        jump inline_cont467("ascending")
      } else {
        jump inline_cont467("peak")
      }
    } else {
      let inline473 = (inline468 < inline470) in
      if inline473 {
        jump inline_cont467("valley")
      } else {
        jump inline_cont467("flat")
      }
    }
  } in
  let inline475 = 1 in
  let inline476 = 2 in
  let inline477 = 3 in
  let inline478 = (inline475 < inline476) in
  if inline478 {
    let inline479 = (inline476 < inline477) in
    if inline479 {
      jump inline_cont474("ascending")
    } else {
      jump inline_cont474("peak")
    }
  } else {
    let inline480 = (inline475 < inline477) in
    if inline480 {
      jump inline_cont474("valley")
    } else {
      jump inline_cont474("flat")
    }
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont481(t438: string) -> unit {
    let t439 = @runtime(core.string_println)(t438) in
    ()
  } in
  jump inline_cont481(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}