fn classify(x/0: i32) -> string {
  let t806 = (x/0 < 0) in
  if t806 {
    "negative"
  } else {
    let t809 = (0 < x/0) in
    if t809 {
      "positive"
    } else {
      "zero"
    }
  }
}

fn triangle_type(a/1: i32, b/2: i32, c/3: i32) -> string {
  let t814 = (a/1 < b/2) in
  if t814 {
    let t817 = (b/2 < c/3) in
    if t817 {
      "ascending"
    } else {
      "peak"
    }
  } else {
    let t820 = (a/1 < c/3) in
    if t820 {
      "valley"
    } else {
      "flat"
    }
  }
}

fn main() -> unit {
  let first/4 = classify(-42) in
  let second/5 = classify(0) in
  let third/6 = classify(17) in
  join inline_cont859(shape1/7: string) -> unit {
    join inline_cont852(shape2/8: string) -> unit {
      join inline_cont845(shape3/9: string) -> unit {
        join inline_cont842(mtmp796: unit) -> unit {
          join inline_cont839(mtmp797: unit) -> unit {
            join inline_cont836(mtmp798: unit) -> unit {
              join inline_cont833(mtmp799: unit) -> unit {
                join inline_cont830(mtmp800: unit) -> unit {
                  let inline828 = trait_impl#ToString#string#to_string(shape3/9) in
                  let inline829 = @runtime(core.string_println)(inline828) in
                  ()
                } in
                let inline831 = trait_impl#ToString#string#to_string(shape2/8) in
                let inline832 = @runtime(core.string_println)(inline831) in
                jump inline_cont830(())
              } in
              let inline834 = trait_impl#ToString#string#to_string(shape1/7) in
              let inline835 = @runtime(core.string_println)(inline834) in
              jump inline_cont833(())
            } in
            let inline837 = trait_impl#ToString#string#to_string(third/6) in
            let inline838 = @runtime(core.string_println)(inline837) in
            jump inline_cont836(())
          } in
          let inline840 = trait_impl#ToString#string#to_string(second/5) in
          let inline841 = @runtime(core.string_println)(inline840) in
          jump inline_cont839(())
        } in
        let inline843 = trait_impl#ToString#string#to_string(first/4) in
        let inline844 = @runtime(core.string_println)(inline843) in
        jump inline_cont842(())
      } in
      let inline846 = 2 in
      let inline847 = 3 in
      let inline848 = 2 in
      let inline849 = (inline846 < inline847) in
      if inline849 {
        let inline850 = (inline847 < inline848) in
        if inline850 {
          jump inline_cont845("ascending")
        } else {
          jump inline_cont845("peak")
        }
      } else {
        let inline851 = (inline846 < inline848) in
        if inline851 {
          jump inline_cont845("valley")
        } else {
          jump inline_cont845("flat")
        }
      }
    } in
    let inline853 = 3 in
    let inline854 = 2 in
    let inline855 = 1 in
    let inline856 = (inline853 < inline854) in
    if inline856 {
      let inline857 = (inline854 < inline855) in
      if inline857 {
        jump inline_cont852("ascending")
      } else {
        jump inline_cont852("peak")
      }
    } else {
      let inline858 = (inline853 < inline855) in
      if inline858 {
        jump inline_cont852("valley")
      } else {
        jump inline_cont852("flat")
      }
    }
  } in
  let inline860 = 1 in
  let inline861 = 2 in
  let inline862 = 3 in
  let inline863 = (inline860 < inline861) in
  if inline863 {
    let inline864 = (inline861 < inline862) in
    if inline864 {
      jump inline_cont859("ascending")
    } else {
      jump inline_cont859("peak")
    }
  } else {
    let inline865 = (inline860 < inline862) in
    if inline865 {
      jump inline_cont859("valley")
    } else {
      jump inline_cont859("flat")
    }
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont866(t823: string) -> unit {
    let t824 = @runtime(core.string_println)(t823) in
    ()
  } in
  jump inline_cont866(value/1)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}