fn child(signal/0: Ref[i32]) -> () {
  let inline0 = 1 in
  let inline1 = @intrinsic(ref.set)(signal/0, inline0) in
  ()
}

fn main() -> () {
  join inline_cont0(signal/0: Ref[i32]) -> () {
    let t0 = closure_env_main_0 { signal_0: signal/0 } in
    let t1 = pack_closure[inherent#closure_env_main_0#closure_env_main_0#apply](t0) in
    let _wild0 = go t1 in
    join exit0() -> () {
      let inline0 = "main" in
      let inline1 = trait_impl#ToString#string#to_string(inline0) in
      let inline2 = @runtime(core.string_println)(inline1) in
      ()
    } in
    joinrec {
      join loop0() -> () {
        join inline_cont1(t2: i32) -> () {
          let t3 = (t2 < 1) in
          if t3 {
            jump loop0()
          } else {
            jump exit0()
          }
        } in
        let inline3 = @intrinsic(ref.get)(signal/0) in
        jump inline_cont1(inline3)
      } in
    } in
    jump loop0()
  } in
  let inline4 = 0 in
  let inline5 = @intrinsic(ref.new)(inline4) in
  jump inline_cont0(inline5)
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> () {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn inherent#closure_env_main_0#closure_env_main_0#apply(env0: closure_env_main_0) -> () {
  let signal/0 = closure_env_main_0.signal_0(env0) in
  let inline0 = inherent#Ref#Ref[T]#set__T_i0(signal/0, 1) in
  ()
}