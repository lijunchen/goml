const ANSWER: isize = 120;

fn factorial(value/0: isize) -> isize {
  let t814 = (value/0 < 2) in
  if t814 {
    1
  } else {
    let t815 = (value/0 - 1) in
    let t816 = factorial(t815) in
    let t817 = (value/0 * t816) in
    t817
  }
}

fn accumulate(limit/1: isize) -> isize {
  let index/2 = 0 in
  let total/3 = 0 in
  join exit820() -> isize {
    total/3
  } in
  joinrec {
    join loop821() -> isize {
      let t822 = (index/2 < limit/1) in
      if t822 {
        let t823 = (total/3 + index/2) in
        let _wild796 = total/3 = t823 in
        let t824 = (index/2 + 1) in
        let _wild797 = index/2 = t824 in
        jump loop821()
      } else {
        jump exit820()
      }
    } in
  } in
  jump loop821()
}

fn make_pair(value/4: isize) -> Pair {
  let t827 = (value/4 + 1) in
  let t828 = Pair { left: value/4, right: t827 } in
  t828
}

fn choose(value/5: isize) -> Choice {
  let t833 = (value/5 > 0) in
  if t833 {
    let t834 = Choice::Value(value/5) in
    t834
  } else {
    Tag_1
  }
}

fn nonnegative(value/6: isize) -> isize {
  let t839 = (value/6 < 0) in
  join k837(jp838: isize) -> isize {
    jp838
  } in
  if t839 {
    let t840 = @intrinsic(compiler.missing)("value must be non-negative") in
    jump k837(t840)
  } else {
    jump k837(value/6)
  }
}

fn shadow(value/7: isize) -> isize {
  let base/9 = (value/7 + 1) in
  let t843 = (base/9 * 2) in
  let t844 = (base/9 + t843) in
  t844
}

fn widen_negative(value/11: i8) -> u64 {
  let t847 = (value/11 as u64) in
  t847
}

fn main() -> unit {
  let values/12 = [6, 10, 4] in
  join inline_cont961(_wild799: unit) -> unit {
    let t849 = @intrinsic(array.get)(values/12, 0) in
    join inline_cont958(_wild800: unit) -> unit {
      let t850 = @intrinsic(array.get)(values/12, 1) in
      join inline_cont955(_wild801: unit) -> unit {
        let t851 = @intrinsic(array.get)(values/12, 2) in
        join inline_cont952(_wild802: unit) -> unit {
          let t852 = 7 in
          let t853 = 8 in
          let t854 = (t852 + t853) in
          join inline_cont949(_wild803: unit) -> unit {
            join k855() -> unit {
              let t856 = factorial(5) in
              let t857 = (t856 == ANSWER) in
              join inline_cont946(_wild806: unit) -> unit {
                let shadowed/16 = 12 in
                join inline_cont943(_wild807: unit) -> unit {
                  join inline_cont936(t858: isize) -> unit {
                    let t859 = (t858 == shadowed/16) in
                    join inline_cont933(_wild808: unit) -> unit {
                      let widened/17 = 18446744073709551615 in
                      join inline_cont930(t860: u64) -> unit {
                        let t861 = (widened/17 == t860) in
                        join inline_cont927(_wild809: unit) -> unit {
                          let t862 = factorial(4) in
                          let inline925 = trait_impl#ToString#isize#to_string(t862) in
                          let inline926 = @runtime(core.string_println)(inline925) in
                          ()
                        } in
                        let inline928 = trait_impl#ToString#bool#to_string(t861) in
                        let inline929 = @runtime(core.string_println)(inline928) in
                        jump inline_cont927(())
                      } in
                      let inline931 = -1 in
                      let inline932 = (inline931 as u64) in
                      jump inline_cont930(inline932)
                    } in
                    let inline934 = trait_impl#ToString#bool#to_string(t859) in
                    let inline935 = @runtime(core.string_println)(inline934) in
                    jump inline_cont933(())
                  } in
                  let inline937 = 3 in
                  let inline939 = (inline937 + 1) in
                  let inline940 = (inline939 * 2) in
                  let inline942 = (inline939 + inline940) in
                  jump inline_cont936(inline942)
                } in
                let inline944 = trait_impl#ToString#isize#to_string(shadowed/16) in
                let inline945 = @runtime(core.string_println)(inline944) in
                jump inline_cont943(())
              } in
              let inline947 = trait_impl#ToString#bool#to_string(t857) in
              let inline948 = @runtime(core.string_println)(inline947) in
              jump inline_cont946(())
            } in
            let x804 = 9 in
            let inline922 = trait_impl#ToString#isize#to_string(x804) in
            let inline923 = @runtime(core.string_println)(inline922) in
            jump k855()
          } in
          let inline950 = trait_impl#ToString#isize#to_string(t854) in
          let inline951 = @runtime(core.string_println)(inline950) in
          jump inline_cont949(())
        } in
        let inline953 = trait_impl#ToString#isize#to_string(t851) in
        let inline954 = @runtime(core.string_println)(inline953) in
        jump inline_cont952(())
      } in
      let inline956 = trait_impl#ToString#isize#to_string(t850) in
      let inline957 = @runtime(core.string_println)(inline956) in
      jump inline_cont955(())
    } in
    let inline959 = trait_impl#ToString#isize#to_string(t849) in
    let inline960 = @runtime(core.string_println)(inline959) in
    jump inline_cont958(())
  } in
  let inline962 = trait_impl#ToString#isize#to_string(ANSWER) in
  let inline963 = @runtime(core.string_println)(inline962) in
  jump inline_cont961(())
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont964(t867: string) -> unit {
    let t868 = @runtime(core.string_println)(t867) in
    ()
  } in
  let inline965 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont964(inline965)
}

fn println__T_bool(value/1: bool) -> unit {
  join inline_cont966(t870: string) -> unit {
    let t871 = @runtime(core.string_println)(t870) in
    ()
  } in
  let inline967 = @runtime(core.bool_to_string)(value/1) in
  jump inline_cont966(inline967)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline969 = (self/404 as i64) in
  let inline970 = signed_decimal_string(inline969) in
  inline970
}

fn trait_impl#ToString#bool#to_string(self/401: bool) -> string {
  let t877 = @runtime(core.bool_to_string)(self/401) in
  t877
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t880 = (value/222 as i64) in
  let inline972 = (t880 < 0) in
  if inline972 {
    let inline973 = (t880 as u64) in
    let inline974 = (0 - inline973) in
    let inline975 = decimal_string(inline974) in
    let inline976 = ("-" + inline975) in
    inline976
  } else {
    let inline977 = (t880 as u64) in
    let inline978 = decimal_string(inline977) in
    inline978
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t886 = (value/214 < 0) in
  if t886 {
    let t887 = (value/214 as u64) in
    let t888 = (0 - t887) in
    let t889 = decimal_string(t888) in
    let t890 = ("-" + t889) in
    t890
  } else {
    let t891 = (value/214 as u64) in
    let t892 = decimal_string(t891) in
    t892
  }
}

fn decimal_string(value/208: u64) -> string {
  let t915 = (value/208 == 0) in
  join k895() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit896() -> string {
      join inline_cont987(t897: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t897) in
        let offset/212 = 0 in
        join exit898() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop899() -> string {
            join inline_cont985(t900: isize) -> string {
              let t901 = (offset/212 < t900) in
              if t901 {
                join inline_cont983(t902: isize) -> string {
                  let t903 = (t902 - offset/212) in
                  let t904 = (t903 - 1) in
                  let t905 = @intrinsic(vec.get)(reversed/209, t904) in
                  join inline_cont981(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t906 = (compound_old358 + compound_value359) in
                    let t907 = offset/212 = t906 in
                    jump loop899()
                  } in
                  let inline982 = @intrinsic(vec.push)(bytes/211, t905) in
                  jump inline_cont981(())
                } in
                let inline984 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont983(inline984)
              } else {
                jump exit898()
              }
            } in
            let inline986 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont985(inline986)
          } in
        } in
        jump loop899()
      } in
      let inline988 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont987(inline988)
    } in
    joinrec {
      join loop908() -> string {
        let t909 = (remaining/210 > 0) in
        if t909 {
          let t910 = (remaining/210 % 10) in
          let t911 = (t910 as u8) in
          let t912 = (t911 + 48) in
          join inline_cont979(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t913 = (compound_old353 / compound_value354) in
            let t914 = remaining/210 = t913 in
            jump loop908()
          } in
          let inline980 = @intrinsic(vec.push)(reversed/209, t912) in
          jump inline_cont979(())
        } else {
          jump exit896()
        }
      } in
    } in
    jump loop908()
  } in
  if t915 {
    "0"
  } else {
    jump k895()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t917 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t920 = @intrinsic(vec.len)(self/526) in
  t920
}