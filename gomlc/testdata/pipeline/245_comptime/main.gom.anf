const ANSWER: int = 120;

fn factorial(value/0: int) -> int {
  let t429 = (value/0 < 2) in
  if t429 {
    1
  } else {
    let t430 = (value/0 - 1) in
    let t431 = factorial(t430) in
    let t432 = (value/0 * t431) in
    t432
  }
}

fn accumulate(limit/1: int) -> int {
  let index/2 = 0 in
  let total/3 = 0 in
  join exit435() -> int {
    total/3
  } in
  joinrec {
    join loop436() -> int {
      let t437 = (index/2 < limit/1) in
      if t437 {
        let t438 = (total/3 + index/2) in
        let _wild411 = total/3 = t438 in
        let t439 = (index/2 + 1) in
        let _wild412 = index/2 = t439 in
        jump loop436()
      } else {
        jump exit435()
      }
    } in
  } in
  jump loop436()
}

fn make_pair(value/4: int) -> Pair {
  let t442 = (value/4 + 1) in
  let t443 = Pair { left: value/4, right: t442 } in
  t443
}

fn choose(value/5: int) -> Choice {
  let t448 = (value/5 > 0) in
  if t448 {
    let t449 = Choice::Value(value/5) in
    t449
  } else {
    Tag_1
  }
}

fn nonnegative(value/6: int) -> int {
  let t454 = (value/6 < 0) in
  join k452(jp453: int) -> int {
    jp453
  } in
  if t454 {
    let t455 = @intrinsic(compiler.missing)("value must be non-negative") in
    jump k452(t455)
  } else {
    jump k452(value/6)
  }
}

fn shadow(value/7: int) -> int {
  let base/9 = (value/7 + 1) in
  let t458 = (base/9 * 2) in
  let t459 = (base/9 + t458) in
  t459
}

fn widen_negative(value/11: int8) -> uint64 {
  let t462 = (value/11 as uint64) in
  t462
}

fn main() -> unit {
  let values/12 = [6, 10, 4] in
  join inline_cont533(_wild414: unit) -> unit {
    let t464 = @intrinsic(array.get)(values/12, 0) in
    join inline_cont530(_wild415: unit) -> unit {
      let t465 = @intrinsic(array.get)(values/12, 1) in
      join inline_cont527(_wild416: unit) -> unit {
        let t466 = @intrinsic(array.get)(values/12, 2) in
        join inline_cont524(_wild417: unit) -> unit {
          let t467 = 7 in
          let t468 = 8 in
          let t469 = (t467 + t468) in
          join inline_cont521(_wild418: unit) -> unit {
            join k470() -> unit {
              let t471 = factorial(5) in
              let t472 = (t471 == ANSWER) in
              join inline_cont518(_wild421: unit) -> unit {
                let shadowed/16 = 12 in
                join inline_cont515(_wild422: unit) -> unit {
                  join inline_cont508(t473: int) -> unit {
                    let t474 = (t473 == shadowed/16) in
                    join inline_cont505(_wild423: unit) -> unit {
                      let widened/17 = 18446744073709551615 in
                      join inline_cont502(t475: uint64) -> unit {
                        let t476 = (widened/17 == t475) in
                        join inline_cont499(_wild424: unit) -> unit {
                          let t477 = factorial(4) in
                          let inline497 = trait_impl#ToString#int#to_string(t477) in
                          let inline498 = @runtime(core.string_println)(inline497) in
                          ()
                        } in
                        let inline500 = trait_impl#ToString#bool#to_string(t476) in
                        let inline501 = @runtime(core.string_println)(inline500) in
                        jump inline_cont499(())
                      } in
                      let inline503 = -1 in
                      let inline504 = (inline503 as uint64) in
                      jump inline_cont502(inline504)
                    } in
                    let inline506 = trait_impl#ToString#bool#to_string(t474) in
                    let inline507 = @runtime(core.string_println)(inline506) in
                    jump inline_cont505(())
                  } in
                  let inline509 = 3 in
                  let inline511 = (inline509 + 1) in
                  let inline512 = (inline511 * 2) in
                  let inline514 = (inline511 + inline512) in
                  jump inline_cont508(inline514)
                } in
                let inline516 = trait_impl#ToString#int#to_string(shadowed/16) in
                let inline517 = @runtime(core.string_println)(inline516) in
                jump inline_cont515(())
              } in
              let inline519 = trait_impl#ToString#bool#to_string(t472) in
              let inline520 = @runtime(core.string_println)(inline519) in
              jump inline_cont518(())
            } in
            let x419 = 9 in
            let inline494 = trait_impl#ToString#int#to_string(x419) in
            let inline495 = @runtime(core.string_println)(inline494) in
            jump k470()
          } in
          let inline522 = trait_impl#ToString#int#to_string(t469) in
          let inline523 = @runtime(core.string_println)(inline522) in
          jump inline_cont521(())
        } in
        let inline525 = trait_impl#ToString#int#to_string(t466) in
        let inline526 = @runtime(core.string_println)(inline525) in
        jump inline_cont524(())
      } in
      let inline528 = trait_impl#ToString#int#to_string(t465) in
      let inline529 = @runtime(core.string_println)(inline528) in
      jump inline_cont527(())
    } in
    let inline531 = trait_impl#ToString#int#to_string(t464) in
    let inline532 = @runtime(core.string_println)(inline531) in
    jump inline_cont530(())
  } in
  let inline534 = trait_impl#ToString#int#to_string(ANSWER) in
  let inline535 = @runtime(core.string_println)(inline534) in
  jump inline_cont533(())
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont536(t482: string) -> unit {
    let t483 = @runtime(core.string_println)(t482) in
    ()
  } in
  let inline537 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont536(inline537)
}

fn println__T_bool(value/1: bool) -> unit {
  join inline_cont538(t485: string) -> unit {
    let t486 = @runtime(core.string_println)(t485) in
    ()
  } in
  let inline539 = @runtime(core.bool_to_string)(value/1) in
  jump inline_cont538(inline539)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t489 = @runtime(core.int_to_string)(self/151) in
  t489
}

fn trait_impl#ToString#bool#to_string(self/148: bool) -> string {
  let t492 = @runtime(core.bool_to_string)(self/148) in
  t492
}