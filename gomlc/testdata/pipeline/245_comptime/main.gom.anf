const ANSWER: isize = 120;

fn factorial(value/0: isize) -> isize {
  let t0 = (value/0 < 2) in
  if t0 {
    1
  } else {
    let t1 = (value/0 - 1) in
    let t2 = factorial(t1) in
    let t3 = (value/0 * t2) in
    t3
  }
}

fn accumulate(limit/0: isize) -> isize {
  let index/0 = 0 in
  let total/0 = 0 in
  join exit0() -> isize {
    total/0
  } in
  joinrec {
    join loop0() -> isize {
      let t0 = (index/0 < limit/0) in
      if t0 {
        let t1 = (total/0 + index/0) in
        let _wild0 = total/0 = t1 in
        let t2 = (index/0 + 1) in
        let _wild1 = index/0 = t2 in
        jump loop0()
      } else {
        jump exit0()
      }
    } in
  } in
  jump loop0()
}

fn make_pair(value/0: isize) -> Pair {
  let t0 = (value/0 + 1) in
  let t1 = Pair { left: value/0, right: t0 } in
  t1
}

fn choose(value/0: isize) -> Choice {
  let t0 = (value/0 > 0) in
  if t0 {
    let t1 = Choice::Value(value/0) in
    t1
  } else {
    Tag_1
  }
}

fn nonnegative(value/0: isize) -> isize {
  let t0 = (value/0 < 0) in
  join k0(jp0: isize) -> isize {
    jp0
  } in
  if t0 {
    let t1 = @intrinsic(compiler.missing)("value must be non-negative") in
    jump k0(t1)
  } else {
    jump k0(value/0)
  }
}

fn shadow(value/0: isize) -> isize {
  let base/0 = (value/0 + 1) in
  let t0 = (base/0 * 2) in
  let t1 = (base/0 + t0) in
  t1
}

fn widen_negative(value/0: i8) -> u64 {
  let t0 = (value/0 as u64) in
  t0
}

fn guarded_match() -> isize {
  let value/0 = 0 in
  let _wild0 = value/0 = 1 in
  value/0
}

fn main() -> unit {
  let values/0 = [6, 10, 4] in
  let _wild0 = println__T_isize(ANSWER) in
  let t0 = @intrinsic(array.get)(values/0, 0) in
  join inline_cont0(_wild1: unit) -> unit {
    let t1 = @intrinsic(array.get)(values/0, 1) in
    join inline_cont1(_wild2: unit) -> unit {
      let t2 = @intrinsic(array.get)(values/0, 2) in
      join inline_cont2(_wild3: unit) -> unit {
        let t3 = 7 in
        let t4 = 8 in
        let t5 = (t3 + t4) in
        join inline_cont3(_wild4: unit) -> unit {
          join k0() -> unit {
            let t6 = factorial(5) in
            let t7 = (t6 == ANSWER) in
            join inline_cont4(_wild5: unit) -> unit {
              let shadowed/0 = 12 in
              join inline_cont5(_wild6: unit) -> unit {
                join inline_cont6(t8: isize) -> unit {
                  let t9 = (t8 == shadowed/0) in
                  join inline_cont7(_wild7: unit) -> unit {
                    let widened/0 = 18446744073709551615 in
                    join inline_cont8(t10: u64) -> unit {
                      let t11 = (widened/0 == t10) in
                      join inline_cont9(_wild8: unit) -> unit {
                        let folded_guard/0 = 1 in
                        join inline_cont10(t12: isize) -> unit {
                          join inline_cont11(_wild9: unit) -> unit {
                            join inline_cont12(_wild10: unit) -> unit {
                              let t13 = factorial(4) in
                              let inline0 = trait_impl#ToString#isize#to_string(t13) in
                              let inline1 = @runtime(core.string_println)(inline0) in
                              ()
                            } in
                            let inline2 = trait_impl#ToString#isize#to_string(folded_guard/0) in
                            let inline3 = @runtime(core.string_println)(inline2) in
                            jump inline_cont12(())
                          } in
                          let inline4 = trait_impl#ToString#isize#to_string(t12) in
                          let inline5 = @runtime(core.string_println)(inline4) in
                          jump inline_cont11(())
                        } in
                        let inline6 = 0 in
                        let inline7 = inline6 = 1 in
                        jump inline_cont10(inline6)
                      } in
                      let inline8 = trait_impl#ToString#bool#to_string(t11) in
                      let inline9 = @runtime(core.string_println)(inline8) in
                      jump inline_cont9(())
                    } in
                    let inline10 = -1 in
                    let inline11 = (inline10 as u64) in
                    jump inline_cont8(inline11)
                  } in
                  let inline12 = trait_impl#ToString#bool#to_string(t9) in
                  let inline13 = @runtime(core.string_println)(inline12) in
                  jump inline_cont7(())
                } in
                let inline14 = 3 in
                let inline15 = (inline14 + 1) in
                let inline16 = (inline15 * 2) in
                let inline17 = (inline15 + inline16) in
                jump inline_cont6(inline17)
              } in
              let inline18 = trait_impl#ToString#isize#to_string(shadowed/0) in
              let inline19 = @runtime(core.string_println)(inline18) in
              jump inline_cont5(())
            } in
            let inline20 = trait_impl#ToString#bool#to_string(t7) in
            let inline21 = @runtime(core.string_println)(inline20) in
            jump inline_cont4(())
          } in
          let x0 = 9 in
          let inline22 = trait_impl#ToString#isize#to_string(x0) in
          let inline23 = @runtime(core.string_println)(inline22) in
          jump k0()
        } in
        let inline24 = trait_impl#ToString#isize#to_string(t5) in
        let inline25 = @runtime(core.string_println)(inline24) in
        jump inline_cont3(())
      } in
      let inline26 = trait_impl#ToString#isize#to_string(t2) in
      let inline27 = @runtime(core.string_println)(inline26) in
      jump inline_cont2(())
    } in
    let inline28 = trait_impl#ToString#isize#to_string(t1) in
    let inline29 = @runtime(core.string_println)(inline28) in
    jump inline_cont1(())
  } in
  let inline30 = trait_impl#ToString#isize#to_string(t0) in
  let inline31 = @runtime(core.string_println)(inline30) in
  jump inline_cont0(())
}

fn println__T_isize(value/0: isize) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn println__T_bool(value/0: bool) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}