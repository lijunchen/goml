const ANSWER: isize = 120;

fn factorial(value/0: isize) -> isize {
  if (value/0 < 2) {
      1
  } else {
      (value/0 * factorial((value/0 - 1)))
  }
}

fn accumulate(limit/0: isize) -> isize {
  let index/0 = 0 in
  let total/0 = 0 in
  let _wild0 =
    while (index/0 < limit/0) {
        let _wild1 = total/0 = (total/0 + index/0) in
        let _wild2 = index/0 = (index/0 + 1) in
        ()
    } in
  total/0
}

fn make_pair(value/0: isize) -> Pair {
  Pair { left: value/0, right: (value/0 + 1) }
}

fn choose(value/0: isize) -> Choice {
  if (value/0 > 0) {
      Choice::Value(value/0)
  } else {
      Choice::Empty
  }
}

fn nonnegative(value/0: isize) -> isize {
  if (value/0 < 0) {
      @intrinsic(compiler.missing)("value must be non-negative")
  } else {
      value/0
  }
}

fn shadow(value/0: isize) -> isize {
  let base/0 = value/0 in
  let nested/0 =
    let base/1 = (base/0 + 1) in
    (base/1 * 2) in
  (base/1 + nested/0)
}

fn widen_negative(value/0: i8) -> u64 {
  (value/0 as u64)
}

fn guarded_match() -> isize {
  let value/0 = 0 in
  if let _wild0 = value/0 = 1 in
  false {
      99
  } else {
      value/0
  }
}

fn main() -> () {
  let values/0 = [6, 10, 4] in
  let pair/0 = Pair { left: 7, right: 8 } in
  let choice/0 = Choice::Value(9) in
  let _wild0 = println__T_isize(ANSWER) in
  let _wild1 = println__T_isize(@intrinsic(array.get)(values/0, 0)) in
  let _wild2 = println__T_isize(@intrinsic(array.get)(values/0, 1)) in
  let _wild3 = println__T_isize(@intrinsic(array.get)(values/0, 2)) in
  let _wild4 = println__T_isize((Pair.left(pair/0) + Pair.right(pair/0))) in
  let _wild5 =
    match choice/0 {
      Choice::Value(x0) => {
        let x0 = Choice::Value._0(choice/0) in
        let value/0 = x0 in
        println__T_isize(value/0)
      },
      Choice::Empty => {
        println__T_isize(0)
      },
    } in
  let _wild6 = println__T_bool((factorial(5) == ANSWER)) in
  let shadowed/0 = 12 in
  let _wild7 = println__T_isize(shadowed/0) in
  let _wild8 = println__T_bool((shadow(3) == shadowed/0)) in
  let widened/0 = 18446744073709551615 in
  let _wild9 = println__T_bool((widened/0 == widen_negative(-1))) in
  let folded_guard/0 = 1 in
  let _wild10 = println__T_isize(guarded_match()) in
  let _wild11 = println__T_isize(folded_guard/0) in
  println__T_isize(factorial(4))
}

fn println__T_isize(value/0: isize) -> () {
  @runtime(core.string_println)(trait_impl#ToString#isize#to_string(value/0))
}

fn println__T_bool(value/0: bool) -> () {
  @runtime(core.string_println)(trait_impl#ToString#bool#to_string(value/0))
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  __goml_builtin_int_to_string(self/0)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  @runtime(core.bool_to_string)(self/0)
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  signed_decimal_string((value/0 as i64))
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}