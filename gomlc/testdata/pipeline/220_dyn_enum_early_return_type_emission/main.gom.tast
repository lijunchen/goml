impl Display for i32 {
  fn show(self/0: i32) -> string {
      (i32::to_string : (i32) -> string)((self/0 : i32))
  }
}

fn build() -> i32 {
    let _ : Boxed = Boxed::One(return 9);
    0
}

fn main() -> unit {
    (println : (string) -> unit)((i32::to_string : (i32) -> string)((build : () -> i32)()))
}