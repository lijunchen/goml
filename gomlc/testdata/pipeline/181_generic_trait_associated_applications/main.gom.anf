fn trait_impl#Convert#[i32]@Value#convert(self/0: Value) -> string {
  "isize"
}

fn trait_impl#Convert#[string]@Value#convert(self/1: Value) -> i32 {
  7
}

fn main() -> unit {
  join inline_cont866(text/2: string) -> unit {
    join inline_cont865(number/3: i32) -> unit {
      join inline_cont862(_wild796: unit) -> unit {
        let inline860 = trait_impl#ToString#i32#to_string(number/3) in
        let inline861 = @runtime(core.string_println)(inline860) in
        ()
      } in
      let inline863 = trait_impl#ToString#string#to_string(text/2) in
      let inline864 = @runtime(core.string_println)(inline863) in
      jump inline_cont862(())
    } in
    jump inline_cont865(7)
  } in
  jump inline_cont866("isize")
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont867(t806: string) -> unit {
    let t807 = @runtime(core.string_println)(t806) in
    ()
  } in
  jump inline_cont867(value/1)
}

fn println__T_i32(value/1: i32) -> unit {
  join inline_cont868(t809: string) -> unit {
    let t810 = @runtime(core.string_println)(t809) in
    ()
  } in
  let inline869 = __goml_builtin_int32_to_string(value/1) in
  jump inline_cont868(inline869)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn trait_impl#ToString#i32#to_string(self/407: i32) -> string {
  let inline871 = (self/407 as i64) in
  let inline872 = signed_decimal_string(inline871) in
  inline872
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t818 = (value/225 as i64) in
  let inline874 = (t818 < 0) in
  if inline874 {
    let inline875 = (t818 as u64) in
    let inline876 = (0 - inline875) in
    let inline877 = decimal_string(inline876) in
    let inline878 = ("-" + inline877) in
    inline878
  } else {
    let inline879 = (t818 as u64) in
    let inline880 = decimal_string(inline879) in
    inline880
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t824 = (value/214 < 0) in
  if t824 {
    let t825 = (value/214 as u64) in
    let t826 = (0 - t825) in
    let t827 = decimal_string(t826) in
    let t828 = ("-" + t827) in
    t828
  } else {
    let t829 = (value/214 as u64) in
    let t830 = decimal_string(t829) in
    t830
  }
}

fn decimal_string(value/208: u64) -> string {
  let t853 = (value/208 == 0) in
  join k833() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit834() -> string {
      join inline_cont889(t835: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t835) in
        let offset/212 = 0 in
        join exit836() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop837() -> string {
            join inline_cont887(t838: isize) -> string {
              let t839 = (offset/212 < t838) in
              if t839 {
                join inline_cont885(t840: isize) -> string {
                  let t841 = (t840 - offset/212) in
                  let t842 = (t841 - 1) in
                  let t843 = @intrinsic(vec.get)(reversed/209, t842) in
                  join inline_cont883(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t844 = (compound_old358 + compound_value359) in
                    let t845 = offset/212 = t844 in
                    jump loop837()
                  } in
                  let inline884 = @intrinsic(vec.push)(bytes/211, t843) in
                  jump inline_cont883(())
                } in
                let inline886 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont885(inline886)
              } else {
                jump exit836()
              }
            } in
            let inline888 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont887(inline888)
          } in
        } in
        jump loop837()
      } in
      let inline890 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont889(inline890)
    } in
    joinrec {
      join loop846() -> string {
        let t847 = (remaining/210 > 0) in
        if t847 {
          let t848 = (remaining/210 % 10) in
          let t849 = (t848 as u8) in
          let t850 = (t849 + 48) in
          join inline_cont881(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t851 = (compound_old353 / compound_value354) in
            let t852 = remaining/210 = t851 in
            jump loop846()
          } in
          let inline882 = @intrinsic(vec.push)(reversed/209, t850) in
          jump inline_cont881(())
        } else {
          jump exit834()
        }
      } in
    } in
    jump loop846()
  } in
  if t853 {
    "0"
  } else {
    jump k833()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t855 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t858 = @intrinsic(vec.len)(self/526) in
  t858
}