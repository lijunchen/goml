package main;
fn my_int_equal(x: i32, y: i32) -> bool {
    !x < y && !y < x
}

fn sum(n: i32) -> i32 {
    if my_int_equal(n, 1) {
        {
            1
        }
    } else {
        {
            n + sum(n - 1)
        }
    }
}

fn main() {
    println(sum(100))
}

