fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t799 = Worker.name(self/0) in
  let t800 = (t799 + ":ready") in
  t800
}

fn trait_impl#Service#[Worker]@Station#get(self/1: Station) -> Worker {
  let t803 = Station.worker(self/1) in
  t803
}

fn main() -> unit {
  let t805 = Worker { name: "build" } in
  let t806 = Station { worker: t805 } in
  join inline_cont820(t807: string) -> unit {
    let inline818 = trait_impl#ToString#string#to_string(t807) in
    let inline819 = @runtime(core.string_println)(inline818) in
    ()
  } in
  let inline821 = trait_impl#Service#[Worker]@Station#get(t806) in
  let inline822 = trait_impl#Ready#Worker#state(inline821) in
  jump inline_cont820(inline822)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont823(t809: string) -> unit {
    let t810 = @runtime(core.string_println)(t809) in
    ()
  } in
  jump inline_cont823(value/1)
}

fn describe__S_Station__T_Worker(service/2: Station) -> string {
  join inline_cont827(t813: Worker) -> string {
    let inline825 = Worker.name(t813) in
    let inline826 = (inline825 + ":ready") in
    inline826
  } in
  let inline828 = Station.worker(service/2) in
  jump inline_cont827(inline828)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}