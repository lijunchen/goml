fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t0 = Worker.name(self/0) in
  let t1 = (t0 + ":ready") in
  t1
}

fn trait_impl#Service#[Worker]@Station#get(self/0: Station) -> Worker {
  let t0 = Station.worker(self/0) in
  t0
}

fn main() -> unit {
  let t0 = Worker { name: "build" } in
  let t1 = Station { worker: t0 } in
  join inline_cont0(t2: string) -> unit {
    let inline0 = trait_impl#ToString#string#to_string(t2) in
    let inline1 = @runtime(core.string_println)(inline0) in
    ()
  } in
  let inline2 = trait_impl#Service#[Worker]@Station#get(t1) in
  let inline3 = trait_impl#Ready#Worker#state(inline2) in
  jump inline_cont0(inline3)
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn describe__S_Station__T_Worker(service/0: Station) -> string {
  join inline_cont0(t0: Worker) -> string {
    let inline0 = Worker.name(t0) in
    let inline1 = (inline0 + ":ready") in
    inline1
  } in
  let inline2 = Station.worker(service/0) in
  jump inline_cont0(inline2)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}