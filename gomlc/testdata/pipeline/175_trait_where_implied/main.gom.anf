fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t190 = Worker.name(self/0) in
  let t191 = (t190 + ":ready") in
  t191
}

fn trait_impl#Service#[Worker]@Station#get(self/1: Station) -> Worker {
  let t194 = Station.worker(self/1) in
  t194
}

fn main() -> unit {
  let t196 = Worker { name: "build" } in
  let t197 = Station { worker: t196 } in
  join inline_cont211(t198: string) -> unit {
    let inline209 = trait_impl#ToString#string#to_string(t198) in
    let inline210 = @runtime(core.string_println)(inline209) in
    ()
  } in
  let inline212 = trait_impl#Service#[Worker]@Station#get(t197) in
  let inline213 = trait_impl#Ready#Worker#state(inline212) in
  jump inline_cont211(inline213)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont214(t200: string) -> unit {
    let t201 = @runtime(core.string_println)(t200) in
    ()
  } in
  jump inline_cont214(value/1)
}

fn describe__S_Station__T_Worker(service/2: Station) -> string {
  join inline_cont218(t204: Worker) -> string {
    let inline216 = Worker.name(t204) in
    let inline217 = (inline216 + ":ready") in
    inline217
  } in
  let inline219 = Station.worker(service/2) in
  jump inline_cont218(inline219)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}