fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t175 = Worker.name(self/0) in
  let t176 = (t175 + ":ready") in
  t176
}

fn trait_impl#Service#[Worker]@Station#get(self/1: Station) -> Worker {
  let t179 = Station.worker(self/1) in
  t179
}

fn main() -> unit {
  let t181 = Worker { name: "build" } in
  let t182 = Station { worker: t181 } in
  join inline_cont196(t183: string) -> unit {
    let inline194 = trait_impl#ToString#string#to_string(t183) in
    let inline195 = @runtime(core.string_println)(inline194) in
    ()
  } in
  let inline197 = trait_impl#Service#[Worker]@Station#get(t182) in
  let inline198 = trait_impl#Ready#Worker#state(inline197) in
  jump inline_cont196(inline198)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont199(t185: string) -> unit {
    let t186 = @runtime(core.string_println)(t185) in
    ()
  } in
  jump inline_cont199(value/1)
}

fn describe__S_Station__T_Worker(service/2: Station) -> string {
  join inline_cont203(t189: Worker) -> string {
    let inline201 = Worker.name(t189) in
    let inline202 = (inline201 + ":ready") in
    inline202
  } in
  let inline204 = Station.worker(service/2) in
  jump inline_cont203(inline204)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}