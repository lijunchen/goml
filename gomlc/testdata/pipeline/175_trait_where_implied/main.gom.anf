fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t158 = Worker.name(self/0) in
  let t159 = (t158 + ":ready") in
  t159
}

fn trait_impl#Service#[Worker]@Station#get(self/1: Station) -> Worker {
  let t162 = Station.worker(self/1) in
  t162
}

fn main() -> unit {
  let t164 = Worker { name: "build" } in
  let t165 = Station { worker: t164 } in
  join inline_cont179(t166: string) -> unit {
    let inline177 = trait_impl#ToString#string#to_string(t166) in
    let inline178 = @runtime(core.string_println)(inline177) in
    ()
  } in
  let inline180 = trait_impl#Service#[Worker]@Station#get(t165) in
  let inline181 = trait_impl#Ready#Worker#state(inline180) in
  jump inline_cont179(inline181)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont182(t168: string) -> unit {
    let t169 = @runtime(core.string_println)(t168) in
    ()
  } in
  jump inline_cont182(value/1)
}

fn describe__S_Station__T_Worker(service/2: Station) -> string {
  join inline_cont186(t172: Worker) -> string {
    let inline184 = Worker.name(t172) in
    let inline185 = (inline184 + ":ready") in
    inline185
  } in
  let inline187 = Station.worker(service/2) in
  jump inline_cont186(inline187)
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  self/38
}