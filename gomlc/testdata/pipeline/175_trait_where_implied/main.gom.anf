fn trait_impl#Ready#Worker#state(self/0: Worker) -> string {
  let t180 = Worker.name(self/0) in
  let t181 = (t180 + ":ready") in
  t181
}

fn trait_impl#Service#[Worker]@Station#get(self/1: Station) -> Worker {
  let t184 = Station.worker(self/1) in
  t184
}

fn main() -> unit {
  let t186 = Worker { name: "build" } in
  let t187 = Station { worker: t186 } in
  join inline_cont201(t188: string) -> unit {
    let inline199 = trait_impl#ToString#string#to_string(t188) in
    let inline200 = @runtime(core.string_println)(inline199) in
    ()
  } in
  let inline202 = trait_impl#Service#[Worker]@Station#get(t187) in
  let inline203 = trait_impl#Ready#Worker#state(inline202) in
  jump inline_cont201(inline203)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont204(t190: string) -> unit {
    let t191 = @runtime(core.string_println)(t190) in
    ()
  } in
  jump inline_cont204(value/31)
}

fn describe__S_Station__T_Worker(service/2: Station) -> string {
  join inline_cont208(t194: Worker) -> string {
    let inline206 = Worker.name(t194) in
    let inline207 = (inline206 + ":ready") in
    inline207
  } in
  let inline209 = Station.worker(service/2) in
  jump inline_cont208(inline209)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}