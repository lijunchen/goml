package main
file main.gom
  trait Ready {
      fn state(Self) -> string;
    }
    
    trait Service[T] where T: Ready {
      fn get(Self) -> T;
    }
    
    struct Worker {
        name: string,
    }
    
    struct Station {
        worker: Worker,
    }
    
    impl Ready for Worker {
      fn state(local/1/0: Worker) -> string {
          self/0.name + ":ready"
      }
    }
    
    impl Service[Worker] for Station {
      fn get(local/1/1: Station) -> Worker {
          self/1.worker
      }
    }
    
    fn describe[T, S: Service[T]](local/1/2: S) -> string {
        Service[T]::get(service/2).state()
    }
    
    fn main() -> unit {
        println(def/1/6(Station {
                worker: Worker {
                    name: "build"
                }
            }));
    }