fn main() -> unit {
  join inline_cont289(i/0: Ref[int]) -> unit {
    join inline_cont286(sum/1: Ref[int]) -> unit {
      join exit197() -> unit {
        join inline_cont284(t198: int) -> unit {
          join inline_cont281(_wild190: unit) -> unit {
            join inline_cont278(j/3: Ref[int]) -> unit {
              join inline_cont275(total/4: Ref[int]) -> unit {
                join exit199() -> unit {
                  join inline_cont273(t200: int) -> unit {
                    let inline271 = trait_impl#ToString#int#to_string(t200) in
                    let inline272 = @runtime(core.string_println)(inline271) in
                    ()
                  } in
                  let inline274 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont273(inline274)
                } in
                joinrec {
                  join loop201() -> unit {
                    join inline_cont268(t202: int) -> unit {
                      let t203 = (t202 + 1) in
                      join inline_cont266(_wild191: unit) -> unit {
                        join inline_cont264(mtmp192: int) -> unit {
                          join k204(jp205: int) -> unit {
                            join inline_cont262(t206: int) -> unit {
                              let t207 = (t206 + jp205) in
                              let inline261 = @intrinsic(ref.set)(total/4, t207) in
                              jump loop201()
                            } in
                            let inline263 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont262(inline263)
                          } in
                          match mtmp192 {
                            5 => {
                              jump exit199()
                            },
                            _ => {
                              let inline259 = @intrinsic(ref.get)(j/3) in
                              jump k204(inline259)
                            },
                          }
                        } in
                        let inline265 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont264(inline265)
                      } in
                      let inline267 = @intrinsic(ref.set)(j/3, t203) in
                      jump inline_cont266(())
                    } in
                    let inline269 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont268(inline269)
                  } in
                } in
                jump loop201()
              } in
              let inline276 = 0 in
              let inline277 = @intrinsic(ref.new)(inline276) in
              jump inline_cont275(inline277)
            } in
            let inline279 = 0 in
            let inline280 = @intrinsic(ref.new)(inline279) in
            jump inline_cont278(inline280)
          } in
          let inline282 = trait_impl#ToString#int#to_string(t198) in
          let inline283 = @runtime(core.string_println)(inline282) in
          jump inline_cont281(())
        } in
        let inline285 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont284(inline285)
      } in
      joinrec {
        join loop209() -> unit {
          join inline_cont256(t210: int) -> unit {
            let t211 = (t210 < 5) in
            if t211 {
              join inline_cont254(t212: int) -> unit {
                let t213 = (t212 + 1) in
                join inline_cont252(_wild187: unit) -> unit {
                  join inline_cont250(t218: int) -> unit {
                    join inline_cont247(t219: bool) -> unit {
                      join k214(jp215: int) -> unit {
                        join inline_cont245(t216: int) -> unit {
                          let t217 = (t216 + jp215) in
                          let inline244 = @intrinsic(ref.set)(sum/1, t217) in
                          jump loop209()
                        } in
                        let inline246 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont245(inline246)
                      } in
                      if t219 {
                        jump loop209()
                      } else {
                        let inline242 = @intrinsic(ref.get)(i/0) in
                        jump k214(inline242)
                      }
                    } in
                    let inline248 = 3 in
                    let inline249 = (t218 == inline248) in
                    jump inline_cont247(inline249)
                  } in
                  let inline251 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont250(inline251)
                } in
                let inline253 = @intrinsic(ref.set)(i/0, t213) in
                jump inline_cont252(())
              } in
              let inline255 = @intrinsic(ref.get)(i/0) in
              jump inline_cont254(inline255)
            } else {
              jump exit197()
            }
          } in
          let inline257 = @intrinsic(ref.get)(i/0) in
          jump inline_cont256(inline257)
        } in
      } in
      jump loop209()
    } in
    let inline287 = 0 in
    let inline288 = @intrinsic(ref.new)(inline287) in
    jump inline_cont286(inline288)
  } in
  let inline290 = 0 in
  let inline291 = @intrinsic(ref.new)(inline290) in
  jump inline_cont289(inline291)
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t223 = @intrinsic(ref.new)(value/273) in
  t223
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t226 = @intrinsic(ref.get)(self/274) in
  t226
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t228 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t231 = (self/101 == other/102) in
  t231
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont292(t233: string) -> unit {
    let t234 = @runtime(core.string_println)(t233) in
    ()
  } in
  let inline293 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont292(inline293)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t237 = @runtime(core.int_to_string)(self/67) in
  t237
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t240 = @runtime(core.int_hash)(self/127) in
  t240
}