fn main() -> unit {
  join inline_cont938(i/0: Ref[isize]) -> unit {
    join inline_cont935(sum/1: Ref[isize]) -> unit {
      join exit806() -> unit {
        join inline_cont933(t807: isize) -> unit {
          join inline_cont930(_wild799: unit) -> unit {
            join inline_cont927(j/3: Ref[isize]) -> unit {
              join inline_cont924(total/4: Ref[isize]) -> unit {
                join exit808() -> unit {
                  join inline_cont922(t809: isize) -> unit {
                    let inline920 = trait_impl#ToString#isize#to_string(t809) in
                    let inline921 = @runtime(core.string_println)(inline920) in
                    ()
                  } in
                  let inline923 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont922(inline923)
                } in
                joinrec {
                  join loop810() -> unit {
                    join inline_cont917(t811: isize) -> unit {
                      let t812 = (t811 + 1) in
                      join inline_cont915(_wild800: unit) -> unit {
                        join inline_cont913(mtmp801: isize) -> unit {
                          join k813(jp814: isize) -> unit {
                            join inline_cont911(t815: isize) -> unit {
                              let t816 = (t815 + jp814) in
                              let inline910 = @intrinsic(ref.set)(total/4, t816) in
                              jump loop810()
                            } in
                            let inline912 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont911(inline912)
                          } in
                          match mtmp801 {
                            5 => {
                              jump exit808()
                            },
                            _ => {
                              let inline908 = @intrinsic(ref.get)(j/3) in
                              jump k813(inline908)
                            },
                          }
                        } in
                        let inline914 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont913(inline914)
                      } in
                      let inline916 = @intrinsic(ref.set)(j/3, t812) in
                      jump inline_cont915(())
                    } in
                    let inline918 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont917(inline918)
                  } in
                } in
                jump loop810()
              } in
              let inline925 = 0 in
              let inline926 = @intrinsic(ref.new)(inline925) in
              jump inline_cont924(inline926)
            } in
            let inline928 = 0 in
            let inline929 = @intrinsic(ref.new)(inline928) in
            jump inline_cont927(inline929)
          } in
          let inline931 = trait_impl#ToString#isize#to_string(t807) in
          let inline932 = @runtime(core.string_println)(inline931) in
          jump inline_cont930(())
        } in
        let inline934 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont933(inline934)
      } in
      joinrec {
        join loop818() -> unit {
          join inline_cont905(t819: isize) -> unit {
            let t820 = (t819 < 5) in
            if t820 {
              join inline_cont903(t821: isize) -> unit {
                let t822 = (t821 + 1) in
                join inline_cont901(_wild796: unit) -> unit {
                  join inline_cont899(t827: isize) -> unit {
                    let t828 = (t827 == 3) in
                    join k823(jp824: isize) -> unit {
                      join inline_cont897(t825: isize) -> unit {
                        let t826 = (t825 + jp824) in
                        let inline896 = @intrinsic(ref.set)(sum/1, t826) in
                        jump loop818()
                      } in
                      let inline898 = @intrinsic(ref.get)(sum/1) in
                      jump inline_cont897(inline898)
                    } in
                    if t828 {
                      jump loop818()
                    } else {
                      let inline894 = @intrinsic(ref.get)(i/0) in
                      jump k823(inline894)
                    }
                  } in
                  let inline900 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont899(inline900)
                } in
                let inline902 = @intrinsic(ref.set)(i/0, t822) in
                jump inline_cont901(())
              } in
              let inline904 = @intrinsic(ref.get)(i/0) in
              jump inline_cont903(inline904)
            } else {
              jump exit806()
            }
          } in
          let inline906 = @intrinsic(ref.get)(i/0) in
          jump inline_cont905(inline906)
        } in
      } in
      jump loop818()
    } in
    let inline936 = 0 in
    let inline937 = @intrinsic(ref.new)(inline936) in
    jump inline_cont935(inline937)
  } in
  let inline939 = 0 in
  let inline940 = @intrinsic(ref.new)(inline939) in
  jump inline_cont938(inline940)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t832 = @intrinsic(ref.new)(value/684) in
  t832
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t835 = @intrinsic(ref.get)(self/685) in
  t835
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t837 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont941(t839: string) -> unit {
    let t840 = @runtime(core.string_println)(t839) in
    ()
  } in
  let inline942 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont941(inline942)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline944 = (self/404 as i64) in
  let inline945 = signed_decimal_string(inline944) in
  inline945
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t846 = (value/222 as i64) in
  let inline947 = (t846 < 0) in
  if inline947 {
    let inline948 = (t846 as u64) in
    let inline949 = (0 - inline948) in
    let inline950 = decimal_string(inline949) in
    let inline951 = ("-" + inline950) in
    inline951
  } else {
    let inline952 = (t846 as u64) in
    let inline953 = decimal_string(inline952) in
    inline953
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t852 = (value/214 < 0) in
  if t852 {
    let t853 = (value/214 as u64) in
    let t854 = (0 - t853) in
    let t855 = decimal_string(t854) in
    let t856 = ("-" + t855) in
    t856
  } else {
    let t857 = (value/214 as u64) in
    let t858 = decimal_string(t857) in
    t858
  }
}

fn decimal_string(value/208: u64) -> string {
  let t881 = (value/208 == 0) in
  join k861() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit862() -> string {
      join inline_cont962(t863: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t863) in
        let offset/212 = 0 in
        join exit864() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop865() -> string {
            join inline_cont960(t866: isize) -> string {
              let t867 = (offset/212 < t866) in
              if t867 {
                join inline_cont958(t868: isize) -> string {
                  let t869 = (t868 - offset/212) in
                  let t870 = (t869 - 1) in
                  let t871 = @intrinsic(vec.get)(reversed/209, t870) in
                  join inline_cont956(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t872 = (compound_old358 + compound_value359) in
                    let t873 = offset/212 = t872 in
                    jump loop865()
                  } in
                  let inline957 = @intrinsic(vec.push)(bytes/211, t871) in
                  jump inline_cont956(())
                } in
                let inline959 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont958(inline959)
              } else {
                jump exit864()
              }
            } in
            let inline961 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont960(inline961)
          } in
        } in
        jump loop865()
      } in
      let inline963 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont962(inline963)
    } in
    joinrec {
      join loop874() -> string {
        let t875 = (remaining/210 > 0) in
        if t875 {
          let t876 = (remaining/210 % 10) in
          let t877 = (t876 as u8) in
          let t878 = (t877 + 48) in
          join inline_cont954(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t879 = (compound_old353 / compound_value354) in
            let t880 = remaining/210 = t879 in
            jump loop874()
          } in
          let inline955 = @intrinsic(vec.push)(reversed/209, t878) in
          jump inline_cont954(())
        } else {
          jump exit862()
        }
      } in
    } in
    jump loop874()
  } in
  if t881 {
    "0"
  } else {
    jump k861()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t883 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t886 = @intrinsic(vec.len)(self/526) in
  t886
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t889 = (self/438 == other/439) in
  t889
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t892 = @runtime(core.int_hash)(self/464) in
  t892
}