fn main() -> unit {
  join inline_cont510(i/0: Ref[int]) -> unit {
    join inline_cont507(sum/1: Ref[int]) -> unit {
      join exit421() -> unit {
        join inline_cont505(t422: int) -> unit {
          join inline_cont502(_wild414: unit) -> unit {
            join inline_cont499(j/3: Ref[int]) -> unit {
              join inline_cont496(total/4: Ref[int]) -> unit {
                join exit423() -> unit {
                  join inline_cont494(t424: int) -> unit {
                    let inline492 = trait_impl#ToString#int#to_string(t424) in
                    let inline493 = @runtime(core.string_println)(inline492) in
                    ()
                  } in
                  let inline495 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont494(inline495)
                } in
                joinrec {
                  join loop425() -> unit {
                    join inline_cont489(t426: int) -> unit {
                      let t427 = (t426 + 1) in
                      join inline_cont487(_wild415: unit) -> unit {
                        join inline_cont485(mtmp416: int) -> unit {
                          join k428(jp429: int) -> unit {
                            join inline_cont483(t430: int) -> unit {
                              let t431 = (t430 + jp429) in
                              let inline482 = @intrinsic(ref.set)(total/4, t431) in
                              jump loop425()
                            } in
                            let inline484 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont483(inline484)
                          } in
                          match mtmp416 {
                            5 => {
                              jump exit423()
                            },
                            _ => {
                              let inline480 = @intrinsic(ref.get)(j/3) in
                              jump k428(inline480)
                            },
                          }
                        } in
                        let inline486 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont485(inline486)
                      } in
                      let inline488 = @intrinsic(ref.set)(j/3, t427) in
                      jump inline_cont487(())
                    } in
                    let inline490 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont489(inline490)
                  } in
                } in
                jump loop425()
              } in
              let inline497 = 0 in
              let inline498 = @intrinsic(ref.new)(inline497) in
              jump inline_cont496(inline498)
            } in
            let inline500 = 0 in
            let inline501 = @intrinsic(ref.new)(inline500) in
            jump inline_cont499(inline501)
          } in
          let inline503 = trait_impl#ToString#int#to_string(t422) in
          let inline504 = @runtime(core.string_println)(inline503) in
          jump inline_cont502(())
        } in
        let inline506 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont505(inline506)
      } in
      joinrec {
        join loop433() -> unit {
          join inline_cont477(t434: int) -> unit {
            let t435 = (t434 < 5) in
            if t435 {
              join inline_cont475(t436: int) -> unit {
                let t437 = (t436 + 1) in
                join inline_cont473(_wild411: unit) -> unit {
                  join inline_cont471(t442: int) -> unit {
                    let t443 = (t442 == 3) in
                    join k438(jp439: int) -> unit {
                      join inline_cont469(t440: int) -> unit {
                        let t441 = (t440 + jp439) in
                        let inline468 = @intrinsic(ref.set)(sum/1, t441) in
                        jump loop433()
                      } in
                      let inline470 = @intrinsic(ref.get)(sum/1) in
                      jump inline_cont469(inline470)
                    } in
                    if t443 {
                      jump loop433()
                    } else {
                      let inline466 = @intrinsic(ref.get)(i/0) in
                      jump k438(inline466)
                    }
                  } in
                  let inline472 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont471(inline472)
                } in
                let inline474 = @intrinsic(ref.set)(i/0, t437) in
                jump inline_cont473(())
              } in
              let inline476 = @intrinsic(ref.get)(i/0) in
              jump inline_cont475(inline476)
            } else {
              jump exit421()
            }
          } in
          let inline478 = @intrinsic(ref.get)(i/0) in
          jump inline_cont477(inline478)
        } in
      } in
      jump loop433()
    } in
    let inline508 = 0 in
    let inline509 = @intrinsic(ref.new)(inline508) in
    jump inline_cont507(inline509)
  } in
  let inline511 = 0 in
  let inline512 = @intrinsic(ref.new)(inline511) in
  jump inline_cont510(inline512)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t447 = @intrinsic(ref.new)(value/431) in
  t447
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t450 = @intrinsic(ref.get)(self/432) in
  t450
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t452 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont513(t454: string) -> unit {
    let t455 = @runtime(core.string_println)(t454) in
    ()
  } in
  let inline514 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont513(inline514)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t458 = @runtime(core.int_to_string)(self/151) in
  t458
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t461 = (self/185 == other/186) in
  t461
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t464 = @runtime(core.int_hash)(self/211) in
  t464
}