fn main() -> unit {
  join inline_cont507(i/0: Ref[int]) -> unit {
    join inline_cont504(sum/1: Ref[int]) -> unit {
      join exit418() -> unit {
        join inline_cont502(t419: int) -> unit {
          join inline_cont499(_wild411: unit) -> unit {
            join inline_cont496(j/3: Ref[int]) -> unit {
              join inline_cont493(total/4: Ref[int]) -> unit {
                join exit420() -> unit {
                  join inline_cont491(t421: int) -> unit {
                    let inline489 = trait_impl#ToString#int#to_string(t421) in
                    let inline490 = @runtime(core.string_println)(inline489) in
                    ()
                  } in
                  let inline492 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont491(inline492)
                } in
                joinrec {
                  join loop422() -> unit {
                    join inline_cont486(t423: int) -> unit {
                      let t424 = (t423 + 1) in
                      join inline_cont484(_wild412: unit) -> unit {
                        join inline_cont482(mtmp413: int) -> unit {
                          join k425(jp426: int) -> unit {
                            join inline_cont480(t427: int) -> unit {
                              let t428 = (t427 + jp426) in
                              let inline479 = @intrinsic(ref.set)(total/4, t428) in
                              jump loop422()
                            } in
                            let inline481 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont480(inline481)
                          } in
                          match mtmp413 {
                            5 => {
                              jump exit420()
                            },
                            _ => {
                              let inline477 = @intrinsic(ref.get)(j/3) in
                              jump k425(inline477)
                            },
                          }
                        } in
                        let inline483 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont482(inline483)
                      } in
                      let inline485 = @intrinsic(ref.set)(j/3, t424) in
                      jump inline_cont484(())
                    } in
                    let inline487 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont486(inline487)
                  } in
                } in
                jump loop422()
              } in
              let inline494 = 0 in
              let inline495 = @intrinsic(ref.new)(inline494) in
              jump inline_cont493(inline495)
            } in
            let inline497 = 0 in
            let inline498 = @intrinsic(ref.new)(inline497) in
            jump inline_cont496(inline498)
          } in
          let inline500 = trait_impl#ToString#int#to_string(t419) in
          let inline501 = @runtime(core.string_println)(inline500) in
          jump inline_cont499(())
        } in
        let inline503 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont502(inline503)
      } in
      joinrec {
        join loop430() -> unit {
          join inline_cont474(t431: int) -> unit {
            let t432 = (t431 < 5) in
            if t432 {
              join inline_cont472(t433: int) -> unit {
                let t434 = (t433 + 1) in
                join inline_cont470(_wild408: unit) -> unit {
                  join inline_cont468(t439: int) -> unit {
                    let t440 = (t439 == 3) in
                    join k435(jp436: int) -> unit {
                      join inline_cont466(t437: int) -> unit {
                        let t438 = (t437 + jp436) in
                        let inline465 = @intrinsic(ref.set)(sum/1, t438) in
                        jump loop430()
                      } in
                      let inline467 = @intrinsic(ref.get)(sum/1) in
                      jump inline_cont466(inline467)
                    } in
                    if t440 {
                      jump loop430()
                    } else {
                      let inline463 = @intrinsic(ref.get)(i/0) in
                      jump k435(inline463)
                    }
                  } in
                  let inline469 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont468(inline469)
                } in
                let inline471 = @intrinsic(ref.set)(i/0, t434) in
                jump inline_cont470(())
              } in
              let inline473 = @intrinsic(ref.get)(i/0) in
              jump inline_cont472(inline473)
            } else {
              jump exit418()
            }
          } in
          let inline475 = @intrinsic(ref.get)(i/0) in
          jump inline_cont474(inline475)
        } in
      } in
      jump loop430()
    } in
    let inline505 = 0 in
    let inline506 = @intrinsic(ref.new)(inline505) in
    jump inline_cont504(inline506)
  } in
  let inline508 = 0 in
  let inline509 = @intrinsic(ref.new)(inline508) in
  jump inline_cont507(inline509)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t444 = @intrinsic(ref.new)(value/431) in
  t444
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t447 = @intrinsic(ref.get)(self/432) in
  t447
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t449 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont510(t451: string) -> unit {
    let t452 = @runtime(core.string_println)(t451) in
    ()
  } in
  let inline511 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont510(inline511)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t455 = @runtime(core.int_to_string)(self/151) in
  t455
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t458 = (self/185 == other/186) in
  t458
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t461 = @runtime(core.int_hash)(self/211) in
  t461
}