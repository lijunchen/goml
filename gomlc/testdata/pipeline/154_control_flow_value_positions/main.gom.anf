fn main() -> unit {
  join inline_cont510(i/0: Ref[int]) -> unit {
    join inline_cont507(sum/1: Ref[int]) -> unit {
      join exit418() -> unit {
        join inline_cont505(t419: int) -> unit {
          join inline_cont502(_wild411: unit) -> unit {
            join inline_cont499(j/3: Ref[int]) -> unit {
              join inline_cont496(total/4: Ref[int]) -> unit {
                join exit420() -> unit {
                  join inline_cont494(t421: int) -> unit {
                    let inline492 = trait_impl#ToString#int#to_string(t421) in
                    let inline493 = @runtime(core.string_println)(inline492) in
                    ()
                  } in
                  let inline495 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont494(inline495)
                } in
                joinrec {
                  join loop422() -> unit {
                    join inline_cont489(t423: int) -> unit {
                      let t424 = (t423 + 1) in
                      join inline_cont487(_wild412: unit) -> unit {
                        join inline_cont485(mtmp413: int) -> unit {
                          join k425(jp426: int) -> unit {
                            join inline_cont483(t427: int) -> unit {
                              let t428 = (t427 + jp426) in
                              let inline482 = @intrinsic(ref.set)(total/4, t428) in
                              jump loop422()
                            } in
                            let inline484 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont483(inline484)
                          } in
                          match mtmp413 {
                            5 => {
                              jump exit420()
                            },
                            _ => {
                              let inline480 = @intrinsic(ref.get)(j/3) in
                              jump k425(inline480)
                            },
                          }
                        } in
                        let inline486 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont485(inline486)
                      } in
                      let inline488 = @intrinsic(ref.set)(j/3, t424) in
                      jump inline_cont487(())
                    } in
                    let inline490 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont489(inline490)
                  } in
                } in
                jump loop422()
              } in
              let inline497 = 0 in
              let inline498 = @intrinsic(ref.new)(inline497) in
              jump inline_cont496(inline498)
            } in
            let inline500 = 0 in
            let inline501 = @intrinsic(ref.new)(inline500) in
            jump inline_cont499(inline501)
          } in
          let inline503 = trait_impl#ToString#int#to_string(t419) in
          let inline504 = @runtime(core.string_println)(inline503) in
          jump inline_cont502(())
        } in
        let inline506 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont505(inline506)
      } in
      joinrec {
        join loop430() -> unit {
          join inline_cont477(t431: int) -> unit {
            let t432 = (t431 < 5) in
            if t432 {
              join inline_cont475(t433: int) -> unit {
                let t434 = (t433 + 1) in
                join inline_cont473(_wild408: unit) -> unit {
                  join inline_cont471(t439: int) -> unit {
                    join inline_cont468(t440: bool) -> unit {
                      join k435(jp436: int) -> unit {
                        join inline_cont466(t437: int) -> unit {
                          let t438 = (t437 + jp436) in
                          let inline465 = @intrinsic(ref.set)(sum/1, t438) in
                          jump loop430()
                        } in
                        let inline467 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont466(inline467)
                      } in
                      if t440 {
                        jump loop430()
                      } else {
                        let inline463 = @intrinsic(ref.get)(i/0) in
                        jump k435(inline463)
                      }
                    } in
                    let inline469 = 3 in
                    let inline470 = (t439 == inline469) in
                    jump inline_cont468(inline470)
                  } in
                  let inline472 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont471(inline472)
                } in
                let inline474 = @intrinsic(ref.set)(i/0, t434) in
                jump inline_cont473(())
              } in
              let inline476 = @intrinsic(ref.get)(i/0) in
              jump inline_cont475(inline476)
            } else {
              jump exit418()
            }
          } in
          let inline478 = @intrinsic(ref.get)(i/0) in
          jump inline_cont477(inline478)
        } in
      } in
      jump loop430()
    } in
    let inline508 = 0 in
    let inline509 = @intrinsic(ref.new)(inline508) in
    jump inline_cont507(inline509)
  } in
  let inline511 = 0 in
  let inline512 = @intrinsic(ref.new)(inline511) in
  jump inline_cont510(inline512)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t444 = @intrinsic(ref.new)(value/431) in
  t444
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t447 = @intrinsic(ref.get)(self/432) in
  t447
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t449 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t452 = (self/185 == other/186) in
  t452
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont513(t454: string) -> unit {
    let t455 = @runtime(core.string_println)(t454) in
    ()
  } in
  let inline514 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont513(inline514)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t458 = @runtime(core.int_to_string)(self/151) in
  t458
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t461 = @runtime(core.int_hash)(self/211) in
  t461
}