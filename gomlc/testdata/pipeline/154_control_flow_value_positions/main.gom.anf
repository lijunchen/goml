fn main() -> unit {
  join inline_cont274(i/0: Ref[int]) -> unit {
    join inline_cont271(sum/1: Ref[int]) -> unit {
      join exit182() -> unit {
        join inline_cont269(t183: int) -> unit {
          join inline_cont266(_wild175: unit) -> unit {
            join inline_cont263(j/3: Ref[int]) -> unit {
              join inline_cont260(total/4: Ref[int]) -> unit {
                join exit184() -> unit {
                  join inline_cont258(t185: int) -> unit {
                    let inline256 = trait_impl#ToString#int#to_string(t185) in
                    let inline257 = @runtime(core.string_println)(inline256) in
                    ()
                  } in
                  let inline259 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont258(inline259)
                } in
                joinrec {
                  join loop186() -> unit {
                    join inline_cont253(t187: int) -> unit {
                      let t188 = (t187 + 1) in
                      join inline_cont251(_wild176: unit) -> unit {
                        join inline_cont249(mtmp177: int) -> unit {
                          join k189(jp190: int) -> unit {
                            join inline_cont247(t191: int) -> unit {
                              let t192 = (t191 + jp190) in
                              let inline246 = @intrinsic(ref.set)(total/4, t192) in
                              jump loop186()
                            } in
                            let inline248 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont247(inline248)
                          } in
                          match mtmp177 {
                            5 => {
                              jump exit184()
                            },
                            _ => {
                              let inline244 = @intrinsic(ref.get)(j/3) in
                              jump k189(inline244)
                            },
                          }
                        } in
                        let inline250 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont249(inline250)
                      } in
                      let inline252 = @intrinsic(ref.set)(j/3, t188) in
                      jump inline_cont251(())
                    } in
                    let inline254 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont253(inline254)
                  } in
                } in
                jump loop186()
              } in
              let inline261 = 0 in
              let inline262 = @intrinsic(ref.new)(inline261) in
              jump inline_cont260(inline262)
            } in
            let inline264 = 0 in
            let inline265 = @intrinsic(ref.new)(inline264) in
            jump inline_cont263(inline265)
          } in
          let inline267 = trait_impl#ToString#int#to_string(t183) in
          let inline268 = @runtime(core.string_println)(inline267) in
          jump inline_cont266(())
        } in
        let inline270 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont269(inline270)
      } in
      joinrec {
        join loop194() -> unit {
          join inline_cont241(t195: int) -> unit {
            let t196 = (t195 < 5) in
            if t196 {
              join inline_cont239(t197: int) -> unit {
                let t198 = (t197 + 1) in
                join inline_cont237(_wild172: unit) -> unit {
                  join inline_cont235(t203: int) -> unit {
                    join inline_cont232(t204: bool) -> unit {
                      join k199(jp200: int) -> unit {
                        join inline_cont230(t201: int) -> unit {
                          let t202 = (t201 + jp200) in
                          let inline229 = @intrinsic(ref.set)(sum/1, t202) in
                          jump loop194()
                        } in
                        let inline231 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont230(inline231)
                      } in
                      if t204 {
                        jump loop194()
                      } else {
                        let inline227 = @intrinsic(ref.get)(i/0) in
                        jump k199(inline227)
                      }
                    } in
                    let inline233 = 3 in
                    let inline234 = (t203 == inline233) in
                    jump inline_cont232(inline234)
                  } in
                  let inline236 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont235(inline236)
                } in
                let inline238 = @intrinsic(ref.set)(i/0, t198) in
                jump inline_cont237(())
              } in
              let inline240 = @intrinsic(ref.get)(i/0) in
              jump inline_cont239(inline240)
            } else {
              jump exit182()
            }
          } in
          let inline242 = @intrinsic(ref.get)(i/0) in
          jump inline_cont241(inline242)
        } in
      } in
      jump loop194()
    } in
    let inline272 = 0 in
    let inline273 = @intrinsic(ref.new)(inline272) in
    jump inline_cont271(inline273)
  } in
  let inline275 = 0 in
  let inline276 = @intrinsic(ref.new)(inline275) in
  jump inline_cont274(inline276)
}

fn inherent#Ref#Ref[T]#new__T_int(value/255: int) -> Ref[int] {
  let t208 = @intrinsic(ref.new)(value/255) in
  t208
}

fn inherent#Ref#Ref[T]#get__T_int(self/256: Ref[int]) -> int {
  let t211 = @intrinsic(ref.get)(self/256) in
  t211
}

fn inherent#Ref#Ref[T]#set__T_int(self/257: Ref[int], value/258: int) -> unit {
  let t213 = @intrinsic(ref.set)(self/257, value/258) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t216 = (self/101 == other/102) in
  t216
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont277(t218: string) -> unit {
    let t219 = @runtime(core.string_println)(t218) in
    ()
  } in
  let inline278 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont277(inline278)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t222 = @runtime(core.int_to_string)(self/67) in
  t222
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t225 = @runtime(core.int_hash)(self/127) in
  t225
}