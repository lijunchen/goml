fn main() -> unit {
  join inline_cont279(i/0: Ref[int]) -> unit {
    join inline_cont276(sum/1: Ref[int]) -> unit {
      join exit187() -> unit {
        let _wild179 = () in
        join inline_cont274(t188: int) -> unit {
          join inline_cont271(_wild180: unit) -> unit {
            join inline_cont268(j/3: Ref[int]) -> unit {
              join inline_cont265(total/4: Ref[int]) -> unit {
                join exit189() -> unit {
                  let _wild184 = () in
                  join inline_cont263(t190: int) -> unit {
                    let inline261 = trait_impl#ToString#int#to_string(t190) in
                    let inline262 = @runtime(core.string_println)(inline261) in
                    ()
                  } in
                  let inline264 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont263(inline264)
                } in
                joinrec {
                  join loop191() -> unit {
                    join inline_cont258(t192: int) -> unit {
                      let t193 = (t192 + 1) in
                      join inline_cont256(_wild181: unit) -> unit {
                        join inline_cont254(mtmp182: int) -> unit {
                          join k194(jp195: int) -> unit {
                            let cur/5 = jp195 in
                            join inline_cont252(t196: int) -> unit {
                              let t197 = (t196 + jp195) in
                              let inline251 = @intrinsic(ref.set)(total/4, t197) in
                              jump loop191()
                            } in
                            let inline253 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont252(inline253)
                          } in
                          match mtmp182 {
                            5 => {
                              jump exit189()
                            },
                            _ => {
                              let inline249 = @intrinsic(ref.get)(j/3) in
                              jump k194(inline249)
                            },
                          }
                        } in
                        let inline255 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont254(inline255)
                      } in
                      let inline257 = @intrinsic(ref.set)(j/3, t193) in
                      jump inline_cont256(())
                    } in
                    let inline259 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont258(inline259)
                  } in
                } in
                jump loop191()
              } in
              let inline266 = 0 in
              let inline267 = @intrinsic(ref.new)(inline266) in
              jump inline_cont265(inline267)
            } in
            let inline269 = 0 in
            let inline270 = @intrinsic(ref.new)(inline269) in
            jump inline_cont268(inline270)
          } in
          let inline272 = trait_impl#ToString#int#to_string(t188) in
          let inline273 = @runtime(core.string_println)(inline272) in
          jump inline_cont271(())
        } in
        let inline275 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont274(inline275)
      } in
      joinrec {
        join loop199() -> unit {
          join inline_cont246(t200: int) -> unit {
            let t201 = (t200 < 5) in
            if t201 {
              join inline_cont244(t202: int) -> unit {
                let t203 = (t202 + 1) in
                join inline_cont242(_wild177: unit) -> unit {
                  join inline_cont240(t208: int) -> unit {
                    join inline_cont237(t209: bool) -> unit {
                      join k204(jp205: int) -> unit {
                        let cur/2 = jp205 in
                        join inline_cont235(t206: int) -> unit {
                          let t207 = (t206 + jp205) in
                          let inline234 = @intrinsic(ref.set)(sum/1, t207) in
                          jump loop199()
                        } in
                        let inline236 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont235(inline236)
                      } in
                      if t209 {
                        jump loop199()
                      } else {
                        let inline232 = @intrinsic(ref.get)(i/0) in
                        jump k204(inline232)
                      }
                    } in
                    let inline238 = 3 in
                    let inline239 = @intrinsic(eq.primitive)(t208, inline238) in
                    jump inline_cont237(inline239)
                  } in
                  let inline241 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont240(inline241)
                } in
                let inline243 = @intrinsic(ref.set)(i/0, t203) in
                jump inline_cont242(())
              } in
              let inline245 = @intrinsic(ref.get)(i/0) in
              jump inline_cont244(inline245)
            } else {
              jump exit187()
            }
          } in
          let inline247 = @intrinsic(ref.get)(i/0) in
          jump inline_cont246(inline247)
        } in
      } in
      jump loop199()
    } in
    let inline277 = 0 in
    let inline278 = @intrinsic(ref.new)(inline277) in
    jump inline_cont276(inline278)
  } in
  let inline280 = 0 in
  let inline281 = @intrinsic(ref.new)(inline280) in
  jump inline_cont279(inline281)
}

fn inherent#Ref#Ref[T]#new__T_int(value/236: int) -> Ref[int] {
  let t213 = @intrinsic(ref.new)(value/236) in
  t213
}

fn inherent#Ref#Ref[T]#get__T_int(self/237: Ref[int]) -> int {
  let t216 = @intrinsic(ref.get)(self/237) in
  t216
}

fn inherent#Ref#Ref[T]#set__T_int(self/238: Ref[int], value/239: int) -> unit {
  let t218 = @intrinsic(ref.set)(self/238, value/239) in
  ()
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t221 = @intrinsic(eq.primitive)(self/88, other/89) in
  t221
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont282(t223: string) -> unit {
    let t224 = @runtime(core.string_println)(t223) in
    ()
  } in
  let inline283 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont282(inline283)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t227 = @runtime(core.int_to_string)(self/69) in
  t227
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t230 = @runtime(core.int_hash)(self/114) in
  t230
}