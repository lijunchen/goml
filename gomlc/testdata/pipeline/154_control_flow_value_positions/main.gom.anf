fn main() -> unit {
  join ret117() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit118() -> unit {
    let _wild110 = () in
    let t119 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild111 = println__T_int(t119) in
    let j/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/4 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit120() -> unit {
      let _wild115 = () in
      let t121 = inherent#Ref#Ref[T]#get__T_int(total/4) in
      let _wild116 = println__T_int(t121) in
      jump ret117()
    } in
    joinrec {
      join loop122() -> unit {
        if true {
          let t123 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          let t124 = (t123 + 1) in
          let _wild112 = inherent#Ref#Ref[T]#set__T_int(j/3, t124) in
          let mtmp113 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          join k125(jp126: int) -> unit {
            let cur/5 = jp126 in
            let t127 = inherent#Ref#Ref[T]#get__T_int(total/4) in
            let t128 = (t127 + cur/5) in
            let _wild114 = inherent#Ref#Ref[T]#set__T_int(total/4, t128) in
            jump loop122()
          } in
          match mtmp113 {
            5 => {
              jump exit120()
            },
            _ => {
              let t129 = inherent#Ref#Ref[T]#get__T_int(j/3) in
              jump k125(t129)
            },
          }
        } else {
          jump exit120()
        }
      } in
    } in
    jump loop122()
  } in
  joinrec {
    join loop130() -> unit {
      let t131 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t132 = (t131 < 5) in
      if t132 {
        let t133 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t134 = (t133 + 1) in
        let _wild108 = inherent#Ref#Ref[T]#set__T_int(i/0, t134) in
        let t139 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t140 = trait_impl#Eq#int#eq(t139, 3) in
        join k135(jp136: int) -> unit {
          let cur/2 = jp136 in
          let t137 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t138 = (t137 + cur/2) in
          let _wild109 = inherent#Ref#Ref[T]#set__T_int(sum/1, t138) in
          jump loop130()
        } in
        if t140 {
          jump loop130()
        } else {
          let t141 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          jump k135(t141)
        }
      } else {
        jump exit118()
      }
    } in
  } in
  jump loop130()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret142(retv143: Ref[int]) -> Ref[int] {
    retv143
  } in
  let t144 = @intrinsic(ref.new)(value/207) in
  jump ret142(t144)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret145(retv146: int) -> int {
    retv146
  } in
  let t147 = @intrinsic(ref.get)(self/208) in
  jump ret145(t147)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret148() -> unit {
    ()
  } in
  let t149 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret148()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret150(retv151: bool) -> bool {
    retv151
  } in
  let t152 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret150(t152)
}

fn println__T_int(value/1: int) -> unit {
  join ret153() -> unit {
    ()
  } in
  let t154 = trait_impl#ToString#int#to_string(value/1) in
  let t155 = @runtime(core.string_println)(t154) in
  jump ret153()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret156(retv157: string) -> string {
    retv157
  } in
  let t158 = @runtime(core.int_to_string)(self/40) in
  jump ret156(t158)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret159(retv160: uint64) -> uint64 {
    retv160
  } in
  let t161 = @runtime(core.int_hash)(self/85) in
  jump ret159(t161)
}