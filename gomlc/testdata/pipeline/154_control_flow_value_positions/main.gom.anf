fn main() -> unit {
  join inline_cont238(i/0: Ref[int]) -> unit {
    join inline_cont235(sum/1: Ref[int]) -> unit {
      join exit146() -> unit {
        let _wild138 = () in
        join inline_cont233(t147: int) -> unit {
          join inline_cont230(_wild139: unit) -> unit {
            join inline_cont227(j/3: Ref[int]) -> unit {
              join inline_cont224(total/4: Ref[int]) -> unit {
                join exit148() -> unit {
                  let _wild143 = () in
                  join inline_cont222(t149: int) -> unit {
                    let inline220 = trait_impl#ToString#int#to_string(t149) in
                    let inline221 = @runtime(core.string_println)(inline220) in
                    ()
                  } in
                  let inline223 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont222(inline223)
                } in
                joinrec {
                  join loop150() -> unit {
                    join inline_cont217(t151: int) -> unit {
                      let t152 = (t151 + 1) in
                      join inline_cont215(_wild140: unit) -> unit {
                        join inline_cont213(mtmp141: int) -> unit {
                          join k153(jp154: int) -> unit {
                            let cur/5 = jp154 in
                            join inline_cont211(t155: int) -> unit {
                              let t156 = (t155 + jp154) in
                              let inline210 = @intrinsic(ref.set)(total/4, t156) in
                              jump loop150()
                            } in
                            let inline212 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont211(inline212)
                          } in
                          match mtmp141 {
                            5 => {
                              jump exit148()
                            },
                            _ => {
                              let inline208 = @intrinsic(ref.get)(j/3) in
                              jump k153(inline208)
                            },
                          }
                        } in
                        let inline214 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont213(inline214)
                      } in
                      let inline216 = @intrinsic(ref.set)(j/3, t152) in
                      jump inline_cont215(())
                    } in
                    let inline218 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont217(inline218)
                  } in
                } in
                jump loop150()
              } in
              let inline225 = 0 in
              let inline226 = @intrinsic(ref.new)(inline225) in
              jump inline_cont224(inline226)
            } in
            let inline228 = 0 in
            let inline229 = @intrinsic(ref.new)(inline228) in
            jump inline_cont227(inline229)
          } in
          let inline231 = trait_impl#ToString#int#to_string(t147) in
          let inline232 = @runtime(core.string_println)(inline231) in
          jump inline_cont230(())
        } in
        let inline234 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont233(inline234)
      } in
      joinrec {
        join loop158() -> unit {
          join inline_cont205(t159: int) -> unit {
            let t160 = (t159 < 5) in
            if t160 {
              join inline_cont203(t161: int) -> unit {
                let t162 = (t161 + 1) in
                join inline_cont201(_wild136: unit) -> unit {
                  join inline_cont199(t167: int) -> unit {
                    join inline_cont196(t168: bool) -> unit {
                      join k163(jp164: int) -> unit {
                        let cur/2 = jp164 in
                        join inline_cont194(t165: int) -> unit {
                          let t166 = (t165 + jp164) in
                          let inline193 = @intrinsic(ref.set)(sum/1, t166) in
                          jump loop158()
                        } in
                        let inline195 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont194(inline195)
                      } in
                      if t168 {
                        jump loop158()
                      } else {
                        let inline191 = @intrinsic(ref.get)(i/0) in
                        jump k163(inline191)
                      }
                    } in
                    let inline197 = 3 in
                    let inline198 = @intrinsic(eq.primitive)(t167, inline197) in
                    jump inline_cont196(inline198)
                  } in
                  let inline200 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont199(inline200)
                } in
                let inline202 = @intrinsic(ref.set)(i/0, t162) in
                jump inline_cont201(())
              } in
              let inline204 = @intrinsic(ref.get)(i/0) in
              jump inline_cont203(inline204)
            } else {
              jump exit146()
            }
          } in
          let inline206 = @intrinsic(ref.get)(i/0) in
          jump inline_cont205(inline206)
        } in
      } in
      jump loop158()
    } in
    let inline236 = 0 in
    let inline237 = @intrinsic(ref.new)(inline236) in
    jump inline_cont235(inline237)
  } in
  let inline239 = 0 in
  let inline240 = @intrinsic(ref.new)(inline239) in
  jump inline_cont238(inline240)
}

fn inherent#Ref#Ref[T]#new__T_int(value/215: int) -> Ref[int] {
  let t172 = @intrinsic(ref.new)(value/215) in
  t172
}

fn inherent#Ref#Ref[T]#get__T_int(self/216: Ref[int]) -> int {
  let t175 = @intrinsic(ref.get)(self/216) in
  t175
}

fn inherent#Ref#Ref[T]#set__T_int(self/217: Ref[int], value/218: int) -> unit {
  let t177 = @intrinsic(ref.set)(self/217, value/218) in
  ()
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t180 = @intrinsic(eq.primitive)(self/88, other/89) in
  t180
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont241(t182: string) -> unit {
    let t183 = @runtime(core.string_println)(t182) in
    ()
  } in
  let inline242 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont241(inline242)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t186 = @runtime(core.int_to_string)(self/69) in
  t186
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t189 = @runtime(core.int_hash)(self/114) in
  t189
}