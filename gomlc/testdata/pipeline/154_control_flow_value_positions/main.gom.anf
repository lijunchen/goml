fn main() -> unit {
  join inline_cont284(i/0: Ref[int]) -> unit {
    join inline_cont281(sum/1: Ref[int]) -> unit {
      join exit192() -> unit {
        join inline_cont279(t193: int) -> unit {
          join inline_cont276(_wild185: unit) -> unit {
            join inline_cont273(j/3: Ref[int]) -> unit {
              join inline_cont270(total/4: Ref[int]) -> unit {
                join exit194() -> unit {
                  join inline_cont268(t195: int) -> unit {
                    let inline266 = trait_impl#ToString#int#to_string(t195) in
                    let inline267 = @runtime(core.string_println)(inline266) in
                    ()
                  } in
                  let inline269 = @intrinsic(ref.get)(total/4) in
                  jump inline_cont268(inline269)
                } in
                joinrec {
                  join loop196() -> unit {
                    join inline_cont263(t197: int) -> unit {
                      let t198 = (t197 + 1) in
                      join inline_cont261(_wild186: unit) -> unit {
                        join inline_cont259(mtmp187: int) -> unit {
                          join k199(jp200: int) -> unit {
                            join inline_cont257(t201: int) -> unit {
                              let t202 = (t201 + jp200) in
                              let inline256 = @intrinsic(ref.set)(total/4, t202) in
                              jump loop196()
                            } in
                            let inline258 = @intrinsic(ref.get)(total/4) in
                            jump inline_cont257(inline258)
                          } in
                          match mtmp187 {
                            5 => {
                              jump exit194()
                            },
                            _ => {
                              let inline254 = @intrinsic(ref.get)(j/3) in
                              jump k199(inline254)
                            },
                          }
                        } in
                        let inline260 = @intrinsic(ref.get)(j/3) in
                        jump inline_cont259(inline260)
                      } in
                      let inline262 = @intrinsic(ref.set)(j/3, t198) in
                      jump inline_cont261(())
                    } in
                    let inline264 = @intrinsic(ref.get)(j/3) in
                    jump inline_cont263(inline264)
                  } in
                } in
                jump loop196()
              } in
              let inline271 = 0 in
              let inline272 = @intrinsic(ref.new)(inline271) in
              jump inline_cont270(inline272)
            } in
            let inline274 = 0 in
            let inline275 = @intrinsic(ref.new)(inline274) in
            jump inline_cont273(inline275)
          } in
          let inline277 = trait_impl#ToString#int#to_string(t193) in
          let inline278 = @runtime(core.string_println)(inline277) in
          jump inline_cont276(())
        } in
        let inline280 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont279(inline280)
      } in
      joinrec {
        join loop204() -> unit {
          join inline_cont251(t205: int) -> unit {
            let t206 = (t205 < 5) in
            if t206 {
              join inline_cont249(t207: int) -> unit {
                let t208 = (t207 + 1) in
                join inline_cont247(_wild182: unit) -> unit {
                  join inline_cont245(t213: int) -> unit {
                    join inline_cont242(t214: bool) -> unit {
                      join k209(jp210: int) -> unit {
                        join inline_cont240(t211: int) -> unit {
                          let t212 = (t211 + jp210) in
                          let inline239 = @intrinsic(ref.set)(sum/1, t212) in
                          jump loop204()
                        } in
                        let inline241 = @intrinsic(ref.get)(sum/1) in
                        jump inline_cont240(inline241)
                      } in
                      if t214 {
                        jump loop204()
                      } else {
                        let inline237 = @intrinsic(ref.get)(i/0) in
                        jump k209(inline237)
                      }
                    } in
                    let inline243 = 3 in
                    let inline244 = (t213 == inline243) in
                    jump inline_cont242(inline244)
                  } in
                  let inline246 = @intrinsic(ref.get)(i/0) in
                  jump inline_cont245(inline246)
                } in
                let inline248 = @intrinsic(ref.set)(i/0, t208) in
                jump inline_cont247(())
              } in
              let inline250 = @intrinsic(ref.get)(i/0) in
              jump inline_cont249(inline250)
            } else {
              jump exit192()
            }
          } in
          let inline252 = @intrinsic(ref.get)(i/0) in
          jump inline_cont251(inline252)
        } in
      } in
      jump loop204()
    } in
    let inline282 = 0 in
    let inline283 = @intrinsic(ref.new)(inline282) in
    jump inline_cont281(inline283)
  } in
  let inline285 = 0 in
  let inline286 = @intrinsic(ref.new)(inline285) in
  jump inline_cont284(inline286)
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t218 = @intrinsic(ref.new)(value/270) in
  t218
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t221 = @intrinsic(ref.get)(self/271) in
  t221
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t223 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t226 = (self/101 == other/102) in
  t226
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont287(t228: string) -> unit {
    let t229 = @runtime(core.string_println)(t228) in
    ()
  } in
  let inline288 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont287(inline288)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t232 = @runtime(core.int_to_string)(self/67) in
  t232
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t235 = @runtime(core.int_hash)(self/127) in
  t235
}