fn main() -> unit {
  join ret77() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit78() -> unit {
    let _wild70 = () in
    let t79 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild71 = println__T_int(t79) in
    let j/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/4 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit80() -> unit {
      let _wild75 = () in
      let t81 = inherent#Ref#Ref[T]#get__T_int(total/4) in
      let _wild76 = println__T_int(t81) in
      jump ret77()
    } in
    joinrec {
      join loop82() -> unit {
        if true {
          let t83 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          let t84 = (t83 + 1) in
          let _wild72 = inherent#Ref#Ref[T]#set__T_int(j/3, t84) in
          let mtmp73 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          join k85(jp86: int) -> unit {
            let cur/5 = jp86 in
            let t87 = inherent#Ref#Ref[T]#get__T_int(total/4) in
            let t88 = (t87 + cur/5) in
            let _wild74 = inherent#Ref#Ref[T]#set__T_int(total/4, t88) in
            jump loop82()
          } in
          match mtmp73 {
            5 => {
              jump exit80()
            },
            _ => {
              let t89 = inherent#Ref#Ref[T]#get__T_int(j/3) in
              jump k85(t89)
            },
          }
        } else {
          jump exit80()
        }
      } in
    } in
    jump loop82()
  } in
  joinrec {
    join loop90() -> unit {
      let t91 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t92 = (t91 < 5) in
      if t92 {
        let t93 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t94 = (t93 + 1) in
        let _wild68 = inherent#Ref#Ref[T]#set__T_int(i/0, t94) in
        let t99 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t100 = trait_impl#Eq#int#eq(t99, 3) in
        join k95(jp96: int) -> unit {
          let cur/2 = jp96 in
          let t97 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t98 = (t97 + cur/2) in
          let _wild69 = inherent#Ref#Ref[T]#set__T_int(sum/1, t98) in
          jump loop90()
        } in
        if t100 {
          jump loop90()
        } else {
          let t101 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          jump k95(t101)
        }
      } else {
        jump exit78()
      }
    } in
  } in
  jump loop90()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret102(retv103: Ref[int]) -> Ref[int] {
    retv103
  } in
  let t104 = @intrinsic(ref.new)(value/207) in
  jump ret102(t104)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret105(retv106: int) -> int {
    retv106
  } in
  let t107 = @intrinsic(ref.get)(self/208) in
  jump ret105(t107)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret108() -> unit {
    ()
  } in
  let t109 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret108()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret110(retv111: bool) -> bool {
    retv111
  } in
  let t112 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret110(t112)
}

fn println__T_int(value/1: int) -> unit {
  join ret113() -> unit {
    ()
  } in
  let t114 = trait_impl#ToString#int#to_string(value/1) in
  let t115 = @runtime(core.string_println)(t114) in
  jump ret113()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret116(retv117: string) -> string {
    retv117
  } in
  let t118 = @runtime(core.int_to_string)(self/40) in
  jump ret116(t118)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret119(retv120: uint64) -> uint64 {
    retv120
  } in
  let t121 = @runtime(core.int_hash)(self/85) in
  jump ret119(t121)
}