fn sum_to(limit/0: i32) -> i32 {
  join inline_cont0(acc/0: Ref[i32]) -> i32 {
    join inline_cont1(i/0: Ref[i32]) -> i32 {
      join exit0() -> i32 {
        let inline0 = @intrinsic(ref.get)(acc/0) in
        inline0
      } in
      joinrec {
        join loop0() -> i32 {
          join inline_cont2(t0: i32) -> i32 {
            let t1 = (t0 < limit/0) in
            if t1 {
              join inline_cont3(current/0: i32) -> i32 {
                join inline_cont4(t2: i32) -> i32 {
                  let t3 = (t2 + current/0) in
                  join inline_cont5(mtmp0: unit) -> i32 {
                    let t4 = (current/0 + 1) in
                    let inline1 = @intrinsic(ref.set)(i/0, t4) in
                    jump loop0()
                  } in
                  let inline2 = @intrinsic(ref.set)(acc/0, t3) in
                  jump inline_cont5(())
                } in
                let inline3 = @intrinsic(ref.get)(acc/0) in
                jump inline_cont4(inline3)
              } in
              let inline4 = @intrinsic(ref.get)(i/0) in
              jump inline_cont3(inline4)
            } else {
              jump exit0()
            }
          } in
          let inline5 = @intrinsic(ref.get)(i/0) in
          jump inline_cont2(inline5)
        } in
      } in
      jump loop0()
    } in
    let inline6 = 0 in
    let inline7 = @intrinsic(ref.new)(inline6) in
    jump inline_cont1(inline7)
  } in
  let inline8 = 0 in
  let inline9 = @intrinsic(ref.new)(inline8) in
  jump inline_cont0(inline9)
}

fn sum_even(limit/0: i32) -> i32 {
  join inline_cont0(acc/0: Ref[i32]) -> i32 {
    join inline_cont1(i/0: Ref[i32]) -> i32 {
      join inline_cont2(is_even/0: Ref[bool]) -> i32 {
        join exit0() -> i32 {
          let inline0 = @intrinsic(ref.get)(acc/0) in
          inline0
        } in
        joinrec {
          join loop0() -> i32 {
            join inline_cont3(t0: i32) -> i32 {
              let t1 = (t0 < limit/0) in
              if t1 {
                join inline_cont4(current/0: i32) -> i32 {
                  let t2 = (current/0 + 1) in
                  join inline_cont5(mtmp0: unit) -> i32 {
                    join inline_cont6(add_now/0: bool) -> i32 {
                      let t3 = (!add_now/0) in
                      join inline_cont7(mtmp1: unit) -> i32 {
                        if add_now/0 {
                          join inline_cont8(t4: i32) -> i32 {
                            let t5 = (t4 + current/0) in
                            let inline1 = @intrinsic(ref.set)(acc/0, t5) in
                            jump loop0()
                          } in
                          let inline2 = @intrinsic(ref.get)(acc/0) in
                          jump inline_cont8(inline2)
                        } else {
                          jump loop0()
                        }
                      } in
                      let inline3 = @intrinsic(ref.set)(is_even/0, t3) in
                      jump inline_cont7(())
                    } in
                    let inline4 = @intrinsic(ref.get)(is_even/0) in
                    jump inline_cont6(inline4)
                  } in
                  let inline5 = @intrinsic(ref.set)(i/0, t2) in
                  jump inline_cont5(())
                } in
                let inline6 = @intrinsic(ref.get)(i/0) in
                jump inline_cont4(inline6)
              } else {
                jump exit0()
              }
            } in
            let inline7 = @intrinsic(ref.get)(i/0) in
            jump inline_cont3(inline7)
          } in
        } in
        jump loop0()
      } in
      let inline8 = true in
      let inline9 = @intrinsic(ref.new)(inline8) in
      jump inline_cont2(inline9)
    } in
    let inline10 = 0 in
    let inline11 = @intrinsic(ref.new)(inline10) in
    jump inline_cont1(inline11)
  } in
  let inline12 = 0 in
  let inline13 = @intrinsic(ref.new)(inline12) in
  jump inline_cont0(inline13)
}

fn main() -> unit {
  let first/0 = sum_to(5) in
  let evens/0 = sum_even(6) in
  join inline_cont0(mtmp0: unit) -> unit {
    join inline_cont1(mtmp1: unit) -> unit {
      join inline_cont2(mtmp2: unit) -> unit {
        let inline0 = trait_impl#ToString#i32#to_string(evens/0) in
        let inline1 = @runtime(core.string_println)(inline0) in
        ()
      } in
      let inline2 = "sum_even(6)=" in
      let inline3 = trait_impl#ToString#string#to_string(inline2) in
      let inline4 = @runtime(core.string_print)(inline3) in
      jump inline_cont2(())
    } in
    let inline5 = trait_impl#ToString#i32#to_string(first/0) in
    let inline6 = @runtime(core.string_println)(inline5) in
    jump inline_cont1(())
  } in
  let inline7 = "sum_to(5)=" in
  let inline8 = trait_impl#ToString#string#to_string(inline7) in
  let inline9 = @runtime(core.string_print)(inline8) in
  jump inline_cont0(())
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn inherent#Ref#Ref[T]#new__T_bool(value/0: bool) -> Ref[bool] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_bool(self/0: Ref[bool]) -> bool {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_bool(self/0: Ref[bool], value/0: bool) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_print)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn println__T_i32(value/0: i32) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int32_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn trait_impl#PartialEq#bool#eq(self/0: bool, other/0: bool) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#bool#hash(self/0: bool) -> u64 {
  if self/0 {
    1
  } else {
    0
  }
}