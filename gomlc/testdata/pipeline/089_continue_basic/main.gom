fn main() {
    let i = Ref::new(0);
    while i.get() < 10 {
        i.set(i.get() + 1);
        if i.get() == 5 {
            continue
        } else {};
        let _ = println(i.get());
    }
    let _ = println("done");
}
