fn cut_prefix(case_id/0: int32) -> Option__string {
  let t181 = (case_id/0 == 0) in
  if t181 {
    let t182 = Option__string::Some("ml") in
    t182
  } else {
    Tag_0
  }
}

fn trim_go(case_id/1: int32) -> Option__string {
  join inline_cont204(mtmp172: Option__string) -> Option__string {
    join k185(jp186: string) -> Option__string {
      let t187 = (jp186 + "!") in
      let t188 = Option__string::Some(t187) in
      t188
    } in
    match mtmp172 {
      Tag_0 => {
        Tag_0
      },
      Tag_1 => {
        let x173 = Option__string::Some._0(mtmp172) in
        jump k185(x173)
      },
    }
  } in
  let inline205 = (case_id/1 == 0) in
  if inline205 {
    let inline206 = Option__string::Some("ml") in
    jump inline_cont204(inline206)
  } else {
    jump inline_cont204(Tag_0)
  }
}

fn show(opt/3: Option__string) -> string {
  match opt/3 {
    Tag_0 => {
      "none"
    },
    Tag_1 => {
      let x174 = Option__string::Some._0(opt/3) in
      let t193 = ("some " + x174) in
      t193
    },
  }
}

fn main() -> unit {
  join inline_cont231(t195: Option__string) -> unit {
    join inline_cont227(t196: string) -> unit {
      join inline_cont224(_wild175: unit) -> unit {
        join inline_cont214(t197: Option__string) -> unit {
          join inline_cont210(t198: string) -> unit {
            let inline208 = trait_impl#ToString#string#to_string(t198) in
            let inline209 = @runtime(core.string_println)(inline208) in
            ()
          } in
          match t197 {
            Tag_0 => {
              jump inline_cont210("none")
            },
            Tag_1 => {
              let inline211 = Option__string::Some._0(t197) in
              let inline213 = ("some " + inline211) in
              jump inline_cont210(inline213)
            },
          }
        } in
        let inline215 = 1 in
        let inline216 = cut_prefix(inline215) in
        join inline_join217(inline218: string) -> unit {
          let inline220 = (inline218 + "!") in
          let inline221 = Option__string::Some(inline220) in
          jump inline_cont214(inline221)
        } in
        match inline216 {
          Tag_0 => {
            jump inline_cont214(Tag_0)
          },
          Tag_1 => {
            let inline222 = Option__string::Some._0(inline216) in
            jump inline_join217(inline222)
          },
        }
      } in
      let inline225 = trait_impl#ToString#string#to_string(t196) in
      let inline226 = @runtime(core.string_println)(inline225) in
      jump inline_cont224(())
    } in
    match t195 {
      Tag_0 => {
        jump inline_cont227("none")
      },
      Tag_1 => {
        let inline228 = Option__string::Some._0(t195) in
        let inline230 = ("some " + inline228) in
        jump inline_cont227(inline230)
      },
    }
  } in
  let inline232 = 0 in
  let inline233 = cut_prefix(inline232) in
  join inline_join234(inline235: string) -> unit {
    let inline237 = (inline235 + "!") in
    let inline238 = Option__string::Some(inline237) in
    jump inline_cont231(inline238)
  } in
  match inline233 {
    Tag_0 => {
      jump inline_cont231(Tag_0)
    },
    Tag_1 => {
      let inline239 = Option__string::Some._0(inline233) in
      jump inline_join234(inline239)
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont241(t200: string) -> unit {
    let t201 = @runtime(core.string_println)(t200) in
    ()
  } in
  jump inline_cont241(value/1)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}