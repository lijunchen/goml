fn touch(counter/0: Ref[isize]) -> isize {
  join inline_cont869(t800: isize) -> isize {
    let next/1 = (t800 + 1) in
    let inline868 = @intrinsic(ref.set)(counter/0, next/1) in
    next/1
  } in
  let inline870 = @intrinsic(ref.get)(counter/0) in
  jump inline_cont869(inline870)
}

fn main() -> unit {
  join inline_cont880(counter/8: Ref[isize]) -> unit {
    join inline_cont876(ignored/9: isize) -> unit {
      let divisor/10 = 2 in
      let quotient/11 = (8 / divisor/10) in
      join inline_cont874(t802: isize) -> unit {
        let inline872 = trait_impl#ToString#isize#to_string(t802) in
        let inline873 = @runtime(core.string_println)(inline872) in
        ()
      } in
      let inline875 = @intrinsic(ref.get)(counter/8) in
      jump inline_cont874(inline875)
    } in
    let inline877 = inherent#Ref#Ref[T]#get__T_isize(counter/8) in
    let inline878 = (inline877 + 1) in
    let inline879 = inherent#Ref#Ref[T]#set__T_isize(counter/8, inline878) in
    jump inline_cont876(inline878)
  } in
  let inline881 = 0 in
  let inline882 = @intrinsic(ref.new)(inline881) in
  jump inline_cont880(inline882)
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t806 = @intrinsic(ref.get)(self/685) in
  t806
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t808 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t811 = @intrinsic(ref.new)(value/684) in
  t811
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont883(t813: string) -> unit {
    let t814 = @runtime(core.string_println)(t813) in
    ()
  } in
  let inline884 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont883(inline884)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline886 = (self/404 as i64) in
  let inline887 = signed_decimal_string(inline886) in
  inline887
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t820 = (value/222 as i64) in
  let inline889 = (t820 < 0) in
  if inline889 {
    let inline890 = (t820 as u64) in
    let inline891 = (0 - inline890) in
    let inline892 = decimal_string(inline891) in
    let inline893 = ("-" + inline892) in
    inline893
  } else {
    let inline894 = (t820 as u64) in
    let inline895 = decimal_string(inline894) in
    inline895
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t826 = (value/214 < 0) in
  if t826 {
    let t827 = (value/214 as u64) in
    let t828 = (0 - t827) in
    let t829 = decimal_string(t828) in
    let t830 = ("-" + t829) in
    t830
  } else {
    let t831 = (value/214 as u64) in
    let t832 = decimal_string(t831) in
    t832
  }
}

fn decimal_string(value/208: u64) -> string {
  let t855 = (value/208 == 0) in
  join k835() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit836() -> string {
      join inline_cont904(t837: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t837) in
        let offset/212 = 0 in
        join exit838() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop839() -> string {
            join inline_cont902(t840: isize) -> string {
              let t841 = (offset/212 < t840) in
              if t841 {
                join inline_cont900(t842: isize) -> string {
                  let t843 = (t842 - offset/212) in
                  let t844 = (t843 - 1) in
                  let t845 = @intrinsic(vec.get)(reversed/209, t844) in
                  join inline_cont898(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t846 = (compound_old358 + compound_value359) in
                    let t847 = offset/212 = t846 in
                    jump loop839()
                  } in
                  let inline899 = @intrinsic(vec.push)(bytes/211, t845) in
                  jump inline_cont898(())
                } in
                let inline901 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont900(inline901)
              } else {
                jump exit838()
              }
            } in
            let inline903 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont902(inline903)
          } in
        } in
        jump loop839()
      } in
      let inline905 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont904(inline905)
    } in
    joinrec {
      join loop848() -> string {
        let t849 = (remaining/210 > 0) in
        if t849 {
          let t850 = (remaining/210 % 10) in
          let t851 = (t850 as u8) in
          let t852 = (t851 + 48) in
          join inline_cont896(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t853 = (compound_old353 / compound_value354) in
            let t854 = remaining/210 = t853 in
            jump loop848()
          } in
          let inline897 = @intrinsic(vec.push)(reversed/209, t852) in
          jump inline_cont896(())
        } else {
          jump exit836()
        }
      } in
    } in
    jump loop848()
  } in
  if t855 {
    "0"
  } else {
    jump k835()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t857 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t860 = @intrinsic(vec.len)(self/526) in
  t860
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t863 = (self/438 == other/439) in
  t863
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t866 = @runtime(core.int_hash)(self/464) in
  t866
}