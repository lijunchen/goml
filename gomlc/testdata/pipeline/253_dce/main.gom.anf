fn touch(counter/0: Ref[int]) -> int {
  join inline_cont202(t176: int) -> int {
    let next/1 = (t176 + 1) in
    let inline201 = @intrinsic(ref.set)(counter/0, next/1) in
    next/1
  } in
  let inline203 = @intrinsic(ref.get)(counter/0) in
  jump inline_cont202(inline203)
}

fn main() -> unit {
  join inline_cont213(counter/8: Ref[int]) -> unit {
    join inline_cont209(ignored/9: int) -> unit {
      let divisor/10 = 2 in
      let quotient/11 = (8 / divisor/10) in
      join inline_cont207(t178: int) -> unit {
        let inline205 = trait_impl#ToString#int#to_string(t178) in
        let inline206 = @runtime(core.string_println)(inline205) in
        ()
      } in
      let inline208 = @intrinsic(ref.get)(counter/8) in
      jump inline_cont207(inline208)
    } in
    let inline210 = inherent#Ref#Ref[T]#get__T_int(counter/8) in
    let inline211 = (inline210 + 1) in
    let inline212 = inherent#Ref#Ref[T]#set__T_int(counter/8, inline211) in
    jump inline_cont209(inline211)
  } in
  let inline214 = 0 in
  let inline215 = @intrinsic(ref.new)(inline214) in
  jump inline_cont213(inline215)
}

fn inherent#Ref#Ref[T]#get__T_int(self/256: Ref[int]) -> int {
  let t182 = @intrinsic(ref.get)(self/256) in
  t182
}

fn inherent#Ref#Ref[T]#set__T_int(self/257: Ref[int], value/258: int) -> unit {
  let t184 = @intrinsic(ref.set)(self/257, value/258) in
  ()
}

fn inherent#Ref#Ref[T]#new__T_int(value/255: int) -> Ref[int] {
  let t187 = @intrinsic(ref.new)(value/255) in
  t187
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont216(t189: string) -> unit {
    let t190 = @runtime(core.string_println)(t189) in
    ()
  } in
  let inline217 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont216(inline217)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t193 = @runtime(core.int_to_string)(self/67) in
  t193
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t196 = (self/101 == other/102) in
  t196
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t199 = @runtime(core.int_hash)(self/127) in
  t199
}