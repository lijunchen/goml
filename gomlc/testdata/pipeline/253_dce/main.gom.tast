fn touch(counter/0: Ref[isize]) -> isize {
    let next/1: isize = (Ref[isize]::get : (Ref[isize]) -> isize)((counter/0 : Ref[isize])) + 1;
    (Ref[isize]::set : (Ref[isize], isize) -> unit)((counter/0 : Ref[isize]), (next/1 : isize));
    (next/1 : isize)
}

fn main() -> unit {
    let dead/2: isize = 1;
    let dead_pair/3: (isize, isize) = ((dead/2 : isize), 2);
    let dead_if/4: isize =
      if true {
          {
              3
          }
      } else {
          {
              4
          }
      };
    let dead_match/5: isize =
      match false {
          true => 5,
          false => 6,
      };
    let dead_closure/7: (isize) -> isize = |value/6: isize| => (value/6 : isize) + (dead/2 : isize);
    let counter/8: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(0);
    let ignored/9: isize = (touch : (Ref[isize]) -> isize)((counter/8 : Ref[isize]));
    let divisor/10: isize = 2;
    let quotient/11: isize = 8 / (divisor/10 : isize);
    (println : (isize) -> unit)((Ref[isize]::get : (Ref[isize]) -> isize)((counter/8 : Ref[isize])))
}