fn touch(counter/0: Ref[int]) -> int {
    let next/1: int = (Ref[int]::get : (Ref[int]) -> int)((counter/0 : Ref[int])) + 1;
    (Ref[int]::set : (Ref[int], int) -> unit)((counter/0 : Ref[int]), (next/1 : int));
    (next/1 : int)
}

fn main() -> unit {
    let dead/2: int = 1;
    let dead_pair/3: (int, int) = ((dead/2 : int), 2);
    let dead_if/4: int =
      if true {
          {
              3
          }
      } else {
          {
              4
          }
      };
    let dead_match/5: int =
      match false {
          true => 5,
          false => 6,
      };
    let dead_closure/7: (int) -> int = |value/6: int| => (value/6 : int) + (dead/2 : int);
    let counter/8: Ref[int] = (Ref[int]::new : (int) -> Ref[int])(0);
    let ignored/9: int = (touch : (Ref[int]) -> int)((counter/8 : Ref[int]));
    let divisor/10: int = 2;
    let quotient/11: int = 8 / (divisor/10 : int);
    (println : (int) -> unit)((Ref[int]::get : (Ref[int]) -> int)((counter/8 : Ref[int])))
}