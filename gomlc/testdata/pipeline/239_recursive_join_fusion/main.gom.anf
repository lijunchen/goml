fn find(wanted/0: int) -> Option__int {
  join inline_cont223(current/1: Ref[int]) -> Option__int {
    joinrec {
      join loop_expr185() -> Option__int {
        join inline_cont221(value/2: int) -> Option__int {
          let t187 = (value/2 >= 5) in
          join k186() -> Option__int {
            jump loop_expr185()
          } in
          if t187 {
            Tag_0
          } else {
            let t189 = (value/2 == wanted/0) in
            if t189 {
              let t190 = Option__int::Some(value/2) in
              t190
            } else {
              let t191 = (value/2 + 1) in
              let inline220 = @intrinsic(ref.set)(current/1, t191) in
              jump k186()
            }
          }
        } in
        let inline222 = @intrinsic(ref.get)(current/1) in
        jump inline_cont221(inline222)
      } in
    } in
    jump loop_expr185()
  } in
  let inline224 = 0 in
  let inline225 = @intrinsic(ref.new)(inline224) in
  jump inline_cont223(inline225)
}

fn any(wanted/3: int) -> bool {
  let inline227 = inherent#Ref#Ref[T]#new__T_int(0) in
  joinrec {
    join inline_join228() -> bool {
      let inline229 = inherent#Ref#Ref[T]#get__T_int(inline227) in
      let inline230 = (inline229 >= 5) in
      join inline_join231() -> bool {
        jump inline_join228()
      } in
      if inline230 {
        false
      } else {
        let inline233 = (inline229 == wanted/3) in
        if inline233 {
          true
        } else {
          let inline235 = (inline229 + 1) in
          let inline236 = inherent#Ref#Ref[T]#set__T_int(inline227, inline235) in
          jump inline_join231()
        }
      }
    } in
  } in
  jump inline_join228()
}

fn main() -> unit {
  join inline_cont246(t197: bool) -> unit {
    join inline_cont243(_wild179: unit) -> unit {
      join inline_cont240(t198: bool) -> unit {
        let inline238 = trait_impl#ToString#bool#to_string(t198) in
        let inline239 = @runtime(core.string_println)(inline238) in
        ()
      } in
      let inline241 = 8 in
      let inline242 = find(inline241) in
      match inline242 {
        Tag_0 => {
          jump inline_cont240(false)
        },
        Tag_1 => {
          jump inline_cont240(true)
        },
      }
    } in
    let inline244 = trait_impl#ToString#bool#to_string(t197) in
    let inline245 = @runtime(core.string_println)(inline244) in
    jump inline_cont243(())
  } in
  let inline247 = 3 in
  let inline248 = find(inline247) in
  match inline248 {
    Tag_0 => {
      jump inline_cont246(false)
    },
    Tag_1 => {
      jump inline_cont246(true)
    },
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/255: int) -> Ref[int] {
  let t201 = @intrinsic(ref.new)(value/255) in
  t201
}

fn inherent#Ref#Ref[T]#get__T_int(self/256: Ref[int]) -> int {
  let t204 = @intrinsic(ref.get)(self/256) in
  t204
}

fn inherent#Ref#Ref[T]#set__T_int(self/257: Ref[int], value/258: int) -> unit {
  let t206 = @intrinsic(ref.set)(self/257, value/258) in
  ()
}

fn println__T_bool(value/1: bool) -> unit {
  join inline_cont249(t208: string) -> unit {
    let t209 = @runtime(core.string_println)(t208) in
    ()
  } in
  let inline250 = @runtime(core.bool_to_string)(value/1) in
  jump inline_cont249(inline250)
}

fn trait_impl#ToString#bool#to_string(self/64: bool) -> string {
  let t212 = @runtime(core.bool_to_string)(self/64) in
  t212
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t215 = (self/101 == other/102) in
  t215
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t218 = @runtime(core.int_hash)(self/127) in
  t218
}