fn find(wanted/0: isize) -> Option__isize {
  join inline_cont0(current/0: Ref[isize]) -> Option__isize {
    joinrec {
      join loop_expr0() -> Option__isize {
        join inline_cont1(value/0: isize) -> Option__isize {
          let t0 = (value/0 >= 5) in
          join k0() -> Option__isize {
            jump loop_expr0()
          } in
          if t0 {
            Tag_0
          } else {
            let t1 = (value/0 == wanted/0) in
            if t1 {
              let t2 = Option__isize::Some(value/0) in
              t2
            } else {
              let t3 = (value/0 + 1) in
              let inline0 = @intrinsic(ref.set)(current/0, t3) in
              jump k0()
            }
          }
        } in
        let inline1 = @intrinsic(ref.get)(current/0) in
        jump inline_cont1(inline1)
      } in
    } in
    jump loop_expr0()
  } in
  let inline2 = 0 in
  let inline3 = @intrinsic(ref.new)(inline2) in
  jump inline_cont0(inline3)
}

fn any(wanted/0: isize) -> bool {
  let inline0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  joinrec {
    join inline_join0() -> bool {
      let inline1 = inherent#Ref#Ref[T]#get__T_isize(inline0) in
      let inline2 = (inline1 >= 5) in
      join inline_join1() -> bool {
        jump inline_join0()
      } in
      if inline2 {
        false
      } else {
        let inline3 = (inline1 == wanted/0) in
        if inline3 {
          true
        } else {
          let inline4 = (inline1 + 1) in
          let inline5 = inherent#Ref#Ref[T]#set__T_isize(inline0, inline4) in
          jump inline_join1()
        }
      }
    } in
  } in
  jump inline_join0()
}

fn main() -> unit {
  join inline_cont0(t0: bool) -> unit {
    join inline_cont1(_wild0: unit) -> unit {
      join inline_cont2(t1: bool) -> unit {
        let inline0 = trait_impl#ToString#bool#to_string(t1) in
        let inline1 = @runtime(core.string_println)(inline0) in
        ()
      } in
      let inline2 = 8 in
      let inline3 = find(inline2) in
      match inline3 {
        Tag_0 => {
          jump inline_cont2(false)
        },
        Tag_1 => {
          jump inline_cont2(true)
        },
      }
    } in
    let inline4 = trait_impl#ToString#bool#to_string(t0) in
    let inline5 = @runtime(core.string_println)(inline4) in
    jump inline_cont1(())
  } in
  let inline6 = 3 in
  let inline7 = find(inline6) in
  match inline7 {
    Tag_0 => {
      jump inline_cont0(false)
    },
    Tag_1 => {
      jump inline_cont0(true)
    },
  }
}

fn inherent#Ref#Ref[T]#new__T_isize(value/0: isize) -> Ref[isize] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_isize(self/0: Ref[isize]) -> isize {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_isize(self/0: Ref[isize], value/0: isize) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_bool(value/0: bool) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}