fn find(wanted/0: int) -> Option__int {
  join inline_cont233(current/1: Ref[int]) -> Option__int {
    joinrec {
      join loop_expr195() -> Option__int {
        join inline_cont231(value/2: int) -> Option__int {
          let t197 = (value/2 >= 5) in
          join k196() -> Option__int {
            jump loop_expr195()
          } in
          if t197 {
            Tag_0
          } else {
            let t199 = (value/2 == wanted/0) in
            if t199 {
              let t200 = Option__int::Some(value/2) in
              t200
            } else {
              let t201 = (value/2 + 1) in
              let inline230 = @intrinsic(ref.set)(current/1, t201) in
              jump k196()
            }
          }
        } in
        let inline232 = @intrinsic(ref.get)(current/1) in
        jump inline_cont231(inline232)
      } in
    } in
    jump loop_expr195()
  } in
  let inline234 = 0 in
  let inline235 = @intrinsic(ref.new)(inline234) in
  jump inline_cont233(inline235)
}

fn any(wanted/3: int) -> bool {
  let inline237 = inherent#Ref#Ref[T]#new__T_int(0) in
  joinrec {
    join inline_join238() -> bool {
      let inline239 = inherent#Ref#Ref[T]#get__T_int(inline237) in
      let inline240 = (inline239 >= 5) in
      join inline_join241() -> bool {
        jump inline_join238()
      } in
      if inline240 {
        false
      } else {
        let inline243 = (inline239 == wanted/3) in
        if inline243 {
          true
        } else {
          let inline245 = (inline239 + 1) in
          let inline246 = inherent#Ref#Ref[T]#set__T_int(inline237, inline245) in
          jump inline_join241()
        }
      }
    } in
  } in
  jump inline_join238()
}

fn main() -> unit {
  join inline_cont256(t207: bool) -> unit {
    join inline_cont253(_wild189: unit) -> unit {
      join inline_cont250(t208: bool) -> unit {
        let inline248 = trait_impl#ToString#bool#to_string(t208) in
        let inline249 = @runtime(core.string_println)(inline248) in
        ()
      } in
      let inline251 = 8 in
      let inline252 = find(inline251) in
      match inline252 {
        Tag_0 => {
          jump inline_cont250(false)
        },
        Tag_1 => {
          jump inline_cont250(true)
        },
      }
    } in
    let inline254 = trait_impl#ToString#bool#to_string(t207) in
    let inline255 = @runtime(core.string_println)(inline254) in
    jump inline_cont253(())
  } in
  let inline257 = 3 in
  let inline258 = find(inline257) in
  match inline258 {
    Tag_0 => {
      jump inline_cont256(false)
    },
    Tag_1 => {
      jump inline_cont256(true)
    },
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t211 = @intrinsic(ref.new)(value/270) in
  t211
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t214 = @intrinsic(ref.get)(self/271) in
  t214
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t216 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn println__T_bool(value/1: bool) -> unit {
  join inline_cont259(t218: string) -> unit {
    let t219 = @runtime(core.string_println)(t218) in
    ()
  } in
  let inline260 = @runtime(core.bool_to_string)(value/1) in
  jump inline_cont259(inline260)
}

fn trait_impl#ToString#bool#to_string(self/64: bool) -> string {
  let t222 = @runtime(core.bool_to_string)(self/64) in
  t222
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t225 = (self/101 == other/102) in
  t225
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t228 = @runtime(core.int_hash)(self/127) in
  t228
}