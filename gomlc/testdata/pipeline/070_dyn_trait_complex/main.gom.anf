fn trait_impl#Display#Point#show(self/0: Point) -> string {
  let t0 = Point.x(self/0) in
  join inline_cont0(t1: string) -> string {
    let t2 = ("Point(" + t1) in
    let t3 = (t2 + ",") in
    let t4 = Point.y(self/0) in
    join inline_cont1(t5: string) -> string {
      let t6 = (t3 + t5) in
      let t7 = (t6 + ")") in
      t7
    } in
    let inline0 = __goml_builtin_int32_to_string(t4) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(t0) in
  jump inline_cont0(inline1)
}

fn trait_impl#Display#Point#show_with(self/0: Point, prefix/0: string, suffix/0: string) -> string {
  let t0 = (prefix/0 + "Point(") in
  let t1 = Point.x(self/0) in
  join inline_cont0(t2: string) -> string {
    let t3 = (t0 + t2) in
    let t4 = (t3 + ",") in
    let t5 = Point.y(self/0) in
    join inline_cont1(t6: string) -> string {
      let t7 = (t4 + t6) in
      let t8 = (t7 + ")") in
      let t9 = (t8 + suffix/0) in
      t9
    } in
    let inline0 = __goml_builtin_int32_to_string(t5) in
    jump inline_cont1(inline0)
  } in
  let inline1 = __goml_builtin_int32_to_string(t1) in
  jump inline_cont0(inline1)
}

fn trait_impl#Display#Point#tick(self/0: Point) -> () {
  ()
}

fn trait_impl#Display#Point#bump(self/0: Point, delta/0: i32) -> i32 {
  let t0 = Point.x(self/0) in
  let t1 = Point.y(self/0) in
  let t2 = (t0 + t1) in
  let t3 = (t2 + delta/0) in
  t3
}

fn trait_impl#Display#Flag#show(self/0: Flag) -> string {
  let t0 = Flag.value(self/0) in
  if t0 {
    "Flag(true)"
  } else {
    "Flag(false)"
  }
}

fn trait_impl#Display#Flag#show_with(self/0: Flag, prefix/0: string, suffix/0: string) -> string {
  let t0 = Flag.value(self/0) in
  if t0 {
    let t1 = (prefix/0 + "Flag(true)") in
    let t2 = (t1 + suffix/0) in
    t2
  } else {
    let t3 = (prefix/0 + "Flag(false)") in
    let t4 = (t3 + suffix/0) in
    t4
  }
}

fn trait_impl#Display#Flag#tick(self/0: Flag) -> () {
  ()
}

fn trait_impl#Display#Flag#bump(self/0: Flag, delta/0: i32) -> i32 {
  let t0 = Flag.value(self/0) in
  if t0 {
    delta/0
  } else {
    let t1 = (-delta/0) in
    t1
  }
}

fn trait_impl#Display#Counter#show(self/0: Counter) -> string {
  let t0 = Counter.cell(self/0) in
  join inline_cont0(t1: i32) -> string {
    join inline_cont1(t2: string) -> string {
      let t3 = ("Counter(" + t2) in
      let t4 = (t3 + ")") in
      t4
    } in
    let inline0 = __goml_builtin_int32_to_string(t1) in
    jump inline_cont1(inline0)
  } in
  let inline1 = @intrinsic(ref.get)(t0) in
  jump inline_cont0(inline1)
}

fn trait_impl#Display#Counter#show_with(
  self/0: Counter,
  prefix/0: string,
  suffix/0: string
) -> string {
  let t0 = (prefix/0 + "Counter(") in
  let t1 = Counter.cell(self/0) in
  join inline_cont0(t2: i32) -> string {
    join inline_cont1(t3: string) -> string {
      let t4 = (t0 + t3) in
      let t5 = (t4 + ")") in
      let t6 = (t5 + suffix/0) in
      t6
    } in
    let inline0 = __goml_builtin_int32_to_string(t2) in
    jump inline_cont1(inline0)
  } in
  let inline1 = @intrinsic(ref.get)(t1) in
  jump inline_cont0(inline1)
}

fn trait_impl#Display#Counter#tick(self/0: Counter) -> () {
  let t0 = Counter.cell(self/0) in
  join inline_cont0(t1: i32) -> () {
    let next/0 = (t1 + 1) in
    let t2 = Counter.cell(self/0) in
    let inline0 = @intrinsic(ref.set)(t2, next/0) in
    ()
  } in
  let inline1 = @intrinsic(ref.get)(t0) in
  jump inline_cont0(inline1)
}

fn trait_impl#Display#Counter#bump(self/0: Counter, delta/0: i32) -> i32 {
  let t0 = Counter.cell(self/0) in
  join inline_cont0(t1: i32) -> i32 {
    let next/0 = (t1 + delta/0) in
    let t2 = Counter.cell(self/0) in
    let inline0 = @intrinsic(ref.set)(t2, next/0) in
    next/0
  } in
  let inline1 = @intrinsic(ref.get)(t0) in
  jump inline_cont0(inline1)
}

fn show_dyn(x/0: dyn Display) -> string {
  let t0 = dyn_call[Display::show_with](x/0, "<", ">") in
  t0
}

fn call_via_closure(x/0: dyn Display, tag/0: string) -> string {
  let t0 = closure_env_f_0 {} in
  let f/0 = pack_closure[inherent#closure_env_f_0#closure_env_f_0#apply](t0) in
  let t1 = f/0(x/0, tag/0) in
  t1
}

fn make_renderer(tag/0: string) -> (dyn Display) -> string {
  let t0 = closure_env_make_renderer_1 { tag_0: tag/0 } in
  let t1 =
    pack_closure[inherent#closure_env_make_renderer_1#closure_env_make_renderer_1#apply](t0) in
  t1
}

fn bump_and_show(x/0: dyn Display, delta/0: i32) -> string {
  let mtmp0 = dyn_call[Display::tick](x/0) in
  let t0 = dyn_call[Display::show_with](x/0, "[", "]") in
  let t1 = (t0 + ":") in
  let t2 = dyn_call[Display::bump](x/0, delta/0) in
  join inline_cont0(t3: string) -> string {
    let t4 = (t1 + t3) in
    t4
  } in
  let inline0 = __goml_builtin_int32_to_string(t2) in
  jump inline_cont0(inline0)
}

fn main() -> () {
  let p1/0 = Point { x: 1, y: 2 } in
  let p2/0 = Point { x: 3, y: 4 } in
  let f1/0 = Flag { value: true } in
  let f2/0 = Flag { value: false } in
  let t0 = inherent#Ref#Ref[T]#new__T_i0(10) in
  let c/0 = Counter { cell: t0 } in
  let dp1/0 = to_dyn[Display]{Point}(p1/0) in
  let dp2/0 = to_dyn[Display]{Point}(p2/0) in
  let df1/0 = to_dyn[Display]{Flag}(f1/0) in
  let df2/0 = to_dyn[Display]{Flag}(f2/0) in
  let dc/0 = to_dyn[Display]{Counter}(c/0) in
  let render_star/0 = make_renderer("*") in
  let render_angle/0 = make_renderer("<") in
  let s0/0 = show_dyn(dp2/0) in
  let s1/0 = call_via_closure(df2/0, "*") in
  let t1 = render_star/0(dp1/0) in
  let t2 = (t1 + "|") in
  let t3 = render_angle/0(df1/0) in
  let s2/0 = (t2 + t3) in
  join inline_cont0(v/0: Vec[dyn Display]) -> () {
    join inline_cont1(_wild0: ()) -> () {
      join inline_cont2(_wild1: ()) -> () {
        join inline_cont3(_wild2: ()) -> () {
          join inline_cont4(vlen/0: isize) -> () {
            join k0(jp0: i32) -> () {
              join inline_cont5(mtmp0: ()) -> () {
                join inline_cont6(mtmp1: ()) -> () {
                  join inline_cont7(mtmp2: ()) -> () {
                    join inline_cont8(i/0: Ref[i32]) -> () {
                      join exit0() -> () {
                        join inline_cont9(t4: string) -> () {
                          let t5 = ("len:" + t4) in
                          join inline_cont10(mtmp3: ()) -> () {
                            join inline_cont11(t6: string) -> () {
                              let t7 = ("delta:" + t6) in
                              let inline0 = trait_impl#ToString#string#to_string(t7) in
                              let inline1 = @runtime(core.string_println)(inline0) in
                              ()
                            } in
                            let inline2 = __goml_builtin_int32_to_string(jp0) in
                            jump inline_cont11(inline2)
                          } in
                          let inline3 = trait_impl#ToString#string#to_string(t5) in
                          let inline4 = @runtime(core.string_println)(inline3) in
                          jump inline_cont10(())
                        } in
                        let inline5 = __goml_builtin_int_to_string(vlen/0) in
                        jump inline_cont9(inline5)
                      } in
                      joinrec {
                        join loop0() -> () {
                          join inline_cont12(t8: i32) -> () {
                            let t9 = (t8 < 3) in
                            if t9 {
                              join inline_cont13(line/0: string) -> () {
                                join inline_cont14(_wild3: ()) -> () {
                                  join inline_cont15(t10: i32) -> () {
                                    let t11 = (t10 + 1) in
                                    let inline6 = @intrinsic(ref.set)(i/0, t11) in
                                    jump loop0()
                                  } in
                                  let inline7 = @intrinsic(ref.get)(i/0) in
                                  jump inline_cont15(inline7)
                                } in
                                let inline8 = trait_impl#ToString#string#to_string(line/0) in
                                let inline9 = @runtime(core.string_println)(inline8) in
                                jump inline_cont14(())
                              } in
                              let inline10 = dyn_call[Display::tick](dc/0) in
                              let inline11 = dyn_call[Display::show_with](dc/0, "[", "]") in
                              let inline12 = (inline11 + ":") in
                              let inline13 = dyn_call[Display::bump](dc/0, jp0) in
                              let inline14 = inherent#i32#i32#to_string(inline13) in
                              let inline15 = (inline12 + inline14) in
                              jump inline_cont13(inline15)
                            } else {
                              jump exit0()
                            }
                          } in
                          let inline16 = @intrinsic(ref.get)(i/0) in
                          jump inline_cont12(inline16)
                        } in
                      } in
                      jump loop0()
                    } in
                    let inline17 = 0 in
                    let inline18 = @intrinsic(ref.new)(inline17) in
                    jump inline_cont8(inline18)
                  } in
                  let inline19 = trait_impl#ToString#string#to_string(s2/0) in
                  let inline20 = @runtime(core.string_println)(inline19) in
                  jump inline_cont7(())
                } in
                let inline21 = trait_impl#ToString#string#to_string(s1/0) in
                let inline22 = @runtime(core.string_println)(inline21) in
                jump inline_cont6(())
              } in
              let inline23 = trait_impl#ToString#string#to_string(s0/0) in
              let inline24 = @runtime(core.string_println)(inline23) in
              jump inline_cont5(())
            } in
            match vlen/0 {
              2 => {
                jump k0(3)
              },
              _ => {
                jump k0(5)
              },
            }
          } in
          let inline25 = @intrinsic(vec.len)(v/0) in
          jump inline_cont4(inline25)
        } in
        let inline26 = @intrinsic(vec.push)(v/0, dc/0) in
        jump inline_cont3(())
      } in
      let inline27 = @intrinsic(vec.push)(v/0, df1/0) in
      jump inline_cont2(())
    } in
    let inline28 = @intrinsic(vec.push)(v/0, dp1/0) in
    jump inline_cont1(())
  } in
  let inline29 = @intrinsic(vec.new)() in
  jump inline_cont0(inline29)
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> () {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Vec#Vec[T]#new__T_dynDisplay() -> Vec[dyn Display] {
  let t0 = @intrinsic(vec.new)() in
  t0
}

fn inherent#Vec#Vec[T]#push__T_dynDisplay(self/0: Vec[dyn Display], elem/0: dyn Display) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_dynDisplay(self/0: Vec[dyn Display]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn inherent#isize#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn inherent#closure_env_f_0#closure_env_f_0#apply(
  env0: closure_env_f_0,
  v/0: dyn Display,
  t/0: string
) -> string {
  let t0 = dyn_call[Display::show_with](v/0, t/0, t/0) in
  t0
}

fn inherent#closure_env_make_renderer_1#closure_env_make_renderer_1#apply(
  env0: closure_env_make_renderer_1,
  x/0: dyn Display
) -> string {
  let tag/0 = closure_env_make_renderer_1.tag_0(env0) in
  let t0 = dyn_call[Display::show_with](x/0, tag/0, tag/0) in
  t0
}