fn optimized(base/0: isize, count/1: isize) -> isize {
  let index/2 = 0 in
  let result/3 = 0 in
  join exit811() -> isize {
    result/3
  } in
  let square/4 = (base/0 * base/0) in
  let scaled/5 = (square/4 * 17) in
  let offset/6 = (scaled/5 + base/0) in
  joinrec {
    join loop812() -> isize {
      let t813 = (index/2 < count/1) in
      if t813 {
        let t814 = (result/3 + offset/6) in
        let _wild796 = result/3 = t814 in
        let t815 = (index/2 + 1) in
        let _wild797 = index/2 = t815 in
        jump loop812()
      } else {
        jump exit811()
      }
    } in
  } in
  jump loop812()
}

fn guarded(divisor/7: isize, count/8: isize) -> isize {
  let index/9 = 0 in
  let result/10 = 0 in
  join exit818() -> isize {
    result/10
  } in
  joinrec {
    join loop819() -> isize {
      let t820 = (index/9 < count/8) in
      if t820 {
        let quotient/11 = (100 / divisor/7) in
        let t821 = (result/10 + quotient/11) in
        let _wild799 = result/10 = t821 in
        let t822 = (index/9 + 1) in
        let _wild800 = index/9 = t822 in
        jump loop819()
      } else {
        jump exit818()
      }
    } in
  } in
  jump loop819()
}

fn changing(count/12: isize) -> isize {
  let index/13 = 0 in
  let value/14 = 1 in
  let result/15 = 0 in
  join exit825() -> isize {
    result/15
  } in
  joinrec {
    join loop826() -> isize {
      let t827 = (index/13 < count/12) in
      if t827 {
        let derived/16 = (value/14 + 1) in
        let t828 = (result/15 + derived/16) in
        let _wild802 = result/15 = t828 in
        let t829 = (value/14 + 1) in
        let _wild803 = value/14 = t829 in
        let t830 = (index/13 + 1) in
        let _wild804 = index/13 = t830 in
        jump loop826()
      } else {
        jump exit825()
      }
    } in
  } in
  jump loop826()
}

fn main() -> unit {
  let t832 = optimized(3, 4) in
  join inline_cont890(_wild806: unit) -> unit {
    let t833 = guarded(0, 0) in
    join inline_cont887(_wild807: unit) -> unit {
      let t834 = changing(3) in
      let inline885 = trait_impl#ToString#isize#to_string(t834) in
      let inline886 = @runtime(core.string_println)(inline885) in
      ()
    } in
    let inline888 = trait_impl#ToString#isize#to_string(t833) in
    let inline889 = @runtime(core.string_println)(inline888) in
    jump inline_cont887(())
  } in
  let inline891 = trait_impl#ToString#isize#to_string(t832) in
  let inline892 = @runtime(core.string_println)(inline891) in
  jump inline_cont890(())
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont893(t836: string) -> unit {
    let t837 = @runtime(core.string_println)(t836) in
    ()
  } in
  let inline894 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont893(inline894)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline896 = (self/404 as i64) in
  let inline897 = signed_decimal_string(inline896) in
  inline897
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t843 = (value/222 as i64) in
  let inline899 = (t843 < 0) in
  if inline899 {
    let inline900 = (t843 as u64) in
    let inline901 = (0 - inline900) in
    let inline902 = decimal_string(inline901) in
    let inline903 = ("-" + inline902) in
    inline903
  } else {
    let inline904 = (t843 as u64) in
    let inline905 = decimal_string(inline904) in
    inline905
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t849 = (value/214 < 0) in
  if t849 {
    let t850 = (value/214 as u64) in
    let t851 = (0 - t850) in
    let t852 = decimal_string(t851) in
    let t853 = ("-" + t852) in
    t853
  } else {
    let t854 = (value/214 as u64) in
    let t855 = decimal_string(t854) in
    t855
  }
}

fn decimal_string(value/208: u64) -> string {
  let t878 = (value/208 == 0) in
  join k858() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit859() -> string {
      join inline_cont914(t860: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t860) in
        let offset/212 = 0 in
        join exit861() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop862() -> string {
            join inline_cont912(t863: isize) -> string {
              let t864 = (offset/212 < t863) in
              if t864 {
                join inline_cont910(t865: isize) -> string {
                  let t866 = (t865 - offset/212) in
                  let t867 = (t866 - 1) in
                  let t868 = @intrinsic(vec.get)(reversed/209, t867) in
                  join inline_cont908(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t869 = (compound_old358 + compound_value359) in
                    let t870 = offset/212 = t869 in
                    jump loop862()
                  } in
                  let inline909 = @intrinsic(vec.push)(bytes/211, t868) in
                  jump inline_cont908(())
                } in
                let inline911 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont910(inline911)
              } else {
                jump exit861()
              }
            } in
            let inline913 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont912(inline913)
          } in
        } in
        jump loop862()
      } in
      let inline915 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont914(inline915)
    } in
    joinrec {
      join loop871() -> string {
        let t872 = (remaining/210 > 0) in
        if t872 {
          let t873 = (remaining/210 % 10) in
          let t874 = (t873 as u8) in
          let t875 = (t874 + 48) in
          join inline_cont906(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t876 = (compound_old353 / compound_value354) in
            let t877 = remaining/210 = t876 in
            jump loop871()
          } in
          let inline907 = @intrinsic(vec.push)(reversed/209, t875) in
          jump inline_cont906(())
        } else {
          jump exit859()
        }
      } in
    } in
    jump loop871()
  } in
  if t878 {
    "0"
  } else {
    jump k858()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t880 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t883 = @intrinsic(vec.len)(self/526) in
  t883
}