fn optimized(base/0: int, count/1: int) -> int {
  let index/2 = 0 in
  let result/3 = 0 in
  join exit187() -> int {
    result/3
  } in
  let square/4 = (base/0 * base/0) in
  let scaled/5 = (square/4 * 17) in
  let offset/6 = (scaled/5 + base/0) in
  joinrec {
    join loop188() -> int {
      let t189 = (index/2 < count/1) in
      if t189 {
        let t190 = (result/3 + offset/6) in
        let _wild172 = result/3 = t190 in
        let t191 = (index/2 + 1) in
        let _wild173 = index/2 = t191 in
        jump loop188()
      } else {
        jump exit187()
      }
    } in
  } in
  jump loop188()
}

fn guarded(divisor/7: int, count/8: int) -> int {
  let index/9 = 0 in
  let result/10 = 0 in
  join exit194() -> int {
    result/10
  } in
  joinrec {
    join loop195() -> int {
      let t196 = (index/9 < count/8) in
      if t196 {
        let quotient/11 = (100 / divisor/7) in
        let t197 = (result/10 + quotient/11) in
        let _wild175 = result/10 = t197 in
        let t198 = (index/9 + 1) in
        let _wild176 = index/9 = t198 in
        jump loop195()
      } else {
        jump exit194()
      }
    } in
  } in
  jump loop195()
}

fn changing(count/12: int) -> int {
  let index/13 = 0 in
  let value/14 = 1 in
  let result/15 = 0 in
  join exit201() -> int {
    result/15
  } in
  joinrec {
    join loop202() -> int {
      let t203 = (index/13 < count/12) in
      if t203 {
        let derived/16 = (value/14 + 1) in
        let t204 = (result/15 + derived/16) in
        let _wild178 = result/15 = t204 in
        let t205 = (value/14 + 1) in
        let _wild179 = value/14 = t205 in
        let t206 = (index/13 + 1) in
        let _wild180 = index/13 = t206 in
        jump loop202()
      } else {
        jump exit201()
      }
    } in
  } in
  jump loop202()
}

fn main() -> unit {
  let t208 = optimized(3, 4) in
  join inline_cont223(_wild182: unit) -> unit {
    let t209 = guarded(0, 0) in
    join inline_cont220(_wild183: unit) -> unit {
      let t210 = changing(3) in
      let inline218 = trait_impl#ToString#int#to_string(t210) in
      let inline219 = @runtime(core.string_println)(inline218) in
      ()
    } in
    let inline221 = trait_impl#ToString#int#to_string(t209) in
    let inline222 = @runtime(core.string_println)(inline221) in
    jump inline_cont220(())
  } in
  let inline224 = trait_impl#ToString#int#to_string(t208) in
  let inline225 = @runtime(core.string_println)(inline224) in
  jump inline_cont223(())
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont226(t212: string) -> unit {
    let t213 = @runtime(core.string_println)(t212) in
    ()
  } in
  let inline227 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont226(inline227)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t216 = @runtime(core.int_to_string)(self/67) in
  t216
}