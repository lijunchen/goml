impl Display for S{
  fn show(self/0: S) -> string {
      "ok"
  }
}

fn to_dyn(x/1: T) -> dyn Display {
    to_dyn[Display]{T}((x/1 : T))
}

fn main() -> unit {
    let _ : dyn Display = (to_dyn : (S) -> dyn Display)(S {});
    (println : (string) -> unit)("ok");
}