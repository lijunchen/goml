fn main() {
    let i = Ref::new(0);
    while i.get() < 8 {
        i.set(i.get() + 1);
        if i.get() == 3 {
            continue
        } else {};
        if i.get() == 6 {
            continue
        } else {};
        let _ = println(i.get());
    }
    let _ = println("done");
}
