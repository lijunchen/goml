fn main() -> unit {
  join inline_cont903(i/0: Ref[isize]) -> unit {
    join exit803() -> unit {
      let inline900 = "done" in
      let inline901 = trait_impl#ToString#string#to_string(inline900) in
      let inline902 = @runtime(core.string_println)(inline901) in
      ()
    } in
    joinrec {
      join loop804() -> unit {
        join inline_cont897(t805: isize) -> unit {
          let t806 = (t805 < 8) in
          if t806 {
            join inline_cont895(t807: isize) -> unit {
              let t808 = (t807 + 1) in
              join inline_cont893(_wild796: unit) -> unit {
                join inline_cont891(t814: isize) -> unit {
                  let t815 = (t814 == 3) in
                  join k809() -> unit {
                    join inline_cont889(t812: isize) -> unit {
                      let t813 = (t812 == 6) in
                      join k810() -> unit {
                        join inline_cont887(t811: isize) -> unit {
                          let inline885 = trait_impl#ToString#isize#to_string(t811) in
                          let inline886 = @runtime(core.string_println)(inline885) in
                          jump loop804()
                        } in
                        let inline888 = @intrinsic(ref.get)(i/0) in
                        jump inline_cont887(inline888)
                      } in
                      if t813 {
                        jump loop804()
                      } else {
                        jump k810()
                      }
                    } in
                    let inline890 = @intrinsic(ref.get)(i/0) in
                    jump inline_cont889(inline890)
                  } in
                  if t815 {
                    jump loop804()
                  } else {
                    jump k809()
                  }
                } in
                let inline892 = @intrinsic(ref.get)(i/0) in
                jump inline_cont891(inline892)
              } in
              let inline894 = @intrinsic(ref.set)(i/0, t808) in
              jump inline_cont893(())
            } in
            let inline896 = @intrinsic(ref.get)(i/0) in
            jump inline_cont895(inline896)
          } else {
            jump exit803()
          }
        } in
        let inline898 = @intrinsic(ref.get)(i/0) in
        jump inline_cont897(inline898)
      } in
    } in
    jump loop804()
  } in
  let inline904 = 0 in
  let inline905 = @intrinsic(ref.new)(inline904) in
  jump inline_cont903(inline905)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t818 = @intrinsic(ref.new)(value/684) in
  t818
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t821 = @intrinsic(ref.get)(self/685) in
  t821
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t823 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont906(t825: string) -> unit {
    let t826 = @runtime(core.string_println)(t825) in
    ()
  } in
  let inline907 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont906(inline907)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont908(t828: string) -> unit {
    let t829 = @runtime(core.string_println)(t828) in
    ()
  } in
  jump inline_cont908(value/1)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline910 = (self/404 as i64) in
  let inline911 = signed_decimal_string(inline910) in
  inline911
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t837 = (value/222 as i64) in
  let inline913 = (t837 < 0) in
  if inline913 {
    let inline914 = (t837 as u64) in
    let inline915 = (0 - inline914) in
    let inline916 = decimal_string(inline915) in
    let inline917 = ("-" + inline916) in
    inline917
  } else {
    let inline918 = (t837 as u64) in
    let inline919 = decimal_string(inline918) in
    inline919
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t843 = (value/214 < 0) in
  if t843 {
    let t844 = (value/214 as u64) in
    let t845 = (0 - t844) in
    let t846 = decimal_string(t845) in
    let t847 = ("-" + t846) in
    t847
  } else {
    let t848 = (value/214 as u64) in
    let t849 = decimal_string(t848) in
    t849
  }
}

fn decimal_string(value/208: u64) -> string {
  let t872 = (value/208 == 0) in
  join k852() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit853() -> string {
      join inline_cont928(t854: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t854) in
        let offset/212 = 0 in
        join exit855() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop856() -> string {
            join inline_cont926(t857: isize) -> string {
              let t858 = (offset/212 < t857) in
              if t858 {
                join inline_cont924(t859: isize) -> string {
                  let t860 = (t859 - offset/212) in
                  let t861 = (t860 - 1) in
                  let t862 = @intrinsic(vec.get)(reversed/209, t861) in
                  join inline_cont922(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t863 = (compound_old358 + compound_value359) in
                    let t864 = offset/212 = t863 in
                    jump loop856()
                  } in
                  let inline923 = @intrinsic(vec.push)(bytes/211, t862) in
                  jump inline_cont922(())
                } in
                let inline925 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont924(inline925)
              } else {
                jump exit855()
              }
            } in
            let inline927 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont926(inline927)
          } in
        } in
        jump loop856()
      } in
      let inline929 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont928(inline929)
    } in
    joinrec {
      join loop865() -> string {
        let t866 = (remaining/210 > 0) in
        if t866 {
          let t867 = (remaining/210 % 10) in
          let t868 = (t867 as u8) in
          let t869 = (t868 + 48) in
          join inline_cont920(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t870 = (compound_old353 / compound_value354) in
            let t871 = remaining/210 = t870 in
            jump loop865()
          } in
          let inline921 = @intrinsic(vec.push)(reversed/209, t869) in
          jump inline_cont920(())
        } else {
          jump exit853()
        }
      } in
    } in
    jump loop865()
  } in
  if t872 {
    "0"
  } else {
    jump k852()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t874 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t877 = @intrinsic(vec.len)(self/526) in
  t877
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t880 = (self/438 == other/439) in
  t880
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t883 = @runtime(core.int_hash)(self/464) in
  t883
}