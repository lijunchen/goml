fn consume(value/0: ()) -> () {
    (value/0 : ())
}

fn main() -> () {
    let unit/1: () = (consume : (()) -> ())(());
    match (unit/1 : ()) {
        () => (println : (string) -> ())("ok"),
    }
}