fn open_handle(ok/0: bool) -> Result__Handle__string {
  if ok/0 {
    let t0 = Handle { name: "config" } in
    let t1 = Result__Handle__string::Ok(t0) in
    t1
  } else {
    let t2 = Result__Handle__string::Err("open failed") in
    t2
  }
}

fn close_handle(handle/0: Handle, ok/0: bool) -> Result__()__string {
  if ok/0 {
    let t0 = Result__()__string::Ok(()) in
    t0
  } else {
    let t1 = Handle.name(handle/0) in
    let t2 = ("close failed for " + t1) in
    let t3 = Result__()__string::Err(t2) in
    t3
  }
}

fn use_handle(open_ok/0: bool, close_ok/0: bool) -> Result__string__string {
  join inline_cont0(mtmp0: Result__Handle__string) -> Result__string__string {
    join k0(jp0: Handle) -> Result__string__string {
      let name/0 = Handle.name(jp0) in
      join inline_cont1(mtmp1: Result__()__string) -> Result__string__string {
        join k1() -> Result__string__string {
          let t0 = ("closed " + name/0) in
          let t1 = Result__string__string::Ok(t0) in
          t1
        } in
        match mtmp1 {
          Tag_0 => {
            jump k1()
          },
          Tag_1 => {
            let x0 = Result__()__string::Err._0(mtmp1) in
            let t2 = Result__string__string::Err(x0) in
            t2
          },
        }
      } in
      if close_ok/0 {
        let inline0 = Result__()__string::Ok(()) in
        jump inline_cont1(inline0)
      } else {
        let inline1 = Handle.name(jp0) in
        let inline2 = ("close failed for " + inline1) in
        let inline3 = Result__()__string::Err(inline2) in
        jump inline_cont1(inline3)
      }
    } in
    match mtmp0 {
      Tag_0 => {
        let x1 = Result__Handle__string::Ok._0(mtmp0) in
        jump k0(x1)
      },
      Tag_1 => {
        let x2 = Result__Handle__string::Err._0(mtmp0) in
        let t3 = Result__string__string::Err(x2) in
        t3
      },
    }
  } in
  if open_ok/0 {
    let inline4 = Handle { name: "config" } in
    let inline5 = Result__Handle__string::Ok(inline4) in
    jump inline_cont0(inline5)
  } else {
    let inline6 = Result__Handle__string::Err("open failed") in
    jump inline_cont0(inline6)
  }
}

fn show(res/0: Result__string__string) -> string {
  match res/0 {
    Tag_0 => {
      let x0 = Result__string__string::Ok._0(res/0) in
      let t0 = ("ok " + x0) in
      t0
    },
    Tag_1 => {
      let x1 = Result__string__string::Err._0(res/0) in
      let t1 = ("err " + x1) in
      t1
    },
  }
}

fn main() -> () {
  let t0 = use_handle(true, true) in
  join inline_cont0(t1: string) -> () {
    join inline_cont1(_wild0: ()) -> () {
      let t2 = use_handle(false, true) in
      join inline_cont2(t3: string) -> () {
        join inline_cont3(_wild1: ()) -> () {
          let t4 = use_handle(true, false) in
          join inline_cont4(t5: string) -> () {
            let inline0 = trait_impl#ToString#string#to_string(t5) in
            let inline1 = @runtime(core.string_println)(inline0) in
            ()
          } in
          match t4 {
            Tag_0 => {
              let inline2 = Result__string__string::Ok._0(t4) in
              let inline3 = ("ok " + inline2) in
              jump inline_cont4(inline3)
            },
            Tag_1 => {
              let inline4 = Result__string__string::Err._0(t4) in
              let inline5 = ("err " + inline4) in
              jump inline_cont4(inline5)
            },
          }
        } in
        let inline6 = trait_impl#ToString#string#to_string(t3) in
        let inline7 = @runtime(core.string_println)(inline6) in
        jump inline_cont3(())
      } in
      match t2 {
        Tag_0 => {
          let inline8 = Result__string__string::Ok._0(t2) in
          let inline9 = ("ok " + inline8) in
          jump inline_cont2(inline9)
        },
        Tag_1 => {
          let inline10 = Result__string__string::Err._0(t2) in
          let inline11 = ("err " + inline10) in
          jump inline_cont2(inline11)
        },
      }
    } in
    let inline12 = trait_impl#ToString#string#to_string(t1) in
    let inline13 = @runtime(core.string_println)(inline12) in
    jump inline_cont1(())
  } in
  match t0 {
    Tag_0 => {
      let inline14 = Result__string__string::Ok._0(t0) in
      let inline15 = ("ok " + inline14) in
      jump inline_cont0(inline15)
    },
    Tag_1 => {
      let inline16 = Result__string__string::Err._0(t0) in
      let inline17 = ("err " + inline16) in
      jump inline_cont0(inline17)
    },
  }
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}