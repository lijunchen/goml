fn main() -> unit {
    let x/0: uint8 = 5;
    let y/1: string =
      match (x/0 : uint8) {
          0 => "zero",
          1 => "one",
          _ : uint8 => "other",
      };
    (println : (string) -> unit)((y/1 : string));
}