fn main() -> unit {
    let x/0: u8 = 5;
    let y/1: string =
      match (x/0 : u8) {
          0 => "zero",
          1 => "one",
          _ : u8 => "other",
      };
    (println : (string) -> unit)((y/1 : string));
}