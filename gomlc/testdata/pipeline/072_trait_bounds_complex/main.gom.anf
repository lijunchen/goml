fn trait_impl#Display#i32#show(self/0: i32) -> string {
  let inline0 = __goml_builtin_int32_to_string(self/0) in
  inline0
}

fn trait_impl#Debug#i32#show(self/0: i32) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = ("i32(" + t0) in
    let t2 = (t1 + ")") in
    t2
  } in
  let inline0 = __goml_builtin_int32_to_string(self/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#MyEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#MyHash#i32#hash(self/0: i32) -> i32 {
  let t0 = (self/0 * 16777619) in
  let t1 = (t0 + 216613626) in
  t1
}

fn trait_impl#Add#i32#add(self/0: i32, other/0: i32) -> i32 {
  let t0 = (self/0 + other/0) in
  t0
}

fn trait_impl#Scale#i32#scale(self/0: i32, factor/0: i32) -> i32 {
  let t0 = (self/0 * factor/0) in
  t0
}

fn trait_impl#Inspect#i32#inspect(self/0: i32) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = ("<" + t0) in
    let t2 = (t1 + ">") in
    t2
  } in
  let inline0 = __goml_builtin_int32_to_string(self/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#Display#Boxed#show(self/0: Boxed) -> string {
  let t0 = Boxed.value(self/0) in
  join inline_cont0(t1: string) -> string {
    let t2 = ("Boxed(" + t1) in
    let t3 = (t2 + ")") in
    t3
  } in
  let inline0 = __goml_builtin_int32_to_string(t0) in
  jump inline_cont0(inline0)
}

fn trait_impl#Debug#Boxed#show(self/0: Boxed) -> string {
  let t0 = Boxed.value(self/0) in
  join inline_cont0(t1: string) -> string {
    let t2 = ("Boxed{value=" + t1) in
    let t3 = (t2 + "}") in
    t3
  } in
  let inline0 = __goml_builtin_int32_to_string(t0) in
  jump inline_cont0(inline0)
}

fn trait_impl#MyEq#Boxed#eq(self/0: Boxed, other/0: Boxed) -> bool {
  let t0 = Boxed.value(self/0) in
  let t1 = Boxed.value(other/0) in
  let t2 = (t0 == t1) in
  t2
}

fn trait_impl#MyHash#Boxed#hash(self/0: Boxed) -> i32 {
  let t0 = Boxed.value(self/0) in
  let t1 = (t0 * 31) in
  let t2 = (t1 + 7) in
  let t3 = (t2 * 1315423911) in
  t3
}

fn trait_impl#Add#Boxed#add(self/0: Boxed, other/0: Boxed) -> Boxed {
  let t0 = Boxed.value(self/0) in
  let t1 = Boxed.value(other/0) in
  let t2 = (t0 + t1) in
  let t3 = Boxed { value: t2 } in
  t3
}

fn trait_impl#Scale#Boxed#scale(self/0: Boxed, factor/0: i32) -> Boxed {
  let t0 = Boxed.value(self/0) in
  let t1 = (t0 * factor/0) in
  let t2 = Boxed { value: t1 } in
  t2
}

fn trait_impl#Inspect#Boxed#inspect(self/0: Boxed) -> string {
  let t0 = Boxed.value(self/0) in
  join inline_cont0(t1: string) -> string {
    let t2 = ("[" + t1) in
    let t3 = (t2 + "]") in
    t3
  } in
  let inline0 = __goml_builtin_int32_to_string(t0) in
  jump inline_cont0(inline0)
}

fn bool_text(x/0: bool) -> string {
  if x/0 {
    "true"
  } else {
    "false"
  }
}

fn main() -> unit {
  let tag/0 = 7 in
  let left/0 = 10 in
  let right/0 = 32 in
  let sum_tag/0 = 0 in
  let first/0 = 1 in
  let second/0 = 2 in
  let third/0 = 3 in
  join inline_cont0(t0: string) -> unit {
    join inline_cont1(mtmp0: unit) -> unit {
      let t1 = Boxed { value: 99 } in
      let t2 = Boxed { value: 3 } in
      let t3 = Boxed { value: 4 } in
      join inline_cont2(t4: string) -> unit {
        join inline_cont3(mtmp1: unit) -> unit {
          join inline_cont4(t5: string) -> unit {
            join inline_cont5(mtmp2: unit) -> unit {
              let t6 = Boxed { value: 1 } in
              let t7 = Boxed { value: 5 } in
              let t8 = Boxed { value: 6 } in
              let t9 = Boxed { value: 7 } in
              join inline_cont6(t10: string) -> unit {
                let inline0 = trait_impl#ToString#string#to_string(t10) in
                let inline1 = @runtime(core.string_println)(inline0) in
                ()
              } in
              let inline2 = trait_impl#Add#Boxed#add(t7, t8) in
              let inline3 = trait_impl#Add#Boxed#add(inline2, t9) in
              let inline4 = tag_text__Q_Boxed(t6) in
              let inline5 = trait_impl#MyHash#Boxed#hash(inline3) in
              let inline6 = (inline4 + " ") in
              let inline7 = trait_impl#Inspect#Boxed#inspect(inline3) in
              let inline8 = (inline6 + inline7) in
              let inline9 = inherent#i32#i32#to_string(inline5) in
              let inline10 = (" @" + inline9) in
              let inline11 = (inline8 + inline10) in
              jump inline_cont6(inline11)
            } in
            let inline12 = trait_impl#ToString#string#to_string(t5) in
            let inline13 = @runtime(core.string_println)(inline12) in
            jump inline_cont5(())
          } in
          let inline14 = trait_impl#Add#i32#add(first/0, second/0) in
          let inline15 = trait_impl#Add#i32#add(inline14, third/0) in
          let inline16 = tag_text__Q_i0(sum_tag/0) in
          let inline17 = trait_impl#MyHash#i32#hash(inline15) in
          let inline18 = (inline16 + " ") in
          let inline19 = trait_impl#Inspect#i32#inspect(inline15) in
          let inline20 = (inline18 + inline19) in
          let inline21 = inherent#i32#i32#to_string(inline17) in
          let inline22 = (" @" + inline21) in
          let inline23 = (inline20 + inline22) in
          jump inline_cont4(inline23)
        } in
        let inline24 = trait_impl#ToString#string#to_string(t4) in
        let inline25 = @runtime(core.string_println)(inline24) in
        jump inline_cont3(())
      } in
      let inline26 = combine_scaled__T_Boxed(t2, t3, 2) in
      let inline27 = report_pair__Q_Boxed__T_Boxed(t1, t2, t3, inline26) in
      jump inline_cont2(inline27)
    } in
    let inline28 = trait_impl#ToString#string#to_string(t0) in
    let inline29 = @runtime(core.string_println)(inline28) in
    jump inline_cont1(())
  } in
  let inline30 = combine_scaled__T_i0(left/0, right/0, 2) in
  let inline31 = report_pair__Q_i32__T_i0(tag/0, left/0, right/0, inline30) in
  jump inline_cont0(inline31)
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn full_report__Q_i32__T_i32(tag/0: i32, a/0: i32, b/0: i32) -> string {
  join inline_cont0(combined/0: i32) -> string {
    let inline0 = trait_impl#MyEq#i32#eq(a/0, b/0) in
    let inline1 = tag_text__Q_i0(tag/0) in
    let inline2 = show_both__T_i0(combined/0) in
    let inline3 = trait_impl#MyHash#i32#hash(combined/0) in
    let inline4 = (inline1 + " ") in
    let inline5 = (inline4 + inline2) in
    let inline6 = bool_text(inline0) in
    let inline7 = (" | eq=" + inline6) in
    let inline8 = inherent#i32#i32#to_string(inline3) in
    let inline9 = (" | hash=" + inline8) in
    let inline10 = (inline7 + inline9) in
    let inline11 = (inline5 + inline10) in
    inline11
  } in
  let inline12 = 2 in
  let inline13 = trait_impl#Add#i32#add(a/0, b/0) in
  let inline14 = trait_impl#Scale#i32#scale(inline13, inline12) in
  jump inline_cont0(inline14)
}

fn full_report__Q_Boxed__T_Boxed(tag/0: Boxed, a/0: Boxed, b/0: Boxed) -> string {
  join inline_cont0(combined/0: Boxed) -> string {
    let inline0 = trait_impl#MyEq#Boxed#eq(a/0, b/0) in
    let inline1 = tag_text__Q_Boxed(tag/0) in
    let inline2 = show_both__T_Boxed(combined/0) in
    let inline3 = trait_impl#MyHash#Boxed#hash(combined/0) in
    let inline4 = (inline1 + " ") in
    let inline5 = (inline4 + inline2) in
    let inline6 = bool_text(inline0) in
    let inline7 = (" | eq=" + inline6) in
    let inline8 = inherent#i32#i32#to_string(inline3) in
    let inline9 = (" | hash=" + inline8) in
    let inline10 = (inline7 + inline9) in
    let inline11 = (inline5 + inline10) in
    inline11
  } in
  let inline12 = 2 in
  let inline13 = trait_impl#Add#Boxed#add(a/0, b/0) in
  let inline14 = trait_impl#Scale#Boxed#scale(inline13, inline12) in
  jump inline_cont0(inline14)
}

fn sum_and_tag__Q_i32__T_i32(tag/0: i32, x/0: i32, y/0: i32, z/0: i32) -> string {
  join inline_cont0(t0: i32) -> string {
    join inline_cont1(total/0: i32) -> string {
      join inline_cont2(header/0: string) -> string {
        join inline_cont3(h/0: i32) -> string {
          let t1 = (header/0 + " ") in
          join inline_cont4(t2: string) -> string {
            let t3 = (t1 + t2) in
            join inline_cont5(t4: string) -> string {
              let t5 = (" @" + t4) in
              let t6 = (t3 + t5) in
              t6
            } in
            let inline0 = __goml_builtin_int32_to_string(h/0) in
            jump inline_cont5(inline0)
          } in
          let inline1 = inherent#i32#i32#to_string(total/0) in
          let inline2 = ("<" + inline1) in
          let inline3 = (inline2 + ">") in
          jump inline_cont4(inline3)
        } in
        let inline4 = (total/0 * 16777619) in
        let inline5 = (inline4 + 216613626) in
        jump inline_cont3(inline5)
      } in
      let inline6 = trait_impl#Debug#i32#show(tag/0) in
      let inline7 = (inline6 + "#") in
      let inline8 = trait_impl#MyHash#i32#hash(tag/0) in
      let inline9 = inherent#i32#i32#to_string(inline8) in
      let inline10 = (inline7 + inline9) in
      jump inline_cont2(inline10)
    } in
    let inline11 = (t0 + z/0) in
    jump inline_cont1(inline11)
  } in
  let inline12 = (x/0 + y/0) in
  jump inline_cont0(inline12)
}

fn sum_and_tag__Q_Boxed__T_Boxed(tag/0: Boxed, x/0: Boxed, y/0: Boxed, z/0: Boxed) -> string {
  join inline_cont0(t0: Boxed) -> string {
    join inline_cont1(total/0: Boxed) -> string {
      join inline_cont2(header/0: string) -> string {
        join inline_cont3(h/0: i32) -> string {
          let t1 = (header/0 + " ") in
          join inline_cont4(t2: string) -> string {
            let t3 = (t1 + t2) in
            join inline_cont5(t4: string) -> string {
              let t5 = (" @" + t4) in
              let t6 = (t3 + t5) in
              t6
            } in
            let inline0 = __goml_builtin_int32_to_string(h/0) in
            jump inline_cont5(inline0)
          } in
          let inline1 = Boxed.value(total/0) in
          let inline2 = inherent#i32#i32#to_string(inline1) in
          let inline3 = ("[" + inline2) in
          let inline4 = (inline3 + "]") in
          jump inline_cont4(inline4)
        } in
        let inline5 = Boxed.value(total/0) in
        let inline6 = (inline5 * 31) in
        let inline7 = (inline6 + 7) in
        let inline8 = (inline7 * 1315423911) in
        jump inline_cont3(inline8)
      } in
      let inline9 = trait_impl#Debug#Boxed#show(tag/0) in
      let inline10 = (inline9 + "#") in
      let inline11 = trait_impl#MyHash#Boxed#hash(tag/0) in
      let inline12 = inherent#i32#i32#to_string(inline11) in
      let inline13 = (inline10 + inline12) in
      jump inline_cont2(inline13)
    } in
    let inline14 = Boxed.value(t0) in
    let inline15 = Boxed.value(z/0) in
    let inline16 = (inline14 + inline15) in
    let inline17 = Boxed { value: inline16 } in
    jump inline_cont1(inline17)
  } in
  let inline18 = Boxed.value(x/0) in
  let inline19 = Boxed.value(y/0) in
  let inline20 = (inline18 + inline19) in
  let inline21 = Boxed { value: inline20 } in
  jump inline_cont0(inline21)
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn combine_scaled__T_i32(a/0: i32, b/0: i32, factor/0: i32) -> i32 {
  join inline_cont0(t0: i32) -> i32 {
    let inline0 = (t0 * factor/0) in
    inline0
  } in
  let inline1 = (a/0 + b/0) in
  jump inline_cont0(inline1)
}

fn report_pair__Q_i32__T_i32(tag/0: i32, a/0: i32, b/0: i32, combined/0: i32) -> string {
  join inline_cont0(same/0: bool) -> string {
    join inline_cont1(header/0: string) -> string {
      join inline_cont2(repr/0: string) -> string {
        join inline_cont3(h/0: i32) -> string {
          let t0 = (header/0 + " ") in
          let t1 = (t0 + repr/0) in
          join inline_cont4(t2: string) -> string {
            let t3 = (" | eq=" + t2) in
            join inline_cont5(t4: string) -> string {
              let t5 = (" | hash=" + t4) in
              let t6 = (t3 + t5) in
              let t7 = (t1 + t6) in
              t7
            } in
            let inline0 = __goml_builtin_int32_to_string(h/0) in
            jump inline_cont5(inline0)
          } in
          if same/0 {
            jump inline_cont4("true")
          } else {
            jump inline_cont4("false")
          }
        } in
        let inline1 = (combined/0 * 16777619) in
        let inline2 = (inline1 + 216613626) in
        jump inline_cont3(inline2)
      } in
      let inline3 = trait_impl#Debug#i32#show(combined/0) in
      let inline4 = (inline3 + " / ") in
      let inline5 = trait_impl#Display#i32#show(combined/0) in
      let inline6 = (inline4 + inline5) in
      jump inline_cont2(inline6)
    } in
    let inline7 = trait_impl#Debug#i32#show(tag/0) in
    let inline8 = (inline7 + "#") in
    let inline9 = trait_impl#MyHash#i32#hash(tag/0) in
    let inline10 = inherent#i32#i32#to_string(inline9) in
    let inline11 = (inline8 + inline10) in
    jump inline_cont1(inline11)
  } in
  let inline12 = (a/0 == b/0) in
  jump inline_cont0(inline12)
}

fn combine_scaled__T_Boxed(a/0: Boxed, b/0: Boxed, factor/0: i32) -> Boxed {
  join inline_cont0(t0: Boxed) -> Boxed {
    let inline0 = Boxed.value(t0) in
    let inline1 = (inline0 * factor/0) in
    let inline2 = Boxed { value: inline1 } in
    inline2
  } in
  let inline3 = Boxed.value(a/0) in
  let inline4 = Boxed.value(b/0) in
  let inline5 = (inline3 + inline4) in
  let inline6 = Boxed { value: inline5 } in
  jump inline_cont0(inline6)
}

fn report_pair__Q_Boxed__T_Boxed(
  tag/0: Boxed,
  a/0: Boxed,
  b/0: Boxed,
  combined/0: Boxed
) -> string {
  join inline_cont0(same/0: bool) -> string {
    join inline_cont1(header/0: string) -> string {
      join inline_cont2(repr/0: string) -> string {
        join inline_cont3(h/0: i32) -> string {
          let t0 = (header/0 + " ") in
          let t1 = (t0 + repr/0) in
          join inline_cont4(t2: string) -> string {
            let t3 = (" | eq=" + t2) in
            join inline_cont5(t4: string) -> string {
              let t5 = (" | hash=" + t4) in
              let t6 = (t3 + t5) in
              let t7 = (t1 + t6) in
              t7
            } in
            let inline0 = __goml_builtin_int32_to_string(h/0) in
            jump inline_cont5(inline0)
          } in
          if same/0 {
            jump inline_cont4("true")
          } else {
            jump inline_cont4("false")
          }
        } in
        let inline1 = Boxed.value(combined/0) in
        let inline2 = (inline1 * 31) in
        let inline3 = (inline2 + 7) in
        let inline4 = (inline3 * 1315423911) in
        jump inline_cont3(inline4)
      } in
      let inline5 = trait_impl#Debug#Boxed#show(combined/0) in
      let inline6 = (inline5 + " / ") in
      let inline7 = trait_impl#Display#Boxed#show(combined/0) in
      let inline8 = (inline6 + inline7) in
      jump inline_cont2(inline8)
    } in
    let inline9 = trait_impl#Debug#Boxed#show(tag/0) in
    let inline10 = (inline9 + "#") in
    let inline11 = trait_impl#MyHash#Boxed#hash(tag/0) in
    let inline12 = inherent#i32#i32#to_string(inline11) in
    let inline13 = (inline10 + inline12) in
    jump inline_cont1(inline13)
  } in
  let inline14 = Boxed.value(a/0) in
  let inline15 = Boxed.value(b/0) in
  let inline16 = (inline14 == inline15) in
  jump inline_cont0(inline16)
}

fn tag_text__Q_i32(tag/0: i32) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (t0 + "#") in
    join inline_cont1(t2: i32) -> string {
      join inline_cont2(t3: string) -> string {
        let t4 = (t1 + t3) in
        t4
      } in
      let inline0 = __goml_builtin_int32_to_string(t2) in
      jump inline_cont2(inline0)
    } in
    let inline1 = (tag/0 * 16777619) in
    let inline2 = (inline1 + 216613626) in
    jump inline_cont1(inline2)
  } in
  let inline3 = inherent#i32#i32#to_string(tag/0) in
  let inline4 = ("i32(" + inline3) in
  let inline5 = (inline4 + ")") in
  jump inline_cont0(inline5)
}

fn tag_text__Q_Boxed(tag/0: Boxed) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (t0 + "#") in
    join inline_cont1(t2: i32) -> string {
      join inline_cont2(t3: string) -> string {
        let t4 = (t1 + t3) in
        t4
      } in
      let inline0 = __goml_builtin_int32_to_string(t2) in
      jump inline_cont2(inline0)
    } in
    let inline1 = Boxed.value(tag/0) in
    let inline2 = (inline1 * 31) in
    let inline3 = (inline2 + 7) in
    let inline4 = (inline3 * 1315423911) in
    jump inline_cont1(inline4)
  } in
  let inline5 = Boxed.value(tag/0) in
  let inline6 = inherent#i32#i32#to_string(inline5) in
  let inline7 = ("Boxed{value=" + inline6) in
  let inline8 = (inline7 + "}") in
  jump inline_cont0(inline8)
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn show_both__T_i32(x/0: i32) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (t0 + " / ") in
    join inline_cont1(t2: string) -> string {
      let t3 = (t1 + t2) in
      t3
    } in
    let inline0 = inherent#i32#i32#to_string(x/0) in
    jump inline_cont1(inline0)
  } in
  let inline1 = inherent#i32#i32#to_string(x/0) in
  let inline2 = ("i32(" + inline1) in
  let inline3 = (inline2 + ")") in
  jump inline_cont0(inline3)
}

fn show_both__T_Boxed(x/0: Boxed) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (t0 + " / ") in
    join inline_cont1(t2: string) -> string {
      let t3 = (t1 + t2) in
      t3
    } in
    let inline0 = Boxed.value(x/0) in
    let inline1 = inherent#i32#i32#to_string(inline0) in
    let inline2 = ("Boxed(" + inline1) in
    let inline3 = (inline2 + ")") in
    jump inline_cont1(inline3)
  } in
  let inline4 = Boxed.value(x/0) in
  let inline5 = inherent#i32#i32#to_string(inline4) in
  let inline6 = ("Boxed{value=" + inline5) in
  let inline7 = (inline6 + "}") in
  jump inline_cont0(inline7)
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}