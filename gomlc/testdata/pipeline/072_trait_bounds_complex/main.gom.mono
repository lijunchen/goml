fn trait_impl#Display#i32#show(self/0: i32) -> string {
  inherent#i32#i32#to_string(self/0)
}

fn trait_impl#Debug#i32#show(self/0: i32) -> string {
  (("i32(" + inherent#i32#i32#to_string(self/0)) + ")")
}

fn trait_impl#MyEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  (self/0 == other/0)
}

fn trait_impl#MyHash#i32#hash(self/0: i32) -> i32 {
  ((self/0 * 16777619) + 216613626)
}

fn trait_impl#Add#i32#add(self/0: i32, other/0: i32) -> i32 {
  (self/0 + other/0)
}

fn trait_impl#Scale#i32#scale(self/0: i32, factor/0: i32) -> i32 {
  (self/0 * factor/0)
}

fn trait_impl#Inspect#i32#inspect(self/0: i32) -> string {
  (("<" + inherent#i32#i32#to_string(self/0)) + ">")
}

fn trait_impl#Display#Boxed#show(self/0: Boxed) -> string {
  (("Boxed(" + inherent#i32#i32#to_string(Boxed.value(self/0))) + ")")
}

fn trait_impl#Debug#Boxed#show(self/0: Boxed) -> string {
  (("Boxed{value=" + inherent#i32#i32#to_string(Boxed.value(self/0))) + "}")
}

fn trait_impl#MyEq#Boxed#eq(self/0: Boxed, other/0: Boxed) -> bool {
  (Boxed.value(self/0) == Boxed.value(other/0))
}

fn trait_impl#MyHash#Boxed#hash(self/0: Boxed) -> i32 {
  (((Boxed.value(self/0) * 31) + 7) * 1315423911)
}

fn trait_impl#Add#Boxed#add(self/0: Boxed, other/0: Boxed) -> Boxed {
  Boxed { value: (Boxed.value(self/0) + Boxed.value(other/0)) }
}

fn trait_impl#Scale#Boxed#scale(self/0: Boxed, factor/0: i32) -> Boxed {
  Boxed { value: (Boxed.value(self/0) * factor/0) }
}

fn trait_impl#Inspect#Boxed#inspect(self/0: Boxed) -> string {
  (("[" + inherent#i32#i32#to_string(Boxed.value(self/0))) + "]")
}

fn bool_text(x/0: bool) -> string {
  if x/0 {
      {
        "true"
      }
  } else {
      {
        "false"
      }
  }
}

fn main() -> () {
  let tag/0 = 7 in
  let left/0 = 10 in
  let right/0 = 32 in
  let sum_tag/0 = 0 in
  let first/0 = 1 in
  let second/0 = 2 in
  let third/0 = 3 in
  let mtmp0 = println__T_string(full_report__Q_i32__T_i0(tag/0, left/0, right/0)) in
  {
    let mtmp1 =
      println__T_string(
        full_report__Q_Boxed__T_Boxed(Boxed { value: 99 }, Boxed { value: 3 }, Boxed { value: 4 })
      ) in
    {
      let mtmp2 =
        println__T_string(sum_and_tag__Q_i32__T_i0(sum_tag/0, first/0, second/0, third/0)) in
      {
        let mtmp3 =
          println__T_string(
            sum_and_tag__Q_Boxed__T_Boxed(
              Boxed { value: 1 },
              Boxed { value: 5 },
              Boxed { value: 6 },
              Boxed { value: 7 }
            )
          ) in
        {
          ()
        }
      }
    }
  }
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn println__T_string(value/0: string) -> () {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn full_report__Q_i32__T_i32(tag/0: i32, a/0: i32, b/0: i32) -> string {
  let combined/0 = combine_scaled__T_i0(a/0, b/0, 2) in
  report_pair__Q_i32__T_i0(tag/0, a/0, b/0, combined/0)
}

fn full_report__Q_Boxed__T_Boxed(tag/0: Boxed, a/0: Boxed, b/0: Boxed) -> string {
  let combined/0 = combine_scaled__T_Boxed(a/0, b/0, 2) in
  report_pair__Q_Boxed__T_Boxed(tag/0, a/0, b/0, combined/0)
}

fn sum_and_tag__Q_i32__T_i32(tag/0: i32, x/0: i32, y/0: i32, z/0: i32) -> string {
  let total/0 = trait_impl#Add#i32#add(trait_impl#Add#i32#add(x/0, y/0), z/0) in
  let header/0 = tag_text__Q_i0(tag/0) in
  let h/0 = trait_impl#MyHash#i32#hash(total/0) in
  (((header/0 + " ") + trait_impl#Inspect#i32#inspect(total/0)) +
    (" @" + inherent#i32#i32#to_string(h/0)))
}

fn sum_and_tag__Q_Boxed__T_Boxed(tag/0: Boxed, x/0: Boxed, y/0: Boxed, z/0: Boxed) -> string {
  let total/0 = trait_impl#Add#Boxed#add(trait_impl#Add#Boxed#add(x/0, y/0), z/0) in
  let header/0 = tag_text__Q_Boxed(tag/0) in
  let h/0 = trait_impl#MyHash#Boxed#hash(total/0) in
  (((header/0 + " ") + trait_impl#Inspect#Boxed#inspect(total/0)) +
    (" @" + inherent#i32#i32#to_string(h/0)))
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn combine_scaled__T_i32(a/0: i32, b/0: i32, factor/0: i32) -> i32 {
  trait_impl#Scale#i32#scale(trait_impl#Add#i32#add(a/0, b/0), factor/0)
}

fn report_pair__Q_i32__T_i32(tag/0: i32, a/0: i32, b/0: i32, combined/0: i32) -> string {
  let same/0 = trait_impl#MyEq#i32#eq(a/0, b/0) in
  let header/0 = tag_text__Q_i0(tag/0) in
  let repr/0 = show_both__T_i0(combined/0) in
  let h/0 = trait_impl#MyHash#i32#hash(combined/0) in
  (((header/0 + " ") + repr/0) +
    ((" | eq=" + bool_text(same/0)) + (" | hash=" + inherent#i32#i32#to_string(h/0))))
}

fn combine_scaled__T_Boxed(a/0: Boxed, b/0: Boxed, factor/0: i32) -> Boxed {
  trait_impl#Scale#Boxed#scale(trait_impl#Add#Boxed#add(a/0, b/0), factor/0)
}

fn report_pair__Q_Boxed__T_Boxed(
  tag/0: Boxed,
  a/0: Boxed,
  b/0: Boxed,
  combined/0: Boxed
) -> string {
  let same/0 = trait_impl#MyEq#Boxed#eq(a/0, b/0) in
  let header/0 = tag_text__Q_Boxed(tag/0) in
  let repr/0 = show_both__T_Boxed(combined/0) in
  let h/0 = trait_impl#MyHash#Boxed#hash(combined/0) in
  (((header/0 + " ") + repr/0) +
    ((" | eq=" + bool_text(same/0)) + (" | hash=" + inherent#i32#i32#to_string(h/0))))
}

fn tag_text__Q_i32(tag/0: i32) -> string {
  ((trait_impl#Debug#i32#show(tag/0) + "#") +
    inherent#i32#i32#to_string(trait_impl#MyHash#i32#hash(tag/0)))
}

fn tag_text__Q_Boxed(tag/0: Boxed) -> string {
  ((trait_impl#Debug#Boxed#show(tag/0) + "#") +
    inherent#i32#i32#to_string(trait_impl#MyHash#Boxed#hash(tag/0)))
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      {
        ("-" + decimal_string((0 - (value/0 as u64))))
      }
  } else {
      {
        decimal_string((value/0 as u64))
      }
  }
}

fn show_both__T_i32(x/0: i32) -> string {
  ((trait_impl#Debug#i32#show(x/0) + " / ") + trait_impl#Display#i32#show(x/0))
}

fn show_both__T_Boxed(x/0: Boxed) -> string {
  ((trait_impl#Debug#Boxed#show(x/0) + " / ") + trait_impl#Display#Boxed#show(x/0))
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        {
          return "0"
        }
    } else {
        {
          ()
        }
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        {
          let _wild2 =
            inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
          let _wild3 =
            {
              let compound_old0 = remaining/0 in
              let compound_value0 = 10 in
              remaining/0 = (compound_old0 / compound_value0)
            } in
        }
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        {
          let _wild5 =
            inherent#Vec#Vec[T]#push__T_u0(
              bytes/0,
              @intrinsic(vec.get)(
                reversed/0,
                ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
              )
            ) in
          let _wild6 =
            {
              let compound_old1 = offset/0 in
              let compound_value1 = 1 in
              offset/0 = (compound_old1 + compound_value1)
            } in
        }
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  {
    let x0 = mtmp0.1 in
    {
      let result/0 = x0 in
      {
        result/0
      }
    }
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}