package main
file main.gom
  static VALUE: OnceCell[isize] = OnceCell::new();

  #[allow(unstructured_go)]
  fn main() -> () {
      let local/1/0 = Channel::new(2);
      go || results/0.send(def/1/0.get_or_init(|| 41));
      go || results/0.send(def/1/0.get_or_init(|| 42));
      let local/1/1 = results/0.recv().unwrap_or(0);
      let local/1/2 = results/0.recv().unwrap_or(0);
      println(first/1 == second/2.to_string())
  }