fn main() -> unit {
  let sum/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let i/1 = inherent#Ref#Ref[T]#new__T_int(1) in
  join exit200() -> unit {
    join inline_cont299(mtmp191: unit) -> unit {
      join inline_cont297(t201: int) -> unit {
        join inline_cont294(mtmp192: unit) -> unit {
          join inline_cont291(even_sum/2: Ref[int]) -> unit {
            join inline_cont288(j/3: Ref[int]) -> unit {
              join exit202() -> unit {
                join inline_cont284(mtmp197: unit) -> unit {
                  join inline_cont282(t203: int) -> unit {
                    let inline280 = trait_impl#ToString#int#to_string(t203) in
                    let inline281 = @runtime(core.string_println)(inline280) in
                    ()
                  } in
                  let inline283 = @intrinsic(ref.get)(even_sum/2) in
                  jump inline_cont282(inline283)
                } in
                let inline285 = "even sum: " in
                let inline286 = trait_impl#ToString#string#to_string(inline285) in
                let inline287 = @runtime(core.string_print)(inline286) in
                jump inline_cont284(())
              } in
              joinrec {
                join loop204() -> unit {
                  join inline_cont277(t205: int) -> unit {
                    let t206 = (t205 <= 10) in
                    if t206 {
                      join inline_cont275(cur/4: int) -> unit {
                        let t207 = (cur/4 + 1) in
                        join inline_cont273(_wild193: unit) -> unit {
                          let t209 = (cur/4 / 2) in
                          let t210 = (t209 * 2) in
                          join inline_cont271(t211: bool) -> unit {
                            join k208() -> unit {
                              jump loop204()
                            } in
                            if t211 {
                              join inline_cont269(t212: int) -> unit {
                                let t213 = (t212 + cur/4) in
                                let inline268 = @intrinsic(ref.set)(even_sum/2, t213) in
                                jump loop204()
                              } in
                              let inline270 = @intrinsic(ref.get)(even_sum/2) in
                              jump inline_cont269(inline270)
                            } else {
                              jump k208()
                            }
                          } in
                          let inline272 = (cur/4 == t210) in
                          jump inline_cont271(inline272)
                        } in
                        let inline274 = @intrinsic(ref.set)(j/3, t207) in
                        jump inline_cont273(())
                      } in
                      let inline276 = @intrinsic(ref.get)(j/3) in
                      jump inline_cont275(inline276)
                    } else {
                      jump exit202()
                    }
                  } in
                  let inline278 = @intrinsic(ref.get)(j/3) in
                  jump inline_cont277(inline278)
                } in
              } in
              jump loop204()
            } in
            let inline289 = 1 in
            let inline290 = @intrinsic(ref.new)(inline289) in
            jump inline_cont288(inline290)
          } in
          let inline292 = 0 in
          let inline293 = @intrinsic(ref.new)(inline292) in
          jump inline_cont291(inline293)
        } in
        let inline295 = trait_impl#ToString#int#to_string(t201) in
        let inline296 = @runtime(core.string_println)(inline295) in
        jump inline_cont294(())
      } in
      let inline298 = @intrinsic(ref.get)(sum/0) in
      jump inline_cont297(inline298)
    } in
    let inline300 = "sum up to break: " in
    let inline301 = trait_impl#ToString#string#to_string(inline300) in
    let inline302 = @runtime(core.string_print)(inline301) in
    jump inline_cont299(())
  } in
  joinrec {
    join loop214() -> unit {
      join inline_cont265(t215: int) -> unit {
        let t216 = (t215 <= 100) in
        if t216 {
          join inline_cont263(t223: int) -> unit {
            join inline_cont260(t224: bool) -> unit {
              join k217() -> unit {
                join inline_cont258(t218: int) -> unit {
                  join inline_cont256(t219: int) -> unit {
                    let t220 = (t218 + t219) in
                    join inline_cont254(_wild188: unit) -> unit {
                      join inline_cont252(t221: int) -> unit {
                        let t222 = (t221 + 1) in
                        let inline251 = @intrinsic(ref.set)(i/1, t222) in
                        jump loop214()
                      } in
                      let inline253 = @intrinsic(ref.get)(i/1) in
                      jump inline_cont252(inline253)
                    } in
                    let inline255 = @intrinsic(ref.set)(sum/0, t220) in
                    jump inline_cont254(())
                  } in
                  let inline257 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont256(inline257)
                } in
                let inline259 = @intrinsic(ref.get)(sum/0) in
                jump inline_cont258(inline259)
              } in
              if t224 {
                jump exit200()
              } else {
                jump k217()
              }
            } in
            let inline261 = 50 in
            let inline262 = (t223 == inline261) in
            jump inline_cont260(inline262)
          } in
          let inline264 = @intrinsic(ref.get)(i/1) in
          jump inline_cont263(inline264)
        } else {
          jump exit200()
        }
      } in
      let inline266 = @intrinsic(ref.get)(i/1) in
      jump inline_cont265(inline266)
    } in
  } in
  jump loop214()
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t227 = @intrinsic(ref.new)(value/273) in
  t227
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t230 = @intrinsic(ref.get)(self/274) in
  t230
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t233 = (self/101 == other/102) in
  t233
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t235 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont303(t237: string) -> unit {
    let t238 = @runtime(core.string_print)(t237) in
    ()
  } in
  jump inline_cont303(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont304(t240: string) -> unit {
    let t241 = @runtime(core.string_println)(t240) in
    ()
  } in
  let inline305 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont304(inline305)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t246 = @runtime(core.int_to_string)(self/67) in
  t246
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t249 = @runtime(core.int_hash)(self/127) in
  t249
}