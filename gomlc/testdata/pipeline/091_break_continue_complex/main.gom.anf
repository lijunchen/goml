fn main() -> unit {
  let sum/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let i/1 = inherent#Ref#Ref[T]#new__T_int(1) in
  join exit185() -> unit {
    let _wild175 = () in
    join inline_cont284(mtmp176: unit) -> unit {
      join inline_cont282(t186: int) -> unit {
        join inline_cont279(mtmp177: unit) -> unit {
          join inline_cont276(even_sum/2: Ref[int]) -> unit {
            join inline_cont273(j/3: Ref[int]) -> unit {
              join exit187() -> unit {
                let _wild181 = () in
                join inline_cont269(mtmp182: unit) -> unit {
                  join inline_cont267(t188: int) -> unit {
                    let inline265 = trait_impl#ToString#int#to_string(t188) in
                    let inline266 = @runtime(core.string_println)(inline265) in
                    ()
                  } in
                  let inline268 = @intrinsic(ref.get)(even_sum/2) in
                  jump inline_cont267(inline268)
                } in
                let inline270 = "even sum: " in
                let inline271 = trait_impl#ToString#string#to_string(inline270) in
                let inline272 = @runtime(core.string_print)(inline271) in
                jump inline_cont269(())
              } in
              joinrec {
                join loop189() -> unit {
                  join inline_cont262(t190: int) -> unit {
                    let t191 = (t190 <= 10) in
                    if t191 {
                      join inline_cont260(cur/4: int) -> unit {
                        let t192 = (cur/4 + 1) in
                        join inline_cont258(_wild178: unit) -> unit {
                          let t194 = (cur/4 / 2) in
                          let t195 = (t194 * 2) in
                          join inline_cont256(t196: bool) -> unit {
                            join k193() -> unit {
                              let _wild180 = () in
                              jump loop189()
                            } in
                            if t196 {
                              join inline_cont254(t197: int) -> unit {
                                let t198 = (t197 + cur/4) in
                                let inline253 = @intrinsic(ref.set)(even_sum/2, t198) in
                                jump loop189()
                              } in
                              let inline255 = @intrinsic(ref.get)(even_sum/2) in
                              jump inline_cont254(inline255)
                            } else {
                              jump k193()
                            }
                          } in
                          let inline257 = @intrinsic(eq.primitive)(cur/4, t195) in
                          jump inline_cont256(inline257)
                        } in
                        let inline259 = @intrinsic(ref.set)(j/3, t192) in
                        jump inline_cont258(())
                      } in
                      let inline261 = @intrinsic(ref.get)(j/3) in
                      jump inline_cont260(inline261)
                    } else {
                      jump exit187()
                    }
                  } in
                  let inline263 = @intrinsic(ref.get)(j/3) in
                  jump inline_cont262(inline263)
                } in
              } in
              jump loop189()
            } in
            let inline274 = 1 in
            let inline275 = @intrinsic(ref.new)(inline274) in
            jump inline_cont273(inline275)
          } in
          let inline277 = 0 in
          let inline278 = @intrinsic(ref.new)(inline277) in
          jump inline_cont276(inline278)
        } in
        let inline280 = trait_impl#ToString#int#to_string(t186) in
        let inline281 = @runtime(core.string_println)(inline280) in
        jump inline_cont279(())
      } in
      let inline283 = @intrinsic(ref.get)(sum/0) in
      jump inline_cont282(inline283)
    } in
    let inline285 = "sum up to break: " in
    let inline286 = trait_impl#ToString#string#to_string(inline285) in
    let inline287 = @runtime(core.string_print)(inline286) in
    jump inline_cont284(())
  } in
  joinrec {
    join loop199() -> unit {
      join inline_cont250(t200: int) -> unit {
        let t201 = (t200 <= 100) in
        if t201 {
          join inline_cont248(t208: int) -> unit {
            join inline_cont245(t209: bool) -> unit {
              join k202() -> unit {
                let _wild172 = () in
                join inline_cont243(t203: int) -> unit {
                  join inline_cont241(t204: int) -> unit {
                    let t205 = (t203 + t204) in
                    join inline_cont239(_wild173: unit) -> unit {
                      join inline_cont237(t206: int) -> unit {
                        let t207 = (t206 + 1) in
                        let inline236 = @intrinsic(ref.set)(i/1, t207) in
                        jump loop199()
                      } in
                      let inline238 = @intrinsic(ref.get)(i/1) in
                      jump inline_cont237(inline238)
                    } in
                    let inline240 = @intrinsic(ref.set)(sum/0, t205) in
                    jump inline_cont239(())
                  } in
                  let inline242 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont241(inline242)
                } in
                let inline244 = @intrinsic(ref.get)(sum/0) in
                jump inline_cont243(inline244)
              } in
              if t209 {
                jump exit185()
              } else {
                jump k202()
              }
            } in
            let inline246 = 50 in
            let inline247 = @intrinsic(eq.primitive)(t208, inline246) in
            jump inline_cont245(inline247)
          } in
          let inline249 = @intrinsic(ref.get)(i/1) in
          jump inline_cont248(inline249)
        } else {
          jump exit185()
        }
      } in
      let inline251 = @intrinsic(ref.get)(i/1) in
      jump inline_cont250(inline251)
    } in
  } in
  jump loop199()
}

fn inherent#Ref#Ref[T]#new__T_int(value/257: int) -> Ref[int] {
  let t212 = @intrinsic(ref.new)(value/257) in
  t212
}

fn inherent#Ref#Ref[T]#get__T_int(self/258: Ref[int]) -> int {
  let t215 = @intrinsic(ref.get)(self/258) in
  t215
}

fn trait_impl#PartialEq#int#eq(self/103: int, other/104: int) -> bool {
  let t218 = @intrinsic(eq.primitive)(self/103, other/104) in
  t218
}

fn inherent#Ref#Ref[T]#set__T_int(self/259: Ref[int], value/260: int) -> unit {
  let t220 = @intrinsic(ref.set)(self/259, value/260) in
  ()
}

fn print__T_string(value/30: string) -> unit {
  join inline_cont288(t222: string) -> unit {
    let t223 = @runtime(core.string_print)(t222) in
    ()
  } in
  jump inline_cont288(value/30)
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont289(t225: string) -> unit {
    let t226 = @runtime(core.string_println)(t225) in
    ()
  } in
  let inline290 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont289(inline290)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t231 = @runtime(core.int_to_string)(self/69) in
  t231
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t234 = @runtime(core.int_hash)(self/129) in
  t234
}