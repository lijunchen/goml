fn main() -> unit {
  let sum/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let i/1 = inherent#Ref#Ref[T]#new__T_int(1) in
  join exit195() -> unit {
    join inline_cont294(mtmp186: unit) -> unit {
      join inline_cont292(t196: int) -> unit {
        join inline_cont289(mtmp187: unit) -> unit {
          join inline_cont286(even_sum/2: Ref[int]) -> unit {
            join inline_cont283(j/3: Ref[int]) -> unit {
              join exit197() -> unit {
                join inline_cont279(mtmp192: unit) -> unit {
                  join inline_cont277(t198: int) -> unit {
                    let inline275 = trait_impl#ToString#int#to_string(t198) in
                    let inline276 = @runtime(core.string_println)(inline275) in
                    ()
                  } in
                  let inline278 = @intrinsic(ref.get)(even_sum/2) in
                  jump inline_cont277(inline278)
                } in
                let inline280 = "even sum: " in
                let inline281 = trait_impl#ToString#string#to_string(inline280) in
                let inline282 = @runtime(core.string_print)(inline281) in
                jump inline_cont279(())
              } in
              joinrec {
                join loop199() -> unit {
                  join inline_cont272(t200: int) -> unit {
                    let t201 = (t200 <= 10) in
                    if t201 {
                      join inline_cont270(cur/4: int) -> unit {
                        let t202 = (cur/4 + 1) in
                        join inline_cont268(_wild188: unit) -> unit {
                          let t204 = (cur/4 / 2) in
                          let t205 = (t204 * 2) in
                          join inline_cont266(t206: bool) -> unit {
                            join k203() -> unit {
                              jump loop199()
                            } in
                            if t206 {
                              join inline_cont264(t207: int) -> unit {
                                let t208 = (t207 + cur/4) in
                                let inline263 = @intrinsic(ref.set)(even_sum/2, t208) in
                                jump loop199()
                              } in
                              let inline265 = @intrinsic(ref.get)(even_sum/2) in
                              jump inline_cont264(inline265)
                            } else {
                              jump k203()
                            }
                          } in
                          let inline267 = (cur/4 == t205) in
                          jump inline_cont266(inline267)
                        } in
                        let inline269 = @intrinsic(ref.set)(j/3, t202) in
                        jump inline_cont268(())
                      } in
                      let inline271 = @intrinsic(ref.get)(j/3) in
                      jump inline_cont270(inline271)
                    } else {
                      jump exit197()
                    }
                  } in
                  let inline273 = @intrinsic(ref.get)(j/3) in
                  jump inline_cont272(inline273)
                } in
              } in
              jump loop199()
            } in
            let inline284 = 1 in
            let inline285 = @intrinsic(ref.new)(inline284) in
            jump inline_cont283(inline285)
          } in
          let inline287 = 0 in
          let inline288 = @intrinsic(ref.new)(inline287) in
          jump inline_cont286(inline288)
        } in
        let inline290 = trait_impl#ToString#int#to_string(t196) in
        let inline291 = @runtime(core.string_println)(inline290) in
        jump inline_cont289(())
      } in
      let inline293 = @intrinsic(ref.get)(sum/0) in
      jump inline_cont292(inline293)
    } in
    let inline295 = "sum up to break: " in
    let inline296 = trait_impl#ToString#string#to_string(inline295) in
    let inline297 = @runtime(core.string_print)(inline296) in
    jump inline_cont294(())
  } in
  joinrec {
    join loop209() -> unit {
      join inline_cont260(t210: int) -> unit {
        let t211 = (t210 <= 100) in
        if t211 {
          join inline_cont258(t218: int) -> unit {
            join inline_cont255(t219: bool) -> unit {
              join k212() -> unit {
                join inline_cont253(t213: int) -> unit {
                  join inline_cont251(t214: int) -> unit {
                    let t215 = (t213 + t214) in
                    join inline_cont249(_wild183: unit) -> unit {
                      join inline_cont247(t216: int) -> unit {
                        let t217 = (t216 + 1) in
                        let inline246 = @intrinsic(ref.set)(i/1, t217) in
                        jump loop209()
                      } in
                      let inline248 = @intrinsic(ref.get)(i/1) in
                      jump inline_cont247(inline248)
                    } in
                    let inline250 = @intrinsic(ref.set)(sum/0, t215) in
                    jump inline_cont249(())
                  } in
                  let inline252 = @intrinsic(ref.get)(i/1) in
                  jump inline_cont251(inline252)
                } in
                let inline254 = @intrinsic(ref.get)(sum/0) in
                jump inline_cont253(inline254)
              } in
              if t219 {
                jump exit195()
              } else {
                jump k212()
              }
            } in
            let inline256 = 50 in
            let inline257 = (t218 == inline256) in
            jump inline_cont255(inline257)
          } in
          let inline259 = @intrinsic(ref.get)(i/1) in
          jump inline_cont258(inline259)
        } else {
          jump exit195()
        }
      } in
      let inline261 = @intrinsic(ref.get)(i/1) in
      jump inline_cont260(inline261)
    } in
  } in
  jump loop209()
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t222 = @intrinsic(ref.new)(value/270) in
  t222
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t225 = @intrinsic(ref.get)(self/271) in
  t225
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t228 = (self/101 == other/102) in
  t228
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t230 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont298(t232: string) -> unit {
    let t233 = @runtime(core.string_print)(t232) in
    ()
  } in
  jump inline_cont298(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont299(t235: string) -> unit {
    let t236 = @runtime(core.string_println)(t235) in
    ()
  } in
  let inline300 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont299(inline300)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t241 = @runtime(core.int_to_string)(self/67) in
  t241
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t244 = @runtime(core.int_hash)(self/127) in
  t244
}