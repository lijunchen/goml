fn main() -> unit {
  join inline_cont525(sum/0: Ref[int]) -> unit {
    join inline_cont522(i/1: Ref[int]) -> unit {
      join exit424() -> unit {
        join inline_cont518(mtmp415: unit) -> unit {
          join inline_cont516(t425: int) -> unit {
            join inline_cont513(mtmp416: unit) -> unit {
              join inline_cont510(even_sum/2: Ref[int]) -> unit {
                join inline_cont507(j/3: Ref[int]) -> unit {
                  join exit426() -> unit {
                    join inline_cont503(mtmp421: unit) -> unit {
                      join inline_cont501(t427: int) -> unit {
                        let inline499 = trait_impl#ToString#int#to_string(t427) in
                        let inline500 = @runtime(core.string_println)(inline499) in
                        ()
                      } in
                      let inline502 = @intrinsic(ref.get)(even_sum/2) in
                      jump inline_cont501(inline502)
                    } in
                    let inline504 = "even sum: " in
                    let inline505 = trait_impl#ToString#string#to_string(inline504) in
                    let inline506 = @runtime(core.string_print)(inline505) in
                    jump inline_cont503(())
                  } in
                  joinrec {
                    join loop428() -> unit {
                      join inline_cont496(t429: int) -> unit {
                        let t430 = (t429 <= 10) in
                        if t430 {
                          join inline_cont494(cur/4: int) -> unit {
                            let t431 = (cur/4 + 1) in
                            join inline_cont492(_wild417: unit) -> unit {
                              let t433 = (cur/4 / 2) in
                              let t434 = (t433 * 2) in
                              let t435 = (cur/4 == t434) in
                              join k432() -> unit {
                                jump loop428()
                              } in
                              if t435 {
                                join inline_cont490(t436: int) -> unit {
                                  let t437 = (t436 + cur/4) in
                                  let inline489 = @intrinsic(ref.set)(even_sum/2, t437) in
                                  jump loop428()
                                } in
                                let inline491 = @intrinsic(ref.get)(even_sum/2) in
                                jump inline_cont490(inline491)
                              } else {
                                jump k432()
                              }
                            } in
                            let inline493 = @intrinsic(ref.set)(j/3, t431) in
                            jump inline_cont492(())
                          } in
                          let inline495 = @intrinsic(ref.get)(j/3) in
                          jump inline_cont494(inline495)
                        } else {
                          jump exit426()
                        }
                      } in
                      let inline497 = @intrinsic(ref.get)(j/3) in
                      jump inline_cont496(inline497)
                    } in
                  } in
                  jump loop428()
                } in
                let inline508 = 1 in
                let inline509 = @intrinsic(ref.new)(inline508) in
                jump inline_cont507(inline509)
              } in
              let inline511 = 0 in
              let inline512 = @intrinsic(ref.new)(inline511) in
              jump inline_cont510(inline512)
            } in
            let inline514 = trait_impl#ToString#int#to_string(t425) in
            let inline515 = @runtime(core.string_println)(inline514) in
            jump inline_cont513(())
          } in
          let inline517 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont516(inline517)
        } in
        let inline519 = "sum up to break: " in
        let inline520 = trait_impl#ToString#string#to_string(inline519) in
        let inline521 = @runtime(core.string_print)(inline520) in
        jump inline_cont518(())
      } in
      joinrec {
        join loop438() -> unit {
          join inline_cont486(t439: int) -> unit {
            let t440 = (t439 <= 100) in
            if t440 {
              join inline_cont484(t447: int) -> unit {
                let t448 = (t447 == 50) in
                join k441() -> unit {
                  join inline_cont482(t442: int) -> unit {
                    join inline_cont480(t443: int) -> unit {
                      let t444 = (t442 + t443) in
                      join inline_cont478(_wild412: unit) -> unit {
                        join inline_cont476(t445: int) -> unit {
                          let t446 = (t445 + 1) in
                          let inline475 = @intrinsic(ref.set)(i/1, t446) in
                          jump loop438()
                        } in
                        let inline477 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont476(inline477)
                      } in
                      let inline479 = @intrinsic(ref.set)(sum/0, t444) in
                      jump inline_cont478(())
                    } in
                    let inline481 = @intrinsic(ref.get)(i/1) in
                    jump inline_cont480(inline481)
                  } in
                  let inline483 = @intrinsic(ref.get)(sum/0) in
                  jump inline_cont482(inline483)
                } in
                if t448 {
                  jump exit424()
                } else {
                  jump k441()
                }
              } in
              let inline485 = @intrinsic(ref.get)(i/1) in
              jump inline_cont484(inline485)
            } else {
              jump exit424()
            }
          } in
          let inline487 = @intrinsic(ref.get)(i/1) in
          jump inline_cont486(inline487)
        } in
      } in
      jump loop438()
    } in
    let inline523 = 1 in
    let inline524 = @intrinsic(ref.new)(inline523) in
    jump inline_cont522(inline524)
  } in
  let inline526 = 0 in
  let inline527 = @intrinsic(ref.new)(inline526) in
  jump inline_cont525(inline527)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t451 = @intrinsic(ref.new)(value/431) in
  t451
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t454 = @intrinsic(ref.get)(self/432) in
  t454
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t456 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont528(t458: string) -> unit {
    let t459 = @runtime(core.string_print)(t458) in
    ()
  } in
  jump inline_cont528(value/0)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont529(t461: string) -> unit {
    let t462 = @runtime(core.string_println)(t461) in
    ()
  } in
  let inline530 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont529(inline530)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t467 = @runtime(core.int_to_string)(self/151) in
  t467
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t470 = (self/185 == other/186) in
  t470
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t473 = @runtime(core.int_hash)(self/211) in
  t473
}