fn main() -> unit {
  join inline_cont953(sum/0: Ref[isize]) -> unit {
    join inline_cont950(i/1: Ref[isize]) -> unit {
      join exit809() -> unit {
        join inline_cont946(mtmp800: unit) -> unit {
          join inline_cont944(t810: isize) -> unit {
            join inline_cont941(mtmp801: unit) -> unit {
              join inline_cont938(even_sum/2: Ref[isize]) -> unit {
                join inline_cont935(j/3: Ref[isize]) -> unit {
                  join exit811() -> unit {
                    join inline_cont931(mtmp806: unit) -> unit {
                      join inline_cont929(t812: isize) -> unit {
                        let inline927 = trait_impl#ToString#isize#to_string(t812) in
                        let inline928 = @runtime(core.string_println)(inline927) in
                        ()
                      } in
                      let inline930 = @intrinsic(ref.get)(even_sum/2) in
                      jump inline_cont929(inline930)
                    } in
                    let inline932 = "even sum: " in
                    let inline933 = trait_impl#ToString#string#to_string(inline932) in
                    let inline934 = @runtime(core.string_print)(inline933) in
                    jump inline_cont931(())
                  } in
                  joinrec {
                    join loop813() -> unit {
                      join inline_cont924(t814: isize) -> unit {
                        let t815 = (t814 <= 10) in
                        if t815 {
                          join inline_cont922(cur/4: isize) -> unit {
                            let t816 = (cur/4 + 1) in
                            join inline_cont920(_wild802: unit) -> unit {
                              let t818 = (cur/4 / 2) in
                              let t819 = (t818 * 2) in
                              let t820 = (cur/4 == t819) in
                              join k817() -> unit {
                                jump loop813()
                              } in
                              if t820 {
                                join inline_cont918(t821: isize) -> unit {
                                  let t822 = (t821 + cur/4) in
                                  let inline917 = @intrinsic(ref.set)(even_sum/2, t822) in
                                  jump loop813()
                                } in
                                let inline919 = @intrinsic(ref.get)(even_sum/2) in
                                jump inline_cont918(inline919)
                              } else {
                                jump k817()
                              }
                            } in
                            let inline921 = @intrinsic(ref.set)(j/3, t816) in
                            jump inline_cont920(())
                          } in
                          let inline923 = @intrinsic(ref.get)(j/3) in
                          jump inline_cont922(inline923)
                        } else {
                          jump exit811()
                        }
                      } in
                      let inline925 = @intrinsic(ref.get)(j/3) in
                      jump inline_cont924(inline925)
                    } in
                  } in
                  jump loop813()
                } in
                let inline936 = 1 in
                let inline937 = @intrinsic(ref.new)(inline936) in
                jump inline_cont935(inline937)
              } in
              let inline939 = 0 in
              let inline940 = @intrinsic(ref.new)(inline939) in
              jump inline_cont938(inline940)
            } in
            let inline942 = trait_impl#ToString#isize#to_string(t810) in
            let inline943 = @runtime(core.string_println)(inline942) in
            jump inline_cont941(())
          } in
          let inline945 = @intrinsic(ref.get)(sum/0) in
          jump inline_cont944(inline945)
        } in
        let inline947 = "sum up to break: " in
        let inline948 = trait_impl#ToString#string#to_string(inline947) in
        let inline949 = @runtime(core.string_print)(inline948) in
        jump inline_cont946(())
      } in
      joinrec {
        join loop823() -> unit {
          join inline_cont914(t824: isize) -> unit {
            let t825 = (t824 <= 100) in
            if t825 {
              join inline_cont912(t832: isize) -> unit {
                let t833 = (t832 == 50) in
                join k826() -> unit {
                  join inline_cont910(t827: isize) -> unit {
                    join inline_cont908(t828: isize) -> unit {
                      let t829 = (t827 + t828) in
                      join inline_cont906(_wild797: unit) -> unit {
                        join inline_cont904(t830: isize) -> unit {
                          let t831 = (t830 + 1) in
                          let inline903 = @intrinsic(ref.set)(i/1, t831) in
                          jump loop823()
                        } in
                        let inline905 = @intrinsic(ref.get)(i/1) in
                        jump inline_cont904(inline905)
                      } in
                      let inline907 = @intrinsic(ref.set)(sum/0, t829) in
                      jump inline_cont906(())
                    } in
                    let inline909 = @intrinsic(ref.get)(i/1) in
                    jump inline_cont908(inline909)
                  } in
                  let inline911 = @intrinsic(ref.get)(sum/0) in
                  jump inline_cont910(inline911)
                } in
                if t833 {
                  jump exit809()
                } else {
                  jump k826()
                }
              } in
              let inline913 = @intrinsic(ref.get)(i/1) in
              jump inline_cont912(inline913)
            } else {
              jump exit809()
            }
          } in
          let inline915 = @intrinsic(ref.get)(i/1) in
          jump inline_cont914(inline915)
        } in
      } in
      jump loop823()
    } in
    let inline951 = 1 in
    let inline952 = @intrinsic(ref.new)(inline951) in
    jump inline_cont950(inline952)
  } in
  let inline954 = 0 in
  let inline955 = @intrinsic(ref.new)(inline954) in
  jump inline_cont953(inline955)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t836 = @intrinsic(ref.new)(value/684) in
  t836
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t839 = @intrinsic(ref.get)(self/685) in
  t839
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t841 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn print__T_string(value/0: string) -> unit {
  join inline_cont956(t843: string) -> unit {
    let t844 = @runtime(core.string_print)(t843) in
    ()
  } in
  jump inline_cont956(value/0)
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont957(t846: string) -> unit {
    let t847 = @runtime(core.string_println)(t846) in
    ()
  } in
  let inline958 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont957(inline958)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline960 = (self/404 as i64) in
  let inline961 = signed_decimal_string(inline960) in
  inline961
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t855 = (value/222 as i64) in
  let inline963 = (t855 < 0) in
  if inline963 {
    let inline964 = (t855 as u64) in
    let inline965 = (0 - inline964) in
    let inline966 = decimal_string(inline965) in
    let inline967 = ("-" + inline966) in
    inline967
  } else {
    let inline968 = (t855 as u64) in
    let inline969 = decimal_string(inline968) in
    inline969
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t861 = (value/214 < 0) in
  if t861 {
    let t862 = (value/214 as u64) in
    let t863 = (0 - t862) in
    let t864 = decimal_string(t863) in
    let t865 = ("-" + t864) in
    t865
  } else {
    let t866 = (value/214 as u64) in
    let t867 = decimal_string(t866) in
    t867
  }
}

fn decimal_string(value/208: u64) -> string {
  let t890 = (value/208 == 0) in
  join k870() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit871() -> string {
      join inline_cont978(t872: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t872) in
        let offset/212 = 0 in
        join exit873() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop874() -> string {
            join inline_cont976(t875: isize) -> string {
              let t876 = (offset/212 < t875) in
              if t876 {
                join inline_cont974(t877: isize) -> string {
                  let t878 = (t877 - offset/212) in
                  let t879 = (t878 - 1) in
                  let t880 = @intrinsic(vec.get)(reversed/209, t879) in
                  join inline_cont972(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t881 = (compound_old358 + compound_value359) in
                    let t882 = offset/212 = t881 in
                    jump loop874()
                  } in
                  let inline973 = @intrinsic(vec.push)(bytes/211, t880) in
                  jump inline_cont972(())
                } in
                let inline975 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont974(inline975)
              } else {
                jump exit873()
              }
            } in
            let inline977 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont976(inline977)
          } in
        } in
        jump loop874()
      } in
      let inline979 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont978(inline979)
    } in
    joinrec {
      join loop883() -> string {
        let t884 = (remaining/210 > 0) in
        if t884 {
          let t885 = (remaining/210 % 10) in
          let t886 = (t885 as u8) in
          let t887 = (t886 + 48) in
          join inline_cont970(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t888 = (compound_old353 / compound_value354) in
            let t889 = remaining/210 = t888 in
            jump loop883()
          } in
          let inline971 = @intrinsic(vec.push)(reversed/209, t887) in
          jump inline_cont970(())
        } else {
          jump exit871()
        }
      } in
    } in
    jump loop883()
  } in
  if t890 {
    "0"
  } else {
    jump k870()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t892 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t895 = @intrinsic(vec.len)(self/526) in
  t895
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t898 = (self/438 == other/439) in
  t898
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t901 = @runtime(core.int_hash)(self/464) in
  t901
}