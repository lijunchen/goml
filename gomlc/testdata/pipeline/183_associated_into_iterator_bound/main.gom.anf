fn trait_impl#Batch#Numbers#items(self/0: Numbers) -> Vec[i32] {
  let t0 = Numbers.values(self/0) in
  t0
}

fn main() -> () {
  join inline_cont0(values/0: Vec[i32]) -> () {
    join inline_cont1(_wild0: ()) -> () {
      join inline_cont2(_wild1: ()) -> () {
        join inline_cont3(_wild2: ()) -> () {
          let t0 = Numbers { values: values/0 } in
          let t1 = count__B_Numbers(t0) in
          let inline0 = trait_impl#ToString#i32#to_string(t1) in
          let inline1 = @runtime(core.string_println)(inline0) in
          ()
        } in
        let inline2 = 30 in
        let inline3 = @intrinsic(vec.push)(values/0, inline2) in
        jump inline_cont3(())
      } in
      let inline4 = 20 in
      let inline5 = @intrinsic(vec.push)(values/0, inline4) in
      jump inline_cont2(())
    } in
    let inline6 = 10 in
    let inline7 = @intrinsic(vec.push)(values/0, inline6) in
    jump inline_cont1(())
  } in
  let inline8 = @intrinsic(vec.new)() in
  jump inline_cont0(inline8)
}

fn inherent#Vec#Vec[T]#new__T_i32() -> Vec[i32] {
  let t0 = @intrinsic(vec.new)() in
  t0
}

fn inherent#Vec#Vec[T]#push__T_i32(self/0: Vec[i32], elem/0: i32) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn println__T_i32(value/0: i32) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int32_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn count__B_Numbers(batch/0: Numbers) -> i32 {
  join inline_cont0(total/0: Ref[i32]) -> i32 {
    join inline_cont1(t0: Vec[i32]) -> i32 {
      join inline_cont2(for_iter0: FnIterator__i32) -> i32 {
        join exit0() -> i32 {
          let inline0 = @intrinsic(ref.get)(total/0) in
          inline0
        } in
        joinrec {
          join loop0() -> i32 {
            join inline_cont3(for_next0: Option__i32) -> i32 {
              match for_next0 {
                Tag_0 => {
                  jump exit0()
                },
                Tag_1 => {
                  join inline_cont4(t1: i32) -> i32 {
                    let t2 = (t1 + 1) in
                    let inline1 = @intrinsic(ref.set)(total/0, t2) in
                    jump loop0()
                  } in
                  let inline2 = @intrinsic(ref.get)(total/0) in
                  jump inline_cont4(inline2)
                },
              }
            } in
            let inline3 = FnIterator__i32.next_fn(for_iter0) in
            let inline4 = inline3() in
            jump inline_cont3(inline4)
          } in
        } in
        jump loop0()
      } in
      let inline5 = inherent#Vec#Vec[T]#iter__T_i0(t0) in
      jump inline_cont2(inline5)
    } in
    let inline6 = Numbers.values(batch/0) in
    jump inline_cont1(inline6)
  } in
  let inline7 = 0 in
  let inline8 = @intrinsic(ref.new)(inline7) in
  jump inline_cont0(inline8)
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn trait_impl#IntoIterator#Vec[i32]#into_iter(self/0: Vec[i32]) -> FnIterator__i32 {
  let inline0 = @intrinsic(ref.new)(0) in
  let inline1 = inherent#Vec#Vec[T]#len__T_i0(self/0) in
  let inline2 =
    closure_env_inherent_Vec_Vec_T_iter_T_i32_0 {
      index_0: inline0,
      len_1: inline1,
      self_2: self/0
    } in
  let inline3 =
    pack_closure[inherent#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#apply](
      inline2
    ) in
  let inline4 = inherent#FnIterator#FnIterator[T]#from_fn__T_i0(inline3) in
  inline4
}

fn trait_impl#Iterator#FnIterator__i32#next(self/0: FnIterator__i32) -> Option__i32 {
  let t0 = FnIterator__i32.next_fn(self/0) in
  let t1 = t0() in
  t1
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> () {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn inherent#Vec#Vec[T]#iter__T_i32(self/0: Vec[i32]) -> FnIterator__i32 {
  let index/0 = @intrinsic(ref.new)(0) in
  join inline_cont0(len/0: isize) -> FnIterator__i32 {
    let t0 =
      closure_env_inherent_Vec_Vec_T_iter_T_i32_0 {
        index_0: index/0,
        len_1: len/0,
        self_2: self/0
      } in
    let t1 =
      pack_closure[inherent#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#apply](
        t0
      ) in
    let inline0 = FnIterator__i32 { next_fn: t1 } in
    inline0
  } in
  let inline1 = @intrinsic(vec.len)(self/0) in
  jump inline_cont0(inline1)
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn inherent#Vec#Vec[T]#len__T_i32(self/0: Vec[i32]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_i32(
  next_fn/0: () -> Option__i32
) -> FnIterator__i32 {
  let t0 = FnIterator__i32 { next_fn: next_fn/0 } in
  t0
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}

fn inherent#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#closure_env_inherent_Vec_Vec_T_iter_T_i32_0#apply(
  env0: closure_env_inherent_Vec_Vec_T_iter_T_i32_0
) -> Option__i32 {
  let index/0 = closure_env_inherent_Vec_Vec_T_iter_T_i32_0.index_0(env0) in
  let len/0 = closure_env_inherent_Vec_Vec_T_iter_T_i32_0.len_1(env0) in
  let self/0 = closure_env_inherent_Vec_Vec_T_iter_T_i32_0.self_2(env0) in
  let current/0 = @intrinsic(ref.get)(index/0) in
  let t0 = (current/0 < len/0) in
  if t0 {
    let value/0 = @intrinsic(vec.get)(self/0, current/0) in
    let t1 = (current/0 + 1) in
    let _wild0 = @intrinsic(ref.set)(index/0, t1) in
    let t2 = Option__i32::Some(value/0) in
    t2
  } else {
    Tag_0
  }
}