fn main() -> unit {
    let mut x/0: isize = 0;
    let get/1: () -> isize = || => (x/0 : isize);
    (x/0 : isize) = 41;
    (println : (string) -> unit)((isize::to_string : (isize) -> string)((get/1 : () -> isize)()));
}