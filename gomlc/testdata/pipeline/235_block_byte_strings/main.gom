fn answer() -> int {
    return {
        let base = 40;
        base + 2
    };
}

fn loop_answer() -> int {
    loop {
        break {
            let base = 6;
            base * 7
        };
    }
}

fn main() -> unit {
    let plain: Vec[byte] = b"A\n\x42";
    let empty = b"";
    let raw = br#"raw \n bytes"#;
    let quoted = br##"quoted "text" and #"##;
    let nested = {
        let value = answer();
        value + loop_answer()
    };
    println(plain.len().to_string());
    println(empty.len().to_string());
    println((plain.get(0).to_int()).to_string());
    println((plain.get(1).to_int()).to_string());
    println((plain.get(2).to_int()).to_string());
    println(raw.len().to_string());
    println(quoted.len().to_string());
    println(nested.to_string());
    if ({
        true
    }) {
        println("block condition")
    }
}
