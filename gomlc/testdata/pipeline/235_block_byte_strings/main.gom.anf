fn answer() -> isize {
  let base/0 = 40 in
  let t809 = (base/0 + 2) in
  t809
}

fn loop_answer() -> isize {
  join exit812(jp813: isize) -> isize {
    jp813
  } in
  joinrec {
    join loop_expr814() -> isize {
      let base/1 = 6 in
      let t815 = (base/1 * 7) in
      jump exit812(t815)
    } in
  } in
  jump loop_expr814()
}

fn main() -> unit {
  let plain/2 = bytes[65, 10, 66] in
  let empty/3 = bytes[] in
  let raw/4 = bytes[114, 97, 119, 32, 92, 110, 32, 98, 121, 116, 101, 115] in
  let quoted/5 =
    bytes[113, 117, 111, 116, 101, 100, 32, 34, 116, 101, 120, 116, 34, 32, 97, 110, 100, 32, 35] in
  let fixed/6 = [65, 66, 67] in
  let value/7 = answer() in
  let t817 = loop_answer() in
  let t818 = (value/7 + t817) in
  let t819 = inherent#Vec#Vec[T]#len__T_u8(plain/2) in
  let t820 = inherent#isize#isize#to_string(t819) in
  let _wild798 = println__T_string(t820) in
  let t821 = inherent#Vec#Vec[T]#len__T_u8(empty/3) in
  let t822 = inherent#isize#isize#to_string(t821) in
  let _wild799 = println__T_string(t822) in
  join inline_cont945(t823: u8) -> unit {
    let t824 = (t823 as isize) in
    join inline_cont943(t825: string) -> unit {
      join inline_cont940(_wild800: unit) -> unit {
        join inline_cont937(t826: u8) -> unit {
          let t827 = (t826 as isize) in
          join inline_cont935(t828: string) -> unit {
            join inline_cont932(_wild801: unit) -> unit {
              join inline_cont929(t829: u8) -> unit {
                let t830 = (t829 as isize) in
                join inline_cont927(t831: string) -> unit {
                  join inline_cont924(_wild802: unit) -> unit {
                    join inline_cont922(t832: isize) -> unit {
                      join inline_cont920(t833: string) -> unit {
                        join inline_cont917(_wild803: unit) -> unit {
                          join inline_cont915(t834: isize) -> unit {
                            join inline_cont913(t835: string) -> unit {
                              join inline_cont910(_wild804: unit) -> unit {
                                let t836 = @intrinsic(array.get)(fixed/6, 2) in
                                let t837 = (t836 as isize) in
                                join inline_cont908(t838: string) -> unit {
                                  join inline_cont905(_wild805: unit) -> unit {
                                    join inline_cont903(t839: string) -> unit {
                                      join inline_cont900(_wild806: unit) -> unit {
                                        let inline897 = "block condition" in
                                        let inline898 =
                                          trait_impl#ToString#string#to_string(inline897) in
                                        let inline899 = @runtime(core.string_println)(inline898) in
                                        ()
                                      } in
                                      let inline901 = trait_impl#ToString#string#to_string(t839) in
                                      let inline902 = @runtime(core.string_println)(inline901) in
                                      jump inline_cont900(())
                                    } in
                                    let inline904 = __goml_builtin_int_to_string(t818) in
                                    jump inline_cont903(inline904)
                                  } in
                                  let inline906 = trait_impl#ToString#string#to_string(t838) in
                                  let inline907 = @runtime(core.string_println)(inline906) in
                                  jump inline_cont905(())
                                } in
                                let inline909 = __goml_builtin_int_to_string(t837) in
                                jump inline_cont908(inline909)
                              } in
                              let inline911 = trait_impl#ToString#string#to_string(t835) in
                              let inline912 = @runtime(core.string_println)(inline911) in
                              jump inline_cont910(())
                            } in
                            let inline914 = __goml_builtin_int_to_string(t834) in
                            jump inline_cont913(inline914)
                          } in
                          let inline916 = @intrinsic(vec.len)(quoted/5) in
                          jump inline_cont915(inline916)
                        } in
                        let inline918 = trait_impl#ToString#string#to_string(t833) in
                        let inline919 = @runtime(core.string_println)(inline918) in
                        jump inline_cont917(())
                      } in
                      let inline921 = __goml_builtin_int_to_string(t832) in
                      jump inline_cont920(inline921)
                    } in
                    let inline923 = @intrinsic(vec.len)(raw/4) in
                    jump inline_cont922(inline923)
                  } in
                  let inline925 = trait_impl#ToString#string#to_string(t831) in
                  let inline926 = @runtime(core.string_println)(inline925) in
                  jump inline_cont924(())
                } in
                let inline928 = __goml_builtin_int_to_string(t830) in
                jump inline_cont927(inline928)
              } in
              let inline930 = 2 in
              let inline931 = @intrinsic(vec.get)(plain/2, inline930) in
              jump inline_cont929(inline931)
            } in
            let inline933 = trait_impl#ToString#string#to_string(t828) in
            let inline934 = @runtime(core.string_println)(inline933) in
            jump inline_cont932(())
          } in
          let inline936 = __goml_builtin_int_to_string(t827) in
          jump inline_cont935(inline936)
        } in
        let inline938 = 1 in
        let inline939 = @intrinsic(vec.get)(plain/2, inline938) in
        jump inline_cont937(inline939)
      } in
      let inline941 = trait_impl#ToString#string#to_string(t825) in
      let inline942 = @runtime(core.string_println)(inline941) in
      jump inline_cont940(())
    } in
    let inline944 = __goml_builtin_int_to_string(t824) in
    jump inline_cont943(inline944)
  } in
  let inline946 = 0 in
  let inline947 = @intrinsic(vec.get)(plain/2, inline946) in
  jump inline_cont945(inline947)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont948(t843: string) -> unit {
    let t844 = @runtime(core.string_println)(t843) in
    ()
  } in
  jump inline_cont948(value/1)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t847 = @intrinsic(vec.len)(self/526) in
  t847
}

fn inherent#isize#isize#to_string(self/285: isize) -> string {
  let inline950 = (self/285 as i64) in
  let inline951 = signed_decimal_string(inline950) in
  inline951
}

fn inherent#Vec#Vec[T]#get__T_u8(self/521: Vec[u8], index/522: isize) -> u8 {
  let t853 = @intrinsic(vec.get)(self/521, index/522) in
  t853
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t858 = (value/222 as i64) in
  let inline953 = (t858 < 0) in
  if inline953 {
    let inline954 = (t858 as u64) in
    let inline955 = (0 - inline954) in
    let inline956 = decimal_string(inline955) in
    let inline957 = ("-" + inline956) in
    inline957
  } else {
    let inline958 = (t858 as u64) in
    let inline959 = decimal_string(inline958) in
    inline959
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t864 = (value/214 < 0) in
  if t864 {
    let t865 = (value/214 as u64) in
    let t866 = (0 - t865) in
    let t867 = decimal_string(t866) in
    let t868 = ("-" + t867) in
    t868
  } else {
    let t869 = (value/214 as u64) in
    let t870 = decimal_string(t869) in
    t870
  }
}

fn decimal_string(value/208: u64) -> string {
  let t893 = (value/208 == 0) in
  join k873() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit874() -> string {
      join inline_cont968(t875: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t875) in
        let offset/212 = 0 in
        join exit876() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop877() -> string {
            join inline_cont966(t878: isize) -> string {
              let t879 = (offset/212 < t878) in
              if t879 {
                join inline_cont964(t880: isize) -> string {
                  let t881 = (t880 - offset/212) in
                  let t882 = (t881 - 1) in
                  let t883 = @intrinsic(vec.get)(reversed/209, t882) in
                  join inline_cont962(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t884 = (compound_old358 + compound_value359) in
                    let t885 = offset/212 = t884 in
                    jump loop877()
                  } in
                  let inline963 = @intrinsic(vec.push)(bytes/211, t883) in
                  jump inline_cont962(())
                } in
                let inline965 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont964(inline965)
              } else {
                jump exit876()
              }
            } in
            let inline967 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont966(inline967)
          } in
        } in
        jump loop877()
      } in
      let inline969 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont968(inline969)
    } in
    joinrec {
      join loop886() -> string {
        let t887 = (remaining/210 > 0) in
        if t887 {
          let t888 = (remaining/210 % 10) in
          let t889 = (t888 as u8) in
          let t890 = (t889 + 48) in
          join inline_cont960(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t891 = (compound_old353 / compound_value354) in
            let t892 = remaining/210 = t891 in
            jump loop886()
          } in
          let inline961 = @intrinsic(vec.push)(reversed/209, t890) in
          jump inline_cont960(())
        } else {
          jump exit874()
        }
      } in
    } in
    jump loop886()
  } in
  if t893 {
    "0"
  } else {
    jump k873()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t895 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}