package main;
fn pair[First, Second](first: First, second: Second) -> (First, Second) {
    (first, second)
}

fn main() -> () {
    let make: (i32, string) -> (i32, string) = pair;
    let value = make(1, "x");
    println(value.0.to_string() + value.1)
}

