fn main() -> unit {
  let value/0 = "a你好z" in
  let t0 = inherent#string#string#starts_with(value/0, "a你") in
  join inline_cont0(_wild0: unit) -> unit {
    let t1 = inherent#string#string#starts_with(value/0, "你") in
    join inline_cont1(_wild1: unit) -> unit {
      let t2 = inherent#string#string#starts_with(value/0, "") in
      join inline_cont2(_wild2: unit) -> unit {
        let t3 = inherent#string#string#ends_with(value/0, "好z") in
        join inline_cont3(_wild3: unit) -> unit {
          let t4 = inherent#string#string#ends_with(value/0, "你好") in
          join inline_cont4(_wild4: unit) -> unit {
            join inline_cont5(t5: bool) -> unit {
              join inline_cont6(_wild5: unit) -> unit {
                let t6 = inherent#string#string#contains(value/0, "你好") in
                join inline_cont7(_wild6: unit) -> unit {
                  let t7 = inherent#string#string#contains(value/0, "好z") in
                  join inline_cont8(_wild7: unit) -> unit {
                    let t8 = inherent#string#string#contains(value/0, "你z") in
                    join inline_cont9(_wild8: unit) -> unit {
                      let t9 = inherent#string#string#contains(value/0, "") in
                      join inline_cont10(_wild9: unit) -> unit {
                        let t10 = inherent#string#string#contains(value/0, "a你好z!") in
                        let inline0 = trait_impl#ToString#bool#to_string(t10) in
                        let inline1 = @runtime(core.string_println)(inline0) in
                        ()
                      } in
                      let inline2 = trait_impl#ToString#bool#to_string(t9) in
                      let inline3 = @runtime(core.string_println)(inline2) in
                      jump inline_cont10(())
                    } in
                    let inline4 = trait_impl#ToString#bool#to_string(t8) in
                    let inline5 = @runtime(core.string_println)(inline4) in
                    jump inline_cont9(())
                  } in
                  let inline6 = trait_impl#ToString#bool#to_string(t7) in
                  let inline7 = @runtime(core.string_println)(inline6) in
                  jump inline_cont8(())
                } in
                let inline8 = trait_impl#ToString#bool#to_string(t6) in
                let inline9 = @runtime(core.string_println)(inline8) in
                jump inline_cont7(())
              } in
              let inline10 = trait_impl#ToString#bool#to_string(t5) in
              let inline11 = @runtime(core.string_println)(inline10) in
              jump inline_cont6(())
            } in
            let inline12 = "" in
            let inline13 = inherent#string#string#byte_len(inline12) in
            let inline14 = inherent#string#string#byte_len(value/0) in
            let inline15 = (inline13 > inline14) in
            if inline15 {
              jump inline_cont5(false)
            } else {
              let inline16 = inherent#string#string#byte_len(value/0) in
              let inline17 = inherent#string#string#byte_len(inline12) in
              let inline18 = (inline16 - inline17) in
              let inline19 = inherent#string#string#is_char_boundary(value/0, inline18) in
              if inline19 {
                let inline20 = inherent#string#string#byte_len(value/0) in
                let inline21 = inherent#string#string#byte_slice(value/0, inline18, inline20) in
                let inline22 = (inline21 == inline12) in
                jump inline_cont5(inline22)
              } else {
                jump inline_cont5(false)
              }
            }
          } in
          let inline23 = trait_impl#ToString#bool#to_string(t4) in
          let inline24 = @runtime(core.string_println)(inline23) in
          jump inline_cont4(())
        } in
        let inline25 = trait_impl#ToString#bool#to_string(t3) in
        let inline26 = @runtime(core.string_println)(inline25) in
        jump inline_cont3(())
      } in
      let inline27 = trait_impl#ToString#bool#to_string(t2) in
      let inline28 = @runtime(core.string_println)(inline27) in
      jump inline_cont2(())
    } in
    let inline29 = trait_impl#ToString#bool#to_string(t1) in
    let inline30 = @runtime(core.string_println)(inline29) in
    jump inline_cont1(())
  } in
  let inline31 = trait_impl#ToString#bool#to_string(t0) in
  let inline32 = @runtime(core.string_println)(inline31) in
  jump inline_cont0(())
}

fn println__T_bool(value/0: bool) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn inherent#string#string#starts_with(self/0: string, prefix/0: string) -> bool {
  join inline_cont0(t0: isize) -> bool {
    join inline_cont1(t1: isize) -> bool {
      let t2 = (t0 <= t1) in
      join k0(jp0: bool) -> bool {
        if jp0 {
          join inline_cont2(t3: isize) -> bool {
            join inline_cont3(t4: string) -> bool {
              let t5 = (t4 == prefix/0) in
              t5
            } in
            let inline0 = 0 in
            let inline1 = string_byte_slice(self/0, inline0, t3) in
            jump inline_cont3(inline1)
          } in
          let inline2 = @runtime(core.string_len)(prefix/0) in
          jump inline_cont2(inline2)
        } else {
          false
        }
      } in
      if t2 {
        join inline_cont4(t6: isize) -> bool {
          let inline3 = string_is_char_boundary(self/0, t6) in
          jump k0(inline3)
        } in
        let inline4 = @runtime(core.string_len)(prefix/0) in
        jump inline_cont4(inline4)
      } else {
        jump k0(false)
      }
    } in
    let inline5 = @runtime(core.string_len)(self/0) in
    jump inline_cont1(inline5)
  } in
  let inline6 = @runtime(core.string_len)(prefix/0) in
  jump inline_cont0(inline6)
}

fn inherent#string#string#ends_with(self/0: string, suffix/0: string) -> bool {
  join inline_cont0(t0: isize) -> bool {
    join inline_cont1(t1: isize) -> bool {
      let t2 = (t0 > t1) in
      if t2 {
        false
      } else {
        join inline_cont2(t3: isize) -> bool {
          join inline_cont3(t4: isize) -> bool {
            let start/0 = (t3 - t4) in
            join inline_cont4(t5: bool) -> bool {
              if t5 {
                join inline_cont5(t6: isize) -> bool {
                  join inline_cont6(t7: string) -> bool {
                    let t8 = (t7 == suffix/0) in
                    t8
                  } in
                  let inline0 = string_byte_slice(self/0, start/0, t6) in
                  jump inline_cont6(inline0)
                } in
                let inline1 = @runtime(core.string_len)(self/0) in
                jump inline_cont5(inline1)
              } else {
                false
              }
            } in
            let inline2 = string_is_char_boundary(self/0, start/0) in
            jump inline_cont4(inline2)
          } in
          let inline3 = @runtime(core.string_len)(suffix/0) in
          jump inline_cont3(inline3)
        } in
        let inline4 = @runtime(core.string_len)(self/0) in
        jump inline_cont2(inline4)
      }
    } in
    let inline5 = @runtime(core.string_len)(self/0) in
    jump inline_cont1(inline5)
  } in
  let inline6 = @runtime(core.string_len)(suffix/0) in
  jump inline_cont0(inline6)
}

fn inherent#string#string#contains(self/0: string, expected/0: string) -> bool {
  join inline_cont0(t0: isize) -> bool {
    let t1 = (t0 == 0) in
    if t1 {
      true
    } else {
      join inline_cont1(t2: isize) -> bool {
        join inline_cont2(t3: isize) -> bool {
          let t4 = (t2 > t3) in
          if t4 {
            false
          } else {
            join inline_cont3(t5: isize) -> bool {
              join inline_cont4(t6: isize) -> bool {
                let t7 = (t5 - t6) in
                let t8 = (t7 + 1) in
                join inline_cont5(t9: FnIterator__isize) -> bool {
                  join inline_cont6(for_iter0: FnIterator__isize) -> bool {
                    join exit0() -> bool {
                      false
                    } in
                    joinrec {
                      join loop0() -> bool {
                        join inline_cont7(for_next0: Option__isize) -> bool {
                          match for_next0 {
                            Tag_0 => {
                              jump exit0()
                            },
                            Tag_1 => {
                              let x0 = Option__isize::Some._0(for_next0) in
                              join inline_cont8(t10: isize) -> bool {
                                let end/0 = (x0 + t10) in
                                join inline_cont9(t11: bool) -> bool {
                                  join k0(jp0: bool) -> bool {
                                    join k1(jp1: bool) -> bool {
                                      if jp1 {
                                        true
                                      } else {
                                        jump loop0()
                                      }
                                    } in
                                    if jp0 {
                                      join inline_cont10(t12: string) -> bool {
                                        let t13 = (t12 == expected/0) in
                                        jump k1(t13)
                                      } in
                                      let inline0 = string_byte_slice(self/0, x0, end/0) in
                                      jump inline_cont10(inline0)
                                    } else {
                                      jump k1(false)
                                    }
                                  } in
                                  if t11 {
                                    let inline1 = string_is_char_boundary(self/0, end/0) in
                                    jump k0(inline1)
                                  } else {
                                    jump k0(false)
                                  }
                                } in
                                let inline2 = string_is_char_boundary(self/0, x0) in
                                jump inline_cont9(inline2)
                              } in
                              let inline3 = @runtime(core.string_len)(expected/0) in
                              jump inline_cont8(inline3)
                            },
                          }
                        } in
                        let inline4 = FnIterator__isize.next_fn(for_iter0) in
                        let inline5 = inline4() in
                        jump inline_cont7(inline5)
                      } in
                    } in
                    jump loop0()
                  } in
                  jump inline_cont6(t9)
                } in
                let inline6 = 0 in
                let inline7 = @intrinsic(ref.new)(inline6) in
                let inline8 = closure_env_goml_builtin_range_0 { current_0: inline7, end_1: t8 } in
                let inline9 =
                  pack_closure[inherent#closure_env_goml_builtin_range_0#closure_env_goml_builtin_range_0#apply](
                    inline8
                  ) in
                let inline10 = inherent#FnIterator#FnIterator[T]#from_fn__T_isize(inline9) in
                jump inline_cont5(inline10)
              } in
              let inline11 = @runtime(core.string_len)(expected/0) in
              jump inline_cont4(inline11)
            } in
            let inline12 = @runtime(core.string_len)(self/0) in
            jump inline_cont3(inline12)
          }
        } in
        let inline13 = @runtime(core.string_len)(self/0) in
        jump inline_cont2(inline13)
      } in
      let inline14 = @runtime(core.string_len)(expected/0) in
      jump inline_cont1(inline14)
    }
  } in
  let inline15 = @runtime(core.string_len)(expected/0) in
  jump inline_cont0(inline15)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn inherent#string#string#byte_len(self/0: string) -> isize {
  let t0 = @runtime(core.string_len)(self/0) in
  t0
}

fn inherent#string#string#is_char_boundary(self/0: string, index/0: isize) -> bool {
  let t0 = string_is_char_boundary(self/0, index/0) in
  t0
}

fn inherent#string#string#byte_slice(self/0: string, start/0: isize, end/0: isize) -> string {
  let inline0 = string_is_char_boundary(self/0, start/0) in
  join inline_join0(inline1: bool) -> string {
    if inline1 {
      let inline2 = @runtime(core.string_byte_slice)(self/0, start/0, end/0) in
      inline2
    } else {
      let inline3 = @runtime(core.string_byte_slice)(self/0, -1, -1) in
      inline3
    }
  } in
  if inline0 {
    let inline4 = string_is_char_boundary(self/0, end/0) in
    jump inline_join0(inline4)
  } else {
    jump inline_join0(false)
  }
}

fn __goml_builtin_range(start/0: isize, end/0: isize) -> FnIterator__isize {
  let current/0 = @intrinsic(ref.new)(start/0) in
  let t0 = closure_env_goml_builtin_range_0 { current_0: current/0, end_1: end/0 } in
  let t1 =
    pack_closure[inherent#closure_env_goml_builtin_range_0#closure_env_goml_builtin_range_0#apply](
      t0
    ) in
  let inline0 = FnIterator__isize { next_fn: t1 } in
  inline0
}

fn trait_impl#IntoIterator#FnIterator__isize#into_iter(
  self/0: FnIterator__isize
) -> FnIterator__isize {
  self/0
}

fn trait_impl#Iterator#FnIterator__isize#next(self/0: FnIterator__isize) -> Option__isize {
  let t0 = FnIterator__isize.next_fn(self/0) in
  let t1 = t0() in
  t1
}

fn string_is_char_boundary(value/0: string, index/0: isize) -> bool {
  let t0 = (index/0 < 0) in
  join k0(jp0: bool) -> bool {
    if jp0 {
      false
    } else {
      join inline_cont0(t1: isize) -> bool {
        let t2 = (index/0 == t1) in
        if t2 {
          true
        } else {
          join inline_cont1(t3: u8) -> bool {
            let t4 = (t3 & 192) in
            let t5 = (t4 != 128) in
            t5
          } in
          let inline0 = @runtime(core.string_byte_get)(value/0, index/0) in
          jump inline_cont1(inline0)
        }
      } in
      let inline1 = @runtime(core.string_len)(value/0) in
      jump inline_cont0(inline1)
    }
  } in
  if t0 {
    jump k0(true)
  } else {
    join inline_cont2(t6: isize) -> bool {
      let t7 = (index/0 > t6) in
      jump k0(t7)
    } in
    let inline2 = @runtime(core.string_len)(value/0) in
    jump inline_cont2(inline2)
  }
}

fn string_byte_slice(value/0: string, start/0: isize, end/0: isize) -> string {
  let t0 = string_is_char_boundary(value/0, start/0) in
  join k0(jp0: bool) -> string {
    if jp0 {
      let t1 = @runtime(core.string_byte_slice)(value/0, start/0, end/0) in
      t1
    } else {
      let t2 = @runtime(core.string_byte_slice)(value/0, -1, -1) in
      t2
    }
  } in
  if t0 {
    let t3 = string_is_char_boundary(value/0, end/0) in
    jump k0(t3)
  } else {
    jump k0(false)
  }
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
  next_fn/0: () -> Option__isize
) -> FnIterator__isize {
  let t0 = FnIterator__isize { next_fn: next_fn/0 } in
  t0
}

fn inherent#string#string#byte_get(self/0: string, index/0: isize) -> u8 {
  let t0 = @runtime(core.string_byte_get)(self/0, index/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}

fn inherent#closure_env_goml_builtin_range_0#closure_env_goml_builtin_range_0#apply(
  env0: closure_env_goml_builtin_range_0
) -> Option__isize {
  let current/0 = closure_env_goml_builtin_range_0.current_0(env0) in
  let end/0 = closure_env_goml_builtin_range_0.end_1(env0) in
  let value/0 = @intrinsic(ref.get)(current/0) in
  let t0 = (value/0 < end/0) in
  if t0 {
    let t1 = (value/0 + 1) in
    let _wild0 = @intrinsic(ref.set)(current/0, t1) in
    let t2 = Option__isize::Some(value/0) in
    t2
  } else {
    Tag_0
  }
}