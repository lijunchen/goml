fn main() -> unit {
  let _wild64 = if true {
      {
        @runtime(core.string_println)("if")
      }
  } else {
      {
        @runtime(core.string_println)("else")
      }
  } in
  let _wild66 = {
    let mtmp65 = 1 in
    match mtmp65 {
      1 => {
        @runtime(core.string_println)("match")
      },
      _ => (),
    }
  } in
  let index/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let _wild68 = while (inherent#Ref#Ref[T]#get__T_int(index/0) < 2) {
      {
        let _wild67 = @runtime(core.string_println)(inherent#int#int#to_string(inherent#Ref#Ref[T]#get__T_int(index/0))) in
        inherent#Ref#Ref[T]#set__T_int(index/0, (inherent#Ref#Ref[T]#get__T_int(index/0) + 1))
      }
  } in
  let values/1 = inherent#Vec#Vec[T]#new__T_string() in
  let _wild69 = inherent#Vec#Vec[T]#push__T_string(values/1, "for") in
  let _wild75 = {
    let for_source70 = values/1 in
    let for_limit71 = @intrinsic(vec.len)(for_source70) in
    let for_index72 = 0 in
    while (for_index72 < for_limit71) {
        {
          let for_item73 = @intrinsic(vec.get)(for_source70, for_index72) in
          let _wild74 = for_index72 = (for_index72 + 1) in
          {
            let value/2 = for_item73 in
            {
              @runtime(core.string_println)(value/2)
            }
          }
        }
    }
  } in
  @runtime(core.string_println)("done")
}

fn inherent#Ref#Ref[T]#new__T_int(value/209: int) -> Ref[int] {
  @intrinsic(ref.new)(value/209)
}

fn inherent#Ref#Ref[T]#get__T_int(self/210: Ref[int]) -> int {
  @intrinsic(ref.get)(self/210)
}

fn inherent#int#int#to_string(self/5: int) -> string {
  @runtime(core.int_to_string)(self/5)
}

fn inherent#Ref#Ref[T]#set__T_int(self/211: Ref[int], value/212: int) -> unit {
  @intrinsic(ref.set)(self/211, value/212)
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#push__T_string(self/128: Vec[string], elem/129: string) -> unit {
  @intrinsic(vec.push)(self/128, elem/129)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  @intrinsic(eq.primitive)(self/59, other/60)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  @runtime(core.int_hash)(self/85)
}