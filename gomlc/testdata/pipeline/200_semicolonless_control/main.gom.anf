fn main() -> unit {
  join k185() -> unit {
    let mtmp173 = 1 in
    join k186() -> unit {
      join inline_cont265(index/0: Ref[int]) -> unit {
        join exit187() -> unit {
          join inline_cont263(values/1: Vec[string]) -> unit {
            join inline_cont260(_wild177: unit) -> unit {
              let for_limit179 = @intrinsic(vec.len)(values/1) in
              let for_index180 = 0 in
              join exit188() -> unit {
                let inline257 = "done" in
                let inline258 = trait_impl#ToString#string#to_string(inline257) in
                let inline259 = @runtime(core.string_println)(inline258) in
                ()
              } in
              joinrec {
                join loop190() -> unit {
                  let t191 = (for_index180 < for_limit179) in
                  if t191 {
                    let for_item181 = @intrinsic(vec.get)(values/1, for_index180) in
                    let t192 = (for_index180 + 1) in
                    let _wild182 = for_index180 = t192 in
                    let inline254 = trait_impl#ToString#string#to_string(for_item181) in
                    let inline255 = @runtime(core.string_println)(inline254) in
                    jump loop190()
                  } else {
                    jump exit188()
                  }
                } in
              } in
              jump loop190()
            } in
            let inline261 = "for" in
            let inline262 = @intrinsic(vec.push)(values/1, inline261) in
            jump inline_cont260(())
          } in
          let inline264 = @intrinsic(vec.new)() in
          jump inline_cont263(inline264)
        } in
        joinrec {
          join loop194() -> unit {
            join inline_cont251(t195: int) -> unit {
              let t196 = (t195 < 2) in
              if t196 {
                join inline_cont249(t197: int) -> unit {
                  join inline_cont247(t198: string) -> unit {
                    join inline_cont244(_wild175: unit) -> unit {
                      join inline_cont242(t199: int) -> unit {
                        let t200 = (t199 + 1) in
                        let inline241 = @intrinsic(ref.set)(index/0, t200) in
                        jump loop194()
                      } in
                      let inline243 = @intrinsic(ref.get)(index/0) in
                      jump inline_cont242(inline243)
                    } in
                    let inline245 = trait_impl#ToString#string#to_string(t198) in
                    let inline246 = @runtime(core.string_println)(inline245) in
                    jump inline_cont244(())
                  } in
                  let inline248 = @runtime(core.int_to_string)(t197) in
                  jump inline_cont247(inline248)
                } in
                let inline250 = @intrinsic(ref.get)(index/0) in
                jump inline_cont249(inline250)
              } else {
                jump exit187()
              }
            } in
            let inline252 = @intrinsic(ref.get)(index/0) in
            jump inline_cont251(inline252)
          } in
        } in
        jump loop194()
      } in
      let inline266 = 0 in
      let inline267 = @intrinsic(ref.new)(inline266) in
      jump inline_cont265(inline267)
    } in
    match mtmp173 {
      1 => {
        let inline237 = "match" in
        let inline238 = trait_impl#ToString#string#to_string(inline237) in
        let inline239 = @runtime(core.string_println)(inline238) in
        jump k186()
      },
      _ => {
        jump k186()
      },
    }
  } in
  let inline233 = "if" in
  let inline234 = trait_impl#ToString#string#to_string(inline233) in
  let inline235 = @runtime(core.string_println)(inline234) in
  jump k185()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont268(t206: string) -> unit {
    let t207 = @runtime(core.string_println)(t206) in
    ()
  } in
  jump inline_cont268(value/1)
}

fn inherent#Ref#Ref[T]#new__T_int(value/255: int) -> Ref[int] {
  let t210 = @intrinsic(ref.new)(value/255) in
  t210
}

fn inherent#Ref#Ref[T]#get__T_int(self/256: Ref[int]) -> int {
  let t213 = @intrinsic(ref.get)(self/256) in
  t213
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t216 = @runtime(core.int_to_string)(self/32) in
  t216
}

fn inherent#Ref#Ref[T]#set__T_int(self/257: Ref[int], value/258: int) -> unit {
  let t218 = @intrinsic(ref.set)(self/257, value/258) in
  ()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  let t221 = @intrinsic(vec.new)() in
  t221
}

fn inherent#Vec#Vec[T]#push__T_string(self/174: Vec[string], elem/175: string) -> unit {
  let t223 = @intrinsic(vec.push)(self/174, elem/175) in
  ()
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t228 = (self/101 == other/102) in
  t228
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t231 = @runtime(core.int_hash)(self/127) in
  t231
}