fn main() -> unit {
  join ret164() -> unit {
    ()
  } in
  join k165() -> unit {
    let _wild152 = () in
    let mtmp153 = 1 in
    join k166() -> unit {
      let _wild154 = () in
      let index/0 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit167() -> unit {
        let _wild156 = () in
        let values/1 = inherent#Vec#Vec[T]#new__T_string() in
        let _wild157 = inherent#Vec#Vec[T]#push__T_string(values/1, "for") in
        let for_source158 = values/1 in
        let for_limit159 = @intrinsic(vec.len)(for_source158) in
        let for_index160 = 0 in
        join exit168() -> unit {
          let _wild163 = () in
          let t169 = @runtime(core.string_println)("done") in
          jump ret164()
        } in
        joinrec {
          join loop170() -> unit {
            let t171 = (for_index160 < for_limit159) in
            if t171 {
              let for_item161 = @intrinsic(vec.get)(for_source158, for_index160) in
              let t172 = (for_index160 + 1) in
              let _wild162 = for_index160 = t172 in
              let value/2 = for_item161 in
              let t173 = @runtime(core.string_println)(value/2) in
              jump loop170()
            } else {
              jump exit168()
            }
          } in
        } in
        jump loop170()
      } in
      joinrec {
        join loop174() -> unit {
          let t175 = inherent#Ref#Ref[T]#get__T_int(index/0) in
          let t176 = (t175 < 2) in
          if t176 {
            let t177 = inherent#Ref#Ref[T]#get__T_int(index/0) in
            let t178 = inherent#int#int#to_string(t177) in
            let _wild155 = @runtime(core.string_println)(t178) in
            let t179 = inherent#Ref#Ref[T]#get__T_int(index/0) in
            let t180 = (t179 + 1) in
            let t181 = inherent#Ref#Ref[T]#set__T_int(index/0, t180) in
            jump loop174()
          } else {
            jump exit167()
          }
        } in
      } in
      jump loop174()
    } in
    match mtmp153 {
      1 => {
        let t182 = @runtime(core.string_println)("match") in
        jump k166()
      },
      _ => {
        jump k166()
      },
    }
  } in
  if true {
    let t183 = @runtime(core.string_println)("if") in
    jump k165()
  } else {
    let t184 = @runtime(core.string_println)("else") in
    jump k165()
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret185(retv186: Ref[int]) -> Ref[int] {
    retv186
  } in
  let t187 = @intrinsic(ref.new)(value/207) in
  jump ret185(t187)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret188(retv189: int) -> int {
    retv189
  } in
  let t190 = @intrinsic(ref.get)(self/208) in
  jump ret188(t190)
}

fn inherent#int#int#to_string(self/5: int) -> string {
  join ret191(retv192: string) -> string {
    retv192
  } in
  let t193 = @runtime(core.int_to_string)(self/5) in
  jump ret191(t193)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret194() -> unit {
    ()
  } in
  let t195 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret194()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  join ret196(retv197: Vec[string]) -> Vec[string] {
    retv197
  } in
  let t198 = @intrinsic(vec.new)() in
  jump ret196(t198)
}

fn inherent#Vec#Vec[T]#push__T_string(self/126: Vec[string], elem/127: string) -> unit {
  join ret199() -> unit {
    ()
  } in
  let t200 = @intrinsic(vec.push)(self/126, elem/127) in
  jump ret199()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret201(retv202: bool) -> bool {
    retv202
  } in
  let t203 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret201(t203)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret204(retv205: uint64) -> uint64 {
    retv205
  } in
  let t206 = @runtime(core.int_hash)(self/85) in
  jump ret204(t206)
}