fn main() -> unit {
  join k424() -> unit {
    let mtmp412 = 1 in
    join k425() -> unit {
      join inline_cont504(index/0: Ref[int]) -> unit {
        join exit426() -> unit {
          join inline_cont502(values/1: Vec[string]) -> unit {
            join inline_cont499(_wild416: unit) -> unit {
              let for_limit418 = @intrinsic(vec.len)(values/1) in
              let for_index419 = 0 in
              join exit427() -> unit {
                let inline496 = "done" in
                let inline497 = trait_impl#ToString#string#to_string(inline496) in
                let inline498 = @runtime(core.string_println)(inline497) in
                ()
              } in
              joinrec {
                join loop429() -> unit {
                  let t430 = (for_index419 < for_limit418) in
                  if t430 {
                    let for_item420 = @intrinsic(vec.get)(values/1, for_index419) in
                    let t431 = (for_index419 + 1) in
                    let _wild421 = for_index419 = t431 in
                    let inline493 = trait_impl#ToString#string#to_string(for_item420) in
                    let inline494 = @runtime(core.string_println)(inline493) in
                    jump loop429()
                  } else {
                    jump exit427()
                  }
                } in
              } in
              jump loop429()
            } in
            let inline500 = "for" in
            let inline501 = @intrinsic(vec.push)(values/1, inline500) in
            jump inline_cont499(())
          } in
          let inline503 = @intrinsic(vec.new)() in
          jump inline_cont502(inline503)
        } in
        joinrec {
          join loop433() -> unit {
            join inline_cont490(t434: int) -> unit {
              let t435 = (t434 < 2) in
              if t435 {
                join inline_cont488(t436: int) -> unit {
                  join inline_cont486(t437: string) -> unit {
                    join inline_cont483(_wild414: unit) -> unit {
                      join inline_cont481(t438: int) -> unit {
                        let t439 = (t438 + 1) in
                        let inline480 = @intrinsic(ref.set)(index/0, t439) in
                        jump loop433()
                      } in
                      let inline482 = @intrinsic(ref.get)(index/0) in
                      jump inline_cont481(inline482)
                    } in
                    let inline484 = trait_impl#ToString#string#to_string(t437) in
                    let inline485 = @runtime(core.string_println)(inline484) in
                    jump inline_cont483(())
                  } in
                  let inline487 = @runtime(core.int_to_string)(t436) in
                  jump inline_cont486(inline487)
                } in
                let inline489 = @intrinsic(ref.get)(index/0) in
                jump inline_cont488(inline489)
              } else {
                jump exit426()
              }
            } in
            let inline491 = @intrinsic(ref.get)(index/0) in
            jump inline_cont490(inline491)
          } in
        } in
        jump loop433()
      } in
      let inline505 = 0 in
      let inline506 = @intrinsic(ref.new)(inline505) in
      jump inline_cont504(inline506)
    } in
    match mtmp412 {
      1 => {
        let inline476 = "match" in
        let inline477 = trait_impl#ToString#string#to_string(inline476) in
        let inline478 = @runtime(core.string_println)(inline477) in
        jump k425()
      },
      _ => {
        jump k425()
      },
    }
  } in
  let inline472 = "if" in
  let inline473 = trait_impl#ToString#string#to_string(inline472) in
  let inline474 = @runtime(core.string_println)(inline473) in
  jump k424()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont507(t445: string) -> unit {
    let t446 = @runtime(core.string_println)(t445) in
    ()
  } in
  jump inline_cont507(value/1)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t449 = @intrinsic(ref.new)(value/431) in
  t449
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t452 = @intrinsic(ref.get)(self/432) in
  t452
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t455 = @runtime(core.int_to_string)(self/32) in
  t455
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t457 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  let t460 = @intrinsic(vec.new)() in
  t460
}

fn inherent#Vec#Vec[T]#push__T_string(self/258: Vec[string], elem/259: string) -> unit {
  let t462 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t467 = (self/185 == other/186) in
  t467
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t470 = @runtime(core.int_hash)(self/211) in
  t470
}