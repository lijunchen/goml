fn main() -> unit {
  join k421() -> unit {
    let mtmp409 = 1 in
    join k422() -> unit {
      join inline_cont501(index/0: Ref[int]) -> unit {
        join exit423() -> unit {
          join inline_cont499(values/1: Vec[string]) -> unit {
            join inline_cont496(_wild413: unit) -> unit {
              let for_limit415 = @intrinsic(vec.len)(values/1) in
              let for_index416 = 0 in
              join exit424() -> unit {
                let inline493 = "done" in
                let inline494 = trait_impl#ToString#string#to_string(inline493) in
                let inline495 = @runtime(core.string_println)(inline494) in
                ()
              } in
              joinrec {
                join loop426() -> unit {
                  let t427 = (for_index416 < for_limit415) in
                  if t427 {
                    let for_item417 = @intrinsic(vec.get)(values/1, for_index416) in
                    let t428 = (for_index416 + 1) in
                    let _wild418 = for_index416 = t428 in
                    let inline490 = trait_impl#ToString#string#to_string(for_item417) in
                    let inline491 = @runtime(core.string_println)(inline490) in
                    jump loop426()
                  } else {
                    jump exit424()
                  }
                } in
              } in
              jump loop426()
            } in
            let inline497 = "for" in
            let inline498 = @intrinsic(vec.push)(values/1, inline497) in
            jump inline_cont496(())
          } in
          let inline500 = @intrinsic(vec.new)() in
          jump inline_cont499(inline500)
        } in
        joinrec {
          join loop430() -> unit {
            join inline_cont487(t431: int) -> unit {
              let t432 = (t431 < 2) in
              if t432 {
                join inline_cont485(t433: int) -> unit {
                  join inline_cont483(t434: string) -> unit {
                    join inline_cont480(_wild411: unit) -> unit {
                      join inline_cont478(t435: int) -> unit {
                        let t436 = (t435 + 1) in
                        let inline477 = @intrinsic(ref.set)(index/0, t436) in
                        jump loop430()
                      } in
                      let inline479 = @intrinsic(ref.get)(index/0) in
                      jump inline_cont478(inline479)
                    } in
                    let inline481 = trait_impl#ToString#string#to_string(t434) in
                    let inline482 = @runtime(core.string_println)(inline481) in
                    jump inline_cont480(())
                  } in
                  let inline484 = @runtime(core.int_to_string)(t433) in
                  jump inline_cont483(inline484)
                } in
                let inline486 = @intrinsic(ref.get)(index/0) in
                jump inline_cont485(inline486)
              } else {
                jump exit423()
              }
            } in
            let inline488 = @intrinsic(ref.get)(index/0) in
            jump inline_cont487(inline488)
          } in
        } in
        jump loop430()
      } in
      let inline502 = 0 in
      let inline503 = @intrinsic(ref.new)(inline502) in
      jump inline_cont501(inline503)
    } in
    match mtmp409 {
      1 => {
        let inline473 = "match" in
        let inline474 = trait_impl#ToString#string#to_string(inline473) in
        let inline475 = @runtime(core.string_println)(inline474) in
        jump k422()
      },
      _ => {
        jump k422()
      },
    }
  } in
  let inline469 = "if" in
  let inline470 = trait_impl#ToString#string#to_string(inline469) in
  let inline471 = @runtime(core.string_println)(inline470) in
  jump k421()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont504(t442: string) -> unit {
    let t443 = @runtime(core.string_println)(t442) in
    ()
  } in
  jump inline_cont504(value/1)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t446 = @intrinsic(ref.new)(value/431) in
  t446
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t449 = @intrinsic(ref.get)(self/432) in
  t449
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t452 = @runtime(core.int_to_string)(self/32) in
  t452
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t454 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  let t457 = @intrinsic(vec.new)() in
  t457
}

fn inherent#Vec#Vec[T]#push__T_string(self/258: Vec[string], elem/259: string) -> unit {
  let t459 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t464 = (self/185 == other/186) in
  t464
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t467 = @runtime(core.int_hash)(self/211) in
  t467
}