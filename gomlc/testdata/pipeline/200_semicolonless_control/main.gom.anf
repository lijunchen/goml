fn main() -> unit {
  join ret76() -> unit {
    ()
  } in
  join k77() -> unit {
    let _wild64 = () in
    let mtmp65 = 1 in
    join k78() -> unit {
      let _wild66 = () in
      let index/0 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit79() -> unit {
        let _wild68 = () in
        let values/1 = inherent#Vec#Vec[T]#new__T_string() in
        let _wild69 = inherent#Vec#Vec[T]#push__T_string(values/1, "for") in
        let for_source70 = values/1 in
        let for_limit71 = @intrinsic(vec.len)(for_source70) in
        let for_index72 = 0 in
        join exit80() -> unit {
          let _wild75 = () in
          let t81 = @runtime(core.string_println)("done") in
          jump ret76()
        } in
        joinrec {
          join loop82() -> unit {
            let t83 = (for_index72 < for_limit71) in
            if t83 {
              let for_item73 = @intrinsic(vec.get)(for_source70, for_index72) in
              let t84 = (for_index72 + 1) in
              let _wild74 = for_index72 = t84 in
              let value/2 = for_item73 in
              let t85 = @runtime(core.string_println)(value/2) in
              jump loop82()
            } else {
              jump exit80()
            }
          } in
        } in
        jump loop82()
      } in
      joinrec {
        join loop86() -> unit {
          let t87 = inherent#Ref#Ref[T]#get__T_int(index/0) in
          let t88 = (t87 < 2) in
          if t88 {
            let t89 = inherent#Ref#Ref[T]#get__T_int(index/0) in
            let t90 = inherent#int#int#to_string(t89) in
            let _wild67 = @runtime(core.string_println)(t90) in
            let t91 = inherent#Ref#Ref[T]#get__T_int(index/0) in
            let t92 = (t91 + 1) in
            let t93 = inherent#Ref#Ref[T]#set__T_int(index/0, t92) in
            jump loop86()
          } else {
            jump exit79()
          }
        } in
      } in
      jump loop86()
    } in
    match mtmp65 {
      1 => {
        let t94 = @runtime(core.string_println)("match") in
        jump k78()
      },
      _ => {
        jump k78()
      },
    }
  } in
  if true {
    let t95 = @runtime(core.string_println)("if") in
    jump k77()
  } else {
    let t96 = @runtime(core.string_println)("else") in
    jump k77()
  }
}

fn inherent#Ref#Ref[T]#new__T_int(value/209: int) -> Ref[int] {
  join ret97(retv98: Ref[int]) -> Ref[int] {
    retv98
  } in
  let t99 = @intrinsic(ref.new)(value/209) in
  jump ret97(t99)
}

fn inherent#Ref#Ref[T]#get__T_int(self/210: Ref[int]) -> int {
  join ret100(retv101: int) -> int {
    retv101
  } in
  let t102 = @intrinsic(ref.get)(self/210) in
  jump ret100(t102)
}

fn inherent#int#int#to_string(self/5: int) -> string {
  join ret103(retv104: string) -> string {
    retv104
  } in
  let t105 = @runtime(core.int_to_string)(self/5) in
  jump ret103(t105)
}

fn inherent#Ref#Ref[T]#set__T_int(self/211: Ref[int], value/212: int) -> unit {
  join ret106() -> unit {
    ()
  } in
  let t107 = @intrinsic(ref.set)(self/211, value/212) in
  jump ret106()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  join ret108(retv109: Vec[string]) -> Vec[string] {
    retv109
  } in
  let t110 = @intrinsic(vec.new)() in
  jump ret108(t110)
}

fn inherent#Vec#Vec[T]#push__T_string(self/128: Vec[string], elem/129: string) -> unit {
  join ret111() -> unit {
    ()
  } in
  let t112 = @intrinsic(vec.push)(self/128, elem/129) in
  jump ret111()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret113(retv114: bool) -> bool {
    retv114
  } in
  let t115 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret113(t115)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret116(retv117: uint64) -> uint64 {
    retv117
  } in
  let t118 = @runtime(core.int_hash)(self/85) in
  jump ret116(t118)
}