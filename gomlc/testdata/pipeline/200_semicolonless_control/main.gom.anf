fn main() -> unit {
  join k195() -> unit {
    let mtmp183 = 1 in
    join k196() -> unit {
      join inline_cont275(index/0: Ref[int]) -> unit {
        join exit197() -> unit {
          join inline_cont273(values/1: Vec[string]) -> unit {
            join inline_cont270(_wild187: unit) -> unit {
              let for_limit189 = @intrinsic(vec.len)(values/1) in
              let for_index190 = 0 in
              join exit198() -> unit {
                let inline267 = "done" in
                let inline268 = trait_impl#ToString#string#to_string(inline267) in
                let inline269 = @runtime(core.string_println)(inline268) in
                ()
              } in
              joinrec {
                join loop200() -> unit {
                  let t201 = (for_index190 < for_limit189) in
                  if t201 {
                    let for_item191 = @intrinsic(vec.get)(values/1, for_index190) in
                    let t202 = (for_index190 + 1) in
                    let _wild192 = for_index190 = t202 in
                    let inline264 = trait_impl#ToString#string#to_string(for_item191) in
                    let inline265 = @runtime(core.string_println)(inline264) in
                    jump loop200()
                  } else {
                    jump exit198()
                  }
                } in
              } in
              jump loop200()
            } in
            let inline271 = "for" in
            let inline272 = @intrinsic(vec.push)(values/1, inline271) in
            jump inline_cont270(())
          } in
          let inline274 = @intrinsic(vec.new)() in
          jump inline_cont273(inline274)
        } in
        joinrec {
          join loop204() -> unit {
            join inline_cont261(t205: int) -> unit {
              let t206 = (t205 < 2) in
              if t206 {
                join inline_cont259(t207: int) -> unit {
                  join inline_cont257(t208: string) -> unit {
                    join inline_cont254(_wild185: unit) -> unit {
                      join inline_cont252(t209: int) -> unit {
                        let t210 = (t209 + 1) in
                        let inline251 = @intrinsic(ref.set)(index/0, t210) in
                        jump loop204()
                      } in
                      let inline253 = @intrinsic(ref.get)(index/0) in
                      jump inline_cont252(inline253)
                    } in
                    let inline255 = trait_impl#ToString#string#to_string(t208) in
                    let inline256 = @runtime(core.string_println)(inline255) in
                    jump inline_cont254(())
                  } in
                  let inline258 = @runtime(core.int_to_string)(t207) in
                  jump inline_cont257(inline258)
                } in
                let inline260 = @intrinsic(ref.get)(index/0) in
                jump inline_cont259(inline260)
              } else {
                jump exit197()
              }
            } in
            let inline262 = @intrinsic(ref.get)(index/0) in
            jump inline_cont261(inline262)
          } in
        } in
        jump loop204()
      } in
      let inline276 = 0 in
      let inline277 = @intrinsic(ref.new)(inline276) in
      jump inline_cont275(inline277)
    } in
    match mtmp183 {
      1 => {
        let inline247 = "match" in
        let inline248 = trait_impl#ToString#string#to_string(inline247) in
        let inline249 = @runtime(core.string_println)(inline248) in
        jump k196()
      },
      _ => {
        jump k196()
      },
    }
  } in
  let inline243 = "if" in
  let inline244 = trait_impl#ToString#string#to_string(inline243) in
  let inline245 = @runtime(core.string_println)(inline244) in
  jump k195()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont278(t216: string) -> unit {
    let t217 = @runtime(core.string_println)(t216) in
    ()
  } in
  jump inline_cont278(value/1)
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t220 = @intrinsic(ref.new)(value/270) in
  t220
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t223 = @intrinsic(ref.get)(self/271) in
  t223
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t226 = @runtime(core.int_to_string)(self/32) in
  t226
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t228 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn inherent#Vec#Vec[T]#new__T_string() -> Vec[string] {
  let t231 = @intrinsic(vec.new)() in
  t231
}

fn inherent#Vec#Vec[T]#push__T_string(self/174: Vec[string], elem/175: string) -> unit {
  let t233 = @intrinsic(vec.push)(self/174, elem/175) in
  ()
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t238 = (self/101 == other/102) in
  t238
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t241 = @runtime(core.int_hash)(self/127) in
  t241
}