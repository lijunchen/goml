package main
file main.gom
  fn main() -> unit {
        if true {
            println("if")
        } else {
            println("else")
        };
        match 1 {
            1 => println("match"),
            _ => (),
        };
        let local/1/0 = Ref::new(0);
        while index/0.get() < 2 {
            {
                println(index/0.get().to_string());
                index/0.set(index/0.get() + 1)
            }
        };
        let local/1/1: Vec[string] = Vec::new();
        values/1.push("for");
        for local/1/2 in values/1 {
            println(value/2)
        };
        println("done")
    }