fn main() -> unit {
    if true {
        {
            (println : (string) -> unit)("if")
        }
    } else {
        {
            (println : (string) -> unit)("else")
        }
    };
    match 1 {
        1 => (println : (string) -> unit)("match"),
        _ : isize => (),
    };
    let index/0: Ref[isize] = (Ref[isize]::new : (isize) -> Ref[isize])(0);
    while (Ref[isize]::get : (Ref[isize]) -> isize)((index/0 : Ref[isize])) < 2 {
        {
            (println : (string) -> unit)(
              (isize::to_string : (isize) -> string)(
                (Ref[isize]::get : (Ref[isize]) -> isize)((index/0 : Ref[isize]))
              )
            );
            (Ref[isize]::set : (Ref[isize], isize) -> unit)(
              (index/0 : Ref[isize]),
              (Ref[isize]::get : (Ref[isize]) -> isize)((index/0 : Ref[isize])) + 1
            )
        }
    };
    let values/1: Vec[string] = (Vec[string]::new : () -> Vec[string])();
    (Vec[string]::push : (Vec[string], string) -> unit)((values/1 : Vec[string]), "for");
    for value/2: string in
      (values/1 : Vec[string]) {
        (println : (string) -> unit)((value/2 : string))
    };
    (println : (string) -> unit)("done")
}