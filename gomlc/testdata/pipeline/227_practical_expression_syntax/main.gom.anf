fn record(log/0: Ref[string], label/0: string, value/0: isize) -> isize {
  join inline_cont0(t0: string) -> isize {
    let t1 = (t0 + label/0) in
    let inline0 = @intrinsic(ref.set)(log/0, t1) in
    value/0
  } in
  let inline1 = @intrinsic(ref.get)(log/0) in
  jump inline_cont0(inline1)
}

fn record_point(log/0: Ref[string], label/0: string, value/0: Point) -> Point {
  join inline_cont0(t0: string) -> Point {
    let t1 = (t0 + label/0) in
    let inline0 = @intrinsic(ref.set)(log/0, t1) in
    value/0
  } in
  let inline1 = @intrinsic(ref.get)(log/0) in
  jump inline_cont0(inline1)
}

fn record_vec(log/0: Ref[string], label/0: string, value/0: Vec[isize]) -> Vec[isize] {
  join inline_cont0(t0: string) -> Vec[isize] {
    let t1 = (t0 + label/0) in
    let inline0 = @intrinsic(ref.set)(log/0, t1) in
    value/0
  } in
  let inline1 = @intrinsic(ref.get)(log/0) in
  jump inline_cont0(inline1)
}

fn main() -> unit {
  let number/0 = 5 in
  let compound_old0 = number/0 in
  let compound_value0 = 3 in
  let t0 = (compound_old0 + compound_value0) in
  let t1 = number/0 = t0 in
  let compound_old1 = number/0 in
  let compound_value1 = 2 in
  let t2 = (compound_old1 * compound_value1) in
  let t3 = number/0 = t2 in
  let compound_old2 = number/0 in
  let compound_value2 = 1 in
  let t4 = (compound_old2 >> compound_value2) in
  let t5 = number/0 = t4 in
  let t6 = inherent#isize#isize#to_string(number/0) in
  let _wild0 = println__T_string(t6) in
  let direct/0 = Point { x: 3, y: 4 } in
  let place_root0 = direct/0 in
  let place0 = Point.x(place_root0) in
  let value0 = 5 in
  let t7 = (place0 + value0) in
  let t8 = Point.y(place_root0) in
  let t9 = Point { x: t7, y: t8 } in
  let t10 = direct/0 = t9 in
  let t11 = Point.x(direct/0) in
  let t12 = inherent#isize#isize#to_string(t11) in
  let t13 = ("" + t12) in
  let t14 = (t13 + ",") in
  let t15 = Point.y(direct/0) in
  let t16 = inherent#isize#isize#to_string(t15) in
  let t17 = (t14 + t16) in
  let _wild1 = println__T_string(t17) in
  let pair/0 = (2, 9) in
  let place_root1 = pair/0 in
  let place1 = place_root1.0 in
  let value1 = 3 in
  let t18 = (place1 * value1) in
  let t19 = place_root1.1 in
  let t20 = (t18, t19) in
  let t21 = pair/0 = t20 in
  let t22 = pair/0.0 in
  let t23 = inherent#isize#isize#to_string(t22) in
  let t24 = ("" + t23) in
  let t25 = (t24 + ",") in
  let t26 = pair/0.1 in
  let t27 = inherent#isize#isize#to_string(t26) in
  let t28 = (t25 + t27) in
  let _wild2 = println__T_string(t28) in
  let log/0 = inherent#Ref#Ref[T]#new__T_string("") in
  let base/0 = Point { x: 1, y: 2 } in
  let struct_update_field/789/0 = record(log/0, "F", 7) in
  let struct_update_base/0 = record_point(log/0, "B", base/0) in
  let t29 = Point.y(struct_update_base/0) in
  let t30 = inherent#Ref#Ref[T]#get__T_string(log/0) in
  let _wild3 = println__T_string(t30) in
  let t31 = inherent#isize#isize#to_string(struct_update_field/789/0) in
  let t32 = ("" + t31) in
  let t33 = (t32 + ",") in
  let t34 = inherent#isize#isize#to_string(t29) in
  let t35 = (t33 + t34) in
  let _wild4 = println__T_string(t35) in
  let _wild5 = inherent#Ref#Ref[T]#set__T_string(log/0, "") in
  let t36 = record(log/0, "A", 10) in
  let t37 = record(log/0, "B", 20) in
  let t38 = [t36, t37] in
  let values/0 = @intrinsic(vec.from_array)(t38) in
  let place_root2 = record_vec(log/0, "R", values/0) in
  let index0 = record(log/0, "I", 1) in
  let place2 = @intrinsic(vec.get)(place_root2, index0) in
  let value2 = record(log/0, "V", 5) in
  let t39 = (place2 + value2) in
  let t40 = @intrinsic(vec.set)(place_root2, index0, t39) in
  let t41 = inherent#Ref#Ref[T]#get__T_string(log/0) in
  let _wild6 = println__T_string(t41) in
  let t42 = inherent#Vec#Vec[T]#get__T_isize(values/0, 0) in
  let t43 = inherent#isize#isize#to_string(t42) in
  let t44 = ("" + t43) in
  let t45 = (t44 + ",") in
  join inline_cont0(t46: isize) -> unit {
    join inline_cont1(t47: string) -> unit {
      let t48 = (t45 + t47) in
      join inline_cont2(_wild7: unit) -> unit {
        join inline_cont3(_wild8: unit) -> unit {
          let t49 = ("" + "k") in
          join inline_cont4(t50: isize) -> unit {
            join inline_cont5(t51: string) -> unit {
              let t52 = (t49 + t51) in
              join inline_cont6(t53: isize) -> unit {
                let t54 = (t52, t53) in
                join inline_cont7(t55: isize) -> unit {
                  let t56 = ("same", t55) in
                  join inline_cont8(t57: isize) -> unit {
                    let t58 = ("same", t57) in
                    let t59 = [t54, t56, t58] in
                    let table/0 = @intrinsic(hashmap.from_array)(t59) in
                    join inline_cont9(t60: string) -> unit {
                      join inline_cont10(_wild9: unit) -> unit {
                        join inline_cont11(mtmp0: Option__isize) -> unit {
                          join k0(jp0: string) -> unit {
                            join inline_cont12(_wild10: unit) -> unit {
                              let t61 = [] in
                              let empty_values/0 = @intrinsic(vec.from_array)(t61) in
                              let t62 = [] in
                              let empty_table/0 = @intrinsic(hashmap.from_array)(t62) in
                              let t63 = ("" + "empty=") in
                              join inline_cont13(t64: isize) -> unit {
                                join inline_cont14(t65: isize) -> unit {
                                  let t66 = (t64 + t65) in
                                  join inline_cont15(t67: string) -> unit {
                                    let t68 = (t63 + t67) in
                                    let t69 = (t68 + " {ok}") in
                                    let inline0 = trait_impl#ToString#string#to_string(t69) in
                                    let inline1 = @runtime(core.string_println)(inline0) in
                                    ()
                                  } in
                                  let inline2 = __goml_builtin_int_to_string(t66) in
                                  jump inline_cont15(inline2)
                                } in
                                let inline3 = @intrinsic(hashmap.len)(empty_table/0) in
                                jump inline_cont14(inline3)
                              } in
                              let inline4 = @intrinsic(vec.len)(empty_values/0) in
                              jump inline_cont13(inline4)
                            } in
                            let inline5 = trait_impl#ToString#string#to_string(jp0) in
                            let inline6 = @runtime(core.string_println)(inline5) in
                            jump inline_cont12(())
                          } in
                          match mtmp0 {
                            Tag_0 => {
                              jump k0("missing")
                            },
                            Tag_1 => {
                              let x0 = Option__isize::Some._0(mtmp0) in
                              let inline7 = __goml_builtin_int_to_string(x0) in
                              jump k0(inline7)
                            },
                          }
                        } in
                        let inline8 = "same" in
                        let inline9 = @intrinsic(hashmap.get)(table/0, inline8) in
                        jump inline_cont11(inline9)
                      } in
                      let inline10 = trait_impl#ToString#string#to_string(t60) in
                      let inline11 = @runtime(core.string_println)(inline10) in
                      jump inline_cont10(())
                    } in
                    let inline12 = @intrinsic(ref.get)(log/0) in
                    jump inline_cont9(inline12)
                  } in
                  let inline13 = "B" in
                  let inline14 = 2 in
                  let inline15 = inherent#Ref#Ref[T]#get__T_string(log/0) in
                  let inline16 = (inline15 + inline13) in
                  let inline17 = inherent#Ref#Ref[T]#set__T_string(log/0, inline16) in
                  jump inline_cont8(inline14)
                } in
                let inline18 = "A" in
                let inline19 = 1 in
                let inline20 = inherent#Ref#Ref[T]#get__T_string(log/0) in
                let inline21 = (inline20 + inline18) in
                let inline22 = inherent#Ref#Ref[T]#set__T_string(log/0, inline21) in
                jump inline_cont7(inline19)
              } in
              let inline23 = "V" in
              let inline24 = 11 in
              let inline25 = inherent#Ref#Ref[T]#get__T_string(log/0) in
              let inline26 = (inline25 + inline23) in
              let inline27 = inherent#Ref#Ref[T]#set__T_string(log/0, inline26) in
              jump inline_cont6(inline24)
            } in
            let inline28 = __goml_builtin_int_to_string(t50) in
            jump inline_cont5(inline28)
          } in
          let inline29 = "K" in
          let inline30 = 1 in
          let inline31 = inherent#Ref#Ref[T]#get__T_string(log/0) in
          let inline32 = (inline31 + inline29) in
          let inline33 = inherent#Ref#Ref[T]#set__T_string(log/0, inline32) in
          jump inline_cont4(inline30)
        } in
        let inline34 = "" in
        let inline35 = @intrinsic(ref.set)(log/0, inline34) in
        jump inline_cont3(())
      } in
      let inline36 = trait_impl#ToString#string#to_string(t48) in
      let inline37 = @runtime(core.string_println)(inline36) in
      jump inline_cont2(())
    } in
    let inline38 = __goml_builtin_int_to_string(t46) in
    jump inline_cont1(inline38)
  } in
  let inline39 = 1 in
  let inline40 = @intrinsic(vec.get)(values/0, inline39) in
  jump inline_cont0(inline40)
}

fn inherent#Ref#Ref[T]#get__T_string(self/0: Ref[string]) -> string {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_string(self/0: Ref[string], value/0: string) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn inherent#isize#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#Ref#Ref[T]#new__T_string(value/0: string) -> Ref[string] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Vec#Vec[T]#get__T_isize(self/0: Vec[isize], index/0: isize) -> isize {
  let t0 = @intrinsic(vec.get)(self/0, index/0) in
  t0
}

fn inherent#HashMap#HashMap[K,V]#get__K_string__V_isize(
  self/0: HashMap[string, isize],
  key/0: string
) -> Option__isize {
  let t0 = @intrinsic(hashmap.get)(self/0, key/0) in
  t0
}

fn inherent#Vec#Vec[T]#len__T_isize(self/0: Vec[isize]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn inherent#HashMap#HashMap[K,V]#len__K_string__V_isize(self/0: HashMap[string, isize]) -> isize {
  let t0 = @intrinsic(hashmap.len)(self/0) in
  t0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#string#eq(self/0: string, other/0: string) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#string#hash(self/0: string) -> u64 {
  let t0 = @runtime(core.string_hash)(self/0) in
  t0
}