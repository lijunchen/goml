fn record(log/0: Ref[string], label/1: string, value/2: isize) -> isize {
  join inline_cont575(t452: string) -> isize {
    let t453 = (t452 + label/1) in
    let inline574 = @intrinsic(ref.set)(log/0, t453) in
    value/2
  } in
  let inline576 = @intrinsic(ref.get)(log/0) in
  jump inline_cont575(inline576)
}

fn record_point(log/3: Ref[string], label/4: string, value/5: Point) -> Point {
  join inline_cont579(t456: string) -> Point {
    let t457 = (t456 + label/4) in
    let inline578 = @intrinsic(ref.set)(log/3, t457) in
    value/5
  } in
  let inline580 = @intrinsic(ref.get)(log/3) in
  jump inline_cont579(inline580)
}

fn record_vec(log/6: Ref[string], label/7: string, value/8: Vec[isize]) -> Vec[isize] {
  join inline_cont583(t460: string) -> Vec[isize] {
    let t461 = (t460 + label/7) in
    let inline582 = @intrinsic(ref.set)(log/6, t461) in
    value/8
  } in
  let inline584 = @intrinsic(ref.get)(log/6) in
  jump inline_cont583(inline584)
}

fn main() -> unit {
  let number/9 = 5 in
  let compound_old414 = number/9 in
  let compound_value415 = 3 in
  let t463 = (compound_old414 + compound_value415) in
  let t464 = number/9 = t463 in
  let compound_old417 = number/9 in
  let compound_value418 = 2 in
  let t465 = (compound_old417 * compound_value418) in
  let t466 = number/9 = t465 in
  let compound_old420 = number/9 in
  let compound_value421 = 1 in
  let t467 = (compound_old420 >> compound_value421) in
  let t468 = number/9 = t467 in
  let t469 = inherent#isize#isize#to_string(number/9) in
  let _wild423 = println__T_string(t469) in
  let direct/10 = Point { x: 3, y: 4 } in
  let place_root424 = direct/10 in
  let place425 = Point.x(place_root424) in
  let value426 = 5 in
  let t470 = (place425 + value426) in
  let t471 = Point.y(place_root424) in
  let t472 = Point { x: t470, y: t471 } in
  let t473 = direct/10 = t472 in
  let t474 = Point.x(direct/10) in
  let t475 = inherent#isize#isize#to_string(t474) in
  let t476 = ("" + t475) in
  let t477 = (t476 + ",") in
  let t478 = Point.y(direct/10) in
  let t479 = inherent#isize#isize#to_string(t478) in
  let t480 = (t477 + t479) in
  let _wild428 = println__T_string(t480) in
  let pair/11 = (2, 9) in
  let place_root429 = pair/11 in
  let place430 = place_root429.0 in
  let value431 = 3 in
  let t481 = (place430 * value431) in
  let t482 = place_root429.1 in
  let t483 = (t481, t482) in
  let t484 = pair/11 = t483 in
  let t485 = pair/11.0 in
  let t486 = inherent#isize#isize#to_string(t485) in
  let t487 = ("" + t486) in
  let t488 = (t487 + ",") in
  let t489 = pair/11.1 in
  let t490 = inherent#isize#isize#to_string(t489) in
  let t491 = (t488 + t490) in
  let _wild433 = println__T_string(t491) in
  let log/12 = inherent#Ref#Ref[T]#new__T_string("") in
  let base/13 = Point { x: 1, y: 2 } in
  let struct_update_field/789/0 = record(log/12, "F", 7) in
  let struct_update_base/789 = record_point(log/12, "B", base/13) in
  let t492 = Point.y(struct_update_base/789) in
  let t494 = inherent#Ref#Ref[T]#get__T_string(log/12) in
  let _wild434 = println__T_string(t494) in
  let t496 = inherent#isize#isize#to_string(struct_update_field/789/0) in
  let t497 = ("" + t496) in
  let t498 = (t497 + ",") in
  let t500 = inherent#isize#isize#to_string(t492) in
  let t501 = (t498 + t500) in
  let _wild435 = println__T_string(t501) in
  let _wild436 = inherent#Ref#Ref[T]#set__T_string(log/12, "") in
  let t502 = record(log/12, "A", 10) in
  let t503 = record(log/12, "B", 20) in
  let t504 = [t502, t503] in
  let values/15 = @intrinsic(vec.from_array)(t504) in
  let place_root437 = record_vec(log/12, "R", values/15) in
  let index438 = record(log/12, "I", 1) in
  let place439 = @intrinsic(vec.get)(place_root437, index438) in
  let value440 = record(log/12, "V", 5) in
  let t505 = (place439 + value440) in
  let t506 = @intrinsic(vec.set)(place_root437, index438, t505) in
  let t507 = inherent#Ref#Ref[T]#get__T_string(log/12) in
  let _wild442 = println__T_string(t507) in
  let t508 = inherent#Vec#Vec[T]#get__T_isize(values/15, 0) in
  let t509 = inherent#isize#isize#to_string(t508) in
  let t510 = ("" + t509) in
  let t511 = (t510 + ",") in
  join inline_cont641(t512: isize) -> unit {
    join inline_cont639(t513: string) -> unit {
      let t514 = (t511 + t513) in
      join inline_cont636(_wild443: unit) -> unit {
        join inline_cont633(_wild444: unit) -> unit {
          let t515 = ("" + "k") in
          join inline_cont627(t516: isize) -> unit {
            join inline_cont625(t517: string) -> unit {
              let t518 = (t515 + t517) in
              join inline_cont619(t519: isize) -> unit {
                let t520 = (t518, t519) in
                join inline_cont613(t521: isize) -> unit {
                  let t522 = ("same", t521) in
                  join inline_cont607(t523: isize) -> unit {
                    let t524 = ("same", t523) in
                    let t525 = [t520, t522, t524] in
                    let table/16 = @intrinsic(hashmap.from_array)(t525) in
                    join inline_cont605(t526: string) -> unit {
                      join inline_cont602(_wild445: unit) -> unit {
                        join inline_cont599(mtmp446: Option__isize) -> unit {
                          join k527(jp528: string) -> unit {
                            join inline_cont596(_wild448: unit) -> unit {
                              let t529 = [] in
                              let empty_values/18 = @intrinsic(vec.from_array)(t529) in
                              let t530 = [] in
                              let empty_table/19 = @intrinsic(hashmap.from_array)(t530) in
                              let t531 = ("" + "empty=") in
                              join inline_cont594(t532: isize) -> unit {
                                join inline_cont592(t533: isize) -> unit {
                                  let t534 = (t532 + t533) in
                                  join inline_cont590(t535: string) -> unit {
                                    let t536 = (t531 + t535) in
                                    let t537 = (t536 + " {ok}") in
                                    let inline588 = trait_impl#ToString#string#to_string(t537) in
                                    let inline589 = @runtime(core.string_println)(inline588) in
                                    ()
                                  } in
                                  let inline591 = @runtime(core.int_to_string)(t534) in
                                  jump inline_cont590(inline591)
                                } in
                                let inline593 = @intrinsic(hashmap.len)(empty_table/19) in
                                jump inline_cont592(inline593)
                              } in
                              let inline595 = @intrinsic(vec.len)(empty_values/18) in
                              jump inline_cont594(inline595)
                            } in
                            let inline597 = trait_impl#ToString#string#to_string(jp528) in
                            let inline598 = @runtime(core.string_println)(inline597) in
                            jump inline_cont596(())
                          } in
                          match mtmp446 {
                            Tag_0 => {
                              jump k527("missing")
                            },
                            Tag_1 => {
                              let x447 = Option__isize::Some._0(mtmp446) in
                              let inline586 = @runtime(core.int_to_string)(x447) in
                              jump k527(inline586)
                            },
                          }
                        } in
                        let inline600 = "same" in
                        let inline601 = @intrinsic(hashmap.get)(table/16, inline600) in
                        jump inline_cont599(inline601)
                      } in
                      let inline603 = trait_impl#ToString#string#to_string(t526) in
                      let inline604 = @runtime(core.string_println)(inline603) in
                      jump inline_cont602(())
                    } in
                    let inline606 = @intrinsic(ref.get)(log/12) in
                    jump inline_cont605(inline606)
                  } in
                  let inline608 = "B" in
                  let inline609 = 2 in
                  let inline610 = inherent#Ref#Ref[T]#get__T_string(log/12) in
                  let inline611 = (inline610 + inline608) in
                  let inline612 = inherent#Ref#Ref[T]#set__T_string(log/12, inline611) in
                  jump inline_cont607(inline609)
                } in
                let inline614 = "A" in
                let inline615 = 1 in
                let inline616 = inherent#Ref#Ref[T]#get__T_string(log/12) in
                let inline617 = (inline616 + inline614) in
                let inline618 = inherent#Ref#Ref[T]#set__T_string(log/12, inline617) in
                jump inline_cont613(inline615)
              } in
              let inline620 = "V" in
              let inline621 = 11 in
              let inline622 = inherent#Ref#Ref[T]#get__T_string(log/12) in
              let inline623 = (inline622 + inline620) in
              let inline624 = inherent#Ref#Ref[T]#set__T_string(log/12, inline623) in
              jump inline_cont619(inline621)
            } in
            let inline626 = @runtime(core.int_to_string)(t516) in
            jump inline_cont625(inline626)
          } in
          let inline628 = "K" in
          let inline629 = 1 in
          let inline630 = inherent#Ref#Ref[T]#get__T_string(log/12) in
          let inline631 = (inline630 + inline628) in
          let inline632 = inherent#Ref#Ref[T]#set__T_string(log/12, inline631) in
          jump inline_cont627(inline629)
        } in
        let inline634 = "" in
        let inline635 = @intrinsic(ref.set)(log/12, inline634) in
        jump inline_cont633(())
      } in
      let inline637 = trait_impl#ToString#string#to_string(t514) in
      let inline638 = @runtime(core.string_println)(inline637) in
      jump inline_cont636(())
    } in
    let inline640 = @runtime(core.int_to_string)(t512) in
    jump inline_cont639(inline640)
  } in
  let inline642 = 1 in
  let inline643 = @intrinsic(vec.get)(values/15, inline642) in
  jump inline_cont641(inline643)
}

fn inherent#Ref#Ref[T]#get__T_string(self/432: Ref[string]) -> string {
  let t541 = @intrinsic(ref.get)(self/432) in
  t541
}

fn inherent#Ref#Ref[T]#set__T_string(self/433: Ref[string], value/434: string) -> unit {
  let t543 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont644(t545: string) -> unit {
    let t546 = @runtime(core.string_println)(t545) in
    ()
  } in
  jump inline_cont644(value/1)
}

fn inherent#isize#isize#to_string(self/32: isize) -> string {
  let t549 = @runtime(core.int_to_string)(self/32) in
  t549
}

fn inherent#Ref#Ref[T]#new__T_string(value/431: string) -> Ref[string] {
  let t552 = @intrinsic(ref.new)(value/431) in
  t552
}

fn inherent#Vec#Vec[T]#get__T_isize(self/268: Vec[isize], index/269: isize) -> isize {
  let t555 = @intrinsic(vec.get)(self/268, index/269) in
  t555
}

fn inherent#HashMap#HashMap[K,V]#get__K_string__V_isize(
  self/420: HashMap[string, isize],
  key/421: string
) -> Option__isize {
  let t558 = @intrinsic(hashmap.get)(self/420, key/421) in
  t558
}

fn inherent#Vec#Vec[T]#len__T_isize(self/273: Vec[isize]) -> isize {
  let t561 = @intrinsic(vec.len)(self/273) in
  t561
}

fn inherent#HashMap#HashMap[K,V]#len__K_string__V_isize(self/427: HashMap[string, isize]) -> isize {
  let t564 = @intrinsic(hashmap.len)(self/427) in
  t564
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#string#eq(self/181: string, other/182: string) -> bool {
  let t569 = (self/181 == other/182) in
  t569
}

fn trait_impl#Hash#string#hash(self/209: string) -> u64 {
  let t572 = @runtime(core.string_hash)(self/209) in
  t572
}