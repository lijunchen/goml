package main
file main.gom
  struct Point {
        x: int,
        y: int,
    }
    
    fn record(local/1/0: Ref[string], local/1/1: string, local/1/2: int) -> int {
        log/0.set(log/0.get() + label/1);
        value/2
    }
    
    fn record_point(local/1/3: Ref[string], local/1/4: string, local/1/5: Point) -> Point {
        log/3.set(log/3.get() + label/4);
        value/5
    }
    
    fn record_vec(local/1/6: Ref[string], local/1/7: string, local/1/8: Vec[int]) -> Vec[int] {
        log/6.set(log/6.get() + label/7);
        value/8
    }
    
    fn main() {
        let mut local/1/9 = 5;
        number/9 += 3;
        number/9 *= 2;
        number/9 >>= 1;
        string_println(number/9.to_string());
        let mut local/1/10 = Point {
            x: 3,
            y: 4
        };
        direct/10.x += 5;
        string_println(f"{direct/10.x},{direct/10.y}");
        let mut local/1/11 = (2, 9);
        pair/11.0 *= 3;
        string_println(f"{pair/11.0},{pair/11.1}");
        let local/1/12 = Ref::new("");
        let local/1/13 = Point {
            x: 1,
            y: 2
        };
        let local/1/14 = Point {
            x: def/1/1(log/12, "F", 7),
            ..def/1/2(log/12, "B", base/13)
        };
        string_println(log/12.get());
        string_println(f"{updated/14.x},{updated/14.y}");
        log/12.set("");
        let local/1/15 = Vec::[def/1/1(log/12, "A", 10), def/1/1(log/12, "B", 20)];
        def/1/3(log/12, "R", values/15)[def/1/1(log/12, "I", 1)] += def/1/1(log/12, "V", 5);
        string_println(log/12.get());
        string_println(f"{values/15.get(0)},{values/15.get(1)}");
        log/12.set("");
        let local/1/16 = HashMap::{f"k{def/1/1(log/12, "K", 1)}" => def/1/1(log/12, "V", 11), "same" => def/1/1(log/12, "A", 1), "same" => def/1/1(log/12, "B", 2)};
        string_println(log/12.get());
        string_println(match table/16.get("same") {
              Option::Some(local/1/17) => value/17.to_string(),
              Option::None => "missing",
          });
        let local/1/18: Vec[int] = Vec::[];
        let local/1/19: HashMap[string, int] = HashMap::{};
        string_println(f"empty={empty_values/18.len() + empty_table/19.len()} {{ok}}");
    }