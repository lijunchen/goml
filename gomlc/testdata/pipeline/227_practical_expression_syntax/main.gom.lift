fn record(log/0: Ref[string], label/1: string, value/2: isize) -> isize {
  let _wild411 =
    inherent#Ref#Ref[T]#set__T_string(
      log/0,
      (inherent#Ref#Ref[T]#get__T_string(log/0) + label/1)
    ) in
  value/2
}

fn record_point(log/3: Ref[string], label/4: string, value/5: Point) -> Point {
  let _wild412 =
    inherent#Ref#Ref[T]#set__T_string(
      log/3,
      (inherent#Ref#Ref[T]#get__T_string(log/3) + label/4)
    ) in
  value/5
}

fn record_vec(log/6: Ref[string], label/7: string, value/8: Vec[isize]) -> Vec[isize] {
  let _wild413 =
    inherent#Ref#Ref[T]#set__T_string(
      log/6,
      (inherent#Ref#Ref[T]#get__T_string(log/6) + label/7)
    ) in
  value/8
}

fn main() -> unit {
  let number/9 = 5 in
  let _wild416 =
    let compound_old414 = number/9 in
    let compound_value415 = 3 in
    number/9 = (compound_old414 + compound_value415) in
  let _wild419 =
    let compound_old417 = number/9 in
    let compound_value418 = 2 in
    number/9 = (compound_old417 * compound_value418) in
  let _wild422 =
    let compound_old420 = number/9 in
    let compound_value421 = 1 in
    number/9 = (compound_old420 >> compound_value421) in
  let _wild423 = println__T_string(inherent#isize#isize#to_string(number/9)) in
  let direct/10 = Point { x: 3, y: 4 } in
  let _wild427 =
    let place_root424 = direct/10 in
    let place425 = Point.x(place_root424) in
    let value426 = 5 in
    direct/10 = Point { x: (place425 + value426), y: Point.y(place_root424) } in
  let _wild428 =
    println__T_string(
      ((("" + inherent#isize#isize#to_string(Point.x(direct/10))) + ",") +
        inherent#isize#isize#to_string(Point.y(direct/10)))
    ) in
  let pair/11 = (2, 9) in
  let _wild432 =
    let place_root429 = pair/11 in
    let place430 = place_root429.0 in
    let value431 = 3 in
    pair/11 = ((place430 * value431), place_root429.1) in
  let _wild433 =
    println__T_string(
      ((("" + inherent#isize#isize#to_string(pair/11.0)) + ",") +
        inherent#isize#isize#to_string(pair/11.1))
    ) in
  let log/12 = inherent#Ref#Ref[T]#new__T_string("") in
  let base/13 = Point { x: 1, y: 2 } in
  let updated/14 =
    let struct_update_field/777/0 = record(log/12, "F", 7) in
    let struct_update_base/777 = record_point(log/12, "B", base/13) in
    Point { x: struct_update_field/777/0, y: Point.y(struct_update_base/777) } in
  let _wild434 = println__T_string(inherent#Ref#Ref[T]#get__T_string(log/12)) in
  let _wild435 =
    println__T_string(
      ((("" + inherent#isize#isize#to_string(Point.x(updated/14))) + ",") +
        inherent#isize#isize#to_string(Point.y(updated/14)))
    ) in
  let _wild436 = inherent#Ref#Ref[T]#set__T_string(log/12, "") in
  let values/15 = @intrinsic(vec.from_array)([record(log/12, "A", 10), record(log/12, "B", 20)]) in
  let _wild441 =
    let place_root437 = record_vec(log/12, "R", values/15) in
    let index438 = record(log/12, "I", 1) in
    let place439 = @intrinsic(vec.get)(place_root437, index438) in
    let value440 = record(log/12, "V", 5) in
    @intrinsic(vec.set)(place_root437, index438, (place439 + value440)) in
  let _wild442 = println__T_string(inherent#Ref#Ref[T]#get__T_string(log/12)) in
  let _wild443 =
    println__T_string(
      ((("" + inherent#isize#isize#to_string(inherent#Vec#Vec[T]#get__T_isize(values/15, 0))) +
        ",") +
        inherent#isize#isize#to_string(inherent#Vec#Vec[T]#get__T_isize(values/15, 1)))
    ) in
  let _wild444 = inherent#Ref#Ref[T]#set__T_string(log/12, "") in
  let table/16 =
    @intrinsic(hashmap.from_array)(
      [
        (
          (("" + "k") + inherent#isize#isize#to_string(record(log/12, "K", 1))),
          record(log/12, "V", 11)
        ),
        ("same", record(log/12, "A", 1)),
        ("same", record(log/12, "B", 2))
      ]
    ) in
  let _wild445 = println__T_string(inherent#Ref#Ref[T]#get__T_string(log/12)) in
  let _wild448 =
    println__T_string(
      let mtmp446 = inherent#HashMap#HashMap[K,V]#get__K_string__V_isize(table/16, "same") in
      match mtmp446 {
        Option__isize::None => {
          "missing"
        },
        Option__isize::Some(x447) => {
          let x447 = Option__isize::Some._0(mtmp446) in
          let value/17 = x447 in
          inherent#isize#isize#to_string(value/17)
        },
      }
    ) in
  let empty_values/18 = @intrinsic(vec.from_array)([]) in
  let empty_table/19 = @intrinsic(hashmap.from_array)([]) in
  let _wild449 =
    println__T_string(
      ((("" + "empty=") +
        inherent#isize#isize#to_string(
          (inherent#Vec#Vec[T]#len__T_isize(empty_values/18) +
            inherent#HashMap#HashMap[K,V]#len__K_string__V_isize(empty_table/19))
        )) +
        " {ok}")
    ) in
  ()
}

fn inherent#Ref#Ref[T]#get__T_string(self/432: Ref[string]) -> string {
  @intrinsic(ref.get)(self/432)
}

fn inherent#Ref#Ref[T]#set__T_string(self/433: Ref[string], value/434: string) -> unit {
  @intrinsic(ref.set)(self/433, value/434)
}

fn println__T_string(value/1: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/1))
}

fn inherent#isize#isize#to_string(self/32: isize) -> string {
  @runtime(core.int_to_string)(self/32)
}

fn inherent#Ref#Ref[T]#new__T_string(value/431: string) -> Ref[string] {
  @intrinsic(ref.new)(value/431)
}

fn inherent#Vec#Vec[T]#get__T_isize(self/268: Vec[isize], index/269: isize) -> isize {
  @intrinsic(vec.get)(self/268, index/269)
}

fn inherent#HashMap#HashMap[K,V]#get__K_string__V_isize(
  self/420: HashMap[string, isize],
  key/421: string
) -> Option__isize {
  @intrinsic(hashmap.get)(self/420, key/421)
}

fn inherent#Vec#Vec[T]#len__T_isize(self/273: Vec[isize]) -> isize {
  @intrinsic(vec.len)(self/273)
}

fn inherent#HashMap#HashMap[K,V]#len__K_string__V_isize(self/427: HashMap[string, isize]) -> isize {
  @intrinsic(hashmap.len)(self/427)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#string#eq(self/181: string, other/182: string) -> bool {
  (self/181 == other/182)
}

fn trait_impl#Hash#string#hash(self/209: string) -> u64 {
  @runtime(core.string_hash)(self/209)
}