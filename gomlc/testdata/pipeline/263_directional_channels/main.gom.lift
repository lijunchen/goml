fn main() -> () {
  let channel/0 = inherent#Channel#Channel[T]#new__T_isize(2) in
  let mtmp0 = inherent#Channel#Channel[T]#split__T_isize(channel/0) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  let receiver/0 = x1 in
  let sender/0 = x0 in
  let _wild0 = inherent#Sender#Sender[T]#send__T_isize(sender/0, 7) in
  let _wild1 =
    select priority {
      recv(receiver/0) as value/0 => {
        println__T_isize(inherent#Option#Option[T]#unwrap_or__T_isize(value/0, 0))
      },
      default => {
        println__T_isize(1)
      },
    } in
  let channel/1 = inherent#Channel#Channel[T]#new__T_isize(1) in
  let sender/1 = inherent#Channel#Channel[T]#sender__T_isize(channel/1) in
  let receiver/1 = inherent#Channel#Channel[T]#receiver__T_isize(channel/1) in
  select priority {
    send(sender/1, 9) => {
      println__T_isize(
        inherent#Option#Option[T]#unwrap_or__T_isize(
          inherent#Receiver#Receiver[T]#recv__T_isize(receiver/1),
          0
        )
      )
    },
    default => {
      println__T_isize(2)
    },
  }
}

fn inherent#Channel#Channel[T]#new__T_isize(capacity/0: isize) -> Channel[isize] {
  @intrinsic(channel.new)(capacity/0)
}

fn inherent#Channel#Channel[T]#split__T_isize(self/0: Channel[isize]) -> (
  Sender[isize],
  Receiver[isize]
) {
  @intrinsic(channel.split)(self/0)
}

fn inherent#Sender#Sender[T]#send__T_isize(self/0: Sender[isize], value/0: isize) -> () {
  @intrinsic(channel.send)(self/0, value/0)
}

fn println__T_isize(value/0: isize) -> () {
  @runtime(core.string_println)(trait_impl#ToString#isize#to_string(value/0))
}

fn inherent#Option#Option[T]#unwrap_or__T_isize(self/0: Option__isize, fallback/0: isize) -> isize {
  match self/0 {
    Option__isize::None => {
      fallback/0
    },
    Option__isize::Some(x0) => {
      let x0 = Option__isize::Some._0(self/0) in
      let value/0 = x0 in
      value/0
    },
  }
}

fn inherent#Channel#Channel[T]#sender__T_isize(self/0: Channel[isize]) -> Sender[isize] {
  @intrinsic(channel.sender)(self/0)
}

fn inherent#Channel#Channel[T]#receiver__T_isize(self/0: Channel[isize]) -> Receiver[isize] {
  @intrinsic(channel.receiver)(self/0)
}

fn inherent#Receiver#Receiver[T]#recv__T_isize(self/0: Receiver[isize]) -> Option__isize {
  let mtmp0 = @intrinsic(channel.recv)(self/0) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  let ok/0 = x1 in
  let value/0 = x0 in
  if ok/0 {
      Option__isize::Some(value/0)
  } else {
      Option__isize::None
  }
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  __goml_builtin_int_to_string(self/0)
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  signed_decimal_string((value/0 as i64))
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}