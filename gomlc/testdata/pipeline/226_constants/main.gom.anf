const BASE: int = 16;

const ANSWER: int = ((BASE * 2) + 10);

const MASK: uint8 = 240;

const GREETING: string = ("constant" + " value");

const ENABLED: bool = true;

fn main() -> unit {
  join inline_cont446(t412: string) -> unit {
    join inline_cont443(_wild408: unit) -> unit {
      join inline_cont441(t413: string) -> unit {
        join inline_cont438(_wild409: unit) -> unit {
          join inline_cont435(_wild410: unit) -> unit {
            join inline_cont433(t414: string) -> unit {
              let inline431 = trait_impl#ToString#string#to_string(t414) in
              let inline432 = @runtime(core.string_println)(inline431) in
              ()
            } in
            let inline434 = @runtime(core.bool_to_string)(ENABLED) in
            jump inline_cont433(inline434)
          } in
          let inline436 = trait_impl#ToString#string#to_string(GREETING) in
          let inline437 = @runtime(core.string_println)(inline436) in
          jump inline_cont435(())
        } in
        let inline439 = trait_impl#ToString#string#to_string(t413) in
        let inline440 = @runtime(core.string_println)(inline439) in
        jump inline_cont438(())
      } in
      let inline442 = @runtime(core.uint8_to_string)(MASK) in
      jump inline_cont441(inline442)
    } in
    let inline444 = trait_impl#ToString#string#to_string(t412) in
    let inline445 = @runtime(core.string_println)(inline444) in
    jump inline_cont443(())
  } in
  let inline447 = @runtime(core.int_to_string)(ANSWER) in
  jump inline_cont446(inline447)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont448(t417: string) -> unit {
    let t418 = @runtime(core.string_println)(t417) in
    ()
  } in
  jump inline_cont448(value/1)
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t421 = @runtime(core.int_to_string)(self/32) in
  t421
}

fn trait_impl#ToString#uint8#to_string(self/156: uint8) -> string {
  let t424 = @runtime(core.uint8_to_string)(self/156) in
  t424
}

fn trait_impl#ToString#bool#to_string(self/148: bool) -> string {
  let t427 = @runtime(core.bool_to_string)(self/148) in
  t427
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}