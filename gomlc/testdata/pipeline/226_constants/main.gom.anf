const BASE: int = 16;

const ANSWER: int = ((BASE * 2) + 10);

const MASK: uint8 = 240;

const GREETING: string = ("constant" + " value");

const ENABLED: bool = true;

fn main() -> unit {
  join inline_cont449(t415: string) -> unit {
    join inline_cont446(_wild411: unit) -> unit {
      join inline_cont444(t416: string) -> unit {
        join inline_cont441(_wild412: unit) -> unit {
          join inline_cont438(_wild413: unit) -> unit {
            join inline_cont436(t417: string) -> unit {
              let inline434 = trait_impl#ToString#string#to_string(t417) in
              let inline435 = @runtime(core.string_println)(inline434) in
              ()
            } in
            let inline437 = @runtime(core.bool_to_string)(ENABLED) in
            jump inline_cont436(inline437)
          } in
          let inline439 = trait_impl#ToString#string#to_string(GREETING) in
          let inline440 = @runtime(core.string_println)(inline439) in
          jump inline_cont438(())
        } in
        let inline442 = trait_impl#ToString#string#to_string(t416) in
        let inline443 = @runtime(core.string_println)(inline442) in
        jump inline_cont441(())
      } in
      let inline445 = @runtime(core.uint8_to_string)(MASK) in
      jump inline_cont444(inline445)
    } in
    let inline447 = trait_impl#ToString#string#to_string(t415) in
    let inline448 = @runtime(core.string_println)(inline447) in
    jump inline_cont446(())
  } in
  let inline450 = @runtime(core.int_to_string)(ANSWER) in
  jump inline_cont449(inline450)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont451(t420: string) -> unit {
    let t421 = @runtime(core.string_println)(t420) in
    ()
  } in
  jump inline_cont451(value/1)
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t424 = @runtime(core.int_to_string)(self/32) in
  t424
}

fn trait_impl#ToString#uint8#to_string(self/156: uint8) -> string {
  let t427 = @runtime(core.uint8_to_string)(self/156) in
  t427
}

fn trait_impl#ToString#bool#to_string(self/148: bool) -> string {
  let t430 = @runtime(core.bool_to_string)(self/148) in
  t430
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}