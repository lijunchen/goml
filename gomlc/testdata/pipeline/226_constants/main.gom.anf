const BASE: int = 16;

const ANSWER: int = ((BASE * 2) + 10);

const MASK: uint8 = 240;

const GREETING: string = ("constant" + " value");

const ENABLED: bool = true;

fn main() -> unit {
  join inline_cont210(t176: string) -> unit {
    join inline_cont207(_wild172: unit) -> unit {
      join inline_cont205(t177: string) -> unit {
        join inline_cont202(_wild173: unit) -> unit {
          join inline_cont199(_wild174: unit) -> unit {
            join inline_cont197(t178: string) -> unit {
              let inline195 = trait_impl#ToString#string#to_string(t178) in
              let inline196 = @runtime(core.string_println)(inline195) in
              ()
            } in
            let inline198 = @runtime(core.bool_to_string)(ENABLED) in
            jump inline_cont197(inline198)
          } in
          let inline200 = trait_impl#ToString#string#to_string(GREETING) in
          let inline201 = @runtime(core.string_println)(inline200) in
          jump inline_cont199(())
        } in
        let inline203 = trait_impl#ToString#string#to_string(t177) in
        let inline204 = @runtime(core.string_println)(inline203) in
        jump inline_cont202(())
      } in
      let inline206 = @runtime(core.uint8_to_string)(MASK) in
      jump inline_cont205(inline206)
    } in
    let inline208 = trait_impl#ToString#string#to_string(t176) in
    let inline209 = @runtime(core.string_println)(inline208) in
    jump inline_cont207(())
  } in
  let inline211 = @runtime(core.int_to_string)(ANSWER) in
  jump inline_cont210(inline211)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont212(t181: string) -> unit {
    let t182 = @runtime(core.string_println)(t181) in
    ()
  } in
  jump inline_cont212(value/31)
}

fn inherent#int#int#to_string(self/34: int) -> string {
  let t185 = @runtime(core.int_to_string)(self/34) in
  t185
}

fn trait_impl#ToString#uint8#to_string(self/74: uint8) -> string {
  let t188 = @runtime(core.uint8_to_string)(self/74) in
  t188
}

fn trait_impl#ToString#bool#to_string(self/66: bool) -> string {
  let t191 = @runtime(core.bool_to_string)(self/66) in
  t191
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}