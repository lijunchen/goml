const BASE: int = 16;

const ANSWER: int = ((BASE * 2) + 10);

const MASK: uint8 = 240;

const GREETING: string = ("constant" + " value");

const ENABLED: bool = true;

fn main() -> unit {
  join inline_cont225(t191: string) -> unit {
    join inline_cont222(_wild187: unit) -> unit {
      join inline_cont220(t192: string) -> unit {
        join inline_cont217(_wild188: unit) -> unit {
          join inline_cont214(_wild189: unit) -> unit {
            join inline_cont212(t193: string) -> unit {
              let inline210 = trait_impl#ToString#string#to_string(t193) in
              let inline211 = @runtime(core.string_println)(inline210) in
              ()
            } in
            let inline213 = @runtime(core.bool_to_string)(ENABLED) in
            jump inline_cont212(inline213)
          } in
          let inline215 = trait_impl#ToString#string#to_string(GREETING) in
          let inline216 = @runtime(core.string_println)(inline215) in
          jump inline_cont214(())
        } in
        let inline218 = trait_impl#ToString#string#to_string(t192) in
        let inline219 = @runtime(core.string_println)(inline218) in
        jump inline_cont217(())
      } in
      let inline221 = @runtime(core.uint8_to_string)(MASK) in
      jump inline_cont220(inline221)
    } in
    let inline223 = trait_impl#ToString#string#to_string(t191) in
    let inline224 = @runtime(core.string_println)(inline223) in
    jump inline_cont222(())
  } in
  let inline226 = @runtime(core.int_to_string)(ANSWER) in
  jump inline_cont225(inline226)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont227(t196: string) -> unit {
    let t197 = @runtime(core.string_println)(t196) in
    ()
  } in
  jump inline_cont227(value/1)
}

fn inherent#int#int#to_string(self/32: int) -> string {
  let t200 = @runtime(core.int_to_string)(self/32) in
  t200
}

fn trait_impl#ToString#uint8#to_string(self/72: uint8) -> string {
  let t203 = @runtime(core.uint8_to_string)(self/72) in
  t203
}

fn trait_impl#ToString#bool#to_string(self/64: bool) -> string {
  let t206 = @runtime(core.bool_to_string)(self/64) in
  t206
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}