package main;
const base: int = 16;

const answer: int = base * 2 + 10;

const mask: byte = b'\xf0';

const greeting: string = "constant" + " value";

const enabled: bool = true;

fn main() -> unit {
    println(answer.to_string());
    println(mask.to_string());
    println(greeting);
    println(enabled.to_string())
}

