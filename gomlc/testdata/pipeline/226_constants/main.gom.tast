const base: int = 16;
const answer: int = ((base * 2) + 10);
const mask: uint8 = 240;
const greeting: string = ("constant" + " value");
const enabled: bool = true;

fn main() -> unit {
    (@runtime(core.string_println) : (string) -> unit)((int::to_string : (int) -> string)((answer : int)));
    (@runtime(core.string_println) : (string) -> unit)((ToString::to_string : (uint8) -> string)((mask : uint8)));
    (@runtime(core.string_println) : (string) -> unit)((greeting : string));
    (@runtime(core.string_println) : (string) -> unit)((ToString::to_string : (bool) -> string)((enabled : bool)))
}