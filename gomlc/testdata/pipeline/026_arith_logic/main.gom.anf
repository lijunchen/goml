fn show_int(label/0: string, value/1: i32) -> unit {
  join inline_cont892(t809: string) -> unit {
    let t810 = (label/0 + t809) in
    let inline890 = trait_impl#ToString#string#to_string(t810) in
    let inline891 = @runtime(core.string_println)(inline890) in
    ()
  } in
  let inline893 = __goml_builtin_int32_to_string(value/1) in
  jump inline_cont892(inline893)
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join inline_cont897(t812: string) -> unit {
    let t813 = (label/2 + t812) in
    let inline895 = trait_impl#ToString#string#to_string(t813) in
    let inline896 = @runtime(core.string_println)(inline895) in
    ()
  } in
  let inline898 = @runtime(core.bool_to_string)(value/3) in
  jump inline_cont897(inline898)
}

fn main() -> unit {
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  join inline_cont934(mtmp798: unit) -> unit {
    join inline_cont929(mtmp799: unit) -> unit {
      join inline_cont924(mtmp800: unit) -> unit {
        join inline_cont919(mtmp801: unit) -> unit {
          join k815(jp816: bool) -> unit {
            join k817(jp818: bool) -> unit {
              let not_result/11 = (!false) in
              let t829 = (!jp816) in
              join k821(jp822: bool) -> unit {
                join k819(jp820: bool) -> unit {
                  join inline_cont914(mtmp804: unit) -> unit {
                    join inline_cont909(mtmp805: unit) -> unit {
                      join inline_cont904(mtmp806: unit) -> unit {
                        let inline900 = "mixed=" in
                        let inline901 = trait_impl#ToString#bool#to_string(jp820) in
                        let inline902 = (inline900 + inline901) in
                        let inline903 = println__T_string(inline902) in
                        ()
                      } in
                      let inline905 = "not=" in
                      let inline906 = trait_impl#ToString#bool#to_string(not_result/11) in
                      let inline907 = (inline905 + inline906) in
                      let inline908 = println__T_string(inline907) in
                      jump inline_cont904(())
                    } in
                    let inline910 = "or=" in
                    let inline911 = trait_impl#ToString#bool#to_string(jp818) in
                    let inline912 = (inline910 + inline911) in
                    let inline913 = println__T_string(inline912) in
                    jump inline_cont909(())
                  } in
                  let inline915 = "and=" in
                  let inline916 = trait_impl#ToString#bool#to_string(jp816) in
                  let inline917 = (inline915 + inline916) in
                  let inline918 = println__T_string(inline917) in
                  jump inline_cont914(())
                } in
                if jp822 {
                  jump k819(true)
                } else {
                  let t823 = (diff/6 - quot/8) in
                  let t824 = (t823 + base/4) in
                  let t825 = (sum/5 / 2) in
                  let mtmp803 = (t824 - t825) in
                  join k826(jp827: bool) -> unit {
                    let t828 = (!jp827) in
                    jump k819(t828)
                  } in
                  match mtmp803 {
                    0 => {
                      jump k826(false)
                    },
                    _ => {
                      jump k826(true)
                    },
                  }
                }
              } in
              if t829 {
                let t830 = (prod/7 * base/4) in
                let t831 = (sum/5 + t830) in
                let t832 = (prod/7 / 2) in
                let mtmp802 = (t831 - t832) in
                match mtmp802 {
                  0 => {
                    jump k821(false)
                  },
                  _ => {
                    jump k821(true)
                  },
                }
              } else {
                jump k821(false)
              }
            } in
            jump k817(true)
          } in
          jump k815(false)
        } in
        let inline920 = "quot=" in
        let inline921 = inherent#i32#i32#to_string(quot/8) in
        let inline922 = (inline920 + inline921) in
        let inline923 = println__T_string(inline922) in
        jump inline_cont919(())
      } in
      let inline925 = "prod=" in
      let inline926 = inherent#i32#i32#to_string(prod/7) in
      let inline927 = (inline925 + inline926) in
      let inline928 = println__T_string(inline927) in
      jump inline_cont924(())
    } in
    let inline930 = "diff=" in
    let inline931 = inherent#i32#i32#to_string(diff/6) in
    let inline932 = (inline930 + inline931) in
    let inline933 = println__T_string(inline932) in
    jump inline_cont929(())
  } in
  let inline935 = "sum=" in
  let inline936 = inherent#i32#i32#to_string(sum/5) in
  let inline937 = (inline935 + inline936) in
  let inline938 = println__T_string(inline937) in
  jump inline_cont934(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont939(t836: string) -> unit {
    let t837 = @runtime(core.string_println)(t836) in
    ()
  } in
  jump inline_cont939(value/1)
}

fn inherent#i32#i32#to_string(self/286: i32) -> string {
  let inline941 = (self/286 as i64) in
  let inline942 = signed_decimal_string(inline941) in
  inline942
}

fn trait_impl#ToString#bool#to_string(self/401: bool) -> string {
  let t843 = @runtime(core.bool_to_string)(self/401) in
  t843
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t848 = (value/225 as i64) in
  let inline944 = (t848 < 0) in
  if inline944 {
    let inline945 = (t848 as u64) in
    let inline946 = (0 - inline945) in
    let inline947 = decimal_string(inline946) in
    let inline948 = ("-" + inline947) in
    inline948
  } else {
    let inline949 = (t848 as u64) in
    let inline950 = decimal_string(inline949) in
    inline950
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t854 = (value/214 < 0) in
  if t854 {
    let t855 = (value/214 as u64) in
    let t856 = (0 - t855) in
    let t857 = decimal_string(t856) in
    let t858 = ("-" + t857) in
    t858
  } else {
    let t859 = (value/214 as u64) in
    let t860 = decimal_string(t859) in
    t860
  }
}

fn decimal_string(value/208: u64) -> string {
  let t883 = (value/208 == 0) in
  join k863() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit864() -> string {
      join inline_cont959(t865: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t865) in
        let offset/212 = 0 in
        join exit866() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop867() -> string {
            join inline_cont957(t868: isize) -> string {
              let t869 = (offset/212 < t868) in
              if t869 {
                join inline_cont955(t870: isize) -> string {
                  let t871 = (t870 - offset/212) in
                  let t872 = (t871 - 1) in
                  let t873 = @intrinsic(vec.get)(reversed/209, t872) in
                  join inline_cont953(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t874 = (compound_old358 + compound_value359) in
                    let t875 = offset/212 = t874 in
                    jump loop867()
                  } in
                  let inline954 = @intrinsic(vec.push)(bytes/211, t873) in
                  jump inline_cont953(())
                } in
                let inline956 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont955(inline956)
              } else {
                jump exit866()
              }
            } in
            let inline958 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont957(inline958)
          } in
        } in
        jump loop867()
      } in
      let inline960 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont959(inline960)
    } in
    joinrec {
      join loop876() -> string {
        let t877 = (remaining/210 > 0) in
        if t877 {
          let t878 = (remaining/210 % 10) in
          let t879 = (t878 as u8) in
          let t880 = (t879 + 48) in
          join inline_cont951(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t881 = (compound_old353 / compound_value354) in
            let t882 = remaining/210 = t881 in
            jump loop876()
          } in
          let inline952 = @intrinsic(vec.push)(reversed/209, t880) in
          jump inline_cont951(())
        } else {
          jump exit864()
        }
      } in
    } in
    jump loop876()
  } in
  if t883 {
    "0"
  } else {
    jump k863()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t885 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t888 = @intrinsic(vec.len)(self/526) in
  t888
}