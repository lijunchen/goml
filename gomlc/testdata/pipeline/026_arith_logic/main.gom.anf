fn show_int(label/0: string, value/1: int32) -> unit {
  join ret80() -> unit {
    ()
  } in
  let t81 = inherent#int32#int32#to_string(value/1) in
  let t82 = (label/0 + t81) in
  let mtmp68 = println__T_string(t82) in
  jump ret80()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret83() -> unit {
    ()
  } in
  let t84 = trait_impl#ToString#bool#to_string(value/3) in
  let t85 = (label/2 + t84) in
  let mtmp69 = println__T_string(t85) in
  jump ret83()
}

fn main() -> unit {
  join ret86() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp70 = show_int("sum=", sum/5) in
  let mtmp71 = show_int("diff=", diff/6) in
  let mtmp72 = show_int("prod=", prod/7) in
  let mtmp73 = show_int("quot=", quot/8) in
  join k87(jp88: bool) -> unit {
    let and_result/9 = jp88 in
    join k89(jp90: bool) -> unit {
      let or_result/10 = jp90 in
      let not_result/11 = (!false) in
      let t101 = (!and_result/9) in
      join k93(jp94: bool) -> unit {
        join k91(jp92: bool) -> unit {
          let mixed/12 = jp92 in
          let mtmp76 = show_bool("and=", and_result/9) in
          let mtmp77 = show_bool("or=", or_result/10) in
          let mtmp78 = show_bool("not=", not_result/11) in
          let mtmp79 = show_bool("mixed=", mixed/12) in
          jump ret86()
        } in
        if jp94 {
          jump k91(true)
        } else {
          let t95 = (diff/6 - quot/8) in
          let t96 = (t95 + base/4) in
          let t97 = (sum/5 / 2) in
          let mtmp75 = (t96 - t97) in
          join k98(jp99: bool) -> unit {
            let t100 = (!jp99) in
            jump k91(t100)
          } in
          match mtmp75 {
            0 => {
              jump k98(false)
            },
            _ => {
              jump k98(true)
            },
          }
        }
      } in
      if t101 {
        let t102 = (prod/7 * base/4) in
        let t103 = (sum/5 + t102) in
        let t104 = (prod/7 / 2) in
        let mtmp74 = (t103 - t104) in
        join k105(jp106: bool) -> unit {
          jump k93(jp106)
        } in
        match mtmp74 {
          0 => {
            jump k105(false)
          },
          _ => {
            jump k105(true)
          },
        }
      } else {
        jump k93(false)
      }
    } in
    if true {
      jump k89(true)
    } else {
      jump k89(false)
    }
  } in
  if true {
    jump k87(false)
  } else {
    jump k87(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret107() -> unit {
    ()
  } in
  let t108 = trait_impl#ToString#string#to_string(value/1) in
  let t109 = @runtime(core.string_println)(t108) in
  jump ret107()
}

fn inherent#int32#int32#to_string(self/6: int32) -> string {
  join ret110(retv111: string) -> string {
    retv111
  } in
  let t112 = @runtime(core.int32_to_string)(self/6) in
  jump ret110(t112)
}

fn trait_impl#ToString#bool#to_string(self/37: bool) -> string {
  join ret113(retv114: string) -> string {
    retv114
  } in
  let t115 = @runtime(core.bool_to_string)(self/37) in
  jump ret113(t115)
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret116(retv117: string) -> string {
    retv117
  } in
  jump ret116(self/38)
}