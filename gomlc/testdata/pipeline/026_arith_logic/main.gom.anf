fn show_int(label/0: string, value/1: int32) -> unit {
  join ret120() -> unit {
    ()
  } in
  let t121 = inherent#int32#int32#to_string(value/1) in
  let t122 = (label/0 + t121) in
  let mtmp108 = println__T_string(t122) in
  jump ret120()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret123() -> unit {
    ()
  } in
  let t124 = trait_impl#ToString#bool#to_string(value/3) in
  let t125 = (label/2 + t124) in
  let mtmp109 = println__T_string(t125) in
  jump ret123()
}

fn main() -> unit {
  join ret126() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp110 = show_int("sum=", sum/5) in
  let mtmp111 = show_int("diff=", diff/6) in
  let mtmp112 = show_int("prod=", prod/7) in
  let mtmp113 = show_int("quot=", quot/8) in
  join k127(jp128: bool) -> unit {
    let and_result/9 = jp128 in
    join k129(jp130: bool) -> unit {
      let or_result/10 = jp130 in
      let not_result/11 = (!false) in
      let t141 = (!and_result/9) in
      join k133(jp134: bool) -> unit {
        join k131(jp132: bool) -> unit {
          let mixed/12 = jp132 in
          let mtmp116 = show_bool("and=", and_result/9) in
          let mtmp117 = show_bool("or=", or_result/10) in
          let mtmp118 = show_bool("not=", not_result/11) in
          let mtmp119 = show_bool("mixed=", mixed/12) in
          jump ret126()
        } in
        if jp134 {
          jump k131(true)
        } else {
          let t135 = (diff/6 - quot/8) in
          let t136 = (t135 + base/4) in
          let t137 = (sum/5 / 2) in
          let mtmp115 = (t136 - t137) in
          join k138(jp139: bool) -> unit {
            let t140 = (!jp139) in
            jump k131(t140)
          } in
          match mtmp115 {
            0 => {
              jump k138(false)
            },
            _ => {
              jump k138(true)
            },
          }
        }
      } in
      if t141 {
        let t142 = (prod/7 * base/4) in
        let t143 = (sum/5 + t142) in
        let t144 = (prod/7 / 2) in
        let mtmp114 = (t143 - t144) in
        join k145(jp146: bool) -> unit {
          jump k133(jp146)
        } in
        match mtmp114 {
          0 => {
            jump k145(false)
          },
          _ => {
            jump k145(true)
          },
        }
      } else {
        jump k133(false)
      }
    } in
    if true {
      jump k129(true)
    } else {
      jump k129(false)
    }
  } in
  if true {
    jump k127(false)
  } else {
    jump k127(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret147() -> unit {
    ()
  } in
  let t148 = trait_impl#ToString#string#to_string(value/1) in
  let t149 = @runtime(core.string_println)(t148) in
  jump ret147()
}

fn inherent#int32#int32#to_string(self/6: int32) -> string {
  join ret150(retv151: string) -> string {
    retv151
  } in
  let t152 = @runtime(core.int32_to_string)(self/6) in
  jump ret150(t152)
}

fn trait_impl#ToString#bool#to_string(self/37: bool) -> string {
  join ret153(retv154: string) -> string {
    retv154
  } in
  let t155 = @runtime(core.bool_to_string)(self/37) in
  jump ret153(t155)
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret156(retv157: string) -> string {
    retv157
  } in
  jump ret156(self/38)
}