fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont237(t194: int) -> unit {
    join inline_cont234(_wild177: unit) -> unit {
      let for_source178 = values/0 in
      let for_limit179 = @intrinsic(vec.len)(values/0) in
      let for_index180 = 0 in
      join exit195() -> unit {
        ()
      } in
      joinrec {
        join loop196() -> unit {
          let t197 = (for_index180 < for_limit179) in
          if t197 {
            let for_item181 = @intrinsic(vec.get)(values/0, for_index180) in
            let t198 = (for_index180 + 1) in
            let _wild182 = for_index180 = t198 in
            let value/1 = for_item181 in
            let inline232 = trait_impl#ToString#int32#to_string(for_item181) in
            let inline233 = @runtime(core.string_println)(inline232) in
            jump loop196()
          } else {
            jump exit195()
          }
        } in
      } in
      jump loop196()
    } in
    let inline235 = trait_impl#ToString#int#to_string(t194) in
    let inline236 = @runtime(core.string_println)(inline235) in
    jump inline_cont234(())
  } in
  let inline238 = @intrinsic(vec.len)(values/0) in
  jump inline_cont237(inline238)
}

fn main() -> unit {
  join inline_cont258(values/2: Vec[int32]) -> unit {
    join inline_cont255(_wild183: unit) -> unit {
      join inline_cont252(_wild184: unit) -> unit {
        join inline_cont249(_wild185: unit) -> unit {
          let _wild186 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild187 = print_values(values/2) in
          join inline_cont247(aliased/3: Vec[int32]) -> unit {
            join inline_cont244(_wild188: unit) -> unit {
              join inline_cont241(_wild189: unit) -> unit {
                let same/4 = aliased/3 in
                let _wild190 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild191 = print_values(aliased/3) in
                join inline_cont239(empty/5: Vec[int32]) -> unit {
                  let _wild192 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t201 = print_values(empty/5) in
                  ()
                } in
                let inline240 = @intrinsic(vec.new)() in
                jump inline_cont239(inline240)
              } in
              let inline242 = 5 in
              let inline243 = @intrinsic(vec.push)(aliased/3, inline242) in
              jump inline_cont241(())
            } in
            let inline245 = 4 in
            let inline246 = @intrinsic(vec.push)(aliased/3, inline245) in
            jump inline_cont244(())
          } in
          let inline248 = @intrinsic(vec.new)() in
          jump inline_cont247(inline248)
        } in
        let inline250 = 3 in
        let inline251 = @intrinsic(vec.push)(values/2, inline250) in
        jump inline_cont249(())
      } in
      let inline253 = 2 in
      let inline254 = @intrinsic(vec.push)(values/2, inline253) in
      jump inline_cont252(())
    } in
    let inline256 = 1 in
    let inline257 = @intrinsic(vec.push)(values/2, inline256) in
    jump inline_cont255(())
  } in
  let inline259 = @intrinsic(vec.new)() in
  jump inline_cont258(inline259)
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont260(t203: string) -> unit {
    let t204 = @runtime(core.string_println)(t203) in
    ()
  } in
  let inline261 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont260(inline261)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/166: Vec[int32]) -> int {
  let t207 = @intrinsic(vec.len)(self/166) in
  t207
}

fn println__T_int32(value/31: int32) -> unit {
  join inline_cont262(t209: string) -> unit {
    let t210 = @runtime(core.string_println)(t209) in
    ()
  } in
  let inline263 = @runtime(core.int32_to_string)(value/31) in
  jump inline_cont262(inline263)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t213 = @intrinsic(vec.new)() in
  t213
}

fn inherent#Vec#Vec[T]#push__T_int32(self/155: Vec[int32], elem/156: int32) -> unit {
  let t215 = @intrinsic(vec.push)(self/155, elem/156) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/200: Vec[int32], other/201: Vec[int32]) -> unit {
  join inline_cont268(len/202: int) -> unit {
    join inline_cont266(_wild116: unit) -> unit {
      let index/203 = 0 in
      join exit217() -> unit {
        ()
      } in
      joinrec {
        join loop218() -> unit {
          let t219 = (index/203 < len/202) in
          if t219 {
            let t220 = @intrinsic(vec.get)(other/201, index/203) in
            join inline_cont264(_wild117: unit) -> unit {
              let compound_old118 = index/203 in
              let compound_value119 = 1 in
              let t221 = (compound_old118 + compound_value119) in
              let t222 = index/203 = t221 in
              let _wild120 = t222 in
              jump loop218()
            } in
            let inline265 = @intrinsic(vec.push)(self/200, t220) in
            jump inline_cont264(())
          } else {
            jump exit217()
          }
        } in
      } in
      jump loop218()
    } in
    let inline267 = @intrinsic(vec.reserve)(self/200, len/202) in
    jump inline_cont266(())
  } in
  let inline269 = @intrinsic(vec.len)(other/201) in
  jump inline_cont268(inline269)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t225 = @runtime(core.int_to_string)(self/69) in
  t225
}

fn trait_impl#ToString#int32#to_string(self/72: int32) -> string {
  let t228 = @runtime(core.int32_to_string)(self/72) in
  t228
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/169: Vec[int32], additional/170: int) -> unit {
  let t230 = @intrinsic(vec.reserve)(self/169, additional/170) in
  ()
}