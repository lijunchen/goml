fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont232(t189: int) -> unit {
    join inline_cont229(_wild172: unit) -> unit {
      let for_limit174 = @intrinsic(vec.len)(values/0) in
      let for_index175 = 0 in
      join exit190() -> unit {
        ()
      } in
      joinrec {
        join loop191() -> unit {
          let t192 = (for_index175 < for_limit174) in
          if t192 {
            let for_item176 = @intrinsic(vec.get)(values/0, for_index175) in
            let t193 = (for_index175 + 1) in
            let _wild177 = for_index175 = t193 in
            let inline227 = trait_impl#ToString#int32#to_string(for_item176) in
            let inline228 = @runtime(core.string_println)(inline227) in
            jump loop191()
          } else {
            jump exit190()
          }
        } in
      } in
      jump loop191()
    } in
    let inline230 = trait_impl#ToString#int#to_string(t189) in
    let inline231 = @runtime(core.string_println)(inline230) in
    jump inline_cont229(())
  } in
  let inline233 = @intrinsic(vec.len)(values/0) in
  jump inline_cont232(inline233)
}

fn main() -> unit {
  join inline_cont253(values/2: Vec[int32]) -> unit {
    join inline_cont250(_wild178: unit) -> unit {
      join inline_cont247(_wild179: unit) -> unit {
        join inline_cont244(_wild180: unit) -> unit {
          let _wild181 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild182 = print_values(values/2) in
          join inline_cont242(aliased/3: Vec[int32]) -> unit {
            join inline_cont239(_wild183: unit) -> unit {
              join inline_cont236(_wild184: unit) -> unit {
                let _wild185 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild186 = print_values(aliased/3) in
                join inline_cont234(empty/5: Vec[int32]) -> unit {
                  let _wild187 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t196 = print_values(empty/5) in
                  ()
                } in
                let inline235 = @intrinsic(vec.new)() in
                jump inline_cont234(inline235)
              } in
              let inline237 = 5 in
              let inline238 = @intrinsic(vec.push)(aliased/3, inline237) in
              jump inline_cont236(())
            } in
            let inline240 = 4 in
            let inline241 = @intrinsic(vec.push)(aliased/3, inline240) in
            jump inline_cont239(())
          } in
          let inline243 = @intrinsic(vec.new)() in
          jump inline_cont242(inline243)
        } in
        let inline245 = 3 in
        let inline246 = @intrinsic(vec.push)(values/2, inline245) in
        jump inline_cont244(())
      } in
      let inline248 = 2 in
      let inline249 = @intrinsic(vec.push)(values/2, inline248) in
      jump inline_cont247(())
    } in
    let inline251 = 1 in
    let inline252 = @intrinsic(vec.push)(values/2, inline251) in
    jump inline_cont250(())
  } in
  let inline254 = @intrinsic(vec.new)() in
  jump inline_cont253(inline254)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont255(t198: string) -> unit {
    let t199 = @runtime(core.string_println)(t198) in
    ()
  } in
  let inline256 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont255(inline256)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/185: Vec[int32]) -> int {
  let t202 = @intrinsic(vec.len)(self/185) in
  t202
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont257(t204: string) -> unit {
    let t205 = @runtime(core.string_println)(t204) in
    ()
  } in
  let inline258 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont257(inline258)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t208 = @intrinsic(vec.new)() in
  t208
}

fn inherent#Vec#Vec[T]#push__T_int32(self/174: Vec[int32], elem/175: int32) -> unit {
  let t210 = @intrinsic(vec.push)(self/174, elem/175) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/219: Vec[int32], other/220: Vec[int32]) -> unit {
  join inline_cont263(len/221: int) -> unit {
    join inline_cont261(_wild136: unit) -> unit {
      let index/222 = 0 in
      join exit212() -> unit {
        ()
      } in
      joinrec {
        join loop213() -> unit {
          let t214 = (index/222 < len/221) in
          if t214 {
            let t215 = @intrinsic(vec.get)(other/220, index/222) in
            join inline_cont259(_wild137: unit) -> unit {
              let compound_old138 = index/222 in
              let compound_value139 = 1 in
              let t216 = (compound_old138 + compound_value139) in
              let t217 = index/222 = t216 in
              jump loop213()
            } in
            let inline260 = @intrinsic(vec.push)(self/219, t215) in
            jump inline_cont259(())
          } else {
            jump exit212()
          }
        } in
      } in
      jump loop213()
    } in
    let inline262 = @intrinsic(vec.reserve)(self/219, len/221) in
    jump inline_cont261(())
  } in
  let inline264 = @intrinsic(vec.len)(other/220) in
  jump inline_cont263(inline264)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t220 = @runtime(core.int_to_string)(self/67) in
  t220
}

fn trait_impl#ToString#int32#to_string(self/70: int32) -> string {
  let t223 = @runtime(core.int32_to_string)(self/70) in
  t223
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/188: Vec[int32], additional/189: int) -> unit {
  let t225 = @intrinsic(vec.reserve)(self/188, additional/189) in
  ()
}