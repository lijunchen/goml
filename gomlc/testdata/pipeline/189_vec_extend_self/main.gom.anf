fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont468(t425: int) -> unit {
    join inline_cont465(_wild408: unit) -> unit {
      let for_limit410 = @intrinsic(vec.len)(values/0) in
      let for_index411 = 0 in
      join exit426() -> unit {
        ()
      } in
      joinrec {
        join loop427() -> unit {
          let t428 = (for_index411 < for_limit410) in
          if t428 {
            let for_item412 = @intrinsic(vec.get)(values/0, for_index411) in
            let t429 = (for_index411 + 1) in
            let _wild413 = for_index411 = t429 in
            let inline463 = trait_impl#ToString#int32#to_string(for_item412) in
            let inline464 = @runtime(core.string_println)(inline463) in
            jump loop427()
          } else {
            jump exit426()
          }
        } in
      } in
      jump loop427()
    } in
    let inline466 = trait_impl#ToString#int#to_string(t425) in
    let inline467 = @runtime(core.string_println)(inline466) in
    jump inline_cont465(())
  } in
  let inline469 = @intrinsic(vec.len)(values/0) in
  jump inline_cont468(inline469)
}

fn main() -> unit {
  join inline_cont489(values/2: Vec[int32]) -> unit {
    join inline_cont486(_wild414: unit) -> unit {
      join inline_cont483(_wild415: unit) -> unit {
        join inline_cont480(_wild416: unit) -> unit {
          let _wild417 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild418 = print_values(values/2) in
          join inline_cont478(aliased/3: Vec[int32]) -> unit {
            join inline_cont475(_wild419: unit) -> unit {
              join inline_cont472(_wild420: unit) -> unit {
                let _wild421 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild422 = print_values(aliased/3) in
                join inline_cont470(empty/5: Vec[int32]) -> unit {
                  let _wild423 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t432 = print_values(empty/5) in
                  ()
                } in
                let inline471 = @intrinsic(vec.new)() in
                jump inline_cont470(inline471)
              } in
              let inline473 = 5 in
              let inline474 = @intrinsic(vec.push)(aliased/3, inline473) in
              jump inline_cont472(())
            } in
            let inline476 = 4 in
            let inline477 = @intrinsic(vec.push)(aliased/3, inline476) in
            jump inline_cont475(())
          } in
          let inline479 = @intrinsic(vec.new)() in
          jump inline_cont478(inline479)
        } in
        let inline481 = 3 in
        let inline482 = @intrinsic(vec.push)(values/2, inline481) in
        jump inline_cont480(())
      } in
      let inline484 = 2 in
      let inline485 = @intrinsic(vec.push)(values/2, inline484) in
      jump inline_cont483(())
    } in
    let inline487 = 1 in
    let inline488 = @intrinsic(vec.push)(values/2, inline487) in
    jump inline_cont486(())
  } in
  let inline490 = @intrinsic(vec.new)() in
  jump inline_cont489(inline490)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont491(t434: string) -> unit {
    let t435 = @runtime(core.string_println)(t434) in
    ()
  } in
  let inline492 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont491(inline492)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/273: Vec[int32]) -> int {
  let t438 = @intrinsic(vec.len)(self/273) in
  t438
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont493(t440: string) -> unit {
    let t441 = @runtime(core.string_println)(t440) in
    ()
  } in
  let inline494 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont493(inline494)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t444 = @intrinsic(vec.new)() in
  t444
}

fn inherent#Vec#Vec[T]#push__T_int32(self/258: Vec[int32], elem/259: int32) -> unit {
  let t446 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/307: Vec[int32], other/308: Vec[int32]) -> unit {
  join inline_cont499(len/309: int) -> unit {
    join inline_cont497(_wild251: unit) -> unit {
      let index/310 = 0 in
      join exit448() -> unit {
        ()
      } in
      joinrec {
        join loop449() -> unit {
          let t450 = (index/310 < len/309) in
          if t450 {
            let t451 = @intrinsic(vec.get)(other/308, index/310) in
            join inline_cont495(_wild252: unit) -> unit {
              let compound_old253 = index/310 in
              let compound_value254 = 1 in
              let t452 = (compound_old253 + compound_value254) in
              let t453 = index/310 = t452 in
              jump loop449()
            } in
            let inline496 = @intrinsic(vec.push)(self/307, t451) in
            jump inline_cont495(())
          } else {
            jump exit448()
          }
        } in
      } in
      jump loop449()
    } in
    let inline498 = @intrinsic(vec.reserve)(self/307, len/309) in
    jump inline_cont497(())
  } in
  let inline500 = @intrinsic(vec.len)(other/308) in
  jump inline_cont499(inline500)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t456 = @runtime(core.int_to_string)(self/151) in
  t456
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t459 = @runtime(core.int32_to_string)(self/154) in
  t459
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/276: Vec[int32], additional/277: int) -> unit {
  let t461 = @intrinsic(vec.reserve)(self/276, additional/277) in
  ()
}