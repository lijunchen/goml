fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont242(t199: int) -> unit {
    join inline_cont239(_wild182: unit) -> unit {
      let for_limit184 = @intrinsic(vec.len)(values/0) in
      let for_index185 = 0 in
      join exit200() -> unit {
        ()
      } in
      joinrec {
        join loop201() -> unit {
          let t202 = (for_index185 < for_limit184) in
          if t202 {
            let for_item186 = @intrinsic(vec.get)(values/0, for_index185) in
            let t203 = (for_index185 + 1) in
            let _wild187 = for_index185 = t203 in
            let inline237 = trait_impl#ToString#int32#to_string(for_item186) in
            let inline238 = @runtime(core.string_println)(inline237) in
            jump loop201()
          } else {
            jump exit200()
          }
        } in
      } in
      jump loop201()
    } in
    let inline240 = trait_impl#ToString#int#to_string(t199) in
    let inline241 = @runtime(core.string_println)(inline240) in
    jump inline_cont239(())
  } in
  let inline243 = @intrinsic(vec.len)(values/0) in
  jump inline_cont242(inline243)
}

fn main() -> unit {
  join inline_cont263(values/2: Vec[int32]) -> unit {
    join inline_cont260(_wild188: unit) -> unit {
      join inline_cont257(_wild189: unit) -> unit {
        join inline_cont254(_wild190: unit) -> unit {
          let _wild191 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild192 = print_values(values/2) in
          join inline_cont252(aliased/3: Vec[int32]) -> unit {
            join inline_cont249(_wild193: unit) -> unit {
              join inline_cont246(_wild194: unit) -> unit {
                let _wild195 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild196 = print_values(aliased/3) in
                join inline_cont244(empty/5: Vec[int32]) -> unit {
                  let _wild197 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t206 = print_values(empty/5) in
                  ()
                } in
                let inline245 = @intrinsic(vec.new)() in
                jump inline_cont244(inline245)
              } in
              let inline247 = 5 in
              let inline248 = @intrinsic(vec.push)(aliased/3, inline247) in
              jump inline_cont246(())
            } in
            let inline250 = 4 in
            let inline251 = @intrinsic(vec.push)(aliased/3, inline250) in
            jump inline_cont249(())
          } in
          let inline253 = @intrinsic(vec.new)() in
          jump inline_cont252(inline253)
        } in
        let inline255 = 3 in
        let inline256 = @intrinsic(vec.push)(values/2, inline255) in
        jump inline_cont254(())
      } in
      let inline258 = 2 in
      let inline259 = @intrinsic(vec.push)(values/2, inline258) in
      jump inline_cont257(())
    } in
    let inline261 = 1 in
    let inline262 = @intrinsic(vec.push)(values/2, inline261) in
    jump inline_cont260(())
  } in
  let inline264 = @intrinsic(vec.new)() in
  jump inline_cont263(inline264)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont265(t208: string) -> unit {
    let t209 = @runtime(core.string_println)(t208) in
    ()
  } in
  let inline266 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont265(inline266)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/189: Vec[int32]) -> int {
  let t212 = @intrinsic(vec.len)(self/189) in
  t212
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont267(t214: string) -> unit {
    let t215 = @runtime(core.string_println)(t214) in
    ()
  } in
  let inline268 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont267(inline268)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t218 = @intrinsic(vec.new)() in
  t218
}

fn inherent#Vec#Vec[T]#push__T_int32(self/174: Vec[int32], elem/175: int32) -> unit {
  let t220 = @intrinsic(vec.push)(self/174, elem/175) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/223: Vec[int32], other/224: Vec[int32]) -> unit {
  join inline_cont273(len/225: int) -> unit {
    join inline_cont271(_wild141: unit) -> unit {
      let index/226 = 0 in
      join exit222() -> unit {
        ()
      } in
      joinrec {
        join loop223() -> unit {
          let t224 = (index/226 < len/225) in
          if t224 {
            let t225 = @intrinsic(vec.get)(other/224, index/226) in
            join inline_cont269(_wild142: unit) -> unit {
              let compound_old143 = index/226 in
              let compound_value144 = 1 in
              let t226 = (compound_old143 + compound_value144) in
              let t227 = index/226 = t226 in
              jump loop223()
            } in
            let inline270 = @intrinsic(vec.push)(self/223, t225) in
            jump inline_cont269(())
          } else {
            jump exit222()
          }
        } in
      } in
      jump loop223()
    } in
    let inline272 = @intrinsic(vec.reserve)(self/223, len/225) in
    jump inline_cont271(())
  } in
  let inline274 = @intrinsic(vec.len)(other/224) in
  jump inline_cont273(inline274)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t230 = @runtime(core.int_to_string)(self/67) in
  t230
}

fn trait_impl#ToString#int32#to_string(self/70: int32) -> string {
  let t233 = @runtime(core.int32_to_string)(self/70) in
  t233
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/192: Vec[int32], additional/193: int) -> unit {
  let t235 = @intrinsic(vec.reserve)(self/192, additional/193) in
  ()
}