fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont471(t428: int) -> unit {
    join inline_cont468(_wild411: unit) -> unit {
      let for_limit413 = @intrinsic(vec.len)(values/0) in
      let for_index414 = 0 in
      join exit429() -> unit {
        ()
      } in
      joinrec {
        join loop430() -> unit {
          let t431 = (for_index414 < for_limit413) in
          if t431 {
            let for_item415 = @intrinsic(vec.get)(values/0, for_index414) in
            let t432 = (for_index414 + 1) in
            let _wild416 = for_index414 = t432 in
            let inline466 = trait_impl#ToString#int32#to_string(for_item415) in
            let inline467 = @runtime(core.string_println)(inline466) in
            jump loop430()
          } else {
            jump exit429()
          }
        } in
      } in
      jump loop430()
    } in
    let inline469 = trait_impl#ToString#int#to_string(t428) in
    let inline470 = @runtime(core.string_println)(inline469) in
    jump inline_cont468(())
  } in
  let inline472 = @intrinsic(vec.len)(values/0) in
  jump inline_cont471(inline472)
}

fn main() -> unit {
  join inline_cont492(values/2: Vec[int32]) -> unit {
    join inline_cont489(_wild417: unit) -> unit {
      join inline_cont486(_wild418: unit) -> unit {
        join inline_cont483(_wild419: unit) -> unit {
          let _wild420 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild421 = print_values(values/2) in
          join inline_cont481(aliased/3: Vec[int32]) -> unit {
            join inline_cont478(_wild422: unit) -> unit {
              join inline_cont475(_wild423: unit) -> unit {
                let _wild424 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild425 = print_values(aliased/3) in
                join inline_cont473(empty/5: Vec[int32]) -> unit {
                  let _wild426 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t435 = print_values(empty/5) in
                  ()
                } in
                let inline474 = @intrinsic(vec.new)() in
                jump inline_cont473(inline474)
              } in
              let inline476 = 5 in
              let inline477 = @intrinsic(vec.push)(aliased/3, inline476) in
              jump inline_cont475(())
            } in
            let inline479 = 4 in
            let inline480 = @intrinsic(vec.push)(aliased/3, inline479) in
            jump inline_cont478(())
          } in
          let inline482 = @intrinsic(vec.new)() in
          jump inline_cont481(inline482)
        } in
        let inline484 = 3 in
        let inline485 = @intrinsic(vec.push)(values/2, inline484) in
        jump inline_cont483(())
      } in
      let inline487 = 2 in
      let inline488 = @intrinsic(vec.push)(values/2, inline487) in
      jump inline_cont486(())
    } in
    let inline490 = 1 in
    let inline491 = @intrinsic(vec.push)(values/2, inline490) in
    jump inline_cont489(())
  } in
  let inline493 = @intrinsic(vec.new)() in
  jump inline_cont492(inline493)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont494(t437: string) -> unit {
    let t438 = @runtime(core.string_println)(t437) in
    ()
  } in
  let inline495 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont494(inline495)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/273: Vec[int32]) -> int {
  let t441 = @intrinsic(vec.len)(self/273) in
  t441
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont496(t443: string) -> unit {
    let t444 = @runtime(core.string_println)(t443) in
    ()
  } in
  let inline497 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont496(inline497)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t447 = @intrinsic(vec.new)() in
  t447
}

fn inherent#Vec#Vec[T]#push__T_int32(self/258: Vec[int32], elem/259: int32) -> unit {
  let t449 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/307: Vec[int32], other/308: Vec[int32]) -> unit {
  join inline_cont502(len/309: int) -> unit {
    join inline_cont500(_wild251: unit) -> unit {
      let index/310 = 0 in
      join exit451() -> unit {
        ()
      } in
      joinrec {
        join loop452() -> unit {
          let t453 = (index/310 < len/309) in
          if t453 {
            let t454 = @intrinsic(vec.get)(other/308, index/310) in
            join inline_cont498(_wild252: unit) -> unit {
              let compound_old253 = index/310 in
              let compound_value254 = 1 in
              let t455 = (compound_old253 + compound_value254) in
              let t456 = index/310 = t455 in
              jump loop452()
            } in
            let inline499 = @intrinsic(vec.push)(self/307, t454) in
            jump inline_cont498(())
          } else {
            jump exit451()
          }
        } in
      } in
      jump loop452()
    } in
    let inline501 = @intrinsic(vec.reserve)(self/307, len/309) in
    jump inline_cont500(())
  } in
  let inline503 = @intrinsic(vec.len)(other/308) in
  jump inline_cont502(inline503)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t459 = @runtime(core.int_to_string)(self/151) in
  t459
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t462 = @runtime(core.int32_to_string)(self/154) in
  t462
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/276: Vec[int32], additional/277: int) -> unit {
  let t464 = @intrinsic(vec.reserve)(self/276, additional/277) in
  ()
}