fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont215(t172: int) -> unit {
    join inline_cont212(_wild155: unit) -> unit {
      let for_source156 = values/0 in
      let for_limit157 = @intrinsic(vec.len)(values/0) in
      let for_index158 = 0 in
      join exit173() -> unit {
        ()
      } in
      joinrec {
        join loop174() -> unit {
          let t175 = (for_index158 < for_limit157) in
          if t175 {
            let for_item159 = @intrinsic(vec.get)(values/0, for_index158) in
            let t176 = (for_index158 + 1) in
            let _wild160 = for_index158 = t176 in
            let value/1 = for_item159 in
            let inline210 = trait_impl#ToString#int32#to_string(for_item159) in
            let inline211 = @runtime(core.string_println)(inline210) in
            jump loop174()
          } else {
            jump exit173()
          }
        } in
      } in
      jump loop174()
    } in
    let inline213 = trait_impl#ToString#int#to_string(t172) in
    let inline214 = @runtime(core.string_println)(inline213) in
    jump inline_cont212(())
  } in
  let inline216 = @intrinsic(vec.len)(values/0) in
  jump inline_cont215(inline216)
}

fn main() -> unit {
  join inline_cont236(values/2: Vec[int32]) -> unit {
    join inline_cont233(_wild161: unit) -> unit {
      join inline_cont230(_wild162: unit) -> unit {
        join inline_cont227(_wild163: unit) -> unit {
          let _wild164 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild165 = print_values(values/2) in
          join inline_cont225(aliased/3: Vec[int32]) -> unit {
            join inline_cont222(_wild166: unit) -> unit {
              join inline_cont219(_wild167: unit) -> unit {
                let same/4 = aliased/3 in
                let _wild168 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild169 = print_values(aliased/3) in
                join inline_cont217(empty/5: Vec[int32]) -> unit {
                  let _wild170 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t179 = print_values(empty/5) in
                  ()
                } in
                let inline218 = @intrinsic(vec.new)() in
                jump inline_cont217(inline218)
              } in
              let inline220 = 5 in
              let inline221 = @intrinsic(vec.push)(aliased/3, inline220) in
              jump inline_cont219(())
            } in
            let inline223 = 4 in
            let inline224 = @intrinsic(vec.push)(aliased/3, inline223) in
            jump inline_cont222(())
          } in
          let inline226 = @intrinsic(vec.new)() in
          jump inline_cont225(inline226)
        } in
        let inline228 = 3 in
        let inline229 = @intrinsic(vec.push)(values/2, inline228) in
        jump inline_cont227(())
      } in
      let inline231 = 2 in
      let inline232 = @intrinsic(vec.push)(values/2, inline231) in
      jump inline_cont230(())
    } in
    let inline234 = 1 in
    let inline235 = @intrinsic(vec.push)(values/2, inline234) in
    jump inline_cont233(())
  } in
  let inline237 = @intrinsic(vec.new)() in
  jump inline_cont236(inline237)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont238(t181: string) -> unit {
    let t182 = @runtime(core.string_println)(t181) in
    ()
  } in
  let inline239 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont238(inline239)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/137: Vec[int32]) -> int {
  let t185 = @intrinsic(vec.len)(self/137) in
  t185
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont240(t187: string) -> unit {
    let t188 = @runtime(core.string_println)(t187) in
    ()
  } in
  let inline241 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont240(inline241)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t191 = @intrinsic(vec.new)() in
  t191
}

fn inherent#Vec#Vec[T]#push__T_int32(self/126: Vec[int32], elem/127: int32) -> unit {
  let t193 = @intrinsic(vec.push)(self/126, elem/127) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/171: Vec[int32], other/172: Vec[int32]) -> unit {
  join inline_cont246(len/173: int) -> unit {
    join inline_cont244(_wild94: unit) -> unit {
      let index/174 = 0 in
      join exit195() -> unit {
        ()
      } in
      joinrec {
        join loop196() -> unit {
          let t197 = (index/174 < len/173) in
          if t197 {
            let t198 = @intrinsic(vec.get)(other/172, index/174) in
            join inline_cont242(_wild95: unit) -> unit {
              let compound_old96 = index/174 in
              let compound_value97 = 1 in
              let t199 = (compound_old96 + compound_value97) in
              let t200 = index/174 = t199 in
              let _wild98 = t200 in
              jump loop196()
            } in
            let inline243 = @intrinsic(vec.push)(self/171, t198) in
            jump inline_cont242(())
          } else {
            jump exit195()
          }
        } in
      } in
      jump loop196()
    } in
    let inline245 = @intrinsic(vec.reserve)(self/171, len/173) in
    jump inline_cont244(())
  } in
  let inline247 = @intrinsic(vec.len)(other/172) in
  jump inline_cont246(inline247)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  let t203 = @runtime(core.int_to_string)(self/40) in
  t203
}

fn trait_impl#ToString#int32#to_string(self/43: int32) -> string {
  let t206 = @runtime(core.int32_to_string)(self/43) in
  t206
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/140: Vec[int32], additional/141: int) -> unit {
  let t208 = @intrinsic(vec.reserve)(self/140, additional/141) in
  ()
}