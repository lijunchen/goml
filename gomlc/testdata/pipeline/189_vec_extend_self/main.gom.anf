fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont196(t153: int) -> unit {
    join inline_cont193(_wild136: unit) -> unit {
      let for_source137 = values/0 in
      let for_limit138 = @intrinsic(vec.len)(values/0) in
      let for_index139 = 0 in
      join exit154() -> unit {
        ()
      } in
      joinrec {
        join loop155() -> unit {
          let t156 = (for_index139 < for_limit138) in
          if t156 {
            let for_item140 = @intrinsic(vec.get)(values/0, for_index139) in
            let t157 = (for_index139 + 1) in
            let _wild141 = for_index139 = t157 in
            let value/1 = for_item140 in
            let inline191 = trait_impl#ToString#int32#to_string(for_item140) in
            let inline192 = @runtime(core.string_println)(inline191) in
            jump loop155()
          } else {
            jump exit154()
          }
        } in
      } in
      jump loop155()
    } in
    let inline194 = trait_impl#ToString#int#to_string(t153) in
    let inline195 = @runtime(core.string_println)(inline194) in
    jump inline_cont193(())
  } in
  let inline197 = @intrinsic(vec.len)(values/0) in
  jump inline_cont196(inline197)
}

fn main() -> unit {
  join inline_cont217(values/2: Vec[int32]) -> unit {
    join inline_cont214(_wild142: unit) -> unit {
      join inline_cont211(_wild143: unit) -> unit {
        join inline_cont208(_wild144: unit) -> unit {
          let _wild145 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild146 = print_values(values/2) in
          join inline_cont206(aliased/3: Vec[int32]) -> unit {
            join inline_cont203(_wild147: unit) -> unit {
              join inline_cont200(_wild148: unit) -> unit {
                let same/4 = aliased/3 in
                let _wild149 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild150 = print_values(aliased/3) in
                join inline_cont198(empty/5: Vec[int32]) -> unit {
                  let _wild151 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t160 = print_values(empty/5) in
                  ()
                } in
                let inline199 = @intrinsic(vec.new)() in
                jump inline_cont198(inline199)
              } in
              let inline201 = 5 in
              let inline202 = @intrinsic(vec.push)(aliased/3, inline201) in
              jump inline_cont200(())
            } in
            let inline204 = 4 in
            let inline205 = @intrinsic(vec.push)(aliased/3, inline204) in
            jump inline_cont203(())
          } in
          let inline207 = @intrinsic(vec.new)() in
          jump inline_cont206(inline207)
        } in
        let inline209 = 3 in
        let inline210 = @intrinsic(vec.push)(values/2, inline209) in
        jump inline_cont208(())
      } in
      let inline212 = 2 in
      let inline213 = @intrinsic(vec.push)(values/2, inline212) in
      jump inline_cont211(())
    } in
    let inline215 = 1 in
    let inline216 = @intrinsic(vec.push)(values/2, inline215) in
    jump inline_cont214(())
  } in
  let inline218 = @intrinsic(vec.new)() in
  jump inline_cont217(inline218)
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont219(t162: string) -> unit {
    let t163 = @runtime(core.string_println)(t162) in
    ()
  } in
  let inline220 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont219(inline220)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/162: Vec[int32]) -> int {
  let t166 = @intrinsic(vec.len)(self/162) in
  t166
}

fn println__T_int32(value/31: int32) -> unit {
  join inline_cont221(t168: string) -> unit {
    let t169 = @runtime(core.string_println)(t168) in
    ()
  } in
  let inline222 = @runtime(core.int32_to_string)(value/31) in
  jump inline_cont221(inline222)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t172 = @intrinsic(vec.new)() in
  t172
}

fn inherent#Vec#Vec[T]#push__T_int32(self/151: Vec[int32], elem/152: int32) -> unit {
  let t174 = @intrinsic(vec.push)(self/151, elem/152) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/196: Vec[int32], other/197: Vec[int32]) -> unit {
  join inline_cont227(len/198: int) -> unit {
    join inline_cont225(_wild100: unit) -> unit {
      let index/199 = 0 in
      join exit176() -> unit {
        ()
      } in
      joinrec {
        join loop177() -> unit {
          let t178 = (index/199 < len/198) in
          if t178 {
            let t179 = @intrinsic(vec.get)(other/197, index/199) in
            join inline_cont223(_wild101: unit) -> unit {
              let compound_old102 = index/199 in
              let compound_value103 = 1 in
              let t180 = (compound_old102 + compound_value103) in
              let t181 = index/199 = t180 in
              let _wild104 = t181 in
              jump loop177()
            } in
            let inline224 = @intrinsic(vec.push)(self/196, t179) in
            jump inline_cont223(())
          } else {
            jump exit176()
          }
        } in
      } in
      jump loop177()
    } in
    let inline226 = @intrinsic(vec.reserve)(self/196, len/198) in
    jump inline_cont225(())
  } in
  let inline228 = @intrinsic(vec.len)(other/197) in
  jump inline_cont227(inline228)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t184 = @runtime(core.int_to_string)(self/69) in
  t184
}

fn trait_impl#ToString#int32#to_string(self/72: int32) -> string {
  let t187 = @runtime(core.int32_to_string)(self/72) in
  t187
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/165: Vec[int32], additional/166: int) -> unit {
  let t189 = @intrinsic(vec.reserve)(self/165, additional/166) in
  ()
}