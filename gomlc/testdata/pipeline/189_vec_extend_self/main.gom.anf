fn print_values(values/0: Vec[int32]) -> unit {
  join inline_cont247(t204: int) -> unit {
    join inline_cont244(_wild187: unit) -> unit {
      let for_limit189 = @intrinsic(vec.len)(values/0) in
      let for_index190 = 0 in
      join exit205() -> unit {
        ()
      } in
      joinrec {
        join loop206() -> unit {
          let t207 = (for_index190 < for_limit189) in
          if t207 {
            let for_item191 = @intrinsic(vec.get)(values/0, for_index190) in
            let t208 = (for_index190 + 1) in
            let _wild192 = for_index190 = t208 in
            let inline242 = trait_impl#ToString#int32#to_string(for_item191) in
            let inline243 = @runtime(core.string_println)(inline242) in
            jump loop206()
          } else {
            jump exit205()
          }
        } in
      } in
      jump loop206()
    } in
    let inline245 = trait_impl#ToString#int#to_string(t204) in
    let inline246 = @runtime(core.string_println)(inline245) in
    jump inline_cont244(())
  } in
  let inline248 = @intrinsic(vec.len)(values/0) in
  jump inline_cont247(inline248)
}

fn main() -> unit {
  join inline_cont268(values/2: Vec[int32]) -> unit {
    join inline_cont265(_wild193: unit) -> unit {
      join inline_cont262(_wild194: unit) -> unit {
        join inline_cont259(_wild195: unit) -> unit {
          let _wild196 = inherent#Vec#Vec[T]#extend__T_int32(values/2, values/2) in
          let _wild197 = print_values(values/2) in
          join inline_cont257(aliased/3: Vec[int32]) -> unit {
            join inline_cont254(_wild198: unit) -> unit {
              join inline_cont251(_wild199: unit) -> unit {
                let _wild200 = inherent#Vec#Vec[T]#extend__T_int32(aliased/3, aliased/3) in
                let _wild201 = print_values(aliased/3) in
                join inline_cont249(empty/5: Vec[int32]) -> unit {
                  let _wild202 = inherent#Vec#Vec[T]#extend__T_int32(empty/5, empty/5) in
                  let t211 = print_values(empty/5) in
                  ()
                } in
                let inline250 = @intrinsic(vec.new)() in
                jump inline_cont249(inline250)
              } in
              let inline252 = 5 in
              let inline253 = @intrinsic(vec.push)(aliased/3, inline252) in
              jump inline_cont251(())
            } in
            let inline255 = 4 in
            let inline256 = @intrinsic(vec.push)(aliased/3, inline255) in
            jump inline_cont254(())
          } in
          let inline258 = @intrinsic(vec.new)() in
          jump inline_cont257(inline258)
        } in
        let inline260 = 3 in
        let inline261 = @intrinsic(vec.push)(values/2, inline260) in
        jump inline_cont259(())
      } in
      let inline263 = 2 in
      let inline264 = @intrinsic(vec.push)(values/2, inline263) in
      jump inline_cont262(())
    } in
    let inline266 = 1 in
    let inline267 = @intrinsic(vec.push)(values/2, inline266) in
    jump inline_cont265(())
  } in
  let inline269 = @intrinsic(vec.new)() in
  jump inline_cont268(inline269)
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont270(t213: string) -> unit {
    let t214 = @runtime(core.string_println)(t213) in
    ()
  } in
  let inline271 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont270(inline271)
}

fn inherent#Vec#Vec[T]#len__T_int32(self/189: Vec[int32]) -> int {
  let t217 = @intrinsic(vec.len)(self/189) in
  t217
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont272(t219: string) -> unit {
    let t220 = @runtime(core.string_println)(t219) in
    ()
  } in
  let inline273 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont272(inline273)
}

fn inherent#Vec#Vec[T]#new__T_int32() -> Vec[int32] {
  let t223 = @intrinsic(vec.new)() in
  t223
}

fn inherent#Vec#Vec[T]#push__T_int32(self/174: Vec[int32], elem/175: int32) -> unit {
  let t225 = @intrinsic(vec.push)(self/174, elem/175) in
  ()
}

fn inherent#Vec#Vec[T]#extend__T_int32(self/223: Vec[int32], other/224: Vec[int32]) -> unit {
  join inline_cont278(len/225: int) -> unit {
    join inline_cont276(_wild141: unit) -> unit {
      let index/226 = 0 in
      join exit227() -> unit {
        ()
      } in
      joinrec {
        join loop228() -> unit {
          let t229 = (index/226 < len/225) in
          if t229 {
            let t230 = @intrinsic(vec.get)(other/224, index/226) in
            join inline_cont274(_wild142: unit) -> unit {
              let compound_old143 = index/226 in
              let compound_value144 = 1 in
              let t231 = (compound_old143 + compound_value144) in
              let t232 = index/226 = t231 in
              jump loop228()
            } in
            let inline275 = @intrinsic(vec.push)(self/223, t230) in
            jump inline_cont274(())
          } else {
            jump exit227()
          }
        } in
      } in
      jump loop228()
    } in
    let inline277 = @intrinsic(vec.reserve)(self/223, len/225) in
    jump inline_cont276(())
  } in
  let inline279 = @intrinsic(vec.len)(other/224) in
  jump inline_cont278(inline279)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t235 = @runtime(core.int_to_string)(self/67) in
  t235
}

fn trait_impl#ToString#int32#to_string(self/70: int32) -> string {
  let t238 = @runtime(core.int32_to_string)(self/70) in
  t238
}

fn inherent#Vec#Vec[T]#reserve__T_int32(self/192: Vec[int32], additional/193: int) -> unit {
  let t240 = @intrinsic(vec.reserve)(self/192, additional/193) in
  ()
}