fn main() -> unit {
  join inline_cont867(t798: string) -> unit {
    join inline_cont864(_wild796: unit) -> unit {
      join inline_cont862(t799: i32) -> unit {
        join inline_cont860(t800: string) -> unit {
          let inline858 = trait_impl#ToString#string#to_string(t800) in
          let inline859 = @runtime(core.string_println)(inline858) in
          ()
        } in
        let inline861 = __goml_builtin_int32_to_string(t799) in
        jump inline_cont860(inline861)
      } in
      let inline863 = 42 in
      jump inline_cont862(inline863)
    } in
    let inline865 = trait_impl#ToString#string#to_string(t798) in
    let inline866 = @runtime(core.string_println)(inline865) in
    jump inline_cont864(())
  } in
  let inline868 = "direct" in
  jump inline_cont867(inline868)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont869(t803: string) -> unit {
    let t804 = @runtime(core.string_println)(t803) in
    ()
  } in
  jump inline_cont869(value/1)
}

fn identity__T_string(value/0: string) -> string {
  value/0
}

fn identity__T_i32(value/0: i32) -> i32 {
  value/0
}

fn inherent#i32#i32#to_string(self/286: i32) -> string {
  let inline871 = (self/286 as i64) in
  let inline872 = signed_decimal_string(inline871) in
  inline872
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t816 = (value/225 as i64) in
  let inline874 = (t816 < 0) in
  if inline874 {
    let inline875 = (t816 as u64) in
    let inline876 = (0 - inline875) in
    let inline877 = decimal_string(inline876) in
    let inline878 = ("-" + inline877) in
    inline878
  } else {
    let inline879 = (t816 as u64) in
    let inline880 = decimal_string(inline879) in
    inline880
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t822 = (value/214 < 0) in
  if t822 {
    let t823 = (value/214 as u64) in
    let t824 = (0 - t823) in
    let t825 = decimal_string(t824) in
    let t826 = ("-" + t825) in
    t826
  } else {
    let t827 = (value/214 as u64) in
    let t828 = decimal_string(t827) in
    t828
  }
}

fn decimal_string(value/208: u64) -> string {
  let t851 = (value/208 == 0) in
  join k831() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit832() -> string {
      join inline_cont889(t833: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t833) in
        let offset/212 = 0 in
        join exit834() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop835() -> string {
            join inline_cont887(t836: isize) -> string {
              let t837 = (offset/212 < t836) in
              if t837 {
                join inline_cont885(t838: isize) -> string {
                  let t839 = (t838 - offset/212) in
                  let t840 = (t839 - 1) in
                  let t841 = @intrinsic(vec.get)(reversed/209, t840) in
                  join inline_cont883(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t842 = (compound_old358 + compound_value359) in
                    let t843 = offset/212 = t842 in
                    jump loop835()
                  } in
                  let inline884 = @intrinsic(vec.push)(bytes/211, t841) in
                  jump inline_cont883(())
                } in
                let inline886 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont885(inline886)
              } else {
                jump exit834()
              }
            } in
            let inline888 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont887(inline888)
          } in
        } in
        jump loop835()
      } in
      let inline890 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont889(inline890)
    } in
    joinrec {
      join loop844() -> string {
        let t845 = (remaining/210 > 0) in
        if t845 {
          let t846 = (remaining/210 % 10) in
          let t847 = (t846 as u8) in
          let t848 = (t847 + 48) in
          join inline_cont881(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t849 = (compound_old353 / compound_value354) in
            let t850 = remaining/210 = t849 in
            jump loop844()
          } in
          let inline882 = @intrinsic(vec.push)(reversed/209, t848) in
          jump inline_cont881(())
        } else {
          jump exit832()
        }
      } in
    } in
    jump loop844()
  } in
  if t851 {
    "0"
  } else {
    jump k831()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t853 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t856 = @intrinsic(vec.len)(self/526) in
  t856
}