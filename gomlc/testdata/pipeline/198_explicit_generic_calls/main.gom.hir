package main
file main.gom
  fn identity(local/1/0: T) -> T {
        value/0
    }
    
    fn main() -> unit {
        string_println([string]::identity("direct"));
        string_println([int32]::identity(42).to_string())
    }