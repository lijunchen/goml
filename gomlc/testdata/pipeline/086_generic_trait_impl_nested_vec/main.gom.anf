fn main() -> unit {
  join inline_cont869(t799: i32) -> unit {
    join inline_cont867(t800: string) -> unit {
      join inline_cont864(_wild796: unit) -> unit {
        join inline_cont863(t801: i32) -> unit {
          join inline_cont861(t802: string) -> unit {
            let inline859 = trait_impl#ToString#string#to_string(t802) in
            let inline860 = @runtime(core.string_println)(inline859) in
            ()
          } in
          let inline862 = __goml_builtin_int32_to_string(t801) in
          jump inline_cont861(inline862)
        } in
        jump inline_cont863(1)
      } in
      let inline865 = trait_impl#ToString#string#to_string(t800) in
      let inline866 = @runtime(core.string_println)(inline865) in
      jump inline_cont864(())
    } in
    let inline868 = __goml_builtin_int32_to_string(t799) in
    jump inline_cont867(inline868)
  } in
  jump inline_cont869(1)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont870(t804: string) -> unit {
    let t805 = @runtime(core.string_println)(t804) in
    ()
  } in
  jump inline_cont870(value/1)
}

fn trait_impl#Size#Wrap__isize#size(self/0: Wrap__isize) -> i32 {
  1
}

fn inherent#i32#i32#to_string(self/286: i32) -> string {
  let inline872 = (self/286 as i64) in
  let inline873 = signed_decimal_string(inline872) in
  inline873
}

fn trait_impl#Size#Wrap__string#size(self/0: Wrap__string) -> i32 {
  1
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t817 = (value/225 as i64) in
  let inline875 = (t817 < 0) in
  if inline875 {
    let inline876 = (t817 as u64) in
    let inline877 = (0 - inline876) in
    let inline878 = decimal_string(inline877) in
    let inline879 = ("-" + inline878) in
    inline879
  } else {
    let inline880 = (t817 as u64) in
    let inline881 = decimal_string(inline880) in
    inline881
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t823 = (value/214 < 0) in
  if t823 {
    let t824 = (value/214 as u64) in
    let t825 = (0 - t824) in
    let t826 = decimal_string(t825) in
    let t827 = ("-" + t826) in
    t827
  } else {
    let t828 = (value/214 as u64) in
    let t829 = decimal_string(t828) in
    t829
  }
}

fn decimal_string(value/208: u64) -> string {
  let t852 = (value/208 == 0) in
  join k832() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit833() -> string {
      join inline_cont890(t834: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t834) in
        let offset/212 = 0 in
        join exit835() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop836() -> string {
            join inline_cont888(t837: isize) -> string {
              let t838 = (offset/212 < t837) in
              if t838 {
                join inline_cont886(t839: isize) -> string {
                  let t840 = (t839 - offset/212) in
                  let t841 = (t840 - 1) in
                  let t842 = @intrinsic(vec.get)(reversed/209, t841) in
                  join inline_cont884(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t843 = (compound_old358 + compound_value359) in
                    let t844 = offset/212 = t843 in
                    jump loop836()
                  } in
                  let inline885 = @intrinsic(vec.push)(bytes/211, t842) in
                  jump inline_cont884(())
                } in
                let inline887 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont886(inline887)
              } else {
                jump exit835()
              }
            } in
            let inline889 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont888(inline889)
          } in
        } in
        jump loop836()
      } in
      let inline891 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont890(inline891)
    } in
    joinrec {
      join loop845() -> string {
        let t846 = (remaining/210 > 0) in
        if t846 {
          let t847 = (remaining/210 % 10) in
          let t848 = (t847 as u8) in
          let t849 = (t848 + 48) in
          join inline_cont882(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t850 = (compound_old353 / compound_value354) in
            let t851 = remaining/210 = t850 in
            jump loop845()
          } in
          let inline883 = @intrinsic(vec.push)(reversed/209, t849) in
          jump inline_cont882(())
        } else {
          jump exit833()
        }
      } in
    } in
    jump loop845()
  } in
  if t852 {
    "0"
  } else {
    jump k832()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t854 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t857 = @intrinsic(vec.len)(self/526) in
  t857
}