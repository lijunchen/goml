fn main() -> unit {
  let c/0 = 'A' in
  join inline_cont175(t158: string) -> unit {
    join inline_cont172(_wild155: unit) -> unit {
      let d/1 = 'b' in
      join k159(jp160: string) -> unit {
        let out/2 = jp160 in
        let inline170 = trait_impl#ToString#string#to_string(jp160) in
        let inline171 = @runtime(core.string_println)(inline170) in
        ()
      } in
      match d/1 {
        'a' => {
          jump k159("A")
        },
        'b' => {
          jump k159("B")
        },
        _ => {
          jump k159("?")
        },
      }
    } in
    let inline173 = trait_impl#ToString#string#to_string(t158) in
    let inline174 = @runtime(core.string_println)(inline173) in
    jump inline_cont172(())
  } in
  let inline176 = @runtime(core.char_to_string)(c/0) in
  jump inline_cont175(inline176)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont177(t162: string) -> unit {
    let t163 = @runtime(core.string_println)(t162) in
    ()
  } in
  jump inline_cont177(value/1)
}

fn inherent#char#char#to_string(self/7: char) -> string {
  let t166 = @runtime(core.char_to_string)(self/7) in
  t166
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  self/38
}