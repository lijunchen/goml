fn range(start/0: i32, end/0: i32) -> i32 {
  let t0 = (start/0 + end/0) in
  t0
}

fn endpoint(calls/0: Ref[isize], value/0: isize) -> isize {
  join inline_cont0(t0: isize) -> isize {
    let t1 = (t0 + 1) in
    let inline0 = @intrinsic(ref.set)(calls/0, t1) in
    value/0
  } in
  let inline1 = @intrinsic(ref.get)(calls/0) in
  jump inline_cont0(inline1)
}

fn main() -> () {
  let for_index0 = 1 in
  let for_limit0 = 4 in
  join exit0() -> () {
    join inline_cont0(calls/0: Ref[isize]) -> () {
      join inline_cont1(for_index1: isize) -> () {
        join inline_cont2(for_limit1: isize) -> () {
          let for_done0 = (for_index1 > for_limit1) in
          join exit1() -> () {
            let for_index2 = 3 in
            let for_limit2 = 1 in
            let for_done1 = (for_index2 > for_limit2) in
            join exit2() -> () {
              let maximum/0 = 9223372036854775807 in
              let for_index3 = maximum/0 in
              let for_done2 = (for_index3 > maximum/0) in
              join exit3() -> () {
                join inline_cont3(iterator/0: FnIterator__isize) -> () {
                  join inline_cont4(mtmp0: Option__isize) -> () {
                    join k0() -> () {
                      join inline_cont5(t0: isize) -> () {
                        join inline_cont6(_wild0: ()) -> () {
                          join inline_cont7(t1: i32) -> () {
                            let inline0 = trait_impl#ToString#i32#to_string(t1) in
                            let inline1 = @runtime(core.string_println)(inline0) in
                            ()
                          } in
                          let inline2 = 10 in
                          let inline3 = 20 in
                          let inline4 = (inline2 + inline3) in
                          jump inline_cont7(inline4)
                        } in
                        let inline5 = trait_impl#ToString#isize#to_string(t0) in
                        let inline6 = @runtime(core.string_println)(inline5) in
                        jump inline_cont6(())
                      } in
                      let inline7 = @intrinsic(ref.get)(calls/0) in
                      jump inline_cont5(inline7)
                    } in
                    match mtmp0 {
                      Tag_0 => {
                        let inline8 = "missing" in
                        let inline9 = trait_impl#ToString#string#to_string(inline8) in
                        let inline10 = @runtime(core.string_println)(inline9) in
                        jump k0()
                      },
                      Tag_1 => {
                        let x0 = Option__isize::Some._0(mtmp0) in
                        let inline11 = trait_impl#ToString#isize#to_string(x0) in
                        let inline12 = @runtime(core.string_println)(inline11) in
                        jump k0()
                      },
                    }
                  } in
                  let inline13 = FnIterator__isize.next_fn(iterator/0) in
                  let inline14 = inline13() in
                  jump inline_cont4(inline14)
                } in
                let inline15 = 8 in
                let inline16 = 8 in
                let inline17 = @intrinsic(ref.new)(inline15) in
                let inline18 = @intrinsic(ref.new)(false) in
                let inline19 =
                  closure_env_goml_builtin_range_inclusive_0 {
                    finished_0: inline18,
                    current_1: inline17,
                    end_2: inline16
                  } in
                let inline20 =
                  pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
                    inline19
                  ) in
                let inline21 = inherent#FnIterator#FnIterator[T]#from_fn__T_isize(inline20) in
                jump inline_cont3(inline21)
              } in
              joinrec {
                join loop3() -> () {
                  let t2 = (!for_done2) in
                  if t2 {
                    let for_item0 = for_index3 in
                    let t3 = (for_index3 == maximum/0) in
                    join k1() -> () {
                      let inline22 = trait_impl#ToString#isize#to_string(for_item0) in
                      let inline23 = @runtime(core.string_println)(inline22) in
                      jump loop3()
                    } in
                    if t3 {
                      let t4 = for_done2 = true in
                      jump k1()
                    } else {
                      let t5 = (for_index3 + 1) in
                      let t6 = for_index3 = t5 in
                      jump k1()
                    }
                  } else {
                    jump exit3()
                  }
                } in
              } in
              jump loop3()
            } in
            joinrec {
              join loop2() -> () {
                let t7 = (!for_done1) in
                if t7 {
                  let for_item1 = for_index2 in
                  let t8 = (for_index2 == for_limit2) in
                  join k2() -> () {
                    let inline24 = trait_impl#ToString#isize#to_string(for_item1) in
                    let inline25 = @runtime(core.string_println)(inline24) in
                    jump loop2()
                  } in
                  if t8 {
                    let t9 = for_done1 = true in
                    jump k2()
                  } else {
                    let t10 = (for_index2 + 1) in
                    let t11 = for_index2 = t10 in
                    jump k2()
                  }
                } else {
                  jump exit2()
                }
              } in
            } in
            jump loop2()
          } in
          joinrec {
            join loop1() -> () {
              let t12 = (!for_done0) in
              if t12 {
                let for_item2 = for_index1 in
                let t13 = (for_index1 == for_limit1) in
                join k3() -> () {
                  let inline26 = trait_impl#ToString#isize#to_string(for_item2) in
                  let inline27 = @runtime(core.string_println)(inline26) in
                  jump loop1()
                } in
                if t13 {
                  let t14 = for_done0 = true in
                  jump k3()
                } else {
                  let t15 = (for_index1 + 1) in
                  let t16 = for_index1 = t15 in
                  jump k3()
                }
              } else {
                jump exit1()
              }
            } in
          } in
          jump loop1()
        } in
        let inline28 = 6 in
        let inline29 = inherent#Ref#Ref[T]#get__T_isize(calls/0) in
        let inline30 = (inline29 + 1) in
        let inline31 = inherent#Ref#Ref[T]#set__T_isize(calls/0, inline30) in
        jump inline_cont2(inline28)
      } in
      let inline32 = 4 in
      let inline33 = inherent#Ref#Ref[T]#get__T_isize(calls/0) in
      let inline34 = (inline33 + 1) in
      let inline35 = inherent#Ref#Ref[T]#set__T_isize(calls/0, inline34) in
      jump inline_cont1(inline32)
    } in
    let inline36 = 0 in
    let inline37 = @intrinsic(ref.new)(inline36) in
    jump inline_cont0(inline37)
  } in
  joinrec {
    join loop0() -> () {
      let t17 = (for_index0 < for_limit0) in
      if t17 {
        let for_item3 = for_index0 in
        let t18 = (for_index0 + 1) in
        let _wild1 = for_index0 = t18 in
        let inline38 = trait_impl#ToString#isize#to_string(for_item3) in
        let inline39 = @runtime(core.string_println)(inline38) in
        jump loop0()
      } else {
        jump exit0()
      }
    } in
  } in
  jump loop0()
}

fn inherent#Ref#Ref[T]#get__T_isize(self/0: Ref[isize]) -> isize {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_isize(self/0: Ref[isize], value/0: isize) -> () {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_isize(value/0: isize) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/0: isize) -> Ref[isize] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn __goml_builtin_range_inclusive(start/0: isize, end/0: isize) -> FnIterator__isize {
  let current/0 = @intrinsic(ref.new)(start/0) in
  let finished/0 = @intrinsic(ref.new)(false) in
  let t0 =
    closure_env_goml_builtin_range_inclusive_0 {
      finished_0: finished/0,
      current_1: current/0,
      end_2: end/0
    } in
  let t1 =
    pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
      t0
    ) in
  let inline0 = FnIterator__isize { next_fn: t1 } in
  inline0
}

fn trait_impl#Iterator#FnIterator__isize#next(self/0: FnIterator__isize) -> Option__isize {
  let t0 = FnIterator__isize.next_fn(self/0) in
  let t1 = t0() in
  t1
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn println__T_i32(value/0: i32) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int32_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
  next_fn/0: () -> Option__isize
) -> FnIterator__isize {
  let t0 = FnIterator__isize { next_fn: next_fn/0 } in
  t0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#isize#hash(self/0: isize) -> u64 {
  let t0 = @runtime(core.int_hash)(self/0) in
  t0
}

fn trait_impl#PartialEq#bool#eq(self/0: bool, other/0: bool) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#bool#hash(self/0: bool) -> u64 {
  if self/0 {
    1
  } else {
    0
  }
}

fn inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply(
  env0: closure_env_goml_builtin_range_inclusive_0
) -> Option__isize {
  let finished/0 = closure_env_goml_builtin_range_inclusive_0.finished_0(env0) in
  let current/0 = closure_env_goml_builtin_range_inclusive_0.current_1(env0) in
  let end/0 = closure_env_goml_builtin_range_inclusive_0.end_2(env0) in
  let t0 = @intrinsic(ref.get)(finished/0) in
  join k0(jp0: bool) -> Option__isize {
    if jp0 {
      Tag_0
    } else {
      let value/0 = @intrinsic(ref.get)(current/0) in
      let t1 = (value/0 == end/0) in
      join k1() -> Option__isize {
        let t2 = Option__isize::Some(value/0) in
        t2
      } in
      if t1 {
        let _wild0 = @intrinsic(ref.set)(finished/0, true) in
        jump k1()
      } else {
        let t3 = (value/0 + 1) in
        let _wild1 = @intrinsic(ref.set)(current/0, t3) in
        jump k1()
      }
    }
  } in
  if t0 {
    jump k0(true)
  } else {
    let t4 = @intrinsic(ref.get)(current/0) in
    let t5 = (t4 > end/0) in
    jump k0(t5)
  }
}