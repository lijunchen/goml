fn range(start/0: int32, end/1: int32) -> int32 {
  let t444 = (start/0 + end/1) in
  t444
}

fn endpoint(calls/2: Ref[int], value/3: int) -> int {
  join inline_cont548(t447: int) -> int {
    let t448 = (t447 + 1) in
    let inline547 = @intrinsic(ref.set)(calls/2, t448) in
    value/3
  } in
  let inline549 = @intrinsic(ref.get)(calls/2) in
  jump inline_cont548(inline549)
}

fn main() -> unit {
  let for_index409 = 1 in
  let for_limit410 = 4 in
  join exit450() -> unit {
    join inline_cont602(calls/5: Ref[int]) -> unit {
      join inline_cont597(for_index415: int) -> unit {
        join inline_cont592(for_limit416: int) -> unit {
          let for_done417 = (for_index415 > for_limit416) in
          join exit451() -> unit {
            let for_index422 = 3 in
            let for_limit423 = 1 in
            let for_done424 = (for_index422 > for_limit423) in
            join exit452() -> unit {
              let maximum/8 = 9223372036854775807 in
              let for_index429 = maximum/8 in
              let for_done431 = (for_index429 > maximum/8) in
              join exit453() -> unit {
                join inline_cont584(iterator/10: FnIterator__int) -> unit {
                  join inline_cont581(mtmp436: Option__int) -> unit {
                    join k454() -> unit {
                      join inline_cont579(t455: int) -> unit {
                        join inline_cont576(_wild439: unit) -> unit {
                          join inline_cont572(t456: int32) -> unit {
                            let inline570 = trait_impl#ToString#int32#to_string(t456) in
                            let inline571 = @runtime(core.string_println)(inline570) in
                            ()
                          } in
                          let inline573 = 10 in
                          let inline574 = 20 in
                          let inline575 = (inline573 + inline574) in
                          jump inline_cont572(inline575)
                        } in
                        let inline577 = trait_impl#ToString#int#to_string(t455) in
                        let inline578 = @runtime(core.string_println)(inline577) in
                        jump inline_cont576(())
                      } in
                      let inline580 = @intrinsic(ref.get)(calls/5) in
                      jump inline_cont579(inline580)
                    } in
                    match mtmp436 {
                      Tag_0 => {
                        let inline563 = "missing" in
                        let inline564 = trait_impl#ToString#string#to_string(inline563) in
                        let inline565 = @runtime(core.string_println)(inline564) in
                        jump k454()
                      },
                      Tag_1 => {
                        let x437 = Option__int::Some._0(mtmp436) in
                        let inline567 = trait_impl#ToString#int#to_string(x437) in
                        let inline568 = @runtime(core.string_println)(inline567) in
                        jump k454()
                      },
                    }
                  } in
                  let inline582 = FnIterator__int.next_fn(iterator/10) in
                  let inline583 = inline582() in
                  jump inline_cont581(inline583)
                } in
                let inline585 = 8 in
                let inline586 = 8 in
                let inline587 = @intrinsic(ref.new)(inline585) in
                let inline588 = @intrinsic(ref.new)(false) in
                let inline589 =
                  closure_env_goml_builtin_range_inclusive_0 {
                    finished_0: inline588,
                    current_1: inline587,
                    end_2: inline586
                  } in
                let inline590 =
                  pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
                    inline589
                  ) in
                let inline591 = inherent#FnIterator#FnIterator[T]#from_fn__T_int(inline590) in
                jump inline_cont584(inline591)
              } in
              joinrec {
                join loop459() -> unit {
                  let t460 = (!for_done431) in
                  if t460 {
                    let for_item432 = for_index429 in
                    let t462 = (for_index429 == maximum/8) in
                    join k461() -> unit {
                      let inline560 = trait_impl#ToString#int#to_string(for_item432) in
                      let inline561 = @runtime(core.string_println)(inline560) in
                      jump loop459()
                    } in
                    if t462 {
                      let t463 = for_done431 = true in
                      jump k461()
                    } else {
                      let t464 = (for_index429 + 1) in
                      let t465 = for_index429 = t464 in
                      jump k461()
                    }
                  } else {
                    jump exit453()
                  }
                } in
              } in
              jump loop459()
            } in
            joinrec {
              join loop466() -> unit {
                let t467 = (!for_done424) in
                if t467 {
                  let for_item425 = for_index422 in
                  let t469 = (for_index422 == for_limit423) in
                  join k468() -> unit {
                    let inline557 = trait_impl#ToString#int#to_string(for_item425) in
                    let inline558 = @runtime(core.string_println)(inline557) in
                    jump loop466()
                  } in
                  if t469 {
                    let t470 = for_done424 = true in
                    jump k468()
                  } else {
                    let t471 = (for_index422 + 1) in
                    let t472 = for_index422 = t471 in
                    jump k468()
                  }
                } else {
                  jump exit452()
                }
              } in
            } in
            jump loop466()
          } in
          joinrec {
            join loop473() -> unit {
              let t474 = (!for_done417) in
              if t474 {
                let for_item418 = for_index415 in
                let t476 = (for_index415 == for_limit416) in
                join k475() -> unit {
                  let inline554 = trait_impl#ToString#int#to_string(for_item418) in
                  let inline555 = @runtime(core.string_println)(inline554) in
                  jump loop473()
                } in
                if t476 {
                  let t477 = for_done417 = true in
                  jump k475()
                } else {
                  let t478 = (for_index415 + 1) in
                  let t479 = for_index415 = t478 in
                  jump k475()
                }
              } else {
                jump exit451()
              }
            } in
          } in
          jump loop473()
        } in
        let inline593 = 6 in
        let inline594 = inherent#Ref#Ref[T]#get__T_int(calls/5) in
        let inline595 = (inline594 + 1) in
        let inline596 = inherent#Ref#Ref[T]#set__T_int(calls/5, inline595) in
        jump inline_cont592(inline593)
      } in
      let inline598 = 4 in
      let inline599 = inherent#Ref#Ref[T]#get__T_int(calls/5) in
      let inline600 = (inline599 + 1) in
      let inline601 = inherent#Ref#Ref[T]#set__T_int(calls/5, inline600) in
      jump inline_cont597(inline598)
    } in
    let inline603 = 0 in
    let inline604 = @intrinsic(ref.new)(inline603) in
    jump inline_cont602(inline604)
  } in
  joinrec {
    join loop480() -> unit {
      let t481 = (for_index409 < for_limit410) in
      if t481 {
        let for_item411 = for_index409 in
        let t482 = (for_index409 + 1) in
        let _wild412 = for_index409 = t482 in
        let inline551 = trait_impl#ToString#int#to_string(for_item411) in
        let inline552 = @runtime(core.string_println)(inline551) in
        jump loop480()
      } else {
        jump exit450()
      }
    } in
  } in
  jump loop480()
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t485 = @intrinsic(ref.get)(self/432) in
  t485
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t487 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont605(t489: string) -> unit {
    let t490 = @runtime(core.string_println)(t489) in
    ()
  } in
  let inline606 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont605(inline606)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t493 = @intrinsic(ref.new)(value/431) in
  t493
}

fn __goml_builtin_range_inclusive(start/498: int, end/499: int) -> FnIterator__int {
  let current/500 = @intrinsic(ref.new)(start/498) in
  let finished/501 = @intrinsic(ref.new)(false) in
  let t496 =
    closure_env_goml_builtin_range_inclusive_0 {
      finished_0: finished/501,
      current_1: current/500,
      end_2: end/499
    } in
  let t497 =
    pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
      t496
    ) in
  let inline608 = FnIterator__int { next_fn: t497 } in
  inline608
}

fn trait_impl#Iterator#FnIterator__int#next(self/255: FnIterator__int) -> Option__int {
  let t501 = FnIterator__int.next_fn(self/255) in
  let t502 = t501() in
  t502
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont609(t504: string) -> unit {
    let t505 = @runtime(core.string_println)(t504) in
    ()
  } in
  jump inline_cont609(value/1)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont610(t507: string) -> unit {
    let t508 = @runtime(core.string_println)(t507) in
    ()
  } in
  let inline611 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont610(inline611)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t511 = @runtime(core.int_to_string)(self/151) in
  t511
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_int(
  next_fn/254: () -> Option__int
) -> FnIterator__int {
  let t514 = FnIterator__int { next_fn: next_fn/254 } in
  t514
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t519 = @runtime(core.int32_to_string)(self/154) in
  t519
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t522 = (self/185 == other/186) in
  t522
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t525 = @runtime(core.int_hash)(self/211) in
  t525
}

fn trait_impl#PartialEq#bool#eq(self/179: bool, other/180: bool) -> bool {
  let t528 = (self/179 == other/180) in
  t528
}

fn trait_impl#Hash#bool#hash(self/208: bool) -> uint64 {
  if self/208 {
    1
  } else {
    0
  }
}

fn inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply(
  env441: closure_env_goml_builtin_range_inclusive_0
) -> Option__int {
  let finished/501 = closure_env_goml_builtin_range_inclusive_0.finished_0(env441) in
  let current/500 = closure_env_goml_builtin_range_inclusive_0.current_1(env441) in
  let end/499 = closure_env_goml_builtin_range_inclusive_0.end_2(env441) in
  let t543 = @intrinsic(ref.get)(finished/501) in
  join k537(jp538: bool) -> Option__int {
    if jp538 {
      Tag_0
    } else {
      let value/502 = @intrinsic(ref.get)(current/500) in
      let t541 = (value/502 == end/499) in
      join k539() -> Option__int {
        let t540 = Option__int::Some(value/502) in
        t540
      } in
      if t541 {
        let _wild405 = @intrinsic(ref.set)(finished/501, true) in
        jump k539()
      } else {
        let t542 = (value/502 + 1) in
        let _wild406 = @intrinsic(ref.set)(current/500, t542) in
        jump k539()
      }
    }
  } in
  if t543 {
    jump k537(true)
  } else {
    let t544 = @intrinsic(ref.get)(current/500) in
    let t545 = (t544 > end/499) in
    jump k537(t545)
  }
}