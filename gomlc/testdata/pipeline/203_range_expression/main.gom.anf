fn range(start/0: i32, end/1: i32) -> i32 {
  let t832 = (start/0 + end/1) in
  t832
}

fn endpoint(calls/2: Ref[isize], value/3: isize) -> isize {
  join inline_cont983(t835: isize) -> isize {
    let t836 = (t835 + 1) in
    let inline982 = @intrinsic(ref.set)(calls/2, t836) in
    value/3
  } in
  let inline984 = @intrinsic(ref.get)(calls/2) in
  jump inline_cont983(inline984)
}

fn main() -> unit {
  let for_index797 = 1 in
  let for_limit798 = 4 in
  join exit838() -> unit {
    join inline_cont1037(calls/5: Ref[isize]) -> unit {
      join inline_cont1032(for_index803: isize) -> unit {
        join inline_cont1027(for_limit804: isize) -> unit {
          let for_done805 = (for_index803 > for_limit804) in
          join exit839() -> unit {
            let for_index810 = 3 in
            let for_limit811 = 1 in
            let for_done812 = (for_index810 > for_limit811) in
            join exit840() -> unit {
              let maximum/8 = 9223372036854775807 in
              let for_index817 = maximum/8 in
              let for_done819 = (for_index817 > maximum/8) in
              join exit841() -> unit {
                join inline_cont1019(iterator/10: FnIterator__isize) -> unit {
                  join inline_cont1016(mtmp824: Option__isize) -> unit {
                    join k842() -> unit {
                      join inline_cont1014(t843: isize) -> unit {
                        join inline_cont1011(_wild827: unit) -> unit {
                          join inline_cont1007(t844: i32) -> unit {
                            let inline1005 = trait_impl#ToString#i32#to_string(t844) in
                            let inline1006 = @runtime(core.string_println)(inline1005) in
                            ()
                          } in
                          let inline1008 = 10 in
                          let inline1009 = 20 in
                          let inline1010 = (inline1008 + inline1009) in
                          jump inline_cont1007(inline1010)
                        } in
                        let inline1012 = trait_impl#ToString#isize#to_string(t843) in
                        let inline1013 = @runtime(core.string_println)(inline1012) in
                        jump inline_cont1011(())
                      } in
                      let inline1015 = @intrinsic(ref.get)(calls/5) in
                      jump inline_cont1014(inline1015)
                    } in
                    match mtmp824 {
                      Tag_0 => {
                        let inline998 = "missing" in
                        let inline999 = trait_impl#ToString#string#to_string(inline998) in
                        let inline1000 = @runtime(core.string_println)(inline999) in
                        jump k842()
                      },
                      Tag_1 => {
                        let x825 = Option__isize::Some._0(mtmp824) in
                        let inline1002 = trait_impl#ToString#isize#to_string(x825) in
                        let inline1003 = @runtime(core.string_println)(inline1002) in
                        jump k842()
                      },
                    }
                  } in
                  let inline1017 = FnIterator__isize.next_fn(iterator/10) in
                  let inline1018 = inline1017() in
                  jump inline_cont1016(inline1018)
                } in
                let inline1020 = 8 in
                let inline1021 = 8 in
                let inline1022 = @intrinsic(ref.new)(inline1020) in
                let inline1023 = @intrinsic(ref.new)(false) in
                let inline1024 =
                  closure_env_goml_builtin_range_inclusive_0 {
                    finished_0: inline1023,
                    current_1: inline1022,
                    end_2: inline1021
                  } in
                let inline1025 =
                  pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
                    inline1024
                  ) in
                let inline1026 = inherent#FnIterator#FnIterator[T]#from_fn__T_isize(inline1025) in
                jump inline_cont1019(inline1026)
              } in
              joinrec {
                join loop847() -> unit {
                  let t848 = (!for_done819) in
                  if t848 {
                    let for_item820 = for_index817 in
                    let t850 = (for_index817 == maximum/8) in
                    join k849() -> unit {
                      let inline995 = trait_impl#ToString#isize#to_string(for_item820) in
                      let inline996 = @runtime(core.string_println)(inline995) in
                      jump loop847()
                    } in
                    if t850 {
                      let t851 = for_done819 = true in
                      jump k849()
                    } else {
                      let t852 = (for_index817 + 1) in
                      let t853 = for_index817 = t852 in
                      jump k849()
                    }
                  } else {
                    jump exit841()
                  }
                } in
              } in
              jump loop847()
            } in
            joinrec {
              join loop854() -> unit {
                let t855 = (!for_done812) in
                if t855 {
                  let for_item813 = for_index810 in
                  let t857 = (for_index810 == for_limit811) in
                  join k856() -> unit {
                    let inline992 = trait_impl#ToString#isize#to_string(for_item813) in
                    let inline993 = @runtime(core.string_println)(inline992) in
                    jump loop854()
                  } in
                  if t857 {
                    let t858 = for_done812 = true in
                    jump k856()
                  } else {
                    let t859 = (for_index810 + 1) in
                    let t860 = for_index810 = t859 in
                    jump k856()
                  }
                } else {
                  jump exit840()
                }
              } in
            } in
            jump loop854()
          } in
          joinrec {
            join loop861() -> unit {
              let t862 = (!for_done805) in
              if t862 {
                let for_item806 = for_index803 in
                let t864 = (for_index803 == for_limit804) in
                join k863() -> unit {
                  let inline989 = trait_impl#ToString#isize#to_string(for_item806) in
                  let inline990 = @runtime(core.string_println)(inline989) in
                  jump loop861()
                } in
                if t864 {
                  let t865 = for_done805 = true in
                  jump k863()
                } else {
                  let t866 = (for_index803 + 1) in
                  let t867 = for_index803 = t866 in
                  jump k863()
                }
              } else {
                jump exit839()
              }
            } in
          } in
          jump loop861()
        } in
        let inline1028 = 6 in
        let inline1029 = inherent#Ref#Ref[T]#get__T_isize(calls/5) in
        let inline1030 = (inline1029 + 1) in
        let inline1031 = inherent#Ref#Ref[T]#set__T_isize(calls/5, inline1030) in
        jump inline_cont1027(inline1028)
      } in
      let inline1033 = 4 in
      let inline1034 = inherent#Ref#Ref[T]#get__T_isize(calls/5) in
      let inline1035 = (inline1034 + 1) in
      let inline1036 = inherent#Ref#Ref[T]#set__T_isize(calls/5, inline1035) in
      jump inline_cont1032(inline1033)
    } in
    let inline1038 = 0 in
    let inline1039 = @intrinsic(ref.new)(inline1038) in
    jump inline_cont1037(inline1039)
  } in
  joinrec {
    join loop868() -> unit {
      let t869 = (for_index797 < for_limit798) in
      if t869 {
        let for_item799 = for_index797 in
        let t870 = (for_index797 + 1) in
        let _wild800 = for_index797 = t870 in
        let inline986 = trait_impl#ToString#isize#to_string(for_item799) in
        let inline987 = @runtime(core.string_println)(inline986) in
        jump loop868()
      } else {
        jump exit838()
      }
    } in
  } in
  jump loop868()
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t873 = @intrinsic(ref.get)(self/685) in
  t873
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t875 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont1040(t877: string) -> unit {
    let t878 = @runtime(core.string_println)(t877) in
    ()
  } in
  let inline1041 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont1040(inline1041)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t881 = @intrinsic(ref.new)(value/684) in
  t881
}

fn __goml_builtin_range_inclusive(start/760: isize, end/761: isize) -> FnIterator__isize {
  let current/762 = @intrinsic(ref.new)(start/760) in
  let finished/763 = @intrinsic(ref.new)(false) in
  let t884 =
    closure_env_goml_builtin_range_inclusive_0 {
      finished_0: finished/763,
      current_1: current/762,
      end_2: end/761
    } in
  let t885 =
    pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
      t884
    ) in
  let inline1043 = FnIterator__isize { next_fn: t885 } in
  inline1043
}

fn trait_impl#Iterator#FnIterator__isize#next(self/508: FnIterator__isize) -> Option__isize {
  let t889 = FnIterator__isize.next_fn(self/508) in
  let t890 = t889() in
  t890
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont1044(t892: string) -> unit {
    let t893 = @runtime(core.string_println)(t892) in
    ()
  } in
  jump inline_cont1044(value/1)
}

fn println__T_i32(value/1: i32) -> unit {
  join inline_cont1045(t895: string) -> unit {
    let t896 = @runtime(core.string_println)(t895) in
    ()
  } in
  let inline1046 = __goml_builtin_int32_to_string(value/1) in
  jump inline_cont1045(inline1046)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline1048 = (self/404 as i64) in
  let inline1049 = signed_decimal_string(inline1048) in
  inline1049
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_isize(
  next_fn/507: () -> Option__isize
) -> FnIterator__isize {
  let t902 = FnIterator__isize { next_fn: next_fn/507 } in
  t902
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn trait_impl#ToString#i32#to_string(self/407: i32) -> string {
  let inline1051 = (self/407 as i64) in
  let inline1052 = signed_decimal_string(inline1051) in
  inline1052
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t910 = (value/222 as i64) in
  let inline1054 = (t910 < 0) in
  if inline1054 {
    let inline1055 = (t910 as u64) in
    let inline1056 = (0 - inline1055) in
    let inline1057 = decimal_string(inline1056) in
    let inline1058 = ("-" + inline1057) in
    inline1058
  } else {
    let inline1059 = (t910 as u64) in
    let inline1060 = decimal_string(inline1059) in
    inline1060
  }
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t914 = (value/225 as i64) in
  let inline1062 = (t914 < 0) in
  if inline1062 {
    let inline1063 = (t914 as u64) in
    let inline1064 = (0 - inline1063) in
    let inline1065 = decimal_string(inline1064) in
    let inline1066 = ("-" + inline1065) in
    inline1066
  } else {
    let inline1067 = (t914 as u64) in
    let inline1068 = decimal_string(inline1067) in
    inline1068
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t920 = (value/214 < 0) in
  if t920 {
    let t921 = (value/214 as u64) in
    let t922 = (0 - t921) in
    let t923 = decimal_string(t922) in
    let t924 = ("-" + t923) in
    t924
  } else {
    let t925 = (value/214 as u64) in
    let t926 = decimal_string(t925) in
    t926
  }
}

fn decimal_string(value/208: u64) -> string {
  let t949 = (value/208 == 0) in
  join k929() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit930() -> string {
      join inline_cont1077(t931: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t931) in
        let offset/212 = 0 in
        join exit932() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop933() -> string {
            join inline_cont1075(t934: isize) -> string {
              let t935 = (offset/212 < t934) in
              if t935 {
                join inline_cont1073(t936: isize) -> string {
                  let t937 = (t936 - offset/212) in
                  let t938 = (t937 - 1) in
                  let t939 = @intrinsic(vec.get)(reversed/209, t938) in
                  join inline_cont1071(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t940 = (compound_old358 + compound_value359) in
                    let t941 = offset/212 = t940 in
                    jump loop933()
                  } in
                  let inline1072 = @intrinsic(vec.push)(bytes/211, t939) in
                  jump inline_cont1071(())
                } in
                let inline1074 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont1073(inline1074)
              } else {
                jump exit932()
              }
            } in
            let inline1076 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont1075(inline1076)
          } in
        } in
        jump loop933()
      } in
      let inline1078 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont1077(inline1078)
    } in
    joinrec {
      join loop942() -> string {
        let t943 = (remaining/210 > 0) in
        if t943 {
          let t944 = (remaining/210 % 10) in
          let t945 = (t944 as u8) in
          let t946 = (t945 + 48) in
          join inline_cont1069(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t947 = (compound_old353 / compound_value354) in
            let t948 = remaining/210 = t947 in
            jump loop942()
          } in
          let inline1070 = @intrinsic(vec.push)(reversed/209, t946) in
          jump inline_cont1069(())
        } else {
          jump exit930()
        }
      } in
    } in
    jump loop942()
  } in
  if t949 {
    "0"
  } else {
    jump k929()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t951 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t954 = @intrinsic(vec.len)(self/526) in
  t954
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t957 = (self/438 == other/439) in
  t957
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t960 = @runtime(core.int_hash)(self/464) in
  t960
}

fn trait_impl#PartialEq#bool#eq(self/432: bool, other/433: bool) -> bool {
  let t963 = (self/432 == other/433) in
  t963
}

fn trait_impl#Hash#bool#hash(self/461: bool) -> u64 {
  if self/461 {
    1
  } else {
    0
  }
}

fn inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply(
  env829: closure_env_goml_builtin_range_inclusive_0
) -> Option__isize {
  let finished/763 = closure_env_goml_builtin_range_inclusive_0.finished_0(env829) in
  let current/762 = closure_env_goml_builtin_range_inclusive_0.current_1(env829) in
  let end/761 = closure_env_goml_builtin_range_inclusive_0.end_2(env829) in
  let t978 = @intrinsic(ref.get)(finished/763) in
  join k972(jp973: bool) -> Option__isize {
    if jp973 {
      Tag_0
    } else {
      let value/764 = @intrinsic(ref.get)(current/762) in
      let t976 = (value/764 == end/761) in
      join k974() -> Option__isize {
        let t975 = Option__isize::Some(value/764) in
        t975
      } in
      if t976 {
        let _wild793 = @intrinsic(ref.set)(finished/763, true) in
        jump k974()
      } else {
        let t977 = (value/764 + 1) in
        let _wild794 = @intrinsic(ref.set)(current/762, t977) in
        jump k974()
      }
    }
  } in
  if t978 {
    jump k972(true)
  } else {
    let t979 = @intrinsic(ref.get)(current/762) in
    let t980 = (t979 > end/761) in
    jump k972(t980)
  }
}