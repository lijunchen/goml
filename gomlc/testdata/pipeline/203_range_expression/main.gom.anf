fn range(start/0: int32, end/1: int32) -> int32 {
  let t447 = (start/0 + end/1) in
  t447
}

fn endpoint(calls/2: Ref[int], value/3: int) -> int {
  join inline_cont551(t450: int) -> int {
    let t451 = (t450 + 1) in
    let inline550 = @intrinsic(ref.set)(calls/2, t451) in
    value/3
  } in
  let inline552 = @intrinsic(ref.get)(calls/2) in
  jump inline_cont551(inline552)
}

fn main() -> unit {
  let for_index412 = 1 in
  let for_limit413 = 4 in
  join exit453() -> unit {
    join inline_cont605(calls/5: Ref[int]) -> unit {
      join inline_cont600(for_index418: int) -> unit {
        join inline_cont595(for_limit419: int) -> unit {
          let for_done420 = (for_index418 > for_limit419) in
          join exit454() -> unit {
            let for_index425 = 3 in
            let for_limit426 = 1 in
            let for_done427 = (for_index425 > for_limit426) in
            join exit455() -> unit {
              let maximum/8 = 9223372036854775807 in
              let for_index432 = maximum/8 in
              let for_done434 = (for_index432 > maximum/8) in
              join exit456() -> unit {
                join inline_cont587(iterator/10: FnIterator__int) -> unit {
                  join inline_cont584(mtmp439: Option__int) -> unit {
                    join k457() -> unit {
                      join inline_cont582(t458: int) -> unit {
                        join inline_cont579(_wild442: unit) -> unit {
                          join inline_cont575(t459: int32) -> unit {
                            let inline573 = trait_impl#ToString#int32#to_string(t459) in
                            let inline574 = @runtime(core.string_println)(inline573) in
                            ()
                          } in
                          let inline576 = 10 in
                          let inline577 = 20 in
                          let inline578 = (inline576 + inline577) in
                          jump inline_cont575(inline578)
                        } in
                        let inline580 = trait_impl#ToString#int#to_string(t458) in
                        let inline581 = @runtime(core.string_println)(inline580) in
                        jump inline_cont579(())
                      } in
                      let inline583 = @intrinsic(ref.get)(calls/5) in
                      jump inline_cont582(inline583)
                    } in
                    match mtmp439 {
                      Tag_0 => {
                        let inline566 = "missing" in
                        let inline567 = trait_impl#ToString#string#to_string(inline566) in
                        let inline568 = @runtime(core.string_println)(inline567) in
                        jump k457()
                      },
                      Tag_1 => {
                        let x440 = Option__int::Some._0(mtmp439) in
                        let inline570 = trait_impl#ToString#int#to_string(x440) in
                        let inline571 = @runtime(core.string_println)(inline570) in
                        jump k457()
                      },
                    }
                  } in
                  let inline585 = FnIterator__int.next_fn(iterator/10) in
                  let inline586 = inline585() in
                  jump inline_cont584(inline586)
                } in
                let inline588 = 8 in
                let inline589 = 8 in
                let inline590 = @intrinsic(ref.new)(inline588) in
                let inline591 = @intrinsic(ref.new)(false) in
                let inline592 =
                  closure_env_goml_builtin_range_inclusive_0 {
                    finished_0: inline591,
                    current_1: inline590,
                    end_2: inline589
                  } in
                let inline593 =
                  pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
                    inline592
                  ) in
                let inline594 = inherent#FnIterator#FnIterator[T]#from_fn__T_int(inline593) in
                jump inline_cont587(inline594)
              } in
              joinrec {
                join loop462() -> unit {
                  let t463 = (!for_done434) in
                  if t463 {
                    let for_item435 = for_index432 in
                    let t465 = (for_index432 == maximum/8) in
                    join k464() -> unit {
                      let inline563 = trait_impl#ToString#int#to_string(for_item435) in
                      let inline564 = @runtime(core.string_println)(inline563) in
                      jump loop462()
                    } in
                    if t465 {
                      let t466 = for_done434 = true in
                      jump k464()
                    } else {
                      let t467 = (for_index432 + 1) in
                      let t468 = for_index432 = t467 in
                      jump k464()
                    }
                  } else {
                    jump exit456()
                  }
                } in
              } in
              jump loop462()
            } in
            joinrec {
              join loop469() -> unit {
                let t470 = (!for_done427) in
                if t470 {
                  let for_item428 = for_index425 in
                  let t472 = (for_index425 == for_limit426) in
                  join k471() -> unit {
                    let inline560 = trait_impl#ToString#int#to_string(for_item428) in
                    let inline561 = @runtime(core.string_println)(inline560) in
                    jump loop469()
                  } in
                  if t472 {
                    let t473 = for_done427 = true in
                    jump k471()
                  } else {
                    let t474 = (for_index425 + 1) in
                    let t475 = for_index425 = t474 in
                    jump k471()
                  }
                } else {
                  jump exit455()
                }
              } in
            } in
            jump loop469()
          } in
          joinrec {
            join loop476() -> unit {
              let t477 = (!for_done420) in
              if t477 {
                let for_item421 = for_index418 in
                let t479 = (for_index418 == for_limit419) in
                join k478() -> unit {
                  let inline557 = trait_impl#ToString#int#to_string(for_item421) in
                  let inline558 = @runtime(core.string_println)(inline557) in
                  jump loop476()
                } in
                if t479 {
                  let t480 = for_done420 = true in
                  jump k478()
                } else {
                  let t481 = (for_index418 + 1) in
                  let t482 = for_index418 = t481 in
                  jump k478()
                }
              } else {
                jump exit454()
              }
            } in
          } in
          jump loop476()
        } in
        let inline596 = 6 in
        let inline597 = inherent#Ref#Ref[T]#get__T_int(calls/5) in
        let inline598 = (inline597 + 1) in
        let inline599 = inherent#Ref#Ref[T]#set__T_int(calls/5, inline598) in
        jump inline_cont595(inline596)
      } in
      let inline601 = 4 in
      let inline602 = inherent#Ref#Ref[T]#get__T_int(calls/5) in
      let inline603 = (inline602 + 1) in
      let inline604 = inherent#Ref#Ref[T]#set__T_int(calls/5, inline603) in
      jump inline_cont600(inline601)
    } in
    let inline606 = 0 in
    let inline607 = @intrinsic(ref.new)(inline606) in
    jump inline_cont605(inline607)
  } in
  joinrec {
    join loop483() -> unit {
      let t484 = (for_index412 < for_limit413) in
      if t484 {
        let for_item414 = for_index412 in
        let t485 = (for_index412 + 1) in
        let _wild415 = for_index412 = t485 in
        let inline554 = trait_impl#ToString#int#to_string(for_item414) in
        let inline555 = @runtime(core.string_println)(inline554) in
        jump loop483()
      } else {
        jump exit453()
      }
    } in
  } in
  jump loop483()
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t488 = @intrinsic(ref.get)(self/432) in
  t488
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t490 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont608(t492: string) -> unit {
    let t493 = @runtime(core.string_println)(t492) in
    ()
  } in
  let inline609 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont608(inline609)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t496 = @intrinsic(ref.new)(value/431) in
  t496
}

fn __goml_builtin_range_inclusive(start/507: int, end/508: int) -> FnIterator__int {
  let current/509 = @intrinsic(ref.new)(start/507) in
  let finished/510 = @intrinsic(ref.new)(false) in
  let t499 =
    closure_env_goml_builtin_range_inclusive_0 {
      finished_0: finished/510,
      current_1: current/509,
      end_2: end/508
    } in
  let t500 =
    pack_closure[inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply](
      t499
    ) in
  let inline611 = FnIterator__int { next_fn: t500 } in
  inline611
}

fn trait_impl#Iterator#FnIterator__int#next(self/255: FnIterator__int) -> Option__int {
  let t504 = FnIterator__int.next_fn(self/255) in
  let t505 = t504() in
  t505
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont612(t507: string) -> unit {
    let t508 = @runtime(core.string_println)(t507) in
    ()
  } in
  jump inline_cont612(value/1)
}

fn println__T_int32(value/1: int32) -> unit {
  join inline_cont613(t510: string) -> unit {
    let t511 = @runtime(core.string_println)(t510) in
    ()
  } in
  let inline614 = @runtime(core.int32_to_string)(value/1) in
  jump inline_cont613(inline614)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t514 = @runtime(core.int_to_string)(self/151) in
  t514
}

fn inherent#FnIterator#FnIterator[T]#from_fn__T_int(
  next_fn/254: () -> Option__int
) -> FnIterator__int {
  let t517 = FnIterator__int { next_fn: next_fn/254 } in
  t517
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  let t522 = @runtime(core.int32_to_string)(self/154) in
  t522
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t525 = (self/185 == other/186) in
  t525
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t528 = @runtime(core.int_hash)(self/211) in
  t528
}

fn trait_impl#PartialEq#bool#eq(self/179: bool, other/180: bool) -> bool {
  let t531 = (self/179 == other/180) in
  t531
}

fn trait_impl#Hash#bool#hash(self/208: bool) -> uint64 {
  if self/208 {
    1
  } else {
    0
  }
}

fn inherent#closure_env_goml_builtin_range_inclusive_0#closure_env_goml_builtin_range_inclusive_0#apply(
  env444: closure_env_goml_builtin_range_inclusive_0
) -> Option__int {
  let finished/510 = closure_env_goml_builtin_range_inclusive_0.finished_0(env444) in
  let current/509 = closure_env_goml_builtin_range_inclusive_0.current_1(env444) in
  let end/508 = closure_env_goml_builtin_range_inclusive_0.end_2(env444) in
  let t546 = @intrinsic(ref.get)(finished/510) in
  join k540(jp541: bool) -> Option__int {
    if jp541 {
      Tag_0
    } else {
      let value/511 = @intrinsic(ref.get)(current/509) in
      let t544 = (value/511 == end/508) in
      join k542() -> Option__int {
        let t543 = Option__int::Some(value/511) in
        t543
      } in
      if t544 {
        let _wild408 = @intrinsic(ref.set)(finished/510, true) in
        jump k542()
      } else {
        let t545 = (value/511 + 1) in
        let _wild409 = @intrinsic(ref.set)(current/509, t545) in
        jump k542()
      }
    }
  } in
  if t546 {
    jump k540(true)
  } else {
    let t547 = @intrinsic(ref.get)(current/509) in
    let t548 = (t547 > end/508) in
    jump k540(t548)
  }
}