fn point32_to_string(p/0: Point) -> string {
  join ret163(retv164: string) -> string {
    retv164
  } in
  let mtmp152 = p/0 in
  let x153 = Point.x(mtmp152) in
  let x154 = Point.y(mtmp152) in
  let y/2 = x154 in
  let x/1 = x153 in
  let t165 = inherent#int32#int32#to_string(x/1) in
  let t166 = ("Point { x: " + t165) in
  let t167 = (t166 + ", y: ") in
  let t168 = inherent#int32#int32#to_string(y/2) in
  let t169 = (t167 + t168) in
  let t170 = (t169 + " }") in
  jump ret163(t170)
}

fn color_to_string(c/3: Color) -> string {
  join ret171(retv172: string) -> string {
    retv172
  } in
  join k173(jp174: string) -> string {
    jump ret171(jp174)
  } in
  match c/3 {
    Tag_0 => {
      jump k173("Red")
    },
    Tag_1 => {
      jump k173("Green")
    },
    Tag_2 => {
      jump k173("Blue")
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  join ret175(retv176: string) -> string {
    retv176
  } in
  let mtmp155 = l/4 in
  let x156 = Line.from(mtmp155) in
  let x157 = Line.to(mtmp155) in
  let x158 = Line.color(mtmp155) in
  let color/7 = x158 in
  let to/6 = x157 in
  let from/5 = x156 in
  let t177 = point32_to_string(from/5) in
  let t178 = ("Line { from: " + t177) in
  let t179 = (t178 + ", to: ") in
  let t180 = point32_to_string(to/6) in
  let t181 = (t179 + t180) in
  let t182 = (t181 + ", color: ") in
  let t183 = color_to_string(color/7) in
  let t184 = (t182 + t183) in
  let t185 = (t184 + " }") in
  jump ret175(t185)
}

fn point_type(p/8: Point) -> string {
  join ret186(retv187: string) -> string {
    retv187
  } in
  let x159 = Point.x(p/8) in
  let x160 = Point.y(p/8) in
  join k188(jp189: string) -> string {
    jump ret186(jp189)
  } in
  match x159 {
    0 => {
      join k190(jp191: string) -> string {
        jump k188(jp191)
      } in
      match x160 {
        0 => {
          jump k190("origin")
        },
        1 => {
          jump k190("up")
        },
        _ => {
          let y/9 = x160 in
          let mtmp161 = (0 < y/9) in
          join k192(jp193: string) -> string {
            jump k190(jp193)
          } in
          match mtmp161 {
            true => {
              jump k192("above")
            },
            false => {
              jump k192("below")
            },
          }
        },
      }
    },
    1 => {
      join k194(jp195: string) -> string {
        jump k188(jp195)
      } in
      match x160 {
        0 => {
          jump k194("right")
        },
        _ => {
          jump k194("unknown")
        },
      }
    },
    _ => {
      jump k188("unknown")
    },
  }
}

fn main() -> unit {
  join ret196() -> unit {
    ()
  } in
  let p0/10 = Point { x: 0, y: 0 } in
  let t197 = point_type(p0/10) in
  let mtmp162 = println__T_string(t197) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Tag_0 } in
  let t198 = line_to_string(line/12) in
  let t199 = println__T_string(t198) in
  jump ret196()
}

fn inherent#int32#int32#to_string(self/6: int32) -> string {
  join ret200(retv201: string) -> string {
    retv201
  } in
  let t202 = @runtime(core.int32_to_string)(self/6) in
  jump ret200(t202)
}

fn println__T_string(value/1: string) -> unit {
  join ret203() -> unit {
    ()
  } in
  let t204 = trait_impl#ToString#string#to_string(value/1) in
  let t205 = @runtime(core.string_println)(t204) in
  jump ret203()
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret206(retv207: string) -> string {
    retv207
  } in
  jump ret206(self/38)
}