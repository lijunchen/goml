fn point32_to_string(p/0: Point) -> string {
  let mtmp411 = p/0 in
  let x412 = Point.x(mtmp411) in
  let x413 = Point.y(mtmp411) in
  let y/2 = x413 in
  let x/1 = x412 in
  (((("Point { x: " + inherent#int32#int32#to_string(x/1)) + ", y: ") +
    inherent#int32#int32#to_string(y/2)) +
    " }")
}

fn color_to_string(c/3: Color) -> string {
  match c/3 {
    Color::Red => {
      "Red"
    },
    Color::Green => {
      "Green"
    },
    Color::Blue => {
      "Blue"
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  let mtmp414 = l/4 in
  let x415 = Line.from(mtmp414) in
  let x416 = Line.to(mtmp414) in
  let x417 = Line.color(mtmp414) in
  let color/7 = x417 in
  let to/6 = x416 in
  let from/5 = x415 in
  (((((("Line { from: " + point32_to_string(from/5)) + ", to: ") + point32_to_string(to/6)) +
    ", color: ") +
    color_to_string(color/7)) +
    " }")
}

fn point_type(p/8: Point) -> string {
  let x418 = Point.x(p/8) in
  let x419 = Point.y(p/8) in
  match x418 {
    0 => {
      match x419 {
        0 => {
          "origin"
        },
        1 => {
          "up"
        },
        _ =>
          let y/9 = x419 in
          let mtmp420 = (0 < y/9) in
          match mtmp420 {
            true => {
              "above"
            },
            false => {
              "below"
            },
          },
      }
    },
    1 => {
      match x419 {
        0 => {
          "right"
        },
        _ => "unknown",
      }
    },
    _ => "unknown",
  }
}

fn main() -> unit {
  let p0/10 = Point { x: 0, y: 0 } in
  let mtmp421 = println__T_string(point_type(p0/10)) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Color::Red } in
  println__T_string(line_to_string(line/12))
}

fn inherent#int32#int32#to_string(self/33: int32) -> string {
  @runtime(core.int32_to_string)(self/33)
}

fn println__T_string(value/1: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/1))
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}