fn main() -> unit {
    let x/0: i32 = 1;
    let y/1: i8 = 1;
    let _ : unit = (print : (string) -> unit)("int32: ");
    let _ : unit = (println : (i32) -> unit)((x/0 : i32));
    let _ : unit = (print : (string) -> unit)("int8: ");
    let _ : unit = (println : (i8) -> unit)((y/1 : i8));
}