fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  join ret154(retv155: string) -> string {
    retv155
  } in
  jump ret154("Shape")
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join ret156(retv157: string) -> string {
    retv157
  } in
  let t158 = inherent#Shape#Shape#name(self/1) in
  let t159 = (t158 + suffix/2) in
  jump ret156(t159)
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join ret160(retv161: string) -> string {
    retv161
  } in
  let t162 = inherent#Shape#Shape#name(self/3) in
  let t163 = (left/4 + t162) in
  let t164 = (t163 + right/5) in
  jump ret160(t164)
}

fn announce(shape/6: Shape) -> unit {
  join ret165() -> unit {
    ()
  } in
  let base/7 = inherent#Shape#Shape#name(shape/6) in
  let with_suffix/8 = inherent#Shape#Shape#rename(shape/6, "!") in
  let combined/9 = inherent#Shape#Shape#join(shape/6, base/7, with_suffix/8) in
  let mtmp152 = println__T_string(combined/9) in
  jump ret165()
}

fn main() -> unit {
  join ret166() -> unit {
    ()
  } in
  let shape/10 = Shape {} in
  let _wild153 = announce(shape/10) in
  jump ret166()
}

fn println__T_string(value/1: string) -> unit {
  join ret167() -> unit {
    ()
  } in
  let t168 = trait_impl#ToString#string#to_string(value/1) in
  let t169 = @runtime(core.string_println)(t168) in
  jump ret167()
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret170(retv171: string) -> string {
    retv171
  } in
  jump ret170(self/38)
}