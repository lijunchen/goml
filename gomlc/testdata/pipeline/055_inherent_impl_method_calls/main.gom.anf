fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  "Shape"
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join inline_cont816(t802: string) -> string {
    let t803 = (t802 + suffix/2) in
    t803
  } in
  jump inline_cont816("Shape")
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join inline_cont817(t806: string) -> string {
    let t807 = (left/4 + t806) in
    let t808 = (t807 + right/5) in
    t808
  } in
  jump inline_cont817("Shape")
}

fn announce(shape/6: Shape) -> unit {
  join inline_cont829(base/7: string) -> unit {
    join inline_cont825(with_suffix/8: string) -> unit {
      join inline_cont821(combined/9: string) -> unit {
        let inline819 = trait_impl#ToString#string#to_string(combined/9) in
        let inline820 = @runtime(core.string_println)(inline819) in
        ()
      } in
      let inline822 = inherent#Shape#Shape#name(shape/6) in
      let inline823 = (base/7 + inline822) in
      let inline824 = (inline823 + with_suffix/8) in
      jump inline_cont821(inline824)
    } in
    let inline826 = "!" in
    let inline827 = inherent#Shape#Shape#name(shape/6) in
    let inline828 = (inline827 + inline826) in
    jump inline_cont825(inline828)
  } in
  jump inline_cont829("Shape")
}

fn main() -> unit {
  let shape/10 = Shape {} in
  let inline831 = inherent#Shape#Shape#name(shape/10) in
  let inline832 = inherent#Shape#Shape#rename(shape/10, "!") in
  let inline833 = inherent#Shape#Shape#join(shape/10, inline831, inline832) in
  let inline834 = println__T_string(inline833) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont835(t812: string) -> unit {
    let t813 = @runtime(core.string_println)(t812) in
    ()
  } in
  jump inline_cont835(value/1)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}