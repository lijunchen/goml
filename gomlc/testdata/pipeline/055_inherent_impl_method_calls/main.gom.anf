fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  "Shape"
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join inline_cont197(t183: string) -> string {
    let t184 = (t183 + suffix/2) in
    t184
  } in
  jump inline_cont197("Shape")
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join inline_cont198(t187: string) -> string {
    let t188 = (left/4 + t187) in
    let t189 = (t188 + right/5) in
    t189
  } in
  jump inline_cont198("Shape")
}

fn announce(shape/6: Shape) -> unit {
  join inline_cont210(base/7: string) -> unit {
    join inline_cont206(with_suffix/8: string) -> unit {
      join inline_cont202(combined/9: string) -> unit {
        let inline200 = trait_impl#ToString#string#to_string(combined/9) in
        let inline201 = @runtime(core.string_println)(inline200) in
        ()
      } in
      let inline203 = inherent#Shape#Shape#name(shape/6) in
      let inline204 = (base/7 + inline203) in
      let inline205 = (inline204 + with_suffix/8) in
      jump inline_cont202(inline205)
    } in
    let inline207 = "!" in
    let inline208 = inherent#Shape#Shape#name(shape/6) in
    let inline209 = (inline208 + inline207) in
    jump inline_cont206(inline209)
  } in
  jump inline_cont210("Shape")
}

fn main() -> unit {
  let shape/10 = Shape {} in
  let inline212 = inherent#Shape#Shape#name(shape/10) in
  let inline213 = inherent#Shape#Shape#rename(shape/10, "!") in
  let inline214 = inherent#Shape#Shape#join(shape/10, inline212, inline213) in
  let inline215 = println__T_string(inline214) in
  ()
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont216(t193: string) -> unit {
    let t194 = @runtime(core.string_println)(t193) in
    ()
  } in
  jump inline_cont216(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}