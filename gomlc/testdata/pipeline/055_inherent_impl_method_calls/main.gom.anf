fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  "Shape"
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join inline_cont156(t142: string) -> string {
    let t143 = (t142 + suffix/2) in
    t143
  } in
  jump inline_cont156("Shape")
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join inline_cont157(t146: string) -> string {
    let t147 = (left/4 + t146) in
    let t148 = (t147 + right/5) in
    t148
  } in
  jump inline_cont157("Shape")
}

fn announce(shape/6: Shape) -> unit {
  join inline_cont169(base/7: string) -> unit {
    join inline_cont165(with_suffix/8: string) -> unit {
      join inline_cont161(combined/9: string) -> unit {
        let inline159 = trait_impl#ToString#string#to_string(combined/9) in
        let inline160 = @runtime(core.string_println)(inline159) in
        ()
      } in
      let inline162 = inherent#Shape#Shape#name(shape/6) in
      let inline163 = (base/7 + inline162) in
      let inline164 = (inline163 + with_suffix/8) in
      jump inline_cont161(inline164)
    } in
    let inline166 = "!" in
    let inline167 = inherent#Shape#Shape#name(shape/6) in
    let inline168 = (inline167 + inline166) in
    jump inline_cont165(inline168)
  } in
  jump inline_cont169("Shape")
}

fn main() -> unit {
  let shape/10 = Shape {} in
  let inline171 = inherent#Shape#Shape#name(shape/10) in
  let inline172 = inherent#Shape#Shape#rename(shape/10, "!") in
  let inline173 = inherent#Shape#Shape#join(shape/10, inline171, inline172) in
  let inline174 = println__T_string(inline173) in
  ()
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont175(t152: string) -> unit {
    let t153 = @runtime(core.string_println)(t152) in
    ()
  } in
  jump inline_cont175(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}