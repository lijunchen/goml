fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  "Shape"
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join inline_cont428(t414: string) -> string {
    let t415 = (t414 + suffix/2) in
    t415
  } in
  jump inline_cont428("Shape")
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join inline_cont429(t418: string) -> string {
    let t419 = (left/4 + t418) in
    let t420 = (t419 + right/5) in
    t420
  } in
  jump inline_cont429("Shape")
}

fn announce(shape/6: Shape) -> unit {
  join inline_cont441(base/7: string) -> unit {
    join inline_cont437(with_suffix/8: string) -> unit {
      join inline_cont433(combined/9: string) -> unit {
        let inline431 = trait_impl#ToString#string#to_string(combined/9) in
        let inline432 = @runtime(core.string_println)(inline431) in
        ()
      } in
      let inline434 = inherent#Shape#Shape#name(shape/6) in
      let inline435 = (base/7 + inline434) in
      let inline436 = (inline435 + with_suffix/8) in
      jump inline_cont433(inline436)
    } in
    let inline438 = "!" in
    let inline439 = inherent#Shape#Shape#name(shape/6) in
    let inline440 = (inline439 + inline438) in
    jump inline_cont437(inline440)
  } in
  jump inline_cont441("Shape")
}

fn main() -> unit {
  let shape/10 = Shape {} in
  let inline443 = inherent#Shape#Shape#name(shape/10) in
  let inline444 = inherent#Shape#Shape#rename(shape/10, "!") in
  let inline445 = inherent#Shape#Shape#join(shape/10, inline443, inline444) in
  let inline446 = println__T_string(inline445) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont447(t424: string) -> unit {
    let t425 = @runtime(core.string_println)(t424) in
    ()
  } in
  jump inline_cont447(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}