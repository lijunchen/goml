fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  "Shape"
}

fn inherent#Shape#Shape#rename(self/0: Shape, suffix/0: string) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (t0 + suffix/0) in
    t1
  } in
  jump inline_cont0("Shape")
}

fn inherent#Shape#Shape#join(self/0: Shape, left/0: string, right/0: string) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = (left/0 + t0) in
    let t2 = (t1 + right/0) in
    t2
  } in
  jump inline_cont0("Shape")
}

fn announce(shape/0: Shape) -> unit {
  join inline_cont0(base/0: string) -> unit {
    join inline_cont1(with_suffix/0: string) -> unit {
      join inline_cont2(combined/0: string) -> unit {
        let inline0 = trait_impl#ToString#string#to_string(combined/0) in
        let inline1 = @runtime(core.string_println)(inline0) in
        ()
      } in
      let inline2 = inherent#Shape#Shape#name(shape/0) in
      let inline3 = (base/0 + inline2) in
      let inline4 = (inline3 + with_suffix/0) in
      jump inline_cont2(inline4)
    } in
    let inline5 = "!" in
    let inline6 = inherent#Shape#Shape#name(shape/0) in
    let inline7 = (inline6 + inline5) in
    jump inline_cont1(inline7)
  } in
  jump inline_cont0("Shape")
}

fn main() -> unit {
  let shape/0 = Shape {} in
  let inline0 = inherent#Shape#Shape#name(shape/0) in
  let inline1 = inherent#Shape#Shape#rename(shape/0, "!") in
  let inline2 = inherent#Shape#Shape#join(shape/0, inline0, inline1) in
  let inline3 = println__T_string(inline2) in
  ()
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}