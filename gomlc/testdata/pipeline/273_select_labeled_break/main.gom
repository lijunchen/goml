fn main() -> unit {
    let keep = Ref::new(false);
    let channel: Channel[isize] = Channel::new(0);
    while true {
        if keep.get() {
            continue
        } else {
            select {
                recv(channel) as _ => break,
                default => break,
            }
        }
    }
    println(42)
}
