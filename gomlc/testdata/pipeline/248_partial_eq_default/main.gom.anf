fn trait_impl#PartialEq#Settings#eq(self/0: Settings, other/0: Settings) -> bool {
  join k0(jp0: bool) -> bool {
    join k1(jp1: bool) -> bool {
      if jp1 {
        let t0 = Settings.label(self/0) in
        let t1 = Settings.label(other/0) in
        let inline0 = (t0 == t1) in
        inline0
      } else {
        false
      }
    } in
    if jp0 {
      let t2 = Settings.retries(self/0) in
      let t3 = Settings.retries(other/0) in
      let inline1 = (t2 == t3) in
      jump k1(inline1)
    } else {
      jump k1(false)
    }
  } in
  let t4 = Settings.enabled(self/0) in
  let t5 = Settings.enabled(other/0) in
  let inline2 = (t4 == t5) in
  jump k0(inline2)
}

fn trait_impl#Default#Settings#default() -> Settings {
  join inline_cont0(t0: bool) -> Settings {
    join inline_cont1(t1: isize) -> Settings {
      join inline_cont2(t2: string) -> Settings {
        let t3 = Settings { enabled: t0, retries: t1, label: t2 } in
        t3
      } in
      jump inline_cont2("")
    } in
    jump inline_cont1(0)
  } in
  jump inline_cont0(false)
}

fn trait_impl#Debug#Settings#debug(self/0: Settings) -> string {
  let x0 = Settings.enabled(self/0) in
  let x1 = Settings.retries(self/0) in
  let x2 = Settings.label(self/0) in
  let t0 = ("Settings { " + "enabled: ") in
  join inline_cont0(t1: string) -> string {
    let t2 = (t0 + t1) in
    let t3 = (t2 + ", ") in
    let t4 = (t3 + "retries: ") in
    join inline_cont1(t5: string) -> string {
      let t6 = (t4 + t5) in
      let t7 = (t6 + ", ") in
      let t8 = (t7 + "label: ") in
      join inline_cont2(t9: string) -> string {
        let t10 = (t8 + t9) in
        let t11 = (t10 + " }") in
        t11
      } in
      let inline0 = trait_impl#ToString#string#to_string(x2) in
      jump inline_cont2(inline0)
    } in
    let inline1 = trait_impl#ToString#isize#to_string(x1) in
    jump inline_cont1(inline1)
  } in
  let inline2 = trait_impl#ToString#bool#to_string(x0) in
  jump inline_cont0(inline2)
}

fn trait_impl#Default#Empty#default() -> Empty {
  let t0 = Empty {} in
  t0
}

fn trait_impl#Debug#Empty#debug(self/0: Empty) -> string {
  "Empty {}"
}

fn trait_impl#Default#State#default() -> State {
  Tag_0
}

fn trait_impl#Debug#State#debug(self/0: State) -> string {
  match self/0 {
    Tag_0 => {
      "State::Idle"
    },
    Tag_1 => {
      let x0 = State::Running._0(self/0) in
      join inline_cont0(t0: string) -> string {
        let t1 = ("State::Running(" + t0) in
        let t2 = (t1 + ")") in
        t2
      } in
      let inline0 = trait_impl#ToString#isize#to_string(x0) in
      jump inline_cont0(inline0)
    },
  }
}

fn trait_impl#Default#Message#default() -> Message {
  join inline_cont0(t0: string) -> Message {
    join inline_cont1(t1: isize) -> Message {
      let t2 = Message::Data(t0, t1) in
      t2
    } in
    jump inline_cont1(0)
  } in
  jump inline_cont0("")
}

fn trait_impl#Debug#Message#debug(self/0: Message) -> string {
  match self/0 {
    Tag_0 => {
      let x0 = Message::Data._0(self/0) in
      let x1 = Message::Data._1(self/0) in
      join inline_cont0(t0: string) -> string {
        let t1 = ("Message::Data(" + t0) in
        let t2 = (t1 + ", ") in
        join inline_cont1(t3: string) -> string {
          let t4 = (t2 + t3) in
          let t5 = (t4 + ")") in
          t5
        } in
        let inline0 = trait_impl#ToString#isize#to_string(x1) in
        jump inline_cont1(inline0)
      } in
      let inline1 = trait_impl#ToString#string#to_string(x0) in
      jump inline_cont0(inline1)
    },
    Tag_1 => {
      "Message::Empty"
    },
  }
}

fn trait_impl#Default#Event#default() -> Event {
  join inline_cont0(t0: string) -> Event {
    join inline_cont1(t1: isize) -> Event {
      let t2 = Event::Data(t0, t1) in
      t2
    } in
    jump inline_cont1(0)
  } in
  jump inline_cont0("")
}

fn trait_impl#Debug#Event#debug(self/0: Event) -> string {
  match self/0 {
    Tag_0 => {
      let x0 = Event::Data._0(self/0) in
      let x1 = Event::Data._1(self/0) in
      let t0 = ("Event::Data { " + "name: ") in
      join inline_cont0(t1: string) -> string {
        let t2 = (t0 + t1) in
        let t3 = (t2 + ", ") in
        let t4 = (t3 + "count: ") in
        join inline_cont1(t5: string) -> string {
          let t6 = (t4 + t5) in
          let t7 = (t6 + " }") in
          t7
        } in
        let inline0 = trait_impl#ToString#isize#to_string(x1) in
        jump inline_cont1(inline0)
      } in
      let inline1 = trait_impl#ToString#string#to_string(x0) in
      jump inline_cont0(inline1)
    },
    Tag_1 => {
      "Event::Empty"
    },
  }
}

fn main() -> () {
  let settings/0 = trait_impl#Default#Settings#default() in
  let t0 = trait_impl#Debug#Settings#debug(settings/0) in
  let _wild0 = println__T_string(t0) in
  let t1 = Settings { enabled: false, retries: 0, label: "" } in
  let t2 = trait_impl#PartialEq#Settings#eq(settings/0, t1) in
  let t3 = trait_impl#ToString#bool#to_string(t2) in
  let _wild1 = println__T_string(t3) in
  let empty/0 = trait_impl#Default#Empty#default() in
  let t4 = trait_impl#Debug#Empty#debug(empty/0) in
  let _wild2 = println__T_string(t4) in
  let pair/0 = trait_impl#Default#PairDefaults__isize#default() in
  let t5 = PairDefaults__isize.first(pair/0) in
  let t6 = PairDefaults__isize.second(pair/0) in
  let t7 = (t5 + t6) in
  let t8 = inherent#isize#isize#to_string(t7) in
  let _wild3 = println__T_string(t8) in
  let t9 = PairDefaults__isize.nested(pair/0) in
  let t10 = inherent#Option#Option[T]#is_none__T_isize(t9) in
  join inline_cont0(t11: string) -> () {
    let _wild4 = println__T_string(t11) in
    join inline_cont1(state/0: State) -> () {
      let t12 = trait_impl#Debug#State#debug(state/0) in
      join inline_cont2(_wild5: ()) -> () {
        join inline_cont3(message/0: Message) -> () {
          let t13 = trait_impl#Debug#Message#debug(message/0) in
          join inline_cont4(_wild6: ()) -> () {
            join inline_cont5(event/0: Event) -> () {
              join inline_cont6(t14: string) -> () {
                join inline_cont7(_wild7: ()) -> () {
                  join inline_cont8(lazy/0: Lazy__NoDefault) -> () {
                    join k0(jp0: string) -> () {
                      join inline_cont9(_wild8: ()) -> () {
                        join inline_cont10(selected/0: Selected__isize) -> () {
                          join k1(jp1: string) -> () {
                            let inline0 = trait_impl#ToString#string#to_string(jp1) in
                            let inline1 = @runtime(core.string_println)(inline0) in
                            ()
                          } in
                          match selected/0 {
                            Tag_0 => {
                              let x0 = Selected__isize::Values._0(selected/0) in
                              let x1 = Selected__isize::Values._1(selected/0) in
                              let t15 = (x0 + x1) in
                              let inline2 = __goml_builtin_int_to_string(t15) in
                              jump k1(inline2)
                            },
                            Tag_1 => {
                              jump k1("ignored")
                            },
                          }
                        } in
                        let inline3 = trait_impl#Default#isize#default() in
                        let inline4 = trait_impl#Default#isize#default() in
                        let inline5 = Selected__isize::Values(inline3, inline4) in
                        jump inline_cont10(inline5)
                      } in
                      let inline6 = trait_impl#ToString#string#to_string(jp0) in
                      let inline7 = @runtime(core.string_println)(inline6) in
                      jump inline_cont9(())
                    } in
                    match lazy/0 {
                      Tag_0 => {
                        jump k0("empty")
                      },
                      Tag_1 => {
                        jump k0("value")
                      },
                    }
                  } in
                  jump inline_cont8(Tag_0)
                } in
                let inline8 = trait_impl#ToString#string#to_string(t14) in
                let inline9 = @runtime(core.string_println)(inline8) in
                jump inline_cont7(())
              } in
              match event/0 {
                Tag_0 => {
                  let inline10 = Event::Data._0(event/0) in
                  let inline11 = Event::Data._1(event/0) in
                  let inline12 = ("Event::Data { " + "name: ") in
                  let inline13 = trait_impl#Debug#string#debug(inline10) in
                  let inline14 = (inline12 + inline13) in
                  let inline15 = (inline14 + ", ") in
                  let inline16 = (inline15 + "count: ") in
                  let inline17 = trait_impl#Debug#isize#debug(inline11) in
                  let inline18 = (inline16 + inline17) in
                  let inline19 = (inline18 + " }") in
                  jump inline_cont6(inline19)
                },
                Tag_1 => {
                  jump inline_cont6("Event::Empty")
                },
              }
            } in
            let inline20 = trait_impl#Default#string#default() in
            let inline21 = trait_impl#Default#isize#default() in
            let inline22 = Event::Data(inline20, inline21) in
            jump inline_cont5(inline22)
          } in
          let inline23 = trait_impl#ToString#string#to_string(t13) in
          let inline24 = @runtime(core.string_println)(inline23) in
          jump inline_cont4(())
        } in
        let inline25 = trait_impl#Default#string#default() in
        let inline26 = trait_impl#Default#isize#default() in
        let inline27 = Message::Data(inline25, inline26) in
        jump inline_cont3(inline27)
      } in
      let inline28 = trait_impl#ToString#string#to_string(t12) in
      let inline29 = @runtime(core.string_println)(inline28) in
      jump inline_cont2(())
    } in
    jump inline_cont1(Tag_0)
  } in
  let inline30 = @runtime(core.bool_to_string)(t10) in
  jump inline_cont0(inline30)
}

fn trait_impl#PartialEq#bool#eq(self/0: bool, other/0: bool) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#PartialEq#string#eq(self/0: string, other/0: string) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Default#bool#default() -> bool {
  false
}

fn trait_impl#Default#isize#default() -> isize {
  0
}

fn trait_impl#Default#string#default() -> string {
  ""
}

fn trait_impl#Debug#bool#debug(self/0: bool) -> string {
  let inline0 = @runtime(core.bool_to_string)(self/0) in
  inline0
}

fn trait_impl#Debug#isize#debug(self/0: isize) -> string {
  let inline0 = __goml_builtin_int_to_string(self/0) in
  inline0
}

fn trait_impl#Debug#string#debug(self/0: string) -> string {
  self/0
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn trait_impl#Default#PairDefaults__isize#default() -> PairDefaults__isize {
  join inline_cont0(t0: isize) -> PairDefaults__isize {
    join inline_cont1(t1: isize) -> PairDefaults__isize {
      join inline_cont2(t2: Option__isize) -> PairDefaults__isize {
        let t3 = PairDefaults__isize { first: t0, second: t1, nested: t2 } in
        t3
      } in
      jump inline_cont2(Tag_0)
    } in
    jump inline_cont1(0)
  } in
  jump inline_cont0(0)
}

fn inherent#isize#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn inherent#Option#Option[T]#is_none__T_isize(self/0: Option__isize) -> bool {
  join inline_cont0(t0: bool) -> bool {
    let t1 = (!t0) in
    t1
  } in
  match self/0 {
    Tag_0 => {
      jump inline_cont0(false)
    },
    Tag_1 => {
      jump inline_cont0(true)
    },
  }
}

fn trait_impl#Default#Lazy__NoDefault#default() -> Lazy__NoDefault {
  Tag_0
}

fn trait_impl#Default#Selected__isize#default() -> Selected__isize {
  join inline_cont0(t0: isize) -> Selected__isize {
    join inline_cont1(t1: isize) -> Selected__isize {
      let t2 = Selected__isize::Values(t0, t1) in
      t2
    } in
    jump inline_cont1(0)
  } in
  jump inline_cont0(0)
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#Default#Option__isize#default() -> Option__isize {
  Tag_0
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn inherent#Option#Option[T]#is_some__T_isize(self/0: Option__isize) -> bool {
  match self/0 {
    Tag_0 => {
      false
    },
    Tag_1 => {
      true
    },
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: ()) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: ()) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}