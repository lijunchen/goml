fn cut_pair(ok/0: bool) -> Option__(string,string) {
  if ok/0 {
    let t420 = ("alpha", "beta") in
    let t421 = Option__(string,string)::Some(t420) in
    t421
  } else {
    Tag_0
  }
}

fn describe(ok/1: bool) -> Option__string {
  join inline_cont444(mtmp408: Option__(string,string)) -> Option__string {
    join k424(jp425: (string, string)) -> Option__string {
      let x411 = jp425.0 in
      let x412 = jp425.1 in
      let t426 = (x411 + "|") in
      let t427 = (t426 + x412) in
      let t428 = Option__string::Some(t427) in
      t428
    } in
    match mtmp408 {
      Tag_0 => {
        Tag_0
      },
      Tag_1 => {
        let x409 = Option__(string,string)::Some._0(mtmp408) in
        jump k424(x409)
      },
    }
  } in
  if ok/1 {
    let inline445 = ("alpha", "beta") in
    let inline446 = Option__(string,string)::Some(inline445) in
    jump inline_cont444(inline446)
  } else {
    jump inline_cont444(Tag_0)
  }
}

fn show(opt/4: Option__string) -> string {
  match opt/4 {
    Tag_0 => {
      "none"
    },
    Tag_1 => {
      let x413 = Option__string::Some._0(opt/4) in
      let t433 = ("some " + x413) in
      t433
    },
  }
}

fn main() -> unit {
  let t435 = describe(true) in
  join inline_cont472(t436: string) -> unit {
    join inline_cont469(_wild414: unit) -> unit {
      join inline_cont454(t437: Option__string) -> unit {
        join inline_cont450(t438: string) -> unit {
          let inline448 = trait_impl#ToString#string#to_string(t438) in
          let inline449 = @runtime(core.string_println)(inline448) in
          ()
        } in
        match t437 {
          Tag_0 => {
            jump inline_cont450("none")
          },
          Tag_1 => {
            let inline451 = Option__string::Some._0(t437) in
            let inline453 = ("some " + inline451) in
            jump inline_cont450(inline453)
          },
        }
      } in
      let inline455 = false in
      let inline456 = cut_pair(inline455) in
      join inline_join457(inline458: (string, string)) -> unit {
        let inline460 = inline458.0 in
        let inline461 = inline458.1 in
        let inline464 = (inline460 + "|") in
        let inline465 = (inline464 + inline461) in
        let inline466 = Option__string::Some(inline465) in
        jump inline_cont454(inline466)
      } in
      match inline456 {
        Tag_0 => {
          jump inline_cont454(Tag_0)
        },
        Tag_1 => {
          let inline467 = Option__(string,string)::Some._0(inline456) in
          jump inline_join457(inline467)
        },
      }
    } in
    let inline470 = trait_impl#ToString#string#to_string(t436) in
    let inline471 = @runtime(core.string_println)(inline470) in
    jump inline_cont469(())
  } in
  match t435 {
    Tag_0 => {
      jump inline_cont472("none")
    },
    Tag_1 => {
      let inline473 = Option__string::Some._0(t435) in
      let inline475 = ("some " + inline473) in
      jump inline_cont472(inline475)
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont476(t440: string) -> unit {
    let t441 = @runtime(core.string_println)(t440) in
    ()
  } in
  jump inline_cont476(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}