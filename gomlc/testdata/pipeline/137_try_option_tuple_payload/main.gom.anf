fn cut_pair(ok/0: bool) -> Option__(string,string) {
  if ok/0 {
    let t808 = ("alpha", "beta") in
    let t809 = Option__(string,string)::Some(t808) in
    t809
  } else {
    Tag_0
  }
}

fn describe(ok/1: bool) -> Option__string {
  join inline_cont832(mtmp796: Option__(string,string)) -> Option__string {
    join k812(jp813: (string, string)) -> Option__string {
      let x799 = jp813.0 in
      let x800 = jp813.1 in
      let t814 = (x799 + "|") in
      let t815 = (t814 + x800) in
      let t816 = Option__string::Some(t815) in
      t816
    } in
    match mtmp796 {
      Tag_0 => {
        Tag_0
      },
      Tag_1 => {
        let x797 = Option__(string,string)::Some._0(mtmp796) in
        jump k812(x797)
      },
    }
  } in
  if ok/1 {
    let inline833 = ("alpha", "beta") in
    let inline834 = Option__(string,string)::Some(inline833) in
    jump inline_cont832(inline834)
  } else {
    jump inline_cont832(Tag_0)
  }
}

fn show(opt/4: Option__string) -> string {
  match opt/4 {
    Tag_0 => {
      "none"
    },
    Tag_1 => {
      let x801 = Option__string::Some._0(opt/4) in
      let t821 = ("some " + x801) in
      t821
    },
  }
}

fn main() -> unit {
  let t823 = describe(true) in
  join inline_cont860(t824: string) -> unit {
    join inline_cont857(_wild802: unit) -> unit {
      join inline_cont842(t825: Option__string) -> unit {
        join inline_cont838(t826: string) -> unit {
          let inline836 = trait_impl#ToString#string#to_string(t826) in
          let inline837 = @runtime(core.string_println)(inline836) in
          ()
        } in
        match t825 {
          Tag_0 => {
            jump inline_cont838("none")
          },
          Tag_1 => {
            let inline839 = Option__string::Some._0(t825) in
            let inline841 = ("some " + inline839) in
            jump inline_cont838(inline841)
          },
        }
      } in
      let inline843 = false in
      let inline844 = cut_pair(inline843) in
      join inline_join845(inline846: (string, string)) -> unit {
        let inline848 = inline846.0 in
        let inline849 = inline846.1 in
        let inline852 = (inline848 + "|") in
        let inline853 = (inline852 + inline849) in
        let inline854 = Option__string::Some(inline853) in
        jump inline_cont842(inline854)
      } in
      match inline844 {
        Tag_0 => {
          jump inline_cont842(Tag_0)
        },
        Tag_1 => {
          let inline855 = Option__(string,string)::Some._0(inline844) in
          jump inline_join845(inline855)
        },
      }
    } in
    let inline858 = trait_impl#ToString#string#to_string(t824) in
    let inline859 = @runtime(core.string_println)(inline858) in
    jump inline_cont857(())
  } in
  match t823 {
    Tag_0 => {
      jump inline_cont860("none")
    },
    Tag_1 => {
      let inline861 = Option__string::Some._0(t823) in
      let inline863 = ("some " + inline861) in
      jump inline_cont860(inline863)
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont864(t828: string) -> unit {
    let t829 = @runtime(core.string_println)(t828) in
    ()
  } in
  jump inline_cont864(value/1)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}