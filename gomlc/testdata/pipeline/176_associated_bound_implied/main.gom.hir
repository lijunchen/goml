package main
file main.gom
  trait Label {
    fn label(Self) -> string;
  }

  trait Source {
    type Item: Label;
    fn get(Self) -> Self::Item;
  }

  struct TextSource {
      value: string,
  }

  impl Label for string {
    fn label(local/1/0: string) -> string {
        "label:" + self/0
    }
  }

  impl Source for TextSource {
    type Item = string;
    fn get(local/1/1: TextSource) -> string {
        self/1.value
    }
  }

  fn render[S: Source](local/1/2: S) -> string {
      Source::get(source/2).label()
  }

  fn main() -> unit {
      println(def/1/5(TextSource { value: "goml" }));
  }