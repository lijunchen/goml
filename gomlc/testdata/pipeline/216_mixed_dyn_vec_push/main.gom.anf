fn trait_impl#Show#i32#show(self/0: i32) -> string {
  let inline443 = @runtime(core.int32_to_string)(self/0) in
  inline443
}

fn trait_impl#Show#Wrap#show(self/1: Wrap) -> string {
  let t419 = Wrap.value(self/1) in
  t419
}

fn main() -> unit {
  join inline_cont454(values/2: Vec[dyn Show]) -> unit {
    let value/3 = 10 in
    let t421 = to_dyn[Show]{i32}(value/3) in
    join inline_cont452(_wild411: unit) -> unit {
      let t422 = Wrap { value: "ok" } in
      let t423 = to_dyn[Show]{Wrap}(t422) in
      join inline_cont450(_wild412: unit) -> unit {
        let t424 = @intrinsic(vec.get)(values/2, 0) in
        let t425 = dyn_call[Show::show](t424) in
        join inline_cont447(_wild413: unit) -> unit {
          let t426 = @intrinsic(vec.get)(values/2, 1) in
          let t427 = dyn_call[Show::show](t426) in
          let inline445 = trait_impl#ToString#string#to_string(t427) in
          let inline446 = @runtime(core.string_println)(inline445) in
          ()
        } in
        let inline448 = trait_impl#ToString#string#to_string(t425) in
        let inline449 = @runtime(core.string_println)(inline448) in
        jump inline_cont447(())
      } in
      let inline451 = @intrinsic(vec.push)(values/2, t423) in
      jump inline_cont450(())
    } in
    let inline453 = @intrinsic(vec.push)(values/2, t421) in
    jump inline_cont452(())
  } in
  let inline455 = @intrinsic(vec.new)() in
  jump inline_cont454(inline455)
}

fn inherent#i32#i32#to_string(self/33: i32) -> string {
  let t431 = @runtime(core.int32_to_string)(self/33) in
  t431
}

fn inherent#Vec#Vec[T]#new__T_dynShow() -> Vec[dyn Show] {
  let t434 = @intrinsic(vec.new)() in
  t434
}

fn inherent#Vec#Vec[T]#push__T_dynShow(self/258: Vec[dyn Show], elem/259: dyn Show) -> unit {
  let t436 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont456(t438: string) -> unit {
    let t439 = @runtime(core.string_println)(t438) in
    ()
  } in
  jump inline_cont456(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}