fn trait_impl#Describe#Tracker#describe(self/0: Tracker) -> string {
  let x0 = Tracker.label(self/0) in
  let x1 = Tracker.count(self/0) in
  let x2 = Tracker.toggled(self/0) in
  join inline_cont0(current/0: i32) -> string {
    join inline_cont1(flag/0: bool) -> string {
      let with_label/0 = ("Tracker(" + x0) in
      let with_count_label/0 = (with_label/0 + ", count: ") in
      join inline_cont2(t0: string) -> string {
        let with_count/0 = (with_count_label/0 + t0) in
        let with_flag_label/0 = (with_count/0 + ", toggled: ") in
        join inline_cont3(t1: string) -> string {
          let t2 = (with_flag_label/0 + t1) in
          let t3 = (t2 + ")") in
          t3
        } in
        let inline0 = @runtime(core.bool_to_string)(flag/0) in
        jump inline_cont3(inline0)
      } in
      let inline1 = __goml_builtin_int32_to_string(current/0) in
      jump inline_cont2(inline1)
    } in
    let inline2 = @intrinsic(ref.get)(x2) in
    jump inline_cont1(inline2)
  } in
  let inline3 = @intrinsic(ref.get)(x1) in
  jump inline_cont0(inline3)
}

fn trait_impl#Describe#Record__i32#describe(self/0: Record__i32) -> string {
  match self/0 {
    Tag_0 => {
      let x0 = Record__i32::Value._0(self/0) in
      join inline_cont0(t0: string) -> string {
        let t1 = ("Value(" + t0) in
        let t2 = (t1 + ")") in
        t2
      } in
      let inline0 = __goml_builtin_int32_to_string(x0) in
      jump inline_cont0(inline0)
    },
    Tag_1 => {
      let x1 = Record__i32::Pair._0(self/0) in
      let x2 = Record__i32::Pair._1(self/0) in
      join inline_cont1(t3: string) -> string {
        let prefix/0 = ("Pair(" + t3) in
        let t4 = (prefix/0 + ", ") in
        join inline_cont2(t5: string) -> string {
          let t6 = (t4 + t5) in
          let t7 = (t6 + ")") in
          t7
        } in
        let inline1 = __goml_builtin_int32_to_string(x2) in
        jump inline_cont2(inline1)
      } in
      let inline2 = __goml_builtin_int32_to_string(x1) in
      jump inline_cont1(inline2)
    },
    Tag_2 => {
      "Empty"
    },
  }
}

fn trait_impl#Describe#Record__string#describe(self/0: Record__string) -> string {
  match self/0 {
    Tag_0 => {
      let x0 = Record__string::Value._0(self/0) in
      let t0 = ("Value(" + x0) in
      let t1 = (t0 + ")") in
      t1
    },
    Tag_1 => {
      let x1 = Record__string::Pair._0(self/0) in
      let x2 = Record__string::Pair._1(self/0) in
      let prefix/0 = ("Pair(" + x1) in
      let t2 = (prefix/0 + ", ") in
      let t3 = (t2 + x2) in
      let t4 = (t3 + ")") in
      t4
    },
    Tag_2 => {
      "Empty"
    },
  }
}

fn format_total(total/0: i32) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = ("total: " + t0) in
    t1
  } in
  let inline0 = __goml_builtin_int32_to_string(total/0) in
  jump inline_cont0(inline0)
}

fn increment(value/0: i32) -> i32 {
  let t0 = (value/0 + 1) in
  t0
}

fn triple(value/0: i32) -> i32 {
  let t0 = (value/0 * 3) in
  t0
}

fn pair_join(parts/0: (string, string)) -> string {
  let x0 = parts/0.0 in
  let x1 = parts/0.1 in
  let t0 = (x0 + " -> ") in
  let t1 = (t0 + x1) in
  t1
}

fn run_transforms(value/0: i32, transforms/0: [(i32) -> i32; 2]) -> [i32; 2] {
  let first/0 = @intrinsic(array.get)(transforms/0, 0) in
  let second/0 = @intrinsic(array.get)(transforms/0, 1) in
  let first_result/0 = first/0(value/0) in
  let second_result/0 = second/0(first_result/0) in
  let results/0 = [first_result/0, value/0] in
  let place_root0 = results/0 in
  let index0 = 1 in
  let place0 = @intrinsic(array.get)(place_root0, index0) in
  let t0 = @intrinsic(array.set)(place_root0, index0, second_result/0) in
  let t1 = results/0 = t0 in
  results/0
}

fn gather(record/0: Record__i32) -> Maybe__i32 {
  match record/0 {
    Tag_0 => {
      let x0 = Record__i32::Value._0(record/0) in
      let t0 = Maybe__i32::Some(x0) in
      t0
    },
    Tag_1 => {
      let x1 = Record__i32::Pair._1(record/0) in
      let t1 = Maybe__i32::Some(x1) in
      t1
    },
    Tag_2 => {
      Tag_1
    },
  }
}

fn build_counter(label/0: string, start/0: i32) -> (
  Tracker,
  () -> Record__i32,
  (i32) -> Record__i32,
  () -> Record__string
) {
  join inline_cont0(count/0: Ref[i32]) -> (
    Tracker,
    () -> Record__i32,
    (i32) -> Record__i32,
    () -> Record__string
  ) {
    join inline_cont1(toggled/0: Ref[bool]) -> (
      Tracker,
      () -> Record__i32,
      (i32) -> Record__i32,
      () -> Record__string
    ) {
      let tracker/0 = Tracker { label: label/0, count: count/0, toggled: toggled/0 } in
      let t0 = closure_env_snapshot_0 { count_0: count/0 } in
      let snapshot/0 =
        pack_closure[inherent#closure_env_snapshot_0#closure_env_snapshot_0#apply](t0) in
      let t1 = closure_env_bump_1 { count_0: count/0 } in
      let bump/0 = pack_closure[inherent#closure_env_bump_1#closure_env_bump_1#apply](t1) in
      let t2 = closure_env_flip_2 { toggled_0: toggled/0 } in
      let flip/0 = pack_closure[inherent#closure_env_flip_2#closure_env_flip_2#apply](t2) in
      let t3 = (tracker/0, snapshot/0, bump/0, flip/0) in
      t3
    } in
    let inline0 = false in
    let inline1 = @intrinsic(ref.new)(inline0) in
    jump inline_cont1(inline1)
  } in
  let inline2 = @intrinsic(ref.new)(start/0) in
  jump inline_cont0(inline2)
}

fn main() -> unit {
  let mtmp0 = build_counter("goml", 2) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  let x2 = mtmp0.2 in
  let x3 = mtmp0.3 in
  let tracker_info/0 = trait_impl#Describe#Tracker#describe(x0) in
  let first_record/0 = x1() in
  let bumped_record/0 = x2(5) in
  let flipped_record/0 = x3() in
  let maybe_first/0 = gather(first_record/0) in
  let maybe_second/0 = gather(bumped_record/0) in
  let chosen/0 = choose__T_Maybe[i32](true, maybe_second/0, maybe_first/0) in
  let stringified/0 = map_maybe__T_i32__U_string(chosen/0, format_total) in
  let transforms/0 = [increment, triple] in
  join inline_cont0(results/0: [i32; 2]) -> unit {
    let first_result/0 = @intrinsic(array.get)(results/0, 0) in
    let second_result/0 = @intrinsic(array.get)(results/0, 1) in
    let t0 = (first_result/0 < second_result/0) in
    join k0(jp0: bool) -> unit {
      let first_text/0 = trait_impl#Describe#Record__i32#describe(first_record/0) in
      let bumped_text/0 = trait_impl#Describe#Record__i32#describe(bumped_record/0) in
      let flipped_text/0 = trait_impl#Describe#Record__string#describe(flipped_record/0) in
      join k1(jp1: string) -> unit {
        join inline_cont1(t1: string) -> unit {
          join inline_cont2(t2: string) -> unit {
            join inline_cont3(pair_text/0: string) -> unit {
              join inline_cont4(bool_text/0: string) -> unit {
                join inline_cont5(mtmp1: unit) -> unit {
                  join inline_cont6(mtmp2: unit) -> unit {
                    join inline_cont7(mtmp3: unit) -> unit {
                      join inline_cont8(mtmp4: unit) -> unit {
                        join inline_cont9(mtmp5: unit) -> unit {
                          join inline_cont10(mtmp6: unit) -> unit {
                            let inline0 = trait_impl#ToString#string#to_string(bool_text/0) in
                            let inline1 = @runtime(core.string_println)(inline0) in
                            ()
                          } in
                          let inline2 = trait_impl#ToString#string#to_string(pair_text/0) in
                          let inline3 = @runtime(core.string_println)(inline2) in
                          jump inline_cont10(())
                        } in
                        let inline4 = trait_impl#ToString#string#to_string(jp1) in
                        let inline5 = @runtime(core.string_println)(inline4) in
                        jump inline_cont9(())
                      } in
                      let inline6 = trait_impl#ToString#string#to_string(flipped_text/0) in
                      let inline7 = @runtime(core.string_println)(inline6) in
                      jump inline_cont8(())
                    } in
                    let inline8 = trait_impl#ToString#string#to_string(bumped_text/0) in
                    let inline9 = @runtime(core.string_println)(inline8) in
                    jump inline_cont7(())
                  } in
                  let inline10 = trait_impl#ToString#string#to_string(first_text/0) in
                  let inline11 = @runtime(core.string_println)(inline10) in
                  jump inline_cont6(())
                } in
                let inline12 = trait_impl#ToString#string#to_string(tracker_info/0) in
                let inline13 = @runtime(core.string_println)(inline12) in
                jump inline_cont5(())
              } in
              let inline14 = @runtime(core.bool_to_string)(jp0) in
              jump inline_cont4(inline14)
            } in
            let inline15 = (t1 + " -> ") in
            let inline16 = (inline15 + t2) in
            jump inline_cont3(inline16)
          } in
          let inline17 = __goml_builtin_int32_to_string(second_result/0) in
          jump inline_cont2(inline17)
        } in
        let inline18 = __goml_builtin_int32_to_string(first_result/0) in
        jump inline_cont1(inline18)
      } in
      match stringified/0 {
        Tag_0 => {
          let x4 = Maybe__string::Some._0(stringified/0) in
          let t3 = ("Snapshot: " + x4) in
          jump k1(t3)
        },
        Tag_1 => {
          jump k1("Snapshot: none")
        },
      }
    } in
    if t0 {
      jump k0(true)
    } else {
      jump k0(false)
    }
  } in
  let inline19 = 4 in
  let inline20 = @intrinsic(array.get)(transforms/0, 0) in
  let inline21 = @intrinsic(array.get)(transforms/0, 1) in
  let inline22 = inline20(inline19) in
  let inline23 = inline21(inline22) in
  let inline24 = [inline22, inline19] in
  let inline25 = inline24 in
  let inline26 = 1 in
  let inline27 = @intrinsic(array.get)(inline25, inline26) in
  let inline28 = @intrinsic(array.set)(inline25, inline26, inline23) in
  let inline29 = inline24 = inline28 in
  jump inline_cont0(inline24)
}

fn inherent#Ref#Ref[T]#get__T_i32(self/0: Ref[i32]) -> i32 {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_bool(self/0: Ref[bool]) -> bool {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#new__T_i32(value/0: i32) -> Ref[i32] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#new__T_bool(value/0: bool) -> Ref[bool] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_i32(self/0: Ref[i32], value/0: i32) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn inherent#Ref#Ref[T]#set__T_bool(self/0: Ref[bool], value/0: bool) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn choose__T_Maybe[i32](
  flag/0: bool,
  when_true/0: Maybe__i32,
  when_false/0: Maybe__i32
) -> Maybe__i32 {
  if flag/0 {
    when_true/0
  } else {
    when_false/0
  }
}

fn map_maybe__T_i32__U_string(value/0: Maybe__i32, f/0: (i32) -> string) -> Maybe__string {
  match value/0 {
    Tag_0 => {
      let x0 = Maybe__i32::Some._0(value/0) in
      let t0 = f/0(x0) in
      let t1 = Maybe__string::Some(t0) in
      t1
    },
    Tag_1 => {
      Tag_1
    },
  }
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn trait_impl#PartialEq#i32#eq(self/0: i32, other/0: i32) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#i32#hash(self/0: i32) -> u64 {
  let t0 = @runtime(core.int32_hash)(self/0) in
  t0
}

fn trait_impl#PartialEq#bool#eq(self/0: bool, other/0: bool) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#bool#hash(self/0: bool) -> u64 {
  if self/0 {
    1
  } else {
    0
  }
}

fn inherent#closure_env_snapshot_0#closure_env_snapshot_0#apply(
  env0: closure_env_snapshot_0
) -> Record__i32 {
  let count/0 = closure_env_snapshot_0.count_0(env0) in
  join inline_cont0(t0: i32) -> Record__i32 {
    let t1 = Record__i32::Value(t0) in
    t1
  } in
  let inline0 = @intrinsic(ref.get)(count/0) in
  jump inline_cont0(inline0)
}

fn inherent#closure_env_bump_1#closure_env_bump_1#apply(
  env0: closure_env_bump_1,
  delta/0: i32
) -> Record__i32 {
  let count/0 = closure_env_bump_1.count_0(env0) in
  join inline_cont0(before/0: i32) -> Record__i32 {
    let t0 = (before/0 + delta/0) in
    join inline_cont1(mtmp0: unit) -> Record__i32 {
      join inline_cont2(t1: i32) -> Record__i32 {
        let t2 = Record__i32::Pair(before/0, t1) in
        t2
      } in
      let inline0 = @intrinsic(ref.get)(count/0) in
      jump inline_cont2(inline0)
    } in
    let inline1 = @intrinsic(ref.set)(count/0, t0) in
    jump inline_cont1(())
  } in
  let inline2 = @intrinsic(ref.get)(count/0) in
  jump inline_cont0(inline2)
}

fn inherent#closure_env_flip_2#closure_env_flip_2#apply(
  env0: closure_env_flip_2
) -> Record__string {
  let toggled/0 = closure_env_flip_2.toggled_0(env0) in
  join inline_cont0(before/0: bool) -> Record__string {
    let t0 = (!before/0) in
    join inline_cont1(mtmp0: unit) -> Record__string {
      join inline_cont2(after/0: bool) -> Record__string {
        join inline_cont3(t1: string) -> Record__string {
          join inline_cont4(t2: string) -> Record__string {
            let t3 = Record__string::Pair(t1, t2) in
            t3
          } in
          let inline0 = @runtime(core.bool_to_string)(after/0) in
          jump inline_cont4(inline0)
        } in
        let inline1 = @runtime(core.bool_to_string)(before/0) in
        jump inline_cont3(inline1)
      } in
      let inline2 = @intrinsic(ref.get)(toggled/0) in
      jump inline_cont2(inline2)
    } in
    let inline3 = @intrinsic(ref.set)(toggled/0, t0) in
    jump inline_cont1(())
  } in
  let inline4 = @intrinsic(ref.get)(toggled/0) in
  jump inline_cont0(inline4)
}