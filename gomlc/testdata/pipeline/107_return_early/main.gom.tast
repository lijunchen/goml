fn early(x/0: i32) -> i32 {
    if (x/0 : i32) < 0 {
        {
            return 0
        }
    } else {
        {}
    };
    if (x/0 : i32) == 0 {
        {
            return 1
        }
    } else {
        {}
    };
    (x/0 : i32) + 2
}

fn closure_early(x/1: i32) -> i32 {
    let f/3: (i32) -> i32 =
      |y/2: i32| =>
        {
            if (y/2 : i32) > 5 {
                {
                    return (y/2 : i32)
                }
            } else {
                {}
            };
            (y/2 : i32) + 10
        };
    (f/3 : (i32) -> i32)((x/1 : i32))
}

fn unit_ret(flag/4: bool) -> () {
    if (flag/4 : bool) {
        {
            return
        }
    } else {
        {}
    };
    let _ : () = (println : (string) -> ())("after");
}

fn main() -> () {
    let _ : () = (print : (string) -> ())("e-1: ");
    let _ : () = (println : (i32) -> ())((early : (i32) -> i32)(-1));
    let _ : () = (print : (string) -> ())("e0: ");
    let _ : () = (println : (i32) -> ())((early : (i32) -> i32)(0));
    let _ : () = (print : (string) -> ())("e3: ");
    let _ : () = (println : (i32) -> ())((early : (i32) -> i32)(3));
    let _ : () = (print : (string) -> ())("c7: ");
    let _ : () = (println : (i32) -> ())((closure_early : (i32) -> i32)(7));
    let _ : () = (print : (string) -> ())("c2: ");
    let _ : () = (println : (i32) -> ())((closure_early : (i32) -> i32)(2));
    (unit_ret : (bool) -> ())(true);
    (unit_ret : (bool) -> ())(false);
}