fn make_array() -> [int32; 3] {
  let t182 = [1, 2, 3] in
  t182
}

fn main() -> unit {
  join inline_cont193(arr/0: [int32; 3]) -> unit {
    let inline/1 = [4, 5, 6] in
    join inline_cont189(mtmp177: unit) -> unit {
      let mtmp178 = arr/0 in
      let mtmp179 = inline/1 in
      ()
    } in
    let inline190 = "array literal" in
    let inline191 = trait_impl#ToString#string#to_string(inline190) in
    let inline192 = @runtime(core.string_print)(inline191) in
    jump inline_cont189(())
  } in
  let inline194 = [1, 2, 3] in
  jump inline_cont193(inline194)
}

fn print__T_string(value/30: string) -> unit {
  join inline_cont195(t185: string) -> unit {
    let t186 = @runtime(core.string_print)(t185) in
    ()
  } in
  jump inline_cont195(value/30)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}