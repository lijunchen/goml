fn make_array() -> [int32; 3] {
  let t141 = [1, 2, 3] in
  t141
}

fn main() -> unit {
  join inline_cont152(arr/0: [int32; 3]) -> unit {
    let inline/1 = [4, 5, 6] in
    join inline_cont148(mtmp136: unit) -> unit {
      let mtmp137 = arr/0 in
      let mtmp138 = inline/1 in
      ()
    } in
    let inline149 = "array literal" in
    let inline150 = trait_impl#ToString#string#to_string(inline149) in
    let inline151 = @runtime(core.string_print)(inline150) in
    jump inline_cont148(())
  } in
  let inline153 = [1, 2, 3] in
  jump inline_cont152(inline153)
}

fn print__T_string(value/30: string) -> unit {
  join inline_cont154(t144: string) -> unit {
    let t145 = @runtime(core.string_print)(t144) in
    ()
  } in
  jump inline_cont154(value/30)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}