fn main() -> unit {
  join ret113() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit114() -> unit {
    let _wild111 = () in
    let t115 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild112 = println__T_int(t115) in
    jump ret113()
  } in
  joinrec {
    join loop116() -> unit {
      let t117 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t118 = (t117 < 7) in
      if t118 {
        let cur/2 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t119 = (cur/2 + 1) in
        let _wild108 = inherent#Ref#Ref[T]#set__T_int(i/0, t119) in
        let t123 = (cur/2 < 5) in
        join k120() -> unit {
          let mtmp109 = () in
          let t121 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t122 = (t121 + cur/2) in
          let _wild110 = inherent#Ref#Ref[T]#set__T_int(sum/1, t122) in
          jump loop116()
        } in
        if t123 {
          join k124() -> unit {
            jump k120()
          } in
          match cur/2 {
            1 => {
              jump loop116()
            },
            3 => {
              jump loop116()
            },
            _ => {
              jump k124()
            },
          }
        } else {
          join k125() -> unit {
            jump k120()
          } in
          match cur/2 {
            5 => {
              jump exit114()
            },
            _ => {
              jump k125()
            },
          }
        }
      } else {
        jump exit114()
      }
    } in
  } in
  jump loop116()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret126(retv127: Ref[int]) -> Ref[int] {
    retv127
  } in
  let t128 = @intrinsic(ref.new)(value/207) in
  jump ret126(t128)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret129(retv130: int) -> int {
    retv130
  } in
  let t131 = @intrinsic(ref.get)(self/208) in
  jump ret129(t131)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret132() -> unit {
    ()
  } in
  let t133 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret132()
}

fn println__T_int(value/1: int) -> unit {
  join ret134() -> unit {
    ()
  } in
  let t135 = trait_impl#ToString#int#to_string(value/1) in
  let t136 = @runtime(core.string_println)(t135) in
  jump ret134()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret137(retv138: string) -> string {
    retv138
  } in
  let t139 = @runtime(core.int_to_string)(self/40) in
  jump ret137(t139)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret140(retv141: bool) -> bool {
    retv141
  } in
  let t142 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret140(t142)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret143(retv144: uint64) -> uint64 {
    retv144
  } in
  let t145 = @runtime(core.int_hash)(self/85) in
  jump ret143(t145)
}