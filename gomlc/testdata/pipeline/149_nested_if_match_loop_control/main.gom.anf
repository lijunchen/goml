fn main() -> unit {
  join ret73() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit74() -> unit {
    let _wild71 = () in
    let t75 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild72 = println__T_int(t75) in
    jump ret73()
  } in
  joinrec {
    join loop76() -> unit {
      let t77 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t78 = (t77 < 7) in
      if t78 {
        let cur/2 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t79 = (cur/2 + 1) in
        let _wild68 = inherent#Ref#Ref[T]#set__T_int(i/0, t79) in
        let t83 = (cur/2 < 5) in
        join k80() -> unit {
          let mtmp69 = () in
          let t81 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t82 = (t81 + cur/2) in
          let _wild70 = inherent#Ref#Ref[T]#set__T_int(sum/1, t82) in
          jump loop76()
        } in
        if t83 {
          join k84() -> unit {
            jump k80()
          } in
          match cur/2 {
            1 => {
              jump loop76()
            },
            3 => {
              jump loop76()
            },
            _ => {
              jump k84()
            },
          }
        } else {
          join k85() -> unit {
            jump k80()
          } in
          match cur/2 {
            5 => {
              jump exit74()
            },
            _ => {
              jump k85()
            },
          }
        }
      } else {
        jump exit74()
      }
    } in
  } in
  jump loop76()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret86(retv87: Ref[int]) -> Ref[int] {
    retv87
  } in
  let t88 = @intrinsic(ref.new)(value/207) in
  jump ret86(t88)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret89(retv90: int) -> int {
    retv90
  } in
  let t91 = @intrinsic(ref.get)(self/208) in
  jump ret89(t91)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret92() -> unit {
    ()
  } in
  let t93 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret92()
}

fn println__T_int(value/1: int) -> unit {
  join ret94() -> unit {
    ()
  } in
  let t95 = trait_impl#ToString#int#to_string(value/1) in
  let t96 = @runtime(core.string_println)(t95) in
  jump ret94()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret97(retv98: string) -> string {
    retv98
  } in
  let t99 = @runtime(core.int_to_string)(self/40) in
  jump ret97(t99)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret100(retv101: bool) -> bool {
    retv101
  } in
  let t102 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret100(t102)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret103(retv104: uint64) -> uint64 {
    retv104
  } in
  let t105 = @runtime(core.int_hash)(self/85) in
  jump ret103(t105)
}