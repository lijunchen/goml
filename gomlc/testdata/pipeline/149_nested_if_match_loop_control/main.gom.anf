fn main() -> unit {
  join inline_cont243(i/0: Ref[int]) -> unit {
    join inline_cont240(sum/1: Ref[int]) -> unit {
      join exit193() -> unit {
        join inline_cont238(t194: int) -> unit {
          let inline236 = trait_impl#ToString#int#to_string(t194) in
          let inline237 = @runtime(core.string_println)(inline236) in
          ()
        } in
        let inline239 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont238(inline239)
      } in
      joinrec {
        join loop195() -> unit {
          join inline_cont233(t196: int) -> unit {
            let t197 = (t196 < 7) in
            if t197 {
              join inline_cont231(cur/2: int) -> unit {
                let t198 = (cur/2 + 1) in
                join inline_cont229(_wild187: unit) -> unit {
                  let t202 = (cur/2 < 5) in
                  join k199() -> unit {
                    join inline_cont227(t200: int) -> unit {
                      let t201 = (t200 + cur/2) in
                      let inline226 = @intrinsic(ref.set)(sum/1, t201) in
                      jump loop195()
                    } in
                    let inline228 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont227(inline228)
                  } in
                  if t202 {
                    match cur/2 {
                      1 => {
                        jump loop195()
                      },
                      3 => {
                        jump loop195()
                      },
                      _ => {
                        jump k199()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit193()
                      },
                      _ => {
                        jump k199()
                      },
                    }
                  }
                } in
                let inline230 = @intrinsic(ref.set)(i/0, t198) in
                jump inline_cont229(())
              } in
              let inline232 = @intrinsic(ref.get)(i/0) in
              jump inline_cont231(inline232)
            } else {
              jump exit193()
            }
          } in
          let inline234 = @intrinsic(ref.get)(i/0) in
          jump inline_cont233(inline234)
        } in
      } in
      jump loop195()
    } in
    let inline241 = 0 in
    let inline242 = @intrinsic(ref.new)(inline241) in
    jump inline_cont240(inline242)
  } in
  let inline244 = 0 in
  let inline245 = @intrinsic(ref.new)(inline244) in
  jump inline_cont243(inline245)
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t207 = @intrinsic(ref.new)(value/273) in
  t207
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t210 = @intrinsic(ref.get)(self/274) in
  t210
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t212 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont246(t214: string) -> unit {
    let t215 = @runtime(core.string_println)(t214) in
    ()
  } in
  let inline247 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont246(inline247)
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t218 = @runtime(core.int_to_string)(self/67) in
  t218
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t221 = (self/101 == other/102) in
  t221
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t224 = @runtime(core.int_hash)(self/127) in
  t224
}