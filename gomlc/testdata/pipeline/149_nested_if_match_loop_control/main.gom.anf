fn main() -> unit {
  join ret157() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit158() -> unit {
    let _wild155 = () in
    let t159 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild156 = println__T_int(t159) in
    jump ret157()
  } in
  joinrec {
    join loop160() -> unit {
      let t161 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t162 = (t161 < 7) in
      if t162 {
        let cur/2 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t163 = (cur/2 + 1) in
        let _wild152 = inherent#Ref#Ref[T]#set__T_int(i/0, t163) in
        let t167 = (cur/2 < 5) in
        join k164() -> unit {
          let mtmp153 = () in
          let t165 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t166 = (t165 + cur/2) in
          let _wild154 = inherent#Ref#Ref[T]#set__T_int(sum/1, t166) in
          jump loop160()
        } in
        if t167 {
          join k168() -> unit {
            jump k164()
          } in
          match cur/2 {
            1 => {
              jump loop160()
            },
            3 => {
              jump loop160()
            },
            _ => {
              jump k168()
            },
          }
        } else {
          join k169() -> unit {
            jump k164()
          } in
          match cur/2 {
            5 => {
              jump exit158()
            },
            _ => {
              jump k169()
            },
          }
        }
      } else {
        jump exit158()
      }
    } in
  } in
  jump loop160()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret170(retv171: Ref[int]) -> Ref[int] {
    retv171
  } in
  let t172 = @intrinsic(ref.new)(value/207) in
  jump ret170(t172)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret173(retv174: int) -> int {
    retv174
  } in
  let t175 = @intrinsic(ref.get)(self/208) in
  jump ret173(t175)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret176() -> unit {
    ()
  } in
  let t177 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret176()
}

fn println__T_int(value/1: int) -> unit {
  join ret178() -> unit {
    ()
  } in
  let t179 = trait_impl#ToString#int#to_string(value/1) in
  let t180 = @runtime(core.string_println)(t179) in
  jump ret178()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret181(retv182: string) -> string {
    retv182
  } in
  let t183 = @runtime(core.int_to_string)(self/40) in
  jump ret181(t183)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret184(retv185: bool) -> bool {
    retv185
  } in
  let t186 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret184(t186)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret187(retv188: uint64) -> uint64 {
    retv188
  } in
  let t189 = @runtime(core.int_hash)(self/85) in
  jump ret187(t189)
}