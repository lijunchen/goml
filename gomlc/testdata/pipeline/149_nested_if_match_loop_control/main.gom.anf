fn main() -> unit {
  join inline_cont467(i/0: Ref[isize]) -> unit {
    join inline_cont464(sum/1: Ref[isize]) -> unit {
      join exit417() -> unit {
        join inline_cont462(t418: isize) -> unit {
          let inline460 = trait_impl#ToString#isize#to_string(t418) in
          let inline461 = @runtime(core.string_println)(inline460) in
          ()
        } in
        let inline463 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont462(inline463)
      } in
      joinrec {
        join loop419() -> unit {
          join inline_cont457(t420: isize) -> unit {
            let t421 = (t420 < 7) in
            if t421 {
              join inline_cont455(cur/2: isize) -> unit {
                let t422 = (cur/2 + 1) in
                join inline_cont453(_wild411: unit) -> unit {
                  let t426 = (cur/2 < 5) in
                  join k423() -> unit {
                    join inline_cont451(t424: isize) -> unit {
                      let t425 = (t424 + cur/2) in
                      let inline450 = @intrinsic(ref.set)(sum/1, t425) in
                      jump loop419()
                    } in
                    let inline452 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont451(inline452)
                  } in
                  if t426 {
                    match cur/2 {
                      1 => {
                        jump loop419()
                      },
                      3 => {
                        jump loop419()
                      },
                      _ => {
                        jump k423()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit417()
                      },
                      _ => {
                        jump k423()
                      },
                    }
                  }
                } in
                let inline454 = @intrinsic(ref.set)(i/0, t422) in
                jump inline_cont453(())
              } in
              let inline456 = @intrinsic(ref.get)(i/0) in
              jump inline_cont455(inline456)
            } else {
              jump exit417()
            }
          } in
          let inline458 = @intrinsic(ref.get)(i/0) in
          jump inline_cont457(inline458)
        } in
      } in
      jump loop419()
    } in
    let inline465 = 0 in
    let inline466 = @intrinsic(ref.new)(inline465) in
    jump inline_cont464(inline466)
  } in
  let inline468 = 0 in
  let inline469 = @intrinsic(ref.new)(inline468) in
  jump inline_cont467(inline469)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/431: isize) -> Ref[isize] {
  let t431 = @intrinsic(ref.new)(value/431) in
  t431
}

fn inherent#Ref#Ref[T]#get__T_isize(self/432: Ref[isize]) -> isize {
  let t434 = @intrinsic(ref.get)(self/432) in
  t434
}

fn inherent#Ref#Ref[T]#set__T_isize(self/433: Ref[isize], value/434: isize) -> unit {
  let t436 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont470(t438: string) -> unit {
    let t439 = @runtime(core.string_println)(t438) in
    ()
  } in
  let inline471 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont470(inline471)
}

fn trait_impl#ToString#isize#to_string(self/151: isize) -> string {
  let t442 = @runtime(core.int_to_string)(self/151) in
  t442
}

fn trait_impl#PartialEq#isize#eq(self/185: isize, other/186: isize) -> bool {
  let t445 = (self/185 == other/186) in
  t445
}

fn trait_impl#Hash#isize#hash(self/211: isize) -> u64 {
  let t448 = @runtime(core.int_hash)(self/211) in
  t448
}