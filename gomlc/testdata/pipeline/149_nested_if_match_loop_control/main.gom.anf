fn main() -> unit {
  join inline_cont228(i/0: Ref[int]) -> unit {
    join inline_cont225(sum/1: Ref[int]) -> unit {
      join exit178() -> unit {
        join inline_cont223(t179: int) -> unit {
          let inline221 = trait_impl#ToString#int#to_string(t179) in
          let inline222 = @runtime(core.string_println)(inline221) in
          ()
        } in
        let inline224 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont223(inline224)
      } in
      joinrec {
        join loop180() -> unit {
          join inline_cont218(t181: int) -> unit {
            let t182 = (t181 < 7) in
            if t182 {
              join inline_cont216(cur/2: int) -> unit {
                let t183 = (cur/2 + 1) in
                join inline_cont214(_wild172: unit) -> unit {
                  let t187 = (cur/2 < 5) in
                  join k184() -> unit {
                    join inline_cont212(t185: int) -> unit {
                      let t186 = (t185 + cur/2) in
                      let inline211 = @intrinsic(ref.set)(sum/1, t186) in
                      jump loop180()
                    } in
                    let inline213 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont212(inline213)
                  } in
                  if t187 {
                    match cur/2 {
                      1 => {
                        jump loop180()
                      },
                      3 => {
                        jump loop180()
                      },
                      _ => {
                        jump k184()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit178()
                      },
                      _ => {
                        jump k184()
                      },
                    }
                  }
                } in
                let inline215 = @intrinsic(ref.set)(i/0, t183) in
                jump inline_cont214(())
              } in
              let inline217 = @intrinsic(ref.get)(i/0) in
              jump inline_cont216(inline217)
            } else {
              jump exit178()
            }
          } in
          let inline219 = @intrinsic(ref.get)(i/0) in
          jump inline_cont218(inline219)
        } in
      } in
      jump loop180()
    } in
    let inline226 = 0 in
    let inline227 = @intrinsic(ref.new)(inline226) in
    jump inline_cont225(inline227)
  } in
  let inline229 = 0 in
  let inline230 = @intrinsic(ref.new)(inline229) in
  jump inline_cont228(inline230)
}

fn inherent#Ref#Ref[T]#new__T_int(value/257: int) -> Ref[int] {
  let t192 = @intrinsic(ref.new)(value/257) in
  t192
}

fn inherent#Ref#Ref[T]#get__T_int(self/258: Ref[int]) -> int {
  let t195 = @intrinsic(ref.get)(self/258) in
  t195
}

fn inherent#Ref#Ref[T]#set__T_int(self/259: Ref[int], value/260: int) -> unit {
  let t197 = @intrinsic(ref.set)(self/259, value/260) in
  ()
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont231(t199: string) -> unit {
    let t200 = @runtime(core.string_println)(t199) in
    ()
  } in
  let inline232 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont231(inline232)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t203 = @runtime(core.int_to_string)(self/69) in
  t203
}

fn trait_impl#PartialEq#int#eq(self/103: int, other/104: int) -> bool {
  let t206 = (self/103 == other/104) in
  t206
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t209 = @runtime(core.int_hash)(self/129) in
  t209
}