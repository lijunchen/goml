fn main() -> unit {
  join inline_cont464(i/0: Ref[int]) -> unit {
    join inline_cont461(sum/1: Ref[int]) -> unit {
      join exit414() -> unit {
        join inline_cont459(t415: int) -> unit {
          let inline457 = trait_impl#ToString#int#to_string(t415) in
          let inline458 = @runtime(core.string_println)(inline457) in
          ()
        } in
        let inline460 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont459(inline460)
      } in
      joinrec {
        join loop416() -> unit {
          join inline_cont454(t417: int) -> unit {
            let t418 = (t417 < 7) in
            if t418 {
              join inline_cont452(cur/2: int) -> unit {
                let t419 = (cur/2 + 1) in
                join inline_cont450(_wild408: unit) -> unit {
                  let t423 = (cur/2 < 5) in
                  join k420() -> unit {
                    join inline_cont448(t421: int) -> unit {
                      let t422 = (t421 + cur/2) in
                      let inline447 = @intrinsic(ref.set)(sum/1, t422) in
                      jump loop416()
                    } in
                    let inline449 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont448(inline449)
                  } in
                  if t423 {
                    match cur/2 {
                      1 => {
                        jump loop416()
                      },
                      3 => {
                        jump loop416()
                      },
                      _ => {
                        jump k420()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit414()
                      },
                      _ => {
                        jump k420()
                      },
                    }
                  }
                } in
                let inline451 = @intrinsic(ref.set)(i/0, t419) in
                jump inline_cont450(())
              } in
              let inline453 = @intrinsic(ref.get)(i/0) in
              jump inline_cont452(inline453)
            } else {
              jump exit414()
            }
          } in
          let inline455 = @intrinsic(ref.get)(i/0) in
          jump inline_cont454(inline455)
        } in
      } in
      jump loop416()
    } in
    let inline462 = 0 in
    let inline463 = @intrinsic(ref.new)(inline462) in
    jump inline_cont461(inline463)
  } in
  let inline465 = 0 in
  let inline466 = @intrinsic(ref.new)(inline465) in
  jump inline_cont464(inline466)
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t428 = @intrinsic(ref.new)(value/431) in
  t428
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t431 = @intrinsic(ref.get)(self/432) in
  t431
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t433 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont467(t435: string) -> unit {
    let t436 = @runtime(core.string_println)(t435) in
    ()
  } in
  let inline468 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont467(inline468)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t439 = @runtime(core.int_to_string)(self/151) in
  t439
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t442 = (self/185 == other/186) in
  t442
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t445 = @runtime(core.int_hash)(self/211) in
  t445
}