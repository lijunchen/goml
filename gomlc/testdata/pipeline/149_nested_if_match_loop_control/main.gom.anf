fn main() -> unit {
  join inline_cont192(i/0: Ref[int]) -> unit {
    join inline_cont189(sum/1: Ref[int]) -> unit {
      join exit142() -> unit {
        let _wild139 = () in
        join inline_cont187(t143: int) -> unit {
          let inline185 = trait_impl#ToString#int#to_string(t143) in
          let inline186 = @runtime(core.string_println)(inline185) in
          ()
        } in
        let inline188 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont187(inline188)
      } in
      joinrec {
        join loop144() -> unit {
          join inline_cont182(t145: int) -> unit {
            let t146 = (t145 < 7) in
            if t146 {
              join inline_cont180(cur/2: int) -> unit {
                let t147 = (cur/2 + 1) in
                join inline_cont178(_wild136: unit) -> unit {
                  let t151 = (cur/2 < 5) in
                  join k148() -> unit {
                    let mtmp137 = () in
                    join inline_cont176(t149: int) -> unit {
                      let t150 = (t149 + cur/2) in
                      let inline175 = @intrinsic(ref.set)(sum/1, t150) in
                      jump loop144()
                    } in
                    let inline177 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont176(inline177)
                  } in
                  if t151 {
                    match cur/2 {
                      1 => {
                        jump loop144()
                      },
                      3 => {
                        jump loop144()
                      },
                      _ => {
                        jump k148()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit142()
                      },
                      _ => {
                        jump k148()
                      },
                    }
                  }
                } in
                let inline179 = @intrinsic(ref.set)(i/0, t147) in
                jump inline_cont178(())
              } in
              let inline181 = @intrinsic(ref.get)(i/0) in
              jump inline_cont180(inline181)
            } else {
              jump exit142()
            }
          } in
          let inline183 = @intrinsic(ref.get)(i/0) in
          jump inline_cont182(inline183)
        } in
      } in
      jump loop144()
    } in
    let inline190 = 0 in
    let inline191 = @intrinsic(ref.new)(inline190) in
    jump inline_cont189(inline191)
  } in
  let inline193 = 0 in
  let inline194 = @intrinsic(ref.new)(inline193) in
  jump inline_cont192(inline194)
}

fn inherent#Ref#Ref[T]#new__T_int(value/232: int) -> Ref[int] {
  let t156 = @intrinsic(ref.new)(value/232) in
  t156
}

fn inherent#Ref#Ref[T]#get__T_int(self/233: Ref[int]) -> int {
  let t159 = @intrinsic(ref.get)(self/233) in
  t159
}

fn inherent#Ref#Ref[T]#set__T_int(self/234: Ref[int], value/235: int) -> unit {
  let t161 = @intrinsic(ref.set)(self/234, value/235) in
  ()
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont195(t163: string) -> unit {
    let t164 = @runtime(core.string_println)(t163) in
    ()
  } in
  let inline196 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont195(inline196)
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t167 = @runtime(core.int_to_string)(self/69) in
  t167
}

fn trait_impl#Eq#int#eq(self/103: int, other/104: int) -> bool {
  let t170 = @intrinsic(eq.primitive)(self/103, other/104) in
  t170
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t173 = @runtime(core.int_hash)(self/129) in
  t173
}