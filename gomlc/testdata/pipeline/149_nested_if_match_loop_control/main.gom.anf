fn main() -> unit {
  join inline_cont211(i/0: Ref[int]) -> unit {
    join inline_cont208(sum/1: Ref[int]) -> unit {
      join exit161() -> unit {
        let _wild158 = () in
        join inline_cont206(t162: int) -> unit {
          let inline204 = trait_impl#ToString#int#to_string(t162) in
          let inline205 = @runtime(core.string_println)(inline204) in
          ()
        } in
        let inline207 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont206(inline207)
      } in
      joinrec {
        join loop163() -> unit {
          join inline_cont201(t164: int) -> unit {
            let t165 = (t164 < 7) in
            if t165 {
              join inline_cont199(cur/2: int) -> unit {
                let t166 = (cur/2 + 1) in
                join inline_cont197(_wild155: unit) -> unit {
                  let t170 = (cur/2 < 5) in
                  join k167() -> unit {
                    let mtmp156 = () in
                    join inline_cont195(t168: int) -> unit {
                      let t169 = (t168 + cur/2) in
                      let inline194 = @intrinsic(ref.set)(sum/1, t169) in
                      jump loop163()
                    } in
                    let inline196 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont195(inline196)
                  } in
                  if t170 {
                    match cur/2 {
                      1 => {
                        jump loop163()
                      },
                      3 => {
                        jump loop163()
                      },
                      _ => {
                        jump k167()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit161()
                      },
                      _ => {
                        jump k167()
                      },
                    }
                  }
                } in
                let inline198 = @intrinsic(ref.set)(i/0, t166) in
                jump inline_cont197(())
              } in
              let inline200 = @intrinsic(ref.get)(i/0) in
              jump inline_cont199(inline200)
            } else {
              jump exit161()
            }
          } in
          let inline202 = @intrinsic(ref.get)(i/0) in
          jump inline_cont201(inline202)
        } in
      } in
      jump loop163()
    } in
    let inline209 = 0 in
    let inline210 = @intrinsic(ref.new)(inline209) in
    jump inline_cont208(inline210)
  } in
  let inline212 = 0 in
  let inline213 = @intrinsic(ref.new)(inline212) in
  jump inline_cont211(inline213)
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  let t175 = @intrinsic(ref.new)(value/207) in
  t175
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  let t178 = @intrinsic(ref.get)(self/208) in
  t178
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  let t180 = @intrinsic(ref.set)(self/209, value/210) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont214(t182: string) -> unit {
    let t183 = @runtime(core.string_println)(t182) in
    ()
  } in
  let inline215 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont214(inline215)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  let t186 = @runtime(core.int_to_string)(self/40) in
  t186
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  let t189 = @intrinsic(eq.primitive)(self/59, other/60) in
  t189
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  let t192 = @runtime(core.int_hash)(self/85) in
  t192
}