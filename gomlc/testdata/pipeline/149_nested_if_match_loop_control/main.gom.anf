fn main() -> unit {
  join inline_cont895(i/0: Ref[isize]) -> unit {
    join inline_cont892(sum/1: Ref[isize]) -> unit {
      join exit802() -> unit {
        join inline_cont890(t803: isize) -> unit {
          let inline888 = trait_impl#ToString#isize#to_string(t803) in
          let inline889 = @runtime(core.string_println)(inline888) in
          ()
        } in
        let inline891 = @intrinsic(ref.get)(sum/1) in
        jump inline_cont890(inline891)
      } in
      joinrec {
        join loop804() -> unit {
          join inline_cont885(t805: isize) -> unit {
            let t806 = (t805 < 7) in
            if t806 {
              join inline_cont883(cur/2: isize) -> unit {
                let t807 = (cur/2 + 1) in
                join inline_cont881(_wild796: unit) -> unit {
                  let t811 = (cur/2 < 5) in
                  join k808() -> unit {
                    join inline_cont879(t809: isize) -> unit {
                      let t810 = (t809 + cur/2) in
                      let inline878 = @intrinsic(ref.set)(sum/1, t810) in
                      jump loop804()
                    } in
                    let inline880 = @intrinsic(ref.get)(sum/1) in
                    jump inline_cont879(inline880)
                  } in
                  if t811 {
                    match cur/2 {
                      1 => {
                        jump loop804()
                      },
                      3 => {
                        jump loop804()
                      },
                      _ => {
                        jump k808()
                      },
                    }
                  } else {
                    match cur/2 {
                      5 => {
                        jump exit802()
                      },
                      _ => {
                        jump k808()
                      },
                    }
                  }
                } in
                let inline882 = @intrinsic(ref.set)(i/0, t807) in
                jump inline_cont881(())
              } in
              let inline884 = @intrinsic(ref.get)(i/0) in
              jump inline_cont883(inline884)
            } else {
              jump exit802()
            }
          } in
          let inline886 = @intrinsic(ref.get)(i/0) in
          jump inline_cont885(inline886)
        } in
      } in
      jump loop804()
    } in
    let inline893 = 0 in
    let inline894 = @intrinsic(ref.new)(inline893) in
    jump inline_cont892(inline894)
  } in
  let inline896 = 0 in
  let inline897 = @intrinsic(ref.new)(inline896) in
  jump inline_cont895(inline897)
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t816 = @intrinsic(ref.new)(value/684) in
  t816
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t819 = @intrinsic(ref.get)(self/685) in
  t819
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t821 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont898(t823: string) -> unit {
    let t824 = @runtime(core.string_println)(t823) in
    ()
  } in
  let inline899 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont898(inline899)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline901 = (self/404 as i64) in
  let inline902 = signed_decimal_string(inline901) in
  inline902
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t830 = (value/222 as i64) in
  let inline904 = (t830 < 0) in
  if inline904 {
    let inline905 = (t830 as u64) in
    let inline906 = (0 - inline905) in
    let inline907 = decimal_string(inline906) in
    let inline908 = ("-" + inline907) in
    inline908
  } else {
    let inline909 = (t830 as u64) in
    let inline910 = decimal_string(inline909) in
    inline910
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t836 = (value/214 < 0) in
  if t836 {
    let t837 = (value/214 as u64) in
    let t838 = (0 - t837) in
    let t839 = decimal_string(t838) in
    let t840 = ("-" + t839) in
    t840
  } else {
    let t841 = (value/214 as u64) in
    let t842 = decimal_string(t841) in
    t842
  }
}

fn decimal_string(value/208: u64) -> string {
  let t865 = (value/208 == 0) in
  join k845() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit846() -> string {
      join inline_cont919(t847: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t847) in
        let offset/212 = 0 in
        join exit848() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop849() -> string {
            join inline_cont917(t850: isize) -> string {
              let t851 = (offset/212 < t850) in
              if t851 {
                join inline_cont915(t852: isize) -> string {
                  let t853 = (t852 - offset/212) in
                  let t854 = (t853 - 1) in
                  let t855 = @intrinsic(vec.get)(reversed/209, t854) in
                  join inline_cont913(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t856 = (compound_old358 + compound_value359) in
                    let t857 = offset/212 = t856 in
                    jump loop849()
                  } in
                  let inline914 = @intrinsic(vec.push)(bytes/211, t855) in
                  jump inline_cont913(())
                } in
                let inline916 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont915(inline916)
              } else {
                jump exit848()
              }
            } in
            let inline918 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont917(inline918)
          } in
        } in
        jump loop849()
      } in
      let inline920 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont919(inline920)
    } in
    joinrec {
      join loop858() -> string {
        let t859 = (remaining/210 > 0) in
        if t859 {
          let t860 = (remaining/210 % 10) in
          let t861 = (t860 as u8) in
          let t862 = (t861 + 48) in
          join inline_cont911(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t863 = (compound_old353 / compound_value354) in
            let t864 = remaining/210 = t863 in
            jump loop858()
          } in
          let inline912 = @intrinsic(vec.push)(reversed/209, t862) in
          jump inline_cont911(())
        } else {
          jump exit846()
        }
      } in
    } in
    jump loop858()
  } in
  if t865 {
    "0"
  } else {
    jump k845()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t867 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t870 = @intrinsic(vec.len)(self/526) in
  t870
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t873 = (self/438 == other/439) in
  t873
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t876 = @runtime(core.int_hash)(self/464) in
  t876
}