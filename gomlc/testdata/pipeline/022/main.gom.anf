fn match_string(s/0: string) -> i32 {
  match s/0 {
    "hello" => {
      1
    },
    "world" => {
      2
    },
    _ => {
      3
    },
  }
}

fn wildcard_position(s/1: string) -> i32 {
  4
}

fn repeated_string(s/2: string) -> i32 {
  match s/2 {
    "hello" => {
      6
    },
    _ => {
      8
    },
  }
}

fn main() -> unit {
  join inline_cont488(t427: i32) -> unit {
    join inline_cont486(t428: string) -> unit {
      join inline_cont483(mtmp411: unit) -> unit {
        join inline_cont481(t429: i32) -> unit {
          join inline_cont479(t430: string) -> unit {
            join inline_cont476(mtmp412: unit) -> unit {
              join inline_cont474(t431: i32) -> unit {
                join inline_cont472(t432: string) -> unit {
                  join inline_cont469(mtmp413: unit) -> unit {
                    join inline_cont467(t433: i32) -> unit {
                      join inline_cont465(t434: string) -> unit {
                        join inline_cont462(mtmp414: unit) -> unit {
                          join inline_cont460(t435: i32) -> unit {
                            join inline_cont458(t436: string) -> unit {
                              join inline_cont455(mtmp415: unit) -> unit {
                                join inline_cont453(t437: i32) -> unit {
                                  join inline_cont451(t438: string) -> unit {
                                    let inline449 = trait_impl#ToString#string#to_string(t438) in
                                    let inline450 = @runtime(core.string_println)(inline449) in
                                    ()
                                  } in
                                  let inline452 = @runtime(core.int32_to_string)(t437) in
                                  jump inline_cont451(inline452)
                                } in
                                let inline454 = "mars" in
                                match inline454 {
                                  "hello" => {
                                    jump inline_cont453(6)
                                  },
                                  _ => {
                                    jump inline_cont453(8)
                                  },
                                }
                              } in
                              let inline456 = trait_impl#ToString#string#to_string(t436) in
                              let inline457 = @runtime(core.string_println)(inline456) in
                              jump inline_cont455(())
                            } in
                            let inline459 = @runtime(core.int32_to_string)(t435) in
                            jump inline_cont458(inline459)
                          } in
                          let inline461 = "hello" in
                          match inline461 {
                            "hello" => {
                              jump inline_cont460(6)
                            },
                            _ => {
                              jump inline_cont460(8)
                            },
                          }
                        } in
                        let inline463 = trait_impl#ToString#string#to_string(t434) in
                        let inline464 = @runtime(core.string_println)(inline463) in
                        jump inline_cont462(())
                      } in
                      let inline466 = @runtime(core.int32_to_string)(t433) in
                      jump inline_cont465(inline466)
                    } in
                    jump inline_cont467(4)
                  } in
                  let inline470 = trait_impl#ToString#string#to_string(t432) in
                  let inline471 = @runtime(core.string_println)(inline470) in
                  jump inline_cont469(())
                } in
                let inline473 = @runtime(core.int32_to_string)(t431) in
                jump inline_cont472(inline473)
              } in
              jump inline_cont474(4)
            } in
            let inline477 = trait_impl#ToString#string#to_string(t430) in
            let inline478 = @runtime(core.string_println)(inline477) in
            jump inline_cont476(())
          } in
          let inline480 = @runtime(core.int32_to_string)(t429) in
          jump inline_cont479(inline480)
        } in
        let inline482 = "planet" in
        match inline482 {
          "hello" => {
            jump inline_cont481(1)
          },
          "world" => {
            jump inline_cont481(2)
          },
          _ => {
            jump inline_cont481(3)
          },
        }
      } in
      let inline484 = trait_impl#ToString#string#to_string(t428) in
      let inline485 = @runtime(core.string_println)(inline484) in
      jump inline_cont483(())
    } in
    let inline487 = @runtime(core.int32_to_string)(t427) in
    jump inline_cont486(inline487)
  } in
  let inline489 = "hello" in
  match inline489 {
    "hello" => {
      jump inline_cont488(1)
    },
    "world" => {
      jump inline_cont488(2)
    },
    _ => {
      jump inline_cont488(3)
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont490(t441: string) -> unit {
    let t442 = @runtime(core.string_println)(t441) in
    ()
  } in
  jump inline_cont490(value/1)
}

fn inherent#i32#i32#to_string(self/33: i32) -> string {
  let t445 = @runtime(core.int32_to_string)(self/33) in
  t445
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}