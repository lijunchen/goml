fn classify(n/0: i32) -> string {
  match n/0 {
    -1 => {
      "minus one"
    },
    0 => {
      "zero"
    },
    1 => {
      "one"
    },
    _ => {
      "other"
    },
  }
}

fn main() -> unit {
  join inline_cont832(t805: string) -> unit {
    join inline_cont829(_wild796: unit) -> unit {
      join inline_cont827(t806: string) -> unit {
        join inline_cont824(_wild797: unit) -> unit {
          join inline_cont822(t807: string) -> unit {
            join inline_cont819(_wild798: unit) -> unit {
              join inline_cont817(t808: string) -> unit {
                let inline815 = trait_impl#ToString#string#to_string(t808) in
                let inline816 = @runtime(core.string_println)(inline815) in
                ()
              } in
              let inline818 = 42 in
              match inline818 {
                -1 => {
                  jump inline_cont817("minus one")
                },
                0 => {
                  jump inline_cont817("zero")
                },
                1 => {
                  jump inline_cont817("one")
                },
                _ => {
                  jump inline_cont817("other")
                },
              }
            } in
            let inline820 = trait_impl#ToString#string#to_string(t807) in
            let inline821 = @runtime(core.string_println)(inline820) in
            jump inline_cont819(())
          } in
          let inline823 = 1 in
          match inline823 {
            -1 => {
              jump inline_cont822("minus one")
            },
            0 => {
              jump inline_cont822("zero")
            },
            1 => {
              jump inline_cont822("one")
            },
            _ => {
              jump inline_cont822("other")
            },
          }
        } in
        let inline825 = trait_impl#ToString#string#to_string(t806) in
        let inline826 = @runtime(core.string_println)(inline825) in
        jump inline_cont824(())
      } in
      let inline828 = 0 in
      match inline828 {
        -1 => {
          jump inline_cont827("minus one")
        },
        0 => {
          jump inline_cont827("zero")
        },
        1 => {
          jump inline_cont827("one")
        },
        _ => {
          jump inline_cont827("other")
        },
      }
    } in
    let inline830 = trait_impl#ToString#string#to_string(t805) in
    let inline831 = @runtime(core.string_println)(inline830) in
    jump inline_cont829(())
  } in
  let inline833 = -1 in
  match inline833 {
    -1 => {
      jump inline_cont832("minus one")
    },
    0 => {
      jump inline_cont832("zero")
    },
    1 => {
      jump inline_cont832("one")
    },
    _ => {
      jump inline_cont832("other")
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont834(t810: string) -> unit {
    let t811 = @runtime(core.string_println)(t810) in
    ()
  } in
  jump inline_cont834(value/1)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}