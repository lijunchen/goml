fn classify(n/0: int32) -> string {
  match n/0 {
    -1 => {
      "minus one"
    },
    0 => {
      "zero"
    },
    1 => {
      "one"
    },
    _ => {
      "other"
    },
  }
}

fn main() -> unit {
  join inline_cont444(t417: string) -> unit {
    join inline_cont441(_wild408: unit) -> unit {
      join inline_cont439(t418: string) -> unit {
        join inline_cont436(_wild409: unit) -> unit {
          join inline_cont434(t419: string) -> unit {
            join inline_cont431(_wild410: unit) -> unit {
              join inline_cont429(t420: string) -> unit {
                let inline427 = trait_impl#ToString#string#to_string(t420) in
                let inline428 = @runtime(core.string_println)(inline427) in
                ()
              } in
              let inline430 = 42 in
              match inline430 {
                -1 => {
                  jump inline_cont429("minus one")
                },
                0 => {
                  jump inline_cont429("zero")
                },
                1 => {
                  jump inline_cont429("one")
                },
                _ => {
                  jump inline_cont429("other")
                },
              }
            } in
            let inline432 = trait_impl#ToString#string#to_string(t419) in
            let inline433 = @runtime(core.string_println)(inline432) in
            jump inline_cont431(())
          } in
          let inline435 = 1 in
          match inline435 {
            -1 => {
              jump inline_cont434("minus one")
            },
            0 => {
              jump inline_cont434("zero")
            },
            1 => {
              jump inline_cont434("one")
            },
            _ => {
              jump inline_cont434("other")
            },
          }
        } in
        let inline437 = trait_impl#ToString#string#to_string(t418) in
        let inline438 = @runtime(core.string_println)(inline437) in
        jump inline_cont436(())
      } in
      let inline440 = 0 in
      match inline440 {
        -1 => {
          jump inline_cont439("minus one")
        },
        0 => {
          jump inline_cont439("zero")
        },
        1 => {
          jump inline_cont439("one")
        },
        _ => {
          jump inline_cont439("other")
        },
      }
    } in
    let inline442 = trait_impl#ToString#string#to_string(t417) in
    let inline443 = @runtime(core.string_println)(inline442) in
    jump inline_cont441(())
  } in
  let inline445 = -1 in
  match inline445 {
    -1 => {
      jump inline_cont444("minus one")
    },
    0 => {
      jump inline_cont444("zero")
    },
    1 => {
      jump inline_cont444("one")
    },
    _ => {
      jump inline_cont444("other")
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont446(t422: string) -> unit {
    let t423 = @runtime(core.string_println)(t422) in
    ()
  } in
  jump inline_cont446(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}