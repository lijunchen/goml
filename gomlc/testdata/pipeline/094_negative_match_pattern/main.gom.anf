fn classify(n/0: i32) -> string {
  match n/0 {
    -1 => {
      "minus one"
    },
    0 => {
      "zero"
    },
    1 => {
      "one"
    },
    _ => {
      "other"
    },
  }
}

fn main() -> unit {
  join inline_cont0(t0: string) -> unit {
    join inline_cont1(_wild0: unit) -> unit {
      join inline_cont2(t1: string) -> unit {
        join inline_cont3(_wild1: unit) -> unit {
          join inline_cont4(t2: string) -> unit {
            join inline_cont5(_wild2: unit) -> unit {
              join inline_cont6(t3: string) -> unit {
                let inline0 = trait_impl#ToString#string#to_string(t3) in
                let inline1 = @runtime(core.string_println)(inline0) in
                ()
              } in
              let inline2 = 42 in
              match inline2 {
                -1 => {
                  jump inline_cont6("minus one")
                },
                0 => {
                  jump inline_cont6("zero")
                },
                1 => {
                  jump inline_cont6("one")
                },
                _ => {
                  jump inline_cont6("other")
                },
              }
            } in
            let inline3 = trait_impl#ToString#string#to_string(t2) in
            let inline4 = @runtime(core.string_println)(inline3) in
            jump inline_cont5(())
          } in
          let inline5 = 1 in
          match inline5 {
            -1 => {
              jump inline_cont4("minus one")
            },
            0 => {
              jump inline_cont4("zero")
            },
            1 => {
              jump inline_cont4("one")
            },
            _ => {
              jump inline_cont4("other")
            },
          }
        } in
        let inline6 = trait_impl#ToString#string#to_string(t1) in
        let inline7 = @runtime(core.string_println)(inline6) in
        jump inline_cont3(())
      } in
      let inline8 = 0 in
      match inline8 {
        -1 => {
          jump inline_cont2("minus one")
        },
        0 => {
          jump inline_cont2("zero")
        },
        1 => {
          jump inline_cont2("one")
        },
        _ => {
          jump inline_cont2("other")
        },
      }
    } in
    let inline9 = trait_impl#ToString#string#to_string(t0) in
    let inline10 = @runtime(core.string_println)(inline9) in
    jump inline_cont1(())
  } in
  let inline11 = -1 in
  match inline11 {
    -1 => {
      jump inline_cont0("minus one")
    },
    0 => {
      jump inline_cont0("zero")
    },
    1 => {
      jump inline_cont0("one")
    },
    _ => {
      jump inline_cont0("other")
    },
  }
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}