fn classify(n/0: int32) -> string {
  match n/0 {
    -1 => {
      "minus one"
    },
    0 => {
      "zero"
    },
    1 => {
      "one"
    },
    _ => {
      "other"
    },
  }
}

fn main() -> unit {
  join inline_cont447(t420: string) -> unit {
    join inline_cont444(_wild411: unit) -> unit {
      join inline_cont442(t421: string) -> unit {
        join inline_cont439(_wild412: unit) -> unit {
          join inline_cont437(t422: string) -> unit {
            join inline_cont434(_wild413: unit) -> unit {
              join inline_cont432(t423: string) -> unit {
                let inline430 = trait_impl#ToString#string#to_string(t423) in
                let inline431 = @runtime(core.string_println)(inline430) in
                ()
              } in
              let inline433 = 42 in
              match inline433 {
                -1 => {
                  jump inline_cont432("minus one")
                },
                0 => {
                  jump inline_cont432("zero")
                },
                1 => {
                  jump inline_cont432("one")
                },
                _ => {
                  jump inline_cont432("other")
                },
              }
            } in
            let inline435 = trait_impl#ToString#string#to_string(t422) in
            let inline436 = @runtime(core.string_println)(inline435) in
            jump inline_cont434(())
          } in
          let inline438 = 1 in
          match inline438 {
            -1 => {
              jump inline_cont437("minus one")
            },
            0 => {
              jump inline_cont437("zero")
            },
            1 => {
              jump inline_cont437("one")
            },
            _ => {
              jump inline_cont437("other")
            },
          }
        } in
        let inline440 = trait_impl#ToString#string#to_string(t421) in
        let inline441 = @runtime(core.string_println)(inline440) in
        jump inline_cont439(())
      } in
      let inline443 = 0 in
      match inline443 {
        -1 => {
          jump inline_cont442("minus one")
        },
        0 => {
          jump inline_cont442("zero")
        },
        1 => {
          jump inline_cont442("one")
        },
        _ => {
          jump inline_cont442("other")
        },
      }
    } in
    let inline445 = trait_impl#ToString#string#to_string(t420) in
    let inline446 = @runtime(core.string_println)(inline445) in
    jump inline_cont444(())
  } in
  let inline448 = -1 in
  match inline448 {
    -1 => {
      jump inline_cont447("minus one")
    },
    0 => {
      jump inline_cont447("zero")
    },
    1 => {
      jump inline_cont447("one")
    },
    _ => {
      jump inline_cont447("other")
    },
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont449(t425: string) -> unit {
    let t426 = @runtime(core.string_println)(t425) in
    ()
  } in
  jump inline_cont449(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}