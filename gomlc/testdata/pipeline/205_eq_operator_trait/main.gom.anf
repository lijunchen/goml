fn trait_impl#PartialEq#AlwaysDifferent#eq(
  self/0: AlwaysDifferent,
  other/0: AlwaysDifferent
) -> bool {
  false
}

fn main() -> () {
  join inline_cont0(t0: bool) -> () {
    join inline_cont1(_wild0: ()) -> () {
      join inline_cont2(t1: bool) -> () {
        let t2 = (!t1) in
        join inline_cont3(_wild1: ()) -> () {
          join inline_cont4(t3: bool) -> () {
            join k0(jp0: bool) -> () {
              join inline_cont5(_wild2: ()) -> () {
                let t4 = AlwaysDifferent { value: 1 } in
                let t5 = AlwaysDifferent { value: 2 } in
                let left_array/0 = [t4, t5] in
                let t6 = AlwaysDifferent { value: 1 } in
                let t7 = AlwaysDifferent { value: 2 } in
                let right_array/0 = [t6, t7] in
                let t8 = @intrinsic(array.get)(left_array/0, 0) in
                let t9 = @intrinsic(array.get)(right_array/0, 0) in
                join inline_cont6(t10: bool) -> () {
                  join k1(jp1: bool) -> () {
                    let inline0 = trait_impl#ToString#bool#to_string(jp1) in
                    let inline1 = @runtime(core.string_println)(inline0) in
                    ()
                  } in
                  if t10 {
                    let t11 = @intrinsic(array.get)(left_array/0, 1) in
                    let t12 = @intrinsic(array.get)(right_array/0, 1) in
                    jump k1(false)
                  } else {
                    jump k1(false)
                  }
                } in
                jump inline_cont6(false)
              } in
              let inline2 = trait_impl#ToString#bool#to_string(jp0) in
              let inline3 = @runtime(core.string_println)(inline2) in
              jump inline_cont5(())
            } in
            if t3 {
              let t13 = 2 in
              let t14 = 2 in
              let inline4 = (t13 == t14) in
              jump k0(inline4)
            } else {
              jump k0(false)
            }
          } in
          jump inline_cont4(false)
        } in
        let inline5 = trait_impl#ToString#bool#to_string(t2) in
        let inline6 = @runtime(core.string_println)(inline5) in
        jump inline_cont3(())
      } in
      jump inline_cont2(false)
    } in
    let inline7 = trait_impl#ToString#bool#to_string(t0) in
    let inline8 = @runtime(core.string_println)(inline7) in
    jump inline_cont1(())
  } in
  jump inline_cont0(false)
}

fn println__T_bool(value/0: bool) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#PartialEq#isize#eq(self/0: isize, other/0: isize) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}