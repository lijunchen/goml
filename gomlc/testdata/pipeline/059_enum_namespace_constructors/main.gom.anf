fn color_is_red(color/0: Color) -> bool {
  join ret69(retv70: bool) -> bool {
    retv70
  } in
  join k71(jp72: bool) -> bool {
    jump ret69(jp72)
  } in
  match color/0 {
    Tag_0 => {
      jump k71(true)
    },
    Tag_1 => {
      jump k71(false)
    },
  }
}

fn toggle_signal(signal/1: Signal) -> Signal {
  join ret73(retv74: Signal) -> Signal {
    retv74
  } in
  join k75(jp76: Signal) -> Signal {
    jump ret73(jp76)
  } in
  match signal/1 {
    Tag_0 => {
      jump k75(Tag_1)
    },
    Tag_1 => {
      jump k75(Tag_0)
    },
  }
}

fn main() -> Signal {
  join ret77(retv78: Signal) -> Signal {
    retv78
  } in
  let current/2 = Tag_0 in
  let mtmp68 = color_is_red(current/2) in
  let t79 = toggle_signal(Tag_0) in
  jump ret77(t79)
}