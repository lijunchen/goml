fn main() -> unit {
    let a/0: isize = 1;
    let a/1: isize = (a/0 : isize) + 2;
    let a/2: isize = (a/1 : isize) + 3;
    let a/3: isize = (a/2 : isize) + 4;
    (println : (string) -> unit)((isize::to_string : (isize) -> string)((a/3 : isize)))
}