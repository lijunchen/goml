fn double(x/0: i32) -> i32 {
  (x/0 * 2)
}

fn increment(x/0: i32) -> i32 {
  (x/0 + 1)
}

fn apply_once(f/0: (i32) -> i32, value/0: i32) -> i32 {
  f/0(value/0)
}

fn compose(f/0: (i32) -> i32, g/0: (i32) -> i32, value/0: i32) -> i32 {
  f/0(g/0(value/0))
}

fn main() -> unit {
  let local/0 = double in
  let first/0 = apply_once(local/0, 4) in
  let composed/0 = compose(double, increment, first/0) in
  let closure_apply/0 = |value/0| apply_once(increment, value/0) in
  let closure_result/0 = closure_apply/0(composed/0) in
  let global_invoker/0 = |func_to_call/0, value/1| apply_once(func_to_call/0, value/1) in
  let invoked_with_global/0 = global_invoker/0(double, 3) in
  let composer_closure/0 = |value/2| compose(double, increment, value/2) in
  let composed_by_closure/0 = composer_closure/0(5) in
  let printer/0 = println__T_string in
  let mtmp0 = printer/0(inherent#i32#i32#to_string(composed/0)) in
  {
    let mtmp1 = printer/0(inherent#i32#i32#to_string(closure_result/0)) in
    {
      let mtmp2 = printer/0(inherent#i32#i32#to_string(invoked_with_global/0)) in
      {
        let mtmp3 = printer/0(inherent#i32#i32#to_string(composed_by_closure/0)) in
        {
          ()
        }
      }
    }
  }
}

fn println__T_string(value/0: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      {
        ("-" + decimal_string((0 - (value/0 as u64))))
      }
  } else {
      {
        decimal_string((value/0 as u64))
      }
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        {
          return "0"
        }
    } else {
        {
          ()
        }
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        {
          let _wild2 =
            inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
          let _wild3 =
            {
              let compound_old0 = remaining/0 in
              let compound_value0 = 10 in
              remaining/0 = (compound_old0 / compound_value0)
            } in
        }
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        {
          let _wild5 =
            inherent#Vec#Vec[T]#push__T_u0(
              bytes/0,
              @intrinsic(vec.get)(
                reversed/0,
                ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
              )
            ) in
          let _wild6 =
            {
              let compound_old1 = offset/0 in
              let compound_value1 = 1 in
              offset/0 = (compound_old1 + compound_value1)
            } in
        }
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  {
    let x0 = mtmp0.1 in
    {
      let result/0 = x0 in
      {
        result/0
      }
    }
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}