fn main() -> () {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/0 = "line one\n\nline three" in
  join inline_cont0(_wild0: ()) -> () {
    let inline0 = trait_impl#ToString#string#to_string(trailing_blank/0) in
    let inline1 = @runtime(core.string_println)(inline0) in
    ()
  } in
  let inline2 = trait_impl#ToString#string#to_string(poem/0) in
  let inline3 = @runtime(core.string_println)(inline2) in
  jump inline_cont0(())
}

fn println__T_string(value/0: string) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}