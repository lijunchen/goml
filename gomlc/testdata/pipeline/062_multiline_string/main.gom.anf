fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont198(_wild187: unit) -> unit {
    let inline196 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline197 = @runtime(core.string_println)(inline196) in
    ()
  } in
  let inline199 = trait_impl#ToString#string#to_string(poem/0) in
  let inline200 = @runtime(core.string_println)(inline199) in
  jump inline_cont198(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont201(t191: string) -> unit {
    let t192 = @runtime(core.string_println)(t191) in
    ()
  } in
  jump inline_cont201(value/1)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}