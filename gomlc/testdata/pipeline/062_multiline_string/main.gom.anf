fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont422(_wild411: unit) -> unit {
    let inline420 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline421 = @runtime(core.string_println)(inline420) in
    ()
  } in
  let inline423 = trait_impl#ToString#string#to_string(poem/0) in
  let inline424 = @runtime(core.string_println)(inline423) in
  jump inline_cont422(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont425(t415: string) -> unit {
    let t416 = @runtime(core.string_println)(t415) in
    ()
  } in
  jump inline_cont425(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}