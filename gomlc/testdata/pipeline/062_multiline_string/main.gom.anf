fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont183(_wild172: unit) -> unit {
    let inline181 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline182 = @runtime(core.string_println)(inline181) in
    ()
  } in
  let inline184 = trait_impl#ToString#string#to_string(poem/0) in
  let inline185 = @runtime(core.string_println)(inline184) in
  jump inline_cont183(())
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont186(t176: string) -> unit {
    let t177 = @runtime(core.string_println)(t176) in
    ()
  } in
  jump inline_cont186(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}