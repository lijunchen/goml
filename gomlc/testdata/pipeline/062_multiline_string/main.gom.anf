fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont188(_wild177: unit) -> unit {
    let inline186 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline187 = @runtime(core.string_println)(inline186) in
    ()
  } in
  let inline189 = trait_impl#ToString#string#to_string(poem/0) in
  let inline190 = @runtime(core.string_println)(inline189) in
  jump inline_cont188(())
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont191(t181: string) -> unit {
    let t182 = @runtime(core.string_println)(t181) in
    ()
  } in
  jump inline_cont191(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}