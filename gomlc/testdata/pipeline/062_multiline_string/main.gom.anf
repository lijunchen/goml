fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont807(_wild796: unit) -> unit {
    let inline805 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline806 = @runtime(core.string_println)(inline805) in
    ()
  } in
  let inline808 = trait_impl#ToString#string#to_string(poem/0) in
  let inline809 = @runtime(core.string_println)(inline808) in
  jump inline_cont807(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont810(t800: string) -> unit {
    let t801 = @runtime(core.string_println)(t800) in
    ()
  } in
  jump inline_cont810(value/1)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}