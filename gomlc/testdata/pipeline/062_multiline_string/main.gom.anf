fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont193(_wild182: unit) -> unit {
    let inline191 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline192 = @runtime(core.string_println)(inline191) in
    ()
  } in
  let inline194 = trait_impl#ToString#string#to_string(poem/0) in
  let inline195 = @runtime(core.string_println)(inline194) in
  jump inline_cont193(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont196(t186: string) -> unit {
    let t187 = @runtime(core.string_println)(t186) in
    ()
  } in
  jump inline_cont196(value/1)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}