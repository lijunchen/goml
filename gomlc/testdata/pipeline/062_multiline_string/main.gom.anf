fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont419(_wild408: unit) -> unit {
    let inline417 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline418 = @runtime(core.string_println)(inline417) in
    ()
  } in
  let inline420 = trait_impl#ToString#string#to_string(poem/0) in
  let inline421 = @runtime(core.string_println)(inline420) in
  jump inline_cont419(())
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont422(t412: string) -> unit {
    let t413 = @runtime(core.string_println)(t412) in
    ()
  } in
  jump inline_cont422(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}