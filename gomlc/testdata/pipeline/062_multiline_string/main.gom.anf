fn main() -> unit {
  let poem/0 =
    "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1 = "line one\n\nline three" in
  join inline_cont147(_wild136: unit) -> unit {
    let inline145 = trait_impl#ToString#string#to_string(trailing_blank/1) in
    let inline146 = @runtime(core.string_println)(inline145) in
    ()
  } in
  let inline148 = trait_impl#ToString#string#to_string(poem/0) in
  let inline149 = @runtime(core.string_println)(inline148) in
  jump inline_cont147(())
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont150(t140: string) -> unit {
    let t141 = @runtime(core.string_println)(t140) in
    ()
  } in
  jump inline_cont150(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}