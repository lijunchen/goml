fn trait_impl#Display#i32#show(self/0: i32) -> string {
  inherent#i32#i32#to_string(self/0)
}

fn render(x/0: dyn Display) -> string {
  dyn_call[Display::show](x/0)
}

fn main() -> unit {
  let v/0 = inherent#Vec#Vec[T]#new__T_dynDisplay() in
  let first/0 = 10 in
  let second/0 = 20 in
  let _wild0 = inherent#Vec#Vec[T]#push__T_dynDisplay(v/0, to_dyn[Display]{i32}(first/0)) in
  let _wild1 = inherent#Vec#Vec[T]#push__T_dynDisplay(v/0, to_dyn[Display]{i32}(second/0)) in
  let s/0 = inherent#Vec#Vec[T]#slice__T_dynDisplay(v/0, 0, 2) in
  let _wild2 = println__T_string(render(@intrinsic(slice.get)(s/0, 0))) in
  let t/0 = inherent#Slice#Slice[T]#sub__T_dynDisplay(s/0, 1, 2) in
  let _wild3 = println__T_string(render(@intrinsic(slice.get)(t/0, 0))) in
  ()
}

fn inherent#i32#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn inherent#Vec#Vec[T]#new__T_dynDisplay() -> Vec[dyn Display] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#push__T_dynDisplay(self/0: Vec[dyn Display], elem/0: dyn Display) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#slice__T_dynDisplay(
  self/0: Vec[dyn Display],
  start/0: isize,
  end/0: isize
) -> Slice[dyn Display] {
  @intrinsic(slice.new)(self/0, start/0, end/0)
}

fn println__T_string(value/0: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn inherent#Slice#Slice[T]#sub__T_dynDisplay(
  self/0: Slice[dyn Display],
  start/0: isize,
  end/0: isize
) -> Slice[dyn Display] {
  @intrinsic(slice.sub)(self/0, start/0, end/0)
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}