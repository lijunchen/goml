fn trait_impl#Display#int32#show(self/0: int32) -> string {
  let inline173 = @runtime(core.int32_to_string)(self/0) in
  inline173
}

fn render(x/1: dyn Display) -> string {
  let t145 = dyn_call[Display::show](x/1) in
  t145
}

fn main() -> unit {
  join inline_cont196(v/2: Vec[dyn Display]) -> unit {
    let first/3 = 10 in
    let second/4 = 20 in
    let t147 = to_dyn[Display]{int32}(first/3) in
    join inline_cont194(_wild136: unit) -> unit {
      let t148 = to_dyn[Display]{int32}(second/4) in
      join inline_cont192(_wild137: unit) -> unit {
        join inline_cont188(s/5: Slice[dyn Display]) -> unit {
          let t149 = @intrinsic(slice.get)(s/5, 0) in
          join inline_cont186(t150: string) -> unit {
            join inline_cont183(_wild138: unit) -> unit {
              join inline_cont179(t/6: Slice[dyn Display]) -> unit {
                let t151 = @intrinsic(slice.get)(t/6, 0) in
                join inline_cont177(t152: string) -> unit {
                  let inline175 = trait_impl#ToString#string#to_string(t152) in
                  let inline176 = @runtime(core.string_println)(inline175) in
                  ()
                } in
                let inline178 = dyn_call[Display::show](t151) in
                jump inline_cont177(inline178)
              } in
              let inline180 = 1 in
              let inline181 = 2 in
              let inline182 = @intrinsic(slice.sub)(s/5, inline180, inline181) in
              jump inline_cont179(inline182)
            } in
            let inline184 = trait_impl#ToString#string#to_string(t150) in
            let inline185 = @runtime(core.string_println)(inline184) in
            jump inline_cont183(())
          } in
          let inline187 = dyn_call[Display::show](t149) in
          jump inline_cont186(inline187)
        } in
        let inline189 = 0 in
        let inline190 = 2 in
        let inline191 = @intrinsic(slice.new)(v/2, inline189, inline190) in
        jump inline_cont188(inline191)
      } in
      let inline193 = @intrinsic(vec.push)(v/2, t148) in
      jump inline_cont192(())
    } in
    let inline195 = @intrinsic(vec.push)(v/2, t147) in
    jump inline_cont194(())
  } in
  let inline197 = @intrinsic(vec.new)() in
  jump inline_cont196(inline197)
}

fn inherent#int32#int32#to_string(self/35: int32) -> string {
  let t155 = @runtime(core.int32_to_string)(self/35) in
  t155
}

fn inherent#Vec#Vec[T]#new__T_dynDisplay() -> Vec[dyn Display] {
  let t158 = @intrinsic(vec.new)() in
  t158
}

fn inherent#Vec#Vec[T]#push__T_dynDisplay(
  self/134: Vec[dyn Display],
  elem/135: dyn Display
) -> unit {
  let t160 = @intrinsic(vec.push)(self/134, elem/135) in
  ()
}

fn inherent#Vec#Vec[T]#slice__T_dynDisplay(
  self/183: Vec[dyn Display],
  start/184: int,
  end/185: int
) -> Slice[dyn Display] {
  let t163 = @intrinsic(slice.new)(self/183, start/184, end/185) in
  t163
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont198(t165: string) -> unit {
    let t166 = @runtime(core.string_println)(t165) in
    ()
  } in
  jump inline_cont198(value/31)
}

fn inherent#Slice#Slice[T]#sub__T_dynDisplay(
  self/195: Slice[dyn Display],
  start/196: int,
  end/197: int
) -> Slice[dyn Display] {
  let t169 = @intrinsic(slice.sub)(self/195, start/196, end/197) in
  t169
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}