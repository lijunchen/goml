fn trait_impl#Display#int32#show(self/0: int32) -> string {
  let inline448 = @runtime(core.int32_to_string)(self/0) in
  inline448
}

fn render(x/1: dyn Display) -> string {
  let t420 = dyn_call[Display::show](x/1) in
  t420
}

fn main() -> unit {
  join inline_cont471(v/2: Vec[dyn Display]) -> unit {
    let first/3 = 10 in
    let second/4 = 20 in
    let t422 = to_dyn[Display]{int32}(first/3) in
    join inline_cont469(_wild411: unit) -> unit {
      let t423 = to_dyn[Display]{int32}(second/4) in
      join inline_cont467(_wild412: unit) -> unit {
        join inline_cont463(s/5: Slice[dyn Display]) -> unit {
          let t424 = @intrinsic(slice.get)(s/5, 0) in
          join inline_cont461(t425: string) -> unit {
            join inline_cont458(_wild413: unit) -> unit {
              join inline_cont454(t/6: Slice[dyn Display]) -> unit {
                let t426 = @intrinsic(slice.get)(t/6, 0) in
                join inline_cont452(t427: string) -> unit {
                  let inline450 = trait_impl#ToString#string#to_string(t427) in
                  let inline451 = @runtime(core.string_println)(inline450) in
                  ()
                } in
                let inline453 = dyn_call[Display::show](t426) in
                jump inline_cont452(inline453)
              } in
              let inline455 = 1 in
              let inline456 = 2 in
              let inline457 = @intrinsic(slice.sub)(s/5, inline455, inline456) in
              jump inline_cont454(inline457)
            } in
            let inline459 = trait_impl#ToString#string#to_string(t425) in
            let inline460 = @runtime(core.string_println)(inline459) in
            jump inline_cont458(())
          } in
          let inline462 = dyn_call[Display::show](t424) in
          jump inline_cont461(inline462)
        } in
        let inline464 = 0 in
        let inline465 = 2 in
        let inline466 = @intrinsic(slice.new)(v/2, inline464, inline465) in
        jump inline_cont463(inline466)
      } in
      let inline468 = @intrinsic(vec.push)(v/2, t423) in
      jump inline_cont467(())
    } in
    let inline470 = @intrinsic(vec.push)(v/2, t422) in
    jump inline_cont469(())
  } in
  let inline472 = @intrinsic(vec.new)() in
  jump inline_cont471(inline472)
}

fn inherent#int32#int32#to_string(self/33: int32) -> string {
  let t430 = @runtime(core.int32_to_string)(self/33) in
  t430
}

fn inherent#Vec#Vec[T]#new__T_dynDisplay() -> Vec[dyn Display] {
  let t433 = @intrinsic(vec.new)() in
  t433
}

fn inherent#Vec#Vec[T]#push__T_dynDisplay(
  self/258: Vec[dyn Display],
  elem/259: dyn Display
) -> unit {
  let t435 = @intrinsic(vec.push)(self/258, elem/259) in
  ()
}

fn inherent#Vec#Vec[T]#slice__T_dynDisplay(
  self/311: Vec[dyn Display],
  start/312: int,
  end/313: int
) -> Slice[dyn Display] {
  let t438 = @intrinsic(slice.new)(self/311, start/312, end/313) in
  t438
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont473(t440: string) -> unit {
    let t441 = @runtime(core.string_println)(t440) in
    ()
  } in
  jump inline_cont473(value/1)
}

fn inherent#Slice#Slice[T]#sub__T_dynDisplay(
  self/408: Slice[dyn Display],
  start/409: int,
  end/410: int
) -> Slice[dyn Display] {
  let t444 = @intrinsic(slice.sub)(self/408, start/409, end/410) in
  t444
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}