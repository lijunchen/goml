fn trait_impl#ToString#Color#to_string(self/0: Color) -> string {
  match self/0 {
    Color::Red => {
      "Color::Red"
    },
    Color::Green => {
      "Color::Green"
    },
    Color::Blue => {
      "Color::Blue"
    },
  }
}

fn trait_impl#ToString#Point#to_string(self/0: Point) -> string {
  let mtmp0 = self/0 in
  let x0 = Point.x(mtmp0) in
  let x1 = Point.y(mtmp0) in
  let x2 = Point.color(mtmp0) in
  let #derive2_field/0 = x2 in
  let #derive1_field/0 = x1 in
  let #derive0_field/0 = x0 in
  ((((((((("Point { " + "x: ") + trait_impl#ToString#i32#to_string(#derive0_field/0)) + ", ") +
    "y: ") +
    trait_impl#ToString#i32#to_string(#derive1_field/0)) +
    ", ") +
    "color: ") +
    trait_impl#ToString#Color#to_string(#derive2_field/0)) +
    " }")
}

fn inherent#Point#Point#new(x/0: i32, y/0: i32, color/0: Color) -> Point {
  Point { x: x/0, y: y/0, color: color/0 }
}

fn trait_impl#ToString#Line#to_string(self/0: Line) -> string {
  let mtmp0 = self/0 in
  let x0 = Line.from(mtmp0) in
  let x1 = Line.to(mtmp0) in
  let x2 = Line.color(mtmp0) in
  let #derive2_field/0 = x2 in
  let #derive1_field/0 = x1 in
  let #derive0_field/0 = x0 in
  ((((((((("Line { " + "from: ") + trait_impl#ToString#Point#to_string(#derive0_field/0)) + ", ") +
    "to: ") +
    trait_impl#ToString#Point#to_string(#derive1_field/0)) +
    ", ") +
    "color: ") +
    trait_impl#ToString#Color#to_string(#derive2_field/0)) +
    " }")
}

fn inherent#Line#Line#new(from/0: Point, to/0: Point, color/0: Color) -> Line {
  Line { from: from/0, to: to/0, color: color/0 }
}

fn trait_impl#ToString#LineList#to_string(self/0: LineList) -> string {
  match self/0 {
    LineList::Nil => {
      "LineList::Nil"
    },
    LineList::Cons(x0, x1) => {
      let x0 = LineList::Cons._0(self/0) in
      let x1 = LineList::Cons._1(self/0) in
      let #derive1_field/0 = x1 in
      let #derive0_field/0 = x0 in
      (((("LineList::Cons(" + trait_impl#ToString#Line#to_string(#derive0_field/0)) + ", ") +
        trait_impl#ToString#LineList#to_string(#derive1_field/0)) +
        ")")
    },
  }
}

fn main() -> () {
  let from/0 = inherent#Point#Point#new(10, 20, Color::Red) in
  let to/0 = inherent#Point#Point#new(30, 40, Color::Green) in
  let line/0 = inherent#Line#Line#new(from/0, to/0, Color::Blue) in
  let lines/0 = LineList::Cons(line/0, LineList::Nil) in
  let _wild0 = println__T_string(trait_impl#ToString#LineList#to_string(lines/0)) in
  ()
}

fn trait_impl#ToString#i32#to_string(self/0: i32) -> string {
  __goml_builtin_int32_to_string(self/0)
}

fn println__T_string(value/0: string) -> () {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/0))
}

fn __goml_builtin_int32_to_string(value/0: i32) -> string {
  signed_decimal_string((value/0 as i64))
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}