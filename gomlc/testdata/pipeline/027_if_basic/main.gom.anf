fn choose(flag/0: bool, x/1: int32, y/2: int32) -> int32 {
  if flag/0 {
    x/1
  } else {
    y/2
  }
}

fn main() -> unit {
  join inline_cont444(yes/3: int32) -> unit {
    join inline_cont440(no/4: int32) -> unit {
      join inline_cont438(t418: string) -> unit {
        let t419 = ("yes=" + t418) in
        join inline_cont435(mtmp411: unit) -> unit {
          join inline_cont433(t420: string) -> unit {
            let t421 = ("no=" + t420) in
            let inline431 = trait_impl#ToString#string#to_string(t421) in
            let inline432 = @runtime(core.string_println)(inline431) in
            ()
          } in
          let inline434 = @runtime(core.int32_to_string)(no/4) in
          jump inline_cont433(inline434)
        } in
        let inline436 = trait_impl#ToString#string#to_string(t419) in
        let inline437 = @runtime(core.string_println)(inline436) in
        jump inline_cont435(())
      } in
      let inline439 = @runtime(core.int32_to_string)(yes/3) in
      jump inline_cont438(inline439)
    } in
    let inline441 = false in
    let inline442 = 10 in
    let inline443 = 99 in
    if inline441 {
      jump inline_cont440(inline442)
    } else {
      jump inline_cont440(inline443)
    }
  } in
  let inline445 = true in
  let inline446 = 10 in
  let inline447 = 99 in
  if inline445 {
    jump inline_cont444(inline446)
  } else {
    jump inline_cont444(inline447)
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont448(t423: string) -> unit {
    let t424 = @runtime(core.string_println)(t423) in
    ()
  } in
  jump inline_cont448(value/1)
}

fn inherent#int32#int32#to_string(self/33: int32) -> string {
  let t427 = @runtime(core.int32_to_string)(self/33) in
  t427
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}