fn unwrap_either(value/0: Either) -> int32 {
  let match411 = value/0 in
  let whole/1 = match411 in
  match whole/1 {
    Either::Left(p415) => {
      let shared/2 = Either::Left._0(whole/1) in
      (shared/2 +
        match whole/1 {
          Either::Left(x412) => {
            0
          },
          Either::Right(x413) => {
            1
          },
        })
    },
    _ =>
      match whole/1 {
        Either::Right(p414) => {
          let shared/2 = Either::Right._0(whole/1) in
          (shared/2 +
            match whole/1 {
              Either::Left(x412) => {
                0
              },
              Either::Right(x413) => {
                1
              },
            })
        },
        _ => @intrinsic(compiler.missing)(""),
      },
  }
}

fn char_group(value/3: char) -> string {
  let match416 = value/3 in
  if (match416 >= 'a') {
      if (match416 <= 'c') {
          "abc"
      } else {
          "other"
      }
  } else {
      "other"
  }
}

fn describe(value/4: Maybe, numbers/5: Vec[int32], view/6: Slice[int32]) -> string {
  let from_if/9 =
    match value/4 {
      Maybe::Some(x417) => {
        let x417 = Maybe::Some._0(value/4) in
        let n/7 = x417 in
        let match418 = n/7 in
        if (match418 == 0) {
            "small"
        } else {
            if (match418 == 1) {
                "small"
            } else {
                if (match418 >= 2) {
                    if (match418 <= 4) {
                        "middle"
                    } else {
                        let x/8 = match418 in
                        if (x/8 > 10) {
                            "large"
                        } else {
                            "other"
                        }
                    }
                } else {
                    let x/8 = match418 in
                    if (x/8 > 10) {
                        "large"
                    } else {
                        "other"
                    }
                }
            }
        }
      },
      _ => "none",
    } in
  let from_vec/12 =
    let match419 = numbers/5 in
    if (@intrinsic(vec.len)(match419) == 0) {
        "empty"
    } else {
        if (@intrinsic(vec.len)(match419) >= 1) {
            let first/10 = @intrinsic(vec.get)(match419, 0) in
            let tail/11 = @intrinsic(slice.new)(match419, 1, @intrinsic(vec.len)(match419)) in
            if (first/10 == (inherent#Slice#Slice[T]#len__T_int32(tail/11) as int32)) {
                "balanced"
            } else {
                if (@intrinsic(vec.len)(match419) >= 1) {
                    "nonempty"
                } else {
                    @intrinsic(compiler.missing)("")
                }
            }
        } else {
            if (@intrinsic(vec.len)(match419) >= 1) {
                "nonempty"
            } else {
                @intrinsic(compiler.missing)("")
            }
        }
    } in
  let from_slice/15 =
    let match420 = view/6 in
    if (@intrinsic(slice.len)(match420) >= 2) {
        let first/13 = @intrinsic(slice.get)(match420, 0) in
        let last/14 =
          @intrinsic(slice.get)(match420, ((@intrinsic(slice.len)(match420) - 1) + 0)) in
        if (first/13 == last/14) {
            "same ends"
        } else {
            "different ends"
        }
    } else {
        "different ends"
    } in
  ((((from_if/9 + "/") + from_vec/12) + "/") + from_slice/15)
}

fn main() -> unit {
  let pair/16 = Pair { left: 3, right: 9 } in
  let mtmp421 = pair/16 in
  let x422 = Pair.left(mtmp421) in
  let left/17 = x422 in
  let values/18 = [1, 2, 3, 1] in
  let mtmp424 = values/18 in
  let first/19 = @intrinsic(array.get)(mtmp424, 0) in
  let last/21 = @intrinsic(array.get)(mtmp424, 3) in
  let middle/20 = [@intrinsic(array.get)(mtmp424, 1), @intrinsic(array.get)(mtmp424, 2)] in
  let _wild425 = println__T_int32(left/17) in
  let _wild426 = println__T_int(((first/19 + @intrinsic(array.get)(middle/20, 0)) + last/21)) in
  let numbers/22 = @intrinsic(vec.new)() in
  let _wild427 = @intrinsic(vec.push)(numbers/22, 1) in
  let _wild428 = @intrinsic(vec.push)(numbers/22, 8) in
  let view/23 =
    @intrinsic(slice.new)(numbers/22, 0, inherent#Vec#Vec[T]#len__T_int32(numbers/22)) in
  let _wild429 = println__T_string(describe(Maybe::Some(3), numbers/22, view/23)) in
  let empty/24 = @intrinsic(vec.new)() in
  let empty_view/25 = @intrinsic(slice.new)(empty/24, 0, 0) in
  let _wild430 = println__T_string(describe(Maybe::None, empty/24, empty_view/25)) in
  let state/26 = inherent#Ref#Ref[T]#new__T_Maybe(Maybe::Some(7)) in
  let _wild435 =
    while true {
        let mtmp431 = inherent#Ref#Ref[T]#get__T_Maybe(state/26) in
        match mtmp431 {
          Maybe::Some(x432) => {
            let x432 = Maybe::Some._0(mtmp431) in
            let n/27 = x432 in
            let _wild433 = println__T_int32(n/27) in
            let _wild434 = inherent#Ref#Ref[T]#set__T_Maybe(state/26, Maybe::None) in
            ()
          },
          _ => break,
        }
    } in
  let _wild439 =
    let mtmp436 = Maybe::Some(6) in
    match mtmp436 {
      Maybe::Some(x437) => {
        let x437 = Maybe::Some._0(mtmp436) in
        let n/28 = x437 in
        let _wild438 = println__T_int32(n/28) in
        ()
      },
      _ => (),
    } in
  let aliased/32 =
    let match440 = Maybe::Some(5) in
    let whole/29 = match440 in
    match whole/29 {
      Maybe::Some(p442) => {
        let value/30 = Maybe::Some._0(whole/29) in
        (value/30 +
          match whole/29 {
            Maybe::None => {
              0
            },
            Maybe::Some(x441) => {
              let x441 = Maybe::Some._0(whole/29) in
              let inner/31 = x441 in
              inner/31
            },
          })
      },
      _ =>
        match match440 {
          Maybe::None => {
            0
          },
          _ => @intrinsic(compiler.missing)(""),
        },
    } in
  let _wild443 = println__T_int32(aliased/32) in
  let _wild444 = println__T_int32(unwrap_either(Either::Right(11))) in
  let _wild445 = println__T_string(char_group('b')) in
  ()
}

fn inherent#Slice#Slice[T]#len__T_int32(self/407: Slice[int32]) -> int {
  @intrinsic(slice.len)(self/407)
}

fn println__T_int32(value/1: int32) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#int32#to_string(value/1))
}

fn println__T_int(value/1: int) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#int#to_string(value/1))
}

fn inherent#Vec#Vec[T]#len__T_int32(self/273: Vec[int32]) -> int {
  @intrinsic(vec.len)(self/273)
}

fn println__T_string(value/1: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/1))
}

fn inherent#Ref#Ref[T]#new__T_Maybe(value/431: Maybe) -> Ref[Maybe] {
  @intrinsic(ref.new)(value/431)
}

fn inherent#Ref#Ref[T]#get__T_Maybe(self/432: Ref[Maybe]) -> Maybe {
  @intrinsic(ref.get)(self/432)
}

fn inherent#Ref#Ref[T]#set__T_Maybe(self/433: Ref[Maybe], value/434: Maybe) -> unit {
  @intrinsic(ref.set)(self/433, value/434)
}

fn trait_impl#ToString#int32#to_string(self/154: int32) -> string {
  @runtime(core.int32_to_string)(self/154)
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  @runtime(core.int_to_string)(self/151)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}