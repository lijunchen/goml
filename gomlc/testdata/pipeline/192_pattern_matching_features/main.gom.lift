fn unwrap_either(value/0: Either) -> i32 {
  let match796 = value/0 in
  let whole/1 = match796 in
  match whole/1 {
    Either::Left(p800) => {
      let shared/2 = Either::Left._0(whole/1) in
      (shared/2 +
        match whole/1 {
          Either::Left(x797) => {
            0
          },
          Either::Right(x798) => {
            1
          },
        })
    },
    _ =>
      match whole/1 {
        Either::Right(p799) => {
          let shared/2 = Either::Right._0(whole/1) in
          (shared/2 +
            match whole/1 {
              Either::Left(x797) => {
                0
              },
              Either::Right(x798) => {
                1
              },
            })
        },
        _ => @intrinsic(compiler.missing)(""),
      },
  }
}

fn char_group(value/3: char) -> string {
  let match801 = value/3 in
  if (match801 >= 'a') {
      if (match801 <= 'c') {
          "abc"
      } else {
          "other"
      }
  } else {
      "other"
  }
}

fn describe(value/4: Maybe, numbers/5: Vec[i32], view/6: Slice[i32]) -> string {
  let from_if/9 =
    match value/4 {
      Maybe::Some(x802) => {
        let x802 = Maybe::Some._0(value/4) in
        let n/7 = x802 in
        let match803 = n/7 in
        if (match803 == 0) {
            "small"
        } else {
            if (match803 == 1) {
                "small"
            } else {
                if (match803 >= 2) {
                    if (match803 <= 4) {
                        "middle"
                    } else {
                        let x/8 = match803 in
                        if (x/8 > 10) {
                            "large"
                        } else {
                            "other"
                        }
                    }
                } else {
                    let x/8 = match803 in
                    if (x/8 > 10) {
                        "large"
                    } else {
                        "other"
                    }
                }
            }
        }
      },
      _ => "none",
    } in
  let from_vec/12 =
    let match804 = numbers/5 in
    if (@intrinsic(vec.len)(match804) == 0) {
        "empty"
    } else {
        if (@intrinsic(vec.len)(match804) >= 1) {
            let first/10 = @intrinsic(vec.get)(match804, 0) in
            let tail/11 = @intrinsic(slice.new)(match804, 1, @intrinsic(vec.len)(match804)) in
            if (first/10 == (inherent#Slice#Slice[T]#len__T_i32(tail/11) as i32)) {
                "balanced"
            } else {
                if (@intrinsic(vec.len)(match804) >= 1) {
                    "nonempty"
                } else {
                    @intrinsic(compiler.missing)("")
                }
            }
        } else {
            if (@intrinsic(vec.len)(match804) >= 1) {
                "nonempty"
            } else {
                @intrinsic(compiler.missing)("")
            }
        }
    } in
  let from_slice/15 =
    let match805 = view/6 in
    if (@intrinsic(slice.len)(match805) >= 2) {
        let first/13 = @intrinsic(slice.get)(match805, 0) in
        let last/14 =
          @intrinsic(slice.get)(match805, ((@intrinsic(slice.len)(match805) - 1) + 0)) in
        if (first/13 == last/14) {
            "same ends"
        } else {
            "different ends"
        }
    } else {
        "different ends"
    } in
  ((((from_if/9 + "/") + from_vec/12) + "/") + from_slice/15)
}

fn main() -> unit {
  let pair/16 = Pair { left: 3, right: 9 } in
  let mtmp806 = pair/16 in
  let x807 = Pair.left(mtmp806) in
  let left/17 = x807 in
  let values/18 = [1, 2, 3, 1] in
  let mtmp809 = values/18 in
  let first/19 = @intrinsic(array.get)(mtmp809, 0) in
  let last/21 = @intrinsic(array.get)(mtmp809, 3) in
  let middle/20 = [@intrinsic(array.get)(mtmp809, 1), @intrinsic(array.get)(mtmp809, 2)] in
  let _wild810 = println__T_i32(left/17) in
  let _wild811 = println__T_isize(((first/19 + @intrinsic(array.get)(middle/20, 0)) + last/21)) in
  let numbers/22 = @intrinsic(vec.new)() in
  let _wild812 = @intrinsic(vec.push)(numbers/22, 1) in
  let _wild813 = @intrinsic(vec.push)(numbers/22, 8) in
  let view/23 = @intrinsic(slice.new)(numbers/22, 0, inherent#Vec#Vec[T]#len__T_i32(numbers/22)) in
  let _wild814 = println__T_string(describe(Maybe::Some(3), numbers/22, view/23)) in
  let empty/24 = @intrinsic(vec.new)() in
  let empty_view/25 = @intrinsic(slice.new)(empty/24, 0, 0) in
  let _wild815 = println__T_string(describe(Maybe::None, empty/24, empty_view/25)) in
  let state/26 = inherent#Ref#Ref[T]#new__T_Maybe(Maybe::Some(7)) in
  let _wild820 =
    while true {
        let mtmp816 = inherent#Ref#Ref[T]#get__T_Maybe(state/26) in
        match mtmp816 {
          Maybe::Some(x817) => {
            let x817 = Maybe::Some._0(mtmp816) in
            let n/27 = x817 in
            let _wild818 = println__T_i32(n/27) in
            let _wild819 = inherent#Ref#Ref[T]#set__T_Maybe(state/26, Maybe::None) in
            ()
          },
          _ => break,
        }
    } in
  let _wild824 =
    let mtmp821 = Maybe::Some(6) in
    match mtmp821 {
      Maybe::Some(x822) => {
        let x822 = Maybe::Some._0(mtmp821) in
        let n/28 = x822 in
        let _wild823 = println__T_i32(n/28) in
        ()
      },
      _ => (),
    } in
  let aliased/32 =
    let match825 = Maybe::Some(5) in
    let whole/29 = match825 in
    match whole/29 {
      Maybe::Some(p827) => {
        let value/30 = Maybe::Some._0(whole/29) in
        (value/30 +
          match whole/29 {
            Maybe::None => {
              0
            },
            Maybe::Some(x826) => {
              let x826 = Maybe::Some._0(whole/29) in
              let inner/31 = x826 in
              inner/31
            },
          })
      },
      _ =>
        match match825 {
          Maybe::None => {
            0
          },
          _ => @intrinsic(compiler.missing)(""),
        },
    } in
  let _wild828 = println__T_i32(aliased/32) in
  let _wild829 = println__T_i32(unwrap_either(Either::Right(11))) in
  let _wild830 = println__T_string(char_group('b')) in
  ()
}

fn inherent#Slice#Slice[T]#len__T_i32(self/660: Slice[i32]) -> isize {
  @intrinsic(slice.len)(self/660)
}

fn println__T_i32(value/1: i32) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#i32#to_string(value/1))
}

fn println__T_isize(value/1: isize) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#isize#to_string(value/1))
}

fn inherent#Vec#Vec[T]#len__T_i32(self/526: Vec[i32]) -> isize {
  @intrinsic(vec.len)(self/526)
}

fn println__T_string(value/1: string) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#string#to_string(value/1))
}

fn inherent#Ref#Ref[T]#new__T_Maybe(value/684: Maybe) -> Ref[Maybe] {
  @intrinsic(ref.new)(value/684)
}

fn inherent#Ref#Ref[T]#get__T_Maybe(self/685: Ref[Maybe]) -> Maybe {
  @intrinsic(ref.get)(self/685)
}

fn inherent#Ref#Ref[T]#set__T_Maybe(self/686: Ref[Maybe], value/687: Maybe) -> unit {
  @intrinsic(ref.set)(self/686, value/687)
}

fn trait_impl#ToString#i32#to_string(self/407: i32) -> string {
  __goml_builtin_int32_to_string(self/407)
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  __goml_builtin_int_to_string(self/404)
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  signed_decimal_string((value/225 as i64))
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  signed_decimal_string((value/222 as i64))
}

fn signed_decimal_string(value/214: i64) -> string {
  if (value/214 < 0) {
      ("-" + decimal_string((0 - (value/214 as u64))))
  } else {
      decimal_string((value/214 as u64))
  }
}

fn decimal_string(value/208: u64) -> string {
  let _wild351 =
    if (value/208 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/209 = @intrinsic(vec.with_capacity)(20) in
  let remaining/210 = value/208 in
  let _wild356 =
    while (remaining/210 > 0) {
        let _wild352 =
          inherent#Vec#Vec[T]#push__T_u8(reversed/209, (((remaining/210 % 10) as u8) + 48)) in
        let _wild355 =
          let compound_old353 = remaining/210 in
          let compound_value354 = 10 in
          remaining/210 = (compound_old353 / compound_value354) in
        ()
    } in
  let bytes/211 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u8(reversed/209)) in
  let offset/212 = 0 in
  let _wild361 =
    while (offset/212 < inherent#Vec#Vec[T]#len__T_u8(reversed/209)) {
        let _wild357 =
          inherent#Vec#Vec[T]#push__T_u8(
            bytes/211,
            @intrinsic(vec.get)(
              reversed/209,
              ((inherent#Vec#Vec[T]#len__T_u8(reversed/209) - offset/212) - 1)
            )
          ) in
        let _wild360 =
          let compound_old358 = offset/212 in
          let compound_value359 = 1 in
          offset/212 = (compound_old358 + compound_value359) in
        ()
    } in
  let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
  let x364 = mtmp362.1 in
  let result/213 = x364 in
  result/213
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  @intrinsic(vec.push)(self/511, elem/512)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/526)
}