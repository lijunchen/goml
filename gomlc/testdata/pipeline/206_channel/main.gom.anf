fn make_string_channel() -> Channel[string] {
  join ret86(retv87: Channel[string]) -> Channel[string] {
    retv87
  } in
  let t88 = inherent#Channel#Channel[T]#new__T_string(0) in
  jump ret86(t88)
}

fn main() -> unit {
  join ret89() -> unit {
    ()
  } in
  let buffered/2 = inherent#Channel#Channel[T]#new__T_int(2) in
  let _wild69 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 10) in
  let _wild70 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 20) in
  let _wild71 = inherent#Channel#Channel[T]#close__T_int(buffered/2) in
  let mtmp72 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
  join k90(jp91: int) -> unit {
    let _wild74 = println__T_int(jp91) in
    let mtmp75 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
    join k92(jp93: int) -> unit {
      let _wild77 = println__T_int(jp93) in
      let mtmp78 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
      join k94(jp95: string) -> unit {
        let _wild80 = println__T_string(jp95) in
        let unbuffered/5 = make_string_channel() in
        let mtmp81 = roundtrip__T_string(unbuffered/5, "ready") in
        join k96(jp97: string) -> unit {
          let _wild83 = println__T_string(jp97) in
          let _wild84 = inherent#Channel#Channel[T]#close__T_string(unbuffered/5) in
          jump ret89()
        } in
        match mtmp81 {
          Tag_0 => {
            jump k96("closed")
          },
          Tag_1 => {
            let x82 = Option__string::Some._0(mtmp81) in
            let value/6 = x82 in
            jump k96(value/6)
          },
        }
      } in
      match mtmp78 {
        Tag_0 => {
          jump k94("closed")
        },
        Tag_1 => {
          let x79 = Option__int::Some._0(mtmp78) in
          jump k94("open")
        },
      }
    } in
    match mtmp75 {
      Tag_0 => {
        jump k92(-1)
      },
      Tag_1 => {
        let x76 = Option__int::Some._0(mtmp75) in
        let value/4 = x76 in
        jump k92(value/4)
      },
    }
  } in
  match mtmp72 {
    Tag_0 => {
      jump k90(-1)
    },
    Tag_1 => {
      let x73 = Option__int::Some._0(mtmp72) in
      let value/3 = x73 in
      jump k90(value/3)
    },
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/211: int) -> Channel[string] {
  join ret98(retv99: Channel[string]) -> Channel[string] {
    retv99
  } in
  let t100 = @intrinsic(channel.new)(capacity/211) in
  jump ret98(t100)
}

fn inherent#Channel#Channel[T]#new__T_int(capacity/211: int) -> Channel[int] {
  join ret101(retv102: Channel[int]) -> Channel[int] {
    retv102
  } in
  let t103 = @intrinsic(channel.new)(capacity/211) in
  jump ret101(t103)
}

fn inherent#Channel#Channel[T]#send__T_int(self/212: Channel[int], value/213: int) -> unit {
  join ret104() -> unit {
    ()
  } in
  let t105 = @intrinsic(channel.send)(self/212, value/213) in
  jump ret104()
}

fn inherent#Channel#Channel[T]#close__T_int(self/217: Channel[int]) -> unit {
  join ret106() -> unit {
    ()
  } in
  let t107 = @intrinsic(channel.close)(self/217) in
  jump ret106()
}

fn println__T_int(value/1: int) -> unit {
  join ret108() -> unit {
    ()
  } in
  let t109 = trait_impl#ToString#int#to_string(value/1) in
  let t110 = @runtime(core.string_println)(t109) in
  jump ret108()
}

fn inherent#Channel#Channel[T]#recv__T_int(self/214: Channel[int]) -> Option__int {
  join ret111(retv112: Option__int) -> Option__int {
    retv112
  } in
  let mtmp60 = @intrinsic(channel.recv)(self/214) in
  let x61 = mtmp60.0 in
  let x62 = mtmp60.1 in
  let ok/216 = x62 in
  let value/215 = x61 in
  join k113(jp114: Option__int) -> Option__int {
    jump ret111(jp114)
  } in
  if ok/216 {
    let t115 = Option__int::Some(value/215) in
    jump k113(t115)
  } else {
    jump k113(Tag_0)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret116() -> unit {
    ()
  } in
  let t117 = trait_impl#ToString#string#to_string(value/1) in
  let t118 = @runtime(core.string_println)(t117) in
  jump ret116()
}

fn roundtrip__T_string(channel/0: Channel[string], value/1: string) -> Option__string {
  join ret119(retv120: Option__string) -> Option__string {
    retv120
  } in
  let t121 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/1 } in
  let _wild68 = go t121 in
  let t122 = inherent#Channel#Channel[T]#recv__T_string(channel/0) in
  jump ret119(t122)
}

fn inherent#Channel#Channel[T]#close__T_string(self/217: Channel[string]) -> unit {
  join ret123() -> unit {
    ()
  } in
  let t124 = @intrinsic(channel.close)(self/217) in
  jump ret123()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret125(retv126: string) -> string {
    retv126
  } in
  let t127 = @runtime(core.int_to_string)(self/40) in
  jump ret125(t127)
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret128(retv129: string) -> string {
    retv129
  } in
  jump ret128(self/38)
}

fn inherent#Channel#Channel[T]#send__T_string(self/212: Channel[string], value/213: string) -> unit {
  join ret130() -> unit {
    ()
  } in
  let t131 = @intrinsic(channel.send)(self/212, value/213) in
  jump ret130()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/214: Channel[string]) -> Option__string {
  join ret132(retv133: Option__string) -> Option__string {
    retv133
  } in
  let mtmp60 = @intrinsic(channel.recv)(self/214) in
  let x61 = mtmp60.0 in
  let x62 = mtmp60.1 in
  let ok/216 = x62 in
  let value/215 = x61 in
  join k134(jp135: Option__string) -> Option__string {
    jump ret132(jp135)
  } in
  if ok/216 {
    let t136 = Option__string::Some(value/215) in
    jump k134(t136)
  } else {
    jump k134(Tag_0)
  }
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(env85: closure_env_roundtrip_T_string_0) -> unit {
  join ret137() -> unit {
    ()
  } in
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env85) in
  let value/1 = closure_env_roundtrip_T_string_0.value_1(env85) in
  let t138 = inherent#Channel#Channel[T]#send__T_string(channel/0, value/1) in
  jump ret137()
}