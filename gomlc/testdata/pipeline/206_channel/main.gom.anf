fn make_string_channel() -> Channel[string] {
  let inline0 = 0 in
  let inline1 = @intrinsic(channel.new)(inline0) in
  inline1
}

fn main() -> unit {
  let buffered/0 = inherent#Channel#Channel[T]#new__T_isize(2) in
  let _wild0 = inherent#Channel#Channel[T]#send__T_isize(buffered/0, 10) in
  let _wild1 = inherent#Channel#Channel[T]#send__T_isize(buffered/0, 20) in
  let _wild2 = inherent#Channel#Channel[T]#close__T_isize(buffered/0) in
  join inline_cont0(mtmp0: Option__isize) -> unit {
    join k0(jp0: isize) -> unit {
      join inline_cont1(_wild3: unit) -> unit {
        join inline_cont2(mtmp1: Option__isize) -> unit {
          join k1(jp1: isize) -> unit {
            join inline_cont3(_wild4: unit) -> unit {
              join inline_cont4(mtmp2: Option__isize) -> unit {
                join k2(jp2: string) -> unit {
                  join inline_cont5(_wild5: unit) -> unit {
                    join inline_cont6(unbuffered/0: Channel[string]) -> unit {
                      join inline_cont7(mtmp3: Option__string) -> unit {
                        join k3(jp3: string) -> unit {
                          join inline_cont8(_wild6: unit) -> unit {
                            let inline0 = @intrinsic(channel.close)(unbuffered/0) in
                            ()
                          } in
                          let inline1 = trait_impl#ToString#string#to_string(jp3) in
                          let inline2 = @runtime(core.string_println)(inline1) in
                          jump inline_cont8(())
                        } in
                        match mtmp3 {
                          Tag_0 => {
                            jump k3("closed")
                          },
                          Tag_1 => {
                            let x0 = Option__string::Some._0(mtmp3) in
                            jump k3(x0)
                          },
                        }
                      } in
                      let inline3 = "ready" in
                      let inline4 =
                        closure_env_roundtrip_T_string_0 {
                          channel_0: unbuffered/0,
                          value_1: inline3
                        } in
                      let inline5 =
                        pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
                          inline4
                        ) in
                      let inline6 = go inline5 in
                      let inline7 = inherent#Channel#Channel[T]#recv__T_string(unbuffered/0) in
                      jump inline_cont7(inline7)
                    } in
                    let inline8 = inherent#Channel#Channel[T]#new__T_string(0) in
                    jump inline_cont6(inline8)
                  } in
                  let inline9 = trait_impl#ToString#string#to_string(jp2) in
                  let inline10 = @runtime(core.string_println)(inline9) in
                  jump inline_cont5(())
                } in
                match mtmp2 {
                  Tag_0 => {
                    jump k2("closed")
                  },
                  Tag_1 => {
                    jump k2("open")
                  },
                }
              } in
              let inline11 = @intrinsic(channel.recv)(buffered/0) in
              let inline12 = inline11.0 in
              let inline13 = inline11.1 in
              if inline13 {
                let inline14 = Option__isize::Some(inline12) in
                jump inline_cont4(inline14)
              } else {
                jump inline_cont4(Tag_0)
              }
            } in
            let inline15 = trait_impl#ToString#isize#to_string(jp1) in
            let inline16 = @runtime(core.string_println)(inline15) in
            jump inline_cont3(())
          } in
          match mtmp1 {
            Tag_0 => {
              jump k1(-1)
            },
            Tag_1 => {
              let x1 = Option__isize::Some._0(mtmp1) in
              jump k1(x1)
            },
          }
        } in
        let inline17 = @intrinsic(channel.recv)(buffered/0) in
        let inline18 = inline17.0 in
        let inline19 = inline17.1 in
        if inline19 {
          let inline20 = Option__isize::Some(inline18) in
          jump inline_cont2(inline20)
        } else {
          jump inline_cont2(Tag_0)
        }
      } in
      let inline21 = trait_impl#ToString#isize#to_string(jp0) in
      let inline22 = @runtime(core.string_println)(inline21) in
      jump inline_cont1(())
    } in
    match mtmp0 {
      Tag_0 => {
        jump k0(-1)
      },
      Tag_1 => {
        let x2 = Option__isize::Some._0(mtmp0) in
        jump k0(x2)
      },
    }
  } in
  let inline23 = @intrinsic(channel.recv)(buffered/0) in
  let inline24 = inline23.0 in
  let inline25 = inline23.1 in
  if inline25 {
    let inline26 = Option__isize::Some(inline24) in
    jump inline_cont0(inline26)
  } else {
    jump inline_cont0(Tag_0)
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/0: isize) -> Channel[string] {
  let t0 = @intrinsic(channel.new)(capacity/0) in
  t0
}

fn inherent#Channel#Channel[T]#new__T_isize(capacity/0: isize) -> Channel[isize] {
  let t0 = @intrinsic(channel.new)(capacity/0) in
  t0
}

fn inherent#Channel#Channel[T]#send__T_isize(self/0: Channel[isize], value/0: isize) -> unit {
  let t0 = @intrinsic(channel.send)(self/0, value/0) in
  ()
}

fn inherent#Channel#Channel[T]#close__T_isize(self/0: Channel[isize]) -> unit {
  let t0 = @intrinsic(channel.close)(self/0) in
  ()
}

fn println__T_isize(value/0: isize) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = __goml_builtin_int_to_string(value/0) in
  jump inline_cont0(inline0)
}

fn inherent#Channel#Channel[T]#recv__T_isize(self/0: Channel[isize]) -> Option__isize {
  let mtmp0 = @intrinsic(channel.recv)(self/0) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  if x1 {
    let t0 = Option__isize::Some(x0) in
    t0
  } else {
    Tag_0
  }
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn roundtrip__T_string(channel/0: Channel[string], value/0: string) -> Option__string {
  let t0 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/0 } in
  let t1 =
    pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
      t0
    ) in
  let _wild0 = go t1 in
  let inline0 = @intrinsic(channel.recv)(channel/0) in
  let inline1 = inline0.0 in
  let inline2 = inline0.1 in
  if inline2 {
    let inline3 = Option__string::Some(inline1) in
    inline3
  } else {
    Tag_0
  }
}

fn inherent#Channel#Channel[T]#close__T_string(self/0: Channel[string]) -> unit {
  let t0 = @intrinsic(channel.close)(self/0) in
  ()
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  let inline0 = (self/0 as i64) in
  let inline1 = signed_decimal_string(inline0) in
  inline1
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn inherent#Channel#Channel[T]#send__T_string(self/0: Channel[string], value/0: string) -> unit {
  let t0 = @intrinsic(channel.send)(self/0, value/0) in
  ()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/0: Channel[string]) -> Option__string {
  let mtmp0 = @intrinsic(channel.recv)(self/0) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  if x1 {
    let t0 = Option__string::Some(x0) in
    t0
  } else {
    Tag_0
  }
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  let t0 = (value/0 as i64) in
  let inline0 = (t0 < 0) in
  if inline0 {
    let inline1 = (t0 as u64) in
    let inline2 = (0 - inline1) in
    let inline3 = decimal_string(inline2) in
    let inline4 = ("-" + inline3) in
    inline4
  } else {
    let inline5 = (t0 as u64) in
    let inline6 = decimal_string(inline5) in
    inline6
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  let t0 = (value/0 < 0) in
  if t0 {
    let t1 = (value/0 as u64) in
    let t2 = (0 - t1) in
    let t3 = decimal_string(t2) in
    let t4 = ("-" + t3) in
    t4
  } else {
    let t5 = (value/0 as u64) in
    let t6 = decimal_string(t5) in
    t6
  }
}

fn decimal_string(value/0: u64) -> string {
  let t0 = (value/0 == 0) in
  join k0() -> string {
    let reversed/0 = @intrinsic(vec.with_capacity)(20) in
    let remaining/0 = value/0 in
    join exit0() -> string {
      join inline_cont0(t1: isize) -> string {
        let bytes/0 = @intrinsic(vec.with_capacity)(t1) in
        let offset/0 = 0 in
        join exit1() -> string {
          let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
          let x0 = mtmp0.1 in
          x0
        } in
        joinrec {
          join loop1() -> string {
            join inline_cont1(t2: isize) -> string {
              let t3 = (offset/0 < t2) in
              if t3 {
                join inline_cont2(t4: isize) -> string {
                  let t5 = (t4 - offset/0) in
                  let t6 = (t5 - 1) in
                  let t7 = @intrinsic(vec.get)(reversed/0, t6) in
                  join inline_cont3(_wild0: unit) -> string {
                    let compound_old0 = offset/0 in
                    let compound_value0 = 1 in
                    let t8 = (compound_old0 + compound_value0) in
                    let t9 = offset/0 = t8 in
                    jump loop1()
                  } in
                  let inline0 = @intrinsic(vec.push)(bytes/0, t7) in
                  jump inline_cont3(())
                } in
                let inline1 = @intrinsic(vec.len)(reversed/0) in
                jump inline_cont2(inline1)
              } else {
                jump exit1()
              }
            } in
            let inline2 = @intrinsic(vec.len)(reversed/0) in
            jump inline_cont1(inline2)
          } in
        } in
        jump loop1()
      } in
      let inline3 = @intrinsic(vec.len)(reversed/0) in
      jump inline_cont0(inline3)
    } in
    joinrec {
      join loop0() -> string {
        let t10 = (remaining/0 > 0) in
        if t10 {
          let t11 = (remaining/0 % 10) in
          let t12 = (t11 as u8) in
          let t13 = (t12 + 48) in
          join inline_cont4(_wild1: unit) -> string {
            let compound_old1 = remaining/0 in
            let compound_value1 = 10 in
            let t14 = (compound_old1 / compound_value1) in
            let t15 = remaining/0 = t14 in
            jump loop0()
          } in
          let inline4 = @intrinsic(vec.push)(reversed/0, t13) in
          jump inline_cont4(())
        } else {
          jump exit0()
        }
      } in
    } in
    jump loop0()
  } in
  if t0 {
    "0"
  } else {
    jump k0()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  let t0 = @intrinsic(vec.push)(self/0, elem/0) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  let t0 = @intrinsic(vec.len)(self/0) in
  t0
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(
  env0: closure_env_roundtrip_T_string_0
) -> unit {
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env0) in
  let value/0 = closure_env_roundtrip_T_string_0.value_1(env0) in
  let inline0 = @intrinsic(channel.send)(channel/0, value/0) in
  ()
}