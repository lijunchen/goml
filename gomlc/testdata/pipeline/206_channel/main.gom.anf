fn make_string_channel() -> Channel[string] {
  let inline481 = 0 in
  let inline482 = @intrinsic(channel.new)(inline481) in
  inline482
}

fn main() -> unit {
  let buffered/2 = inherent#Channel#Channel[T]#new__T_int(2) in
  let _wild409 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 10) in
  let _wild410 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 20) in
  let _wild411 = inherent#Channel#Channel[T]#close__T_int(buffered/2) in
  join inline_cont519(mtmp412: Option__int) -> unit {
    join k430(jp431: int) -> unit {
      join inline_cont516(_wild414: unit) -> unit {
        join inline_cont509(mtmp415: Option__int) -> unit {
          join k432(jp433: int) -> unit {
            join inline_cont506(_wild417: unit) -> unit {
              join inline_cont499(mtmp418: Option__int) -> unit {
                join k434(jp435: string) -> unit {
                  join inline_cont496(_wild420: unit) -> unit {
                    join inline_cont494(unbuffered/5: Channel[string]) -> unit {
                      join inline_cont488(mtmp421: Option__string) -> unit {
                        join k436(jp437: string) -> unit {
                          join inline_cont485(_wild423: unit) -> unit {
                            let inline484 = @intrinsic(channel.close)(unbuffered/5) in
                            ()
                          } in
                          let inline486 = trait_impl#ToString#string#to_string(jp437) in
                          let inline487 = @runtime(core.string_println)(inline486) in
                          jump inline_cont485(())
                        } in
                        match mtmp421 {
                          Tag_0 => {
                            jump k436("closed")
                          },
                          Tag_1 => {
                            let x422 = Option__string::Some._0(mtmp421) in
                            jump k436(x422)
                          },
                        }
                      } in
                      let inline489 = "ready" in
                      let inline490 =
                        closure_env_roundtrip_T_string_0 {
                          channel_0: unbuffered/5,
                          value_1: inline489
                        } in
                      let inline491 =
                        pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
                          inline490
                        ) in
                      let inline492 = go inline491 in
                      let inline493 = inherent#Channel#Channel[T]#recv__T_string(unbuffered/5) in
                      jump inline_cont488(inline493)
                    } in
                    let inline495 = inherent#Channel#Channel[T]#new__T_string(0) in
                    jump inline_cont494(inline495)
                  } in
                  let inline497 = trait_impl#ToString#string#to_string(jp435) in
                  let inline498 = @runtime(core.string_println)(inline497) in
                  jump inline_cont496(())
                } in
                match mtmp418 {
                  Tag_0 => {
                    jump k434("closed")
                  },
                  Tag_1 => {
                    jump k434("open")
                  },
                }
              } in
              let inline500 = @intrinsic(channel.recv)(buffered/2) in
              let inline501 = inline500.0 in
              let inline502 = inline500.1 in
              if inline502 {
                let inline505 = Option__int::Some(inline501) in
                jump inline_cont499(inline505)
              } else {
                jump inline_cont499(Tag_0)
              }
            } in
            let inline507 = trait_impl#ToString#int#to_string(jp433) in
            let inline508 = @runtime(core.string_println)(inline507) in
            jump inline_cont506(())
          } in
          match mtmp415 {
            Tag_0 => {
              jump k432(-1)
            },
            Tag_1 => {
              let x416 = Option__int::Some._0(mtmp415) in
              jump k432(x416)
            },
          }
        } in
        let inline510 = @intrinsic(channel.recv)(buffered/2) in
        let inline511 = inline510.0 in
        let inline512 = inline510.1 in
        if inline512 {
          let inline515 = Option__int::Some(inline511) in
          jump inline_cont509(inline515)
        } else {
          jump inline_cont509(Tag_0)
        }
      } in
      let inline517 = trait_impl#ToString#int#to_string(jp431) in
      let inline518 = @runtime(core.string_println)(inline517) in
      jump inline_cont516(())
    } in
    match mtmp412 {
      Tag_0 => {
        jump k430(-1)
      },
      Tag_1 => {
        let x413 = Option__int::Some._0(mtmp412) in
        jump k430(x413)
      },
    }
  } in
  let inline520 = @intrinsic(channel.recv)(buffered/2) in
  let inline521 = inline520.0 in
  let inline522 = inline520.1 in
  if inline522 {
    let inline525 = Option__int::Some(inline521) in
    jump inline_cont519(inline525)
  } else {
    jump inline_cont519(Tag_0)
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/435: int) -> Channel[string] {
  let t440 = @intrinsic(channel.new)(capacity/435) in
  t440
}

fn inherent#Channel#Channel[T]#new__T_int(capacity/435: int) -> Channel[int] {
  let t443 = @intrinsic(channel.new)(capacity/435) in
  t443
}

fn inherent#Channel#Channel[T]#send__T_int(self/436: Channel[int], value/437: int) -> unit {
  let t445 = @intrinsic(channel.send)(self/436, value/437) in
  ()
}

fn inherent#Channel#Channel[T]#close__T_int(self/441: Channel[int]) -> unit {
  let t447 = @intrinsic(channel.close)(self/441) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont526(t449: string) -> unit {
    let t450 = @runtime(core.string_println)(t449) in
    ()
  } in
  let inline527 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont526(inline527)
}

fn inherent#Channel#Channel[T]#recv__T_int(self/438: Channel[int]) -> Option__int {
  let mtmp379 = @intrinsic(channel.recv)(self/438) in
  let x380 = mtmp379.0 in
  let x381 = mtmp379.1 in
  if x381 {
    let t455 = Option__int::Some(x380) in
    t455
  } else {
    Tag_0
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont528(t457: string) -> unit {
    let t458 = @runtime(core.string_println)(t457) in
    ()
  } in
  jump inline_cont528(value/1)
}

fn roundtrip__T_string(channel/0: Channel[string], value/1: string) -> Option__string {
  let t461 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/1 } in
  let t462 =
    pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
      t461
    ) in
  let _wild408 = go t462 in
  let inline530 = @intrinsic(channel.recv)(channel/0) in
  let inline531 = inline530.0 in
  let inline532 = inline530.1 in
  if inline532 {
    let inline535 = Option__string::Some(inline531) in
    inline535
  } else {
    Tag_0
  }
}

fn inherent#Channel#Channel[T]#close__T_string(self/441: Channel[string]) -> unit {
  let t465 = @intrinsic(channel.close)(self/441) in
  ()
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t468 = @runtime(core.int_to_string)(self/151) in
  t468
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn inherent#Channel#Channel[T]#send__T_string(
  self/436: Channel[string],
  value/437: string
) -> unit {
  let t472 = @intrinsic(channel.send)(self/436, value/437) in
  ()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/438: Channel[string]) -> Option__string {
  let mtmp379 = @intrinsic(channel.recv)(self/438) in
  let x380 = mtmp379.0 in
  let x381 = mtmp379.1 in
  if x381 {
    let t477 = Option__string::Some(x380) in
    t477
  } else {
    Tag_0
  }
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(
  env425: closure_env_roundtrip_T_string_0
) -> unit {
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env425) in
  let value/1 = closure_env_roundtrip_T_string_0.value_1(env425) in
  let inline537 = @intrinsic(channel.send)(channel/0, value/1) in
  ()
}