fn make_string_channel() -> Channel[string] {
  let inline484 = 0 in
  let inline485 = @intrinsic(channel.new)(inline484) in
  inline485
}

fn main() -> unit {
  let buffered/2 = inherent#Channel#Channel[T]#new__T_int(2) in
  let _wild412 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 10) in
  let _wild413 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 20) in
  let _wild414 = inherent#Channel#Channel[T]#close__T_int(buffered/2) in
  join inline_cont522(mtmp415: Option__int) -> unit {
    join k433(jp434: int) -> unit {
      join inline_cont519(_wild417: unit) -> unit {
        join inline_cont512(mtmp418: Option__int) -> unit {
          join k435(jp436: int) -> unit {
            join inline_cont509(_wild420: unit) -> unit {
              join inline_cont502(mtmp421: Option__int) -> unit {
                join k437(jp438: string) -> unit {
                  join inline_cont499(_wild423: unit) -> unit {
                    join inline_cont497(unbuffered/5: Channel[string]) -> unit {
                      join inline_cont491(mtmp424: Option__string) -> unit {
                        join k439(jp440: string) -> unit {
                          join inline_cont488(_wild426: unit) -> unit {
                            let inline487 = @intrinsic(channel.close)(unbuffered/5) in
                            ()
                          } in
                          let inline489 = trait_impl#ToString#string#to_string(jp440) in
                          let inline490 = @runtime(core.string_println)(inline489) in
                          jump inline_cont488(())
                        } in
                        match mtmp424 {
                          Tag_0 => {
                            jump k439("closed")
                          },
                          Tag_1 => {
                            let x425 = Option__string::Some._0(mtmp424) in
                            jump k439(x425)
                          },
                        }
                      } in
                      let inline492 = "ready" in
                      let inline493 =
                        closure_env_roundtrip_T_string_0 {
                          channel_0: unbuffered/5,
                          value_1: inline492
                        } in
                      let inline494 =
                        pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
                          inline493
                        ) in
                      let inline495 = go inline494 in
                      let inline496 = inherent#Channel#Channel[T]#recv__T_string(unbuffered/5) in
                      jump inline_cont491(inline496)
                    } in
                    let inline498 = inherent#Channel#Channel[T]#new__T_string(0) in
                    jump inline_cont497(inline498)
                  } in
                  let inline500 = trait_impl#ToString#string#to_string(jp438) in
                  let inline501 = @runtime(core.string_println)(inline500) in
                  jump inline_cont499(())
                } in
                match mtmp421 {
                  Tag_0 => {
                    jump k437("closed")
                  },
                  Tag_1 => {
                    jump k437("open")
                  },
                }
              } in
              let inline503 = @intrinsic(channel.recv)(buffered/2) in
              let inline504 = inline503.0 in
              let inline505 = inline503.1 in
              if inline505 {
                let inline508 = Option__int::Some(inline504) in
                jump inline_cont502(inline508)
              } else {
                jump inline_cont502(Tag_0)
              }
            } in
            let inline510 = trait_impl#ToString#int#to_string(jp436) in
            let inline511 = @runtime(core.string_println)(inline510) in
            jump inline_cont509(())
          } in
          match mtmp418 {
            Tag_0 => {
              jump k435(-1)
            },
            Tag_1 => {
              let x419 = Option__int::Some._0(mtmp418) in
              jump k435(x419)
            },
          }
        } in
        let inline513 = @intrinsic(channel.recv)(buffered/2) in
        let inline514 = inline513.0 in
        let inline515 = inline513.1 in
        if inline515 {
          let inline518 = Option__int::Some(inline514) in
          jump inline_cont512(inline518)
        } else {
          jump inline_cont512(Tag_0)
        }
      } in
      let inline520 = trait_impl#ToString#int#to_string(jp434) in
      let inline521 = @runtime(core.string_println)(inline520) in
      jump inline_cont519(())
    } in
    match mtmp415 {
      Tag_0 => {
        jump k433(-1)
      },
      Tag_1 => {
        let x416 = Option__int::Some._0(mtmp415) in
        jump k433(x416)
      },
    }
  } in
  let inline523 = @intrinsic(channel.recv)(buffered/2) in
  let inline524 = inline523.0 in
  let inline525 = inline523.1 in
  if inline525 {
    let inline528 = Option__int::Some(inline524) in
    jump inline_cont522(inline528)
  } else {
    jump inline_cont522(Tag_0)
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/435: int) -> Channel[string] {
  let t443 = @intrinsic(channel.new)(capacity/435) in
  t443
}

fn inherent#Channel#Channel[T]#new__T_int(capacity/435: int) -> Channel[int] {
  let t446 = @intrinsic(channel.new)(capacity/435) in
  t446
}

fn inherent#Channel#Channel[T]#send__T_int(self/436: Channel[int], value/437: int) -> unit {
  let t448 = @intrinsic(channel.send)(self/436, value/437) in
  ()
}

fn inherent#Channel#Channel[T]#close__T_int(self/441: Channel[int]) -> unit {
  let t450 = @intrinsic(channel.close)(self/441) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont529(t452: string) -> unit {
    let t453 = @runtime(core.string_println)(t452) in
    ()
  } in
  let inline530 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont529(inline530)
}

fn inherent#Channel#Channel[T]#recv__T_int(self/438: Channel[int]) -> Option__int {
  let mtmp379 = @intrinsic(channel.recv)(self/438) in
  let x380 = mtmp379.0 in
  let x381 = mtmp379.1 in
  if x381 {
    let t458 = Option__int::Some(x380) in
    t458
  } else {
    Tag_0
  }
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont531(t460: string) -> unit {
    let t461 = @runtime(core.string_println)(t460) in
    ()
  } in
  jump inline_cont531(value/1)
}

fn roundtrip__T_string(channel/0: Channel[string], value/1: string) -> Option__string {
  let t464 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/1 } in
  let t465 =
    pack_closure[inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply](
      t464
    ) in
  let _wild411 = go t465 in
  let inline533 = @intrinsic(channel.recv)(channel/0) in
  let inline534 = inline533.0 in
  let inline535 = inline533.1 in
  if inline535 {
    let inline538 = Option__string::Some(inline534) in
    inline538
  } else {
    Tag_0
  }
}

fn inherent#Channel#Channel[T]#close__T_string(self/441: Channel[string]) -> unit {
  let t468 = @intrinsic(channel.close)(self/441) in
  ()
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t471 = @runtime(core.int_to_string)(self/151) in
  t471
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn inherent#Channel#Channel[T]#send__T_string(
  self/436: Channel[string],
  value/437: string
) -> unit {
  let t475 = @intrinsic(channel.send)(self/436, value/437) in
  ()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/438: Channel[string]) -> Option__string {
  let mtmp379 = @intrinsic(channel.recv)(self/438) in
  let x380 = mtmp379.0 in
  let x381 = mtmp379.1 in
  if x381 {
    let t480 = Option__string::Some(x380) in
    t480
  } else {
    Tag_0
  }
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(
  env428: closure_env_roundtrip_T_string_0
) -> unit {
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env428) in
  let value/1 = closure_env_roundtrip_T_string_0.value_1(env428) in
  let inline540 = @intrinsic(channel.send)(channel/0, value/1) in
  ()
}