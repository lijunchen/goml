fn make_string_channel() -> Channel[string] {
  join ret126(retv127: Channel[string]) -> Channel[string] {
    retv127
  } in
  let t128 = inherent#Channel#Channel[T]#new__T_string(0) in
  jump ret126(t128)
}

fn main() -> unit {
  join ret129() -> unit {
    ()
  } in
  let buffered/2 = inherent#Channel#Channel[T]#new__T_int(2) in
  let _wild109 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 10) in
  let _wild110 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 20) in
  let _wild111 = inherent#Channel#Channel[T]#close__T_int(buffered/2) in
  let mtmp112 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
  join k130(jp131: int) -> unit {
    let _wild114 = println__T_int(jp131) in
    let mtmp115 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
    join k132(jp133: int) -> unit {
      let _wild117 = println__T_int(jp133) in
      let mtmp118 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
      join k134(jp135: string) -> unit {
        let _wild120 = println__T_string(jp135) in
        let unbuffered/5 = make_string_channel() in
        let mtmp121 = roundtrip__T_string(unbuffered/5, "ready") in
        join k136(jp137: string) -> unit {
          let _wild123 = println__T_string(jp137) in
          let _wild124 = inherent#Channel#Channel[T]#close__T_string(unbuffered/5) in
          jump ret129()
        } in
        match mtmp121 {
          Tag_0 => {
            jump k136("closed")
          },
          Tag_1 => {
            let x122 = Option__string::Some._0(mtmp121) in
            let value/6 = x122 in
            jump k136(value/6)
          },
        }
      } in
      match mtmp118 {
        Tag_0 => {
          jump k134("closed")
        },
        Tag_1 => {
          let x119 = Option__int::Some._0(mtmp118) in
          jump k134("open")
        },
      }
    } in
    match mtmp115 {
      Tag_0 => {
        jump k132(-1)
      },
      Tag_1 => {
        let x116 = Option__int::Some._0(mtmp115) in
        let value/4 = x116 in
        jump k132(value/4)
      },
    }
  } in
  match mtmp112 {
    Tag_0 => {
      jump k130(-1)
    },
    Tag_1 => {
      let x113 = Option__int::Some._0(mtmp112) in
      let value/3 = x113 in
      jump k130(value/3)
    },
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/211: int) -> Channel[string] {
  join ret138(retv139: Channel[string]) -> Channel[string] {
    retv139
  } in
  let t140 = @intrinsic(channel.new)(capacity/211) in
  jump ret138(t140)
}

fn inherent#Channel#Channel[T]#new__T_int(capacity/211: int) -> Channel[int] {
  join ret141(retv142: Channel[int]) -> Channel[int] {
    retv142
  } in
  let t143 = @intrinsic(channel.new)(capacity/211) in
  jump ret141(t143)
}

fn inherent#Channel#Channel[T]#send__T_int(self/212: Channel[int], value/213: int) -> unit {
  join ret144() -> unit {
    ()
  } in
  let t145 = @intrinsic(channel.send)(self/212, value/213) in
  jump ret144()
}

fn inherent#Channel#Channel[T]#close__T_int(self/217: Channel[int]) -> unit {
  join ret146() -> unit {
    ()
  } in
  let t147 = @intrinsic(channel.close)(self/217) in
  jump ret146()
}

fn println__T_int(value/1: int) -> unit {
  join ret148() -> unit {
    ()
  } in
  let t149 = trait_impl#ToString#int#to_string(value/1) in
  let t150 = @runtime(core.string_println)(t149) in
  jump ret148()
}

fn inherent#Channel#Channel[T]#recv__T_int(self/214: Channel[int]) -> Option__int {
  join ret151(retv152: Option__int) -> Option__int {
    retv152
  } in
  let mtmp60 = @intrinsic(channel.recv)(self/214) in
  let x61 = mtmp60.0 in
  let x62 = mtmp60.1 in
  let ok/216 = x62 in
  let value/215 = x61 in
  join k153(jp154: Option__int) -> Option__int {
    jump ret151(jp154)
  } in
  if ok/216 {
    let t155 = Option__int::Some(value/215) in
    jump k153(t155)
  } else {
    jump k153(Tag_0)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret156() -> unit {
    ()
  } in
  let t157 = trait_impl#ToString#string#to_string(value/1) in
  let t158 = @runtime(core.string_println)(t157) in
  jump ret156()
}

fn roundtrip__T_string(channel/0: Channel[string], value/1: string) -> Option__string {
  join ret159(retv160: Option__string) -> Option__string {
    retv160
  } in
  let t161 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/1 } in
  let _wild108 = go t161 in
  let t162 = inherent#Channel#Channel[T]#recv__T_string(channel/0) in
  jump ret159(t162)
}

fn inherent#Channel#Channel[T]#close__T_string(self/217: Channel[string]) -> unit {
  join ret163() -> unit {
    ()
  } in
  let t164 = @intrinsic(channel.close)(self/217) in
  jump ret163()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret165(retv166: string) -> string {
    retv166
  } in
  let t167 = @runtime(core.int_to_string)(self/40) in
  jump ret165(t167)
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret168(retv169: string) -> string {
    retv169
  } in
  jump ret168(self/38)
}

fn inherent#Channel#Channel[T]#send__T_string(self/212: Channel[string], value/213: string) -> unit {
  join ret170() -> unit {
    ()
  } in
  let t171 = @intrinsic(channel.send)(self/212, value/213) in
  jump ret170()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/214: Channel[string]) -> Option__string {
  join ret172(retv173: Option__string) -> Option__string {
    retv173
  } in
  let mtmp60 = @intrinsic(channel.recv)(self/214) in
  let x61 = mtmp60.0 in
  let x62 = mtmp60.1 in
  let ok/216 = x62 in
  let value/215 = x61 in
  join k174(jp175: Option__string) -> Option__string {
    jump ret172(jp175)
  } in
  if ok/216 {
    let t176 = Option__string::Some(value/215) in
    jump k174(t176)
  } else {
    jump k174(Tag_0)
  }
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(env125: closure_env_roundtrip_T_string_0) -> unit {
  join ret177() -> unit {
    ()
  } in
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env125) in
  let value/1 = closure_env_roundtrip_T_string_0.value_1(env125) in
  let t178 = inherent#Channel#Channel[T]#send__T_string(channel/0, value/1) in
  jump ret177()
}