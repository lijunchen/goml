fn make_string_channel() -> Channel[string] {
  let t175 = inherent#Channel#Channel[T]#new__T_string(0) in
  t175
}

fn main() -> unit {
  let buffered/2 = inherent#Channel#Channel[T]#new__T_int(2) in
  let _wild156 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 10) in
  let _wild157 = inherent#Channel#Channel[T]#send__T_int(buffered/2, 20) in
  let _wild158 = inherent#Channel#Channel[T]#close__T_int(buffered/2) in
  let mtmp159 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
  join k177(jp178: int) -> unit {
    let _wild161 = println__T_int(jp178) in
    let mtmp162 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
    join k179(jp180: int) -> unit {
      let _wild164 = println__T_int(jp180) in
      let mtmp165 = inherent#Channel#Channel[T]#recv__T_int(buffered/2) in
      join k181(jp182: string) -> unit {
        let _wild167 = println__T_string(jp182) in
        let unbuffered/5 = make_string_channel() in
        let mtmp168 = roundtrip__T_string(unbuffered/5, "ready") in
        join k183(jp184: string) -> unit {
          let _wild170 = println__T_string(jp184) in
          let _wild171 = inherent#Channel#Channel[T]#close__T_string(unbuffered/5) in
          ()
        } in
        match mtmp168 {
          Tag_0 => {
            jump k183("closed")
          },
          Tag_1 => {
            let x169 = Option__string::Some._0(mtmp168) in
            let value/6 = x169 in
            jump k183(x169)
          },
        }
      } in
      match mtmp165 {
        Tag_0 => {
          jump k181("closed")
        },
        Tag_1 => {
          let x166 = Option__int::Some._0(mtmp165) in
          jump k181("open")
        },
      }
    } in
    match mtmp162 {
      Tag_0 => {
        jump k179(-1)
      },
      Tag_1 => {
        let x163 = Option__int::Some._0(mtmp162) in
        let value/4 = x163 in
        jump k179(x163)
      },
    }
  } in
  match mtmp159 {
    Tag_0 => {
      jump k177(-1)
    },
    Tag_1 => {
      let x160 = Option__int::Some._0(mtmp159) in
      let value/3 = x160 in
      jump k177(x160)
    },
  }
}

fn inherent#Channel#Channel[T]#new__T_string(capacity/211: int) -> Channel[string] {
  let t187 = @intrinsic(channel.new)(capacity/211) in
  t187
}

fn inherent#Channel#Channel[T]#new__T_int(capacity/211: int) -> Channel[int] {
  let t190 = @intrinsic(channel.new)(capacity/211) in
  t190
}

fn inherent#Channel#Channel[T]#send__T_int(self/212: Channel[int], value/213: int) -> unit {
  let t192 = @intrinsic(channel.send)(self/212, value/213) in
  ()
}

fn inherent#Channel#Channel[T]#close__T_int(self/217: Channel[int]) -> unit {
  let t194 = @intrinsic(channel.close)(self/217) in
  ()
}

fn println__T_int(value/1: int) -> unit {
  let t196 = trait_impl#ToString#int#to_string(value/1) in
  let t197 = @runtime(core.string_println)(t196) in
  ()
}

fn inherent#Channel#Channel[T]#recv__T_int(self/214: Channel[int]) -> Option__int {
  let mtmp101 = @intrinsic(channel.recv)(self/214) in
  let x102 = mtmp101.0 in
  let x103 = mtmp101.1 in
  let ok/216 = x103 in
  let value/215 = x102 in
  if x103 {
    let t202 = Option__int::Some(x102) in
    t202
  } else {
    Tag_0
  }
}

fn println__T_string(value/1: string) -> unit {
  let t204 = trait_impl#ToString#string#to_string(value/1) in
  let t205 = @runtime(core.string_println)(t204) in
  ()
}

fn roundtrip__T_string(channel/0: Channel[string], value/1: string) -> Option__string {
  let t208 = closure_env_roundtrip_T_string_0 { channel_0: channel/0, value_1: value/1 } in
  let _wild155 = go t208 in
  let t209 = inherent#Channel#Channel[T]#recv__T_string(channel/0) in
  t209
}

fn inherent#Channel#Channel[T]#close__T_string(self/217: Channel[string]) -> unit {
  let t211 = @intrinsic(channel.close)(self/217) in
  ()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  let t214 = @runtime(core.int_to_string)(self/40) in
  t214
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  self/38
}

fn inherent#Channel#Channel[T]#send__T_string(self/212: Channel[string], value/213: string) -> unit {
  let t218 = @intrinsic(channel.send)(self/212, value/213) in
  ()
}

fn inherent#Channel#Channel[T]#recv__T_string(self/214: Channel[string]) -> Option__string {
  let mtmp101 = @intrinsic(channel.recv)(self/214) in
  let x102 = mtmp101.0 in
  let x103 = mtmp101.1 in
  let ok/216 = x103 in
  let value/215 = x102 in
  if x103 {
    let t223 = Option__string::Some(x102) in
    t223
  } else {
    Tag_0
  }
}

fn inherent#closure_env_roundtrip_T_string_0#closure_env_roundtrip_T_string_0#apply(env172: closure_env_roundtrip_T_string_0) -> unit {
  let channel/0 = closure_env_roundtrip_T_string_0.channel_0(env172) in
  let value/1 = closure_env_roundtrip_T_string_0.value_1(env172) in
  let t225 = inherent#Channel#Channel[T]#send__T_string(channel/0, value/1) in
  ()
}