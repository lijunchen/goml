fn parse(flag/0: bool) -> Result[i32, string] {
    if (flag/0 : bool) {
        {
            Result::Ok(41)
        }
    } else {
        {
            Result::Err("bad")
        }
    }
}

fn compute(flag/1: bool) -> Result[i32, string] {
    let value/2: i32 =
      match (parse : (bool) -> Result[i32, string])((flag/1 : bool)) {
          Result::Ok(try_value/12: i32) => (try_value/12 : i32),
          Result::Err(try_residual/12: string) => return Result::Err((try_residual/12 : string)),
      };
    Result::Ok((value/2 : i32) + 1)
}

fn show(value/3: Result[i32, string]) -> string {
    match (value/3 : Result[i32, string]) {
        Result::Ok(value/4: i32) => (i32::to_string : (i32) -> string)((value/4 : i32)),
        Result::Err(error/5: string) => (error/5 : string),
    }
}

fn main() -> () {
    (println : (string) -> ())(
      (show : (Result[i32, string]) -> string)((compute : (bool) -> Result[i32, string])(true))
    );
    (println : (string) -> ())(
      (show : (Result[i32, string]) -> string)((compute : (bool) -> Result[i32, string])(false))
    )
}