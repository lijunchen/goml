fn main() -> unit {
  join k799() -> unit {
    ()
  } in
  let x796 = 42 in
  join inline_cont846(t800: string) -> unit {
    let inline844 = trait_impl#ToString#string#to_string(t800) in
    let inline845 = @runtime(core.string_println)(inline844) in
    jump k799()
  } in
  let inline847 = __goml_builtin_uint8_to_string(x796) in
  jump inline_cont846(inline847)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont848(t804: string) -> unit {
    let t805 = @runtime(core.string_println)(t804) in
    ()
  } in
  jump inline_cont848(value/1)
}

fn trait_impl#ToString#u8#to_string(self/409: u8) -> string {
  let inline850 = (self/409 as u64) in
  let inline851 = decimal_string(inline850) in
  inline851
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_uint8_to_string(value/228: u8) -> string {
  let t813 = (value/228 as u64) in
  let t814 = decimal_string(t813) in
  t814
}

fn decimal_string(value/208: u64) -> string {
  let t837 = (value/208 == 0) in
  join k817() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit818() -> string {
      join inline_cont860(t819: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t819) in
        let offset/212 = 0 in
        join exit820() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop821() -> string {
            join inline_cont858(t822: isize) -> string {
              let t823 = (offset/212 < t822) in
              if t823 {
                join inline_cont856(t824: isize) -> string {
                  let t825 = (t824 - offset/212) in
                  let t826 = (t825 - 1) in
                  let t827 = @intrinsic(vec.get)(reversed/209, t826) in
                  join inline_cont854(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t828 = (compound_old358 + compound_value359) in
                    let t829 = offset/212 = t828 in
                    jump loop821()
                  } in
                  let inline855 = @intrinsic(vec.push)(bytes/211, t827) in
                  jump inline_cont854(())
                } in
                let inline857 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont856(inline857)
              } else {
                jump exit820()
              }
            } in
            let inline859 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont858(inline859)
          } in
        } in
        jump loop821()
      } in
      let inline861 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont860(inline861)
    } in
    joinrec {
      join loop830() -> string {
        let t831 = (remaining/210 > 0) in
        if t831 {
          let t832 = (remaining/210 % 10) in
          let t833 = (t832 as u8) in
          let t834 = (t833 + 48) in
          join inline_cont852(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t835 = (compound_old353 / compound_value354) in
            let t836 = remaining/210 = t835 in
            jump loop830()
          } in
          let inline853 = @intrinsic(vec.push)(reversed/209, t834) in
          jump inline_cont852(())
        } else {
          jump exit818()
        }
      } in
    } in
    jump loop830()
  } in
  if t837 {
    "0"
  } else {
    jump k817()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t839 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t842 = @intrinsic(vec.len)(self/526) in
  t842
}