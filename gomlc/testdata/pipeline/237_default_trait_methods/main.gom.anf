fn trait_impl#Describe#DefaultValue#name(self/3: DefaultValue) -> string {
  let t414 = DefaultValue.name(self/3) in
  t414
}

fn trait_impl#Describe#DefaultValue#describe(default_arg0: DefaultValue) -> string {
  let inline444 = trait_impl#Describe#DefaultValue#name(default_arg0) in
  let inline445 = ("default:" + inline444) in
  inline445
}

fn trait_impl#Describe#OverrideValue#name(self/4: OverrideValue) -> string {
  "override-name"
}

fn trait_impl#Describe#OverrideValue#describe(self/5: OverrideValue) -> string {
  "override"
}

fn describe_dyn(value/6: dyn Describe) -> string {
  let t424 = dyn_call[Describe::describe](value/6) in
  t424
}

fn main() -> unit {
  let default_value/7 = DefaultValue { name: "value" } in
  join inline_cont463(t426: string) -> unit {
    join inline_cont460(_wild408: unit) -> unit {
      join inline_cont458(t427: string) -> unit {
        join inline_cont455(_wild409: unit) -> unit {
          join inline_cont454(t429: string) -> unit {
            join inline_cont451(_wild410: unit) -> unit {
              let t430 = to_dyn[Describe]{DefaultValue}(default_value/7) in
              join inline_cont449(t431: string) -> unit {
                let inline447 = trait_impl#ToString#string#to_string(t431) in
                let inline448 = @runtime(core.string_println)(inline447) in
                ()
              } in
              let inline450 = dyn_call[Describe::describe](t430) in
              jump inline_cont449(inline450)
            } in
            let inline452 = trait_impl#ToString#string#to_string(t429) in
            let inline453 = @runtime(core.string_println)(inline452) in
            jump inline_cont451(())
          } in
          jump inline_cont454("override")
        } in
        let inline456 = trait_impl#ToString#string#to_string(t427) in
        let inline457 = @runtime(core.string_println)(inline456) in
        jump inline_cont455(())
      } in
      let inline459 = "generic" in
      jump inline_cont458(inline459)
    } in
    let inline461 = trait_impl#ToString#string#to_string(t426) in
    let inline462 = @runtime(core.string_println)(inline461) in
    jump inline_cont460(())
  } in
  let inline464 = trait_impl#Describe#DefaultValue#name(default_value/7) in
  let inline465 = ("default:" + inline464) in
  jump inline_cont463(inline465)
}

fn trait_default#Describe#describe__Self_DefaultValue(self/0: DefaultValue) -> string {
  join inline_cont466(t434: string) -> string {
    let t435 = ("default:" + t434) in
    t435
  } in
  let inline467 = DefaultValue.name(self/0) in
  jump inline_cont466(inline467)
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont468(t437: string) -> unit {
    let t438 = @runtime(core.string_println)(t437) in
    ()
  } in
  jump inline_cont468(value/1)
}

fn trait_default#Keep#keep__Self_DefaultValue__T_string(
  self/1: DefaultValue,
  value/2: string
) -> string {
  value/2
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}