fn trait_impl#Describe#DefaultValue#name(self/0: DefaultValue) -> string {
  let t0 = DefaultValue.name(self/0) in
  t0
}

fn trait_impl#Describe#DefaultValue#describe(default_arg0: DefaultValue) -> string {
  let inline0 = trait_impl#Describe#DefaultValue#name(default_arg0) in
  let inline1 = ("default:" + inline0) in
  inline1
}

fn trait_impl#Describe#OverrideValue#name(self/0: OverrideValue) -> string {
  "override-name"
}

fn trait_impl#Describe#OverrideValue#describe(self/0: OverrideValue) -> string {
  "override"
}

fn describe_dyn(value/0: dyn Describe) -> string {
  let t0 = dyn_call[Describe::describe](value/0) in
  t0
}

fn main() -> unit {
  let default_value/0 = DefaultValue { name: "value" } in
  join inline_cont0(t0: string) -> unit {
    join inline_cont1(_wild0: unit) -> unit {
      join inline_cont2(t1: string) -> unit {
        join inline_cont3(_wild1: unit) -> unit {
          join inline_cont4(t2: string) -> unit {
            join inline_cont5(_wild2: unit) -> unit {
              let t3 = to_dyn[Describe]{DefaultValue}(default_value/0) in
              join inline_cont6(t4: string) -> unit {
                let inline0 = trait_impl#ToString#string#to_string(t4) in
                let inline1 = @runtime(core.string_println)(inline0) in
                ()
              } in
              let inline2 = dyn_call[Describe::describe](t3) in
              jump inline_cont6(inline2)
            } in
            let inline3 = trait_impl#ToString#string#to_string(t2) in
            let inline4 = @runtime(core.string_println)(inline3) in
            jump inline_cont5(())
          } in
          jump inline_cont4("override")
        } in
        let inline5 = trait_impl#ToString#string#to_string(t1) in
        let inline6 = @runtime(core.string_println)(inline5) in
        jump inline_cont3(())
      } in
      let inline7 = "generic" in
      jump inline_cont2(inline7)
    } in
    let inline8 = trait_impl#ToString#string#to_string(t0) in
    let inline9 = @runtime(core.string_println)(inline8) in
    jump inline_cont1(())
  } in
  let inline10 = trait_impl#Describe#DefaultValue#name(default_value/0) in
  let inline11 = ("default:" + inline10) in
  jump inline_cont0(inline11)
}

fn trait_default#Describe#describe__Self_DefaultValue(self/0: DefaultValue) -> string {
  join inline_cont0(t0: string) -> string {
    let t1 = ("default:" + t0) in
    t1
  } in
  let inline0 = DefaultValue.name(self/0) in
  jump inline_cont0(inline0)
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_default#Keep#keep__Self_DefaultValue__T_string(
  self/0: DefaultValue,
  value/0: string
) -> string {
  value/0
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}