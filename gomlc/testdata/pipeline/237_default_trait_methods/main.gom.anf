fn trait_impl#Describe#DefaultValue#name(self/3: DefaultValue) -> string {
  let t178 = DefaultValue.name(self/3) in
  t178
}

fn trait_impl#Describe#DefaultValue#describe(default_arg0: DefaultValue) -> string {
  let inline208 = trait_impl#Describe#DefaultValue#name(default_arg0) in
  let inline209 = ("default:" + inline208) in
  inline209
}

fn trait_impl#Describe#OverrideValue#name(self/4: OverrideValue) -> string {
  "override-name"
}

fn trait_impl#Describe#OverrideValue#describe(self/5: OverrideValue) -> string {
  "override"
}

fn describe_dyn(value/6: dyn Describe) -> string {
  let t188 = dyn_call[Describe::describe](value/6) in
  t188
}

fn main() -> unit {
  let default_value/7 = DefaultValue { name: "value" } in
  join inline_cont227(t190: string) -> unit {
    join inline_cont224(_wild172: unit) -> unit {
      join inline_cont222(t191: string) -> unit {
        join inline_cont219(_wild173: unit) -> unit {
          join inline_cont218(t193: string) -> unit {
            join inline_cont215(_wild174: unit) -> unit {
              let t194 = to_dyn[Describe]{DefaultValue}(default_value/7) in
              join inline_cont213(t195: string) -> unit {
                let inline211 = trait_impl#ToString#string#to_string(t195) in
                let inline212 = @runtime(core.string_println)(inline211) in
                ()
              } in
              let inline214 = dyn_call[Describe::describe](t194) in
              jump inline_cont213(inline214)
            } in
            let inline216 = trait_impl#ToString#string#to_string(t193) in
            let inline217 = @runtime(core.string_println)(inline216) in
            jump inline_cont215(())
          } in
          jump inline_cont218("override")
        } in
        let inline220 = trait_impl#ToString#string#to_string(t191) in
        let inline221 = @runtime(core.string_println)(inline220) in
        jump inline_cont219(())
      } in
      let inline223 = "generic" in
      jump inline_cont222(inline223)
    } in
    let inline225 = trait_impl#ToString#string#to_string(t190) in
    let inline226 = @runtime(core.string_println)(inline225) in
    jump inline_cont224(())
  } in
  let inline228 = trait_impl#Describe#DefaultValue#name(default_value/7) in
  let inline229 = ("default:" + inline228) in
  jump inline_cont227(inline229)
}

fn trait_default#Describe#describe__Self_DefaultValue(self/0: DefaultValue) -> string {
  join inline_cont230(t198: string) -> string {
    let t199 = ("default:" + t198) in
    t199
  } in
  let inline231 = DefaultValue.name(self/0) in
  jump inline_cont230(inline231)
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont232(t201: string) -> unit {
    let t202 = @runtime(core.string_println)(t201) in
    ()
  } in
  jump inline_cont232(value/31)
}

fn trait_default#Keep#keep__Self_DefaultValue__T_string(
  self/1: DefaultValue,
  value/2: string
) -> string {
  value/2
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}