fn main() -> unit {
  join inline_cont905(v/0: Vec[i32]) -> unit {
    join inline_cont902(_wild796: unit) -> unit {
      join inline_cont899(_wild797: unit) -> unit {
        join inline_cont896(_wild798: unit) -> unit {
          let first/1 = @intrinsic(vec.get)(v/0, 0) in
          let second/2 = @intrinsic(vec.get)(v/0, 1) in
          let third/3 = @intrinsic(vec.get)(v/0, 2) in
          join inline_cont894(len/4: isize) -> unit {
            join inline_cont892(t804: string) -> unit {
              join inline_cont889(mtmp799: unit) -> unit {
                join inline_cont887(t805: string) -> unit {
                  join inline_cont884(mtmp800: unit) -> unit {
                    join inline_cont882(t806: string) -> unit {
                      join inline_cont879(mtmp801: unit) -> unit {
                        join inline_cont877(t807: string) -> unit {
                          let inline875 = trait_impl#ToString#string#to_string(t807) in
                          let inline876 = @runtime(core.string_println)(inline875) in
                          ()
                        } in
                        let inline878 = __goml_builtin_int_to_string(len/4) in
                        jump inline_cont877(inline878)
                      } in
                      let inline880 = trait_impl#ToString#string#to_string(t806) in
                      let inline881 = @runtime(core.string_println)(inline880) in
                      jump inline_cont879(())
                    } in
                    let inline883 = __goml_builtin_int32_to_string(third/3) in
                    jump inline_cont882(inline883)
                  } in
                  let inline885 = trait_impl#ToString#string#to_string(t805) in
                  let inline886 = @runtime(core.string_println)(inline885) in
                  jump inline_cont884(())
                } in
                let inline888 = __goml_builtin_int32_to_string(second/2) in
                jump inline_cont887(inline888)
              } in
              let inline890 = trait_impl#ToString#string#to_string(t804) in
              let inline891 = @runtime(core.string_println)(inline890) in
              jump inline_cont889(())
            } in
            let inline893 = __goml_builtin_int32_to_string(first/1) in
            jump inline_cont892(inline893)
          } in
          let inline895 = @intrinsic(vec.len)(v/0) in
          jump inline_cont894(inline895)
        } in
        let inline897 = 30 in
        let inline898 = @intrinsic(vec.push)(v/0, inline897) in
        jump inline_cont896(())
      } in
      let inline900 = 20 in
      let inline901 = @intrinsic(vec.push)(v/0, inline900) in
      jump inline_cont899(())
    } in
    let inline903 = 10 in
    let inline904 = @intrinsic(vec.push)(v/0, inline903) in
    jump inline_cont902(())
  } in
  let inline906 = @intrinsic(vec.new)() in
  jump inline_cont905(inline906)
}

fn inherent#Vec#Vec[T]#new__T_i32() -> Vec[i32] {
  let t810 = @intrinsic(vec.new)() in
  t810
}

fn inherent#Vec#Vec[T]#push__T_i32(self/511: Vec[i32], elem/512: i32) -> unit {
  let t812 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_i32(self/526: Vec[i32]) -> isize {
  let t815 = @intrinsic(vec.len)(self/526) in
  t815
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont907(t817: string) -> unit {
    let t818 = @runtime(core.string_println)(t817) in
    ()
  } in
  jump inline_cont907(value/1)
}

fn inherent#i32#i32#to_string(self/286: i32) -> string {
  let inline909 = (self/286 as i64) in
  let inline910 = signed_decimal_string(inline909) in
  inline910
}

fn inherent#isize#isize#to_string(self/285: isize) -> string {
  let inline912 = (self/285 as i64) in
  let inline913 = signed_decimal_string(inline912) in
  inline913
}

fn trait_impl#ToString#string#to_string(self/402: string) -> string {
  self/402
}

fn __goml_builtin_int32_to_string(value/225: i32) -> string {
  let t829 = (value/225 as i64) in
  let inline915 = (t829 < 0) in
  if inline915 {
    let inline916 = (t829 as u64) in
    let inline917 = (0 - inline916) in
    let inline918 = decimal_string(inline917) in
    let inline919 = ("-" + inline918) in
    inline919
  } else {
    let inline920 = (t829 as u64) in
    let inline921 = decimal_string(inline920) in
    inline921
  }
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t833 = (value/222 as i64) in
  let inline923 = (t833 < 0) in
  if inline923 {
    let inline924 = (t833 as u64) in
    let inline925 = (0 - inline924) in
    let inline926 = decimal_string(inline925) in
    let inline927 = ("-" + inline926) in
    inline927
  } else {
    let inline928 = (t833 as u64) in
    let inline929 = decimal_string(inline928) in
    inline929
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t839 = (value/214 < 0) in
  if t839 {
    let t840 = (value/214 as u64) in
    let t841 = (0 - t840) in
    let t842 = decimal_string(t841) in
    let t843 = ("-" + t842) in
    t843
  } else {
    let t844 = (value/214 as u64) in
    let t845 = decimal_string(t844) in
    t845
  }
}

fn decimal_string(value/208: u64) -> string {
  let t868 = (value/208 == 0) in
  join k848() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit849() -> string {
      join inline_cont938(t850: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t850) in
        let offset/212 = 0 in
        join exit851() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop852() -> string {
            join inline_cont936(t853: isize) -> string {
              let t854 = (offset/212 < t853) in
              if t854 {
                join inline_cont934(t855: isize) -> string {
                  let t856 = (t855 - offset/212) in
                  let t857 = (t856 - 1) in
                  let t858 = @intrinsic(vec.get)(reversed/209, t857) in
                  join inline_cont932(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t859 = (compound_old358 + compound_value359) in
                    let t860 = offset/212 = t859 in
                    jump loop852()
                  } in
                  let inline933 = @intrinsic(vec.push)(bytes/211, t858) in
                  jump inline_cont932(())
                } in
                let inline935 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont934(inline935)
              } else {
                jump exit851()
              }
            } in
            let inline937 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont936(inline937)
          } in
        } in
        jump loop852()
      } in
      let inline939 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont938(inline939)
    } in
    joinrec {
      join loop861() -> string {
        let t862 = (remaining/210 > 0) in
        if t862 {
          let t863 = (remaining/210 % 10) in
          let t864 = (t863 as u8) in
          let t865 = (t864 + 48) in
          join inline_cont930(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t866 = (compound_old353 / compound_value354) in
            let t867 = remaining/210 = t866 in
            jump loop861()
          } in
          let inline931 = @intrinsic(vec.push)(reversed/209, t865) in
          jump inline_cont930(())
        } else {
          jump exit849()
        }
      } in
    } in
    jump loop861()
  } in
  if t868 {
    "0"
  } else {
    jump k848()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t870 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t873 = @intrinsic(vec.len)(self/526) in
  t873
}