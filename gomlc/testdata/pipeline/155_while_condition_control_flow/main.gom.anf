fn main() -> unit {
  join ret80() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit81() -> unit {
    let _wild70 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit82() -> unit {
      let _wild73 = () in
      let t83 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild74 = println__T_int(t83) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit84() -> unit {
        let _wild78 = () in
        let t85 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild79 = println__T_int(t85) in
        jump ret80()
      } in
      joinrec {
        join loop86() -> unit {
          let mtmp75 = inherent#Ref#Ref[T]#get__T_int(k/3) in
          join k87(jp88: bool) -> unit {
            if jp88 {
              let t89 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t90 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t91 = (t89 + t90) in
              let _wild76 = inherent#Ref#Ref[T]#set__T_int(sum/4, t91) in
              let t92 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t93 = (t92 + 1) in
              let _wild77 = inherent#Ref#Ref[T]#set__T_int(k/3, t93) in
              jump loop86()
            } else {
              jump exit84()
            }
          } in
          match mtmp75 {
            0 => {
              jump k87(true)
            },
            1 => {
              let t96 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t97 = trait_impl#Eq#int#eq(t96, 0) in
              join k94(jp95: bool) -> unit {
                jump k87(jp95)
              } in
              if t97 {
                jump k94(true)
              } else {
                jump k94(false)
              }
            },
            2 => {
              jump k87(true)
            },
            _ => {
              jump k87(false)
            },
          }
        } in
      } in
      jump loop86()
    } in
    joinrec {
      join loop98() -> unit {
        let t106 = inherent#Ref#Ref[T]#get__T_int(j/1) in
        let t107 = (t106 < 4) in
        join k99(jp100: bool) -> unit {
          if jp100 {
            let t101 = inherent#Ref#Ref[T]#get__T_int(total/2) in
            let t102 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t103 = (t101 + t102) in
            let _wild71 = inherent#Ref#Ref[T]#set__T_int(total/2, t103) in
            let t104 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t105 = (t104 + 1) in
            let _wild72 = inherent#Ref#Ref[T]#set__T_int(j/1, t105) in
            jump loop98()
          } else {
            jump exit82()
          }
        } in
        if t107 {
          let t110 = inherent#Ref#Ref[T]#get__T_int(j/1) in
          let t111 = trait_impl#Eq#int#eq(t110, 1) in
          join k108(jp109: bool) -> unit {
            jump k99(jp109)
          } in
          if t111 {
            jump k108(true)
          } else {
            let t112 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t113 = trait_impl#Eq#int#eq(t112, 3) in
            let t114 = (!t113) in
            jump k108(t114)
          }
        } else {
          jump k99(false)
        }
      } in
    } in
    jump loop98()
  } in
  joinrec {
    join loop115() -> unit {
      let t121 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t122 = (t121 < 3) in
      join k116(jp117: bool) -> unit {
        if jp117 {
          let t118 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let _wild68 = println__T_int(t118) in
          let t119 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t120 = (t119 + 1) in
          let _wild69 = inherent#Ref#Ref[T]#set__T_int(i/0, t120) in
          jump loop115()
        } else {
          jump exit81()
        }
      } in
      if t122 {
        jump k116(true)
      } else {
        jump k116(false)
      }
    } in
  } in
  jump loop115()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret123(retv124: Ref[int]) -> Ref[int] {
    retv124
  } in
  let t125 = @intrinsic(ref.new)(value/207) in
  jump ret123(t125)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret126(retv127: int) -> int {
    retv127
  } in
  let t128 = @intrinsic(ref.get)(self/208) in
  jump ret126(t128)
}

fn println__T_int(value/1: int) -> unit {
  join ret129() -> unit {
    ()
  } in
  let t130 = trait_impl#ToString#int#to_string(value/1) in
  let t131 = @runtime(core.string_println)(t130) in
  jump ret129()
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret132() -> unit {
    ()
  } in
  let t133 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret132()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret134(retv135: bool) -> bool {
    retv135
  } in
  let t136 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret134(t136)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret137(retv138: string) -> string {
    retv138
  } in
  let t139 = @runtime(core.int_to_string)(self/40) in
  jump ret137(t139)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret140(retv141: uint64) -> uint64 {
    retv141
  } in
  let t142 = @runtime(core.int_hash)(self/85) in
  jump ret140(t142)
}