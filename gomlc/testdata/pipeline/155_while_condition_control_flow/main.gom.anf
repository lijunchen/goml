fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit195() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit196() -> unit {
      let t197 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild188 = println__T_int(t197) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit198() -> unit {
        let t199 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild193 = println__T_int(t199) in
        ()
      } in
      joinrec {
        join loop200() -> unit {
          join inline_cont305(mtmp189: int) -> unit {
            join k201(jp202: bool) -> unit {
              if jp202 {
                join inline_cont303(t203: int) -> unit {
                  join inline_cont301(t204: int) -> unit {
                    let t205 = (t203 + t204) in
                    join inline_cont299(_wild190: unit) -> unit {
                      join inline_cont297(t206: int) -> unit {
                        let t207 = (t206 + 1) in
                        let inline296 = @intrinsic(ref.set)(k/3, t207) in
                        jump loop200()
                      } in
                      let inline298 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont297(inline298)
                    } in
                    let inline300 = @intrinsic(ref.set)(sum/4, t205) in
                    jump inline_cont299(())
                  } in
                  let inline302 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont301(inline302)
                } in
                let inline304 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont303(inline304)
              } else {
                jump exit198()
              }
            } in
            match mtmp189 {
              0 => {
                jump k201(true)
              },
              1 => {
                join inline_cont293(t210: int) -> unit {
                  join inline_cont290(t211: bool) -> unit {
                    if t211 {
                      jump k201(true)
                    } else {
                      jump k201(false)
                    }
                  } in
                  let inline291 = 0 in
                  let inline292 = (t210 == inline291) in
                  jump inline_cont290(inline292)
                } in
                let inline294 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont293(inline294)
              },
              2 => {
                jump k201(true)
              },
              _ => {
                jump k201(false)
              },
            }
          } in
          let inline306 = @intrinsic(ref.get)(k/3) in
          jump inline_cont305(inline306)
        } in
      } in
      jump loop200()
    } in
    joinrec {
      join loop212() -> unit {
        join inline_cont288(t220: int) -> unit {
          let t221 = (t220 < 4) in
          join k213(jp214: bool) -> unit {
            if jp214 {
              join inline_cont286(t215: int) -> unit {
                join inline_cont284(t216: int) -> unit {
                  let t217 = (t215 + t216) in
                  join inline_cont282(_wild185: unit) -> unit {
                    join inline_cont280(t218: int) -> unit {
                      let t219 = (t218 + 1) in
                      let inline279 = @intrinsic(ref.set)(j/1, t219) in
                      jump loop212()
                    } in
                    let inline281 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont280(inline281)
                  } in
                  let inline283 = @intrinsic(ref.set)(total/2, t217) in
                  jump inline_cont282(())
                } in
                let inline285 = @intrinsic(ref.get)(j/1) in
                jump inline_cont284(inline285)
              } in
              let inline287 = @intrinsic(ref.get)(total/2) in
              jump inline_cont286(inline287)
            } else {
              jump exit196()
            }
          } in
          if t221 {
            join inline_cont276(t224: int) -> unit {
              join inline_cont273(t225: bool) -> unit {
                if t225 {
                  jump k213(true)
                } else {
                  join inline_cont271(t226: int) -> unit {
                    join inline_cont268(t227: bool) -> unit {
                      let t228 = (!t227) in
                      jump k213(t228)
                    } in
                    let inline269 = 3 in
                    let inline270 = (t226 == inline269) in
                    jump inline_cont268(inline270)
                  } in
                  let inline272 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont271(inline272)
                }
              } in
              let inline274 = 1 in
              let inline275 = (t224 == inline274) in
              jump inline_cont273(inline275)
            } in
            let inline277 = @intrinsic(ref.get)(j/1) in
            jump inline_cont276(inline277)
          } else {
            jump k213(false)
          }
        } in
        let inline289 = @intrinsic(ref.get)(j/1) in
        jump inline_cont288(inline289)
      } in
    } in
    jump loop212()
  } in
  joinrec {
    join loop229() -> unit {
      join inline_cont266(t235: int) -> unit {
        let t236 = (t235 < 3) in
        join k230(jp231: bool) -> unit {
          if jp231 {
            join inline_cont264(t232: int) -> unit {
              join inline_cont261(_wild182: unit) -> unit {
                join inline_cont259(t233: int) -> unit {
                  let t234 = (t233 + 1) in
                  let inline258 = @intrinsic(ref.set)(i/0, t234) in
                  jump loop229()
                } in
                let inline260 = @intrinsic(ref.get)(i/0) in
                jump inline_cont259(inline260)
              } in
              let inline262 = trait_impl#ToString#int#to_string(t232) in
              let inline263 = @runtime(core.string_println)(inline262) in
              jump inline_cont261(())
            } in
            let inline265 = @intrinsic(ref.get)(i/0) in
            jump inline_cont264(inline265)
          } else {
            jump exit195()
          }
        } in
        if t236 {
          jump k230(true)
        } else {
          jump k230(false)
        }
      } in
      let inline267 = @intrinsic(ref.get)(i/0) in
      jump inline_cont266(inline267)
    } in
  } in
  jump loop229()
}

fn inherent#Ref#Ref[T]#new__T_int(value/270: int) -> Ref[int] {
  let t239 = @intrinsic(ref.new)(value/270) in
  t239
}

fn inherent#Ref#Ref[T]#get__T_int(self/271: Ref[int]) -> int {
  let t242 = @intrinsic(ref.get)(self/271) in
  t242
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont307(t244: string) -> unit {
    let t245 = @runtime(core.string_println)(t244) in
    ()
  } in
  let inline308 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont307(inline308)
}

fn inherent#Ref#Ref[T]#set__T_int(self/272: Ref[int], value/273: int) -> unit {
  let t247 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t250 = (self/101 == other/102) in
  t250
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t253 = @runtime(core.int_to_string)(self/67) in
  t253
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t256 = @runtime(core.int_hash)(self/127) in
  t256
}