fn main() -> unit {
  join ret76() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit77() -> unit {
    let _wild66 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit78() -> unit {
      let _wild69 = () in
      let t79 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild70 = println__T_int(t79) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit80() -> unit {
        let _wild74 = () in
        let t81 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild75 = println__T_int(t81) in
        jump ret76()
      } in
      joinrec {
        join loop82() -> unit {
          let mtmp71 = inherent#Ref#Ref[T]#get__T_int(k/3) in
          join k83(jp84: bool) -> unit {
            if jp84 {
              let t85 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t86 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t87 = (t85 + t86) in
              let _wild72 = inherent#Ref#Ref[T]#set__T_int(sum/4, t87) in
              let t88 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t89 = (t88 + 1) in
              let _wild73 = inherent#Ref#Ref[T]#set__T_int(k/3, t89) in
              jump loop82()
            } else {
              jump exit80()
            }
          } in
          match mtmp71 {
            0 => {
              jump k83(true)
            },
            1 => {
              let t92 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t93 = trait_impl#Eq#int#eq(t92, 0) in
              join k90(jp91: bool) -> unit {
                jump k83(jp91)
              } in
              if t93 {
                jump k90(true)
              } else {
                jump k90(false)
              }
            },
            2 => {
              jump k83(true)
            },
            _ => {
              jump k83(false)
            },
          }
        } in
      } in
      jump loop82()
    } in
    joinrec {
      join loop94() -> unit {
        let t102 = inherent#Ref#Ref[T]#get__T_int(j/1) in
        let t103 = (t102 < 4) in
        join k95(jp96: bool) -> unit {
          if jp96 {
            let t97 = inherent#Ref#Ref[T]#get__T_int(total/2) in
            let t98 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t99 = (t97 + t98) in
            let _wild67 = inherent#Ref#Ref[T]#set__T_int(total/2, t99) in
            let t100 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t101 = (t100 + 1) in
            let _wild68 = inherent#Ref#Ref[T]#set__T_int(j/1, t101) in
            jump loop94()
          } else {
            jump exit78()
          }
        } in
        if t103 {
          let t106 = inherent#Ref#Ref[T]#get__T_int(j/1) in
          let t107 = trait_impl#Eq#int#eq(t106, 1) in
          join k104(jp105: bool) -> unit {
            jump k95(jp105)
          } in
          if t107 {
            jump k104(true)
          } else {
            let t108 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t109 = trait_impl#Eq#int#eq(t108, 3) in
            let t110 = (!t109) in
            jump k104(t110)
          }
        } else {
          jump k95(false)
        }
      } in
    } in
    jump loop94()
  } in
  joinrec {
    join loop111() -> unit {
      let t117 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t118 = (t117 < 3) in
      join k112(jp113: bool) -> unit {
        if jp113 {
          let t114 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let _wild64 = println__T_int(t114) in
          let t115 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t116 = (t115 + 1) in
          let _wild65 = inherent#Ref#Ref[T]#set__T_int(i/0, t116) in
          jump loop111()
        } else {
          jump exit77()
        }
      } in
      if t118 {
        jump k112(true)
      } else {
        jump k112(false)
      }
    } in
  } in
  jump loop111()
}

fn inherent#Ref#Ref[T]#new__T_int(value/209: int) -> Ref[int] {
  join ret119(retv120: Ref[int]) -> Ref[int] {
    retv120
  } in
  let t121 = @intrinsic(ref.new)(value/209) in
  jump ret119(t121)
}

fn inherent#Ref#Ref[T]#get__T_int(self/210: Ref[int]) -> int {
  join ret122(retv123: int) -> int {
    retv123
  } in
  let t124 = @intrinsic(ref.get)(self/210) in
  jump ret122(t124)
}

fn println__T_int(value/1: int) -> unit {
  join ret125() -> unit {
    ()
  } in
  let t126 = trait_impl#ToString#int#to_string(value/1) in
  let t127 = @runtime(core.string_println)(t126) in
  jump ret125()
}

fn inherent#Ref#Ref[T]#set__T_int(self/211: Ref[int], value/212: int) -> unit {
  join ret128() -> unit {
    ()
  } in
  let t129 = @intrinsic(ref.set)(self/211, value/212) in
  jump ret128()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret130(retv131: bool) -> bool {
    retv131
  } in
  let t132 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret130(t132)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret133(retv134: string) -> string {
    retv134
  } in
  let t135 = @runtime(core.int_to_string)(self/40) in
  jump ret133(t135)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret136(retv137: uint64) -> uint64 {
    retv137
  } in
  let t138 = @runtime(core.int_hash)(self/85) in
  jump ret136(t138)
}