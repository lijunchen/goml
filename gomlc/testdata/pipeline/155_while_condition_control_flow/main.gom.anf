fn main() -> unit {
  join ret120() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit121() -> unit {
    let _wild110 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit122() -> unit {
      let _wild113 = () in
      let t123 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild114 = println__T_int(t123) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit124() -> unit {
        let _wild118 = () in
        let t125 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild119 = println__T_int(t125) in
        jump ret120()
      } in
      joinrec {
        join loop126() -> unit {
          let mtmp115 = inherent#Ref#Ref[T]#get__T_int(k/3) in
          join k127(jp128: bool) -> unit {
            if jp128 {
              let t129 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t130 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t131 = (t129 + t130) in
              let _wild116 = inherent#Ref#Ref[T]#set__T_int(sum/4, t131) in
              let t132 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t133 = (t132 + 1) in
              let _wild117 = inherent#Ref#Ref[T]#set__T_int(k/3, t133) in
              jump loop126()
            } else {
              jump exit124()
            }
          } in
          match mtmp115 {
            0 => {
              jump k127(true)
            },
            1 => {
              let t136 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t137 = trait_impl#Eq#int#eq(t136, 0) in
              join k134(jp135: bool) -> unit {
                jump k127(jp135)
              } in
              if t137 {
                jump k134(true)
              } else {
                jump k134(false)
              }
            },
            2 => {
              jump k127(true)
            },
            _ => {
              jump k127(false)
            },
          }
        } in
      } in
      jump loop126()
    } in
    joinrec {
      join loop138() -> unit {
        let t146 = inherent#Ref#Ref[T]#get__T_int(j/1) in
        let t147 = (t146 < 4) in
        join k139(jp140: bool) -> unit {
          if jp140 {
            let t141 = inherent#Ref#Ref[T]#get__T_int(total/2) in
            let t142 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t143 = (t141 + t142) in
            let _wild111 = inherent#Ref#Ref[T]#set__T_int(total/2, t143) in
            let t144 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t145 = (t144 + 1) in
            let _wild112 = inherent#Ref#Ref[T]#set__T_int(j/1, t145) in
            jump loop138()
          } else {
            jump exit122()
          }
        } in
        if t147 {
          let t150 = inherent#Ref#Ref[T]#get__T_int(j/1) in
          let t151 = trait_impl#Eq#int#eq(t150, 1) in
          join k148(jp149: bool) -> unit {
            jump k139(jp149)
          } in
          if t151 {
            jump k148(true)
          } else {
            let t152 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t153 = trait_impl#Eq#int#eq(t152, 3) in
            let t154 = (!t153) in
            jump k148(t154)
          }
        } else {
          jump k139(false)
        }
      } in
    } in
    jump loop138()
  } in
  joinrec {
    join loop155() -> unit {
      let t161 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t162 = (t161 < 3) in
      join k156(jp157: bool) -> unit {
        if jp157 {
          let t158 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let _wild108 = println__T_int(t158) in
          let t159 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t160 = (t159 + 1) in
          let _wild109 = inherent#Ref#Ref[T]#set__T_int(i/0, t160) in
          jump loop155()
        } else {
          jump exit121()
        }
      } in
      if t162 {
        jump k156(true)
      } else {
        jump k156(false)
      }
    } in
  } in
  jump loop155()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret163(retv164: Ref[int]) -> Ref[int] {
    retv164
  } in
  let t165 = @intrinsic(ref.new)(value/207) in
  jump ret163(t165)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret166(retv167: int) -> int {
    retv167
  } in
  let t168 = @intrinsic(ref.get)(self/208) in
  jump ret166(t168)
}

fn println__T_int(value/1: int) -> unit {
  join ret169() -> unit {
    ()
  } in
  let t170 = trait_impl#ToString#int#to_string(value/1) in
  let t171 = @runtime(core.string_println)(t170) in
  jump ret169()
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret172() -> unit {
    ()
  } in
  let t173 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret172()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret174(retv175: bool) -> bool {
    retv175
  } in
  let t176 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret174(t176)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret177(retv178: string) -> string {
    retv178
  } in
  let t179 = @runtime(core.int_to_string)(self/40) in
  jump ret177(t179)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret180(retv181: uint64) -> uint64 {
    retv181
  } in
  let t182 = @runtime(core.int_hash)(self/85) in
  jump ret180(t182)
}