fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  join exit424() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_isize(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_isize(0) in
    join exit425() -> unit {
      let t426 = inherent#Ref#Ref[T]#get__T_isize(total/2) in
      let _wild417 = println__T_isize(t426) in
      let k/3 = inherent#Ref#Ref[T]#new__T_isize(0) in
      join inline_cont531(sum/4: Ref[isize]) -> unit {
        join exit427() -> unit {
          join inline_cont529(t428: isize) -> unit {
            let inline527 = trait_impl#ToString#isize#to_string(t428) in
            let inline528 = @runtime(core.string_println)(inline527) in
            ()
          } in
          let inline530 = @intrinsic(ref.get)(sum/4) in
          jump inline_cont529(inline530)
        } in
        joinrec {
          join loop429() -> unit {
            join inline_cont524(mtmp418: isize) -> unit {
              join k430(jp431: bool) -> unit {
                if jp431 {
                  join inline_cont522(t432: isize) -> unit {
                    join inline_cont520(t433: isize) -> unit {
                      let t434 = (t432 + t433) in
                      join inline_cont518(_wild419: unit) -> unit {
                        join inline_cont516(t435: isize) -> unit {
                          let t436 = (t435 + 1) in
                          let inline515 = @intrinsic(ref.set)(k/3, t436) in
                          jump loop429()
                        } in
                        let inline517 = @intrinsic(ref.get)(k/3) in
                        jump inline_cont516(inline517)
                      } in
                      let inline519 = @intrinsic(ref.set)(sum/4, t434) in
                      jump inline_cont518(())
                    } in
                    let inline521 = @intrinsic(ref.get)(k/3) in
                    jump inline_cont520(inline521)
                  } in
                  let inline523 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont522(inline523)
                } else {
                  jump exit427()
                }
              } in
              match mtmp418 {
                0 => {
                  jump k430(true)
                },
                1 => {
                  join inline_cont512(t439: isize) -> unit {
                    let t440 = (t439 == 0) in
                    if t440 {
                      jump k430(true)
                    } else {
                      jump k430(false)
                    }
                  } in
                  let inline513 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont512(inline513)
                },
                2 => {
                  jump k430(true)
                },
                _ => {
                  jump k430(false)
                },
              }
            } in
            let inline525 = @intrinsic(ref.get)(k/3) in
            jump inline_cont524(inline525)
          } in
        } in
        jump loop429()
      } in
      let inline532 = 0 in
      let inline533 = @intrinsic(ref.new)(inline532) in
      jump inline_cont531(inline533)
    } in
    joinrec {
      join loop441() -> unit {
        join inline_cont510(t449: isize) -> unit {
          let t450 = (t449 < 4) in
          join k442(jp443: bool) -> unit {
            if jp443 {
              join inline_cont508(t444: isize) -> unit {
                join inline_cont506(t445: isize) -> unit {
                  let t446 = (t444 + t445) in
                  join inline_cont504(_wild414: unit) -> unit {
                    join inline_cont502(t447: isize) -> unit {
                      let t448 = (t447 + 1) in
                      let inline501 = @intrinsic(ref.set)(j/1, t448) in
                      jump loop441()
                    } in
                    let inline503 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont502(inline503)
                  } in
                  let inline505 = @intrinsic(ref.set)(total/2, t446) in
                  jump inline_cont504(())
                } in
                let inline507 = @intrinsic(ref.get)(j/1) in
                jump inline_cont506(inline507)
              } in
              let inline509 = @intrinsic(ref.get)(total/2) in
              jump inline_cont508(inline509)
            } else {
              jump exit425()
            }
          } in
          if t450 {
            join inline_cont498(t453: isize) -> unit {
              let t454 = (t453 == 1) in
              if t454 {
                jump k442(true)
              } else {
                join inline_cont496(t455: isize) -> unit {
                  let t456 = (t455 != 3) in
                  jump k442(t456)
                } in
                let inline497 = @intrinsic(ref.get)(j/1) in
                jump inline_cont496(inline497)
              }
            } in
            let inline499 = @intrinsic(ref.get)(j/1) in
            jump inline_cont498(inline499)
          } else {
            jump k442(false)
          }
        } in
        let inline511 = @intrinsic(ref.get)(j/1) in
        jump inline_cont510(inline511)
      } in
    } in
    jump loop441()
  } in
  joinrec {
    join loop457() -> unit {
      join inline_cont494(t463: isize) -> unit {
        let t464 = (t463 < 3) in
        join k458(jp459: bool) -> unit {
          if jp459 {
            join inline_cont492(t460: isize) -> unit {
              join inline_cont489(_wild411: unit) -> unit {
                join inline_cont487(t461: isize) -> unit {
                  let t462 = (t461 + 1) in
                  let inline486 = @intrinsic(ref.set)(i/0, t462) in
                  jump loop457()
                } in
                let inline488 = @intrinsic(ref.get)(i/0) in
                jump inline_cont487(inline488)
              } in
              let inline490 = trait_impl#ToString#isize#to_string(t460) in
              let inline491 = @runtime(core.string_println)(inline490) in
              jump inline_cont489(())
            } in
            let inline493 = @intrinsic(ref.get)(i/0) in
            jump inline_cont492(inline493)
          } else {
            jump exit424()
          }
        } in
        if t464 {
          jump k458(true)
        } else {
          jump k458(false)
        }
      } in
      let inline495 = @intrinsic(ref.get)(i/0) in
      jump inline_cont494(inline495)
    } in
  } in
  jump loop457()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/431: isize) -> Ref[isize] {
  let t467 = @intrinsic(ref.new)(value/431) in
  t467
}

fn inherent#Ref#Ref[T]#get__T_isize(self/432: Ref[isize]) -> isize {
  let t470 = @intrinsic(ref.get)(self/432) in
  t470
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont534(t472: string) -> unit {
    let t473 = @runtime(core.string_println)(t472) in
    ()
  } in
  let inline535 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont534(inline535)
}

fn inherent#Ref#Ref[T]#set__T_isize(self/433: Ref[isize], value/434: isize) -> unit {
  let t475 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn trait_impl#ToString#isize#to_string(self/151: isize) -> string {
  let t478 = @runtime(core.int_to_string)(self/151) in
  t478
}

fn trait_impl#PartialEq#isize#eq(self/185: isize, other/186: isize) -> bool {
  let t481 = (self/185 == other/186) in
  t481
}

fn trait_impl#Hash#isize#hash(self/211: isize) -> u64 {
  let t484 = @runtime(core.int_hash)(self/211) in
  t484
}