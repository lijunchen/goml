fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit421() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit422() -> unit {
      let t423 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild414 = println__T_int(t423) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      join inline_cont528(sum/4: Ref[int]) -> unit {
        join exit424() -> unit {
          join inline_cont526(t425: int) -> unit {
            let inline524 = trait_impl#ToString#int#to_string(t425) in
            let inline525 = @runtime(core.string_println)(inline524) in
            ()
          } in
          let inline527 = @intrinsic(ref.get)(sum/4) in
          jump inline_cont526(inline527)
        } in
        joinrec {
          join loop426() -> unit {
            join inline_cont521(mtmp415: int) -> unit {
              join k427(jp428: bool) -> unit {
                if jp428 {
                  join inline_cont519(t429: int) -> unit {
                    join inline_cont517(t430: int) -> unit {
                      let t431 = (t429 + t430) in
                      join inline_cont515(_wild416: unit) -> unit {
                        join inline_cont513(t432: int) -> unit {
                          let t433 = (t432 + 1) in
                          let inline512 = @intrinsic(ref.set)(k/3, t433) in
                          jump loop426()
                        } in
                        let inline514 = @intrinsic(ref.get)(k/3) in
                        jump inline_cont513(inline514)
                      } in
                      let inline516 = @intrinsic(ref.set)(sum/4, t431) in
                      jump inline_cont515(())
                    } in
                    let inline518 = @intrinsic(ref.get)(k/3) in
                    jump inline_cont517(inline518)
                  } in
                  let inline520 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont519(inline520)
                } else {
                  jump exit424()
                }
              } in
              match mtmp415 {
                0 => {
                  jump k427(true)
                },
                1 => {
                  join inline_cont509(t436: int) -> unit {
                    let t437 = (t436 == 0) in
                    if t437 {
                      jump k427(true)
                    } else {
                      jump k427(false)
                    }
                  } in
                  let inline510 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont509(inline510)
                },
                2 => {
                  jump k427(true)
                },
                _ => {
                  jump k427(false)
                },
              }
            } in
            let inline522 = @intrinsic(ref.get)(k/3) in
            jump inline_cont521(inline522)
          } in
        } in
        jump loop426()
      } in
      let inline529 = 0 in
      let inline530 = @intrinsic(ref.new)(inline529) in
      jump inline_cont528(inline530)
    } in
    joinrec {
      join loop438() -> unit {
        join inline_cont507(t446: int) -> unit {
          let t447 = (t446 < 4) in
          join k439(jp440: bool) -> unit {
            if jp440 {
              join inline_cont505(t441: int) -> unit {
                join inline_cont503(t442: int) -> unit {
                  let t443 = (t441 + t442) in
                  join inline_cont501(_wild411: unit) -> unit {
                    join inline_cont499(t444: int) -> unit {
                      let t445 = (t444 + 1) in
                      let inline498 = @intrinsic(ref.set)(j/1, t445) in
                      jump loop438()
                    } in
                    let inline500 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont499(inline500)
                  } in
                  let inline502 = @intrinsic(ref.set)(total/2, t443) in
                  jump inline_cont501(())
                } in
                let inline504 = @intrinsic(ref.get)(j/1) in
                jump inline_cont503(inline504)
              } in
              let inline506 = @intrinsic(ref.get)(total/2) in
              jump inline_cont505(inline506)
            } else {
              jump exit422()
            }
          } in
          if t447 {
            join inline_cont495(t450: int) -> unit {
              let t451 = (t450 == 1) in
              if t451 {
                jump k439(true)
              } else {
                join inline_cont493(t452: int) -> unit {
                  let t453 = (t452 != 3) in
                  jump k439(t453)
                } in
                let inline494 = @intrinsic(ref.get)(j/1) in
                jump inline_cont493(inline494)
              }
            } in
            let inline496 = @intrinsic(ref.get)(j/1) in
            jump inline_cont495(inline496)
          } else {
            jump k439(false)
          }
        } in
        let inline508 = @intrinsic(ref.get)(j/1) in
        jump inline_cont507(inline508)
      } in
    } in
    jump loop438()
  } in
  joinrec {
    join loop454() -> unit {
      join inline_cont491(t460: int) -> unit {
        let t461 = (t460 < 3) in
        join k455(jp456: bool) -> unit {
          if jp456 {
            join inline_cont489(t457: int) -> unit {
              join inline_cont486(_wild408: unit) -> unit {
                join inline_cont484(t458: int) -> unit {
                  let t459 = (t458 + 1) in
                  let inline483 = @intrinsic(ref.set)(i/0, t459) in
                  jump loop454()
                } in
                let inline485 = @intrinsic(ref.get)(i/0) in
                jump inline_cont484(inline485)
              } in
              let inline487 = trait_impl#ToString#int#to_string(t457) in
              let inline488 = @runtime(core.string_println)(inline487) in
              jump inline_cont486(())
            } in
            let inline490 = @intrinsic(ref.get)(i/0) in
            jump inline_cont489(inline490)
          } else {
            jump exit421()
          }
        } in
        if t461 {
          jump k455(true)
        } else {
          jump k455(false)
        }
      } in
      let inline492 = @intrinsic(ref.get)(i/0) in
      jump inline_cont491(inline492)
    } in
  } in
  jump loop454()
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t464 = @intrinsic(ref.new)(value/431) in
  t464
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t467 = @intrinsic(ref.get)(self/432) in
  t467
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont531(t469: string) -> unit {
    let t470 = @runtime(core.string_println)(t469) in
    ()
  } in
  let inline532 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont531(inline532)
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t472 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t475 = @runtime(core.int_to_string)(self/151) in
  t475
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t478 = (self/185 == other/186) in
  t478
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t481 = @runtime(core.int_hash)(self/211) in
  t481
}