fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit421() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit422() -> unit {
      let t423 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild414 = println__T_int(t423) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit424() -> unit {
        let t425 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild419 = println__T_int(t425) in
        ()
      } in
      joinrec {
        join loop426() -> unit {
          join inline_cont531(mtmp415: int) -> unit {
            join k427(jp428: bool) -> unit {
              if jp428 {
                join inline_cont529(t429: int) -> unit {
                  join inline_cont527(t430: int) -> unit {
                    let t431 = (t429 + t430) in
                    join inline_cont525(_wild416: unit) -> unit {
                      join inline_cont523(t432: int) -> unit {
                        let t433 = (t432 + 1) in
                        let inline522 = @intrinsic(ref.set)(k/3, t433) in
                        jump loop426()
                      } in
                      let inline524 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont523(inline524)
                    } in
                    let inline526 = @intrinsic(ref.set)(sum/4, t431) in
                    jump inline_cont525(())
                  } in
                  let inline528 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont527(inline528)
                } in
                let inline530 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont529(inline530)
              } else {
                jump exit424()
              }
            } in
            match mtmp415 {
              0 => {
                jump k427(true)
              },
              1 => {
                join inline_cont519(t436: int) -> unit {
                  join inline_cont516(t437: bool) -> unit {
                    if t437 {
                      jump k427(true)
                    } else {
                      jump k427(false)
                    }
                  } in
                  let inline517 = 0 in
                  let inline518 = (t436 == inline517) in
                  jump inline_cont516(inline518)
                } in
                let inline520 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont519(inline520)
              },
              2 => {
                jump k427(true)
              },
              _ => {
                jump k427(false)
              },
            }
          } in
          let inline532 = @intrinsic(ref.get)(k/3) in
          jump inline_cont531(inline532)
        } in
      } in
      jump loop426()
    } in
    joinrec {
      join loop438() -> unit {
        join inline_cont514(t446: int) -> unit {
          let t447 = (t446 < 4) in
          join k439(jp440: bool) -> unit {
            if jp440 {
              join inline_cont512(t441: int) -> unit {
                join inline_cont510(t442: int) -> unit {
                  let t443 = (t441 + t442) in
                  join inline_cont508(_wild411: unit) -> unit {
                    join inline_cont506(t444: int) -> unit {
                      let t445 = (t444 + 1) in
                      let inline505 = @intrinsic(ref.set)(j/1, t445) in
                      jump loop438()
                    } in
                    let inline507 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont506(inline507)
                  } in
                  let inline509 = @intrinsic(ref.set)(total/2, t443) in
                  jump inline_cont508(())
                } in
                let inline511 = @intrinsic(ref.get)(j/1) in
                jump inline_cont510(inline511)
              } in
              let inline513 = @intrinsic(ref.get)(total/2) in
              jump inline_cont512(inline513)
            } else {
              jump exit422()
            }
          } in
          if t447 {
            join inline_cont502(t450: int) -> unit {
              join inline_cont499(t451: bool) -> unit {
                if t451 {
                  jump k439(true)
                } else {
                  join inline_cont497(t452: int) -> unit {
                    join inline_cont494(t453: bool) -> unit {
                      let t454 = (!t453) in
                      jump k439(t454)
                    } in
                    let inline495 = 3 in
                    let inline496 = (t452 == inline495) in
                    jump inline_cont494(inline496)
                  } in
                  let inline498 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont497(inline498)
                }
              } in
              let inline500 = 1 in
              let inline501 = (t450 == inline500) in
              jump inline_cont499(inline501)
            } in
            let inline503 = @intrinsic(ref.get)(j/1) in
            jump inline_cont502(inline503)
          } else {
            jump k439(false)
          }
        } in
        let inline515 = @intrinsic(ref.get)(j/1) in
        jump inline_cont514(inline515)
      } in
    } in
    jump loop438()
  } in
  joinrec {
    join loop455() -> unit {
      join inline_cont492(t461: int) -> unit {
        let t462 = (t461 < 3) in
        join k456(jp457: bool) -> unit {
          if jp457 {
            join inline_cont490(t458: int) -> unit {
              join inline_cont487(_wild408: unit) -> unit {
                join inline_cont485(t459: int) -> unit {
                  let t460 = (t459 + 1) in
                  let inline484 = @intrinsic(ref.set)(i/0, t460) in
                  jump loop455()
                } in
                let inline486 = @intrinsic(ref.get)(i/0) in
                jump inline_cont485(inline486)
              } in
              let inline488 = trait_impl#ToString#int#to_string(t458) in
              let inline489 = @runtime(core.string_println)(inline488) in
              jump inline_cont487(())
            } in
            let inline491 = @intrinsic(ref.get)(i/0) in
            jump inline_cont490(inline491)
          } else {
            jump exit421()
          }
        } in
        if t462 {
          jump k456(true)
        } else {
          jump k456(false)
        }
      } in
      let inline493 = @intrinsic(ref.get)(i/0) in
      jump inline_cont492(inline493)
    } in
  } in
  jump loop455()
}

fn inherent#Ref#Ref[T]#new__T_int(value/431: int) -> Ref[int] {
  let t465 = @intrinsic(ref.new)(value/431) in
  t465
}

fn inherent#Ref#Ref[T]#get__T_int(self/432: Ref[int]) -> int {
  let t468 = @intrinsic(ref.get)(self/432) in
  t468
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont533(t470: string) -> unit {
    let t471 = @runtime(core.string_println)(t470) in
    ()
  } in
  let inline534 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont533(inline534)
}

fn inherent#Ref#Ref[T]#set__T_int(self/433: Ref[int], value/434: int) -> unit {
  let t473 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/185: int, other/186: int) -> bool {
  let t476 = (self/185 == other/186) in
  t476
}

fn trait_impl#ToString#int#to_string(self/151: int) -> string {
  let t479 = @runtime(core.int_to_string)(self/151) in
  t479
}

fn trait_impl#Hash#int#hash(self/211: int) -> uint64 {
  let t482 = @runtime(core.int_hash)(self/211) in
  t482
}