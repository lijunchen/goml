fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_isize(0) in
  join exit809() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_isize(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_isize(0) in
    join exit810() -> unit {
      let t811 = inherent#Ref#Ref[T]#get__T_isize(total/2) in
      let _wild802 = println__T_isize(t811) in
      let k/3 = inherent#Ref#Ref[T]#new__T_isize(0) in
      join inline_cont959(sum/4: Ref[isize]) -> unit {
        join exit812() -> unit {
          join inline_cont957(t813: isize) -> unit {
            let inline955 = trait_impl#ToString#isize#to_string(t813) in
            let inline956 = @runtime(core.string_println)(inline955) in
            ()
          } in
          let inline958 = @intrinsic(ref.get)(sum/4) in
          jump inline_cont957(inline958)
        } in
        joinrec {
          join loop814() -> unit {
            join inline_cont952(mtmp803: isize) -> unit {
              join k815(jp816: bool) -> unit {
                if jp816 {
                  join inline_cont950(t817: isize) -> unit {
                    join inline_cont948(t818: isize) -> unit {
                      let t819 = (t817 + t818) in
                      join inline_cont946(_wild804: unit) -> unit {
                        join inline_cont944(t820: isize) -> unit {
                          let t821 = (t820 + 1) in
                          let inline943 = @intrinsic(ref.set)(k/3, t821) in
                          jump loop814()
                        } in
                        let inline945 = @intrinsic(ref.get)(k/3) in
                        jump inline_cont944(inline945)
                      } in
                      let inline947 = @intrinsic(ref.set)(sum/4, t819) in
                      jump inline_cont946(())
                    } in
                    let inline949 = @intrinsic(ref.get)(k/3) in
                    jump inline_cont948(inline949)
                  } in
                  let inline951 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont950(inline951)
                } else {
                  jump exit812()
                }
              } in
              match mtmp803 {
                0 => {
                  jump k815(true)
                },
                1 => {
                  join inline_cont940(t824: isize) -> unit {
                    let t825 = (t824 == 0) in
                    if t825 {
                      jump k815(true)
                    } else {
                      jump k815(false)
                    }
                  } in
                  let inline941 = @intrinsic(ref.get)(sum/4) in
                  jump inline_cont940(inline941)
                },
                2 => {
                  jump k815(true)
                },
                _ => {
                  jump k815(false)
                },
              }
            } in
            let inline953 = @intrinsic(ref.get)(k/3) in
            jump inline_cont952(inline953)
          } in
        } in
        jump loop814()
      } in
      let inline960 = 0 in
      let inline961 = @intrinsic(ref.new)(inline960) in
      jump inline_cont959(inline961)
    } in
    joinrec {
      join loop826() -> unit {
        join inline_cont938(t834: isize) -> unit {
          let t835 = (t834 < 4) in
          join k827(jp828: bool) -> unit {
            if jp828 {
              join inline_cont936(t829: isize) -> unit {
                join inline_cont934(t830: isize) -> unit {
                  let t831 = (t829 + t830) in
                  join inline_cont932(_wild799: unit) -> unit {
                    join inline_cont930(t832: isize) -> unit {
                      let t833 = (t832 + 1) in
                      let inline929 = @intrinsic(ref.set)(j/1, t833) in
                      jump loop826()
                    } in
                    let inline931 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont930(inline931)
                  } in
                  let inline933 = @intrinsic(ref.set)(total/2, t831) in
                  jump inline_cont932(())
                } in
                let inline935 = @intrinsic(ref.get)(j/1) in
                jump inline_cont934(inline935)
              } in
              let inline937 = @intrinsic(ref.get)(total/2) in
              jump inline_cont936(inline937)
            } else {
              jump exit810()
            }
          } in
          if t835 {
            join inline_cont926(t838: isize) -> unit {
              let t839 = (t838 == 1) in
              if t839 {
                jump k827(true)
              } else {
                join inline_cont924(t840: isize) -> unit {
                  let t841 = (t840 != 3) in
                  jump k827(t841)
                } in
                let inline925 = @intrinsic(ref.get)(j/1) in
                jump inline_cont924(inline925)
              }
            } in
            let inline927 = @intrinsic(ref.get)(j/1) in
            jump inline_cont926(inline927)
          } else {
            jump k827(false)
          }
        } in
        let inline939 = @intrinsic(ref.get)(j/1) in
        jump inline_cont938(inline939)
      } in
    } in
    jump loop826()
  } in
  joinrec {
    join loop842() -> unit {
      join inline_cont922(t848: isize) -> unit {
        let t849 = (t848 < 3) in
        join k843(jp844: bool) -> unit {
          if jp844 {
            join inline_cont920(t845: isize) -> unit {
              join inline_cont917(_wild796: unit) -> unit {
                join inline_cont915(t846: isize) -> unit {
                  let t847 = (t846 + 1) in
                  let inline914 = @intrinsic(ref.set)(i/0, t847) in
                  jump loop842()
                } in
                let inline916 = @intrinsic(ref.get)(i/0) in
                jump inline_cont915(inline916)
              } in
              let inline918 = trait_impl#ToString#isize#to_string(t845) in
              let inline919 = @runtime(core.string_println)(inline918) in
              jump inline_cont917(())
            } in
            let inline921 = @intrinsic(ref.get)(i/0) in
            jump inline_cont920(inline921)
          } else {
            jump exit809()
          }
        } in
        if t849 {
          jump k843(true)
        } else {
          jump k843(false)
        }
      } in
      let inline923 = @intrinsic(ref.get)(i/0) in
      jump inline_cont922(inline923)
    } in
  } in
  jump loop842()
}

fn inherent#Ref#Ref[T]#new__T_isize(value/684: isize) -> Ref[isize] {
  let t852 = @intrinsic(ref.new)(value/684) in
  t852
}

fn inherent#Ref#Ref[T]#get__T_isize(self/685: Ref[isize]) -> isize {
  let t855 = @intrinsic(ref.get)(self/685) in
  t855
}

fn println__T_isize(value/1: isize) -> unit {
  join inline_cont962(t857: string) -> unit {
    let t858 = @runtime(core.string_println)(t857) in
    ()
  } in
  let inline963 = __goml_builtin_int_to_string(value/1) in
  jump inline_cont962(inline963)
}

fn inherent#Ref#Ref[T]#set__T_isize(self/686: Ref[isize], value/687: isize) -> unit {
  let t860 = @intrinsic(ref.set)(self/686, value/687) in
  ()
}

fn trait_impl#ToString#isize#to_string(self/404: isize) -> string {
  let inline965 = (self/404 as i64) in
  let inline966 = signed_decimal_string(inline965) in
  inline966
}

fn __goml_builtin_int_to_string(value/222: isize) -> string {
  let t866 = (value/222 as i64) in
  let inline968 = (t866 < 0) in
  if inline968 {
    let inline969 = (t866 as u64) in
    let inline970 = (0 - inline969) in
    let inline971 = decimal_string(inline970) in
    let inline972 = ("-" + inline971) in
    inline972
  } else {
    let inline973 = (t866 as u64) in
    let inline974 = decimal_string(inline973) in
    inline974
  }
}

fn signed_decimal_string(value/214: i64) -> string {
  let t872 = (value/214 < 0) in
  if t872 {
    let t873 = (value/214 as u64) in
    let t874 = (0 - t873) in
    let t875 = decimal_string(t874) in
    let t876 = ("-" + t875) in
    t876
  } else {
    let t877 = (value/214 as u64) in
    let t878 = decimal_string(t877) in
    t878
  }
}

fn decimal_string(value/208: u64) -> string {
  let t901 = (value/208 == 0) in
  join k881() -> string {
    let reversed/209 = @intrinsic(vec.with_capacity)(20) in
    let remaining/210 = value/208 in
    join exit882() -> string {
      join inline_cont983(t883: isize) -> string {
        let bytes/211 = @intrinsic(vec.with_capacity)(t883) in
        let offset/212 = 0 in
        join exit884() -> string {
          let mtmp362 = @runtime(core.string_from_utf8)(bytes/211) in
          let x364 = mtmp362.1 in
          x364
        } in
        joinrec {
          join loop885() -> string {
            join inline_cont981(t886: isize) -> string {
              let t887 = (offset/212 < t886) in
              if t887 {
                join inline_cont979(t888: isize) -> string {
                  let t889 = (t888 - offset/212) in
                  let t890 = (t889 - 1) in
                  let t891 = @intrinsic(vec.get)(reversed/209, t890) in
                  join inline_cont977(_wild357: unit) -> string {
                    let compound_old358 = offset/212 in
                    let compound_value359 = 1 in
                    let t892 = (compound_old358 + compound_value359) in
                    let t893 = offset/212 = t892 in
                    jump loop885()
                  } in
                  let inline978 = @intrinsic(vec.push)(bytes/211, t891) in
                  jump inline_cont977(())
                } in
                let inline980 = @intrinsic(vec.len)(reversed/209) in
                jump inline_cont979(inline980)
              } else {
                jump exit884()
              }
            } in
            let inline982 = @intrinsic(vec.len)(reversed/209) in
            jump inline_cont981(inline982)
          } in
        } in
        jump loop885()
      } in
      let inline984 = @intrinsic(vec.len)(reversed/209) in
      jump inline_cont983(inline984)
    } in
    joinrec {
      join loop894() -> string {
        let t895 = (remaining/210 > 0) in
        if t895 {
          let t896 = (remaining/210 % 10) in
          let t897 = (t896 as u8) in
          let t898 = (t897 + 48) in
          join inline_cont975(_wild352: unit) -> string {
            let compound_old353 = remaining/210 in
            let compound_value354 = 10 in
            let t899 = (compound_old353 / compound_value354) in
            let t900 = remaining/210 = t899 in
            jump loop894()
          } in
          let inline976 = @intrinsic(vec.push)(reversed/209, t898) in
          jump inline_cont975(())
        } else {
          jump exit882()
        }
      } in
    } in
    jump loop894()
  } in
  if t901 {
    "0"
  } else {
    jump k881()
  }
}

fn inherent#Vec#Vec[T]#push__T_u8(self/511: Vec[u8], elem/512: u8) -> unit {
  let t903 = @intrinsic(vec.push)(self/511, elem/512) in
  ()
}

fn inherent#Vec#Vec[T]#len__T_u8(self/526: Vec[u8]) -> isize {
  let t906 = @intrinsic(vec.len)(self/526) in
  t906
}

fn trait_impl#PartialEq#isize#eq(self/438: isize, other/439: isize) -> bool {
  let t909 = (self/438 == other/439) in
  t909
}

fn trait_impl#Hash#isize#hash(self/464: isize) -> u64 {
  let t912 = @runtime(core.int_hash)(self/464) in
  t912
}