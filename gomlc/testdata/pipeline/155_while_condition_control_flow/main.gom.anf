fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit168() -> unit {
    let _wild157 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit169() -> unit {
      let _wild160 = () in
      let t170 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild161 = println__T_int(t170) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit171() -> unit {
        let _wild165 = () in
        let t172 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild166 = println__T_int(t172) in
        ()
      } in
      joinrec {
        join loop173() -> unit {
          join inline_cont278(mtmp162: int) -> unit {
            join k174(jp175: bool) -> unit {
              if jp175 {
                join inline_cont276(t176: int) -> unit {
                  join inline_cont274(t177: int) -> unit {
                    let t178 = (t176 + t177) in
                    join inline_cont272(_wild163: unit) -> unit {
                      join inline_cont270(t179: int) -> unit {
                        let t180 = (t179 + 1) in
                        let inline269 = @intrinsic(ref.set)(k/3, t180) in
                        jump loop173()
                      } in
                      let inline271 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont270(inline271)
                    } in
                    let inline273 = @intrinsic(ref.set)(sum/4, t178) in
                    jump inline_cont272(())
                  } in
                  let inline275 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont274(inline275)
                } in
                let inline277 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont276(inline277)
              } else {
                jump exit171()
              }
            } in
            match mtmp162 {
              0 => {
                jump k174(true)
              },
              1 => {
                join inline_cont266(t183: int) -> unit {
                  join inline_cont263(t184: bool) -> unit {
                    if t184 {
                      jump k174(true)
                    } else {
                      jump k174(false)
                    }
                  } in
                  let inline264 = 0 in
                  let inline265 = @intrinsic(eq.primitive)(t183, inline264) in
                  jump inline_cont263(inline265)
                } in
                let inline267 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont266(inline267)
              },
              2 => {
                jump k174(true)
              },
              _ => {
                jump k174(false)
              },
            }
          } in
          let inline279 = @intrinsic(ref.get)(k/3) in
          jump inline_cont278(inline279)
        } in
      } in
      jump loop173()
    } in
    joinrec {
      join loop185() -> unit {
        join inline_cont261(t193: int) -> unit {
          let t194 = (t193 < 4) in
          join k186(jp187: bool) -> unit {
            if jp187 {
              join inline_cont259(t188: int) -> unit {
                join inline_cont257(t189: int) -> unit {
                  let t190 = (t188 + t189) in
                  join inline_cont255(_wild158: unit) -> unit {
                    join inline_cont253(t191: int) -> unit {
                      let t192 = (t191 + 1) in
                      let inline252 = @intrinsic(ref.set)(j/1, t192) in
                      jump loop185()
                    } in
                    let inline254 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont253(inline254)
                  } in
                  let inline256 = @intrinsic(ref.set)(total/2, t190) in
                  jump inline_cont255(())
                } in
                let inline258 = @intrinsic(ref.get)(j/1) in
                jump inline_cont257(inline258)
              } in
              let inline260 = @intrinsic(ref.get)(total/2) in
              jump inline_cont259(inline260)
            } else {
              jump exit169()
            }
          } in
          if t194 {
            join inline_cont249(t197: int) -> unit {
              join inline_cont246(t198: bool) -> unit {
                if t198 {
                  jump k186(true)
                } else {
                  join inline_cont244(t199: int) -> unit {
                    join inline_cont241(t200: bool) -> unit {
                      let t201 = (!t200) in
                      jump k186(t201)
                    } in
                    let inline242 = 3 in
                    let inline243 = @intrinsic(eq.primitive)(t199, inline242) in
                    jump inline_cont241(inline243)
                  } in
                  let inline245 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont244(inline245)
                }
              } in
              let inline247 = 1 in
              let inline248 = @intrinsic(eq.primitive)(t197, inline247) in
              jump inline_cont246(inline248)
            } in
            let inline250 = @intrinsic(ref.get)(j/1) in
            jump inline_cont249(inline250)
          } else {
            jump k186(false)
          }
        } in
        let inline262 = @intrinsic(ref.get)(j/1) in
        jump inline_cont261(inline262)
      } in
    } in
    jump loop185()
  } in
  joinrec {
    join loop202() -> unit {
      join inline_cont239(t208: int) -> unit {
        let t209 = (t208 < 3) in
        join k203(jp204: bool) -> unit {
          if jp204 {
            join inline_cont237(t205: int) -> unit {
              join inline_cont234(_wild155: unit) -> unit {
                join inline_cont232(t206: int) -> unit {
                  let t207 = (t206 + 1) in
                  let inline231 = @intrinsic(ref.set)(i/0, t207) in
                  jump loop202()
                } in
                let inline233 = @intrinsic(ref.get)(i/0) in
                jump inline_cont232(inline233)
              } in
              let inline235 = trait_impl#ToString#int#to_string(t205) in
              let inline236 = @runtime(core.string_println)(inline235) in
              jump inline_cont234(())
            } in
            let inline238 = @intrinsic(ref.get)(i/0) in
            jump inline_cont237(inline238)
          } else {
            jump exit168()
          }
        } in
        if t209 {
          jump k203(true)
        } else {
          jump k203(false)
        }
      } in
      let inline240 = @intrinsic(ref.get)(i/0) in
      jump inline_cont239(inline240)
    } in
  } in
  jump loop202()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  let t212 = @intrinsic(ref.new)(value/207) in
  t212
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  let t215 = @intrinsic(ref.get)(self/208) in
  t215
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont280(t217: string) -> unit {
    let t218 = @runtime(core.string_println)(t217) in
    ()
  } in
  let inline281 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont280(inline281)
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  let t220 = @intrinsic(ref.set)(self/209, value/210) in
  ()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  let t223 = @intrinsic(eq.primitive)(self/59, other/60) in
  t223
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  let t226 = @runtime(core.int_to_string)(self/40) in
  t226
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  let t229 = @runtime(core.int_hash)(self/85) in
  t229
}