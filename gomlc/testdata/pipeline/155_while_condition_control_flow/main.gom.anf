fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit200() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit201() -> unit {
      let t202 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild193 = println__T_int(t202) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit203() -> unit {
        let t204 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild198 = println__T_int(t204) in
        ()
      } in
      joinrec {
        join loop205() -> unit {
          join inline_cont310(mtmp194: int) -> unit {
            join k206(jp207: bool) -> unit {
              if jp207 {
                join inline_cont308(t208: int) -> unit {
                  join inline_cont306(t209: int) -> unit {
                    let t210 = (t208 + t209) in
                    join inline_cont304(_wild195: unit) -> unit {
                      join inline_cont302(t211: int) -> unit {
                        let t212 = (t211 + 1) in
                        let inline301 = @intrinsic(ref.set)(k/3, t212) in
                        jump loop205()
                      } in
                      let inline303 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont302(inline303)
                    } in
                    let inline305 = @intrinsic(ref.set)(sum/4, t210) in
                    jump inline_cont304(())
                  } in
                  let inline307 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont306(inline307)
                } in
                let inline309 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont308(inline309)
              } else {
                jump exit203()
              }
            } in
            match mtmp194 {
              0 => {
                jump k206(true)
              },
              1 => {
                join inline_cont298(t215: int) -> unit {
                  join inline_cont295(t216: bool) -> unit {
                    if t216 {
                      jump k206(true)
                    } else {
                      jump k206(false)
                    }
                  } in
                  let inline296 = 0 in
                  let inline297 = (t215 == inline296) in
                  jump inline_cont295(inline297)
                } in
                let inline299 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont298(inline299)
              },
              2 => {
                jump k206(true)
              },
              _ => {
                jump k206(false)
              },
            }
          } in
          let inline311 = @intrinsic(ref.get)(k/3) in
          jump inline_cont310(inline311)
        } in
      } in
      jump loop205()
    } in
    joinrec {
      join loop217() -> unit {
        join inline_cont293(t225: int) -> unit {
          let t226 = (t225 < 4) in
          join k218(jp219: bool) -> unit {
            if jp219 {
              join inline_cont291(t220: int) -> unit {
                join inline_cont289(t221: int) -> unit {
                  let t222 = (t220 + t221) in
                  join inline_cont287(_wild190: unit) -> unit {
                    join inline_cont285(t223: int) -> unit {
                      let t224 = (t223 + 1) in
                      let inline284 = @intrinsic(ref.set)(j/1, t224) in
                      jump loop217()
                    } in
                    let inline286 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont285(inline286)
                  } in
                  let inline288 = @intrinsic(ref.set)(total/2, t222) in
                  jump inline_cont287(())
                } in
                let inline290 = @intrinsic(ref.get)(j/1) in
                jump inline_cont289(inline290)
              } in
              let inline292 = @intrinsic(ref.get)(total/2) in
              jump inline_cont291(inline292)
            } else {
              jump exit201()
            }
          } in
          if t226 {
            join inline_cont281(t229: int) -> unit {
              join inline_cont278(t230: bool) -> unit {
                if t230 {
                  jump k218(true)
                } else {
                  join inline_cont276(t231: int) -> unit {
                    join inline_cont273(t232: bool) -> unit {
                      let t233 = (!t232) in
                      jump k218(t233)
                    } in
                    let inline274 = 3 in
                    let inline275 = (t231 == inline274) in
                    jump inline_cont273(inline275)
                  } in
                  let inline277 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont276(inline277)
                }
              } in
              let inline279 = 1 in
              let inline280 = (t229 == inline279) in
              jump inline_cont278(inline280)
            } in
            let inline282 = @intrinsic(ref.get)(j/1) in
            jump inline_cont281(inline282)
          } else {
            jump k218(false)
          }
        } in
        let inline294 = @intrinsic(ref.get)(j/1) in
        jump inline_cont293(inline294)
      } in
    } in
    jump loop217()
  } in
  joinrec {
    join loop234() -> unit {
      join inline_cont271(t240: int) -> unit {
        let t241 = (t240 < 3) in
        join k235(jp236: bool) -> unit {
          if jp236 {
            join inline_cont269(t237: int) -> unit {
              join inline_cont266(_wild187: unit) -> unit {
                join inline_cont264(t238: int) -> unit {
                  let t239 = (t238 + 1) in
                  let inline263 = @intrinsic(ref.set)(i/0, t239) in
                  jump loop234()
                } in
                let inline265 = @intrinsic(ref.get)(i/0) in
                jump inline_cont264(inline265)
              } in
              let inline267 = trait_impl#ToString#int#to_string(t237) in
              let inline268 = @runtime(core.string_println)(inline267) in
              jump inline_cont266(())
            } in
            let inline270 = @intrinsic(ref.get)(i/0) in
            jump inline_cont269(inline270)
          } else {
            jump exit200()
          }
        } in
        if t241 {
          jump k235(true)
        } else {
          jump k235(false)
        }
      } in
      let inline272 = @intrinsic(ref.get)(i/0) in
      jump inline_cont271(inline272)
    } in
  } in
  jump loop234()
}

fn inherent#Ref#Ref[T]#new__T_int(value/273: int) -> Ref[int] {
  let t244 = @intrinsic(ref.new)(value/273) in
  t244
}

fn inherent#Ref#Ref[T]#get__T_int(self/274: Ref[int]) -> int {
  let t247 = @intrinsic(ref.get)(self/274) in
  t247
}

fn println__T_int(value/1: int) -> unit {
  join inline_cont312(t249: string) -> unit {
    let t250 = @runtime(core.string_println)(t249) in
    ()
  } in
  let inline313 = @runtime(core.int_to_string)(value/1) in
  jump inline_cont312(inline313)
}

fn inherent#Ref#Ref[T]#set__T_int(self/275: Ref[int], value/276: int) -> unit {
  let t252 = @intrinsic(ref.set)(self/275, value/276) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/101: int, other/102: int) -> bool {
  let t255 = (self/101 == other/102) in
  t255
}

fn trait_impl#ToString#int#to_string(self/67: int) -> string {
  let t258 = @runtime(core.int_to_string)(self/67) in
  t258
}

fn trait_impl#Hash#int#hash(self/127: int) -> uint64 {
  let t261 = @runtime(core.int_hash)(self/127) in
  t261
}