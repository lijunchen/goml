fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit149() -> unit {
    let _wild138 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit150() -> unit {
      let _wild141 = () in
      let t151 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild142 = println__T_int(t151) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit152() -> unit {
        let _wild146 = () in
        let t153 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild147 = println__T_int(t153) in
        ()
      } in
      joinrec {
        join loop154() -> unit {
          join inline_cont259(mtmp143: int) -> unit {
            join k155(jp156: bool) -> unit {
              if jp156 {
                join inline_cont257(t157: int) -> unit {
                  join inline_cont255(t158: int) -> unit {
                    let t159 = (t157 + t158) in
                    join inline_cont253(_wild144: unit) -> unit {
                      join inline_cont251(t160: int) -> unit {
                        let t161 = (t160 + 1) in
                        let inline250 = @intrinsic(ref.set)(k/3, t161) in
                        jump loop154()
                      } in
                      let inline252 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont251(inline252)
                    } in
                    let inline254 = @intrinsic(ref.set)(sum/4, t159) in
                    jump inline_cont253(())
                  } in
                  let inline256 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont255(inline256)
                } in
                let inline258 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont257(inline258)
              } else {
                jump exit152()
              }
            } in
            match mtmp143 {
              0 => {
                jump k155(true)
              },
              1 => {
                join inline_cont247(t164: int) -> unit {
                  join inline_cont244(t165: bool) -> unit {
                    if t165 {
                      jump k155(true)
                    } else {
                      jump k155(false)
                    }
                  } in
                  let inline245 = 0 in
                  let inline246 = @intrinsic(eq.primitive)(t164, inline245) in
                  jump inline_cont244(inline246)
                } in
                let inline248 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont247(inline248)
              },
              2 => {
                jump k155(true)
              },
              _ => {
                jump k155(false)
              },
            }
          } in
          let inline260 = @intrinsic(ref.get)(k/3) in
          jump inline_cont259(inline260)
        } in
      } in
      jump loop154()
    } in
    joinrec {
      join loop166() -> unit {
        join inline_cont242(t174: int) -> unit {
          let t175 = (t174 < 4) in
          join k167(jp168: bool) -> unit {
            if jp168 {
              join inline_cont240(t169: int) -> unit {
                join inline_cont238(t170: int) -> unit {
                  let t171 = (t169 + t170) in
                  join inline_cont236(_wild139: unit) -> unit {
                    join inline_cont234(t172: int) -> unit {
                      let t173 = (t172 + 1) in
                      let inline233 = @intrinsic(ref.set)(j/1, t173) in
                      jump loop166()
                    } in
                    let inline235 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont234(inline235)
                  } in
                  let inline237 = @intrinsic(ref.set)(total/2, t171) in
                  jump inline_cont236(())
                } in
                let inline239 = @intrinsic(ref.get)(j/1) in
                jump inline_cont238(inline239)
              } in
              let inline241 = @intrinsic(ref.get)(total/2) in
              jump inline_cont240(inline241)
            } else {
              jump exit150()
            }
          } in
          if t175 {
            join inline_cont230(t178: int) -> unit {
              join inline_cont227(t179: bool) -> unit {
                if t179 {
                  jump k167(true)
                } else {
                  join inline_cont225(t180: int) -> unit {
                    join inline_cont222(t181: bool) -> unit {
                      let t182 = (!t181) in
                      jump k167(t182)
                    } in
                    let inline223 = 3 in
                    let inline224 = @intrinsic(eq.primitive)(t180, inline223) in
                    jump inline_cont222(inline224)
                  } in
                  let inline226 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont225(inline226)
                }
              } in
              let inline228 = 1 in
              let inline229 = @intrinsic(eq.primitive)(t178, inline228) in
              jump inline_cont227(inline229)
            } in
            let inline231 = @intrinsic(ref.get)(j/1) in
            jump inline_cont230(inline231)
          } else {
            jump k167(false)
          }
        } in
        let inline243 = @intrinsic(ref.get)(j/1) in
        jump inline_cont242(inline243)
      } in
    } in
    jump loop166()
  } in
  joinrec {
    join loop183() -> unit {
      join inline_cont220(t189: int) -> unit {
        let t190 = (t189 < 3) in
        join k184(jp185: bool) -> unit {
          if jp185 {
            join inline_cont218(t186: int) -> unit {
              join inline_cont215(_wild136: unit) -> unit {
                join inline_cont213(t187: int) -> unit {
                  let t188 = (t187 + 1) in
                  let inline212 = @intrinsic(ref.set)(i/0, t188) in
                  jump loop183()
                } in
                let inline214 = @intrinsic(ref.get)(i/0) in
                jump inline_cont213(inline214)
              } in
              let inline216 = trait_impl#ToString#int#to_string(t186) in
              let inline217 = @runtime(core.string_println)(inline216) in
              jump inline_cont215(())
            } in
            let inline219 = @intrinsic(ref.get)(i/0) in
            jump inline_cont218(inline219)
          } else {
            jump exit149()
          }
        } in
        if t190 {
          jump k184(true)
        } else {
          jump k184(false)
        }
      } in
      let inline221 = @intrinsic(ref.get)(i/0) in
      jump inline_cont220(inline221)
    } in
  } in
  jump loop183()
}

fn inherent#Ref#Ref[T]#new__T_int(value/215: int) -> Ref[int] {
  let t193 = @intrinsic(ref.new)(value/215) in
  t193
}

fn inherent#Ref#Ref[T]#get__T_int(self/216: Ref[int]) -> int {
  let t196 = @intrinsic(ref.get)(self/216) in
  t196
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont261(t198: string) -> unit {
    let t199 = @runtime(core.string_println)(t198) in
    ()
  } in
  let inline262 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont261(inline262)
}

fn inherent#Ref#Ref[T]#set__T_int(self/217: Ref[int], value/218: int) -> unit {
  let t201 = @intrinsic(ref.set)(self/217, value/218) in
  ()
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t204 = @intrinsic(eq.primitive)(self/88, other/89) in
  t204
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t207 = @runtime(core.int_to_string)(self/69) in
  t207
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t210 = @runtime(core.int_hash)(self/114) in
  t210
}