fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit185() -> unit {
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit186() -> unit {
      let t187 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild178 = println__T_int(t187) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit188() -> unit {
        let t189 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild183 = println__T_int(t189) in
        ()
      } in
      joinrec {
        join loop190() -> unit {
          join inline_cont295(mtmp179: int) -> unit {
            join k191(jp192: bool) -> unit {
              if jp192 {
                join inline_cont293(t193: int) -> unit {
                  join inline_cont291(t194: int) -> unit {
                    let t195 = (t193 + t194) in
                    join inline_cont289(_wild180: unit) -> unit {
                      join inline_cont287(t196: int) -> unit {
                        let t197 = (t196 + 1) in
                        let inline286 = @intrinsic(ref.set)(k/3, t197) in
                        jump loop190()
                      } in
                      let inline288 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont287(inline288)
                    } in
                    let inline290 = @intrinsic(ref.set)(sum/4, t195) in
                    jump inline_cont289(())
                  } in
                  let inline292 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont291(inline292)
                } in
                let inline294 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont293(inline294)
              } else {
                jump exit188()
              }
            } in
            match mtmp179 {
              0 => {
                jump k191(true)
              },
              1 => {
                join inline_cont283(t200: int) -> unit {
                  join inline_cont280(t201: bool) -> unit {
                    if t201 {
                      jump k191(true)
                    } else {
                      jump k191(false)
                    }
                  } in
                  let inline281 = 0 in
                  let inline282 = (t200 == inline281) in
                  jump inline_cont280(inline282)
                } in
                let inline284 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont283(inline284)
              },
              2 => {
                jump k191(true)
              },
              _ => {
                jump k191(false)
              },
            }
          } in
          let inline296 = @intrinsic(ref.get)(k/3) in
          jump inline_cont295(inline296)
        } in
      } in
      jump loop190()
    } in
    joinrec {
      join loop202() -> unit {
        join inline_cont278(t210: int) -> unit {
          let t211 = (t210 < 4) in
          join k203(jp204: bool) -> unit {
            if jp204 {
              join inline_cont276(t205: int) -> unit {
                join inline_cont274(t206: int) -> unit {
                  let t207 = (t205 + t206) in
                  join inline_cont272(_wild175: unit) -> unit {
                    join inline_cont270(t208: int) -> unit {
                      let t209 = (t208 + 1) in
                      let inline269 = @intrinsic(ref.set)(j/1, t209) in
                      jump loop202()
                    } in
                    let inline271 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont270(inline271)
                  } in
                  let inline273 = @intrinsic(ref.set)(total/2, t207) in
                  jump inline_cont272(())
                } in
                let inline275 = @intrinsic(ref.get)(j/1) in
                jump inline_cont274(inline275)
              } in
              let inline277 = @intrinsic(ref.get)(total/2) in
              jump inline_cont276(inline277)
            } else {
              jump exit186()
            }
          } in
          if t211 {
            join inline_cont266(t214: int) -> unit {
              join inline_cont263(t215: bool) -> unit {
                if t215 {
                  jump k203(true)
                } else {
                  join inline_cont261(t216: int) -> unit {
                    join inline_cont258(t217: bool) -> unit {
                      let t218 = (!t217) in
                      jump k203(t218)
                    } in
                    let inline259 = 3 in
                    let inline260 = (t216 == inline259) in
                    jump inline_cont258(inline260)
                  } in
                  let inline262 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont261(inline262)
                }
              } in
              let inline264 = 1 in
              let inline265 = (t214 == inline264) in
              jump inline_cont263(inline265)
            } in
            let inline267 = @intrinsic(ref.get)(j/1) in
            jump inline_cont266(inline267)
          } else {
            jump k203(false)
          }
        } in
        let inline279 = @intrinsic(ref.get)(j/1) in
        jump inline_cont278(inline279)
      } in
    } in
    jump loop202()
  } in
  joinrec {
    join loop219() -> unit {
      join inline_cont256(t225: int) -> unit {
        let t226 = (t225 < 3) in
        join k220(jp221: bool) -> unit {
          if jp221 {
            join inline_cont254(t222: int) -> unit {
              join inline_cont251(_wild172: unit) -> unit {
                join inline_cont249(t223: int) -> unit {
                  let t224 = (t223 + 1) in
                  let inline248 = @intrinsic(ref.set)(i/0, t224) in
                  jump loop219()
                } in
                let inline250 = @intrinsic(ref.get)(i/0) in
                jump inline_cont249(inline250)
              } in
              let inline252 = trait_impl#ToString#int#to_string(t222) in
              let inline253 = @runtime(core.string_println)(inline252) in
              jump inline_cont251(())
            } in
            let inline255 = @intrinsic(ref.get)(i/0) in
            jump inline_cont254(inline255)
          } else {
            jump exit185()
          }
        } in
        if t226 {
          jump k220(true)
        } else {
          jump k220(false)
        }
      } in
      let inline257 = @intrinsic(ref.get)(i/0) in
      jump inline_cont256(inline257)
    } in
  } in
  jump loop219()
}

fn inherent#Ref#Ref[T]#new__T_int(value/257: int) -> Ref[int] {
  let t229 = @intrinsic(ref.new)(value/257) in
  t229
}

fn inherent#Ref#Ref[T]#get__T_int(self/258: Ref[int]) -> int {
  let t232 = @intrinsic(ref.get)(self/258) in
  t232
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont297(t234: string) -> unit {
    let t235 = @runtime(core.string_println)(t234) in
    ()
  } in
  let inline298 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont297(inline298)
}

fn inherent#Ref#Ref[T]#set__T_int(self/259: Ref[int], value/260: int) -> unit {
  let t237 = @intrinsic(ref.set)(self/259, value/260) in
  ()
}

fn trait_impl#PartialEq#int#eq(self/103: int, other/104: int) -> bool {
  let t240 = (self/103 == other/104) in
  t240
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t243 = @runtime(core.int_to_string)(self/69) in
  t243
}

fn trait_impl#Hash#int#hash(self/129: int) -> uint64 {
  let t246 = @runtime(core.int_hash)(self/129) in
  t246
}