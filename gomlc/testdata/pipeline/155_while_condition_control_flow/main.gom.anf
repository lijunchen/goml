fn main() -> unit {
  join ret164() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit165() -> unit {
    let _wild154 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit166() -> unit {
      let _wild157 = () in
      let t167 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild158 = println__T_int(t167) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit168() -> unit {
        let _wild162 = () in
        let t169 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild163 = println__T_int(t169) in
        jump ret164()
      } in
      joinrec {
        join loop170() -> unit {
          let mtmp159 = inherent#Ref#Ref[T]#get__T_int(k/3) in
          join k171(jp172: bool) -> unit {
            if jp172 {
              let t173 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t174 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t175 = (t173 + t174) in
              let _wild160 = inherent#Ref#Ref[T]#set__T_int(sum/4, t175) in
              let t176 = inherent#Ref#Ref[T]#get__T_int(k/3) in
              let t177 = (t176 + 1) in
              let _wild161 = inherent#Ref#Ref[T]#set__T_int(k/3, t177) in
              jump loop170()
            } else {
              jump exit168()
            }
          } in
          match mtmp159 {
            0 => {
              jump k171(true)
            },
            1 => {
              let t180 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
              let t181 = trait_impl#Eq#int#eq(t180, 0) in
              join k178(jp179: bool) -> unit {
                jump k171(jp179)
              } in
              if t181 {
                jump k178(true)
              } else {
                jump k178(false)
              }
            },
            2 => {
              jump k171(true)
            },
            _ => {
              jump k171(false)
            },
          }
        } in
      } in
      jump loop170()
    } in
    joinrec {
      join loop182() -> unit {
        let t190 = inherent#Ref#Ref[T]#get__T_int(j/1) in
        let t191 = (t190 < 4) in
        join k183(jp184: bool) -> unit {
          if jp184 {
            let t185 = inherent#Ref#Ref[T]#get__T_int(total/2) in
            let t186 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t187 = (t185 + t186) in
            let _wild155 = inherent#Ref#Ref[T]#set__T_int(total/2, t187) in
            let t188 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t189 = (t188 + 1) in
            let _wild156 = inherent#Ref#Ref[T]#set__T_int(j/1, t189) in
            jump loop182()
          } else {
            jump exit166()
          }
        } in
        if t191 {
          let t194 = inherent#Ref#Ref[T]#get__T_int(j/1) in
          let t195 = trait_impl#Eq#int#eq(t194, 1) in
          join k192(jp193: bool) -> unit {
            jump k183(jp193)
          } in
          if t195 {
            jump k192(true)
          } else {
            let t196 = inherent#Ref#Ref[T]#get__T_int(j/1) in
            let t197 = trait_impl#Eq#int#eq(t196, 3) in
            let t198 = (!t197) in
            jump k192(t198)
          }
        } else {
          jump k183(false)
        }
      } in
    } in
    jump loop182()
  } in
  joinrec {
    join loop199() -> unit {
      let t205 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t206 = (t205 < 3) in
      join k200(jp201: bool) -> unit {
        if jp201 {
          let t202 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let _wild152 = println__T_int(t202) in
          let t203 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t204 = (t203 + 1) in
          let _wild153 = inherent#Ref#Ref[T]#set__T_int(i/0, t204) in
          jump loop199()
        } else {
          jump exit165()
        }
      } in
      if t206 {
        jump k200(true)
      } else {
        jump k200(false)
      }
    } in
  } in
  jump loop199()
}

fn inherent#Ref#Ref[T]#new__T_int(value/207: int) -> Ref[int] {
  join ret207(retv208: Ref[int]) -> Ref[int] {
    retv208
  } in
  let t209 = @intrinsic(ref.new)(value/207) in
  jump ret207(t209)
}

fn inherent#Ref#Ref[T]#get__T_int(self/208: Ref[int]) -> int {
  join ret210(retv211: int) -> int {
    retv211
  } in
  let t212 = @intrinsic(ref.get)(self/208) in
  jump ret210(t212)
}

fn println__T_int(value/1: int) -> unit {
  join ret213() -> unit {
    ()
  } in
  let t214 = trait_impl#ToString#int#to_string(value/1) in
  let t215 = @runtime(core.string_println)(t214) in
  jump ret213()
}

fn inherent#Ref#Ref[T]#set__T_int(self/209: Ref[int], value/210: int) -> unit {
  join ret216() -> unit {
    ()
  } in
  let t217 = @intrinsic(ref.set)(self/209, value/210) in
  jump ret216()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret218(retv219: bool) -> bool {
    retv219
  } in
  let t220 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret218(t220)
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret221(retv222: string) -> string {
    retv222
  } in
  let t223 = @runtime(core.int_to_string)(self/40) in
  jump ret221(t223)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret224(retv225: uint64) -> uint64 {
    retv225
  } in
  let t226 = @runtime(core.int_hash)(self/85) in
  jump ret224(t226)
}