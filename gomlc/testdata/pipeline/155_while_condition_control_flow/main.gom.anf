fn main() -> unit {
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit190() -> unit {
    let _wild179 = () in
    let j/1 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit191() -> unit {
      let _wild182 = () in
      let t192 = inherent#Ref#Ref[T]#get__T_int(total/2) in
      let _wild183 = println__T_int(t192) in
      let k/3 = inherent#Ref#Ref[T]#new__T_int(0) in
      let sum/4 = inherent#Ref#Ref[T]#new__T_int(0) in
      join exit193() -> unit {
        let _wild187 = () in
        let t194 = inherent#Ref#Ref[T]#get__T_int(sum/4) in
        let _wild188 = println__T_int(t194) in
        ()
      } in
      joinrec {
        join loop195() -> unit {
          join inline_cont300(mtmp184: int) -> unit {
            join k196(jp197: bool) -> unit {
              if jp197 {
                join inline_cont298(t198: int) -> unit {
                  join inline_cont296(t199: int) -> unit {
                    let t200 = (t198 + t199) in
                    join inline_cont294(_wild185: unit) -> unit {
                      join inline_cont292(t201: int) -> unit {
                        let t202 = (t201 + 1) in
                        let inline291 = @intrinsic(ref.set)(k/3, t202) in
                        jump loop195()
                      } in
                      let inline293 = @intrinsic(ref.get)(k/3) in
                      jump inline_cont292(inline293)
                    } in
                    let inline295 = @intrinsic(ref.set)(sum/4, t200) in
                    jump inline_cont294(())
                  } in
                  let inline297 = @intrinsic(ref.get)(k/3) in
                  jump inline_cont296(inline297)
                } in
                let inline299 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont298(inline299)
              } else {
                jump exit193()
              }
            } in
            match mtmp184 {
              0 => {
                jump k196(true)
              },
              1 => {
                join inline_cont288(t205: int) -> unit {
                  join inline_cont285(t206: bool) -> unit {
                    if t206 {
                      jump k196(true)
                    } else {
                      jump k196(false)
                    }
                  } in
                  let inline286 = 0 in
                  let inline287 = @intrinsic(eq.primitive)(t205, inline286) in
                  jump inline_cont285(inline287)
                } in
                let inline289 = @intrinsic(ref.get)(sum/4) in
                jump inline_cont288(inline289)
              },
              2 => {
                jump k196(true)
              },
              _ => {
                jump k196(false)
              },
            }
          } in
          let inline301 = @intrinsic(ref.get)(k/3) in
          jump inline_cont300(inline301)
        } in
      } in
      jump loop195()
    } in
    joinrec {
      join loop207() -> unit {
        join inline_cont283(t215: int) -> unit {
          let t216 = (t215 < 4) in
          join k208(jp209: bool) -> unit {
            if jp209 {
              join inline_cont281(t210: int) -> unit {
                join inline_cont279(t211: int) -> unit {
                  let t212 = (t210 + t211) in
                  join inline_cont277(_wild180: unit) -> unit {
                    join inline_cont275(t213: int) -> unit {
                      let t214 = (t213 + 1) in
                      let inline274 = @intrinsic(ref.set)(j/1, t214) in
                      jump loop207()
                    } in
                    let inline276 = @intrinsic(ref.get)(j/1) in
                    jump inline_cont275(inline276)
                  } in
                  let inline278 = @intrinsic(ref.set)(total/2, t212) in
                  jump inline_cont277(())
                } in
                let inline280 = @intrinsic(ref.get)(j/1) in
                jump inline_cont279(inline280)
              } in
              let inline282 = @intrinsic(ref.get)(total/2) in
              jump inline_cont281(inline282)
            } else {
              jump exit191()
            }
          } in
          if t216 {
            join inline_cont271(t219: int) -> unit {
              join inline_cont268(t220: bool) -> unit {
                if t220 {
                  jump k208(true)
                } else {
                  join inline_cont266(t221: int) -> unit {
                    join inline_cont263(t222: bool) -> unit {
                      let t223 = (!t222) in
                      jump k208(t223)
                    } in
                    let inline264 = 3 in
                    let inline265 = @intrinsic(eq.primitive)(t221, inline264) in
                    jump inline_cont263(inline265)
                  } in
                  let inline267 = @intrinsic(ref.get)(j/1) in
                  jump inline_cont266(inline267)
                }
              } in
              let inline269 = 1 in
              let inline270 = @intrinsic(eq.primitive)(t219, inline269) in
              jump inline_cont268(inline270)
            } in
            let inline272 = @intrinsic(ref.get)(j/1) in
            jump inline_cont271(inline272)
          } else {
            jump k208(false)
          }
        } in
        let inline284 = @intrinsic(ref.get)(j/1) in
        jump inline_cont283(inline284)
      } in
    } in
    jump loop207()
  } in
  joinrec {
    join loop224() -> unit {
      join inline_cont261(t230: int) -> unit {
        let t231 = (t230 < 3) in
        join k225(jp226: bool) -> unit {
          if jp226 {
            join inline_cont259(t227: int) -> unit {
              join inline_cont256(_wild177: unit) -> unit {
                join inline_cont254(t228: int) -> unit {
                  let t229 = (t228 + 1) in
                  let inline253 = @intrinsic(ref.set)(i/0, t229) in
                  jump loop224()
                } in
                let inline255 = @intrinsic(ref.get)(i/0) in
                jump inline_cont254(inline255)
              } in
              let inline257 = trait_impl#ToString#int#to_string(t227) in
              let inline258 = @runtime(core.string_println)(inline257) in
              jump inline_cont256(())
            } in
            let inline260 = @intrinsic(ref.get)(i/0) in
            jump inline_cont259(inline260)
          } else {
            jump exit190()
          }
        } in
        if t231 {
          jump k225(true)
        } else {
          jump k225(false)
        }
      } in
      let inline262 = @intrinsic(ref.get)(i/0) in
      jump inline_cont261(inline262)
    } in
  } in
  jump loop224()
}

fn inherent#Ref#Ref[T]#new__T_int(value/236: int) -> Ref[int] {
  let t234 = @intrinsic(ref.new)(value/236) in
  t234
}

fn inherent#Ref#Ref[T]#get__T_int(self/237: Ref[int]) -> int {
  let t237 = @intrinsic(ref.get)(self/237) in
  t237
}

fn println__T_int(value/31: int) -> unit {
  join inline_cont302(t239: string) -> unit {
    let t240 = @runtime(core.string_println)(t239) in
    ()
  } in
  let inline303 = @runtime(core.int_to_string)(value/31) in
  jump inline_cont302(inline303)
}

fn inherent#Ref#Ref[T]#set__T_int(self/238: Ref[int], value/239: int) -> unit {
  let t242 = @intrinsic(ref.set)(self/238, value/239) in
  ()
}

fn trait_impl#Eq#int#eq(self/88: int, other/89: int) -> bool {
  let t245 = @intrinsic(eq.primitive)(self/88, other/89) in
  t245
}

fn trait_impl#ToString#int#to_string(self/69: int) -> string {
  let t248 = @runtime(core.int_to_string)(self/69) in
  t248
}

fn trait_impl#Hash#int#hash(self/114: int) -> uint64 {
  let t251 = @runtime(core.int_hash)(self/114) in
  t251
}