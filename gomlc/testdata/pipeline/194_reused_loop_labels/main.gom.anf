fn main() -> unit {
  join inline_cont227(running/0: Ref[bool]) -> unit {
    join exit176() -> unit {
      let _wild174 = () in
      let inline224 = "ok" in
      let inline225 = trait_impl#ToString#string#to_string(inline224) in
      let inline226 = @runtime(core.string_println)(inline225) in
      ()
    } in
    joinrec {
      join loop178() -> unit {
        join inline_cont221(t179: bool) -> unit {
          if t179 {
            let event/1 = Tag_0 in
            join k180() -> unit {
              let _wild172 = () in
              join inline_cont218(scanning/3: Ref[bool]) -> unit {
                join exit181() -> unit {
                  let _wild173 = () in
                  let inline216 = false in
                  let inline217 = @intrinsic(ref.set)(running/0, inline216) in
                  jump loop178()
                } in
                joinrec {
                  join loop183() -> unit {
                    join inline_cont213(t184: bool) -> unit {
                      if t184 {
                        jump loop183()
                      } else {
                        jump exit181()
                      }
                    } in
                    let inline214 = @intrinsic(ref.get)(scanning/3) in
                    jump inline_cont213(inline214)
                  } in
                } in
                jump loop183()
              } in
              let inline219 = false in
              let inline220 = @intrinsic(ref.new)(inline219) in
              jump inline_cont218(inline220)
            } in
            match event/1 {
              Tag_0 => {
                join inline_cont210(scanning/2: Ref[bool]) -> unit {
                  join exit185() -> unit {
                    jump k180()
                  } in
                  joinrec {
                    join loop186() -> unit {
                      join inline_cont208(t187: bool) -> unit {
                        if t187 {
                          jump loop186()
                        } else {
                          jump exit185()
                        }
                      } in
                      let inline209 = @intrinsic(ref.get)(scanning/2) in
                      jump inline_cont208(inline209)
                    } in
                  } in
                  jump loop186()
                } in
                let inline211 = false in
                let inline212 = @intrinsic(ref.new)(inline211) in
                jump inline_cont210(inline212)
              },
              Tag_1 => {
                jump k180()
              },
              Tag_2 => {
                jump k180()
              },
              Tag_3 => {
                jump k180()
              },
            }
          } else {
            jump exit176()
          }
        } in
        let inline222 = @intrinsic(ref.get)(running/0) in
        jump inline_cont221(inline222)
      } in
    } in
    jump loop178()
  } in
  let inline228 = true in
  let inline229 = @intrinsic(ref.new)(inline228) in
  jump inline_cont227(inline229)
}

fn inherent#Ref#Ref[T]#new__T_bool(value/257: bool) -> Ref[bool] {
  let t190 = @intrinsic(ref.new)(value/257) in
  t190
}

fn inherent#Ref#Ref[T]#get__T_bool(self/258: Ref[bool]) -> bool {
  let t193 = @intrinsic(ref.get)(self/258) in
  t193
}

fn inherent#Ref#Ref[T]#set__T_bool(self/259: Ref[bool], value/260: bool) -> unit {
  let t195 = @intrinsic(ref.set)(self/259, value/260) in
  ()
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont230(t197: string) -> unit {
    let t198 = @runtime(core.string_println)(t197) in
    ()
  } in
  jump inline_cont230(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#PartialEq#bool#eq(self/97: bool, other/98: bool) -> bool {
  let t203 = (self/97 == other/98) in
  t203
}

fn trait_impl#Hash#bool#hash(self/126: bool) -> uint64 {
  if self/126 {
    1
  } else {
    0
  }
}