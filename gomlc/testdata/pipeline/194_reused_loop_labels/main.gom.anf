fn main() -> unit {
  join inline_cont191(running/0: Ref[bool]) -> unit {
    join exit140() -> unit {
      let _wild138 = () in
      let inline188 = "ok" in
      let inline189 = trait_impl#ToString#string#to_string(inline188) in
      let inline190 = @runtime(core.string_println)(inline189) in
      ()
    } in
    joinrec {
      join loop142() -> unit {
        join inline_cont185(t143: bool) -> unit {
          if t143 {
            let event/1 = Tag_0 in
            join k144() -> unit {
              let _wild136 = () in
              join inline_cont182(scanning/3: Ref[bool]) -> unit {
                join exit145() -> unit {
                  let _wild137 = () in
                  let inline180 = false in
                  let inline181 = @intrinsic(ref.set)(running/0, inline180) in
                  jump loop142()
                } in
                joinrec {
                  join loop147() -> unit {
                    join inline_cont177(t148: bool) -> unit {
                      if t148 {
                        jump loop147()
                      } else {
                        jump exit145()
                      }
                    } in
                    let inline178 = @intrinsic(ref.get)(scanning/3) in
                    jump inline_cont177(inline178)
                  } in
                } in
                jump loop147()
              } in
              let inline183 = false in
              let inline184 = @intrinsic(ref.new)(inline183) in
              jump inline_cont182(inline184)
            } in
            match event/1 {
              Tag_0 => {
                join inline_cont174(scanning/2: Ref[bool]) -> unit {
                  join exit149() -> unit {
                    jump k144()
                  } in
                  joinrec {
                    join loop150() -> unit {
                      join inline_cont172(t151: bool) -> unit {
                        if t151 {
                          jump loop150()
                        } else {
                          jump exit149()
                        }
                      } in
                      let inline173 = @intrinsic(ref.get)(scanning/2) in
                      jump inline_cont172(inline173)
                    } in
                  } in
                  jump loop150()
                } in
                let inline175 = false in
                let inline176 = @intrinsic(ref.new)(inline175) in
                jump inline_cont174(inline176)
              },
              Tag_1 => {
                jump k144()
              },
              Tag_2 => {
                jump k144()
              },
              Tag_3 => {
                jump k144()
              },
            }
          } else {
            jump exit140()
          }
        } in
        let inline186 = @intrinsic(ref.get)(running/0) in
        jump inline_cont185(inline186)
      } in
    } in
    jump loop142()
  } in
  let inline192 = true in
  let inline193 = @intrinsic(ref.new)(inline192) in
  jump inline_cont191(inline193)
}

fn inherent#Ref#Ref[T]#new__T_bool(value/232: bool) -> Ref[bool] {
  let t154 = @intrinsic(ref.new)(value/232) in
  t154
}

fn inherent#Ref#Ref[T]#get__T_bool(self/233: Ref[bool]) -> bool {
  let t157 = @intrinsic(ref.get)(self/233) in
  t157
}

fn inherent#Ref#Ref[T]#set__T_bool(self/234: Ref[bool], value/235: bool) -> unit {
  let t159 = @intrinsic(ref.set)(self/234, value/235) in
  ()
}

fn println__T_string(value/31: string) -> unit {
  join inline_cont194(t161: string) -> unit {
    let t162 = @runtime(core.string_println)(t161) in
    ()
  } in
  jump inline_cont194(value/31)
}

fn trait_impl#ToString#string#to_string(self/67: string) -> string {
  self/67
}

fn trait_impl#Eq#bool#eq(self/97: bool, other/98: bool) -> bool {
  let t167 = @intrinsic(eq.primitive)(self/97, other/98) in
  t167
}

fn trait_impl#Hash#bool#hash(self/126: bool) -> uint64 {
  if self/126 {
    1
  } else {
    0
  }
}