fn main() -> unit {
  join inline_cont237(running/0: Ref[bool]) -> unit {
    join exit186() -> unit {
      let inline234 = "ok" in
      let inline235 = trait_impl#ToString#string#to_string(inline234) in
      let inline236 = @runtime(core.string_println)(inline235) in
      ()
    } in
    joinrec {
      join loop188() -> unit {
        join inline_cont231(t189: bool) -> unit {
          if t189 {
            let event/1 = Tag_0 in
            join k190() -> unit {
              join inline_cont228(scanning/3: Ref[bool]) -> unit {
                join exit191() -> unit {
                  let inline226 = false in
                  let inline227 = @intrinsic(ref.set)(running/0, inline226) in
                  jump loop188()
                } in
                joinrec {
                  join loop193() -> unit {
                    join inline_cont223(t194: bool) -> unit {
                      if t194 {
                        jump loop193()
                      } else {
                        jump exit191()
                      }
                    } in
                    let inline224 = @intrinsic(ref.get)(scanning/3) in
                    jump inline_cont223(inline224)
                  } in
                } in
                jump loop193()
              } in
              let inline229 = false in
              let inline230 = @intrinsic(ref.new)(inline229) in
              jump inline_cont228(inline230)
            } in
            match event/1 {
              Tag_0 => {
                join inline_cont220(scanning/2: Ref[bool]) -> unit {
                  join exit195() -> unit {
                    jump k190()
                  } in
                  joinrec {
                    join loop196() -> unit {
                      join inline_cont218(t197: bool) -> unit {
                        if t197 {
                          jump loop196()
                        } else {
                          jump exit195()
                        }
                      } in
                      let inline219 = @intrinsic(ref.get)(scanning/2) in
                      jump inline_cont218(inline219)
                    } in
                  } in
                  jump loop196()
                } in
                let inline221 = false in
                let inline222 = @intrinsic(ref.new)(inline221) in
                jump inline_cont220(inline222)
              },
              Tag_1 => {
                jump k190()
              },
              Tag_2 => {
                jump k190()
              },
              Tag_3 => {
                jump k190()
              },
            }
          } else {
            jump exit186()
          }
        } in
        let inline232 = @intrinsic(ref.get)(running/0) in
        jump inline_cont231(inline232)
      } in
    } in
    jump loop188()
  } in
  let inline238 = true in
  let inline239 = @intrinsic(ref.new)(inline238) in
  jump inline_cont237(inline239)
}

fn inherent#Ref#Ref[T]#new__T_bool(value/270: bool) -> Ref[bool] {
  let t200 = @intrinsic(ref.new)(value/270) in
  t200
}

fn inherent#Ref#Ref[T]#get__T_bool(self/271: Ref[bool]) -> bool {
  let t203 = @intrinsic(ref.get)(self/271) in
  t203
}

fn inherent#Ref#Ref[T]#set__T_bool(self/272: Ref[bool], value/273: bool) -> unit {
  let t205 = @intrinsic(ref.set)(self/272, value/273) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont240(t207: string) -> unit {
    let t208 = @runtime(core.string_println)(t207) in
    ()
  } in
  jump inline_cont240(value/1)
}

fn trait_impl#ToString#string#to_string(self/65: string) -> string {
  self/65
}

fn trait_impl#PartialEq#bool#eq(self/95: bool, other/96: bool) -> bool {
  let t213 = (self/95 == other/96) in
  t213
}

fn trait_impl#Hash#bool#hash(self/124: bool) -> uint64 {
  if self/124 {
    1
  } else {
    0
  }
}