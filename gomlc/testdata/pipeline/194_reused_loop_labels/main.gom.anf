fn main() -> unit {
  join inline_cont463(running/0: Ref[bool]) -> unit {
    join exit412() -> unit {
      let inline460 = "ok" in
      let inline461 = trait_impl#ToString#string#to_string(inline460) in
      let inline462 = @runtime(core.string_println)(inline461) in
      ()
    } in
    joinrec {
      join loop414() -> unit {
        join inline_cont457(t415: bool) -> unit {
          if t415 {
            let event/1 = Tag_0 in
            join k416() -> unit {
              join inline_cont454(scanning/3: Ref[bool]) -> unit {
                join exit417() -> unit {
                  let inline452 = false in
                  let inline453 = @intrinsic(ref.set)(running/0, inline452) in
                  jump loop414()
                } in
                joinrec {
                  join loop419() -> unit {
                    join inline_cont449(t420: bool) -> unit {
                      if t420 {
                        jump loop419()
                      } else {
                        jump exit417()
                      }
                    } in
                    let inline450 = @intrinsic(ref.get)(scanning/3) in
                    jump inline_cont449(inline450)
                  } in
                } in
                jump loop419()
              } in
              let inline455 = false in
              let inline456 = @intrinsic(ref.new)(inline455) in
              jump inline_cont454(inline456)
            } in
            match event/1 {
              Tag_0 => {
                join inline_cont446(scanning/2: Ref[bool]) -> unit {
                  join exit421() -> unit {
                    jump k416()
                  } in
                  joinrec {
                    join loop422() -> unit {
                      join inline_cont444(t423: bool) -> unit {
                        if t423 {
                          jump loop422()
                        } else {
                          jump exit421()
                        }
                      } in
                      let inline445 = @intrinsic(ref.get)(scanning/2) in
                      jump inline_cont444(inline445)
                    } in
                  } in
                  jump loop422()
                } in
                let inline447 = false in
                let inline448 = @intrinsic(ref.new)(inline447) in
                jump inline_cont446(inline448)
              },
              Tag_1 => {
                jump k416()
              },
              Tag_2 => {
                jump k416()
              },
              Tag_3 => {
                jump k416()
              },
            }
          } else {
            jump exit412()
          }
        } in
        let inline458 = @intrinsic(ref.get)(running/0) in
        jump inline_cont457(inline458)
      } in
    } in
    jump loop414()
  } in
  let inline464 = true in
  let inline465 = @intrinsic(ref.new)(inline464) in
  jump inline_cont463(inline465)
}

fn inherent#Ref#Ref[T]#new__T_bool(value/431: bool) -> Ref[bool] {
  let t426 = @intrinsic(ref.new)(value/431) in
  t426
}

fn inherent#Ref#Ref[T]#get__T_bool(self/432: Ref[bool]) -> bool {
  let t429 = @intrinsic(ref.get)(self/432) in
  t429
}

fn inherent#Ref#Ref[T]#set__T_bool(self/433: Ref[bool], value/434: bool) -> unit {
  let t431 = @intrinsic(ref.set)(self/433, value/434) in
  ()
}

fn println__T_string(value/1: string) -> unit {
  join inline_cont466(t433: string) -> unit {
    let t434 = @runtime(core.string_println)(t433) in
    ()
  } in
  jump inline_cont466(value/1)
}

fn trait_impl#ToString#string#to_string(self/149: string) -> string {
  self/149
}

fn trait_impl#PartialEq#bool#eq(self/179: bool, other/180: bool) -> bool {
  let t439 = (self/179 == other/180) in
  t439
}

fn trait_impl#Hash#bool#hash(self/208: bool) -> uint64 {
  if self/208 {
    1
  } else {
    0
  }
}