fn main() -> unit {
  join inline_cont0(running/0: Ref[bool]) -> unit {
    join exit0() -> unit {
      let inline0 = "ok" in
      let inline1 = trait_impl#ToString#string#to_string(inline0) in
      let inline2 = @runtime(core.string_println)(inline1) in
      ()
    } in
    joinrec {
      join loop0() -> unit {
        join inline_cont1(t0: bool) -> unit {
          if t0 {
            let event/0 = Tag_0 in
            join k0() -> unit {
              join inline_cont2(scanning/0: Ref[bool]) -> unit {
                join exit1() -> unit {
                  let inline3 = false in
                  let inline4 = @intrinsic(ref.set)(running/0, inline3) in
                  jump loop0()
                } in
                joinrec {
                  join loop1() -> unit {
                    join inline_cont3(t1: bool) -> unit {
                      if t1 {
                        jump loop1()
                      } else {
                        jump exit1()
                      }
                    } in
                    let inline5 = @intrinsic(ref.get)(scanning/0) in
                    jump inline_cont3(inline5)
                  } in
                } in
                jump loop1()
              } in
              let inline6 = false in
              let inline7 = @intrinsic(ref.new)(inline6) in
              jump inline_cont2(inline7)
            } in
            match event/0 {
              Tag_0 => {
                join inline_cont4(scanning/1: Ref[bool]) -> unit {
                  join exit2() -> unit {
                    jump k0()
                  } in
                  joinrec {
                    join loop2() -> unit {
                      join inline_cont5(t2: bool) -> unit {
                        if t2 {
                          jump loop2()
                        } else {
                          jump exit2()
                        }
                      } in
                      let inline8 = @intrinsic(ref.get)(scanning/1) in
                      jump inline_cont5(inline8)
                    } in
                  } in
                  jump loop2()
                } in
                let inline9 = false in
                let inline10 = @intrinsic(ref.new)(inline9) in
                jump inline_cont4(inline10)
              },
              Tag_1 => {
                jump k0()
              },
              Tag_2 => {
                jump k0()
              },
              Tag_3 => {
                jump k0()
              },
            }
          } else {
            jump exit0()
          }
        } in
        let inline11 = @intrinsic(ref.get)(running/0) in
        jump inline_cont1(inline11)
      } in
    } in
    jump loop0()
  } in
  let inline12 = true in
  let inline13 = @intrinsic(ref.new)(inline12) in
  jump inline_cont0(inline13)
}

fn inherent#Ref#Ref[T]#new__T_bool(value/0: bool) -> Ref[bool] {
  let t0 = @intrinsic(ref.new)(value/0) in
  t0
}

fn inherent#Ref#Ref[T]#get__T_bool(self/0: Ref[bool]) -> bool {
  let t0 = @intrinsic(ref.get)(self/0) in
  t0
}

fn inherent#Ref#Ref[T]#set__T_bool(self/0: Ref[bool], value/0: bool) -> unit {
  let t0 = @intrinsic(ref.set)(self/0, value/0) in
  ()
}

fn println__T_string(value/0: string) -> unit {
  join inline_cont0(t0: string) -> unit {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  jump inline_cont0(value/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#PartialEq#bool#eq(self/0: bool, other/0: bool) -> bool {
  let t0 = (self/0 == other/0) in
  t0
}

fn trait_impl#Hash#bool#hash(self/0: bool) -> u64 {
  if self/0 {
    1
  } else {
    0
  }
}