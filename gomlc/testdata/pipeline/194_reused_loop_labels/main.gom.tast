fn main() -> unit {
    let running/0: Ref[bool] = (Ref[bool]::new : (bool) -> Ref[bool])(true);
    while (Ref[bool]::get : (Ref[bool]) -> bool)((running/0 : Ref[bool])) {
        {
            let event/1: Event = Event::Open;
            match (event/1 : Event) {
                Event::Open =>
                  {
                      let scanning/2: Ref[bool] = (Ref[bool]::new : (bool) -> Ref[bool])(false);
                      while (Ref[bool]::get : (Ref[bool]) -> bool)((scanning/2 : Ref[bool])) {
                          {}
                      }
                  },
                Event::Close => (),
                Event::Advance => (),
                Event::Error => (),
            };
            let scanning/3: Ref[bool] = (Ref[bool]::new : (bool) -> Ref[bool])(false);
            while (Ref[bool]::get : (Ref[bool]) -> bool)((scanning/3 : Ref[bool])) {
                {}
            };
            (Ref[bool]::set : (Ref[bool], bool) -> unit)((running/0 : Ref[bool]), false)
        }
    };
    (println : (string) -> unit)("ok")
}