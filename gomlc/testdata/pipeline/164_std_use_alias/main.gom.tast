fn main() -> () {
    (std::io::println : (string) -> ())(
      (ToString::to_string : (bool) -> string)(
        (Vec[string]::len : (Vec[string]) -> isize)((std::env::args : () -> Vec[string])()) > 0
      )
    )
}