fn loop_return_unit(flag/0: bool) -> () {
    while if (flag/0 : bool) {
        {
            return
        }
    } else {
        {
            return
        }
    } {
        {
            (println : (string) -> ())("unreachable");
        }
    }
}

fn loop_return() -> i32 {
    while match 0 {
        _ : isize => return 5,
    } {
        {
            (println : (string) -> ())("unreachable");
        }
    };
    0
}

fn main() -> () {
    (loop_return_unit : (bool) -> ())(true);
    (println : (i32) -> ())((loop_return : () -> i32)());
}