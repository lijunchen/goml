impl ToString for (isize, isize) {
  fn to_string(self/0: (isize, isize)) -> string {
      "(?, ?)"
  }
}

fn main() -> () {
    let x/1: isize = 123;
    let _ : () =
      (println : (string) -> ())((ToString::to_string : (isize) -> string)((x/1 : isize)));
    let x/2: bool = true;
    let _ : () = (println : (string) -> ())((ToString::to_string : (bool) -> string)((x/2 : bool)));
    let x/3: (isize, isize) = (3, 4);
    let _ : () =
      (println : (string) -> ())(
        (ToString::to_string : ((isize, isize)) -> string)((x/3 : (isize, isize)))
      );
}