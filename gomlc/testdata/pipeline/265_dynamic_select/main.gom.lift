fn print_selection(value/0: std::channel::Selection__isize) -> unit {
  match value/0 {
    std::channel::Selection__isize::Received(x0, x1) => {
      let x0 = std::channel::Selection__isize::Received._0(value/0) in
      let x1 = std::channel::Selection__isize::Received._1(value/0) in
      let value/1 = x1 in
      let index/0 = x0 in
      let _wild0 = println__T_isize(index/0) in
      println__T_isize(inherent#Option#Option[T]#unwrap_or__T_isize(value/1, -1))
    },
    std::channel::Selection__isize::Sent(x2) => {
      let x2 = std::channel::Selection__isize::Sent._0(value/0) in
      let index/1 = x2 in
      println__T_isize(index/1)
    },
  }
}

fn main() -> unit {
  let first/0 = inherent#Channel#Channel[T]#new__T_isize(1) in
  let second/0 = inherent#Channel#Channel[T]#new__T_isize(1) in
  let _wild0 = inherent#Channel#Channel[T]#send__T_isize(first/0, 10) in
  let _wild1 = inherent#Channel#Channel[T]#send__T_isize(second/0, 20) in
  let operations/0 =
    @intrinsic(vec.from_array)(
      [
        std::channel::Operation__isize::Receive(
          inherent#Channel#Channel[T]#receiver__T_isize(first/0)
        ),
        std::channel::Operation__isize::Receive(
          inherent#Channel#Channel[T]#receiver__T_isize(second/0)
        )
      ]
    ) in
  let _wild2 =
    let mtmp0 =
      std::channel::try_select_priority__T_isize(
        @intrinsic(slice.new)(
          operations/0,
          0,
          inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](operations/0)
        )
      ) in
    match mtmp0 {
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok(x0) => {
        let x0 =
          Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok._0(mtmp0) in
        match x0 {
          Option__std::channel::Selection__isize::Some(x1) => {
            let x1 = Option__std::channel::Selection__isize::Some._0(x0) in
            let value/0 = x1 in
            print_selection(value/0)
          },
          _ => println__T_isize(-1),
        }
      },
      _ => println__T_isize(-1),
    } in
  let _wild3 =
    let mtmp1 =
      std::channel::try_select__T_isize(
        @intrinsic(slice.new)(
          operations/0,
          0,
          inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](operations/0)
        )
      ) in
    match mtmp1 {
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok(x2) => {
        let x2 =
          Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok._0(mtmp1) in
        match x2 {
          Option__std::channel::Selection__isize::Some(x3) => {
            let x3 = Option__std::channel::Selection__isize::Some._0(x2) in
            let value/1 = x3 in
            print_selection(value/1)
          },
          _ => println__T_isize(-2),
        }
      },
      _ => println__T_isize(-2),
    } in
  let empty/0 = inherent#Channel#Channel[T]#new__T_isize(0) in
  let target/0 = inherent#Channel#Channel[T]#new__T_isize(1) in
  let mixed/0 =
    @intrinsic(vec.from_array)(
      [
        std::channel::Operation__isize::Receive(
          inherent#Channel#Channel[T]#receiver__T_isize(empty/0)
        ),
        std::channel::Operation__isize::Send(
          inherent#Channel#Channel[T]#sender__T_isize(target/0),
          30
        )
      ]
    ) in
  let _wild4 =
    let mtmp2 =
      std::channel::select__T_isize(
        @intrinsic(slice.new)(
          mixed/0,
          0,
          inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](mixed/0)
        )
      ) in
    match mtmp2 {
      Result__std::channel::Selection__isize__std::channel::SelectError::Ok(x4) => {
        let x4 = Result__std::channel::Selection__isize__std::channel::SelectError::Ok._0(mtmp2) in
        let value/2 = x4 in
        print_selection(value/2)
      },
      Result__std::channel::Selection__isize__std::channel::SelectError::Err(x5) => {
        println__T_isize(-3)
      },
    } in
  let _wild5 =
    println__T_isize(
      inherent#Option#Option[T]#unwrap_or__T_isize(
        inherent#Channel#Channel[T]#recv__T_isize(target/0),
        -4
      )
    ) in
  let none/0 =
    @intrinsic(vec.from_array)(
      [
        std::channel::Operation__isize::Receive(
          inherent#Channel#Channel[T]#receiver__T_isize(empty/0)
        )
      ]
    ) in
  let _wild6 =
    println__T_isize(
      let mtmp3 =
        std::channel::try_select__T_isize(
          @intrinsic(slice.new)(
            none/0,
            0,
            inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](none/0)
          )
        ) in
      match mtmp3 {
        Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok(x6) => {
          let x6 =
            Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok._0(
              mtmp3
            ) in
          match x6 {
            Option__std::channel::Selection__isize::None => {
              40
            },
            _ => -5,
          }
        },
        _ => -5,
      }
    ) in
  let no_operations/0 = inherent#Vec#Vec[T]#new__T_std::channel::Operation[isize]() in
  println__T_isize(
    let mtmp4 =
      std::channel::select__T_isize(
        @intrinsic(slice.new)(
          no_operations/0,
          0,
          inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](no_operations/0)
        )
      ) in
    match mtmp4 {
      Result__std::channel::Selection__isize__std::channel::SelectError::Err(x7) => {
        let x7 = Result__std::channel::Selection__isize__std::channel::SelectError::Err._0(mtmp4) in
        match x7 {
          std::channel::SelectError::Empty => {
            50
          },
        }
      },
      _ => -6,
    }
  )
}

fn println__T_isize(value/0: isize) -> unit {
  @runtime(core.string_println)(trait_impl#ToString#isize#to_string(value/0))
}

fn inherent#Option#Option[T]#unwrap_or__T_isize(self/0: Option__isize, fallback/0: isize) -> isize {
  match self/0 {
    Option__isize::None => {
      fallback/0
    },
    Option__isize::Some(x0) => {
      let x0 = Option__isize::Some._0(self/0) in
      let value/0 = x0 in
      value/0
    },
  }
}

fn inherent#Channel#Channel[T]#new__T_isize(capacity/0: isize) -> Channel[isize] {
  @intrinsic(channel.new)(capacity/0)
}

fn inherent#Channel#Channel[T]#send__T_isize(self/0: Channel[isize], value/0: isize) -> unit {
  @intrinsic(channel.send)(self/0, value/0)
}

fn inherent#Channel#Channel[T]#receiver__T_isize(self/0: Channel[isize]) -> Receiver[isize] {
  @intrinsic(channel.receiver)(self/0)
}

fn std::channel::try_select_priority__T_isize(
  operations/0: Slice[std::channel::Operation__isize]
) -> Result__Option__std::channel::Selection__isize__std::channel::SelectError {
  if (inherent#Slice#Slice[T]#len__T_std::channel::Operation[isize](operations/0) == 0) {
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Err(
        std::channel::SelectError::Empty
      )
  } else {
      let prepared/0 = std::channel::prepare__T_isize(operations/0) in
      let mtmp0 = std::channel::prepared_slices__T_isize(prepared/0) in
      let x0 = mtmp0.0 in
      let x1 = mtmp0.1 in
      let x2 = mtmp0.2 in
      let x3 = mtmp0.3 in
      let values/0 = x3 in
      let senders/0 = x2 in
      let receivers/0 = x1 in
      let kinds/0 = x0 in
      let mtmp1 =
        @intrinsic(channel.dynamic_try_select_priority)(
          kinds/0,
          receivers/0,
          senders/0,
          values/0
        ) in
      let x4 = mtmp1.0 in
      let x5 = mtmp1.1 in
      let x6 = mtmp1.2 in
      let x7 = mtmp1.3 in
      let x8 = mtmp1.4 in
      let selected/0 = x8 in
      let open/0 = x7 in
      let value/0 = x6 in
      let kind/0 = x5 in
      let index/0 = x4 in
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok(
        if selected/0 {
            Option__std::channel::Selection__isize::Some(
              std::channel::selection__T_isize(index/0, kind/0, value/0, open/0)
            )
        } else {
            Option__std::channel::Selection__isize::None
        }
      )
  }
}

fn inherent#Vec#Vec[T]#len__T_std::channel::Operation[isize](
  self/0: Vec[std::channel::Operation__isize]
) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn std::channel::try_select__T_isize(
  operations/0: Slice[std::channel::Operation__isize]
) -> Result__Option__std::channel::Selection__isize__std::channel::SelectError {
  if (inherent#Slice#Slice[T]#len__T_std::channel::Operation[isize](operations/0) == 0) {
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Err(
        std::channel::SelectError::Empty
      )
  } else {
      let prepared/0 = std::channel::prepare__T_isize(operations/0) in
      let mtmp0 = std::channel::prepared_slices__T_isize(prepared/0) in
      let x0 = mtmp0.0 in
      let x1 = mtmp0.1 in
      let x2 = mtmp0.2 in
      let x3 = mtmp0.3 in
      let values/0 = x3 in
      let senders/0 = x2 in
      let receivers/0 = x1 in
      let kinds/0 = x0 in
      let mtmp1 =
        @intrinsic(channel.dynamic_try_select)(kinds/0, receivers/0, senders/0, values/0) in
      let x4 = mtmp1.0 in
      let x5 = mtmp1.1 in
      let x6 = mtmp1.2 in
      let x7 = mtmp1.3 in
      let x8 = mtmp1.4 in
      let selected/0 = x8 in
      let open/0 = x7 in
      let value/0 = x6 in
      let kind/0 = x5 in
      let index/0 = x4 in
      Result__Option__std::channel::Selection__isize__std::channel::SelectError::Ok(
        if selected/0 {
            Option__std::channel::Selection__isize::Some(
              std::channel::selection__T_isize(index/0, kind/0, value/0, open/0)
            )
        } else {
            Option__std::channel::Selection__isize::None
        }
      )
  }
}

fn inherent#Channel#Channel[T]#sender__T_isize(self/0: Channel[isize]) -> Sender[isize] {
  @intrinsic(channel.sender)(self/0)
}

fn std::channel::select__T_isize(
  operations/0: Slice[std::channel::Operation__isize]
) -> Result__std::channel::Selection__isize__std::channel::SelectError {
  if (inherent#Slice#Slice[T]#len__T_std::channel::Operation[isize](operations/0) == 0) {
      Result__std::channel::Selection__isize__std::channel::SelectError::Err(
        std::channel::SelectError::Empty
      )
  } else {
      let prepared/0 = std::channel::prepare__T_isize(operations/0) in
      let mtmp0 = std::channel::prepared_slices__T_isize(prepared/0) in
      let x0 = mtmp0.0 in
      let x1 = mtmp0.1 in
      let x2 = mtmp0.2 in
      let x3 = mtmp0.3 in
      let values/0 = x3 in
      let senders/0 = x2 in
      let receivers/0 = x1 in
      let kinds/0 = x0 in
      let mtmp1 = @intrinsic(channel.dynamic_select)(kinds/0, receivers/0, senders/0, values/0) in
      let x4 = mtmp1.0 in
      let x5 = mtmp1.1 in
      let x6 = mtmp1.2 in
      let x7 = mtmp1.3 in
      let open/0 = x7 in
      let value/0 = x6 in
      let kind/0 = x5 in
      let index/0 = x4 in
      Result__std::channel::Selection__isize__std::channel::SelectError::Ok(
        std::channel::selection__T_isize(index/0, kind/0, value/0, open/0)
      )
  }
}

fn inherent#Channel#Channel[T]#recv__T_isize(self/0: Channel[isize]) -> Option__isize {
  let mtmp0 = @intrinsic(channel.recv)(self/0) in
  let x0 = mtmp0.0 in
  let x1 = mtmp0.1 in
  let ok/0 = x1 in
  let value/0 = x0 in
  if ok/0 {
      Option__isize::Some(value/0)
  } else {
      Option__isize::None
  }
}

fn inherent#Vec#Vec[T]#new__T_std::channel::Operation[isize]() -> Vec[
  std::channel::Operation__isize
] {
  @intrinsic(vec.new)()
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  __goml_builtin_int_to_string(self/0)
}

fn inherent#Slice#Slice[T]#len__T_std::channel::Operation[isize](
  self/0: Slice[std::channel::Operation__isize]
) -> isize {
  @intrinsic(slice.len)(self/0)
}

fn std::channel::prepare__T_isize(
  operations/0: Slice[std::channel::Operation__isize]
) -> std::channel::PreparedOperations__isize {
  let kinds/0 =
    inherent#Vec#Vec[T]#with_capacity__T_isize(
      inherent#Slice#Slice[T]#len__T_std::channel::Operation[isize](operations/0)
    ) in
  let receivers/0 = inherent#Vec#Vec[T]#new__T_Receiver[isize]() in
  let senders/0 = inherent#Vec#Vec[T]#new__T_Sender[isize]() in
  let values/0 = inherent#Vec#Vec[T]#new__T_isize() in
  let _wild0 =
    let for_source0 = operations/0 in
    let for_limit0 = @intrinsic(slice.len)(for_source0) in
    let for_index0 = 0 in
    while (for_index0 < for_limit0) {
        let for_item0 = @intrinsic(slice.get)(for_source0, for_index0) in
        let _wild1 = for_index0 = (for_index0 + 1) in
        let operation/0 = for_item0 in
        match operation/0 {
          std::channel::Operation__isize::Receive(x0) => {
            let x0 = std::channel::Operation__isize::Receive._0(operation/0) in
            let receiver/0 = x0 in
            let _wild2 = inherent#Vec#Vec[T]#push__T_isize(kinds/0, 0) in
            inherent#Vec#Vec[T]#push__T_Receiver[isize](receivers/0, receiver/0)
          },
          std::channel::Operation__isize::Send(x1, x2) => {
            let x1 = std::channel::Operation__isize::Send._0(operation/0) in
            let x2 = std::channel::Operation__isize::Send._1(operation/0) in
            let value/0 = x2 in
            let sender/0 = x1 in
            let _wild3 = inherent#Vec#Vec[T]#push__T_isize(kinds/0, 1) in
            let _wild4 = inherent#Vec#Vec[T]#push__T_Sender[isize](senders/0, sender/0) in
            inherent#Vec#Vec[T]#push__T_isize(values/0, value/0)
          },
        }
    } in
  std::channel::PreparedOperations__isize {
    kinds: kinds/0,
    receivers: receivers/0,
    senders: senders/0,
    values: values/0
  }
}

fn std::channel::prepared_slices__T_isize(prepared/0: std::channel::PreparedOperations__isize) -> (
  Slice[isize],
  Slice[Receiver[isize]],
  Slice[Sender[isize]],
  Slice[isize]
) {
  (
    @intrinsic(slice.new)(
      std::channel::PreparedOperations__isize.kinds(prepared/0),
      0,
      inherent#Vec#Vec[T]#len__T_isize(std::channel::PreparedOperations__isize.kinds(prepared/0))
    ),
    @intrinsic(slice.new)(
      std::channel::PreparedOperations__isize.receivers(prepared/0),
      0,
      inherent#Vec#Vec[T]#len__T_Receiver[isize](
        std::channel::PreparedOperations__isize.receivers(prepared/0)
      )
    ),
    @intrinsic(slice.new)(
      std::channel::PreparedOperations__isize.senders(prepared/0),
      0,
      inherent#Vec#Vec[T]#len__T_Sender[isize](
        std::channel::PreparedOperations__isize.senders(prepared/0)
      )
    ),
    @intrinsic(slice.new)(
      std::channel::PreparedOperations__isize.values(prepared/0),
      0,
      inherent#Vec#Vec[T]#len__T_isize(std::channel::PreparedOperations__isize.values(prepared/0))
    )
  )
}

fn std::channel::selection__T_isize(
  index/0: isize,
  kind/0: isize,
  value/0: isize,
  open/0: bool
) -> std::channel::Selection__isize {
  if (kind/0 == 0) {
      std::channel::Selection__isize::Received(
        index/0,
        if open/0 {
            Option__isize::Some(value/0)
        } else {
            Option__isize::None
        }
      )
  } else {
      std::channel::Selection__isize::Sent(index/0)
  }
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  signed_decimal_string((value/0 as i64))
}

fn inherent#Vec#Vec[T]#with_capacity__T_isize(capacity/0: isize) -> Vec[isize] {
  @intrinsic(vec.with_capacity)(capacity/0)
}

fn inherent#Vec#Vec[T]#new__T_Receiver[isize]() -> Vec[Receiver[isize]] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#new__T_Sender[isize]() -> Vec[Sender[isize]] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#new__T_isize() -> Vec[isize] {
  @intrinsic(vec.new)()
}

fn inherent#Vec#Vec[T]#push__T_isize(self/0: Vec[isize], elem/0: isize) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#push__T_Receiver[isize](
  self/0: Vec[Receiver[isize]],
  elem/0: Receiver[isize]
) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#push__T_Sender[isize](
  self/0: Vec[Sender[isize]],
  elem/0: Sender[isize]
) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_isize(self/0: Vec[isize]) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn inherent#Vec#Vec[T]#len__T_Receiver[isize](self/0: Vec[Receiver[isize]]) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn inherent#Vec#Vec[T]#len__T_Sender[isize](self/0: Vec[Sender[isize]]) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      ("-" + decimal_string((0 - (value/0 as u64))))
  } else {
      decimal_string((value/0 as u64))
  }
}

fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        return "0"
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        let _wild2 =
          inherent#Vec#Vec[T]#push__T_u0(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
        let _wild3 =
          let compound_old0 = remaining/0 in
          let compound_value0 = 10 in
          remaining/0 = (compound_old0 / compound_value0) in
        ()
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[T]#len__T_u0(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[T]#len__T_u0(reversed/0)) {
        let _wild5 =
          inherent#Vec#Vec[T]#push__T_u0(
            bytes/0,
            @intrinsic(vec.get)(
              reversed/0,
              ((inherent#Vec#Vec[T]#len__T_u0(reversed/0) - offset/0) - 1)
            )
          ) in
        let _wild6 =
          let compound_old1 = offset/0 in
          let compound_value1 = 1 in
          offset/0 = (compound_old1 + compound_value1) in
        ()
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  let x0 = mtmp0.1 in
  let result/0 = x0 in
  result/0
}

fn inherent#Vec#Vec[T]#push__T_u8(self/0: Vec[u8], elem/0: u8) -> unit {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len__T_u8(self/0: Vec[u8]) -> isize {
  @intrinsic(vec.len)(self/0)
}