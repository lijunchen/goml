package model;

use std::cmp;

#[derive(PartialEq, Eq, PartialOrd, Ord, Hash, Default, Debug)]
pub struct Key {
    pub major: int,
    pub minor: int,
}

#[derive(PartialEq, Eq, PartialOrd, Ord, Default, Debug)]
pub enum State {
    Active(string),
    Idle,
}
