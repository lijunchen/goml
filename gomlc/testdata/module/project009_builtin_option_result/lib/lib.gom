package lib;

pub fn maybe_one() -> Option[i32] {
    Option::Some(1)
}

pub fn maybe_err(x: i32) -> Result[i32, string] {
    if x == 0 {
        Result::Err("zero")
    } else {
        Result::Ok(x)
    }
}
