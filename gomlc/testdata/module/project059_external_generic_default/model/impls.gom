package model;

use tests::external_generic_default::api;

impl api::Pick for Value {
    fn number(self: Self) -> int {
        self.number
    }
}

impl api::Decode for Value {
    fn decode(value: int) -> Result[Value, string] {
        Result::Ok(Value { number: value })
    }
}
