package math;

pub struct Pair {
    pub a: i32,
    pub b: i32,
}

pub enum Op {
    Add,
    Sub,
}

pub fn apply(op: Op, a: i32, b: i32) -> i32 {
    match op {
        Op::Add => a + b,
        Op::Sub => a - b,
    }
}

pub fn apply_pair(op: Op, p: Pair) -> i32 {
    apply(op, p.a, p.b)
}
