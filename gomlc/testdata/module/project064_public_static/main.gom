package main;

use test::public_static::cache;

fn main() -> () {
    println(cache::value().get(0).to_string());
    println(cache::VALUE.get_or_init(|| Vec::from_array([9]).freeze()).get(1).to_string())
}
