package main;

use select_project::worker;

fn main() -> () {
    let channel = worker::ready(42);
    let value = select {
        recv(channel) as received => received.unwrap_or(-1),
        default => 0,
    };
    println(value)
}
