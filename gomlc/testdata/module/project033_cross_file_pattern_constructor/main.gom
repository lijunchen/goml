package main;

fn label(status: Status) -> string {
    match status {
        Idle => "idle",
        Busy(value) => value.to_string(),
    }
}

fn main() -> () {
    println(label(Idle));
    println(label(Busy(4)));
}
