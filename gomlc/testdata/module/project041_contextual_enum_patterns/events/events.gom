package events;

pub enum Event {
    Ready(i32),
    Failed(string),
}

pub fn ready(value: i32) -> Event {
    Event::Ready(value)
}

pub fn failed(message: string) -> Event {
    Event::Failed(message)
}
