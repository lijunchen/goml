package main;

use project013_dyn_coercion_across_packages::datapkg;
use project013_dyn_coercion_across_packages::usepkg;

fn main() -> () {
    println(usepkg::call_wrap(datapkg::Wrap { value: 5 }));
}
