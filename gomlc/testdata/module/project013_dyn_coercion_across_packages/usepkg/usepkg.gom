package usepkg;

use project013_dyn_coercion_across_packages::datapkg;
use project013_dyn_coercion_across_packages::traitpkg;
use traitpkg::Display;

pub fn call_wrap(x: datapkg::Wrap) -> string {
    x.show()
}
