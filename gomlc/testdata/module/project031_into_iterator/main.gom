package main;

use test::project031::iterables;

fn total[S: IntoIterator](source: S) -> int32 where S::Item = int32 {
    let result = Ref::new(0);
    for value in source {
        result.set(result.get() + value);
    }
    result.get()
}

fn main() -> unit {
    println(total(iterables::numbers()));
}
