package usepkg;

use project015_trait_impl_visibility_for_builtin_container::traitpkg;

pub fn measure() -> string {
    let v: Vec[i32] = Vec::new();
    v.push(1);
    traitpkg::Size::size(v).to_string()
}
