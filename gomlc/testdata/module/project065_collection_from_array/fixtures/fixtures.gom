package fixtures;

pub fn values() -> Vec[isize] {
    let source = [1, 2, 3];
    Vec::from_array(source)
}

pub fn counts() -> HashMap[string, isize] {
    HashMap::from_array([("a", 1), ("a", 2)])
}
