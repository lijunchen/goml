package lib;

pub enum Color {
    Red,
    Green,
}

pub fn color_to_int(c: Color) -> i32 {
    match c {
        Color::Red => 1,
        Color::Green => 2,
    }
}

pub struct Point {
    pub x: i32,
    pub y: i32,
}

pub fn sum_point(p: Point) -> i32 {
    p.x + p.y
}
