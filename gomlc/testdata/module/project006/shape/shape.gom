package shape;

pub struct Point {
    x: i32,
    y: i32,
}

pub fn make(x: i32, y: i32) -> Point {
    Point { x: x, y: y }
}

pub fn inc(p: Point) -> Point {
    Point { x: p.x + 1, y: p.y + 1 }
}

pub fn sum(p: Point) -> i32 {
    p.x + p.y
}
