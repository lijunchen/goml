impl PartialEq for dyn Hash {
    fn eq(self: dyn Hash, other: dyn Hash) -> bool {
        Hash::hash(self) == Hash::hash(other)
    }
}

impl Eq for dyn Hash {}

fn main() -> unit {
    let h: HashMap[Ref[dyn Hash], string] = HashMap::new();
    let inner: dyn Hash = 1;
    let r = Ref::new(inner);
    h.set(r, "one");
    println(h.contains(r).to_string());
}
