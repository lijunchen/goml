trait A {
    fn f(self: Self) -> i32;
}

trait A_vtable {
    fn g(self: Self) -> i32;
}

struct S {}

impl A for S {
    fn f(self: S) -> i32 {
        3
    }
}

impl A_vtable for S {
    fn g(self: S) -> i32 {
        4
    }
}

fn main() -> () {
    let left: dyn A = S {} as dyn A;
    let right: dyn A_vtable = S {} as dyn A_vtable;
    println(A::f(left) + A_vtable::g(right));
}
