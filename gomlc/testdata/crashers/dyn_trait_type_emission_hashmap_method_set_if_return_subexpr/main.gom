trait Display {
    fn show(self: Self) -> string;
}

impl Display for i32 {
    fn show(self: i32) -> string {
        self.to_string()
    }
}

fn build() -> i32 {
    let m: HashMap[i32, dyn Display] = HashMap::new();
    let value: i32 = 1;
    m.set(1, if true { return 7 } else { value as dyn Display });
    0
}

fn main() -> unit {
    println(build().to_string())
}
