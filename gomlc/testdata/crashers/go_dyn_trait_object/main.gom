trait C {
    fn call(self: Self) -> ();
}

impl C for () -> () {
    fn call(self: () -> ()) -> () {
        self()
    }
}

fn main() -> () {
    let c: dyn C = (|| ()) as dyn C;
    go c;
    ()
}
