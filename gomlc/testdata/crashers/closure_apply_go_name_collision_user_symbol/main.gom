struct GomlInherentClosureEnvF0ClosureEnvF0Apply {
    value: i32,
}

fn main() -> unit {
    let y = 1;
    let f = |x: i32| x + y;
    let c = GomlInherentClosureEnvF0ClosureEnvF0Apply {
        value: 2,
    };
    println((f(3) + c.value).to_string())
}
