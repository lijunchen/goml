struct Tuple2_int32_int32 {
    first: i32,
    second: i32,
}

fn main() -> () {
    let t = (1, 2);
    let u = Tuple2_int32_int32 {
        first: 3,
        second: 4,
    };
    println((t.0 + t.1 + u.first + u.second).to_string())
}
