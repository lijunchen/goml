fn side(label: string, value: bool) -> bool {
    println(label);
    value
}

fn main() -> () {
    println((side("a", false) && side("b", true)).to_string());
    println((side("c", true) || side("d", false)).to_string());
}
