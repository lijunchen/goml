trait Show {
    fn show(self: Self) -> string;
}

impl Show for i32 {
    fn show(self: i32) -> string {
        self.to_string()
    }
}

impl Hash for Ref[dyn Show] {
    fn hash(self: Ref[dyn Show]) -> u64 {
        string_hash(Show::show(self.get()))
    }
}

fn main() -> unit {
    let inner: dyn Show = 1 as dyn Show;
    let r = Ref::new(inner);
    let _ = Hash::hash(r);
}
