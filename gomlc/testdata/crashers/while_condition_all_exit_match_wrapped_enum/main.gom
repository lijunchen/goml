enum WrapBool {
    Value(bool),
}

fn read_wrap(w: WrapBool) -> bool {
    match w {
        WrapBool::Value(value) => value,
    }
}

fn main() -> () {
    while read_wrap(WrapBool::Value(match 0 {
        0 => break,
        _ => continue,
    })) {
        break;
    };
}
