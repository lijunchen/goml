enum Weird[T] {
    Next(Ref[Weird[(i32) -> i32]]),
    Done(T),
}

fn main() -> unit {
    let x: Weird[i32] = Weird::Done(1);
    match x {
        Weird::Done(v) => println(v.to_string()),
        Weird::Next(_) => println("next"),
    }
}
