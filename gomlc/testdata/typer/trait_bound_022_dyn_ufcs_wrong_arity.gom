trait Display {
    fn show(self: Self) -> string;
}

impl Display for i32 {
    fn show(self: i32) -> string {
        self.to_string()
    }
}

fn main() {
    let d: dyn Display = 1 as dyn Display;
    let _ = Display::show(d, d);
    ()
}
