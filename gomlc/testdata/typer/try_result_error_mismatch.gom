fn maybe(flag: bool) -> Result[i32, string] {
    if flag {
        Result::Ok(1)
    } else {
        Result::Err("bad")
    }
}

fn bad(flag: bool) -> Result[i32, i32] {
    let value = maybe(flag)?;
    Result::Ok(value)
}
