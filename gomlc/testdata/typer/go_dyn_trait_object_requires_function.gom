trait C {
    fn call(self: Self) -> unit;
}

impl C for () -> unit {
    fn call(self: () -> unit) -> unit {
        self()
    }
}

fn main() -> unit {
    let c: dyn C = (|| ()) as dyn C;
    go c;
    ()
}
