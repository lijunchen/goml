fn maybe(flag: bool) -> Option[i32] {
    if flag {
        Option::Some(1)
    } else {
        Option::None
    }
}

fn bad(flag: bool) -> Result[i32, string] {
    let value = maybe(flag)?;
    Result::Ok(value)
}
