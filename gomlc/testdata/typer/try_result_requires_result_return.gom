fn maybe(flag: bool) -> Result[i32, string] {
    if flag {
        Result::Ok(1)
    } else {
        Result::Err("bad")
    }
}

fn bad(flag: bool) -> Option[i32] {
    let value = maybe(flag)?;
    Option::Some(value)
}
