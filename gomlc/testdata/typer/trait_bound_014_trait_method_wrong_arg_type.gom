trait Scale {
    fn scale(self: Self, arg1: i32) -> Self;
}

impl Scale for i32 {
    fn scale(self: i32, factor: i32) -> i32 {
        self * factor
    }
}

fn main() {
    let _ = Scale::scale(3, true);
    ()
}
