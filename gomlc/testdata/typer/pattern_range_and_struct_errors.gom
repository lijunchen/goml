struct Point {
    x: i32,
    y: i32,
}

fn main() -> unit {
    let Point { x } = Point { x: 1, y: 2 };
    let Point { x: first, x: second, .. } = Point { x: 1, y: 2 };
    let Point { z, .. } = Point { x: 1, y: 2 };
    let _ = match x + first {
        5..5 => 0,
        _ => 0,
    };
}
