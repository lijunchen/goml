trait Pick {
    fn pick(self: Self) -> i32;
}

impl Pick for i32 {
    fn pick(self: i32) -> i32 {
        1
    }
}

impl[T] Pick for T {
    fn pick(self: T) -> i32 {
        2
    }
}

fn main() {
    let _ = Pick::pick(1);
    ()
}
