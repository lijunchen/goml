trait Add {
    fn add(self: Self, arg1: Self) -> Self;
}

impl Add for i32 {
    fn add(self: i32, other: i32) -> i32 {
        self + other
    }
}

fn main() {
    let _ = Add::add(true, false);
    ()
}
