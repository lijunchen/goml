trait Add {
    fn add(self: Self, arg1: Self) -> Self;
}

impl Add for int32 {
    fn add(self: int32, other: int32) -> int32 {
        self + other
    }
}

fn main() {
    let _ = Add::add(true, false);
    ()
}
