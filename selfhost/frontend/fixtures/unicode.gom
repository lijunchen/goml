package unicode_fixture;

fn greeting() -> string {
    "你好，GoML 😀"
}

fn symbol() -> char {
    '界'
}

fn main() {
    // Unicode before a diagnostic keeps byte spans stable: λ变量🙂
    println(greeting());
    println(symbol())
}
