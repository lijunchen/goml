package ast;

use std::json;

#[derive(PartialEq, Eq, ToString)]
pub enum Kind {
    Comment,
    CommentGroup,
    Field,
    FieldList,
    BadExpr,
    Ident,
    Ellipsis,
    BasicLit,
    FuncLit,
    CompositeLit,
    ParenExpr,
    SelectorExpr,
    IndexExpr,
    IndexListExpr,
    SliceExpr,
    TypeAssertExpr,
    CallExpr,
    StarExpr,
    UnaryExpr,
    BinaryExpr,
    KeyValueExpr,
    ArrayType,
    StructType,
    FuncType,
    InterfaceType,
    MapType,
    ChanType,
    BadStmt,
    DeclStmt,
    EmptyStmt,
    LabeledStmt,
    ExprStmt,
    SendStmt,
    IncDecStmt,
    AssignStmt,
    GoStmt,
    DeferStmt,
    ReturnStmt,
    BranchStmt,
    BlockStmt,
    IfStmt,
    CaseClause,
    SwitchStmt,
    TypeSwitchStmt,
    CommClause,
    SelectStmt,
    ForStmt,
    RangeStmt,
    ImportSpec,
    ValueSpec,
    TypeSpec,
    BadDecl,
    GenDecl,
    FuncDecl,
    File,
}

pub struct Span {
    pub start: int,
    pub end: int,
}

pub struct Node {
    pub kind: Kind,
    pub span: Span,
    pub fields: Vec[(string, Value)],
}

pub enum Value {
    Null,
    Node(Node),
    Nodes(Vec[Node]),
    String(string),
    Int(int),
    Bool(bool),
}

pub struct FieldLookup {
    pub value: Value,
    pub present: bool,
}

pub fn span(start: int, end: int) -> Span {
    Span { start, end }
}

pub fn node(kind: Kind, start: int, end: int, fields: Vec[(string, Value)]) -> Node {
    Node { kind, span: span(start, end), fields }
}

pub fn kind_equal(left: Kind, right: Kind) -> bool {
    left == right
}

pub fn kind_name(kind: Kind) -> string {
    let rendered = kind.to_string();
    rendered.byte_slice(6, rendered.byte_len())
}

pub fn null() -> Value {
    Value::Null
}

pub fn node_value(value: Node) -> Value {
    Value::Node(value)
}

pub fn optional_node(value: Option[Node]) -> Value {
    match value {
        Some(value) => Value::Node(value),
        None => Value::Null,
    }
}

pub fn nodes(values: Vec[Node]) -> Value {
    Value::Nodes(values)
}

pub fn text_value(value: string) -> Value {
    Value::String(value)
}

pub fn int_value(value: int) -> Value {
    Value::Int(value)
}

pub fn bool_value(value: bool) -> Value {
    Value::Bool(value)
}

pub fn field(name: string, value: Value) -> (string, Value) {
    (name, value)
}

pub fn get_field(value: Node, name: string) -> FieldLookup {
    for entry in value.fields {
        if entry.0 == name {
            return FieldLookup { value: entry.1, present: true }
        } else {}
    }
    FieldLookup { value: Value::Null, present: false }
}

pub fn get_node(value: Node, name: string) -> Option[Node] {
    let field = get_field(value, name);
    if !field.present {
        return Option::None
    } else {};
    match field.value {
        Value::Node(node) => Option::Some(node),
        _ => Option::None,
    }
}

pub fn get_nodes(value: Node, name: string) -> Vec[Node] {
    let field = get_field(value, name);
    if !field.present {
        return Vec::from_array([])
    } else {};
    match field.value {
        Value::Nodes(nodes) => nodes,
        _ => Vec::from_array([]),
    }
}

pub fn get_string(value: Node, name: string) -> Option[string] {
    let field = get_field(value, name);
    if !field.present {
        return Option::None
    } else {};
    match field.value {
        Value::String(text) => Option::Some(text),
        _ => Option::None,
    }
}

fn value_to_json(value: Value) -> json::Value {
    match value {
        Null => json::Value::Null,
        Node(node) => to_json(node),
        Nodes(nodes) => {
            let values: Vec[json::Value] = Vec::with_capacity(nodes.len());
            for node in nodes {
                values.push(to_json(node))
            }
            json::Value::Array(values)
        },
        String(text) => json::Value::String(text),
        Int(number) => json::Value::Number(number.to_string()),
        Bool(value) => json::Value::Bool(value),
    }
}

pub fn to_json(value: Node) -> json::Value {
    let fields: Vec[(string, json::Value)] = Vec::with_capacity(value.fields.len());
    for entry in value.fields {
        fields.push((entry.0, value_to_json(entry.1)))
    }
    json::Value::Object(
        Vec::from_array(
            [
                ("kind", json::Value::String(kind_name(value.kind))),
                ("start", json::Value::Number(value.span.start.to_string())),
                ("end", json::Value::Number(value.span.end.to_string())),
                ("fields", json::Value::Object(fields)),
            ],
        ),
    )
}

pub fn encode(value: Node) -> string {
    json::encode(to_json(value))
}
