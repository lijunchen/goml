package compiler_test;

#[test]
fn parser_diagnostic_snapshots_match() -> unit {
    assert_diagnostic_suite("diagnostics", 4)
}

#[test]
fn typer_diagnostic_snapshots_match() -> unit {
    assert_diagnostic_suite("typer", 84)
}
