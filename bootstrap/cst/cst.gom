package cst;

use gomlc::lexer;
use std::text;

#[derive(Eq, ToString)]
pub enum NodeKind {
    TombStone,
    ErrorTree,
    EXTERN,
    PACKAGE,
    USE,
    FN,
    ENUM,
    TRAIT,
    TRAIT_METHOD_SIG_LIST,
    TRAIT_METHOD_SIG,
    TRAIT_ASSOCIATED_TYPE,
    IMPL,
    IMPL_ASSOCIATED_TYPE,
    STRUCT,
    STRUCT_FIELD_LIST,
    STRUCT_FIELD,
    TYPE_LIST,
    TYPE_UNIT,
    TYPE_BOOL,
    TYPE_INT8,
    TYPE_INT16,
    TYPE_INT32,
    TYPE_INT64,
    TYPE_UINT8,
    TYPE_UINT16,
    TYPE_UINT32,
    TYPE_UINT64,
    TYPE_FLOAT32,
    TYPE_FLOAT64,
    TYPE_STRING,
    TYPE_CHAR,
    TYPE_TUPLE,
    TYPE_TAPP,
    TYPE_DYN,
    TYPE_ARRAY,
    TYPE_FUNC,
    EXPR_LITERAL,
    EXPR_IDENT,
    PATH,
    EXPR_PAREN,
    EXPR_BINARY,
    EXPR_PREFIX,
    EXPR_CAST,
    EXPR_IF,
    EXPR_IF_COND,
    EXPR_IF_THEN,
    EXPR_IF_ELSE,
    EXPR_WHILE,
    EXPR_WHILE_COND,
    EXPR_WHILE_BODY,
    EXPR_FOR,
    EXPR_BREAK,
    EXPR_CONTINUE,
    EXPR_MATCH,
    EXPR_GO,
    EXPR_RETURN,
    EXPR_UNIT,
    EXPR_BOOL,
    EXPR_INT,
    EXPR_INT8,
    EXPR_INT16,
    EXPR_INT32,
    EXPR_INT64,
    EXPR_UINT8,
    EXPR_UINT16,
    EXPR_UINT32,
    EXPR_UINT64,
    EXPR_FLOAT,
    EXPR_FLOAT32,
    EXPR_FLOAT64,
    EXPR_CHAR,
    EXPR_STR,
    EXPR_MULTILINE_STR,
    EXPR_ARRAY_LITERAL,
    EXPR_TUPLE,
    EXPR_CALL,
    EXPR_INDEX,
    EXPR_TRY,
    EXPR_STRUCT_LITERAL,
    ARG_LIST,
    ARG,
    STRUCT_LITERAL_FIELD_LIST,
    STRUCT_LITERAL_FIELD,
    EXPR_CLOSURE,
    EXPR_CLOSURE_BODY,
    CLOSURE_PARAM_LIST,
    CLOSURE_PARAM,
    STMT_LET,
    STMT_ASSIGN,
    STMT_EXPR,
    STRUCT_PATTERN_FIELD_LIST,
    STRUCT_PATTERN_FIELD,
    VARIANT_LIST,
    VARIANT,
    PATTERN,
    PATTERN_UNIT,
    PATTERN_BOOL,
    PATTERN_STRING,
    PATTERN_INT,
    PATTERN_INT8,
    PATTERN_INT16,
    PATTERN_INT32,
    PATTERN_INT64,
    PATTERN_UINT8,
    PATTERN_UINT16,
    PATTERN_UINT32,
    PATTERN_UINT64,
    PATTERN_FLOAT,
    PATTERN_FLOAT32,
    PATTERN_FLOAT64,
    PATTERN_CHAR,
    PATTERN_TUPLE,
    PATTERN_ARRAY,
    PATTERN_CONSTR,
    PATTERN_VARIABLE,
    PATTERN_WILDCARD,
    PATTERN_ALIAS,
    PATTERN_OR,
    PATTERN_RANGE,
    PATTERN_REST,
    MATCH_ARM_LIST,
    MATCH_ARM,
    MATCH_ARM_GUARD,
    PARAM,
    PARAM_LIST,
    GENERIC,
    GENERIC_LIST,
    TRAIT_SET,
    WHERE_CLAUSE,
    WHERE_PREDICATE,
    TYPE_PARAM_LIST,
    BLOCK,
    FILE,
    ATTRIBUTE_LIST,
    ATTRIBUTE,
}

pub struct CstNode {
    pub kind: NodeKind,
    pub children: Vec[CstElement],
    pub byte_len: int32,
}

pub enum CstElement {
    Node(CstNode),
    Token(lexer::Token),
}

pub struct Diagnostic {
    pub message: string,
    pub start: int32,
    pub end: int32,
}

pub struct ParseResult {
    pub root: CstNode,
    pub diagnostics: Vec[Diagnostic],
}

pub fn node(kind: NodeKind, children: Vec[CstElement], byte_len: int32) -> CstNode {
    CstNode { kind, children, byte_len }
}

pub fn diagnostic(message: string, start: int32, end: int32) -> Diagnostic {
    Diagnostic { message, start, end }
}

pub fn parse_result(root: CstNode, diagnostics: Vec[Diagnostic]) -> ParseResult {
    ParseResult { root, diagnostics }
}

pub fn node_kind(value: CstNode) -> NodeKind {
    value.kind
}

pub fn node_children(value: CstNode) -> Vec[CstElement] {
    value.children
}

pub fn node_byte_len(value: CstNode) -> int32 {
    value.byte_len
}

pub fn element_byte_len(value: CstElement) -> int32 {
    match value {
        CstElement::Node(node_value) => node_value.byte_len,
        CstElement::Token(token) => token.text.byte_len(),
    }
}

pub fn node_kind_name(kind: NodeKind) -> string {
    let rendered = kind.to_string();
    rendered.byte_slice(10, rendered.byte_len())
}

pub fn node_kind_equal(left: NodeKind, right: NodeKind) -> bool {
    Eq::eq(left, right)
}

fn debug_write_spaces(builder: text::StringBuilder, count: int32) -> unit {
    for _ in range(0, count) {
        builder.write_string("  ")
    }
}

fn debug_utf8_width(value: uint8) -> int32 {
    if value < 128u8 {
        1
    } else if value < 224u8 {
        2
    } else if value < 240u8 {
        3
    } else {
        4
    }
}

fn debug_hex_digit(value: uint8) -> char {
    match value {
        0u8 => '0',
        1u8 => '1',
        2u8 => '2',
        3u8 => '3',
        4u8 => '4',
        5u8 => '5',
        6u8 => '6',
        7u8 => '7',
        8u8 => '8',
        9u8 => '9',
        10u8 => 'a',
        11u8 => 'b',
        12u8 => 'c',
        13u8 => 'd',
        14u8 => 'e',
        _ => 'f',
    }
}

fn debug_write_control_escape(builder: text::StringBuilder, value: uint8) -> unit {
    builder.write_string("\\u{");
    if value >= 16u8 {
        builder.write_char(debug_hex_digit(value / 16u8))
    } else {
        ()
    };
    builder.write_char(debug_hex_digit(value % 16u8));
    builder.write_char('}')
}

fn debug_write_quoted_string(builder: text::StringBuilder, value: string) -> unit {
    builder.write_char('"');
    let index = Ref::new(0);
    while index.get() < value.byte_len() {
        let byte = value.byte_get(index.get());
        if byte == 34u8 {
            builder.write_string("\\\"")
        } else if byte == 92u8 {
            builder.write_string("\\\\")
        } else if byte == 10u8 {
            builder.write_string("\\n")
        } else if byte == 13u8 {
            builder.write_string("\\r")
        } else if byte == 9u8 {
            builder.write_string("\\t")
        } else if byte == 0u8 {
            builder.write_string("\\0")
        } else if byte < 32u8 || byte == 127u8 {
            debug_write_control_escape(builder, byte)
        } else {
            let width = debug_utf8_width(byte);
            builder.write_string(value.byte_slice(index.get(), index.get() + width))
        };
        index.set(index.get() + debug_utf8_width(byte))
    };
    builder.write_char('"')
}

fn debug_token_text(value: string) -> string {
    if value.byte_len() < 25 {
        value
    } else {
        let boundary = Ref::new(21);
        while !value.is_char_boundary(boundary.get()) {
            boundary.set(boundary.get() + 1)
        };
        value.byte_slice(0, boundary.get()) + " ..."
    }
}

fn debug_write_node(
    value: CstNode,
    start: int32,
    depth: int32,
    builder: text::StringBuilder,
) -> int32 {
    let end = start + value.byte_len;
    debug_write_spaces(builder, depth);
    builder.write_string(node_kind_name(value.kind));
    builder.write_char('@');
    builder.write_string(start.to_string());
    builder.write_string("..");
    builder.write_string(end.to_string());
    builder.write_char('\n');
    let cursor = Ref::new(start);
    for child in value.children {
        match child {
            CstElement::Node(child_node) => cursor.set(debug_write_node(
                child_node,
                cursor.get(),
                depth + 1,
                builder,
            )),
            CstElement::Token(token) => {
                let token_end = cursor.get() + token.text.byte_len();
                debug_write_spaces(builder, depth + 1);
                builder.write_string(lexer::token_kind_name(token.kind));
                builder.write_char('@');
                builder.write_string(cursor.get().to_string());
                builder.write_string("..");
                builder.write_string(token_end.to_string());
                builder.write_char(' ');
                debug_write_quoted_string(builder, debug_token_text(token.text));
                builder.write_char('\n');
                cursor.set(token_end)
            },
        }
    };
    end
}

pub fn debug_tree(result: ParseResult) -> string {
    let builder = text::StringBuilder::new();
    let _ = debug_write_node(result.root, 0, 0, builder);
    let rendered = builder.finish();
    rendered.byte_slice(0, rendered.byte_len() - 1)
}

fn encode_node(value: CstNode, start: int32, builder: text::StringBuilder) -> int32 {
    let end = start + value.byte_len;
    builder.write_string("N\t");
    builder.write_string(node_kind_name(value.kind));
    builder.write_string("\t");
    builder.write_string(start.to_string());
    builder.write_string("\t");
    builder.write_string(end.to_string());
    builder.write_string("\n");
    let cursor = Ref::new(start);
    for child in value.children {
        match child {
            CstElement::Node(child_node) => cursor.set(encode_node(child_node, cursor.get(), builder)),
            CstElement::Token(token) => {
                builder.write_string("T\t");
                builder.write_string(lexer::token_kind_name(token.kind));
                builder.write_string("\t");
                builder.write_string(cursor.get().to_string());
                builder.write_string("\t");
                builder.write_string((cursor.get() + token.text.byte_len()).to_string());
                builder.write_string("\t");
                builder.write_string(lexer::encode_text_hex(token.text));
                builder.write_string("\n");
                cursor.set(cursor.get() + token.text.byte_len())
            },
        }
    };
    builder.write_string("E\n");
    end
}

pub fn encode(result: ParseResult) -> string {
    let builder = text::StringBuilder::new();
    let _ = encode_node(result.root, 0, builder);
    for item in result.diagnostics {
        builder.write_string("D\t");
        builder.write_string(item.start.to_string());
        builder.write_string("\t");
        builder.write_string(item.end.to_string());
        builder.write_string("\t");
        builder.write_string(lexer::encode_text_hex(item.message));
        builder.write_string("\n")
    };
    builder.finish()
}
