trait Mark {
    fn mark(self: Self) -> unit;
}

trait Source {
    type Item: Mark;
    fn get(self: Self) -> Self::Item;
}

struct Value {}
struct Item {}

impl Source for Value {
    type Item = Item;
    fn get(self: Value) -> Item { Item {} }
}
