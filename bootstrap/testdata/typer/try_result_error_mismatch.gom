fn maybe(flag: bool) -> Result[int32, string] {
    if flag {
        Result::Ok(1)
    } else {
        Result::Err("bad")
    }
}

fn bad(flag: bool) -> Result[int32, int32] {
    let value = maybe(flag)?;
    Result::Ok(value)
}
