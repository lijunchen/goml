enum Status {
  Active,
  Inactive,
  Pending,
}

fn main() -> unit {
  let code: int32 = 1;
  match code {
    Status::Active => println("active"),
    Status::Inactive => println("inactive"),
    _ => println("unknown"),
  }
}
