fn apply[T](f: (T) -> T, x: T) -> T {
    f(x)
}

fn main() {
    let result = apply(|x| x + 1, 10);
    println(result.to_string());
    
    let result2 = apply(|s| s + " world", "hello");
    println(result2);
}
