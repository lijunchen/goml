fn main() {
    let i = Ref::new(0);
    while true {
        let v = i.get();
        match v {
            0 => println("zero"),
            1 => println("one"),
            _ => break,
        };
        i.set(i.get() + 1);
    };
    println("done");
}
