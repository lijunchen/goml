trait Describable {
    fn describe(self: Self) -> string;
}

struct Pair[A, B] {
    first: A,
    second: B,
}

impl Describable for Pair[int32, string] {
    fn describe(self: Pair[int32, string]) -> string {
        "pair(" + self.first.to_string() + ", " + self.second + ")"
    }
}

fn main() {
    let p: Pair[int32, string] = Pair { first: 42, second: "hello" };
    println(Describable::describe(p));
}
