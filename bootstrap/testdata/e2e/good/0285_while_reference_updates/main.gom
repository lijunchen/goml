fn main() {
    let sum = Ref::new(0);
    let i = Ref::new(1);
    while i.get() <= 100 {
        sum.set(sum.get() + i.get());
        i.set(i.get() + 1);
    };
    println(sum.get().to_string());
}
