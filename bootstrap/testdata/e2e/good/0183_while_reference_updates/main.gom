fn main() {
    let sum = Ref::new(0);
    let i = Ref::new(1);
    while i.get() <= 10 {
        let v = i.get();
        let s = sum.get();
        sum.set(s + v);
        i.set(v + 1);
    };
    println(sum.get().to_string());
}
