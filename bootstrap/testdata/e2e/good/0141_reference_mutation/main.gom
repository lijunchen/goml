fn main() {
    let x: Ref[int32] = Ref::new(42);
    let y: Ref[int32] = x;
    y.set(100);
    println(x.get().to_string());
    println(y.get().to_string());
    println((x.get() == y.get()).to_string());
}
