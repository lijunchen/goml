struct State {
    count: int32,
    name: string,
}

fn main() {
    let s = Ref::new(State { count: 0, name: "init" });
    println(s.get().count.to_string() + " " + s.get().name);
    s.set(State { count: 1, name: "updated" });
    println(s.get().count.to_string() + " " + s.get().name);
}
