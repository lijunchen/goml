#[derive(ToString)]
enum Color {
    Red,
    Green,
    Blue,
    Custom(int32, int32, int32),
}

fn main() {
    let c = Color::Red;
    println(c.to_string());
    let c2 = Color::Custom(255, 128, 0);
    println(c2.to_string());
}
