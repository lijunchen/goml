fn main() {
    let i = Ref::new(0);
    let sum = Ref::new(0);
    while i.get() < 10 {
        sum.set(sum.get() + i.get());
        i.set(i.get() + 1);
    };
    println(sum.get().to_string());
}
