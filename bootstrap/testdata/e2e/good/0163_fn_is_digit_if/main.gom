fn is_digit(c: char) -> bool {
    c >= '0' && c <= '9'
}

fn main() {
    let a = 'A';
    let z = '0';
    println(a.to_string());
    println(z.to_string());
    if is_digit('5') {
        println("5 is digit");
    } else {
        println("5 is not digit");
    };
    if is_digit('x') {
        println("x is digit");
    } else {
        println("x is not digit");
    };
}
