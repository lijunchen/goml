fn maybe_print(cond: bool, msg: string) {
    if cond {
        println(msg);
    } else {
        ()
    };
}

fn main() {
    maybe_print(true, "yes");
    maybe_print(false, "no");
    println("done");
}
