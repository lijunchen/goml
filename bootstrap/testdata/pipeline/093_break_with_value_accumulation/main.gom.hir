package main
file main.gom
  fn main() {
        let local/1/0 = Ref::new(0);
        let local/1/1 = Ref::new(0);
        while i/1.get() < 20 {
            {
                i/1.set(i/1.get() + 1);
                if i/1.get() > 5 {
                    break
                } else {
                    ()
                };
                sum/0.set(sum/0.get() + i/1.get());
            }
        };
        let _ = print("sum: ");
        let _ = println(sum/0.get());
        let _ = print("i at break: ");
        let _ = println(i/1.get());
    }