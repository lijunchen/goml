package main
file main.gom
  fn parse_text(local/1/0: bool) -> Result[string, string] {
        if ok/0 {
            Result::Ok("goml")
        } else {
            Result::Err("parse failed")
        }
    }
    
    fn normalize_text(local/1/1: bool) -> Result[string, string] {
        let local/1/2 = def/1/0(ok/1)?;
        Result::Ok(text/2 + "!")
    }
    
    fn decorate_text(local/1/3: bool) -> Result[string, string] {
        let local/1/4 = def/1/1(ok/3)?;
        Result::Ok("[" + text/4 + "]")
    }
    
    fn show(local/1/5: Result[string, string]) -> string {
        match res/5 {
            Result::Ok(local/1/6) => "ok " + value/6,
            Result::Err(local/1/7) => "err " + err/7,
        }
    }
    
    fn main() -> unit {
        println(def/1/3(def/1/2(true)));
        println(def/1/3(def/1/2(false)));
    }