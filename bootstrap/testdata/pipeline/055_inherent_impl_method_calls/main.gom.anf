fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  join ret66(retv67: string) -> string {
    retv67
  } in
  jump ret66("Shape")
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join ret68(retv69: string) -> string {
    retv69
  } in
  let t70 = inherent#Shape#Shape#name(self/1) in
  let t71 = (t70 + suffix/2) in
  jump ret68(t71)
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join ret72(retv73: string) -> string {
    retv73
  } in
  let t74 = inherent#Shape#Shape#name(self/3) in
  let t75 = (left/4 + t74) in
  let t76 = (t75 + right/5) in
  jump ret72(t76)
}

fn announce(shape/6: Shape) -> unit {
  join ret77() -> unit {
    ()
  } in
  let base/7 = inherent#Shape#Shape#name(shape/6) in
  let with_suffix/8 = inherent#Shape#Shape#rename(shape/6, "!") in
  let combined/9 = inherent#Shape#Shape#join(shape/6, base/7, with_suffix/8) in
  let mtmp64 = println__T_string(combined/9) in
  jump ret77()
}

fn main() -> unit {
  join ret78() -> unit {
    ()
  } in
  let shape/10 = Shape {} in
  let _wild65 = announce(shape/10) in
  jump ret78()
}

fn println__T_string(value/1: string) -> unit {
  join ret79() -> unit {
    ()
  } in
  let t80 = trait_impl#ToString#string#to_string(value/1) in
  let t81 = @runtime(core.string_println)(t80) in
  jump ret79()
}

fn trait_impl#ToString#string#to_string(self/38: string) -> string {
  join ret82(retv83: string) -> string {
    retv83
  } in
  jump ret82(self/38)
}