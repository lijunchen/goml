fn color_is_red(color/0: Color) -> bool {
  join ret65(retv66: bool) -> bool {
    retv66
  } in
  join k67(jp68: bool) -> bool {
    jump ret65(jp68)
  } in
  match color/0 {
    Tag_0 => {
      jump k67(true)
    },
    Tag_1 => {
      jump k67(false)
    },
  }
}

fn toggle_signal(signal/1: Signal) -> Signal {
  join ret69(retv70: Signal) -> Signal {
    retv70
  } in
  join k71(jp72: Signal) -> Signal {
    jump ret69(jp72)
  } in
  match signal/1 {
    Tag_0 => {
      jump k71(Tag_1)
    },
    Tag_1 => {
      jump k71(Tag_0)
    },
  }
}

fn main() -> Signal {
  join ret73(retv74: Signal) -> Signal {
    retv74
  } in
  let current/2 = Tag_0 in
  let mtmp64 = color_is_red(current/2) in
  let t75 = toggle_signal(Tag_0) in
  jump ret73(t75)
}