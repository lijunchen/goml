fn child(signal/0: Ref[int32]) -> unit {
    (Ref[int32]::set : (Ref[int32], int32) -> unit)((signal/0 : Ref[int32]), 1)
}

fn main() -> unit {
    let signal/1: Ref[int32] = (Ref[int32]::new : (int32) -> Ref[int32])(0);
    go || => {
        (child : (Ref[int32]) -> unit)((signal/1 : Ref[int32]))
    };
    while (Ref[int32]::get : (Ref[int32]) -> int32)((signal/1 : Ref[int32])) < 1 {
        {
            ()
        }
    };
    (println : (string) -> unit)("main");
}