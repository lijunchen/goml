fn main() -> unit {
  join ret79() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let total/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit80() -> unit {
    let _wild69 = () in
    let t81 = inherent#Ref#Ref[T]#get__T_int(total/1) in
    let _wild70 = println__T_int(t81) in
    let j/2 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total2/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit82() -> unit {
      let _wild77 = () in
      let t83 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
      let _wild78 = println__T_int(t83) in
      jump ret79()
    } in
    joinrec {
      join loop84() -> unit {
        let mtmp71 = inherent#Ref#Ref[T]#get__T_int(j/2) in
        join k85(jp86: bool) -> unit {
          if jp86 {
            let t87 = inherent#Ref#Ref[T]#get__T_int(total2/3) in
            let t88 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t89 = (t87 + t88) in
            let _wild74 = inherent#Ref#Ref[T]#set__T_int(total2/3, t89) in
            let t91 = inherent#Ref#Ref[T]#get__T_int(j/2) in
            let t92 = trait_impl#Eq#int#eq(t91, 2) in
            join k90() -> unit {
              let _wild76 = () in
              jump loop84()
            } in
            if t92 {
              jump exit82()
            } else {
              jump k90()
            }
          } else {
            jump exit82()
          }
        } in
        match mtmp71 {
          0 => {
            let _wild72 = inherent#Ref#Ref[T]#set__T_int(j/2, 1) in
            jump k85(true)
          },
          1 => {
            let _wild73 = inherent#Ref#Ref[T]#set__T_int(j/2, 2) in
            jump k85(true)
          },
          2 => {
            jump k85(true)
          },
          _ => {
            jump k85(false)
          },
        }
      } in
    } in
    jump loop84()
  } in
  joinrec {
    join loop93() -> unit {
      let t104 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t105 = trait_impl#Eq#int#eq(t104, 0) in
      join k94(jp95: bool) -> unit {
        if jp95 {
          let t96 = inherent#Ref#Ref[T]#get__T_int(total/1) in
          let t97 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t98 = (t96 + t97) in
          let _wild65 = inherent#Ref#Ref[T]#set__T_int(total/1, t98) in
          let t102 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          let t103 = trait_impl#Eq#int#eq(t102, 1) in
          join k99() -> unit {
            let _wild67 = () in
            let t100 = inherent#Ref#Ref[T]#get__T_int(i/0) in
            let t101 = (t100 + 1) in
            let _wild68 = inherent#Ref#Ref[T]#set__T_int(i/0, t101) in
            jump loop93()
          } in
          if t103 {
            let _wild66 = inherent#Ref#Ref[T]#set__T_int(i/0, 2) in
            jump loop93()
          } else {
            jump k99()
          }
        } else {
          jump exit80()
        }
      } in
      if t105 {
        let _wild64 = inherent#Ref#Ref[T]#set__T_int(i/0, 1) in
        jump k94(true)
      } else {
        let t108 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t109 = (t108 < 4) in
        join k106(jp107: bool) -> unit {
          jump k94(jp107)
        } in
        if t109 {
          jump k106(true)
        } else {
          jump k106(false)
        }
      }
    } in
  } in
  jump loop93()
}

fn inherent#Ref#Ref[T]#new__T_int(value/209: int) -> Ref[int] {
  join ret110(retv111: Ref[int]) -> Ref[int] {
    retv111
  } in
  let t112 = @intrinsic(ref.new)(value/209) in
  jump ret110(t112)
}

fn inherent#Ref#Ref[T]#get__T_int(self/210: Ref[int]) -> int {
  join ret113(retv114: int) -> int {
    retv114
  } in
  let t115 = @intrinsic(ref.get)(self/210) in
  jump ret113(t115)
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret116(retv117: bool) -> bool {
    retv117
  } in
  let t118 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret116(t118)
}

fn inherent#Ref#Ref[T]#set__T_int(self/211: Ref[int], value/212: int) -> unit {
  join ret119() -> unit {
    ()
  } in
  let t120 = @intrinsic(ref.set)(self/211, value/212) in
  jump ret119()
}

fn println__T_int(value/1: int) -> unit {
  join ret121() -> unit {
    ()
  } in
  let t122 = trait_impl#ToString#int#to_string(value/1) in
  let t123 = @runtime(core.string_println)(t122) in
  jump ret121()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret124(retv125: string) -> string {
    retv125
  } in
  let t126 = @runtime(core.int_to_string)(self/40) in
  jump ret124(t126)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret127(retv128: uint64) -> uint64 {
    retv128
  } in
  let t129 = @runtime(core.int_hash)(self/85) in
  jump ret127(t129)
}