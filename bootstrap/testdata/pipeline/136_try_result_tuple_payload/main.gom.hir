package main
file main.gom
  fn split_host_port(local/1/0: bool) -> Result[(string, string), string] {
        if ok/0 {
            Result::Ok(("example.com", "443"))
        } else {
            Result::Err("missing port")
        }
    }
    
    fn render(local/1/1: bool) -> Result[string, string] {
        let (local/1/2, local/1/3) = def/1/0(ok/1)?;
        Result::Ok(host/2 + ":" + port/3)
    }
    
    fn show(local/1/4: Result[string, string]) -> string {
        match res/4 {
            Result::Ok(local/1/5) => "ok " + value/5,
            Result::Err(local/1/6) => "err " + err/6,
        }
    }
    
    fn main() -> unit {
        println(def/1/2(def/1/1(true)));
        println(def/1/2(def/1/1(false)));
    }