fn main() -> unit {
  join ret73() -> unit {
    ()
  } in
  let i/0 = inherent#Ref#Ref[T]#new__T_int(0) in
  let sum/1 = inherent#Ref#Ref[T]#new__T_int(0) in
  join exit74() -> unit {
    let _wild66 = () in
    let t75 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
    let _wild67 = println__T_int(t75) in
    let j/3 = inherent#Ref#Ref[T]#new__T_int(0) in
    let total/4 = inherent#Ref#Ref[T]#new__T_int(0) in
    join exit76() -> unit {
      let _wild71 = () in
      let t77 = inherent#Ref#Ref[T]#get__T_int(total/4) in
      let _wild72 = println__T_int(t77) in
      jump ret73()
    } in
    joinrec {
      join loop78() -> unit {
        if true {
          let t79 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          let t80 = (t79 + 1) in
          let _wild68 = inherent#Ref#Ref[T]#set__T_int(j/3, t80) in
          let mtmp69 = inherent#Ref#Ref[T]#get__T_int(j/3) in
          join k81(jp82: int) -> unit {
            let cur/5 = jp82 in
            let t83 = inherent#Ref#Ref[T]#get__T_int(total/4) in
            let t84 = (t83 + cur/5) in
            let _wild70 = inherent#Ref#Ref[T]#set__T_int(total/4, t84) in
            jump loop78()
          } in
          match mtmp69 {
            5 => {
              jump exit76()
            },
            _ => {
              let t85 = inherent#Ref#Ref[T]#get__T_int(j/3) in
              jump k81(t85)
            },
          }
        } else {
          jump exit76()
        }
      } in
    } in
    jump loop78()
  } in
  joinrec {
    join loop86() -> unit {
      let t87 = inherent#Ref#Ref[T]#get__T_int(i/0) in
      let t88 = (t87 < 5) in
      if t88 {
        let t89 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t90 = (t89 + 1) in
        let _wild64 = inherent#Ref#Ref[T]#set__T_int(i/0, t90) in
        let t95 = inherent#Ref#Ref[T]#get__T_int(i/0) in
        let t96 = trait_impl#Eq#int#eq(t95, 3) in
        join k91(jp92: int) -> unit {
          let cur/2 = jp92 in
          let t93 = inherent#Ref#Ref[T]#get__T_int(sum/1) in
          let t94 = (t93 + cur/2) in
          let _wild65 = inherent#Ref#Ref[T]#set__T_int(sum/1, t94) in
          jump loop86()
        } in
        if t96 {
          jump loop86()
        } else {
          let t97 = inherent#Ref#Ref[T]#get__T_int(i/0) in
          jump k91(t97)
        }
      } else {
        jump exit74()
      }
    } in
  } in
  jump loop86()
}

fn inherent#Ref#Ref[T]#new__T_int(value/209: int) -> Ref[int] {
  join ret98(retv99: Ref[int]) -> Ref[int] {
    retv99
  } in
  let t100 = @intrinsic(ref.new)(value/209) in
  jump ret98(t100)
}

fn inherent#Ref#Ref[T]#get__T_int(self/210: Ref[int]) -> int {
  join ret101(retv102: int) -> int {
    retv102
  } in
  let t103 = @intrinsic(ref.get)(self/210) in
  jump ret101(t103)
}

fn inherent#Ref#Ref[T]#set__T_int(self/211: Ref[int], value/212: int) -> unit {
  join ret104() -> unit {
    ()
  } in
  let t105 = @intrinsic(ref.set)(self/211, value/212) in
  jump ret104()
}

fn trait_impl#Eq#int#eq(self/59: int, other/60: int) -> bool {
  join ret106(retv107: bool) -> bool {
    retv107
  } in
  let t108 = @intrinsic(eq.primitive)(self/59, other/60) in
  jump ret106(t108)
}

fn println__T_int(value/1: int) -> unit {
  join ret109() -> unit {
    ()
  } in
  let t110 = trait_impl#ToString#int#to_string(value/1) in
  let t111 = @runtime(core.string_println)(t110) in
  jump ret109()
}

fn trait_impl#ToString#int#to_string(self/40: int) -> string {
  join ret112(retv113: string) -> string {
    retv113
  } in
  let t114 = @runtime(core.int_to_string)(self/40) in
  jump ret112(t114)
}

fn trait_impl#Hash#int#hash(self/85: int) -> uint64 {
  join ret115(retv116: uint64) -> uint64 {
    retv116
  } in
  let t117 = @runtime(core.int_hash)(self/85) in
  jump ret115(t117)
}