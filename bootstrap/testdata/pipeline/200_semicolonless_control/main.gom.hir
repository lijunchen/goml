package main
file main.gom
  fn main() -> unit {
        if true {
            string_println("if")
        } else {
            string_println("else")
        };
        match 1 {
            1 => string_println("match"),
            _ => (),
        };
        let local/1/0 = Ref::new(0);
        while index/0.get() < 2 {
            {
                string_println(index/0.get().to_string());
                index/0.set(index/0.get() + 1)
            }
        };
        let local/1/1: Vec[string] = Vec::new();
        values/1.push("for");
        for local/1/2 in values/1 {
            string_println(value/2)
        };
        string_println("done")
    }