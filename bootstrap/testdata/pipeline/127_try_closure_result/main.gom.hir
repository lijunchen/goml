package main
file main.gom
  fn parse_text(local/1/0: bool) -> Result[string, string] {
        if ok/0 {
            Result::Ok("body")
        } else {
            Result::Err("parse failed")
        }
    }
    
    fn decorate(local/1/1: string, local/1/2: bool) -> Result[string, string] {
        let local/1/4 = || {
            let local/1/3 = def/1/0(ok/2)?;
            Result::Ok(prefix/1 + ":" + text/3)
        };
        run/4()
    }
    
    fn show(local/1/5: Result[string, string]) -> string {
        match res/5 {
            Result::Ok(local/1/6) => "ok " + value/6,
            Result::Err(local/1/7) => "err " + err/7,
        }
    }
    
    fn main() -> unit {
        println(def/1/2(def/1/1("outer", true)));
        println(def/1/2(def/1/1("outer", false)));
    }