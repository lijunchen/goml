package main;
enum Color {
    Red,
    Green,
    Blue
}

fn main() {
    let a = (Blue, Red);
    match a {
        (Red, Green) => print(0),
        (Red, Red) => print(1),
        (Blue, Blue) => print(2),
        _ => print(3),
    }
}

