package cmp;

pub use prelude::{Ordering};

pub trait PartialOrd: PartialEq {
    fn partial_cmp(self: Self, other: Self) -> Option[Ordering];
    fn lt(self: Self, other: Self) -> bool {
        match self.partial_cmp(other) {
            Option::Some(ordering) => ordering == Ordering::Less,
            Option::None => false,
        }
    }
    fn le(self: Self, other: Self) -> bool {
        match self.partial_cmp(other) {
            Option::Some(ordering) => ordering != Ordering::Greater,
            Option::None => false,
        }
    }
    fn gt(self: Self, other: Self) -> bool {
        match self.partial_cmp(other) {
            Option::Some(ordering) => ordering == Ordering::Greater,
            Option::None => false,
        }
    }
    fn ge(self: Self, other: Self) -> bool {
        match self.partial_cmp(other) {
            Option::Some(ordering) => ordering != Ordering::Less,
            Option::None => false,
        }
    }
}

impl PartialOrd for Ordering {
    fn partial_cmp(self: Ordering, other: Ordering) -> Option[Ordering] {
        Option::Some(
            if self == other {
                Ordering::Equal
            } else {
                match self {
                    Ordering::Less => Ordering::Less,
                    Ordering::Equal => if other == Ordering::Greater {
                        Ordering::Less
                    } else {
                        Ordering::Greater
                    },
                    Ordering::Greater => Ordering::Greater,
                }
            },
        )
    }
}

pub trait Ord: Eq + PartialOrd {
    fn cmp(self: Self, other: Self) -> Ordering;
}

impl Ord for Ordering {
    fn cmp(self: Ordering, other: Ordering) -> Ordering {
        if self == other {
            Ordering::Equal
        } else {
            match self {
                Ordering::Less => Ordering::Less,
                Ordering::Equal => if other == Ordering::Greater {
                    Ordering::Less
                } else {
                    Ordering::Greater
                },
                Ordering::Greater => Ordering::Greater,
            }
        }
    }
}

impl Ord for unit {
    fn cmp(self: unit, other: unit) -> Ordering {
        Ordering::Equal
    }
}

impl PartialOrd for unit {
    fn partial_cmp(self: unit, other: unit) -> Option[Ordering] {
        Option::Some(Ordering::Equal)
    }
}

impl Ord for bool {
    fn cmp(self: bool, other: bool) -> Ordering {
        if self == other {
            Ordering::Equal
        } else if self {
            Ordering::Greater
        } else {
            Ordering::Less
        }
    }
}

impl PartialOrd for bool {
    fn partial_cmp(self: bool, other: bool) -> Option[Ordering] {
        Option::Some(
            if self == other {
                Ordering::Equal
            } else if self {
                Ordering::Greater
            } else {
                Ordering::Less
            },
        )
    }
}

impl Ord for string {
    fn cmp(self: string, other: string) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for string {
    fn partial_cmp(self: string, other: string) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for char {
    fn cmp(self: char, other: char) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for char {
    fn partial_cmp(self: char, other: char) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for int {
    fn cmp(self: int, other: int) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for int {
    fn partial_cmp(self: int, other: int) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for int8 {
    fn cmp(self: int8, other: int8) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for int8 {
    fn partial_cmp(self: int8, other: int8) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for int16 {
    fn cmp(self: int16, other: int16) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for int16 {
    fn partial_cmp(self: int16, other: int16) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for int32 {
    fn cmp(self: int32, other: int32) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for int32 {
    fn partial_cmp(self: int32, other: int32) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for int64 {
    fn cmp(self: int64, other: int64) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for int64 {
    fn partial_cmp(self: int64, other: int64) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for uint {
    fn cmp(self: uint, other: uint) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for uint {
    fn partial_cmp(self: uint, other: uint) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for uint8 {
    fn cmp(self: uint8, other: uint8) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for uint8 {
    fn partial_cmp(self: uint8, other: uint8) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for uint16 {
    fn cmp(self: uint16, other: uint16) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for uint16 {
    fn partial_cmp(self: uint16, other: uint16) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for uint32 {
    fn cmp(self: uint32, other: uint32) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for uint32 {
    fn partial_cmp(self: uint32, other: uint32) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl Ord for uint64 {
    fn cmp(self: uint64, other: uint64) -> Ordering {
        if self < other {
            Ordering::Less
        } else if self > other {
            Ordering::Greater
        } else {
            Ordering::Equal
        }
    }
}

impl PartialOrd for uint64 {
    fn partial_cmp(self: uint64, other: uint64) -> Option[Ordering] {
        Option::Some(
            if self < other {
                Ordering::Less
            } else if self > other {
                Ordering::Greater
            } else {
                Ordering::Equal
            },
        )
    }
}

impl PartialOrd for float32 {
    fn partial_cmp(self: float32, other: float32) -> Option[Ordering] {
        if self < other {
            Option::Some(Ordering::Less)
        } else if self > other {
            Option::Some(Ordering::Greater)
        } else if self == other {
            Option::Some(Ordering::Equal)
        } else {
            Option::None
        }
    }
}

impl PartialOrd for float64 {
    fn partial_cmp(self: float64, other: float64) -> Option[Ordering] {
        if self < other {
            Option::Some(Ordering::Less)
        } else if self > other {
            Option::Some(Ordering::Greater)
        } else if self == other {
            Option::Some(Ordering::Equal)
        } else {
            Option::None
        }
    }
}

impl[T: PartialOrd] PartialOrd for Vec[T] {
    fn partial_cmp(self: Vec[T], other: Vec[T]) -> Option[Ordering] {
        let limit = if self.len() < other.len() {
            self.len()
        } else {
            other.len()
        };
        let mut index = 0;
        while index < limit {
            match PartialOrd::partial_cmp(self[index], other[index]) {
                Option::None => return Option::None,
                Option::Some(Ordering::Equal) => {
                    index += 1;
                },
                Option::Some(ordering) => return Option::Some(ordering),
            }
        }
        PartialOrd::partial_cmp(self.len(), other.len())
    }
}

impl[T: Ord] Ord for Vec[T] {
    fn cmp(self: Vec[T], other: Vec[T]) -> Ordering {
        let limit = if self.len() < other.len() {
            self.len()
        } else {
            other.len()
        };
        let mut index = 0;
        while index < limit {
            match Ord::cmp(self[index], other[index]) {
                Ordering::Equal => {
                    index += 1;
                },
                ordering => return ordering,
            }
        }
        Ord::cmp(self.len(), other.len())
    }
}

impl[T: PartialOrd] PartialOrd for Slice[T] {
    fn partial_cmp(self: Slice[T], other: Slice[T]) -> Option[Ordering] {
        let limit = if self.len() < other.len() {
            self.len()
        } else {
            other.len()
        };
        let mut index = 0;
        while index < limit {
            match PartialOrd::partial_cmp(self[index], other[index]) {
                Option::None => return Option::None,
                Option::Some(Ordering::Equal) => {
                    index += 1;
                },
                Option::Some(ordering) => return Option::Some(ordering),
            }
        }
        PartialOrd::partial_cmp(self.len(), other.len())
    }
}

impl[T: Ord] Ord for Slice[T] {
    fn cmp(self: Slice[T], other: Slice[T]) -> Ordering {
        let limit = if self.len() < other.len() {
            self.len()
        } else {
            other.len()
        };
        let mut index = 0;
        while index < limit {
            match Ord::cmp(self[index], other[index]) {
                Ordering::Equal => {
                    index += 1;
                },
                ordering => return ordering,
            }
        }
        Ord::cmp(self.len(), other.len())
    }
}

impl[T: PartialOrd] PartialOrd for Option[T] {
    fn partial_cmp(self: Option[T], other: Option[T]) -> Option[Ordering] {
        match (self, other) {
            (Option::None, Option::None) => Option::Some(Ordering::Equal),
            (Option::None, Option::Some(_)) => Option::Some(Ordering::Less),
            (Option::Some(_), Option::None) => Option::Some(Ordering::Greater),
            (Option::Some(left), Option::Some(right)) => PartialOrd::partial_cmp(left, right),
        }
    }
}

impl[T: Ord] Ord for Option[T] {
    fn cmp(self: Option[T], other: Option[T]) -> Ordering {
        match (self, other) {
            (Option::None, Option::None) => Ordering::Equal,
            (Option::None, Option::Some(_)) => Ordering::Less,
            (Option::Some(_), Option::None) => Ordering::Greater,
            (Option::Some(left), Option::Some(right)) => Ord::cmp(left, right),
        }
    }
}

impl[T: PartialOrd, E: PartialOrd] PartialOrd for Result[T, E] {
    fn partial_cmp(self: Result[T, E], other: Result[T, E]) -> Option[Ordering] {
        match (self, other) {
            (Result::Ok(left), Result::Ok(right)) => PartialOrd::partial_cmp(left, right),
            (Result::Ok(_), Result::Err(_)) => Option::Some(Ordering::Less),
            (Result::Err(_), Result::Ok(_)) => Option::Some(Ordering::Greater),
            (Result::Err(left), Result::Err(right)) => PartialOrd::partial_cmp(left, right),
        }
    }
}

impl[T: Ord, E: Ord] Ord for Result[T, E] {
    fn cmp(self: Result[T, E], other: Result[T, E]) -> Ordering {
        match (self, other) {
            (Result::Ok(left), Result::Ok(right)) => Ord::cmp(left, right),
            (Result::Ok(_), Result::Err(_)) => Ordering::Less,
            (Result::Err(_), Result::Ok(_)) => Ordering::Greater,
            (Result::Err(left), Result::Err(right)) => Ord::cmp(left, right),
        }
    }
}

pub struct Reverse[T] {
    pub value: T,
}

impl[T] Reverse[T] {
    pub fn new(value: T) -> Reverse[T] {
        Reverse { value }
    }

    pub fn into_inner(self: Reverse[T]) -> T {
        self.value
    }
}

impl[T: PartialEq] PartialEq for Reverse[T] {
    fn eq(self: Reverse[T], other: Reverse[T]) -> bool {
        self.value == other.value
    }
}

impl[T: Eq] Eq for Reverse[T] {}

impl[T: PartialOrd] PartialOrd for Reverse[T] {
    fn partial_cmp(self: Reverse[T], other: Reverse[T]) -> Option[Ordering] {
        PartialOrd::partial_cmp(other.value, self.value)
    }
}

impl[T: Ord] Ord for Reverse[T] {
    fn cmp(self: Reverse[T], other: Reverse[T]) -> Ordering {
        Ord::cmp(other.value, self.value)
    }
}

pub fn compare[T: Ord](left: T, right: T) -> Ordering {
    Ord::cmp(left, right)
}

pub fn min[T: Ord](left: T, right: T) -> T {
    min_by(left, right, |first: T, second: T| compare(first, second))
}

pub fn max[T: Ord](left: T, right: T) -> T {
    max_by(left, right, |first: T, second: T| compare(first, second))
}

pub fn min_by[T](left: T, right: T, compare_fn: (T, T) -> Ordering) -> T {
    if compare_fn(left, right).is_gt() {
        right
    } else {
        left
    }
}

pub fn max_by[T](left: T, right: T, compare_fn: (T, T) -> Ordering) -> T {
    if compare_fn(left, right).is_gt() {
        left
    } else {
        right
    }
}

pub fn min_by_key[T, K: Ord](left: T, right: T, key: (T) -> K) -> T {
    let left_key = key(left);
    let right_key = key(right);
    min_by(left, right, |_: T, _: T| compare(left_key, right_key))
}

pub fn max_by_key[T, K: Ord](left: T, right: T, key: (T) -> K) -> T {
    let left_key = key(left);
    let right_key = key(right);
    max_by(left, right, |_: T, _: T| compare(left_key, right_key))
}

pub fn clamp[T: Ord](value: T, minimum: T, maximum: T) -> Result[T, string] {
    if compare(minimum, maximum).is_gt() {
        Result::Err("minimum exceeds maximum")
    } else if compare(value, minimum).is_lt() {
        Result::Ok(minimum)
    } else if compare(value, maximum).is_gt() {
        Result::Ok(maximum)
    } else {
        Result::Ok(value)
    }
}
