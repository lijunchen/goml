fn fib(x: int) -> int {
    match int_less(x, 2) {
        false => int_add(fib(int_sub(x, 1)), fib(int_sub(x, 2))),
        true => 1,
    }
}

fn main() {
    string_print(int_to_string(fib(10)))
}

