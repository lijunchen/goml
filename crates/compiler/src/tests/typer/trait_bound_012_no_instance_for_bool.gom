trait Add {
    fn add(Self, Self) -> Self;
}

impl Add for int32 {
    fn add(self: int32, other: int32) -> int32 {
        self + other
    }
}

fn main() {
    let _ = Add::add(true, false);
    ()
}
