trait Display {
    fn show(Self) -> string;
}

impl Display for int32 {
    fn show(self: int32) -> string {
        (self).to_string()
    }
}

fn main() {
    let d: dyn Display = 1;
    let _ = d.show();
    ()
}
