trait Show {
    fn show(Self) -> string;
}

impl[T] Show for Vec[T] {
    fn show(self: Vec[T]) -> string {
        "vec"
    }
}

fn main() {
    let r = ref(1);
    let _ = Show::show(r);
    ()
}
