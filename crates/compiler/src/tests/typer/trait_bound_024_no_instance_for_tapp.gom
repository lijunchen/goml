trait Show {
    fn show(self: Self) -> string;
}

impl[T] Show for Vec[T] {
    fn show(self: Vec[T]) -> string {
        "vec"
    }
}

fn main() {
    let r = Ref::new(1);
    let _ = Show::show(r);
    ()
}
