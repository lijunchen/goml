enum Attempt[T, E] {
    Ok(T),
    Err(E),
}

fn attempt() -> Attempt[int32, string] {
    Attempt::Ok(1)
}

fn bad() -> Attempt[int32, string] {
    let value = attempt()?;
    Attempt::Ok(value)
}
