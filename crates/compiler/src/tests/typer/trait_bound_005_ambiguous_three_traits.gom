trait A {
    fn ping(Self) -> string;
}

trait B {
    fn ping(Self) -> string;
}

trait C {
    fn ping(Self) -> string;
}

fn f[T: A + B + C](x: T) -> string {
    x.ping()
}

fn main() {
    ()
}

