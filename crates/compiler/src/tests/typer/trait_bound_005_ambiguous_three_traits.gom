trait A {
    fn ping(self: Self) -> string;
}

trait B {
    fn ping(self: Self) -> string;
}

trait C {
    fn ping(self: Self) -> string;
}

fn f[T: A + B + C](x: T) -> string {
    x.ping()
}

fn main() {
    ()
}

