fn one() -> Vec[int32] {
    let v: Vec[int32] = Vec::new();
    let v = v.push(1i32);
    v
}

fn main() -> unit {
    let xs: Vec[int32] = one();
    let xs = xs.push(2i32);
    println(xs.len());
}
