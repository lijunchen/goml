fn one() -> Vec[int32] {
    let v: Vec[int32] = Vec::new();
    vec_push(v, 1i32);
    v
}

fn main() -> unit {
    let xs: Vec[int32] = one();
    let _ = vec_push(xs, 2i32);
    println(vec_len(xs));
}
