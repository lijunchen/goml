extern "go" "strings" "ToUpper" upper(text: string) -> string

enum Choice {
    strings,
    other,
}

fn main() -> unit {
    let choice = Choice::strings;
    println(upper("ok"));
    match choice {
        Choice::strings => println("variant"),
        Choice::other => println("other"),
    };
}
