extern "go" "strings" "ToUpper" upper(text: string) -> string

enum Choice {
    upper,
    other,
}

fn main() -> unit {
    let choice = Choice::upper;
    match choice {
        Choice::upper => string_println("variant"),
        Choice::other => string_println("other"),
    };
}
