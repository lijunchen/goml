extern "go" "strings" "*Builder" type RawBuilder

#[go_value]
extern "go" "" "`strings.Builder`" raw_text() -> string

struct strings {
    value: int32,
}

fn main() -> unit {
    let _ = strings { value: 1i32 };
    println(raw_text());
}
