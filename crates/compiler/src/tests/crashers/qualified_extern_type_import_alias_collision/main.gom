extern "go" "strings" "strings.Builder" type RawBuilder

#[go_value]
extern "go" "" "new(strings.Builder)" new_builder() -> RawBuilder

struct strings {
    value: int32,
}

fn main() -> unit {
    let _ = new_builder();
    let s = strings { value: 11i32 };
    println(s.value);
}
