package Main
import Shape
import Geo

fn main() {
    let p = Shape::make(3, 4);
    let q = Geo::move(p, 1, 2);
    let total = Geo::squared(q) + Shape::sum(q);
    string_println(int32_to_string(total));
}
