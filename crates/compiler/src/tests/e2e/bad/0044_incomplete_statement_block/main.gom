package main;

fn early_return(x: int32) -> string {
    if x > 0 {
        return "positive"
    } else {
        return "non-positive"
    }
}

fn main() {
    string_println(early_return(5));
    string_println(early_return(-3));
}
