fn main() {
    let x = 10;
    let result = if x > 5 {
        let a = x * 2;
        let b = a + 1;
        b
    } else {
        let c = x - 1;
        c
    };
    println((result).to_string());
    
    let msg = match result {
        21 => {
            let prefix = "found: ";
            prefix + "twenty-one"
        },
        _ => "other",
    };
    println(msg);
}
