fn curry[A, B, C](f: (A, B) -> C) -> (A) -> (B) -> C {
    |a| |b| f(a, b)
}

fn main() {
    let add = |a: int32, b: int32| a + b;
    let curried = curry(add);
    let add5 = curried(5);
    println(add5(10).to_string());
    println(curried(3)(4).to_string());
}
