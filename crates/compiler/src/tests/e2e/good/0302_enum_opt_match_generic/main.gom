enum Opt[T] {
    Some(T),
    None,
}

fn none[T]() -> Opt[T] {
    Opt::None
}

fn main() {
    let x: Opt[int32] = none();
    match x {
        Opt::Some(v) => println(int32_to_string(v)),
        Opt::None => println("none"),
    };
}
