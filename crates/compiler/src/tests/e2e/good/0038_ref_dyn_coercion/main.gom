trait Show {
    fn show(Self) -> string;
}

struct Num { val: int32 }

impl Show for Num {
    fn show(self: Num) -> string {
        self.val.to_string()
    }
}

fn main() {
    let r: Ref[dyn Show] = Ref::new(Num { val: 42 });
    println(Show::show(r.get()));
}
