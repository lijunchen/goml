trait Show {
    fn show(Self) -> string;
}

struct Num { val: int32 }

impl Show for Num {
    fn show(self: Num) -> string {
        (self.val).to_string()
    }
}

fn main() {
    let r: Ref[dyn Show] = ref(Num { val: 42 });
    println(Show::show(ref_get(r)));
}
