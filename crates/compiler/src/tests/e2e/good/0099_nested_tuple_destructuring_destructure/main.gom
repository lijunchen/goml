fn main() {
    let t = (1, "hello", true);
    let (a, b, c) = t;
    println(a.to_string());
    println(b);
    
    let nested = ((1, 2), (3, 4));
    let ((x1, y1), (x2, y2)) = nested;
    println((x1 + x2).to_string());
    println((y1 + y2).to_string());
}
