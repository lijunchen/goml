fn maybe_log(skip: bool) {
    if skip {
        string_println("skip");
        return
    } else {
        ()
    };
    string_println("run");
}

fn main() {
    maybe_log(true);
    maybe_log(false);
}
