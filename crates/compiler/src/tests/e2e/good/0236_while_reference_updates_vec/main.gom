fn main() {
    let v: Vec[uint8] = Vec::new();
    let v = v.push(1);
    let v = v.push(2);
    let v = v.push(3);
    let i = Ref::new(0);
    while i.get() < v.len() {
        println(v[i.get()].to_string());
        i.set(i.get() + 1);
    };
}
