fn main() {
    let v: Vec[uint8] = Vec::new();
    let v = v.push(1);
    let v = v.push(2);
    let v = v.push(3);
    let i = ref(0);
    while ref_get(i) < v.len() {
        println(uint8_to_string(v[ref_get(i)]));
        ref_set(i, ref_get(i) + 1);
    };
}
