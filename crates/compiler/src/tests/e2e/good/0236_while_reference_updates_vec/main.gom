fn main() {
    let v: Vec[uint8] = Vec::new();
    let v = v.push(1);
    let v = v.push(2);
    let v = v.push(3);
    let i = ref(0);
    while ref_get(i) < v.len() {
        println((v[ref_get(i)]).to_string());
        ref_set(i, ref_get(i) + 1);
    };
}
