fn transform[A, B](items: [A; 3], f: (A) -> B) -> (B, B, B) {
    (f(array_get(items, 0)), f(array_get(items, 1)), f(array_get(items, 2)))
}

fn main() {
    let result = transform(["hello", "world", "!"], |s| s + s);
    let (a, b, c) = result;
    println(a);
    println(b);
    println(c);
}
