struct Config {
    width: uint16,
    height: uint16,
}

fn main() {
    let width: uint16 = 800;
    let height: uint16 = 600;
    let c = Config { width, height };
    println(uint16_to_string(c.width));
    println(uint16_to_string(c.height));
}
