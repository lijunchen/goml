struct Pair[A, B] {
    fst: A,
    snd: B,
}

fn swap[A, B](p: Pair[A, B]) -> Pair[B, A] {
    Pair { fst: p.snd, snd: p.fst }
}

fn main() {
    let p = Pair { fst: 1, snd: "hello" };
    let q = swap(p);
    println(q.fst);
    println(q.snd.to_string());
    
    let r = Pair { fst: Pair { fst: 1, snd: 2 }, snd: "nested" };
    println(r.fst.fst.to_string());
    println(r.fst.snd.to_string());
    println(r.snd);
}
