enum Result[T, E] {
    Ok(T),
    Err(E),
}

fn main() {
    let r: Result[uint8, string] = Result::Ok(42);
    match r {
        Result::Ok(v) => println(uint8_to_string(v)),
        Result::Err(e) => println(e),
    };
}
