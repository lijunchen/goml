trait Speak {
    fn speak(Self) -> string;
}

struct Dog {}
struct Cat {}

impl Speak for Dog {
    fn speak(self: Dog) -> string { "woof" }
}
impl Speak for Cat {
    fn speak(self: Cat) -> string { "meow" }
}

fn choose(is_dog: bool) -> dyn Speak {
    match is_dog {
        true => Dog {},
        false => Cat {},
    }
}

fn main() {
    println(Speak::speak(choose(true)));
    println(Speak::speak(choose(false)));
}
