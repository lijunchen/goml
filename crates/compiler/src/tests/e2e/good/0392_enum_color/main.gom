#[derive(ToString)]
struct Point {
    x: int32,
    y: int32,
}

#[derive(ToString)]
enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let p = Point { x: 10, y: 20 };
    println(p.to_string());
    println(Color::Red.to_string());
    println(Color::Green.to_string());
}
