package main;

fn first[T](a: T, b: T) -> T {
    a
}

fn main() {
    let r1: uint8 = first(10, 20);
    string_println(uint8_to_string(r1));
    
    let r2: float32 = first(1.5, 2.5);
    string_println(float32_to_string(r2));
}
