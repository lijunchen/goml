fn main() {
    let x: int64 = -100i64;
    let result = match x {
        -100i64 => "negative hundred",
        0i64 => "zero",
        100i64 => "hundred",
        _ => "other",
    };
    println(result);
}
