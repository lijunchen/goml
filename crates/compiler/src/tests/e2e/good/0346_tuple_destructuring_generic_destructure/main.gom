fn dup[T](x: T) -> (T, T) {
    (x, x)
}

fn triple[T](x: T) -> (T, T, T) {
    (x, x, x)
}

fn main() {
    let (a, b) = dup(42);
    println(a.to_string());
    println(b.to_string());

    let (x, y, z) = triple("hi");
    println(x + y + z);
}
