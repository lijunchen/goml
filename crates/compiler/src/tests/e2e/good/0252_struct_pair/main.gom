struct Pair[A, B] {
    first: A,
    second: B,
}

fn make_pair[A, B](a: A, b: B) -> Pair[A, B] {
    Pair { first: a, second: b }
}

fn main() {
    let p: Pair[uint8, float32] = make_pair(10, 3.14);
    string_println(uint8_to_string(p.first));
    string_println(float32_to_string(p.second));
}
