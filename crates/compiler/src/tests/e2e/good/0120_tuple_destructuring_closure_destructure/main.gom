fn main() {
    let x: (int32, string, bool) = (42, "hello", true);
    let (a, _, c) = x;
    println((a).to_string());
    println((c).to_string());
    
    let f = |t: (int32, int32)| {
        let (a, b) = t;
        a + b
    };
    println((f((10, 20))).to_string());
}
