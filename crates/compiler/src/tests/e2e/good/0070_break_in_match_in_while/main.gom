fn main() {
    let i = ref(0);
    while true {
        let v = ref_get(i);
        match v {
            0 => println("zero"),
            1 => println("one"),
            _ => break,
        };
        ref_set(i, ref_get(i) + 1);
    };
    println("done");
}
