struct Inner { val: int32 }
struct Outer { inner: Inner, name: string }

fn main() {
    let o = Outer { inner: Inner { val: 42 }, name: "test" };
    let Outer { inner: Inner { val }, name } = o;
    println((val).to_string());
    println(name);
}
