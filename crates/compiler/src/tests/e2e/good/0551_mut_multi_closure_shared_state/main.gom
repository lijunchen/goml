fn make_pair(seed: int32) -> (() -> int32, () -> int32) {
    let mut n = seed;
    let inc = || {
        n = n + 1;
        n
    };
    let get = || n;
    (inc, get)
}

fn main() {
    let (inc, get) = make_pair(5);

    println(int32_to_string(get()));
    println(int32_to_string(inc()));
    println(int32_to_string(get()));
    println(int32_to_string(inc()));
    println(int32_to_string(get()));
}
