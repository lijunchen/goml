fn main() {
    let arr: [uint8; 5] = [1, 2, 3, 4, 5];
    let i = Ref::new(0);
    let sum: uint8 = 0u8;
    let total = Ref::new(sum);
    while i.get() < 5 {
        total.set(total.get() + arr[i.get()]);
        i.set(i.get() + 1);
    };
    println(total.get().to_string());
}
