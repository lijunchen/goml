package main;

fn compose[A, B, C](f: (B) -> C, g: (A) -> B) -> (A) -> C {
    |x: A| f(g(x))
}

fn main() {
    let double = |x: int32| x * 2;
    let add_one = |x: int32| x + 1;
    let double_then_add = compose(add_one, double);
    string_println(int32_to_string(double_then_add(5)));
}
