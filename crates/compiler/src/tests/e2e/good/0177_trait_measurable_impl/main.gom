trait Measurable {
    fn measure(Self) -> int32;
}

struct Rect {
    w: int32,
    h: int32,
}

impl Rect {
    fn area(self: Rect) -> int32 {
        self.w * self.h
    }
    fn perimeter(self: Rect) -> int32 {
        2 * (self.w + self.h)
    }
}

impl Measurable for Rect {
    fn measure(self: Rect) -> int32 {
        Rect::area(self)
    }
}

fn main() {
    let r = Rect { w: 3, h: 4 };
    println((Rect::area(r)).to_string());
    println((Rect::perimeter(r)).to_string());
    println((Measurable::measure(r)).to_string());
}
