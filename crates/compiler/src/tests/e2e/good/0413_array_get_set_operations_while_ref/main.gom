fn main() {
    let arr = [1, 2, 3, 4, 5];
    array_set(arr, 2, 99);
    let i = ref(0i32);
    while ref_get(i) < 5 {
        println(array_get(arr, ref_get(i)).to_string());
        ref_set(i, ref_get(i) + 1);
    };
}
