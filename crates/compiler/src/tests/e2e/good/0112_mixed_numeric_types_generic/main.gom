fn identity[T](x: T) -> T {
    x
}

fn main() {
    let a: uint8 = identity(42);
    println((a).to_string());
    
    let b: int64 = identity(100);
    println((b).to_string());
    
    let c: float32 = identity(3.14);
    println((c).to_string());
}
