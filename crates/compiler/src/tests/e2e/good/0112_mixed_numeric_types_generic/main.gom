fn identity[T](x: T) -> T {
    x
}

fn main() {
    let a: uint8 = identity(42);
    println(uint8_to_string(a));
    
    let b: int64 = identity(100);
    println(int64_to_string(b));
    
    let c: float32 = identity(3.14);
    println(float32_to_string(c));
}
