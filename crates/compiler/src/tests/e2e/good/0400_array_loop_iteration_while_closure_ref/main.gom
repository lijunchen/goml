fn main() {
    let i = Ref::new(0);
    let callbacks: [() -> unit; 3] = [
        || println("first"),
        || println("second"),
        || println("third"),
    ];
    while i.get() < 3 {
        let cb = callbacks[i.get()];
        cb();
        i.set(i.get() + 1);
    };
}
