fn main() {
    let i = ref(0i32);
    let callbacks: [() -> unit; 3] = [
        || println("first"),
        || println("second"),
        || println("third"),
    ];
    while ref_get(i) < 3 {
        let cb = callbacks[ref_get(i)];
        cb();
        ref_set(i, ref_get(i) + 1);
    };
}
