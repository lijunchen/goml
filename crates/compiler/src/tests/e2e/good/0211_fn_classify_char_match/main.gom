package main;

fn classify_char(c: char) -> string {
    match c {
        'a' => "a",
        'b' => "b",
        _ => "other",
    }
}

fn main() {
    string_println(classify_char('a'));
    string_println(classify_char('z'));
}
