package main;

struct Point { x: int32, y: int32 }

fn swap[T](a: T, b: T) -> (T, T) {
    (b, a)
}

fn main() {
    let (a, b) = swap(Point { x: 1, y: 2 }, Point { x: 3, y: 4 });
    string_println(int32_to_string(a.x));
    string_println(int32_to_string(b.x));
}
