enum Option[T] {
    Some(T),
    None,
}

fn main() {
    let (a, (b, c)) = (1, (2, 3));
    println((a + b + c).to_string());
    
    let (x, y, z) = (10, "hello", true);
    println((x).to_string());
    println(y);
    if z { println("true") } else { println("false") };
}
