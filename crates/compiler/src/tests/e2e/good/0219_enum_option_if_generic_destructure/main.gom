enum Option[T] {
    Some(T),
    None,
}

fn main() {
    let (a, (b, c)) = (1, (2, 3));
    println(int32_to_string(a + b + c));
    
    let (x, y, z) = (10, "hello", true);
    println(int32_to_string(x));
    println(y);
    if z { println("true") } else { println("false") };
}
