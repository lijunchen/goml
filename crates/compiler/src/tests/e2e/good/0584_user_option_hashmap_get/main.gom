enum Option[T] {
    Nada,
    Just(T),
}

fn main() -> unit {
    let map = HashMap::new();
    map.set(1i32, 2i32);
    match map.get(1i32) {
        Option::Just(value) => println(value),
        Option::Nada => println(0i32),
    }
}
