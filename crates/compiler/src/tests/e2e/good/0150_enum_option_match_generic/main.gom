enum Option[T] {
    Some(T),
    None,
}

fn main() {
    let x: Option[uint8] = Option::Some(42u8);
    match x {
        Option::Some(v) => println((v).to_string()),
        Option::None => println("none"),
    };
}
