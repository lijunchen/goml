fn apply[T](f: (T) -> T, x: T) -> T {
    f(x)
}

fn double(x: int32) -> int32 {
    x * 2
}

fn main() {
    let result = apply(double, 21);
    println(result.to_string());
    
    let inc = apply(|x| x + 1, 10);
    println(inc.to_string());
}
