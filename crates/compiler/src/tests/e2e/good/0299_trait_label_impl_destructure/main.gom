trait Label {
    fn label(Self) -> string;
}

impl Label for (int32, int32) {
    fn label(self: (int32, int32)) -> string {
        let (x, y) = self;
        "(" + x.to_string() + "," + y.to_string() + ")"
    }
}

fn main() {
    let p = (3, 4);
    println(Label::label(p));
}
