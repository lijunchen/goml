struct Counter {
    count: uint8,
}

impl Counter {
    fn add(self: Counter, n: uint8) -> Counter {
        Counter { count: self.count + n }
    }
}

fn main() {
    let c = Counter { count: 0 };
    let c2 = c.add(5);
    println(uint8_to_string(c2.count));
}
