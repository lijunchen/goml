
fn main() {
    let x: int32 = if true { 1 } else { 2 };
    let y = match x {
        1 => "one",
        2 => "two",
        _ => "other",
    };
    string_println(y);
    
    let z: uint8 = if true { 10 } else { 20 };
    string_println(uint8_to_string(z));
}
