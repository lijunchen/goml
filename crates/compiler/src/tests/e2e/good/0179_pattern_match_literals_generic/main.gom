fn choose[T](cond: bool, a: T, b: T) -> T {
    match cond {
        true => a,
        false => b,
    }
}

fn main() {
    println(int32_to_string(choose(true, 1, 2)));
    println(choose(false, "hello", "world"));
}
