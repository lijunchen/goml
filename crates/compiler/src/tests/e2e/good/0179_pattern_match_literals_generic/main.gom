fn choose[T](cond: bool, a: T, b: T) -> T {
    match cond {
        true => a,
        false => b,
    }
}

fn main() {
    string_println(int32_to_string(choose(true, 1, 2)));
    string_println(choose(false, "hello", "world"));
}
