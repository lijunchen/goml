fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn parse_text(ok/0: bool) -> Result[string, string] {
  if ok/0 {
      {
        Result::Ok("ignored")
      }
  } else {
      {
        Result::Err("parse failed")
      }
  }
}

fn check(ok/1: bool) -> Result[string, string] {
  let mtmp3 = {
    let mtmp0 = parse_text(ok/1) in
    match mtmp0 {
      Result::Ok(x1) => {
        {
          let x1 = Result::Ok._0(mtmp0) in
          {
            let try_value/12 = x1 in
            try_value/12
          }
        }
      },
      Result::Err(x2) => {
        {
          let x2 = Result::Err._0(mtmp0) in
          {
            let try_residual/12 = x2 in
            return Result::Err(try_residual/12)
          }
        }
      },
    }
  } in
  {
    Result::Ok("ok")
  }
}

fn show(res/2: Result[string, string]) -> string {
  match res/2 {
    Result::Ok(x4) => {
      {
        let x4 = Result::Ok._0(res/2) in
        {
          let value/3 = x4 in
          ("ok " + value/3)
        }
      }
    },
    Result::Err(x5) => {
      {
        let x5 = Result::Err._0(res/2) in
        {
          let err/4 = x5 in
          ("err " + err/4)
        }
      }
    },
  }
}

fn main() -> unit {
  let _wild6 = println(show(check(true))) in
  let _wild7 = println(show(check(false))) in
}