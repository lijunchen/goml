package main
file main.gom
  fn parse_text(local/1/0: bool) -> Result[string, string] {
        if ok/0 {
            Result::Ok("ignored")
        } else {
            Result::Err("parse failed")
        }
    }
    
    fn check(local/1/1: bool) -> Result[string, string] {
        let _ = def/1/0(ok/1)?;
        Result::Ok("ok")
    }
    
    fn show(local/1/2: Result[string, string]) -> string {
        match res/2 {
            Result::Ok(local/1/3) => "ok " + value/3,
            Result::Err(local/1/4) => "err " + err/4,
        }
    }
    
    fn main() -> unit {
        println(def/1/2(def/1/1(true)));
        println(def/1/2(def/1/1(false)));
    }