package main
file main.gom
  #[go_option_last]
extern "go" "strings" "Cut" cut_pair(text: string, sep: string) -> Option[(string, string)]
    
    fn describe(local/1/0: string) -> string {
        match def/1/0(text/0, ":") {
            Option::Some((local/1/1, local/1/2)) => before/1 + "|" + after/2,
            Option::None => "missing",
        }
    }
    
    fn main() {
        string_println(def/1/1("alpha:beta"));
        string_println(def/1/1("plain"));
    }