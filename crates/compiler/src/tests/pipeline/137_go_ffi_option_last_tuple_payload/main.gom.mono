fn describe(text/0: string) -> string {
  {
    let mtmp0 = cut_pair(text/0, ":") in
    match mtmp0 {
      Option__(string,string)::None => {
        "missing"
      },
      Option__(string,string)::Some(x1) => {
        {
          let x1 = Option__(string,string)::Some._0(mtmp0) in
          {
            let x2 = x1.0 in
            let x3 = x1.1 in
            {
              let after/2 = x3 in
              {
                let before/1 = x2 in
                ((before/1 + "|") + after/2)
              }
            }
          }
        }
      },
    }
  }
}

fn main() -> unit {
  let _wild4 = println__T_string(describe("alpha:beta")) in
  let _wild5 = println__T_string(describe("plain")) in
}

fn println__T_string(value/1: string) -> unit {
  string_println(value/1)
}