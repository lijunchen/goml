fn is_flag8(value/0: uint8) -> bool {
  match value/0 {
    0 => {
      true
    },
    200 => {
      true
    },
    _ => false,
  }
}

fn is_flag16(value/1: uint16) -> bool {
  match value/1 {
    1024 => {
      true
    },
    65000 => {
      true
    },
    _ => false,
  }
}

fn is_flag32(value/2: uint32) -> bool {
  match value/2 {
    4000000000 => {
      true
    },
    1234567890 => {
      true
    },
    _ => false,
  }
}

fn is_flag64(value/3: uint64) -> bool {
  match value/3 {
    900000000 => {
      true
    },
    600000000 => {
      true
    },
    _ => false,
  }
}

fn match_struct(counter/4: Counter) -> bool {
  let x0 = Counter.start(counter/4) in
  let x1 = Counter.end(counter/4) in
  match x1 {
    900000000 => {
      match x0 {
        4000000000 => {
          true
        },
        _ => false,
      }
    },
    600000000 => {
      true
    },
    _ => false,
  }
}

fn report(label/5: string, value/6: bool) -> string {
  let t3 = bool_to_string(value/6) in
  string_add(label/5, t3)
}

fn main() -> unit {
  let counter/7 = Counter { start: 4000000000, end: 900000000 } in
  let alt_counter/8 = Counter { start: 12, end: 600000000 } in
  let t5 = is_flag8(200) in
  let t4 = report("u8_hit=", t5) in
  let t8 = is_flag8(15) in
  let t7 = report(",u8_miss=", t8) in
  let t11 = is_flag16(65000) in
  let t10 = report(",u16_hit=", t11) in
  let t14 = is_flag16(42) in
  let t13 = report(",u16_miss=", t14) in
  let t17 = is_flag32(1234567890) in
  let t16 = report(",u32_hit=", t17) in
  let t20 = is_flag32(99) in
  let t19 = report(",u32_miss=", t20) in
  let t23 = is_flag64(900000000) in
  let t22 = report(",u64_hit=", t23) in
  let t26 = is_flag64(700000000) in
  let t25 = report(",u64_miss=", t26) in
  let t29 = match_struct(counter/7) in
  let t28 = report(",struct_first=", t29) in
  let t31 = match_struct(alt_counter/8) in
  let t30 = report(",struct_second=", t31) in
  let t27 = string_add(t28, t30) in
  let t24 = string_add(t25, t27) in
  let t21 = string_add(t22, t24) in
  let t18 = string_add(t19, t21) in
  let t15 = string_add(t16, t18) in
  let t12 = string_add(t13, t15) in
  let t9 = string_add(t10, t12) in
  let t6 = string_add(t7, t9) in
  let message/9 = string_add(t4, t6) in
  let mtmp2 = string_println(message/9) in
  ()
}