fn child(signal/0: Ref[int32]) -> unit {
  ref_set(signal/0, 1)
}

fn main() -> unit {
  let signal/1 = ref(0) in
  let mtmp1 = spawn(closure_env_main_0 { signal_0: signal/1 }) in
  let mtmp2 = while int32_less(ref_get(signal/1), 1) {
      ()
  } in
  let mtmp3 = string_println("main") in
  ()
}

fn __closure_apply4(env5: closure_env_main_0) -> unit {
  let signal/1 = closure_env_main_0.signal_0(env5) in
  let mtmp0 = child(signal/1) in
  ()
}