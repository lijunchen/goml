fn main() -> unit {
  join ret1() -> unit {
    ()
  } in
  let parts/0 = fields("alpha beta gamma") in
  let t2 = vec_len(parts/0) in
  let t3 = slice(parts/0, 0, t2) in
  let t4 = join(t3) in
  let _wild0 = println__T_string(t4) in
  jump ret1()
}

fn println__T_string(value/1: string) -> unit {
  join ret5() -> unit {
    ()
  } in
  let t6 = string_println(value/1) in
  jump ret5()
}