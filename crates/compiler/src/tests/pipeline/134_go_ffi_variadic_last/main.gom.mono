fn main() -> unit {
  let parts/0 = fields("alpha beta gamma") in
  let _wild0 = println__T_string(join(slice(parts/0, 0, vec_len(parts/0)))) in
}

fn println__T_string(value/1: string) -> unit {
  string_println(value/1)
}