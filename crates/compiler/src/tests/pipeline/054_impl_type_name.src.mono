fn impl_TypeName_Point_type_name(self/0: Point) -> string {
  let mtmp0 = self/0 in
  let x1 = Point.x(mtmp0) in
  let x2 = Point.y(mtmp0) in
  let y/2 = x2 in
  let x/1 = x1 in
  let prefix/3 = string_add("Point(", int32_to_string(x/1)) in
  string_add(prefix/3, string_add(", ", string_add(int32_to_string(y/2), ")")))
}

fn impl_TypeName_Shape_type_name(self/4: Shape) -> string {
  match self/4 {
    Shape::Unit => {
      "Unit"
    },
    Shape::Location(x3) => {
      let x3 = Shape::Location._0(self/4) in
      let point/5 = x3 in
      string_add("Shape::", impl_TypeName_Point_type_name(point/5))
    },
  }
}

fn show_point(point/6: Point) -> string {
  impl_TypeName_Point_type_name(point/6)
}

fn show_shape(shape/7: Shape) -> string {
  impl_TypeName_Shape_type_name(shape/7)
}

fn main() -> unit {
  let point/8 = Point { x: 7, y: 9 } in
  let mtmp4 = string_println(show_point(point/8)) in
  let unit_shape/9 = Shape::Unit in
  let mtmp5 = string_println(show_shape(unit_shape/9)) in
  let location_shape/10 = Shape::Location(Point { x: 1, y: 2 }) in
  let mtmp6 = string_println(show_shape(location_shape/10)) in
  ()
}