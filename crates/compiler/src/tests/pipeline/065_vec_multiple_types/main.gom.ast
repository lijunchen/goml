fn main() -> unit {
    let vi: Vec[int32] = vec_new() in
    let vi = vec_push(vi, 42) in
    let val_i = vec_get(vi, 0) in
    let len_i = vec_len(vi) in
    let vs: Vec[string] = vec_new() in
    let vs = vec_push(vs, "hello") in
    let vs = vec_push(vs, "world") in
    let val_s = vec_get(vs, 1) in
    let len_s = vec_len(vs) in
    let vb: Vec[bool] = vec_new() in
    let vb = vec_push(vb, true) in
    let vb = vec_push(vb, false) in
    let val_b = vec_get(vb, 0) in
    let len_b = vec_len(vb) in
    let _ = string_println(int32_to_string(val_i)) in
    let _ = string_println(int32_to_string(len_i)) in
    let _ = string_println(val_s) in
    let _ = string_println(int32_to_string(len_s)) in
    let _ = string_println(bool_to_string(val_b)) in
    let _ = string_println(int32_to_string(len_b)) in
    ()
}

