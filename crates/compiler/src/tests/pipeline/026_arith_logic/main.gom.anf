fn show_int(label/0: string, value/1: int32) -> unit {
  join ret12() -> unit {
    ()
  } in
  let t13 = $goml_builtin_runtime$int32_to_string(value/1) in
  let t14 = (label/0 + t13) in
  let mtmp0 = println__T_string(t14) in
  jump ret12()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret15() -> unit {
    ()
  } in
  let t16 = $goml_builtin_runtime$bool_to_string(value/3) in
  let t17 = (label/2 + t16) in
  let mtmp1 = println__T_string(t17) in
  jump ret15()
}

fn main() -> unit {
  join ret18() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp2 = show_int("sum=", sum/5) in
  let mtmp3 = show_int("diff=", diff/6) in
  let mtmp4 = show_int("prod=", prod/7) in
  let mtmp5 = show_int("quot=", quot/8) in
  join k19(jp20: bool) -> unit {
    let and_result/9 = jp20 in
    join k21(jp22: bool) -> unit {
      let or_result/10 = jp22 in
      let not_result/11 = (!false) in
      let t33 = (!and_result/9) in
      join k25(jp26: bool) -> unit {
        join k23(jp24: bool) -> unit {
          let mixed/12 = jp24 in
          let mtmp8 = show_bool("and=", and_result/9) in
          let mtmp9 = show_bool("or=", or_result/10) in
          let mtmp10 = show_bool("not=", not_result/11) in
          let mtmp11 = show_bool("mixed=", mixed/12) in
          jump ret18()
        } in
        if jp26 {
          jump k23(true)
        } else {
          let t27 = (diff/6 - quot/8) in
          let t28 = (t27 + base/4) in
          let t29 = (sum/5 / 2) in
          let mtmp7 = (t28 - t29) in
          join k30(jp31: bool) -> unit {
            let t32 = (!jp31) in
            jump k23(t32)
          } in
          match mtmp7 {
            0 => {
              jump k30(false)
            },
            _ => {
              jump k30(true)
            },
          }
        }
      } in
      if t33 {
        let t34 = (prod/7 * base/4) in
        let t35 = (sum/5 + t34) in
        let t36 = (prod/7 / 2) in
        let mtmp6 = (t35 - t36) in
        join k37(jp38: bool) -> unit {
          jump k25(jp38)
        } in
        match mtmp6 {
          0 => {
            jump k37(false)
          },
          _ => {
            jump k37(true)
          },
        }
      } else {
        jump k25(false)
      }
    } in
    if true {
      jump k21(true)
    } else {
      jump k21(false)
    }
  } in
  if true {
    jump k19(false)
  } else {
    jump k19(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret39() -> unit {
    ()
  } in
  let t40 = string_println(value/1) in
  jump ret39()
}