fn show_int(label/0: string, value/1: int32) -> unit {
  join ret19() -> unit {
    ()
  } in
  let t20 = inherent#int32#int32#to_string(value/1) in
  let t21 = (label/0 + t20) in
  let mtmp7 = println__T_string(t21) in
  jump ret19()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret22() -> unit {
    ()
  } in
  let t23 = trait_impl#ToString#bool#to_string(value/3) in
  let t24 = (label/2 + t23) in
  let mtmp8 = println__T_string(t24) in
  jump ret22()
}

fn main() -> unit {
  join ret25() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp9 = show_int("sum=", sum/5) in
  let mtmp10 = show_int("diff=", diff/6) in
  let mtmp11 = show_int("prod=", prod/7) in
  let mtmp12 = show_int("quot=", quot/8) in
  join k26(jp27: bool) -> unit {
    let and_result/9 = jp27 in
    join k28(jp29: bool) -> unit {
      let or_result/10 = jp29 in
      let not_result/11 = (!false) in
      let t40 = (!and_result/9) in
      join k32(jp33: bool) -> unit {
        join k30(jp31: bool) -> unit {
          let mixed/12 = jp31 in
          let mtmp15 = show_bool("and=", and_result/9) in
          let mtmp16 = show_bool("or=", or_result/10) in
          let mtmp17 = show_bool("not=", not_result/11) in
          let mtmp18 = show_bool("mixed=", mixed/12) in
          jump ret25()
        } in
        if jp33 {
          jump k30(true)
        } else {
          let t34 = (diff/6 - quot/8) in
          let t35 = (t34 + base/4) in
          let t36 = (sum/5 / 2) in
          let mtmp14 = (t35 - t36) in
          join k37(jp38: bool) -> unit {
            let t39 = (!jp38) in
            jump k30(t39)
          } in
          match mtmp14 {
            0 => {
              jump k37(false)
            },
            _ => {
              jump k37(true)
            },
          }
        }
      } in
      if t40 {
        let t41 = (prod/7 * base/4) in
        let t42 = (sum/5 + t41) in
        let t43 = (prod/7 / 2) in
        let mtmp13 = (t42 - t43) in
        join k44(jp45: bool) -> unit {
          jump k32(jp45)
        } in
        match mtmp13 {
          0 => {
            jump k44(false)
          },
          _ => {
            jump k44(true)
          },
        }
      } else {
        jump k32(false)
      }
    } in
    if true {
      jump k28(true)
    } else {
      jump k28(false)
    }
  } in
  if true {
    jump k26(false)
  } else {
    jump k26(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret46() -> unit {
    ()
  } in
  let t47 = trait_impl#ToString#string#to_string(value/1) in
  let t48 = @runtime(core.string_println)(t47) in
  jump ret46()
}

fn inherent#int32#int32#to_string(self/2: int32) -> string {
  join ret49(retv50: string) -> string {
    retv50
  } in
  let t51 = @runtime(core.int32_to_string)(self/2) in
  jump ret49(t51)
}

fn trait_impl#ToString#bool#to_string(self/8: bool) -> string {
  join ret52(retv53: string) -> string {
    retv53
  } in
  let t54 = @runtime(core.bool_to_string)(self/8) in
  jump ret52(t54)
}

fn trait_impl#ToString#string#to_string(self/9: string) -> string {
  join ret55(retv56: string) -> string {
    retv56
  } in
  jump ret55(self/9)
}