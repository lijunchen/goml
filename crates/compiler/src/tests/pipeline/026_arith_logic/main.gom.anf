fn show_int(label/0: string, value/1: int32) -> unit {
  join ret70() -> unit {
    ()
  } in
  let t71 = inherent#int32#int32#to_string(value/1) in
  let t72 = (label/0 + t71) in
  let mtmp58 = println__T_string(t72) in
  jump ret70()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret73() -> unit {
    ()
  } in
  let t74 = trait_impl#ToString#bool#to_string(value/3) in
  let t75 = (label/2 + t74) in
  let mtmp59 = println__T_string(t75) in
  jump ret73()
}

fn main() -> unit {
  join ret76() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp60 = show_int("sum=", sum/5) in
  let mtmp61 = show_int("diff=", diff/6) in
  let mtmp62 = show_int("prod=", prod/7) in
  let mtmp63 = show_int("quot=", quot/8) in
  join k77(jp78: bool) -> unit {
    let and_result/9 = jp78 in
    join k79(jp80: bool) -> unit {
      let or_result/10 = jp80 in
      let not_result/11 = (!false) in
      let t91 = (!and_result/9) in
      join k83(jp84: bool) -> unit {
        join k81(jp82: bool) -> unit {
          let mixed/12 = jp82 in
          let mtmp66 = show_bool("and=", and_result/9) in
          let mtmp67 = show_bool("or=", or_result/10) in
          let mtmp68 = show_bool("not=", not_result/11) in
          let mtmp69 = show_bool("mixed=", mixed/12) in
          jump ret76()
        } in
        if jp84 {
          jump k81(true)
        } else {
          let t85 = (diff/6 - quot/8) in
          let t86 = (t85 + base/4) in
          let t87 = (sum/5 / 2) in
          let mtmp65 = (t86 - t87) in
          join k88(jp89: bool) -> unit {
            let t90 = (!jp89) in
            jump k81(t90)
          } in
          match mtmp65 {
            0 => {
              jump k88(false)
            },
            _ => {
              jump k88(true)
            },
          }
        }
      } in
      if t91 {
        let t92 = (prod/7 * base/4) in
        let t93 = (sum/5 + t92) in
        let t94 = (prod/7 / 2) in
        let mtmp64 = (t93 - t94) in
        join k95(jp96: bool) -> unit {
          jump k83(jp96)
        } in
        match mtmp64 {
          0 => {
            jump k95(false)
          },
          _ => {
            jump k95(true)
          },
        }
      } else {
        jump k83(false)
      }
    } in
    if true {
      jump k79(true)
    } else {
      jump k79(false)
    }
  } in
  if true {
    jump k77(false)
  } else {
    jump k77(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret97() -> unit {
    ()
  } in
  let t98 = trait_impl#ToString#string#to_string(value/1) in
  let t99 = @runtime(core.string_println)(t98) in
  jump ret97()
}

fn inherent#int32#int32#to_string(self/2: int32) -> string {
  join ret100(retv101: string) -> string {
    retv101
  } in
  let t102 = @runtime(core.int32_to_string)(self/2) in
  jump ret100(t102)
}

fn trait_impl#ToString#bool#to_string(self/33: bool) -> string {
  join ret103(retv104: string) -> string {
    retv104
  } in
  let t105 = @runtime(core.bool_to_string)(self/33) in
  jump ret103(t105)
}

fn trait_impl#ToString#string#to_string(self/34: string) -> string {
  join ret106(retv107: string) -> string {
    retv107
  } in
  jump ret106(self/34)
}