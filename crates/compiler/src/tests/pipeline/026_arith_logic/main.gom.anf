fn show_int(label/0: string, value/1: int32) -> unit {
  join ret73() -> unit {
    ()
  } in
  let t74 = inherent#int32#int32#to_string(value/1) in
  let t75 = (label/0 + t74) in
  let mtmp61 = println__T_string(t75) in
  jump ret73()
}

fn show_bool(label/2: string, value/3: bool) -> unit {
  join ret76() -> unit {
    ()
  } in
  let t77 = trait_impl#ToString#bool#to_string(value/3) in
  let t78 = (label/2 + t77) in
  let mtmp62 = println__T_string(t78) in
  jump ret76()
}

fn main() -> unit {
  join ret79() -> unit {
    ()
  } in
  let base/4 = 10 in
  let sum/5 = (base/4 + 5) in
  let diff/6 = (sum/5 - 3) in
  let prod/7 = (diff/6 * 2) in
  let quot/8 = (prod/7 / 4) in
  let mtmp63 = show_int("sum=", sum/5) in
  let mtmp64 = show_int("diff=", diff/6) in
  let mtmp65 = show_int("prod=", prod/7) in
  let mtmp66 = show_int("quot=", quot/8) in
  join k80(jp81: bool) -> unit {
    let and_result/9 = jp81 in
    join k82(jp83: bool) -> unit {
      let or_result/10 = jp83 in
      let not_result/11 = (!false) in
      let t94 = (!and_result/9) in
      join k86(jp87: bool) -> unit {
        join k84(jp85: bool) -> unit {
          let mixed/12 = jp85 in
          let mtmp69 = show_bool("and=", and_result/9) in
          let mtmp70 = show_bool("or=", or_result/10) in
          let mtmp71 = show_bool("not=", not_result/11) in
          let mtmp72 = show_bool("mixed=", mixed/12) in
          jump ret79()
        } in
        if jp87 {
          jump k84(true)
        } else {
          let t88 = (diff/6 - quot/8) in
          let t89 = (t88 + base/4) in
          let t90 = (sum/5 / 2) in
          let mtmp68 = (t89 - t90) in
          join k91(jp92: bool) -> unit {
            let t93 = (!jp92) in
            jump k84(t93)
          } in
          match mtmp68 {
            0 => {
              jump k91(false)
            },
            _ => {
              jump k91(true)
            },
          }
        }
      } in
      if t94 {
        let t95 = (prod/7 * base/4) in
        let t96 = (sum/5 + t95) in
        let t97 = (prod/7 / 2) in
        let mtmp67 = (t96 - t97) in
        join k98(jp99: bool) -> unit {
          jump k86(jp99)
        } in
        match mtmp67 {
          0 => {
            jump k98(false)
          },
          _ => {
            jump k98(true)
          },
        }
      } else {
        jump k86(false)
      }
    } in
    if true {
      jump k82(true)
    } else {
      jump k82(false)
    }
  } in
  if true {
    jump k80(false)
  } else {
    jump k80(false)
  }
}

fn println__T_string(value/1: string) -> unit {
  join ret100() -> unit {
    ()
  } in
  let t101 = trait_impl#ToString#string#to_string(value/1) in
  let t102 = @runtime(core.string_println)(t101) in
  jump ret100()
}

fn inherent#int32#int32#to_string(self/5: int32) -> string {
  join ret103(retv104: string) -> string {
    retv104
  } in
  let t105 = @runtime(core.int32_to_string)(self/5) in
  jump ret103(t105)
}

fn trait_impl#ToString#bool#to_string(self/36: bool) -> string {
  join ret106(retv107: string) -> string {
    retv107
  } in
  let t108 = @runtime(core.bool_to_string)(self/36) in
  jump ret106(t108)
}

fn trait_impl#ToString#string#to_string(self/37: string) -> string {
  join ret109(retv110: string) -> string {
    retv110
  } in
  jump ret109(self/37)
}