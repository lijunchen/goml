fn child(signal/0: Ref[int32]) -> unit {
  ref_set(signal/0, 1)
}

fn main() -> unit {
  let signal/1 = ref(0) in
  let mtmp1 = spawn(|| => let mtmp0 = child(signal/1) in
  ()) in
  let mtmp2 = while int_less(ref_get(signal/1), 1) {
      ()
  } in
  let mtmp3 = string_println("main") in
  ()
}