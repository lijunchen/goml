fn sum(point/0: Point) -> int32 {
  int32_add(Point.x(point/0), Point.y(point/0))
}

fn impl_inherent_Color_identity(color/1: Color) -> Color {
  color/1
}

fn main() -> unit {
  let point/2 = Point { x: 1, y: 2 } in
  let mtmp0 = sum(point/2) in
  string_println("()")
}