fn main() -> unit {
  let c/0 = 'A' in
  let t2 = char_to_string(c/0) in
  let _wild1 = string_println(t2) in
  let d/1 = 'b' in
  let out/2 = match d/1 {
    'a' => {
      "A"
    },
    'b' => {
      "B"
    },
    _ => "?",
  } in
  let _wild0 = string_println(out/2) in
  ()
}