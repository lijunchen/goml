fn main() -> unit {
  let c/0 = 'A' in
  let t2 = char_to_string(c/0) in
  let _wild0 = string_println(t2) in
  let d/1 = 'b' in
  join k3(jp4: string) -> unit {
    let out/2 = jp4 in
    let _wild1 = string_println(out/2) in
    ()
  } in
  match d/1 {
    'a' => {
      jump k3("A")
    },
    'b' => {
      jump k3("B")
    },
    _ => {
      jump k3("?")
    },
  }
}