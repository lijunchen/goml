fn main() -> unit {
  join ret1() -> unit {
    ()
  } in
  let x/0 = 5 in
  join k2(jp3: string) -> unit {
    let y/1 = jp3 in
    let _wild0 = string_println(y/1) in
    jump ret1()
  } in
  match x/0 {
    0 => {
      jump k2("zero")
    },
    1 => {
      jump k2("one")
    },
    _ => {
      jump k2("other")
    },
  }
}