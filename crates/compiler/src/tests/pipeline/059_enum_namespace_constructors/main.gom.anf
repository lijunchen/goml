fn color_is_red(color/0: Color) -> bool {
  join ret59(retv60: bool) -> bool {
    retv60
  } in
  join k61(jp62: bool) -> bool {
    jump ret59(jp62)
  } in
  match color/0 {
    Tag_0 => {
      jump k61(true)
    },
    Tag_1 => {
      jump k61(false)
    },
  }
}

fn toggle_signal(signal/1: Signal) -> Signal {
  join ret63(retv64: Signal) -> Signal {
    retv64
  } in
  join k65(jp66: Signal) -> Signal {
    jump ret63(jp66)
  } in
  match signal/1 {
    Tag_0 => {
      jump k65(Tag_1)
    },
    Tag_1 => {
      jump k65(Tag_0)
    },
  }
}

fn main() -> Signal {
  join ret67(retv68: Signal) -> Signal {
    retv68
  } in
  let current/2 = Tag_0 in
  let mtmp58 = color_is_red(current/2) in
  let t69 = toggle_signal(Tag_0) in
  jump ret67(t69)
}