fn describe(path/0: string) -> string {
  join ret4(retv5: string) -> string {
    retv5
  } in
  let mtmp0 = open_reader(path/0) in
  join k6(jp7: string) -> string {
    jump ret4(jp7)
  } in
  match mtmp0 {
    Tag_0 => {
      let x1 = Result__Reader__GoError::Ok._0(mtmp0) in
      let reader/1 = x1 in
      let t8 = format_reader("type=%T", reader/1) in
      jump k6(t8)
    },
    Tag_1 => {
      let x2 = Result__Reader__GoError::Err._0(mtmp0) in
      let err/2 = x2 in
      let t9 = go_error_to_string(err/2) in
      let t10 = ("err=" + t9) in
      jump k6(t10)
    },
  }
}

fn main() -> unit {
  join ret11() -> unit {
    ()
  } in
  let t12 = describe("/etc/hosts") in
  let _wild3 = println__T_string(t12) in
  jump ret11()
}

fn println__T_string(value/1: string) -> unit {
  join ret13() -> unit {
    ()
  } in
  let t14 = string_println(value/1) in
  jump ret13()
}