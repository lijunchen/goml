struct Shape {  }

impl Shape {
  fn item_27(local_0: ()) -> string {
    "Shape"
  }
  fn item_28(local_0: (), local_1: string) -> string {
    local_0.name() + local_1
  }
  fn item_29(local_0: (), local_1: string, local_2: string) -> string {
    local_1 + local_0.name() + local_2
  }
}

fn announce(local_0: Shape) -> () {
  let local_1 = local_0.name() in
  let local_2 = local_0.rename("!") in
  let local_3 = local_0.join(local_1, local_2) in
  let _ = string_println(local_3) in
  ()
}

fn main() -> () {
  let local_0 = Shape {  } in
  let _ = announce(local_0) in
  ()
}