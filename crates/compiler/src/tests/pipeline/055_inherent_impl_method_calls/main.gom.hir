package Main
file main.gom
  struct Shape {
    }
    
    impl Shape {
      fn name(local/1/0: Self) -> string {
        "Shape";
      }
      fn rename(local/1/1: Self, local/1/2: string) -> string {
        self/1.name() + suffix/2;
      }
      fn join(local/1/3: Self, local/1/4: string, local/1/5: string) -> string {
        left/4 + self/3.name() + right/5;
      }
    }
    
    fn announce(local/1/6: Shape) -> unit {
      let local/1/7 = shape/6.name();
      let local/1/8 = shape/6.rename("!");
      let local/1/9 = shape/6.join(base/7, with_suffix/8);
      let _ = string_println(combined/9);
      ();
    }
    
    fn main() -> unit {
      let local/1/10 = Shape {};
      def/1/2(shape/10);
      ();
    }