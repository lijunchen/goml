fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  join ret9(retv10: string) -> string {
    retv10
  } in
  jump ret9("Shape")
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join ret11(retv12: string) -> string {
    retv12
  } in
  let t13 = inherent#Shape#Shape#name(self/1) in
  let t14 = (t13 + suffix/2) in
  jump ret11(t14)
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join ret15(retv16: string) -> string {
    retv16
  } in
  let t17 = inherent#Shape#Shape#name(self/3) in
  let t18 = (left/4 + t17) in
  let t19 = (t18 + right/5) in
  jump ret15(t19)
}

fn announce(shape/6: Shape) -> unit {
  join ret20() -> unit {
    ()
  } in
  let base/7 = inherent#Shape#Shape#name(shape/6) in
  let with_suffix/8 = inherent#Shape#Shape#rename(shape/6, "!") in
  let combined/9 = inherent#Shape#Shape#join(shape/6, base/7, with_suffix/8) in
  let mtmp7 = println__T_string(combined/9) in
  jump ret20()
}

fn main() -> unit {
  join ret21() -> unit {
    ()
  } in
  let shape/10 = Shape {} in
  let _wild8 = announce(shape/10) in
  jump ret21()
}

fn println__T_string(value/1: string) -> unit {
  join ret22() -> unit {
    ()
  } in
  let t23 = trait_impl#ToString#string#to_string(value/1) in
  let t24 = @runtime(core.string_println)(t23) in
  jump ret22()
}

fn trait_impl#ToString#string#to_string(self/9: string) -> string {
  join ret25(retv26: string) -> string {
    retv26
  } in
  jump ret25(self/9)
}