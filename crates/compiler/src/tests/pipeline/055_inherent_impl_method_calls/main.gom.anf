fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  join ret60(retv61: string) -> string {
    retv61
  } in
  jump ret60("Shape")
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join ret62(retv63: string) -> string {
    retv63
  } in
  let t64 = inherent#Shape#Shape#name(self/1) in
  let t65 = (t64 + suffix/2) in
  jump ret62(t65)
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join ret66(retv67: string) -> string {
    retv67
  } in
  let t68 = inherent#Shape#Shape#name(self/3) in
  let t69 = (left/4 + t68) in
  let t70 = (t69 + right/5) in
  jump ret66(t70)
}

fn announce(shape/6: Shape) -> unit {
  join ret71() -> unit {
    ()
  } in
  let base/7 = inherent#Shape#Shape#name(shape/6) in
  let with_suffix/8 = inherent#Shape#Shape#rename(shape/6, "!") in
  let combined/9 = inherent#Shape#Shape#join(shape/6, base/7, with_suffix/8) in
  let mtmp58 = println__T_string(combined/9) in
  jump ret71()
}

fn main() -> unit {
  join ret72() -> unit {
    ()
  } in
  let shape/10 = Shape {} in
  let _wild59 = announce(shape/10) in
  jump ret72()
}

fn println__T_string(value/1: string) -> unit {
  join ret73() -> unit {
    ()
  } in
  let t74 = trait_impl#ToString#string#to_string(value/1) in
  let t75 = @runtime(core.string_println)(t74) in
  jump ret73()
}

fn trait_impl#ToString#string#to_string(self/34: string) -> string {
  join ret76(retv77: string) -> string {
    retv77
  } in
  jump ret76(self/34)
}