fn inherent#Shape#Shape#name(self/0: Shape) -> string {
  join ret6(retv7: string) -> string {
    retv7
  } in
  jump ret6("Shape")
}

fn inherent#Shape#Shape#rename(self/1: Shape, suffix/2: string) -> string {
  join ret8(retv9: string) -> string {
    retv9
  } in
  let t10 = inherent#Shape#Shape#name(self/1) in
  let t11 = (t10 + suffix/2) in
  jump ret8(t11)
}

fn inherent#Shape#Shape#join(self/3: Shape, left/4: string, right/5: string) -> string {
  join ret12(retv13: string) -> string {
    retv13
  } in
  let t14 = inherent#Shape#Shape#name(self/3) in
  let t15 = (left/4 + t14) in
  let t16 = (t15 + right/5) in
  jump ret12(t16)
}

fn announce(shape/6: Shape) -> unit {
  join ret17() -> unit {
    ()
  } in
  let base/7 = inherent#Shape#Shape#name(shape/6) in
  let with_suffix/8 = inherent#Shape#Shape#rename(shape/6, "!") in
  let combined/9 = inherent#Shape#Shape#join(shape/6, base/7, with_suffix/8) in
  let mtmp4 = println__T_string(combined/9) in
  jump ret17()
}

fn main() -> unit {
  join ret18() -> unit {
    ()
  } in
  let shape/10 = Shape {} in
  let _wild5 = announce(shape/10) in
  jump ret18()
}

fn println__T_string(value/1: string) -> unit {
  join ret19() -> unit {
    ()
  } in
  let t20 = trait_impl#ToString#string#to_string(value/1) in
  let t21 = @runtime(core.string_println)(t20) in
  jump ret19()
}

fn trait_impl#ToString#string#to_string(self/9: string) -> string {
  join ret22(retv23: string) -> string {
    retv23
  } in
  jump ret22(self/9)
}