fn main() {
    let a = 10;
    let f1 = |x: int| {
        let b = 20;
        let f2 = |y: int| {
            let c = 30;
            let f3 = |z: int| {
                let d = 40;
                let f4 = |w: int| {
                    a + b + c + d + x + y + z + w
                };
                f4(4)
            };
            f3(3)
        };
        f2(2)
    };
    let result = f1(1);
    string_println(int_to_string(result))
}
