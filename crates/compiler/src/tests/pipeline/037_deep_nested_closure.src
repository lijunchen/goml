fn main() {
    let a = 10 in
    let f1 = |x: int| {
        let b = 20 in
        let f2 = |y: int| {
            let c = 30 in
            let f3 = |z: int| {
                let d = 40 in
                let f4 = |w: int| {
                    a + b + c + d + x + y + z + w
                } in
                f4(4)
            } in
            f3(3)
        } in
        f2(2)
    } in
    let result = f1(1) in
    string_println(int_to_string(result))
}
