fn is_flag8(value/0: uint8) -> bool {
  join ret64(retv65: bool) -> bool {
    retv65
  } in
  join k66(jp67: bool) -> bool {
    jump ret64(jp67)
  } in
  match value/0 {
    0 => {
      jump k66(true)
    },
    200 => {
      jump k66(true)
    },
    _ => {
      jump k66(false)
    },
  }
}

fn is_flag16(value/1: uint16) -> bool {
  join ret68(retv69: bool) -> bool {
    retv69
  } in
  join k70(jp71: bool) -> bool {
    jump ret68(jp71)
  } in
  match value/1 {
    1024 => {
      jump k70(true)
    },
    65000 => {
      jump k70(true)
    },
    _ => {
      jump k70(false)
    },
  }
}

fn is_flag32(value/2: uint32) -> bool {
  join ret72(retv73: bool) -> bool {
    retv73
  } in
  join k74(jp75: bool) -> bool {
    jump ret72(jp75)
  } in
  match value/2 {
    4000000000 => {
      jump k74(true)
    },
    1234567890 => {
      jump k74(true)
    },
    _ => {
      jump k74(false)
    },
  }
}

fn is_flag64(value/3: uint64) -> bool {
  join ret76(retv77: bool) -> bool {
    retv77
  } in
  join k78(jp79: bool) -> bool {
    jump ret76(jp79)
  } in
  match value/3 {
    900000000 => {
      jump k78(true)
    },
    600000000 => {
      jump k78(true)
    },
    _ => {
      jump k78(false)
    },
  }
}

fn match_struct(counter/4: Counter) -> bool {
  join ret80(retv81: bool) -> bool {
    retv81
  } in
  let x61 = Counter.start(counter/4) in
  let x62 = Counter.end(counter/4) in
  join k82(jp83: bool) -> bool {
    jump ret80(jp83)
  } in
  match x62 {
    900000000 => {
      join k84(jp85: bool) -> bool {
        jump k82(jp85)
      } in
      match x61 {
        4000000000 => {
          jump k84(true)
        },
        _ => {
          jump k84(false)
        },
      }
    },
    600000000 => {
      jump k82(true)
    },
    _ => {
      jump k82(false)
    },
  }
}

fn report(label/5: string, value/6: bool) -> string {
  join ret86(retv87: string) -> string {
    retv87
  } in
  let t88 = trait_impl#ToString#bool#to_string(value/6) in
  let t89 = (label/5 + t88) in
  jump ret86(t89)
}

fn main() -> unit {
  join ret90() -> unit {
    ()
  } in
  let counter/7 = Counter { start: 4000000000, end: 900000000 } in
  let alt_counter/8 = Counter { start: 12, end: 600000000 } in
  let t91 = is_flag8(200) in
  let t92 = report("u8_hit=", t91) in
  let t93 = is_flag8(15) in
  let t94 = report(",u8_miss=", t93) in
  let t95 = (t92 + t94) in
  let t96 = is_flag16(65000) in
  let t97 = report(",u16_hit=", t96) in
  let t98 = (t95 + t97) in
  let t99 = is_flag16(42) in
  let t100 = report(",u16_miss=", t99) in
  let t101 = (t98 + t100) in
  let t102 = is_flag32(1234567890) in
  let t103 = report(",u32_hit=", t102) in
  let t104 = (t101 + t103) in
  let t105 = is_flag32(99) in
  let t106 = report(",u32_miss=", t105) in
  let t107 = (t104 + t106) in
  let t108 = is_flag64(900000000) in
  let t109 = report(",u64_hit=", t108) in
  let t110 = (t107 + t109) in
  let t111 = is_flag64(700000000) in
  let t112 = report(",u64_miss=", t111) in
  let t113 = (t110 + t112) in
  let t114 = match_struct(counter/7) in
  let t115 = report(",struct_first=", t114) in
  let t116 = (t113 + t115) in
  let t117 = match_struct(alt_counter/8) in
  let t118 = report(",struct_second=", t117) in
  let message/9 = (t116 + t118) in
  let mtmp63 = println__T_string(message/9) in
  jump ret90()
}

fn trait_impl#ToString#bool#to_string(self/36: bool) -> string {
  join ret119(retv120: string) -> string {
    retv120
  } in
  let t121 = @runtime(core.bool_to_string)(self/36) in
  jump ret119(t121)
}

fn println__T_string(value/1: string) -> unit {
  join ret122() -> unit {
    ()
  } in
  let t123 = trait_impl#ToString#string#to_string(value/1) in
  let t124 = @runtime(core.string_println)(t123) in
  jump ret122()
}

fn trait_impl#ToString#string#to_string(self/37: string) -> string {
  join ret125(retv126: string) -> string {
    retv126
  } in
  jump ret125(self/37)
}