package main;
struct Values {
}

impl Iterator for Values {
  type Item = int32;
  fn next(self: Values) -> Option[int32] {
      Option::None
  }
}

fn main() -> unit {
    for value in Values {} {
        string_println(value.to_string());
    };
}

