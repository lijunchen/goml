fn fib(x/0: int) -> int {
  match (int_less : (int, int) -> bool)((x/0 : int), 2) {
      false => (fib : (int) -> int)((x/0 : int) - 1) + (fib : (int) -> int)((x/0 : int) - 2),
      true => 1,
  }
}

fn main() -> unit {
  (string_print : (string) -> unit)((int_to_string : (int) -> string)((fib : (int) -> int)(10)))
}