fn classify(x/0: int32) -> string {
  join ret64(retv65: string) -> string {
    retv65
  } in
  let t68 = (x/0 < 0) in
  join k66(jp67: string) -> string {
    jump ret64(jp67)
  } in
  if t68 {
    jump k66("negative")
  } else {
    let t71 = (0 < x/0) in
    join k69(jp70: string) -> string {
      jump k66(jp70)
    } in
    if t71 {
      jump k69("positive")
    } else {
      jump k69("zero")
    }
  }
}

fn triangle_type(a/1: int32, b/2: int32, c/3: int32) -> string {
  join ret72(retv73: string) -> string {
    retv73
  } in
  let t76 = (a/1 < b/2) in
  join k74(jp75: string) -> string {
    jump ret72(jp75)
  } in
  if t76 {
    let t79 = (b/2 < c/3) in
    join k77(jp78: string) -> string {
      jump k74(jp78)
    } in
    if t79 {
      jump k77("ascending")
    } else {
      jump k77("peak")
    }
  } else {
    let t82 = (a/1 < c/3) in
    join k80(jp81: string) -> string {
      jump k74(jp81)
    } in
    if t82 {
      jump k80("valley")
    } else {
      jump k80("flat")
    }
  }
}

fn main() -> unit {
  join ret83() -> unit {
    ()
  } in
  let first/4 = classify(-42) in
  let second/5 = classify(0) in
  let third/6 = classify(17) in
  let shape1/7 = triangle_type(1, 2, 3) in
  let shape2/8 = triangle_type(3, 2, 1) in
  let shape3/9 = triangle_type(2, 3, 2) in
  let mtmp58 = println__T_string(first/4) in
  let mtmp59 = println__T_string(second/5) in
  let mtmp60 = println__T_string(third/6) in
  let mtmp61 = println__T_string(shape1/7) in
  let mtmp62 = println__T_string(shape2/8) in
  let mtmp63 = println__T_string(shape3/9) in
  jump ret83()
}

fn println__T_string(value/1: string) -> unit {
  join ret84() -> unit {
    ()
  } in
  let t85 = trait_impl#ToString#string#to_string(value/1) in
  let t86 = @runtime(core.string_println)(t85) in
  jump ret84()
}

fn trait_impl#ToString#string#to_string(self/34: string) -> string {
  join ret87(retv88: string) -> string {
    retv88
  } in
  jump ret87(self/34)
}