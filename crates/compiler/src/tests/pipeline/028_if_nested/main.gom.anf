fn classify(x/0: int32) -> string {
  join ret67(retv68: string) -> string {
    retv68
  } in
  let t71 = (x/0 < 0) in
  join k69(jp70: string) -> string {
    jump ret67(jp70)
  } in
  if t71 {
    jump k69("negative")
  } else {
    let t74 = (0 < x/0) in
    join k72(jp73: string) -> string {
      jump k69(jp73)
    } in
    if t74 {
      jump k72("positive")
    } else {
      jump k72("zero")
    }
  }
}

fn triangle_type(a/1: int32, b/2: int32, c/3: int32) -> string {
  join ret75(retv76: string) -> string {
    retv76
  } in
  let t79 = (a/1 < b/2) in
  join k77(jp78: string) -> string {
    jump ret75(jp78)
  } in
  if t79 {
    let t82 = (b/2 < c/3) in
    join k80(jp81: string) -> string {
      jump k77(jp81)
    } in
    if t82 {
      jump k80("ascending")
    } else {
      jump k80("peak")
    }
  } else {
    let t85 = (a/1 < c/3) in
    join k83(jp84: string) -> string {
      jump k77(jp84)
    } in
    if t85 {
      jump k83("valley")
    } else {
      jump k83("flat")
    }
  }
}

fn main() -> unit {
  join ret86() -> unit {
    ()
  } in
  let first/4 = classify(-42) in
  let second/5 = classify(0) in
  let third/6 = classify(17) in
  let shape1/7 = triangle_type(1, 2, 3) in
  let shape2/8 = triangle_type(3, 2, 1) in
  let shape3/9 = triangle_type(2, 3, 2) in
  let mtmp61 = println__T_string(first/4) in
  let mtmp62 = println__T_string(second/5) in
  let mtmp63 = println__T_string(third/6) in
  let mtmp64 = println__T_string(shape1/7) in
  let mtmp65 = println__T_string(shape2/8) in
  let mtmp66 = println__T_string(shape3/9) in
  jump ret86()
}

fn println__T_string(value/1: string) -> unit {
  join ret87() -> unit {
    ()
  } in
  let t88 = trait_impl#ToString#string#to_string(value/1) in
  let t89 = @runtime(core.string_println)(t88) in
  jump ret87()
}

fn trait_impl#ToString#string#to_string(self/37: string) -> string {
  join ret90(retv91: string) -> string {
    retv91
  } in
  jump ret90(self/37)
}