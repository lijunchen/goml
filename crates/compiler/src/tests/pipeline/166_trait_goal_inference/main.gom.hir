package main
file main.gom
  trait Measure {
      fn measure(Self) -> int32;
    }
    
    impl Measure for Vec[int32] {
      fn measure(local/1/0: Vec[int32]) -> int32 {
          self/0.len()
      }
    }
    
    fn read_measure(local/1/1: T) -> int32 {
        Measure::measure(value/1)
    }
    
    fn main() -> unit {
        let local/1/2 = vec_new();
        println(def/1/2(values/2).to_string())
    }