fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn describe(path/0: string) -> Result[string, GoError] {
  let file/1 = {
    let mtmp0 = open_file(path/0) in
    match mtmp0 {
      Result::Ok(x1) => {
        {
          let x1 = Result::Ok._0(mtmp0) in
          {
            let try_value/4 = x1 in
            try_value/4
          }
        }
      },
      Result::Err(x2) => {
        {
          let x2 = Result::Err._0(mtmp0) in
          {
            let try_residual/4 = x2 in
            return Result::Err(try_residual/4)
          }
        }
      },
    }
  } in
  let name/2 = file_name(file/1) in
  let _wild6 = {
    let mtmp3 = close_file(file/1) in
    match mtmp3 {
      Result::Ok(x4) => {
        {
          let x4 = Result::Ok._0(mtmp3) in
          {
            let try_value/11 = x4 in
            try_value/11
          }
        }
      },
      Result::Err(x5) => {
        {
          let x5 = Result::Err._0(mtmp3) in
          {
            let try_residual/11 = x5 in
            return Result::Err(try_residual/11)
          }
        }
      },
    }
  } in
  Result::Ok(("ok=" + name/2))
}

fn show(res/3: Result[string, GoError]) -> string {
  match res/3 {
    Result::Ok(x7) => {
      {
        let x7 = Result::Ok._0(res/3) in
        {
          let value/4 = x7 in
          value/4
        }
      }
    },
    Result::Err(x8) => {
      {
        let x8 = Result::Err._0(res/3) in
        {
          let err/5 = x8 in
          ("err=" + trait_call[ToString::to_string](err/5))
        }
      }
    },
  }
}

fn main() -> unit {
  let _wild9 = println(show(describe("/etc/hosts"))) in
  let _wild10 = println(show(describe("/definitely/missing"))) in
}