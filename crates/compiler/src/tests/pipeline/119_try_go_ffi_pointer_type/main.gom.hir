package main
file main.gom
  extern "go" "os" "*File" type File
    
    #[go_error_last]
extern "go" "os" "Open" open_file(path: string) -> Result[File, GoError]
    
    extern "go" "os" "(*File).Name" file_name(file: File) -> string
    
    #[go_error]
extern "go" "os" "(*File).Close" close_file(file: File) -> Result[unit, GoError]
    
    fn describe(local/1/0: string) -> Result[string, GoError] {
        let local/1/1 = def/1/1(path/0)?;
        let local/1/2 = def/1/2(file/1);
        def/1/3(file/1)?;
        Result::Ok("ok=" + name/2)
    }
    
    fn show(local/1/3: Result[string, GoError]) -> string {
        match res/3 {
            Result::Ok(local/1/4) => value/4,
            Result::Err(local/1/5) => "err=" + err/5.to_string(),
        }
    }
    
    fn main() -> unit {
        println(def/1/5(def/1/4("/etc/hosts")));
        println(def/1/5(def/1/4("/definitely/missing")));
    }