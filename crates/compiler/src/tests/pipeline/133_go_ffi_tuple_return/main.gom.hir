package main
file main.gom
  extern "go" "strings" "Cut" cut(s: string, sep: string) -> (string, string, bool)
    
    fn describe(local/1/0: string) -> string {
        let (local/1/1, local/1/2, local/1/3) = def/1/0(input/0, ":");
        if found/3 {
            before/1 + "|" + after/2
        } else {
            "missing"
        }
    }
    
    fn main() {
        println(def/1/1("left:right"));
        println(def/1/1("plain"));
    }