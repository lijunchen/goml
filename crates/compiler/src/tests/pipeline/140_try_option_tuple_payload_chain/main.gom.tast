fn print(value/0: T) -> unit {
    (string_print : (string) -> unit)((ToString::to_string : (T) -> string)((value/0 : T)))
}

fn println(value/1: T) -> unit {
    (string_println : (string) -> unit)((ToString::to_string : (T) -> string)((value/1 : T)))
}

fn cut_pair(ok/0: bool) -> Option[(string, string)] {
    if (ok/0 : bool) {
        {
            Option::Some(("left", "right"))
        }
    } else {
        {
            Option::None
        }
    }
}

fn pair(ok/1: bool) -> Option[(string, string)] {
    (cut_pair : (bool) -> Option[(string, string)])((ok/1 : bool))
}

fn describe(ok/2: bool) -> Option[string] {
    let (before/3: string, after/4: string) = match (pair : (bool) -> Option[(string, string)])((ok/2 : bool)) {
        Option::Some(try_value/16: (string, string)) => (try_value/16 : (string, string)),
        Option::None => return Option::None,
    };
    Option::Some((before/3 : string) + ":" + (after/4 : string))
}

fn show(opt/5: Option[string]) -> string {
    match (opt/5 : Option[string]) {
        Option::Some(value/6: string) => "some " + (value/6 : string),
        Option::None => "none",
    }
}

fn main() -> unit {
    (println : (string) -> unit)((show : (Option[string]) -> string)((describe : (bool) -> Option[string])(true)));
    (println : (string) -> unit)((show : (Option[string]) -> string)((describe : (bool) -> Option[string])(false)));
}