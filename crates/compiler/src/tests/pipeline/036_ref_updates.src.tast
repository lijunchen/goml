fn main() -> unit {
  let reference/0: ref int = ref10 in
  let current/1: int = !(reference/0 : ref int) in
  let _ : unit = (reference/0 : ref int) = (current/1 : int) + 5 in
  let _ : unit = (string_println : (string) -> unit)((int_to_string : (int) -> string)(!(reference/0 : ref int))) in
  ()
}