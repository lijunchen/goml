fn main() {
    let poem = \\roses are red
        \\violets are blue
        \\"quotes" stay quoted
        \\backslash \\\\ stays too
    ;

    let trailing_blank = \\line one
        \\
        \\line three
    ;

    string_println(poem);
    string_println(trailing_blank);
}
