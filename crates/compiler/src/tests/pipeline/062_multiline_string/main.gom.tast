fn main() -> unit {
  let poem/0: string = "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
  let trailing_blank/1: string = "line one\n\nline three" in
  let _ : unit = (string_println : (string) -> unit)((poem/0 : string)) in
  let _ : unit = (string_println : (string) -> unit)((trailing_blank/1 : string)) in
  ()
}