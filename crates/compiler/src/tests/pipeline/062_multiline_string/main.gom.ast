fn main() {
    let poem = "roses are red\nviolets are blue\n\"quotes\" stay quoted\nbackslash \\\\\\\\ stays too" in
    let trailing_blank = "line one\n\nline three" in
    let _ = string_println(poem) in
    let _ = string_println(trailing_blank) in
    ()
}

