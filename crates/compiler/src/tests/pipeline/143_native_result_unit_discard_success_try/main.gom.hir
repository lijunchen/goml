package main
file main.gom
  extern "go" "" "int" type GoInt
    
    #[go_error_last]
extern "go" "fmt" "Print" raw_print(text: string) -> Result[GoInt, GoError]
    
    fn touch(local/1/0: string) -> Result[unit, GoError] {
        let _ = def/1/1(text/0)?;
        Result::Ok(())
    }
    
    fn show(local/1/1: Result[unit, GoError]) -> string {
        match res/1 {
            Result::Ok(()) => "ok",
            Result::Err(local/1/2) => "err=" + err/2.to_string(),
        }
    }
    
    fn main() {
        println(def/1/3(def/1/2("hello")));
    }