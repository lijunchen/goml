package main
file main.gom
  #[go_option_last]
extern "go" "strings" "CutPrefix" cut_prefix(s: string, prefix: string) -> Option[string]
    
    fn describe(local/1/0: string, local/1/1: string) -> string {
        match def/1/0(input/0, prefix/1) {
            Option::Some(local/1/2) => "some=" + rest/2,
            Option::None => "none",
        }
    }
    
    fn main() -> unit {
        println(def/1/1("goml", "go"));
        println(def/1/1("goml", "ml"));
    }