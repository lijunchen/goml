package main
  imports std
file main.gom
  fn show_read(local/1/0: Result[string, string]) -> string {
        match res/0 {
            Result::Ok(local/1/1) => value/1,
            Result::Err(local/1/2) => "err " + err/2,
        }
    }
    
    fn show_dir(local/1/3: Result[Vec[string], string]) -> string {
        match res/3 {
            Result::Ok(local/1/4) => names/4.len() > 0.to_string(),
            Result::Err(local/1/5) => "err " + err/5,
        }
    }
    
    fn main() -> unit {
        let _ = std::fs::write_file("goml-std-test.txt", "std-ok");
        std::io::println(def/1/0(std::fs::read_file("goml-std-test.txt")));
        std::io::println(std::fs::exists("goml-std-test.txt").to_string());
        std::io::println(def/1/1(std::fs::read_dir(".")));
    }