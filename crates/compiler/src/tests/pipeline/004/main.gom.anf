fn main() -> bool {
  join ret67(retv68: bool) -> bool {
    retv68
  } in
  let a/0 = (Tag_2, Tag_2) in
  let x64 = a/0.0 in
  let x65 = a/0.1 in
  join k69(jp70: bool) -> bool {
    jump ret67(jp70)
  } in
  match x65 {
    Tag_0 => {
      join k71(jp72: bool) -> bool {
        jump k69(jp72)
      } in
      match x64 {
        Tag_0 => {
          jump k71(true)
        },
        _ => {
          jump k71(false)
        },
      }
    },
    Tag_1 => {
      join k73(jp74: bool) -> bool {
        jump k69(jp74)
      } in
      match x64 {
        Tag_0 => {
          jump k73(true)
        },
        _ => {
          jump k73(false)
        },
      }
    },
    Tag_2 => {
      join k75(jp76: bool) -> bool {
        jump k69(jp76)
      } in
      match x64 {
        Tag_2 => {
          let mtmp66 = print__T_bool(true) in
          jump k75(false)
        },
        _ => {
          jump k75(false)
        },
      }
    },
  }
}

fn print__T_bool(value/0: bool) -> unit {
  join ret77() -> unit {
    ()
  } in
  let t78 = trait_impl#ToString#bool#to_string(value/0) in
  let t79 = @runtime(core.string_print)(t78) in
  jump ret77()
}

fn trait_impl#ToString#bool#to_string(self/37: bool) -> string {
  join ret80(retv81: string) -> string {
    retv81
  } in
  let t82 = @runtime(core.bool_to_string)(self/37) in
  jump ret80(t82)
}