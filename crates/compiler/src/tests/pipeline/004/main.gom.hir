enum Color { Red, Green, Blue }

fn main() -> bool {
  let local_0 = (Color::Blue, Color::Blue) in
  match local_0 {
    (Color::Red, Color::Green) => true,
    (Color::Red, Color::Red) => true,
    (Color::Blue, Color::Blue) => let _ = string_print(bool_to_string(true)) in
    false,
    _ => false,
  }
}