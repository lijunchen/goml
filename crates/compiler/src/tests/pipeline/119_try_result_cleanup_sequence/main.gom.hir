package main
file main.gom
  struct Handle {
        name: string,
    }
    
    fn open_handle(local/1/0: bool) -> Result[Handle, string] {
        if ok/0 {
            Result::Ok(Handle {
                name: "config"
            })
        } else {
            Result::Err("open failed")
        }
    }
    
    fn close_handle(local/1/1: Handle, local/1/2: bool) -> Result[unit, string] {
        if ok/2 {
            Result::Ok(())
        } else {
            Result::Err("close failed for " + handle/1.name)
        }
    }
    
    fn use_handle(local/1/3: bool, local/1/4: bool) -> Result[string, string] {
        let local/1/5 = def/1/1(open_ok/3)?;
        let local/1/6 = handle/5.name;
        def/1/2(handle/5, close_ok/4)?;
        Result::Ok("closed " + name/6)
    }
    
    fn show(local/1/7: Result[string, string]) -> string {
        match res/7 {
            Result::Ok(local/1/8) => "ok " + value/8,
            Result::Err(local/1/9) => "err " + err/9,
        }
    }
    
    fn main() -> unit {
        println(def/1/4(def/1/3(true, true)));
        println(def/1/4(def/1/3(false, true)));
        println(def/1/4(def/1/3(true, false)));
    }