package main
file main.gom
  extern "go" "time" "Duration" type Duration
    
    #[go_error_last]
extern "go" "time" "ParseDuration" parse_duration(s: string) -> Result[Duration, GoError]
    
    #[go_error]
extern "go" "os" "Setenv" setenv(key: string, value: string) -> Result[unit, GoError]
    
    extern "go" "fmt" "Sprintf" format_duration(format: string, value: Duration) -> string
    
    fn configure_and_format(local/1/0: string) -> Result[string, GoError] {
        def/1/2("GOML_TRY_FFI", "ok")?;
        let local/1/1 = def/1/1(input/0)?;
        Result::Ok(def/1/3("dur=%v", value/1))
    }
    
    fn show(local/1/2: Result[string, GoError]) -> string {
        match res/2 {
            Result::Ok(local/1/3) => value/3,
            Result::Err(local/1/4) => "err=" + err/4.to_string(),
        }
    }
    
    fn main() -> unit {
        println(def/1/5(def/1/4("2s")));
        println(def/1/5(def/1/4("bad")));
    }