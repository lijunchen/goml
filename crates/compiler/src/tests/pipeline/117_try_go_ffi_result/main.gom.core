fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn configure_and_format(input/0: string) -> Result[string, GoError] {
  let _wild3 = {
    let mtmp0 = setenv("GOML_TRY_FFI", "ok") in
    match mtmp0 {
      Result::Ok(x1) => {
        {
          let x1 = Result::Ok._0(mtmp0) in
          {
            let try_value/5 = x1 in
            try_value/5
          }
        }
      },
      Result::Err(x2) => {
        {
          let x2 = Result::Err._0(mtmp0) in
          {
            let try_residual/5 = x2 in
            return Result::Err(try_residual/5)
          }
        }
      },
    }
  } in
  let value/1 = {
    let mtmp4 = parse_duration(input/0) in
    match mtmp4 {
      Result::Ok(x5) => {
        {
          let x5 = Result::Ok._0(mtmp4) in
          {
            let try_value/9 = x5 in
            try_value/9
          }
        }
      },
      Result::Err(x6) => {
        {
          let x6 = Result::Err._0(mtmp4) in
          {
            let try_residual/9 = x6 in
            return Result::Err(try_residual/9)
          }
        }
      },
    }
  } in
  Result::Ok(format_duration("dur=%v", value/1))
}

fn show(res/2: Result[string, GoError]) -> string {
  match res/2 {
    Result::Ok(x7) => {
      {
        let x7 = Result::Ok._0(res/2) in
        {
          let value/3 = x7 in
          value/3
        }
      }
    },
    Result::Err(x8) => {
      {
        let x8 = Result::Err._0(res/2) in
        {
          let err/4 = x8 in
          ("err=" + trait_call[ToString::to_string](err/4))
        }
      }
    },
  }
}

fn main() -> unit {
  let _wild9 = println(show(configure_and_format("2s"))) in
  let _wild10 = println(show(configure_and_format("bad"))) in
}