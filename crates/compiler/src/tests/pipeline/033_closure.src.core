fn main() -> unit {
  let base/0 = 5 in
  let add_base/2 = |x/1: int| => int_add(x/1, base/0) in
  let result/3 = add_base/2(7) in
  let printer/7 = |prefix/4: string, value/5: int| => let message/6 = string_add(prefix/4, int_to_string(value/5)) in
  string_println(message/6) in
  let mtmp0 = printer/7("result: ", result/3) in
  let unused/9 = |y/8: int| => int_add(y/8, result/3) in
  let no_capture/11 = |z/10: int| => int_mul(z/10, 2) in
  let doubled/12 = no_capture/11(3) in
  let mtmp1 = string_println(int_to_string(doubled/12)) in
  ()
}