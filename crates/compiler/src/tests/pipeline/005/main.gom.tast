fn main() -> unit {
    let a/0: (Color, Color) = (Color::Blue, Color::Red);
    match (a/0 : (Color, Color)) {
        (Color::Red, Color::Green) => (print : (int) -> unit)(0),
        (Color::Red, Color::Red) => (print : (int) -> unit)(1),
        (Color::Blue, Color::Blue) => (print : (int) -> unit)(2),
        _ : (Color, Color) => (print : (int) -> unit)(3),
    }
}