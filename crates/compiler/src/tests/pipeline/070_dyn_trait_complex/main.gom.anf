fn trait_impl#Display#Point#show(self/0: Point) -> string {
  let t88 = Point.x(self/0) in
  let t87 = int32_to_string(t88) in
  let t86 = ("Point(" + t87) in
  let t85 = (t86 + ",") in
  let t90 = Point.y(self/0) in
  let t89 = int32_to_string(t90) in
  let t84 = (t85 + t89) in
  (t84 + ")")
}

fn trait_impl#Display#Point#show_with(self/1: Point, prefix/2: string, suffix/3: string) -> string {
  let t95 = (prefix/2 + "Point(") in
  let t97 = Point.x(self/1) in
  let t96 = int32_to_string(t97) in
  let t94 = (t95 + t96) in
  let t93 = (t94 + ",") in
  let t99 = Point.y(self/1) in
  let t98 = int32_to_string(t99) in
  let t92 = (t93 + t98) in
  let t91 = (t92 + ")") in
  (t91 + suffix/3)
}

fn trait_impl#Display#Point#tick(self/4: Point) -> unit {
  ()
}

fn trait_impl#Display#Point#bump(self/5: Point, delta/6: int32) -> int32 {
  let t101 = Point.x(self/5) in
  let t102 = Point.y(self/5) in
  let t100 = (t101 + t102) in
  (t100 + delta/6)
}

fn trait_impl#Display#Flag#show(self/7: Flag) -> string {
  let t103 = Flag.value(self/7) in
  if t103 {
    "Flag(true)"
  } else {
    "Flag(false)"
  }
}

fn trait_impl#Display#Flag#show_with(self/8: Flag, prefix/9: string, suffix/10: string) -> string {
  let t104 = Flag.value(self/8) in
  if t104 {
    let t105 = (prefix/9 + "Flag(true)") in
    (t105 + suffix/10)
  } else {
    let t106 = (prefix/9 + "Flag(false)") in
    (t106 + suffix/10)
  }
}

fn trait_impl#Display#Flag#tick(self/11: Flag) -> unit {
  ()
}

fn trait_impl#Display#Flag#bump(self/12: Flag, delta/13: int32) -> int32 {
  let t107 = Flag.value(self/12) in
  if t107 {
    delta/13
  } else {
    (-delta/13)
  }
}

fn trait_impl#Display#Counter#show(self/14: Counter) -> string {
  let t111 = Counter.cell(self/14) in
  let t110 = ref_get(t111) in
  let t109 = int32_to_string(t110) in
  let t108 = ("Counter(" + t109) in
  (t108 + ")")
}

fn trait_impl#Display#Counter#show_with(self/15: Counter, prefix/16: string, suffix/17: string) -> string {
  let t114 = (prefix/16 + "Counter(") in
  let t117 = Counter.cell(self/15) in
  let t116 = ref_get(t117) in
  let t115 = int32_to_string(t116) in
  let t113 = (t114 + t115) in
  let t112 = (t113 + ")") in
  (t112 + suffix/17)
}

fn trait_impl#Display#Counter#tick(self/18: Counter) -> unit {
  let t119 = Counter.cell(self/18) in
  let t118 = ref_get(t119) in
  let next/19 = (t118 + 1) in
  let t120 = Counter.cell(self/18) in
  let mtmp0 = ref_set(t120, next/19) in
  ()
}

fn trait_impl#Display#Counter#bump(self/20: Counter, delta/21: int32) -> int32 {
  let t122 = Counter.cell(self/20) in
  let t121 = ref_get(t122) in
  let next/22 = (t121 + delta/21) in
  let t123 = Counter.cell(self/20) in
  let mtmp1 = ref_set(t123, next/22) in
  next/22
}

fn show_dyn(x/23: dyn Display) -> string {
  dyn_call[Display::show_with](x/23, "<", ">")
}

fn call_via_closure(x/24: dyn Display, tag/25: string) -> string {
  let f/28 = closure_env_f_0 {} in
  inherent#closure_env_f_0#closure_env_f_0#apply(f/28, x/24, tag/25)
}

fn make_renderer(tag/29: string) -> closure_env_make_renderer_1 {
  closure_env_make_renderer_1 { tag_0: tag/29 }
}

fn bump_and_show(x/31: dyn Display, delta/32: int32) -> string {
  let mtmp2 = dyn_call[Display::tick](x/31) in
  let t125 = dyn_call[Display::show_with](x/31, "[", "]") in
  let t124 = (t125 + ":") in
  let t127 = dyn_call[Display::bump](x/31, delta/32) in
  let t126 = int32_to_string(t127) in
  (t124 + t126)
}

fn main() -> unit {
  let p1/33 = Point { x: 1, y: 2 } in
  let p2/34 = Point { x: 3, y: 4 } in
  let f1/35 = Flag { value: true } in
  let f2/36 = Flag { value: false } in
  let t128 = ref(10) in
  let c/37 = Counter { cell: t128 } in
  let dp1/38 = to_dyn[Display]{Point}(p1/33) in
  let dp2/39 = to_dyn[Display]{Point}(p2/34) in
  let df1/40 = to_dyn[Display]{Flag}(f1/35) in
  let df2/41 = to_dyn[Display]{Flag}(f2/36) in
  let dc/42 = to_dyn[Display]{Counter}(c/37) in
  let render_star/43 = make_renderer("*") in
  let render_angle/44 = make_renderer("<") in
  let s0/45 = show_dyn(dp2/39) in
  let s1/46 = call_via_closure(df2/41, "*") in
  let t130 = inherent#closure_env_make_renderer_1#closure_env_make_renderer_1#apply(render_star/43, dp1/38) in
  let t129 = (t130 + "|") in
  let t131 = inherent#closure_env_make_renderer_1#closure_env_make_renderer_1#apply(render_angle/44, df1/40) in
  let s2/47 = (t129 + t131) in
  let v/48 = vec_new() in
  let v/49 = vec_push(v/48, dp1/38) in
  let v/50 = vec_push(v/49, df1/40) in
  let v/51 = vec_push(v/50, dc/42) in
  let vlen/52 = vec_len(v/51) in
  let delta/53 = match vlen/52 {
    2 => {
      3
    },
    _ => 5,
  } in
  let mtmp42 = string_println(s0/45) in
  let mtmp62 = string_println(s1/46) in
  let mtmp72 = string_println(s2/47) in
  let i/54 = ref(0) in
  let mtmp78 = while let t132 = ref_get(i/54) in
  (t132 < 3) {
    let line/55 = bump_and_show(dc/42, delta/53) in
    let _wild77 = string_println(line/55) in
    let t134 = ref_get(i/54) in
    let t133 = (t134 + 1) in
    let mtmp76 = ref_set(i/54, t133) in
    ()
  } in
  let t136 = int32_to_string(vlen/52) in
  let t135 = ("len:" + t136) in
  let mtmp80 = string_println(t135) in
  let t138 = int32_to_string(delta/53) in
  let t137 = ("delta:" + t138) in
  let mtmp81 = string_println(t137) in
  ()
}

fn inherent#closure_env_f_0#closure_env_f_0#apply(env82: closure_env_f_0, v/26: dyn Display, t/27: string) -> string {
  dyn_call[Display::show_with](v/26, t/27, t/27)
}

fn inherent#closure_env_make_renderer_1#closure_env_make_renderer_1#apply(env83: closure_env_make_renderer_1, x/30: dyn Display) -> string {
  let tag/29 = closure_env_make_renderer_1.tag_0(env83) in
  dyn_call[Display::show_with](x/30, tag/29, tag/29)
}