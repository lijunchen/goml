fn print(value/0: T) -> unit {
    (string_print : (string) -> unit)((ToString::to_string : (T) -> string)((value/0 : T)))
}

fn println(value/1: T) -> unit {
    (string_println : (string) -> unit)((ToString::to_string : (T) -> string)((value/1 : T)))
}

fn split_host_port(ok/0: bool) -> Result[(string, string), string] {
    if (ok/0 : bool) {
        {
            Result::Ok(("localhost", "8080"))
        }
    } else {
        {
            Result::Err("missing port")
        }
    }
}

fn pair(ok/1: bool) -> Result[(string, string), string] {
    (split_host_port : (bool) -> Result[(string, string), string])((ok/1 : bool))
}

fn render(ok/2: bool) -> Result[string, string] {
    let (host/3: string, port/4: string) = match (pair : (bool) -> Result[(string, string), string])((ok/2 : bool)) {
        Result::Ok(try_value/17: (string, string)) => (try_value/17 : (string, string)),
        Result::Err(try_residual/17: string) => return Result::Err((try_residual/17 : string)),
    };
    Result::Ok((host/3 : string) + "=" + (port/4 : string))
}

fn show(res/5: Result[string, string]) -> string {
    match (res/5 : Result[string, string]) {
        Result::Ok(value/6: string) => "ok " + (value/6 : string),
        Result::Err(err/7: string) => "err " + (err/7 : string),
    }
}

fn main() -> unit {
    (println : (string) -> unit)((show : (Result[string, string]) -> string)((render : (bool) -> Result[string, string])(true)));
    (println : (string) -> unit)((show : (Result[string, string]) -> string)((render : (bool) -> Result[string, string])(false)));
}