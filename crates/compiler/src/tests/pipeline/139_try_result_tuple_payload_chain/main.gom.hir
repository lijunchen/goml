package main
file main.gom
  fn split_host_port(local/1/0: bool) -> Result[(string, string), string] {
        if ok/0 {
            Result::Ok(("localhost", "8080"))
        } else {
            Result::Err("missing port")
        }
    }
    
    fn pair(local/1/1: bool) -> Result[(string, string), string] {
        def/1/0(ok/1)
    }
    
    fn render(local/1/2: bool) -> Result[string, string] {
        let (local/1/3, local/1/4) = def/1/1(ok/2)?;
        Result::Ok(host/3 + "=" + port/4)
    }
    
    fn show(local/1/5: Result[string, string]) -> string {
        match res/5 {
            Result::Ok(local/1/6) => "ok " + value/6,
            Result::Err(local/1/7) => "err " + err/7,
        }
    }
    
    fn main() -> unit {
        println(def/1/3(def/1/2(true)));
        println(def/1/3(def/1/2(false)));
    }