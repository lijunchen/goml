fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn pair(text/0: string) -> Result[(string, string), GoError] {
  split_host_port(text/0)
}

fn render(text/1: string) -> Result[string, GoError] {
  let mtmp3 = {
    let mtmp0 = pair(text/1) in
    match mtmp0 {
      Result::Ok(x1) => {
        {
          let x1 = Result::Ok._0(mtmp0) in
          {
            let try_value/7 = x1 in
            try_value/7
          }
        }
      },
      Result::Err(x2) => {
        {
          let x2 = Result::Err._0(mtmp0) in
          {
            let try_residual/7 = x2 in
            return Result::Err(try_residual/7)
          }
        }
      },
    }
  } in
  {
    let x4 = mtmp3.0 in
    let x5 = mtmp3.1 in
    {
      let port/3 = x5 in
      {
        let host/2 = x4 in
        {
          Result::Ok(((host/2 + "|") + port/3))
        }
      }
    }
  }
}

fn show(res/4: Result[string, GoError]) -> string {
  match res/4 {
    Result::Ok(x6) => {
      {
        let x6 = Result::Ok._0(res/4) in
        {
          let text/5 = x6 in
          text/5
        }
      }
    },
    Result::Err(x7) => {
      {
        let x7 = Result::Err._0(res/4) in
        {
          let err/6 = x7 in
          ("err=" + trait_call[ToString::to_string](err/6))
        }
      }
    },
  }
}

fn main() -> unit {
  let _wild8 = println(show(render("example.com:443"))) in
}