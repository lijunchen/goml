package main
file main.gom
  #[go_error_last]
extern "go" "net" "SplitHostPort" split_host_port(text: string) -> Result[(string, string), GoError]
    
    fn pair(local/1/0: string) -> Result[(string, string), GoError] {
        def/1/0(text/0)
    }
    
    fn render(local/1/1: string) -> Result[string, GoError] {
        let (local/1/2, local/1/3) = def/1/1(text/1)?;
        Result::Ok(host/2 + "|" + port/3)
    }
    
    fn show(local/1/4: Result[string, GoError]) -> string {
        match res/4 {
            Result::Ok(local/1/5) => text/5,
            Result::Err(local/1/6) => "err=" + err/6.to_string(),
        }
    }
    
    fn main() -> unit {
        println(def/1/3(def/1/2("example.com:443")));
    }