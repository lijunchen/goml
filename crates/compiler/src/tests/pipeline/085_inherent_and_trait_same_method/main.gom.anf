fn inherent#Boxed#Boxed#format(self/0: Boxed) -> string {
  join ret2(retv3: string) -> string {
    retv3
  } in
  jump ret2("inherent")
}

fn trait_impl#Render#Boxed#format(self/1: Boxed) -> string {
  join ret4(retv5: string) -> string {
    retv5
  } in
  let t6 = Boxed.value(self/1) in
  let t7 = int32_to_string(t6) in
  jump ret4(t7)
}

fn main() -> unit {
  join ret8() -> unit {
    ()
  } in
  let t9 = Boxed { value: 9 } in
  let t10 = inherent#Boxed#Boxed#format(t9) in
  let _wild0 = println__T_string(t10) in
  let t11 = Boxed { value: 9 } in
  let t12 = trait_impl#Render#Boxed#format(t11) in
  let _wild1 = println__T_string(t12) in
  jump ret8()
}

fn println__T_string(value/1: string) -> unit {
  join ret13() -> unit {
    ()
  } in
  let t14 = string_println(value/1) in
  jump ret13()
}