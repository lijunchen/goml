package main
file main.gom
  fn cut_pair(local/1/0: bool) -> Option[(string, string)] {
        if ok/0 {
            Option::Some(("alpha", "beta"))
        } else {
            Option::None
        }
    }
    
    fn describe(local/1/1: bool) -> Option[string] {
        let (local/1/2, local/1/3) = def/1/0(ok/1)?;
        Option::Some(before/2 + "|" + after/3)
    }
    
    fn show(local/1/4: Option[string]) -> string {
        match opt/4 {
            Option::Some(local/1/5) => "some " + value/5,
            Option::None => "none",
        }
    }
    
    fn main() -> unit {
        println(def/1/2(def/1/1(true)));
        println(def/1/2(def/1/1(false)));
    }