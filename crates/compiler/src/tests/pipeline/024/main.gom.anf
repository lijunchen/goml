fn point32_to_string(p/0: Point) -> string {
  join ret15(retv16: string) -> string {
    retv16
  } in
  let mtmp4 = p/0 in
  let x5 = Point.x(mtmp4) in
  let x6 = Point.y(mtmp4) in
  let y/2 = x6 in
  let x/1 = x5 in
  let t17 = inherent#int32#int32#to_string(x/1) in
  let t18 = ("Point { x: " + t17) in
  let t19 = (t18 + ", y: ") in
  let t20 = inherent#int32#int32#to_string(y/2) in
  let t21 = (t19 + t20) in
  let t22 = (t21 + " }") in
  jump ret15(t22)
}

fn color_to_string(c/3: Color) -> string {
  join ret23(retv24: string) -> string {
    retv24
  } in
  join k25(jp26: string) -> string {
    jump ret23(jp26)
  } in
  match c/3 {
    Tag_0 => {
      jump k25("Red")
    },
    Tag_1 => {
      jump k25("Green")
    },
    Tag_2 => {
      jump k25("Blue")
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  join ret27(retv28: string) -> string {
    retv28
  } in
  let mtmp7 = l/4 in
  let x8 = Line.from(mtmp7) in
  let x9 = Line.to(mtmp7) in
  let x10 = Line.color(mtmp7) in
  let color/7 = x10 in
  let to/6 = x9 in
  let from/5 = x8 in
  let t29 = point32_to_string(from/5) in
  let t30 = ("Line { from: " + t29) in
  let t31 = (t30 + ", to: ") in
  let t32 = point32_to_string(to/6) in
  let t33 = (t31 + t32) in
  let t34 = (t33 + ", color: ") in
  let t35 = color_to_string(color/7) in
  let t36 = (t34 + t35) in
  let t37 = (t36 + " }") in
  jump ret27(t37)
}

fn point_type(p/8: Point) -> string {
  join ret38(retv39: string) -> string {
    retv39
  } in
  let x11 = Point.x(p/8) in
  let x12 = Point.y(p/8) in
  join k40(jp41: string) -> string {
    jump ret38(jp41)
  } in
  match x11 {
    0 => {
      join k42(jp43: string) -> string {
        jump k40(jp43)
      } in
      match x12 {
        0 => {
          jump k42("origin")
        },
        1 => {
          jump k42("up")
        },
        _ => {
          let y/9 = x12 in
          let mtmp13 = (0 < y/9) in
          join k44(jp45: string) -> string {
            jump k42(jp45)
          } in
          match mtmp13 {
            true => {
              jump k44("above")
            },
            false => {
              jump k44("below")
            },
          }
        },
      }
    },
    1 => {
      join k46(jp47: string) -> string {
        jump k40(jp47)
      } in
      match x12 {
        0 => {
          jump k46("right")
        },
        _ => {
          jump k46("unknown")
        },
      }
    },
    _ => {
      jump k40("unknown")
    },
  }
}

fn main() -> unit {
  join ret48() -> unit {
    ()
  } in
  let p0/10 = Point { x: 0, y: 0 } in
  let t49 = point_type(p0/10) in
  let mtmp14 = println__T_string(t49) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Tag_0 } in
  let t50 = line_to_string(line/12) in
  let t51 = println__T_string(t50) in
  jump ret48()
}

fn inherent#int32#int32#to_string(self/2: int32) -> string {
  join ret52(retv53: string) -> string {
    retv53
  } in
  let t54 = @runtime(core.int32_to_string)(self/2) in
  jump ret52(t54)
}

fn println__T_string(value/1: string) -> unit {
  join ret55() -> unit {
    ()
  } in
  let t56 = trait_impl#ToString#string#to_string(value/1) in
  let t57 = @runtime(core.string_println)(t56) in
  jump ret55()
}

fn trait_impl#ToString#string#to_string(self/9: string) -> string {
  join ret58(retv59: string) -> string {
    retv59
  } in
  jump ret58(self/9)
}