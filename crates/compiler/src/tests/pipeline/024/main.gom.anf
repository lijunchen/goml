fn point32_to_string(p/0: Point) -> string {
  join ret33(retv34: string) -> string {
    retv34
  } in
  let mtmp22 = p/0 in
  let x23 = Point.x(mtmp22) in
  let x24 = Point.y(mtmp22) in
  let y/2 = x24 in
  let x/1 = x23 in
  let t35 = inherent#int32#int32#to_string(x/1) in
  let t36 = ("Point { x: " + t35) in
  let t37 = (t36 + ", y: ") in
  let t38 = inherent#int32#int32#to_string(y/2) in
  let t39 = (t37 + t38) in
  let t40 = (t39 + " }") in
  jump ret33(t40)
}

fn color_to_string(c/3: Color) -> string {
  join ret41(retv42: string) -> string {
    retv42
  } in
  join k43(jp44: string) -> string {
    jump ret41(jp44)
  } in
  match c/3 {
    Tag_0 => {
      jump k43("Red")
    },
    Tag_1 => {
      jump k43("Green")
    },
    Tag_2 => {
      jump k43("Blue")
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  join ret45(retv46: string) -> string {
    retv46
  } in
  let mtmp25 = l/4 in
  let x26 = Line.from(mtmp25) in
  let x27 = Line.to(mtmp25) in
  let x28 = Line.color(mtmp25) in
  let color/7 = x28 in
  let to/6 = x27 in
  let from/5 = x26 in
  let t47 = point32_to_string(from/5) in
  let t48 = ("Line { from: " + t47) in
  let t49 = (t48 + ", to: ") in
  let t50 = point32_to_string(to/6) in
  let t51 = (t49 + t50) in
  let t52 = (t51 + ", color: ") in
  let t53 = color_to_string(color/7) in
  let t54 = (t52 + t53) in
  let t55 = (t54 + " }") in
  jump ret45(t55)
}

fn point_type(p/8: Point) -> string {
  join ret56(retv57: string) -> string {
    retv57
  } in
  let x29 = Point.x(p/8) in
  let x30 = Point.y(p/8) in
  join k58(jp59: string) -> string {
    jump ret56(jp59)
  } in
  match x29 {
    0 => {
      join k60(jp61: string) -> string {
        jump k58(jp61)
      } in
      match x30 {
        0 => {
          jump k60("origin")
        },
        1 => {
          jump k60("up")
        },
        _ => {
          let y/9 = x30 in
          let mtmp31 = (0 < y/9) in
          join k62(jp63: string) -> string {
            jump k60(jp63)
          } in
          match mtmp31 {
            true => {
              jump k62("above")
            },
            false => {
              jump k62("below")
            },
          }
        },
      }
    },
    1 => {
      join k64(jp65: string) -> string {
        jump k58(jp65)
      } in
      match x30 {
        0 => {
          jump k64("right")
        },
        _ => {
          jump k64("unknown")
        },
      }
    },
    _ => {
      jump k58("unknown")
    },
  }
}

fn main() -> unit {
  join ret66() -> unit {
    ()
  } in
  let p0/10 = Point { x: 0, y: 0 } in
  let t67 = point_type(p0/10) in
  let mtmp32 = println__T_string(t67) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Tag_0 } in
  let t68 = line_to_string(line/12) in
  let t69 = println__T_string(t68) in
  jump ret66()
}

fn inherent#int32#int32#to_string(self/2: int32) -> string {
  join ret70(retv71: string) -> string {
    retv71
  } in
  let t72 = @runtime(core.int32_to_string)(self/2) in
  jump ret70(t72)
}

fn println__T_string(value/1: string) -> unit {
  join ret73() -> unit {
    ()
  } in
  let t74 = trait_impl#ToString#string#to_string(value/1) in
  let t75 = @runtime(core.string_println)(t74) in
  jump ret73()
}

fn trait_impl#ToString#string#to_string(self/9: string) -> string {
  join ret76(retv77: string) -> string {
    retv77
  } in
  jump ret76(self/9)
}