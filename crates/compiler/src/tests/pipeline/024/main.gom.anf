fn point32_to_string(p/0: Point) -> string {
  join ret72(retv73: string) -> string {
    retv73
  } in
  let mtmp61 = p/0 in
  let x62 = Point.x(mtmp61) in
  let x63 = Point.y(mtmp61) in
  let y/2 = x63 in
  let x/1 = x62 in
  let t74 = inherent#int32#int32#to_string(x/1) in
  let t75 = ("Point { x: " + t74) in
  let t76 = (t75 + ", y: ") in
  let t77 = inherent#int32#int32#to_string(y/2) in
  let t78 = (t76 + t77) in
  let t79 = (t78 + " }") in
  jump ret72(t79)
}

fn color_to_string(c/3: Color) -> string {
  join ret80(retv81: string) -> string {
    retv81
  } in
  join k82(jp83: string) -> string {
    jump ret80(jp83)
  } in
  match c/3 {
    Tag_0 => {
      jump k82("Red")
    },
    Tag_1 => {
      jump k82("Green")
    },
    Tag_2 => {
      jump k82("Blue")
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  join ret84(retv85: string) -> string {
    retv85
  } in
  let mtmp64 = l/4 in
  let x65 = Line.from(mtmp64) in
  let x66 = Line.to(mtmp64) in
  let x67 = Line.color(mtmp64) in
  let color/7 = x67 in
  let to/6 = x66 in
  let from/5 = x65 in
  let t86 = point32_to_string(from/5) in
  let t87 = ("Line { from: " + t86) in
  let t88 = (t87 + ", to: ") in
  let t89 = point32_to_string(to/6) in
  let t90 = (t88 + t89) in
  let t91 = (t90 + ", color: ") in
  let t92 = color_to_string(color/7) in
  let t93 = (t91 + t92) in
  let t94 = (t93 + " }") in
  jump ret84(t94)
}

fn point_type(p/8: Point) -> string {
  join ret95(retv96: string) -> string {
    retv96
  } in
  let x68 = Point.x(p/8) in
  let x69 = Point.y(p/8) in
  join k97(jp98: string) -> string {
    jump ret95(jp98)
  } in
  match x68 {
    0 => {
      join k99(jp100: string) -> string {
        jump k97(jp100)
      } in
      match x69 {
        0 => {
          jump k99("origin")
        },
        1 => {
          jump k99("up")
        },
        _ => {
          let y/9 = x69 in
          let mtmp70 = (0 < y/9) in
          join k101(jp102: string) -> string {
            jump k99(jp102)
          } in
          match mtmp70 {
            true => {
              jump k101("above")
            },
            false => {
              jump k101("below")
            },
          }
        },
      }
    },
    1 => {
      join k103(jp104: string) -> string {
        jump k97(jp104)
      } in
      match x69 {
        0 => {
          jump k103("right")
        },
        _ => {
          jump k103("unknown")
        },
      }
    },
    _ => {
      jump k97("unknown")
    },
  }
}

fn main() -> unit {
  join ret105() -> unit {
    ()
  } in
  let p0/10 = Point { x: 0, y: 0 } in
  let t106 = point_type(p0/10) in
  let mtmp71 = println__T_string(t106) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Tag_0 } in
  let t107 = line_to_string(line/12) in
  let t108 = println__T_string(t107) in
  jump ret105()
}

fn inherent#int32#int32#to_string(self/5: int32) -> string {
  join ret109(retv110: string) -> string {
    retv110
  } in
  let t111 = @runtime(core.int32_to_string)(self/5) in
  jump ret109(t111)
}

fn println__T_string(value/1: string) -> unit {
  join ret112() -> unit {
    ()
  } in
  let t113 = trait_impl#ToString#string#to_string(value/1) in
  let t114 = @runtime(core.string_println)(t113) in
  jump ret112()
}

fn trait_impl#ToString#string#to_string(self/37: string) -> string {
  join ret115(retv116: string) -> string {
    retv116
  } in
  jump ret115(self/37)
}