fn point32_to_string(p/0: Point) -> string {
  join ret69(retv70: string) -> string {
    retv70
  } in
  let mtmp58 = p/0 in
  let x59 = Point.x(mtmp58) in
  let x60 = Point.y(mtmp58) in
  let y/2 = x60 in
  let x/1 = x59 in
  let t71 = inherent#int32#int32#to_string(x/1) in
  let t72 = ("Point { x: " + t71) in
  let t73 = (t72 + ", y: ") in
  let t74 = inherent#int32#int32#to_string(y/2) in
  let t75 = (t73 + t74) in
  let t76 = (t75 + " }") in
  jump ret69(t76)
}

fn color_to_string(c/3: Color) -> string {
  join ret77(retv78: string) -> string {
    retv78
  } in
  join k79(jp80: string) -> string {
    jump ret77(jp80)
  } in
  match c/3 {
    Tag_0 => {
      jump k79("Red")
    },
    Tag_1 => {
      jump k79("Green")
    },
    Tag_2 => {
      jump k79("Blue")
    },
  }
}

fn line_to_string(l/4: Line) -> string {
  join ret81(retv82: string) -> string {
    retv82
  } in
  let mtmp61 = l/4 in
  let x62 = Line.from(mtmp61) in
  let x63 = Line.to(mtmp61) in
  let x64 = Line.color(mtmp61) in
  let color/7 = x64 in
  let to/6 = x63 in
  let from/5 = x62 in
  let t83 = point32_to_string(from/5) in
  let t84 = ("Line { from: " + t83) in
  let t85 = (t84 + ", to: ") in
  let t86 = point32_to_string(to/6) in
  let t87 = (t85 + t86) in
  let t88 = (t87 + ", color: ") in
  let t89 = color_to_string(color/7) in
  let t90 = (t88 + t89) in
  let t91 = (t90 + " }") in
  jump ret81(t91)
}

fn point_type(p/8: Point) -> string {
  join ret92(retv93: string) -> string {
    retv93
  } in
  let x65 = Point.x(p/8) in
  let x66 = Point.y(p/8) in
  join k94(jp95: string) -> string {
    jump ret92(jp95)
  } in
  match x65 {
    0 => {
      join k96(jp97: string) -> string {
        jump k94(jp97)
      } in
      match x66 {
        0 => {
          jump k96("origin")
        },
        1 => {
          jump k96("up")
        },
        _ => {
          let y/9 = x66 in
          let mtmp67 = (0 < y/9) in
          join k98(jp99: string) -> string {
            jump k96(jp99)
          } in
          match mtmp67 {
            true => {
              jump k98("above")
            },
            false => {
              jump k98("below")
            },
          }
        },
      }
    },
    1 => {
      join k100(jp101: string) -> string {
        jump k94(jp101)
      } in
      match x66 {
        0 => {
          jump k100("right")
        },
        _ => {
          jump k100("unknown")
        },
      }
    },
    _ => {
      jump k94("unknown")
    },
  }
}

fn main() -> unit {
  join ret102() -> unit {
    ()
  } in
  let p0/10 = Point { x: 0, y: 0 } in
  let t103 = point_type(p0/10) in
  let mtmp68 = println__T_string(t103) in
  let p1/11 = Point { x: 10, y: 10 } in
  let line/12 = Line { from: p0/10, to: p1/11, color: Tag_0 } in
  let t104 = line_to_string(line/12) in
  let t105 = println__T_string(t104) in
  jump ret102()
}

fn inherent#int32#int32#to_string(self/2: int32) -> string {
  join ret106(retv107: string) -> string {
    retv107
  } in
  let t108 = @runtime(core.int32_to_string)(self/2) in
  jump ret106(t108)
}

fn println__T_string(value/1: string) -> unit {
  join ret109() -> unit {
    ()
  } in
  let t110 = trait_impl#ToString#string#to_string(value/1) in
  let t111 = @runtime(core.string_println)(t110) in
  jump ret109()
}

fn trait_impl#ToString#string#to_string(self/34: string) -> string {
  join ret112(retv113: string) -> string {
    retv113
  } in
  jump ret112(self/34)
}