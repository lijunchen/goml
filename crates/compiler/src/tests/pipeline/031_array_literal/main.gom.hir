fn make_array() -> [int32; 3] {
  [1, 2, 3]
}

fn main() -> () {
  let local_0 = make_array() in
  let local_1 = [4, 5, 6] in
  let _ = string_print("array literal") in
  let _ = local_0 in
  let _ = local_1 in
  ()
}