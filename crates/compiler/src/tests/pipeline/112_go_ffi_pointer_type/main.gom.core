fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn describe(path/0: string) -> string {
  {
    let mtmp0 = open_file(path/0) in
    match mtmp0 {
      Result::Ok(x1) => {
        {
          let x1 = Result::Ok._0(mtmp0) in
          {
            let file/1 = x1 in
            {
              let name/2 = file_name(file/1) in
              {
                let mtmp3 = close_file(file/1) in
                match mtmp3 {
                  Result::Ok(x4) => {
                    {
                      let x4 = Result::Ok._0(mtmp3) in
                      match x4 {
                        () => {
                          ("ok=" + name/2)
                        },
                      }
                    }
                  },
                  Result::Err(x5) => {
                    {
                      let x5 = Result::Err._0(mtmp3) in
                      {
                        let err/3 = x5 in
                        ("close_err=" + trait_call[ToString::to_string](err/3))
                      }
                    }
                  },
                }
              }
            }
          }
        }
      },
      Result::Err(x2) => {
        {
          let x2 = Result::Err._0(mtmp0) in
          {
            let err/4 = x2 in
            ("open_err=" + trait_call[ToString::to_string](err/4))
          }
        }
      },
    }
  }
}

fn main() -> unit {
  let _wild6 = println(describe("/etc/hosts")) in
  let _wild7 = println(describe("/definitely/missing")) in
}