fn trait_impl#TypeName#Point#type_name(self/0: Point) -> string {
  join ret7(retv8: string) -> string {
    retv8
  } in
  let mtmp0 = self/0 in
  let x1 = Point.x(mtmp0) in
  let x2 = Point.y(mtmp0) in
  let y/2 = x2 in
  let x/1 = x1 in
  let t9 = int32_to_string(x/1) in
  let prefix/3 = ("Point(" + t9) in
  let t10 = (prefix/3 + ", ") in
  let t11 = int32_to_string(y/2) in
  let t12 = (t10 + t11) in
  let t13 = (t12 + ")") in
  jump ret7(t13)
}

fn trait_impl#TypeName#Shape#type_name(self/4: Shape) -> string {
  join ret14(retv15: string) -> string {
    retv15
  } in
  join k16(jp17: string) -> string {
    jump ret14(jp17)
  } in
  match self/4 {
    Tag_0 => {
      jump k16("Unit")
    },
    Tag_1 => {
      let x3 = Shape::Location._0(self/4) in
      let point/5 = x3 in
      let t18 = trait_impl#TypeName#Point#type_name(point/5) in
      let t19 = ("Shape::" + t18) in
      jump k16(t19)
    },
  }
}

fn show_point(point/6: Point) -> string {
  join ret20(retv21: string) -> string {
    retv21
  } in
  let t22 = trait_impl#TypeName#Point#type_name(point/6) in
  jump ret20(t22)
}

fn show_shape(shape/7: Shape) -> string {
  join ret23(retv24: string) -> string {
    retv24
  } in
  let t25 = trait_impl#TypeName#Shape#type_name(shape/7) in
  jump ret23(t25)
}

fn main() -> unit {
  join ret26() -> unit {
    ()
  } in
  let point/8 = Point { x: 7, y: 9 } in
  let t27 = show_point(point/8) in
  let mtmp4 = string_println(t27) in
  let unit_shape/9 = Tag_0 in
  let t28 = show_shape(unit_shape/9) in
  let mtmp5 = string_println(t28) in
  let t29 = Point { x: 1, y: 2 } in
  let location_shape/10 = Shape::Location(t29) in
  let t30 = show_shape(location_shape/10) in
  let mtmp6 = string_println(t30) in
  jump ret26()
}