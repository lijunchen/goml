struct Point { x: int32, y: int32 }

enum Shape { Unit, Location(Point) }

trait TypeName {
  fn item_25() -> string
}

impl TypeName for Point {
  fn item_29(local_0: ()) -> string {
    let Point { x: local_1, y: local_2 } = local_0 in
    let local_3 = "Point(" + int32_to_string(local_1) in
    local_3 + ", " + int32_to_string(local_2) + ")"
  }
}

impl TypeName for Shape {
  fn item_30(local_0: ()) -> string {
    match local_0 {
      Shape::Unit => "Unit",
      Shape::Location(local_1) => "Shape::" + TypeName::type_name(local_1),
    }
  }
}

fn show_point(local_0: Point) -> string {
  TypeName::type_name(local_0)
}

fn show_shape(local_0: Shape) -> string {
  TypeName::type_name(local_0)
}

fn main() -> () {
  let local_0 = Point { x: 7, y: 9 } in
  let _ = string_println(show_point(local_0)) in
  let local_1 = Shape::Unit in
  let _ = string_println(show_shape(local_1)) in
  let local_2 = Shape::Location(Point { x: 1, y: 2 }) in
  let _ = string_println(show_shape(local_2)) in
  ()
}