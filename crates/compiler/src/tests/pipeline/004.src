enum Color {
    Red,
    Green,
    Blue,
}

fn main() -> bool {
    let a = (Blue, Blue) in
    match a {
        (Red, Green) => true,
        (Red, Red) => true,
        (Blue, Blue) => let _ = string_print(bool_to_string(true)) in false,
        _ => false,
    }
}
