impl ToString for S{
  fn to_string(self/0: S) -> string {
      "S(" + (int32_to_string : (int32) -> string)((self/0 : S).value) + ")";
  }
}

fn main() -> unit {
    let s/1: S = S { value: 7 };
    (println : (string) -> unit)((ToString::to_string : (S) -> string)((s/1 : S)));
    ();
}