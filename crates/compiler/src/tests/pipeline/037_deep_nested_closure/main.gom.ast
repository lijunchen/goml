fn main() {
    let a = 10 in
    let f1 = |x: int32| let b = 20 in
    let f2 = |y: int32| let c = 30 in
    let f3 = |z: int32| let d = 40 in
    let f4 = |w: int32| a + b + c + d + x + y + z + w in
    f4(4) in
    f3(3) in
    f2(2) in
    let result = f1(1) in
    string_println(int32_to_string(result))
}

