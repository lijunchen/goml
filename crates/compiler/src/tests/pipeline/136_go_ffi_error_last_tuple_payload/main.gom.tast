fn print(value/0: T) -> unit {
    (string_print : (string) -> unit)((ToString::to_string : (T) -> string)((value/0 : T)))
}

fn println(value/1: T) -> unit {
    (string_println : (string) -> unit)((ToString::to_string : (T) -> string)((value/1 : T)))
}

extern "go" "net" split_host_port(text: string) -> Result[(string, string), GoError]

fn render(text/0: string) -> Result[string, GoError] {
    let (host/1: string, port/2: string) = match (split_host_port : (string) -> Result[(string, string), GoError])((text/0 : string)) {
        Result::Ok(try_value/4: (string, string)) => (try_value/4 : (string, string)),
        Result::Err(try_residual/4: GoError) => return Result::Err((try_residual/4 : GoError)),
    };
    Result::Ok((host/1 : string) + "|" + (port/2 : string))
}

fn show(res/3: Result[string, GoError]) -> string {
    match (res/3 : Result[string, GoError]) {
        Result::Ok(text/4: string) => (text/4 : string),
        Result::Err(err/5: GoError) => (ToString::to_string : (GoError) -> string)((err/5 : GoError)),
    }
}

fn main() -> unit {
    (string_println : (string) -> unit)((show : (Result[string, GoError]) -> string)((render : (string) -> Result[string, GoError])("example.com:443")));
}