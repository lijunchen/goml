package main
file main.gom
  #[go_error_last]
extern "go" "net" "SplitHostPort" split_host_port(text: string) -> Result[(string, string), GoError]
    
    fn render(local/1/0: string) -> Result[string, GoError] {
        let (local/1/1, local/1/2) = def/1/0(text/0)?;
        Result::Ok(host/1 + "|" + port/2)
    }
    
    fn show(local/1/3: Result[string, GoError]) -> string {
        match res/3 {
            Result::Ok(local/1/4) => text/4,
            Result::Err(local/1/5) => err/5.to_string(),
        }
    }
    
    fn main() {
        string_println(def/1/2(def/1/1("example.com:443")));
    }