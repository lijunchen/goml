fn trait_impl#Display#Point#show(self/0: Point) -> string {
  "Point"
}

fn trait_impl#Display#Maybe[int32]#show(self/1: Maybe__int32) -> string {
  match self/1 {
    Tag_0 => {
      let x0 = Maybe__int32::Just._0(self/1) in
      let value/2 = x0 in
      let t5 = int32_to_string(value/2) in
      let t4 = ("Just(" + t5) in
      (t4 + ")")
    },
    Tag_1 => {
      "Nothing"
    },
  }
}

fn make_maybe(flag/3: bool) -> Maybe__int32 {
  if flag/3 {
    Maybe__int32::Just(42)
  } else {
    Tag_1
  }
}

fn main() -> unit {
  let point/4 = Point { x: 1, y: 2 } in
  let some_number/5 = make_maybe(true) in
  let none_number/6 = make_maybe(false) in
  let t6 = trait_impl#Display#Point#show(point/4) in
  let mtmp1 = string_println(t6) in
  let t7 = trait_impl#Display#Maybe[int32]#show(some_number/5) in
  let mtmp2 = string_println(t7) in
  let t8 = trait_impl#Display#Maybe[int32]#show(none_number/6) in
  let mtmp3 = string_println(t8) in
  ()
}