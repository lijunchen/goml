package main
file main.gom
  struct Pair[A, B] {
        first: A,
        second: B,
    }
    
    fn main() {
        let local/1/0: Pair[uint8, float32] = Pair {
            first: 10,
            second: 3.14
        };
        println(uint8_to_string(p/0.first));
        println(float32_to_string(p/0.second));
    }