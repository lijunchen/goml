package main
file main.gom
  trait Display {
      fn show(Self) -> string;
    }
    
    impl Display for int32 {
      fn show(local/1/0: int32) -> string {
          int32_to_string(self/0)
      }
    }
    
    fn mk_renderer(local/1/1: T) -> () -> string {
        let local/1/2: dyn Display = x/1;
        || Display::show(d/2)
    }
    
    fn main() {
        let local/1/3 = def/1/2(42);
        println(f/3());
    }