fn main() -> unit {
  join ret64() -> unit {
    ()
  } in
  let running/0 = inherent#Ref#Ref[T]#new__T_bool(true) in
  join exit65() -> unit {
    let _wild63 = () in
    let t66 = @runtime(core.string_println)("ok") in
    jump ret64()
  } in
  joinrec {
    join loop67() -> unit {
      let t68 = inherent#Ref#Ref[T]#get__T_bool(running/0) in
      if t68 {
        let event/1 = Tag_0 in
        join k69() -> unit {
          let _wild61 = () in
          let scanning/3 = inherent#Ref#Ref[T]#new__T_bool(false) in
          join exit70() -> unit {
            let _wild62 = () in
            let t71 = inherent#Ref#Ref[T]#set__T_bool(running/0, false) in
            jump loop67()
          } in
          joinrec {
            join loop72() -> unit {
              let t73 = inherent#Ref#Ref[T]#get__T_bool(scanning/3) in
              if t73 {
                jump loop72()
              } else {
                jump exit70()
              }
            } in
          } in
          jump loop72()
        } in
        match event/1 {
          Tag_0 => {
            let scanning/2 = inherent#Ref#Ref[T]#new__T_bool(false) in
            join exit74() -> unit {
              jump k69()
            } in
            joinrec {
              join loop75() -> unit {
                let t76 = inherent#Ref#Ref[T]#get__T_bool(scanning/2) in
                if t76 {
                  jump loop75()
                } else {
                  jump exit74()
                }
              } in
            } in
            jump loop75()
          },
          Tag_1 => {
            jump k69()
          },
          Tag_2 => {
            jump k69()
          },
          Tag_3 => {
            jump k69()
          },
        }
      } else {
        jump exit65()
      }
    } in
  } in
  jump loop67()
}

fn inherent#Ref#Ref[T]#new__T_bool(value/204: bool) -> Ref[bool] {
  join ret77(retv78: Ref[bool]) -> Ref[bool] {
    retv78
  } in
  let t79 = @intrinsic(ref.new)(value/204) in
  jump ret77(t79)
}

fn inherent#Ref#Ref[T]#get__T_bool(self/205: Ref[bool]) -> bool {
  join ret80(retv81: bool) -> bool {
    retv81
  } in
  let t82 = @intrinsic(ref.get)(self/205) in
  jump ret80(t82)
}

fn inherent#Ref#Ref[T]#set__T_bool(self/206: Ref[bool], value/207: bool) -> unit {
  join ret83() -> unit {
    ()
  } in
  let t84 = @intrinsic(ref.set)(self/206, value/207) in
  jump ret83()
}

fn trait_impl#Eq#bool#eq(self/51: bool, other/52: bool) -> bool {
  join ret85(retv86: bool) -> bool {
    retv86
  } in
  let t87 = @intrinsic(eq.primitive)(self/51, other/52) in
  jump ret85(t87)
}

fn trait_impl#Hash#bool#hash(self/78: bool) -> uint64 {
  join ret88(retv89: uint64) -> uint64 {
    retv89
  } in
  join k90(jp91: uint64) -> uint64 {
    jump ret88(jp91)
  } in
  if self/78 {
    jump k90(1)
  } else {
    jump k90(0)
  }
}