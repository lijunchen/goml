fn trait_impl#Display#Point#show(self/0: Point) -> string {
  (((("Point(" + int32_to_string(Point.x(self/0))) + ",") + int32_to_string(Point.y(self/0))) + ")")
}

fn trait_impl#Display#Flag#show(self/1: Flag) -> string {
  if Flag.value(self/1) {
      "Flag(true)"
  } else {
      "Flag(false)"
  }
}

fn main() -> unit {
  let p/2 = Point { x: 1, y: 2 } in
  let t/3 = Flag { value: true } in
  let dp/4 = to_dyn[Display]{Point}(p/2) in
  let dt/5 = to_dyn[Display]{Flag}(t/3) in
  let mtmp0 = string_println(dyn_call[Display::show](dp/4)) in
  let mtmp1 = string_println(dyn_call[Display::show](dt/5)) in
  ()
}