package main
file main.gom
  extern "go" "time" "Duration" type Duration
    
    #[go_error_last]
extern "go" "time" "ParseDuration" parse_duration(s: string) -> Result[Duration, GoError]
    
    extern "go" "fmt" "Sprintf" format_duration(format: string, value: Duration) -> string
    
    fn render_with_prefix(local/1/0: string, local/1/1: string) -> Result[string, GoError] {
        let local/1/3 = || {
            let local/1/2 = def/1/1(text/1)?;
            Result::Ok(prefix/0 + def/1/2("%v", value/2))
        };
        run/3()
    }
    
    fn show(local/1/4: Result[string, GoError]) -> string {
        match res/4 {
            Result::Ok(local/1/5) => value/5,
            Result::Err(local/1/6) => "err=" + err/6.to_string(),
        }
    }
    
    fn main() -> unit {
        println(def/1/4(def/1/3("seen=", "5s")));
        println(def/1/4(def/1/3("seen=", "bad")));
    }