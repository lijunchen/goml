package main
file main.gom
  #[go_option_last]
extern "go" "strings" "Cut" cut_pair(text: string, sep: string) -> Option[(string, string)]
    
    fn pair(local/1/0: string) -> Option[(string, string)] {
        def/1/0(text/0, ":")
    }
    
    fn describe(local/1/1: string) -> string {
        match def/1/1(text/1) {
            Option::Some((local/1/2, local/1/3)) => before/2 + "|" + after/3,
            Option::None => "missing",
        }
    }
    
    fn main() -> unit {
        println(def/1/2("alpha:beta"));
        println(def/1/2("plain"));
    }