fn pair(text/0: string) -> Option__(string,string) {
  join ret6(retv7: Option__(string,string)) -> Option__(string,string) {
    retv7
  } in
  let t8 = cut_pair(text/0, ":") in
  jump ret6(t8)
}

fn describe(text/1: string) -> string {
  join ret9(retv10: string) -> string {
    retv10
  } in
  let mtmp0 = pair(text/1) in
  join k11(jp12: string) -> string {
    jump ret9(jp12)
  } in
  match mtmp0 {
    Tag_0 => {
      jump k11("missing")
    },
    Tag_1 => {
      let x1 = Option__(string,string)::Some._0(mtmp0) in
      let x2 = x1.0 in
      let x3 = x1.1 in
      let after/3 = x3 in
      let before/2 = x2 in
      let t13 = (before/2 + "|") in
      let t14 = (t13 + after/3) in
      jump k11(t14)
    },
  }
}

fn main() -> unit {
  join ret15() -> unit {
    ()
  } in
  let t16 = describe("alpha:beta") in
  let _wild4 = println__T_string(t16) in
  let t17 = describe("plain") in
  let _wild5 = println__T_string(t17) in
  jump ret15()
}

fn println__T_string(value/1: string) -> unit {
  join ret18() -> unit {
    ()
  } in
  let t19 = string_println(value/1) in
  jump ret18()
}