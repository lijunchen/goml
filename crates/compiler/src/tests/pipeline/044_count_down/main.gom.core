fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn main() -> unit {
  let counter/0 = ref(0) in
  let _wild3 = go || => {
    while (ref_get(counter/0) < 10) {
        {
          let _wild0 = string_println("hello") in
          let _wild1 = sleep(duration(1000)) in
          let _wild2 = ref_set(counter/0, (ref_get(counter/0) + 1)) in
        }
    }
  } in
  while (ref_get(counter/0) < 10) {
      {
        ()
      }
  }
}