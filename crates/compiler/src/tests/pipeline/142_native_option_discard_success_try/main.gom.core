fn print(value/0: T) -> unit {
  string_print(trait_call[ToString::to_string](value/0))
}

fn println(value/1: T) -> unit {
  string_println(trait_call[ToString::to_string](value/1))
}

fn touch(text/0: string) -> Option[string] {
  let mtmp2 = {
    let mtmp0 = cut_pair(text/0, ":") in
    match mtmp0 {
      Option::None => {
        return Option::None
      },
      Option::Some(x1) => {
        {
          let x1 = Option::Some._0(mtmp0) in
          {
            let try_value/5 = x1 in
            try_value/5
          }
        }
      },
    }
  } in
  {
    Option::Some("ok")
  }
}

fn show(value/1: Option[string]) -> string {
  match value/1 {
    Option::None => {
      "none"
    },
    Option::Some(x3) => {
      {
        let x3 = Option::Some._0(value/1) in
        {
          let text/2 = x3 in
          text/2
        }
      }
    },
  }
}

fn main() -> unit {
  let _wild4 = println(show(touch("left:right"))) in
  let _wild5 = println(show(touch("plain"))) in
}