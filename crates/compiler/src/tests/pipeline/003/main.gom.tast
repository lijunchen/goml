fn main() -> unit {
    let _ : unit = (print : (unit) -> unit)(());
    let _ : unit = (print : (bool) -> unit)(true);
    let _ : unit = (print : (bool) -> unit)(false);
    let _ : unit = (print : (int32) -> unit)(123);
    ();
}