fn trait_impl#Display#int32#show(self/0: int32) -> string {
  int32_to_string(self/0)
}

fn trait_impl#Debug#int32#show(self/1: int32) -> string {
  let t5 = int32_to_string(self/1) in
  let t4 = ("i32(" + t5) in
  (t4 + ")")
}

fn trait_impl#Eq#int32#eq(self/2: int32, other/3: int32) -> bool {
  (self/2 == other/3)
}

fn trait_impl#Hash#int32#hash(self/4: int32) -> int32 {
  let t6 = (self/4 * 16777619) in
  (t6 + 216613626)
}

fn trait_impl#Add#int32#add(self/5: int32, other/6: int32) -> int32 {
  (self/5 + other/6)
}

fn trait_impl#Scale#int32#scale(self/7: int32, factor/8: int32) -> int32 {
  (self/7 * factor/8)
}

fn trait_impl#Inspect#int32#inspect(self/9: int32) -> string {
  let t8 = int32_to_string(self/9) in
  let t7 = ("<" + t8) in
  (t7 + ">")
}

fn trait_impl#Display#Boxed#show(self/10: Boxed) -> string {
  let t11 = Boxed.value(self/10) in
  let t10 = int32_to_string(t11) in
  let t9 = ("Boxed(" + t10) in
  (t9 + ")")
}

fn trait_impl#Debug#Boxed#show(self/11: Boxed) -> string {
  let t14 = Boxed.value(self/11) in
  let t13 = int32_to_string(t14) in
  let t12 = ("Boxed{value=" + t13) in
  (t12 + "}")
}

fn trait_impl#Eq#Boxed#eq(self/12: Boxed, other/13: Boxed) -> bool {
  let t15 = Boxed.value(self/12) in
  let t16 = Boxed.value(other/13) in
  (t15 == t16)
}

fn trait_impl#Hash#Boxed#hash(self/14: Boxed) -> int32 {
  let t19 = Boxed.value(self/14) in
  let t18 = (t19 * 31) in
  let t17 = (t18 + 7) in
  (t17 * 1315423911)
}

fn trait_impl#Add#Boxed#add(self/15: Boxed, other/16: Boxed) -> Boxed {
  let t21 = Boxed.value(self/15) in
  let t22 = Boxed.value(other/16) in
  let t20 = (t21 + t22) in
  Boxed { value: t20 }
}

fn trait_impl#Scale#Boxed#scale(self/17: Boxed, factor/18: int32) -> Boxed {
  let t24 = Boxed.value(self/17) in
  let t23 = (t24 * factor/18) in
  Boxed { value: t23 }
}

fn trait_impl#Inspect#Boxed#inspect(self/19: Boxed) -> string {
  let t27 = Boxed.value(self/19) in
  let t26 = int32_to_string(t27) in
  let t25 = ("[" + t26) in
  (t25 + "]")
}

fn bool_text(x/20: bool) -> string {
  if x/20 {
    "true"
  } else {
    "false"
  }
}

fn main() -> unit {
  let t28 = combine_and_render__T_int32(10, 32) in
  let mtmp0 = string_println(t28) in
  let t30 = Boxed { value: 3 } in
  let t31 = Boxed { value: 4 } in
  let t29 = combine_and_render__T_Boxed(t30, t31) in
  let mtmp1 = string_println(t29) in
  let t32 = sum_and_inspect__T_int32(1, 2, 3) in
  let mtmp2 = string_println(t32) in
  let t34 = Boxed { value: 5 } in
  let t35 = Boxed { value: 6 } in
  let t36 = Boxed { value: 7 } in
  let t33 = sum_and_inspect__T_Boxed(t34, t35, t36) in
  let mtmp3 = string_println(t33) in
  ()
}

fn combine_and_render__T_int32(a/21: int32, b/22: int32) -> string {
  let t37 = trait_impl#Add#int32#add(a/21, b/22) in
  let scaled/23 = trait_impl#Scale#int32#scale(t37, 2) in
  let same/24 = trait_impl#Eq#int32#eq(a/21, b/22) in
  let t38 = trait_impl#Hash#int32#hash(a/21) in
  let t39 = trait_impl#Hash#int32#hash(b/22) in
  let sum_hash/25 = (t38 + t39) in
  let dbg/26 = trait_impl#Debug#int32#show(scaled/23) in
  let disp/27 = trait_impl#Display#int32#show(scaled/23) in
  let t42 = (dbg/26 + " | ") in
  let t41 = (t42 + disp/27) in
  let t40 = (t41 + " | eq=") in
  let t44 = bool_text(same/24) in
  let t46 = int32_to_string(sum_hash/25) in
  let t45 = (" | hash=" + t46) in
  let t43 = (t44 + t45) in
  (t40 + t43)
}

fn combine_and_render__T_Boxed(a/21: Boxed, b/22: Boxed) -> string {
  let t47 = trait_impl#Add#Boxed#add(a/21, b/22) in
  let scaled/23 = trait_impl#Scale#Boxed#scale(t47, 2) in
  let same/24 = trait_impl#Eq#Boxed#eq(a/21, b/22) in
  let t48 = trait_impl#Hash#Boxed#hash(a/21) in
  let t49 = trait_impl#Hash#Boxed#hash(b/22) in
  let sum_hash/25 = (t48 + t49) in
  let dbg/26 = trait_impl#Debug#Boxed#show(scaled/23) in
  let disp/27 = trait_impl#Display#Boxed#show(scaled/23) in
  let t52 = (dbg/26 + " | ") in
  let t51 = (t52 + disp/27) in
  let t50 = (t51 + " | eq=") in
  let t54 = bool_text(same/24) in
  let t56 = int32_to_string(sum_hash/25) in
  let t55 = (" | hash=" + t56) in
  let t53 = (t54 + t55) in
  (t50 + t53)
}

fn sum_and_inspect__T_int32(x/28: int32, y/29: int32, z/30: int32) -> string {
  let t57 = trait_impl#Add#int32#add(x/28, y/29) in
  let total/31 = trait_impl#Add#int32#add(t57, z/30) in
  let h/32 = trait_impl#Hash#int32#hash(total/31) in
  let t59 = trait_impl#Inspect#int32#inspect(total/31) in
  let t58 = (t59 + " @") in
  let t60 = int32_to_string(h/32) in
  (t58 + t60)
}

fn sum_and_inspect__T_Boxed(x/28: Boxed, y/29: Boxed, z/30: Boxed) -> string {
  let t61 = trait_impl#Add#Boxed#add(x/28, y/29) in
  let total/31 = trait_impl#Add#Boxed#add(t61, z/30) in
  let h/32 = trait_impl#Hash#Boxed#hash(total/31) in
  let t63 = trait_impl#Inspect#Boxed#inspect(total/31) in
  let t62 = (t63 + " @") in
  let t64 = int32_to_string(h/32) in
  (t62 + t64)
}