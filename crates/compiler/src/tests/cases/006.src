fn main() {
    let a = (true, false) in
    let _ = match a {
        (true, b) => string_print(bool_to_string(b)),
        _ => (),
    } in
    let c = (true, true) in
    match c {
        (true, d) => string_print(bool_to_string(d)),
        _ => (),
    }
}
