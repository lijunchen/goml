fn match_string(s/0: String) -> Int {
  match s/0 {
    "hello" => {
      1
    },
    "world" => {
      2
    },
    _ => 3,
  }
}

fn wildcard_position(s/1: String) -> Int {
  4
}

fn repeated_string(s/2: String) -> Int {
  match s/2 {
    "hello" => {
      6
    },
    _ => 8,
  }
}

fn main() -> Unit {
  let t6 = match_string("hello") in
  let t5 = int_to_string(t6) in
  let mtmp0 = string_println(t5) in
  let t8 = match_string("planet") in
  let t7 = int_to_string(t8) in
  let mtmp1 = string_println(t7) in
  let t10 = wildcard_position("world") in
  let t9 = int_to_string(t10) in
  let mtmp2 = string_println(t9) in
  let t12 = wildcard_position("sun") in
  let t11 = int_to_string(t12) in
  let mtmp3 = string_println(t11) in
  let t14 = repeated_string("hello") in
  let t13 = int_to_string(t14) in
  let mtmp4 = string_println(t13) in
  let t16 = repeated_string("mars") in
  let t15 = int_to_string(t16) in
  string_println(t15)
}