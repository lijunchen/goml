use crate::common;
pub extern "go" "io" "Reader" type RawReader
pub extern "go" "" "int" type GoInt
pub extern "go" "bufio" "*Reader" type RawBufReader
pub extern "go" "bufio" "*Writer" type RawBufWriter
pub extern "go" "bufio" "*ReadWriter" type RawReadWriter
pub extern "go" "bufio" "*Scanner" type RawScanner
pub extern "go" "bufio" "SplitFunc" type RawSplitFunc
pub extern "go" "bytes" "*Buffer" type RawBytesBuffer

pub extern "go" "" "int" raw_go_int(value: int32) -> GoInt
pub extern "go" "" "int32" raw_int32(value: GoInt) -> int32
pub extern "go" "strings" "NewReader" raw_string_reader(text: string) -> RawReader
pub extern "go" "bufio" "NewReader" raw_new_reader(reader: RawReader) -> RawBufReader
pub extern "go" "bufio" "NewReaderSize" raw_new_reader_size(reader: RawReader, size: GoInt) -> RawBufReader
pub extern "go" "bufio" "NewWriter" raw_new_writer(buffer: RawBytesBuffer) -> RawBufWriter
pub extern "go" "bufio" "NewWriterSize" raw_new_writer_size(buffer: RawBytesBuffer, size: GoInt) -> RawBufWriter
pub extern "go" "bufio" "NewReadWriter" raw_new_read_writer(reader: RawBufReader, writer: RawBufWriter) -> RawReadWriter
pub extern "go" "bufio" "NewScanner" raw_new_scanner(reader: RawReader) -> RawScanner
pub extern "go" "bytes" "NewBufferString" raw_new_buffer(text: string) -> RawBytesBuffer
pub extern "go" "bytes" "(*Buffer).String" raw_buffer_string(buffer: RawBytesBuffer) -> string

#[go_value]
pub extern "go" "bufio" "ScanLines" raw_scan_lines() -> RawSplitFunc

#[go_value]
pub extern "go" "bufio" "ScanWords" raw_scan_words() -> RawSplitFunc

#[go_value]
pub extern "go" "bufio" "ScanBytes" raw_scan_bytes() -> RawSplitFunc

#[go_value]
pub extern "go" "bufio" "ScanRunes" raw_scan_runes() -> RawSplitFunc

#[go_value]
pub extern "go" "bufio" "ErrInvalidUnreadByte" raw_err_invalid_unread_byte() -> GoError

#[go_value]
pub extern "go" "bufio" "ErrTooLong" raw_err_too_long() -> GoError

#[go_value]
pub extern "go" "bufio" "ErrFinalToken" raw_err_final_token() -> GoError

#[go_value]
pub extern "go" "bufio" "MaxScanTokenSize" raw_max_scan_token_size() -> GoInt

pub extern "go" "bufio" "(*Reader).Buffered" raw_reader_buffered(reader: RawBufReader) -> GoInt
pub extern "go" "bufio" "(*Reader).Size" raw_reader_size(reader: RawBufReader) -> GoInt
pub extern "go" "bufio" "(*Reader).Reset" raw_reader_reset(reader: RawBufReader, next: RawReader) -> unit

#[go_error_last]
pub extern "go" "bufio" "(*Reader).Discard" raw_reader_discard(reader: RawBufReader, n: GoInt) -> Result[GoInt, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).Peek" raw_reader_peek(reader: RawBufReader, n: GoInt) -> Result[Vec[uint8], GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).Read" raw_reader_read(reader: RawBufReader, buf: Vec[uint8]) -> Result[GoInt, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadLine" raw_read_line(reader: RawBufReader) -> Result[(Vec[uint8], bool), GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadByte" raw_read_byte(reader: RawBufReader) -> Result[uint8, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadBytes" raw_read_bytes(reader: RawBufReader, delim: uint8) -> Result[Vec[uint8], GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadRune" raw_read_rune(reader: RawBufReader) -> Result[(char, GoInt), GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadSlice" raw_read_slice(reader: RawBufReader, delim: uint8) -> Result[Vec[uint8], GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).ReadString" raw_read_string(reader: RawBufReader, delim: uint8) -> Result[string, GoError]

#[go_error]
pub extern "go" "bufio" "(*Reader).UnreadByte" raw_reader_unread_byte(reader: RawBufReader) -> Result[unit, GoError]

#[go_error]
pub extern "go" "bufio" "(*Reader).UnreadRune" raw_reader_unread_rune(reader: RawBufReader) -> Result[unit, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Reader).WriteTo" raw_reader_write_to(reader: RawBufReader, buffer: RawBytesBuffer) -> Result[int64, GoError]

pub extern "go" "bufio" "(*Scanner).Buffer" raw_scanner_buffer(scanner: RawScanner, buf: Vec[uint8], max: GoInt) -> unit
pub extern "go" "bufio" "(*Scanner).Split" raw_scanner_split(scanner: RawScanner, split: RawSplitFunc) -> unit
pub extern "go" "bufio" "(*Scanner).Split" raw_scanner_split_custom(
    scanner: RawScanner,
    split: (Vec[uint8], bool) -> Result[(GoInt, Vec[uint8]), GoError],
) -> unit
pub extern "go" "bufio" "(*Scanner).Scan" raw_scanner_scan(scanner: RawScanner) -> bool
pub extern "go" "bufio" "(*Scanner).Bytes" raw_scanner_bytes(scanner: RawScanner) -> Vec[uint8]
pub extern "go" "bufio" "(*Scanner).Text" raw_scanner_text(scanner: RawScanner) -> string

#[go_error]
pub extern "go" "bufio" "(*Scanner).Err" raw_scanner_err(scanner: RawScanner) -> Result[unit, GoError]

pub extern "go" "bufio" "(*Writer).Available" raw_writer_available(writer: RawBufWriter) -> GoInt
pub extern "go" "bufio" "(*Writer).AvailableBuffer" raw_writer_available_buffer(writer: RawBufWriter) -> Vec[uint8]
pub extern "go" "bufio" "(*Writer).Buffered" raw_writer_buffered(writer: RawBufWriter) -> GoInt
pub extern "go" "bufio" "(*Writer).Reset" raw_writer_reset(writer: RawBufWriter, buffer: RawBytesBuffer) -> unit
pub extern "go" "bufio" "(*Writer).Size" raw_writer_size(writer: RawBufWriter) -> GoInt

#[go_error]
pub extern "go" "bufio" "(*Writer).Flush" raw_writer_flush(writer: RawBufWriter) -> Result[unit, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Writer).ReadFrom" raw_writer_read_from(writer: RawBufWriter, reader: RawReader) -> Result[int64, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Writer).Write" raw_writer_write(writer: RawBufWriter, data: Vec[uint8]) -> Result[GoInt, GoError]

#[go_error]
pub extern "go" "bufio" "(*Writer).WriteByte" raw_writer_write_byte(writer: RawBufWriter, value: uint8) -> Result[unit, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Writer).WriteRune" raw_writer_write_rune(writer: RawBufWriter, value: char) -> Result[GoInt, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*Writer).WriteString" raw_writer_write_string(writer: RawBufWriter, text: string) -> Result[GoInt, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*ReadWriter).ReadString" raw_read_writer_read_string(read_writer: RawReadWriter, delim: uint8) -> Result[string, GoError]

#[go_error_last]
pub extern "go" "bufio" "(*ReadWriter).WriteString" raw_read_writer_write_string(read_writer: RawReadWriter, text: string) -> Result[GoInt, GoError]

#[go_error]
pub extern "go" "bufio" "(*ReadWriter).Flush" raw_read_writer_flush(read_writer: RawReadWriter) -> Result[unit, GoError]

pub struct Reader {
    raw: RawBufReader,
}

pub struct Writer {
    raw: RawBufWriter,
    buffer: RawBytesBuffer,
}

pub struct ReadWriter {
    raw: RawReadWriter,
    reader: Reader,
    writer: Writer,
}

pub struct Scanner {
    raw: RawScanner,
}

pub fn from_text(text: string) -> Reader {
    Reader {
        raw: raw_new_reader(raw_string_reader(text)),
    }
}

pub fn from_text_size(text: string, size: int32) -> Reader {
    Reader {
        raw: raw_new_reader_size(raw_string_reader(text), raw_go_int(size)),
    }
}

pub fn reader_buffered(reader: Reader) -> int32 {
    raw_int32(raw_reader_buffered(reader.raw))
}

pub fn reader_size(reader: Reader) -> int32 {
    raw_int32(raw_reader_size(reader.raw))
}

pub fn reader_discard(reader: Reader, n: int32) -> Result[int32, GoError] {
    let discarded = raw_reader_discard(reader.raw, raw_go_int(n))?;
    Result::Ok(raw_int32(discarded))
}

pub fn peek_text(reader: Reader, n: int32) -> Result[string, GoError] {
    let data = raw_reader_peek(reader.raw, raw_go_int(n))?;
    Result::Ok(crate::common::string_of_bytes(data))
}

pub fn scanner_from_text(text: string) -> Scanner {
    Scanner {
        raw: raw_new_scanner(raw_string_reader(text)),
    }
}

pub fn writer() -> Writer {
    let buffer = raw_new_buffer("");
    Writer {
        raw: raw_new_writer(buffer),
        buffer: buffer,
    }
}

pub fn writer_size(size: int32) -> Writer {
    let buffer = raw_new_buffer("");
    Writer {
        raw: raw_new_writer_size(buffer, raw_go_int(size)),
        buffer: buffer,
    }
}

pub fn read_writer_from_text(text: string) -> ReadWriter {
    let reader = from_text(text);
    let writer = writer();
    ReadWriter {
        raw: raw_new_read_writer(reader.raw, writer.raw),
        reader: reader,
        writer: writer,
    }
}

pub fn read_line(reader: Reader) -> Result[(string, bool), GoError] {
    let (line, prefix) = raw_read_line(reader.raw)?;
    Result::Ok((crate::common::string_of_bytes(line), prefix))
}

pub fn read(reader: Reader, buf: Vec[uint8]) -> Result[(Vec[uint8], int32), GoError] {
    let count = raw_reader_read(reader.raw, buf)?;
    Result::Ok((buf, raw_int32(count)))
}

pub fn read_byte(reader: Reader) -> Result[uint8, GoError] {
    raw_read_byte(reader.raw)
}

pub fn read_bytes_text(reader: Reader, delim: uint8) -> Result[string, GoError] {
    let data = raw_read_bytes(reader.raw, delim)?;
    Result::Ok(crate::common::string_of_bytes(data))
}

pub fn read_rune(reader: Reader) -> Result[(char, int32), GoError] {
    let (value, size) = raw_read_rune(reader.raw)?;
    Result::Ok((value, raw_int32(size)))
}

pub fn read_slice_text(reader: Reader, delim: uint8) -> Result[string, GoError] {
    let data = raw_read_slice(reader.raw, delim)?;
    Result::Ok(crate::common::string_of_bytes(data))
}

pub fn read_string(reader: Reader, delim: uint8) -> Result[string, GoError] {
    raw_read_string(reader.raw, delim)
}

pub fn unread_byte(reader: Reader) -> Result[unit, GoError] {
    raw_reader_unread_byte(reader.raw)
}

pub fn unread_rune(reader: Reader) -> Result[unit, GoError] {
    raw_reader_unread_rune(reader.raw)
}

pub fn reset_text(reader: Reader, text: string) -> unit {
    raw_reader_reset(reader.raw, raw_string_reader(text))
}

pub fn write_to_text(reader: Reader) -> Result[(int64, string), GoError] {
    let buffer = raw_new_buffer("");
    let count = raw_reader_write_to(reader.raw, buffer)?;
    Result::Ok((count, raw_buffer_string(buffer)))
}

pub fn split_lines(scanner: Scanner) -> unit {
    raw_scanner_split(scanner.raw, raw_scan_lines())
}

pub fn split_words(scanner: Scanner) -> unit {
    raw_scanner_split(scanner.raw, raw_scan_words())
}

pub fn split_bytes(scanner: Scanner) -> unit {
    raw_scanner_split(scanner.raw, raw_scan_bytes())
}

pub fn split_runes(scanner: Scanner) -> unit {
    raw_scanner_split(scanner.raw, raw_scan_runes())
}

pub fn split_custom(
    scanner: Scanner,
    split: (Vec[uint8], bool) -> Result[(int32, Vec[uint8]), GoError],
) -> unit {
    raw_scanner_split_custom(scanner.raw, |data, at_eof| {
        let (advance, token) = split(data, at_eof)?;
        Result::Ok((raw_go_int(advance), token))
    })
}

pub fn buffer(scanner: Scanner, buf: Vec[uint8], max: int32) -> unit {
    raw_scanner_buffer(scanner.raw, buf, raw_go_int(max))
}

pub fn scan(scanner: Scanner) -> bool {
    raw_scanner_scan(scanner.raw)
}

pub fn bytes_text(scanner: Scanner) -> string {
    crate::common::string_of_bytes(raw_scanner_bytes(scanner.raw))
}

pub fn text(scanner: Scanner) -> string {
    raw_scanner_text(scanner.raw)
}

pub fn err(scanner: Scanner) -> Result[unit, GoError] {
    raw_scanner_err(scanner.raw)
}

pub fn scan_all_csv(scanner: Scanner) -> Result[string, GoError] {
    let acc = ref("");
    let first = ref(true);
    while scan(scanner) {
        let part = text(scanner);
        let next = if ref_get(first) {
            part
        } else {
            ref_get(acc) + "," + part
        };
        ref_set(acc, next);
        ref_set(first, false);
    };
    err(scanner)?;
    Result::Ok(ref_get(acc))
}

pub fn scan_words_csv(text: string) -> Result[string, GoError] {
    let scanner = scanner_from_text(text);
    split_words(scanner);
    scan_all_csv(scanner)
}

pub fn write_text(writer: Writer, text: string) -> Result[int32, GoError] {
    let count = raw_writer_write_string(writer.raw, text)?;
    Result::Ok(raw_int32(count))
}

pub fn write_bytes(writer: Writer, data: Vec[uint8]) -> Result[int32, GoError] {
    let count = raw_writer_write(writer.raw, data)?;
    Result::Ok(raw_int32(count))
}

pub fn write_byte(writer: Writer, value: uint8) -> Result[unit, GoError] {
    raw_writer_write_byte(writer.raw, value)
}

pub fn write_rune(writer: Writer, value: char) -> Result[int32, GoError] {
    let count = raw_writer_write_rune(writer.raw, value)?;
    Result::Ok(raw_int32(count))
}

pub fn flush(writer: Writer) -> Result[unit, GoError] {
    raw_writer_flush(writer.raw)
}

pub fn available(writer: Writer) -> int32 {
    raw_int32(raw_writer_available(writer.raw))
}

pub fn available_buffer(writer: Writer) -> Vec[uint8] {
    raw_writer_available_buffer(writer.raw)
}

pub fn buffered(writer: Writer) -> int32 {
    raw_int32(raw_writer_buffered(writer.raw))
}

pub fn writer_size_value(writer: Writer) -> int32 {
    raw_int32(raw_writer_size(writer.raw))
}

pub fn contents(writer: Writer) -> string {
    raw_buffer_string(writer.buffer)
}

pub fn read_from_text(writer: Writer, text: string) -> Result[int64, GoError] {
    raw_writer_read_from(writer.raw, raw_string_reader(text))
}

pub fn reset(writer: Writer) -> Writer {
    let buffer = raw_new_buffer("");
    raw_writer_reset(writer.raw, buffer);
    Writer {
        raw: writer.raw,
        buffer: buffer,
    }
}

pub fn read_writer_read_string(read_writer: ReadWriter, delim: uint8) -> Result[string, GoError] {
    raw_read_writer_read_string(read_writer.raw, delim)
}

pub fn read_writer_read_line(read_writer: ReadWriter) -> Result[(string, bool), GoError] {
    read_line(read_writer.reader)
}

pub fn read_writer_peek_text(read_writer: ReadWriter, n: int32) -> Result[string, GoError] {
    peek_text(read_writer.reader, n)
}

pub fn read_writer_discard(read_writer: ReadWriter, n: int32) -> Result[int32, GoError] {
    reader_discard(read_writer.reader, n)
}

pub fn read_writer_read(read_writer: ReadWriter, buf: Vec[uint8]) -> Result[(Vec[uint8], int32), GoError] {
    read(read_writer.reader, buf)
}

pub fn read_writer_read_byte(read_writer: ReadWriter) -> Result[uint8, GoError] {
    read_byte(read_writer.reader)
}

pub fn read_writer_read_bytes_text(read_writer: ReadWriter, delim: uint8) -> Result[string, GoError] {
    read_bytes_text(read_writer.reader, delim)
}

pub fn read_writer_read_rune(read_writer: ReadWriter) -> Result[(char, int32), GoError] {
    read_rune(read_writer.reader)
}

pub fn read_writer_read_slice_text(read_writer: ReadWriter, delim: uint8) -> Result[string, GoError] {
    read_slice_text(read_writer.reader, delim)
}

pub fn read_writer_unread_byte(read_writer: ReadWriter) -> Result[unit, GoError] {
    unread_byte(read_writer.reader)
}

pub fn read_writer_unread_rune(read_writer: ReadWriter) -> Result[unit, GoError] {
    unread_rune(read_writer.reader)
}

pub fn read_writer_reader_buffered(read_writer: ReadWriter) -> int32 {
    reader_buffered(read_writer.reader)
}

pub fn read_writer_reader_size(read_writer: ReadWriter) -> int32 {
    reader_size(read_writer.reader)
}

pub fn read_writer_reader_reset_text(read_writer: ReadWriter, text: string) -> ReadWriter {
    reset_text(read_writer.reader, text);
    read_writer
}

pub fn read_writer_write_to_text(read_writer: ReadWriter) -> Result[(int64, string), GoError] {
    write_to_text(read_writer.reader)
}

pub fn read_writer_write_text(read_writer: ReadWriter, text: string) -> Result[int32, GoError] {
    let count = raw_read_writer_write_string(read_writer.raw, text)?;
    Result::Ok(raw_int32(count))
}

pub fn read_writer_write_bytes(read_writer: ReadWriter, data: Vec[uint8]) -> Result[int32, GoError] {
    write_bytes(read_writer.writer, data)
}

pub fn read_writer_write_byte(read_writer: ReadWriter, value: uint8) -> Result[unit, GoError] {
    write_byte(read_writer.writer, value)
}

pub fn read_writer_write_rune(read_writer: ReadWriter, value: char) -> Result[int32, GoError] {
    write_rune(read_writer.writer, value)
}

pub fn read_writer_flush(read_writer: ReadWriter) -> Result[unit, GoError] {
    raw_read_writer_flush(read_writer.raw)
}

pub fn read_writer_writer_buffered(read_writer: ReadWriter) -> int32 {
    buffered(read_writer.writer)
}

pub fn read_writer_writer_size(read_writer: ReadWriter) -> int32 {
    writer_size_value(read_writer.writer)
}

pub fn read_writer_writer_reset(read_writer: ReadWriter) -> ReadWriter {
    let writer = reset(read_writer.writer);
    ReadWriter {
        raw: read_writer.raw,
        reader: read_writer.reader,
        writer: writer,
    }
}

pub fn read_writer_available(read_writer: ReadWriter) -> int32 {
    available(read_writer.writer)
}

pub fn read_writer_available_buffer(read_writer: ReadWriter) -> Vec[uint8] {
    available_buffer(read_writer.writer)
}

pub fn read_writer_contents(read_writer: ReadWriter) -> string {
    contents(read_writer.writer)
}

pub fn read_writer_read_from_text(read_writer: ReadWriter, text: string) -> Result[int64, GoError] {
    read_from_text(read_writer.writer, text)
}

pub fn max_scan_token_size() -> int32 {
    raw_int32(raw_max_scan_token_size())
}

pub fn err_invalid_unread_byte() -> GoError {
    raw_err_invalid_unread_byte()
}

pub fn err_too_long() -> GoError {
    raw_err_too_long()
}

pub fn err_final_token() -> GoError {
    raw_err_final_token()
}

impl Reader {
    fn buffered(self: Reader) -> int32 {
        reader_buffered(self)
    }

    fn size(self: Reader) -> int32 {
        reader_size(self)
    }

    fn discard(self: Reader, n: int32) -> Result[int32, GoError] {
        reader_discard(self, n)
    }

    fn peek_text(self: Reader, n: int32) -> Result[string, GoError] {
        peek_text(self, n)
    }

    fn read_line(self: Reader) -> Result[(string, bool), GoError] {
        read_line(self)
    }

    fn read(self: Reader, buf: Vec[uint8]) -> Result[(Vec[uint8], int32), GoError] {
        read(self, buf)
    }

    fn read_byte(self: Reader) -> Result[uint8, GoError] {
        read_byte(self)
    }

    fn read_bytes_text(self: Reader, delim: uint8) -> Result[string, GoError] {
        read_bytes_text(self, delim)
    }

    fn read_rune(self: Reader) -> Result[(char, int32), GoError] {
        read_rune(self)
    }

    fn read_slice_text(self: Reader, delim: uint8) -> Result[string, GoError] {
        read_slice_text(self, delim)
    }

    fn read_string(self: Reader, delim: uint8) -> Result[string, GoError] {
        read_string(self, delim)
    }

    fn unread_byte(self: Reader) -> Result[unit, GoError] {
        unread_byte(self)
    }

    fn unread_rune(self: Reader) -> Result[unit, GoError] {
        unread_rune(self)
    }

    fn reset_text(self: Reader, text: string) -> unit {
        reset_text(self, text)
    }

    fn write_to_text(self: Reader) -> Result[(int64, string), GoError] {
        write_to_text(self)
    }
}

impl Writer {
    fn write_text(self: Writer, text: string) -> Result[int32, GoError] {
        write_text(self, text)
    }

    fn write_bytes(self: Writer, data: Vec[uint8]) -> Result[int32, GoError] {
        write_bytes(self, data)
    }

    fn write_byte(self: Writer, value: uint8) -> Result[unit, GoError] {
        write_byte(self, value)
    }

    fn write_rune(self: Writer, value: char) -> Result[int32, GoError] {
        write_rune(self, value)
    }

    fn flush(self: Writer) -> Result[unit, GoError] {
        flush(self)
    }

    fn available(self: Writer) -> int32 {
        available(self)
    }

    fn available_buffer(self: Writer) -> Vec[uint8] {
        available_buffer(self)
    }

    fn buffered(self: Writer) -> int32 {
        buffered(self)
    }

    fn size(self: Writer) -> int32 {
        writer_size_value(self)
    }

    fn contents(self: Writer) -> string {
        contents(self)
    }

    fn read_from_text(self: Writer, text: string) -> Result[int64, GoError] {
        read_from_text(self, text)
    }

    fn reset(self: Writer) -> Writer {
        reset(self)
    }
}

impl ReadWriter {
    fn read_line(self: ReadWriter) -> Result[(string, bool), GoError] {
        read_writer_read_line(self)
    }

    fn peek_text(self: ReadWriter, n: int32) -> Result[string, GoError] {
        read_writer_peek_text(self, n)
    }

    fn discard(self: ReadWriter, n: int32) -> Result[int32, GoError] {
        read_writer_discard(self, n)
    }

    fn read(self: ReadWriter, buf: Vec[uint8]) -> Result[(Vec[uint8], int32), GoError] {
        read_writer_read(self, buf)
    }

    fn read_string(self: ReadWriter, delim: uint8) -> Result[string, GoError] {
        read_writer_read_string(self, delim)
    }

    fn read_byte(self: ReadWriter) -> Result[uint8, GoError] {
        read_writer_read_byte(self)
    }

    fn read_bytes_text(self: ReadWriter, delim: uint8) -> Result[string, GoError] {
        read_writer_read_bytes_text(self, delim)
    }

    fn read_rune(self: ReadWriter) -> Result[(char, int32), GoError] {
        read_writer_read_rune(self)
    }

    fn read_slice_text(self: ReadWriter, delim: uint8) -> Result[string, GoError] {
        read_writer_read_slice_text(self, delim)
    }

    fn unread_byte(self: ReadWriter) -> Result[unit, GoError] {
        read_writer_unread_byte(self)
    }

    fn unread_rune(self: ReadWriter) -> Result[unit, GoError] {
        read_writer_unread_rune(self)
    }

    fn reader_buffered(self: ReadWriter) -> int32 {
        read_writer_reader_buffered(self)
    }

    fn reader_size(self: ReadWriter) -> int32 {
        read_writer_reader_size(self)
    }

    fn reader_reset_text(self: ReadWriter, text: string) -> ReadWriter {
        read_writer_reader_reset_text(self, text)
    }

    fn write_to_text(self: ReadWriter) -> Result[(int64, string), GoError] {
        read_writer_write_to_text(self)
    }

    fn write_text(self: ReadWriter, text: string) -> Result[int32, GoError] {
        read_writer_write_text(self, text)
    }

    fn write_bytes(self: ReadWriter, data: Vec[uint8]) -> Result[int32, GoError] {
        read_writer_write_bytes(self, data)
    }

    fn write_byte(self: ReadWriter, value: uint8) -> Result[unit, GoError] {
        read_writer_write_byte(self, value)
    }

    fn write_rune(self: ReadWriter, value: char) -> Result[int32, GoError] {
        read_writer_write_rune(self, value)
    }

    fn flush(self: ReadWriter) -> Result[unit, GoError] {
        read_writer_flush(self)
    }

    fn writer_buffered(self: ReadWriter) -> int32 {
        read_writer_writer_buffered(self)
    }

    fn writer_size(self: ReadWriter) -> int32 {
        read_writer_writer_size(self)
    }

    fn writer_reset(self: ReadWriter) -> ReadWriter {
        read_writer_writer_reset(self)
    }

    fn available(self: ReadWriter) -> int32 {
        read_writer_available(self)
    }

    fn available_buffer(self: ReadWriter) -> Vec[uint8] {
        read_writer_available_buffer(self)
    }

    fn contents(self: ReadWriter) -> string {
        read_writer_contents(self)
    }

    fn read_from_text(self: ReadWriter, text: string) -> Result[int64, GoError] {
        read_writer_read_from_text(self, text)
    }
}

impl Scanner {
    fn buffer(self: Scanner, buf: Vec[uint8], max: int32) -> unit {
        buffer(self, buf, max)
    }

    fn split_lines(self: Scanner) -> unit {
        split_lines(self)
    }

    fn split_words(self: Scanner) -> unit {
        split_words(self)
    }

    fn split_bytes(self: Scanner) -> unit {
        split_bytes(self)
    }

    fn split_runes(self: Scanner) -> unit {
        split_runes(self)
    }

    fn split_custom(
        self: Scanner,
        split: (Vec[uint8], bool) -> Result[(int32, Vec[uint8]), GoError],
    ) -> unit {
        split_custom(self, split)
    }

    fn scan(self: Scanner) -> bool {
        scan(self)
    }

    fn bytes_text(self: Scanner) -> string {
        bytes_text(self)
    }

    fn text(self: Scanner) -> string {
        text(self)
    }

    fn err(self: Scanner) -> Result[unit, GoError] {
        err(self)
    }

    fn scan_all_csv(self: Scanner) -> Result[string, GoError] {
        scan_all_csv(self)
    }
}
