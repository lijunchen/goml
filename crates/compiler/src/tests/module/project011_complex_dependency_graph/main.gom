package main;

use project011_complex_dependency_graph::math;
use project011_complex_dependency_graph::pipeline;

fn main() {
    println(pipeline::run(4));
    println(math::square_plus_seed(3).to_string());
}
