package main;

use tests::struct_like_enum_variant::events;

fn describe(value: events::Event) -> int32 {
    match value {
        events::Event::Move { x, y } => x + y,
        events::Event::Stop => 0,
    }
}

fn main() -> unit {
    println(describe(events::moved(20, 22)));
}
