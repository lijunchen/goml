package main;
use datapkg;
use usepkg;

fn main() -> unit {
    let x = datapkg::Card { value: 3 };
    println(usepkg::render_method(x));
    let y = datapkg::Card { value: 3 };
    println(usepkg::render_fancy(y));
}
