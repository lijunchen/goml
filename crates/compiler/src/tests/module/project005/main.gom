package main;

use project005::geo;
use project005::shape;

fn main() {
    let p = shape::make(3, 4);
    let q = geo::move(p, 1, 2);
    let total = geo::squared(q) + shape::sum(q);
    println(total);
}
