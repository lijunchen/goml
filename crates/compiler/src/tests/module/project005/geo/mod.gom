use crate::shape;
pub fn squared(p: crate::shape::Point) -> int32 {
    let crate::shape::Point { x: x, y: y } = p;
    x * x + y * y
}

pub fn move(p: crate::shape::Point, dx: int32, dy: int32) -> crate::shape::Point {
    crate::shape::Point { x: p.x + dx, y: p.y + dy }
}
