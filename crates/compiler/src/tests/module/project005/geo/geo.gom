package geo;

use project005::shape;

pub fn squared(p: shape::Point) -> int32 {
    let shape::Point { x: x, y: y } = p;
    x * x + y * y
}

pub fn move(p: shape::Point, dx: int32, dy: int32) -> shape::Point {
    shape::Point { x: p.x + dx, y: p.y + dy }
}
